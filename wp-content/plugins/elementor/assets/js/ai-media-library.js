(function(react, _wordpress_i18n, _elementor_ui, _elementor_icons, _elementor_ui_TextField) {

//#region \0rolldown/runtime.js
	var __create = Object.create;
	var __defProp = Object.defineProperty;
	var __name = (target, value) => __defProp(target, "name", {
		value,
		configurable: true
	});
	var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
	var __getOwnPropNames = Object.getOwnPropertyNames;
	var __getProtoOf = Object.getPrototypeOf;
	var __hasOwnProp = Object.prototype.hasOwnProperty;
	var __esmMin = (fn, res, err) => () => {
		if (err) throw err[0];
		try {
			return fn && (res = fn(fn = 0)), res;
		} catch (e) {
			throw err = [e], e;
		}
	};
	var __commonJSMin = (cb, mod) => () => (mod || (cb((mod = { exports: {} }).exports, mod), cb = null), mod.exports);
	var __exportAll = (all, no_symbols) => {
		let target = {};
		for (var name in all) {
			__defProp(target, name, {
				get: all[name],
				enumerable: true
			});
		}
		if (!no_symbols) {
			__defProp(target, Symbol.toStringTag, { value: "Module" });
		}
		return target;
	};
	var __copyProps = (to, from, except, desc) => {
		if (from && typeof from === "object" || typeof from === "function") {
			for (var keys = __getOwnPropNames(from), i = 0, n = keys.length, key; i < n; i++) {
				key = keys[i];
				if (!__hasOwnProp.call(to, key) && key !== except) {
					__defProp(to, key, {
						get: ((k) => from[k]).bind(null, key),
						enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable
					});
				}
			}
		}
		return to;
	};
	var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", {
		value: mod,
		enumerable: true
	}) : target, mod));

//#endregion
let react$1 = __toESM(react, 1);
react = __toESM(react);
_elementor_ui_TextField = __toESM(_elementor_ui_TextField);

//#region node_modules/@babel/runtime/helpers/esm/arrayWithHoles.js
	function _arrayWithHoles(r) {
		if (Array.isArray(r)) return r;
	}
	var init_arrayWithHoles = __esmMin((() => {}));

//#endregion
//#region node_modules/@babel/runtime/helpers/esm/iterableToArrayLimit.js
	function _iterableToArrayLimit(r, l) {
		var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"];
		if (null != t) {
			var e;
			var n;
			var i;
			var u;
			var a = [];
			var f = !0;
			var o = !1;
			try {
				if (i = (t = t.call(r)).next, 0 === l) {
					if (Object(t) !== t) return;
					f = !1;
				} else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0);
			} catch (r) {
				o = !0, n = r;
			} finally {
				try {
					if (!f && null != t["return"] && (u = t["return"](), Object(u) !== u)) return;
				} finally {
					if (o) throw n;
				}
			}
			return a;
		}
	}
	var init_iterableToArrayLimit = __esmMin((() => {}));

//#endregion
//#region node_modules/@babel/runtime/helpers/esm/arrayLikeToArray.js
	function _arrayLikeToArray$2(r, a) {
		(null == a || a > r.length) && (a = r.length);
		for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e];
		return n;
	}
	var init_arrayLikeToArray = __esmMin((() => {
		__name(_arrayLikeToArray$2, "_arrayLikeToArray");
	}));

//#endregion
//#region node_modules/@babel/runtime/helpers/esm/unsupportedIterableToArray.js
	function _unsupportedIterableToArray$2(r, a) {
		if (r) {
			if ("string" == typeof r) return _arrayLikeToArray$2(r, a);
			var t = {}.toString.call(r).slice(8, -1);
			return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray$2(r, a) : void 0;
		}
	}
	var init_unsupportedIterableToArray = __esmMin((() => {
		init_arrayLikeToArray();
		__name(_unsupportedIterableToArray$2, "_unsupportedIterableToArray");
	}));

//#endregion
//#region node_modules/@babel/runtime/helpers/esm/nonIterableRest.js
	function _nonIterableRest() {
		throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
	}
	var init_nonIterableRest = __esmMin((() => {}));

//#endregion
//#region node_modules/@babel/runtime/helpers/esm/slicedToArray.js
	function _slicedToArray(r, e) {
		return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray$2(r, e) || _nonIterableRest();
	}
	var init_slicedToArray = __esmMin((() => {
		init_arrayWithHoles();
		init_iterableToArrayLimit();
		init_unsupportedIterableToArray();
		init_nonIterableRest();
	}));

//#endregion
//#region node_modules/@babel/runtime/helpers/esm/taggedTemplateLiteral.js
init_slicedToArray();
	function _taggedTemplateLiteral(e, t) {
		return t || (t = e.slice(0)), Object.freeze(Object.defineProperties(e, { raw: { value: Object.freeze(t) } }));
	}

//#endregion
//#region node_modules/prop-types/node_modules/react-is/cjs/react-is.development.js
/** @license React v16.13.1
	* react-is.development.js
	*
	* Copyright (c) Facebook, Inc. and its affiliates.
	*
	* This source code is licensed under the MIT license found in the
	* LICENSE file in the root directory of this source tree.
	*/
	var require_react_is_development$1 = /* @__PURE__ */ __commonJSMin(((exports) => {
		(function() {
			"use strict";
			var hasSymbol = typeof Symbol === "function" && Symbol.for;
			var REACT_ELEMENT_TYPE = hasSymbol ? Symbol.for("react.element") : 60103;
			var REACT_PORTAL_TYPE = hasSymbol ? Symbol.for("react.portal") : 60106;
			var REACT_FRAGMENT_TYPE = hasSymbol ? Symbol.for("react.fragment") : 60107;
			var REACT_STRICT_MODE_TYPE = hasSymbol ? Symbol.for("react.strict_mode") : 60108;
			var REACT_PROFILER_TYPE = hasSymbol ? Symbol.for("react.profiler") : 60114;
			var REACT_PROVIDER_TYPE = hasSymbol ? Symbol.for("react.provider") : 60109;
			var REACT_CONTEXT_TYPE = hasSymbol ? Symbol.for("react.context") : 60110;
			var REACT_ASYNC_MODE_TYPE = hasSymbol ? Symbol.for("react.async_mode") : 60111;
			var REACT_CONCURRENT_MODE_TYPE = hasSymbol ? Symbol.for("react.concurrent_mode") : 60111;
			var REACT_FORWARD_REF_TYPE = hasSymbol ? Symbol.for("react.forward_ref") : 60112;
			var REACT_SUSPENSE_TYPE = hasSymbol ? Symbol.for("react.suspense") : 60113;
			var REACT_SUSPENSE_LIST_TYPE = hasSymbol ? Symbol.for("react.suspense_list") : 60120;
			var REACT_MEMO_TYPE = hasSymbol ? Symbol.for("react.memo") : 60115;
			var REACT_LAZY_TYPE = hasSymbol ? Symbol.for("react.lazy") : 60116;
			var REACT_BLOCK_TYPE = hasSymbol ? Symbol.for("react.block") : 60121;
			var REACT_FUNDAMENTAL_TYPE = hasSymbol ? Symbol.for("react.fundamental") : 60117;
			var REACT_RESPONDER_TYPE = hasSymbol ? Symbol.for("react.responder") : 60118;
			var REACT_SCOPE_TYPE = hasSymbol ? Symbol.for("react.scope") : 60119;
			function isValidElementType(type) {
				return typeof type === "string" || typeof type === "function" || type === REACT_FRAGMENT_TYPE || type === REACT_CONCURRENT_MODE_TYPE || type === REACT_PROFILER_TYPE || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || typeof type === "object" && type !== null && (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || type.$$typeof === REACT_FUNDAMENTAL_TYPE || type.$$typeof === REACT_RESPONDER_TYPE || type.$$typeof === REACT_SCOPE_TYPE || type.$$typeof === REACT_BLOCK_TYPE);
			}
			function typeOf(object) {
				if (typeof object === "object" && object !== null) {
					var $$typeof = object.$$typeof;
					switch ($$typeof) {
						case REACT_ELEMENT_TYPE:
							var type = object.type;
							switch (type) {
								case REACT_ASYNC_MODE_TYPE:
								case REACT_CONCURRENT_MODE_TYPE:
								case REACT_FRAGMENT_TYPE:
								case REACT_PROFILER_TYPE:
								case REACT_STRICT_MODE_TYPE:
								case REACT_SUSPENSE_TYPE: return type;
								default:
									var $$typeofType = type && type.$$typeof;
									switch ($$typeofType) {
										case REACT_CONTEXT_TYPE:
										case REACT_FORWARD_REF_TYPE:
										case REACT_LAZY_TYPE:
										case REACT_MEMO_TYPE:
										case REACT_PROVIDER_TYPE: return $$typeofType;
										default: return $$typeof;
									}
							}
						case REACT_PORTAL_TYPE: return $$typeof;
					}
				}
			}
			var AsyncMode = REACT_ASYNC_MODE_TYPE;
			var ConcurrentMode = REACT_CONCURRENT_MODE_TYPE;
			var ContextConsumer = REACT_CONTEXT_TYPE;
			var ContextProvider = REACT_PROVIDER_TYPE;
			var Element = REACT_ELEMENT_TYPE;
			var ForwardRef = REACT_FORWARD_REF_TYPE;
			var Fragment = REACT_FRAGMENT_TYPE;
			var Lazy = REACT_LAZY_TYPE;
			var Memo = REACT_MEMO_TYPE;
			var Portal = REACT_PORTAL_TYPE;
			var Profiler = REACT_PROFILER_TYPE;
			var StrictMode = REACT_STRICT_MODE_TYPE;
			var Suspense = REACT_SUSPENSE_TYPE;
			var hasWarnedAboutDeprecatedIsAsyncMode = false;
			function isAsyncMode(object) {
				if (!hasWarnedAboutDeprecatedIsAsyncMode) {
					hasWarnedAboutDeprecatedIsAsyncMode = true;
					console["warn"]("The ReactIs.isAsyncMode() alias has been deprecated, and will be removed in React 17+. Update your code to use ReactIs.isConcurrentMode() instead. It has the exact same API.");
				}
				return isConcurrentMode(object) || typeOf(object) === REACT_ASYNC_MODE_TYPE;
			}
			function isConcurrentMode(object) {
				return typeOf(object) === REACT_CONCURRENT_MODE_TYPE;
			}
			function isContextConsumer(object) {
				return typeOf(object) === REACT_CONTEXT_TYPE;
			}
			function isContextProvider(object) {
				return typeOf(object) === REACT_PROVIDER_TYPE;
			}
			function isElement(object) {
				return typeof object === "object" && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
			}
			function isForwardRef(object) {
				return typeOf(object) === REACT_FORWARD_REF_TYPE;
			}
			function isFragment(object) {
				return typeOf(object) === REACT_FRAGMENT_TYPE;
			}
			function isLazy(object) {
				return typeOf(object) === REACT_LAZY_TYPE;
			}
			function isMemo(object) {
				return typeOf(object) === REACT_MEMO_TYPE;
			}
			function isPortal(object) {
				return typeOf(object) === REACT_PORTAL_TYPE;
			}
			function isProfiler(object) {
				return typeOf(object) === REACT_PROFILER_TYPE;
			}
			function isStrictMode(object) {
				return typeOf(object) === REACT_STRICT_MODE_TYPE;
			}
			function isSuspense(object) {
				return typeOf(object) === REACT_SUSPENSE_TYPE;
			}
			exports.AsyncMode = AsyncMode;
			exports.ConcurrentMode = ConcurrentMode;
			exports.ContextConsumer = ContextConsumer;
			exports.ContextProvider = ContextProvider;
			exports.Element = Element;
			exports.ForwardRef = ForwardRef;
			exports.Fragment = Fragment;
			exports.Lazy = Lazy;
			exports.Memo = Memo;
			exports.Portal = Portal;
			exports.Profiler = Profiler;
			exports.StrictMode = StrictMode;
			exports.Suspense = Suspense;
			exports.isAsyncMode = isAsyncMode;
			exports.isConcurrentMode = isConcurrentMode;
			exports.isContextConsumer = isContextConsumer;
			exports.isContextProvider = isContextProvider;
			exports.isElement = isElement;
			exports.isForwardRef = isForwardRef;
			exports.isFragment = isFragment;
			exports.isLazy = isLazy;
			exports.isMemo = isMemo;
			exports.isPortal = isPortal;
			exports.isProfiler = isProfiler;
			exports.isStrictMode = isStrictMode;
			exports.isSuspense = isSuspense;
			exports.isValidElementType = isValidElementType;
			exports.typeOf = typeOf;
		})();
	}));

//#endregion
//#region node_modules/prop-types/node_modules/react-is/index.js
	var require_react_is$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		module.exports = require_react_is_development$1();
	}));

//#endregion
//#region node_modules/object-assign/index.js
/*
	object-assign
	(c) Sindre Sorhus
	@license MIT
	*/
	var require_object_assign = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		var getOwnPropertySymbols = Object.getOwnPropertySymbols;
		var hasOwnProperty = Object.prototype.hasOwnProperty;
		var propIsEnumerable = Object.prototype.propertyIsEnumerable;
		function toObject(val) {
			if (val === null || val === void 0) throw new TypeError("Object.assign cannot be called with null or undefined");
			return Object(val);
		}
		function shouldUseNative() {
			try {
				if (!Object.assign) return false;
				var test1 = /* @__PURE__ */ new String("abc");
				test1[5] = "de";
				if (Object.getOwnPropertyNames(test1)[0] === "5") return false;
				var test2 = {};
				for (var i = 0; i < 10; i++) test2["_" + String.fromCharCode(i)] = i;
				if (Object.getOwnPropertyNames(test2).map(function(n) {
					return test2[n];
				}).join("") !== "0123456789") return false;
				var test3 = {};
				"abcdefghijklmnopqrst".split("").forEach(function(letter) {
					test3[letter] = letter;
				});
				if (Object.keys(Object.assign({}, test3)).join("") !== "abcdefghijklmnopqrst") return false;
				return true;
			} catch (err) {
				return false;
			}
		}
		module.exports = shouldUseNative() ? Object.assign : function(target, source) {
			var from;
			var to = toObject(target);
			var symbols;
			for (var s = 1; s < arguments.length; s++) {
				from = Object(arguments[s]);
				for (var key in from) if (hasOwnProperty.call(from, key)) to[key] = from[key];
				if (getOwnPropertySymbols) {
					symbols = getOwnPropertySymbols(from);
					for (var i = 0; i < symbols.length; i++) if (propIsEnumerable.call(from, symbols[i])) to[symbols[i]] = from[symbols[i]];
				}
			}
			return to;
		};
	}));

//#endregion
//#region node_modules/prop-types/lib/ReactPropTypesSecret.js
/**
	* Copyright (c) 2013-present, Facebook, Inc.
	*
	* This source code is licensed under the MIT license found in the
	* LICENSE file in the root directory of this source tree.
	*/
	var require_ReactPropTypesSecret = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		var ReactPropTypesSecret = "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED";
		module.exports = ReactPropTypesSecret;
	}));

//#endregion
//#region node_modules/prop-types/lib/has.js
	var require_has = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		module.exports = Function.call.bind(Object.prototype.hasOwnProperty);
	}));

//#endregion
//#region node_modules/prop-types/checkPropTypes.js
/**
	* Copyright (c) 2013-present, Facebook, Inc.
	*
	* This source code is licensed under the MIT license found in the
	* LICENSE file in the root directory of this source tree.
	*/
	var require_checkPropTypes = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		var printWarning = function() {};
		var ReactPropTypesSecret = require_ReactPropTypesSecret();
		var loggedTypeFailures = {};
		var has = require_has();
		printWarning = function(text) {
			var message = "Warning: " + text;
			if (typeof console !== "undefined") console.error(message);
			try {
				throw new Error(message);
			} catch (x) {}
		};
		/**
		* Assert that the values match with the type specs.
		* Error messages are memorized and will only be shown once.
		*
		* @param {object} typeSpecs Map of name to a ReactPropType
		* @param {object} values Runtime values that need to be type-checked
		* @param {string} location e.g. "prop", "context", "child context"
		* @param {string} componentName Name of the component for error messages.
		* @param {?Function} getStack Returns the component stack.
		* @private
		*/
		function checkPropTypes(typeSpecs, values, location, componentName, getStack) {
			for (var typeSpecName in typeSpecs) if (has(typeSpecs, typeSpecName)) {
				var error;
				try {
					if (typeof typeSpecs[typeSpecName] !== "function") {
						var err = Error((componentName || "React class") + ": " + location + " type `" + typeSpecName + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + typeof typeSpecs[typeSpecName] + "`.This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`.");
						err.name = "Invariant Violation";
						throw err;
					}
					error = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, ReactPropTypesSecret);
				} catch (ex) {
					error = ex;
				}
				if (error && !(error instanceof Error)) printWarning((componentName || "React class") + ": type specification of " + location + " `" + typeSpecName + "` is invalid; the type checker function must return `null` or an `Error` but returned a " + typeof error + ". You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument).");
				if (error instanceof Error && !(error.message in loggedTypeFailures)) {
					loggedTypeFailures[error.message] = true;
					var stack = getStack ? getStack() : "";
					printWarning("Failed " + location + " type: " + error.message + (stack != null ? stack : ""));
				}
			}
		}
		/**
		* Resets warning cache when testing.
		*
		* @private
		*/
		checkPropTypes.resetWarningCache = function() {
			loggedTypeFailures = {};
		};
		module.exports = checkPropTypes;
	}));

//#endregion
//#region node_modules/prop-types/factoryWithTypeCheckers.js
/**
	* Copyright (c) 2013-present, Facebook, Inc.
	*
	* This source code is licensed under the MIT license found in the
	* LICENSE file in the root directory of this source tree.
	*/
	var require_factoryWithTypeCheckers = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		var ReactIs = require_react_is$1();
		var assign = require_object_assign();
		var ReactPropTypesSecret = require_ReactPropTypesSecret();
		var has = require_has();
		var checkPropTypes = require_checkPropTypes();
		var printWarning = function() {};
		printWarning = function(text) {
			var message = "Warning: " + text;
			if (typeof console !== "undefined") console.error(message);
			try {
				throw new Error(message);
			} catch (x) {}
		};
		function emptyFunctionThatReturnsNull() {
			return null;
		}
		module.exports = function(isValidElement, throwOnDirectAccess) {
			var ITERATOR_SYMBOL = typeof Symbol === "function" && Symbol.iterator;
			var FAUX_ITERATOR_SYMBOL = "@@iterator";
			/**
			* Returns the iterator method function contained on the iterable object.
			*
			* Be sure to invoke the function with the iterable as context:
			*
			*     var iteratorFn = getIteratorFn(myIterable);
			*     if (iteratorFn) {
			*       var iterator = iteratorFn.call(myIterable);
			*       ...
			*     }
			*
			* @param {?object} maybeIterable
			* @return {?function}
			*/
			function getIteratorFn(maybeIterable) {
				var iteratorFn = maybeIterable && (ITERATOR_SYMBOL && maybeIterable[ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL]);
				if (typeof iteratorFn === "function") return iteratorFn;
			}
			/**
			* Collection of methods that allow declaration and validation of props that are
			* supplied to React components. Example usage:
			*
			*   var Props = require('ReactPropTypes');
			*   var MyArticle = React.createClass({
			*     propTypes: {
			*       // An optional string prop named "description".
			*       description: Props.string,
			*
			*       // A required enum prop named "category".
			*       category: Props.oneOf(['News','Photos']).isRequired,
			*
			*       // A prop named "dialog" that requires an instance of Dialog.
			*       dialog: Props.instanceOf(Dialog).isRequired
			*     },
			*     render: function() { ... }
			*   });
			*
			* A more formal specification of how these methods are used:
			*
			*   type := array|bool|func|object|number|string|oneOf([...])|instanceOf(...)
			*   decl := ReactPropTypes.{type}(.isRequired)?
			*
			* Each and every declaration produces a function with the same signature. This
			* allows the creation of custom validation functions. For example:
			*
			*  var MyLink = React.createClass({
			*    propTypes: {
			*      // An optional string or URI prop named "href".
			*      href: function(props, propName, componentName) {
			*        var propValue = props[propName];
			*        if (propValue != null && typeof propValue !== 'string' &&
			*            !(propValue instanceof URI)) {
			*          return new Error(
			*            'Expected a string or an URI for ' + propName + ' in ' +
			*            componentName
			*          );
			*        }
			*      }
			*    },
			*    render: function() {...}
			*  });
			*
			* @internal
			*/
			var ANONYMOUS = "<<anonymous>>";
			var ReactPropTypes = {
				array: createPrimitiveTypeChecker("array"),
				bigint: createPrimitiveTypeChecker("bigint"),
				bool: createPrimitiveTypeChecker("boolean"),
				func: createPrimitiveTypeChecker("function"),
				number: createPrimitiveTypeChecker("number"),
				object: createPrimitiveTypeChecker("object"),
				string: createPrimitiveTypeChecker("string"),
				symbol: createPrimitiveTypeChecker("symbol"),
				any: createAnyTypeChecker(),
				arrayOf: createArrayOfTypeChecker,
				element: createElementTypeChecker(),
				elementType: createElementTypeTypeChecker(),
				instanceOf: createInstanceTypeChecker,
				node: createNodeChecker(),
				objectOf: createObjectOfTypeChecker,
				oneOf: createEnumTypeChecker,
				oneOfType: createUnionTypeChecker,
				shape: createShapeTypeChecker,
				exact: createStrictShapeTypeChecker
			};
			/**
			* inlined Object.is polyfill to avoid requiring consumers ship their own
			* https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is
			*/
			function is(x, y) {
				if (x === y) return x !== 0 || 1 / x === 1 / y;
				else return x !== x && y !== y;
			}
			/**
			* We use an Error-like object for backward compatibility as people may call
			* PropTypes directly and inspect their output. However, we don't use real
			* Errors anymore. We don't inspect their stack anyway, and creating them
			* is prohibitively expensive if they are created too often, such as what
			* happens in oneOfType() for any type before the one that matched.
			*/
			function PropTypeError(message, data) {
				this.message = message;
				this.data = data && typeof data === "object" ? data : {};
				this.stack = "";
			}
			PropTypeError.prototype = Error.prototype;
			function createChainableTypeChecker(validate) {
				var manualPropTypeCallCache = {};
				var manualPropTypeWarningCount = 0;
				function checkType(isRequired, props, propName, componentName, location, propFullName, secret) {
					componentName = componentName || ANONYMOUS;
					propFullName = propFullName || propName;
					if (secret !== ReactPropTypesSecret) {
						if (throwOnDirectAccess) {
							var err = /* @__PURE__ */ new Error("Calling PropTypes validators directly is not supported by the `prop-types` package. Use `PropTypes.checkPropTypes()` to call them. Read more at http://fb.me/use-check-prop-types");
							err.name = "Invariant Violation";
							throw err;
						} else if (typeof console !== "undefined") {
							var cacheKey = componentName + ":" + propName;
							if (!manualPropTypeCallCache[cacheKey] && manualPropTypeWarningCount < 3) {
								printWarning("You are manually calling a React.PropTypes validation function for the `" + propFullName + "` prop on `" + componentName + "`. This is deprecated and will throw in the standalone `prop-types` package. You may be seeing this warning due to a third-party PropTypes library. See https://fb.me/react-warning-dont-call-proptypes for details.");
								manualPropTypeCallCache[cacheKey] = true;
								manualPropTypeWarningCount++;
							}
						}
					}
					if (props[propName] == null) {
						if (isRequired) {
							if (props[propName] === null) return new PropTypeError("The " + location + " `" + propFullName + "` is marked as required " + ("in `" + componentName + "`, but its value is `null`."));
							return new PropTypeError("The " + location + " `" + propFullName + "` is marked as required in " + ("`" + componentName + "`, but its value is `undefined`."));
						}
						return null;
					} else return validate(props, propName, componentName, location, propFullName);
				}
				var chainedCheckType = checkType.bind(null, false);
				chainedCheckType.isRequired = checkType.bind(null, true);
				return chainedCheckType;
			}
			function createPrimitiveTypeChecker(expectedType) {
				function validate(props, propName, componentName, location, propFullName, secret) {
					var propValue = props[propName];
					if (getPropType(propValue) !== expectedType) {
						var preciseType = getPreciseType(propValue);
						return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + preciseType + "` supplied to `" + componentName + "`, expected ") + ("`" + expectedType + "`."), { expectedType });
					}
					return null;
				}
				return createChainableTypeChecker(validate);
			}
			function createAnyTypeChecker() {
				return createChainableTypeChecker(emptyFunctionThatReturnsNull);
			}
			function createArrayOfTypeChecker(typeChecker) {
				function validate(props, propName, componentName, location, propFullName) {
					if (typeof typeChecker !== "function") return new PropTypeError("Property `" + propFullName + "` of component `" + componentName + "` has invalid PropType notation inside arrayOf.");
					var propValue = props[propName];
					if (!Array.isArray(propValue)) {
						var propType = getPropType(propValue);
						return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected an array."));
					}
					for (var i = 0; i < propValue.length; i++) {
						var error = typeChecker(propValue, i, componentName, location, propFullName + "[" + i + "]", ReactPropTypesSecret);
						if (error instanceof Error) return error;
					}
					return null;
				}
				return createChainableTypeChecker(validate);
			}
			function createElementTypeChecker() {
				function validate(props, propName, componentName, location, propFullName) {
					var propValue = props[propName];
					if (!isValidElement(propValue)) {
						var propType = getPropType(propValue);
						return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected a single ReactElement."));
					}
					return null;
				}
				return createChainableTypeChecker(validate);
			}
			function createElementTypeTypeChecker() {
				function validate(props, propName, componentName, location, propFullName) {
					var propValue = props[propName];
					if (!ReactIs.isValidElementType(propValue)) {
						var propType = getPropType(propValue);
						return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected a single ReactElement type."));
					}
					return null;
				}
				return createChainableTypeChecker(validate);
			}
			function createInstanceTypeChecker(expectedClass) {
				function validate(props, propName, componentName, location, propFullName) {
					if (!(props[propName] instanceof expectedClass)) {
						var expectedClassName = expectedClass.name || ANONYMOUS;
						var actualClassName = getClassName(props[propName]);
						return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + actualClassName + "` supplied to `" + componentName + "`, expected ") + ("instance of `" + expectedClassName + "`."));
					}
					return null;
				}
				return createChainableTypeChecker(validate);
			}
			function createEnumTypeChecker(expectedValues) {
				if (!Array.isArray(expectedValues)) {
					if (arguments.length > 1) printWarning("Invalid arguments supplied to oneOf, expected an array, got " + arguments.length + " arguments. A common mistake is to write oneOf(x, y, z) instead of oneOf([x, y, z]).");
					else printWarning("Invalid argument supplied to oneOf, expected an array.");
					return emptyFunctionThatReturnsNull;
				}
				function validate(props, propName, componentName, location, propFullName) {
					var propValue = props[propName];
					for (var i = 0; i < expectedValues.length; i++) if (is(propValue, expectedValues[i])) return null;
					var valuesString = JSON.stringify(expectedValues, function replacer(key, value) {
						if (getPreciseType(value) === "symbol") return String(value);
						return value;
					});
					return new PropTypeError("Invalid " + location + " `" + propFullName + "` of value `" + String(propValue) + "` " + ("supplied to `" + componentName + "`, expected one of " + valuesString + "."));
				}
				return createChainableTypeChecker(validate);
			}
			function createObjectOfTypeChecker(typeChecker) {
				function validate(props, propName, componentName, location, propFullName) {
					if (typeof typeChecker !== "function") return new PropTypeError("Property `" + propFullName + "` of component `" + componentName + "` has invalid PropType notation inside objectOf.");
					var propValue = props[propName];
					var propType = getPropType(propValue);
					if (propType !== "object") return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected an object."));
					for (var key in propValue) if (has(propValue, key)) {
						var error = typeChecker(propValue, key, componentName, location, propFullName + "." + key, ReactPropTypesSecret);
						if (error instanceof Error) return error;
					}
					return null;
				}
				return createChainableTypeChecker(validate);
			}
			function createUnionTypeChecker(arrayOfTypeCheckers) {
				if (!Array.isArray(arrayOfTypeCheckers)) {
					printWarning("Invalid argument supplied to oneOfType, expected an instance of array.");
					return emptyFunctionThatReturnsNull;
				}
				for (var i = 0; i < arrayOfTypeCheckers.length; i++) {
					var checker = arrayOfTypeCheckers[i];
					if (typeof checker !== "function") {
						printWarning("Invalid argument supplied to oneOfType. Expected an array of check functions, but received " + getPostfixForTypeWarning(checker) + " at index " + i + ".");
						return emptyFunctionThatReturnsNull;
					}
				}
				function validate(props, propName, componentName, location, propFullName) {
					var expectedTypes = [];
					for (var i = 0; i < arrayOfTypeCheckers.length; i++) {
						var checker = arrayOfTypeCheckers[i];
						var checkerResult = checker(props, propName, componentName, location, propFullName, ReactPropTypesSecret);
						if (checkerResult == null) return null;
						if (checkerResult.data && has(checkerResult.data, "expectedType")) expectedTypes.push(checkerResult.data.expectedType);
					}
					var expectedTypesMessage = expectedTypes.length > 0 ? ", expected one of type [" + expectedTypes.join(", ") + "]" : "";
					return new PropTypeError("Invalid " + location + " `" + propFullName + "` supplied to " + ("`" + componentName + "`" + expectedTypesMessage + "."));
				}
				return createChainableTypeChecker(validate);
			}
			function createNodeChecker() {
				function validate(props, propName, componentName, location, propFullName) {
					if (!isNode(props[propName])) return new PropTypeError("Invalid " + location + " `" + propFullName + "` supplied to " + ("`" + componentName + "`, expected a ReactNode."));
					return null;
				}
				return createChainableTypeChecker(validate);
			}
			function invalidValidatorError(componentName, location, propFullName, key, type) {
				return new PropTypeError((componentName || "React class") + ": " + location + " type `" + propFullName + "." + key + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + type + "`.");
			}
			function createShapeTypeChecker(shapeTypes) {
				function validate(props, propName, componentName, location, propFullName) {
					var propValue = props[propName];
					var propType = getPropType(propValue);
					if (propType !== "object") return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type `" + propType + "` " + ("supplied to `" + componentName + "`, expected `object`."));
					for (var key in shapeTypes) {
						var checker = shapeTypes[key];
						if (typeof checker !== "function") return invalidValidatorError(componentName, location, propFullName, key, getPreciseType(checker));
						var error = checker(propValue, key, componentName, location, propFullName + "." + key, ReactPropTypesSecret);
						if (error) return error;
					}
					return null;
				}
				return createChainableTypeChecker(validate);
			}
			function createStrictShapeTypeChecker(shapeTypes) {
				function validate(props, propName, componentName, location, propFullName) {
					var propValue = props[propName];
					var propType = getPropType(propValue);
					if (propType !== "object") return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type `" + propType + "` " + ("supplied to `" + componentName + "`, expected `object`."));
					for (var key in assign({}, props[propName], shapeTypes)) {
						var checker = shapeTypes[key];
						if (has(shapeTypes, key) && typeof checker !== "function") return invalidValidatorError(componentName, location, propFullName, key, getPreciseType(checker));
						if (!checker) return new PropTypeError("Invalid " + location + " `" + propFullName + "` key `" + key + "` supplied to `" + componentName + "`.\nBad object: " + JSON.stringify(props[propName], null, "  ") + "\nValid keys: " + JSON.stringify(Object.keys(shapeTypes), null, "  "));
						var error = checker(propValue, key, componentName, location, propFullName + "." + key, ReactPropTypesSecret);
						if (error) return error;
					}
					return null;
				}
				return createChainableTypeChecker(validate);
			}
			function isNode(propValue) {
				switch (typeof propValue) {
					case "number":
					case "string":
					case "undefined": return true;
					case "boolean": return !propValue;
					case "object":
						if (Array.isArray(propValue)) return propValue.every(isNode);
						if (propValue === null || isValidElement(propValue)) return true;
						var iteratorFn = getIteratorFn(propValue);
						if (iteratorFn) {
							var iterator = iteratorFn.call(propValue);
							var step;
							if (iteratorFn !== propValue.entries) {
								while (!(step = iterator.next()).done) if (!isNode(step.value)) return false;
							} else while (!(step = iterator.next()).done) {
								var entry = step.value;
								if (entry) {
									if (!isNode(entry[1])) return false;
								}
							}
						} else return false;
						return true;
					default: return false;
				}
			}
			function isSymbol(propType, propValue) {
				if (propType === "symbol") return true;
				if (!propValue) return false;
				if (propValue["@@toStringTag"] === "Symbol") return true;
				if (typeof Symbol === "function" && propValue instanceof Symbol) return true;
				return false;
			}
			function getPropType(propValue) {
				var propType = typeof propValue;
				if (Array.isArray(propValue)) return "array";
				if (propValue instanceof RegExp) return "object";
				if (isSymbol(propType, propValue)) return "symbol";
				return propType;
			}
			function getPreciseType(propValue) {
				if (typeof propValue === "undefined" || propValue === null) return "" + propValue;
				var propType = getPropType(propValue);
				if (propType === "object") {
					if (propValue instanceof Date) return "date";
					else if (propValue instanceof RegExp) return "regexp";
				}
				return propType;
			}
			function getPostfixForTypeWarning(value) {
				var type = getPreciseType(value);
				switch (type) {
					case "array":
					case "object": return "an " + type;
					case "boolean":
					case "date":
					case "regexp": return "a " + type;
					default: return type;
				}
			}
			function getClassName(propValue) {
				if (!propValue.constructor || !propValue.constructor.name) return ANONYMOUS;
				return propValue.constructor.name;
			}
			ReactPropTypes.checkPropTypes = checkPropTypes;
			ReactPropTypes.resetWarningCache = checkPropTypes.resetWarningCache;
			ReactPropTypes.PropTypes = ReactPropTypes;
			return ReactPropTypes;
		};
	}));

//#endregion
//#region node_modules/prop-types/index.js
	var require_prop_types = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		var ReactIs = require_react_is$1();
		var throwOnDirectAccess = true;
		module.exports = require_factoryWithTypeCheckers()(ReactIs.isElement, throwOnDirectAccess);
	}));

//#endregion
//#region modules/ai/assets/js/editor/context/requests-ids.js
	var import_prop_types$13, Context, useRequestIds, getUniqueId, RequestIdsProvider;
	var init_requests_ids = __esmMin((() => {
		import_prop_types$13 = /* @__PURE__ */ __toESM(require_prop_types());
		Context = (0, react.createContext)({});
		useRequestIds = function useRequestIds() {
			var context = (0, react.useContext)(Context);
			if (!context) throw new Error("useRequestIds must be used within a RequestIdsProvider");
			return context;
		};
		getUniqueId = function getUniqueId(prefix) {
			return prefix + "-" + Math.random().toString(16).substr(2, 7);
		};
		window.EDITOR_SESSION_ID = window.EDITOR_SESSION_ID || getUniqueId("editor-session");
		RequestIdsProvider = function RequestIdsProvider(props) {
			var editorSessionId = (0, react.useRef)(window.EDITOR_SESSION_ID);
			var sessionId = (0, react.useRef)("");
			var generateId = (0, react.useRef)("");
			var batchId = (0, react.useRef)("");
			var requestId = (0, react.useRef)("");
			sessionId.current = getUniqueId("session");
			var setGenerate = function setGenerate() {
				generateId.current = getUniqueId("generate");
				return generateId;
			};
			var setBatch = function setBatch() {
				batchId.current = getUniqueId("batch");
				return batchId;
			};
			var setRequest = function setRequest() {
				requestId.current = getUniqueId("request");
				return requestId;
			};
			var _useState2 = _slicedToArray((0, react.useState)(0), 2);
			var usagePercentage = _useState2[0];
			var setUsagePercentage = _useState2[1];
			return /*#__PURE__*/ react.default.createElement(Context.Provider, { value: {
				editorSessionId,
				sessionId,
				generateId,
				batchId,
				requestId,
				setGenerate,
				setBatch,
				setRequest,
				usagePercentage,
				updateUsagePercentage: function updateUsagePercentage(newPercentage) {
					setUsagePercentage(newPercentage);
				}
			} }, props.children);
		};
		RequestIdsProvider.propTypes = { children: import_prop_types$13.default.node.isRequired };
	}));

//#endregion
//#region node_modules/@babel/runtime/helpers/esm/extends.js
	function _extends$1() {
		return _extends$1 = Object.assign ? Object.assign.bind() : function(n) {
			for (var e = 1; e < arguments.length; e++) {
				var t = arguments[e];
				for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]);
			}
			return n;
		}, _extends$1.apply(null, arguments);
	}
	var init_extends = __esmMin((() => {
		__name(_extends$1, "_extends");
	}));

//#endregion
//#region node_modules/@babel/runtime/helpers/esm/typeof.js
	function _typeof$1(o) {
		"@babel/helpers - typeof";
		return _typeof$1 = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o) {
			return typeof o;
		} : function(o) {
			return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
		}, _typeof$1(o);
	}
	var init_typeof$1 = __esmMin((() => {
		__name(_typeof$1, "_typeof");
	}));

//#endregion
//#region node_modules/@babel/runtime/helpers/esm/toPrimitive.js
	function toPrimitive$1(t, r) {
		if ("object" != _typeof$1(t) || !t) return t;
		var e = t[Symbol.toPrimitive];
		if (void 0 !== e) {
			var i = e.call(t, r || "default");
			if ("object" != _typeof$1(i)) return i;
			throw new TypeError("@@toPrimitive must return a primitive value.");
		}
		return ("string" === r ? String : Number)(t);
	}
	var init_toPrimitive$1 = __esmMin((() => {
		init_typeof$1();
		__name(toPrimitive$1, "toPrimitive");
	}));

//#endregion
//#region node_modules/@babel/runtime/helpers/esm/toPropertyKey.js
	function toPropertyKey$1(t) {
		var i = toPrimitive$1(t, "string");
		return "symbol" == _typeof$1(i) ? i : i + "";
	}
	var init_toPropertyKey$1 = __esmMin((() => {
		init_typeof$1();
		init_toPrimitive$1();
		__name(toPropertyKey$1, "toPropertyKey");
	}));

//#endregion
//#region node_modules/@babel/runtime/helpers/esm/defineProperty.js
	function _defineProperty$1(e, r, t) {
		return (r = toPropertyKey$1(r)) in e ? Object.defineProperty(e, r, {
			value: t,
			enumerable: !0,
			configurable: !0,
			writable: !0
		}) : e[r] = t, e;
	}
	var init_defineProperty$1 = __esmMin((() => {
		init_toPropertyKey$1();
		__name(_defineProperty$1, "_defineProperty");
	}));

//#endregion
//#region node_modules/@babel/runtime/helpers/esm/asyncToGenerator.js
	function asyncGeneratorStep$1(n, t, e, r, o, a, c) {
		try {
			var i = n[a](c);
			var u = i.value;
		} catch (n) {
			e(n);
			return;
		}
		i.done ? t(u) : Promise.resolve(u).then(r, o);
	}
	function _asyncToGenerator$1(n) {
		return function() {
			var t = this;
			var e = arguments;
			return new Promise(function(r, o) {
				var a = n.apply(t, e);
				function _next(n) {
					asyncGeneratorStep$1(a, r, o, _next, _throw, "next", n);
				}
				function _throw(n) {
					asyncGeneratorStep$1(a, r, o, _next, _throw, "throw", n);
				}
				_next(void 0);
			});
		};
	}
	var init_asyncToGenerator = __esmMin((() => {
		__name(asyncGeneratorStep$1, "asyncGeneratorStep");
		__name(_asyncToGenerator$1, "_asyncToGenerator");
	}));

//#endregion
//#region node_modules/@babel/runtime/helpers/OverloadYield.js
	var require_OverloadYield = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		function _OverloadYield(e, d) {
			this.v = e, this.k = d;
		}
		module.exports = _OverloadYield, module.exports.__esModule = true, module.exports["default"] = module.exports;
	}));

//#endregion
//#region node_modules/@babel/runtime/helpers/regeneratorDefine.js
	var require_regeneratorDefine = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		function _regeneratorDefine(e, r, n, t) {
			var i = Object.defineProperty;
			try {
				i({}, "", {});
			} catch (e) {
				i = 0;
			}
			module.exports = _regeneratorDefine = function regeneratorDefine(e, r, n, t) {
				function o(r, n) {
					_regeneratorDefine(e, r, function(e) {
						return this._invoke(r, n, e);
					});
				}
				r ? i ? i(e, r, {
					value: n,
					enumerable: !t,
					configurable: !t,
					writable: !t
				}) : e[r] = n : (o("next", 0), o("throw", 1), o("return", 2));
			}, module.exports.__esModule = true, module.exports["default"] = module.exports, _regeneratorDefine(e, r, n, t);
		}
		module.exports = _regeneratorDefine, module.exports.__esModule = true, module.exports["default"] = module.exports;
	}));

//#endregion
//#region node_modules/@babel/runtime/helpers/regenerator.js
	var require_regenerator$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		var regeneratorDefine = require_regeneratorDefine();
		function _regenerator() {
			/*! regenerator-runtime -- Copyright (c) 2014-present, Facebook, Inc. -- license (MIT): https://github.com/babel/babel/blob/main/packages/babel-helpers/LICENSE */
			var e;
			var t;
			var r = "function" == typeof Symbol ? Symbol : {};
			var n = r.iterator || "@@iterator";
			var o = r.toStringTag || "@@toStringTag";
			function i(r, n, o, i) {
				var c = n && n.prototype instanceof Generator ? n : Generator;
				var u = Object.create(c.prototype);
				return regeneratorDefine(u, "_invoke", function(r, n, o) {
					var i;
					var c;
					var u;
					var f = 0;
					var p = o || [];
					var y = !1;
					var G = {
						p: 0,
						n: 0,
						v: e,
						a: d,
						f: d.bind(e, 4),
						d: function d(t, r) {
							return i = t, c = 0, u = e, G.n = r, a;
						}
					};
					function d(r, n) {
						for (c = r, u = n, t = 0; !y && f && !o && t < p.length; t++) {
							var o;
							var i = p[t];
							var d = G.p;
							var l = i[2];
							r > 3 ? (o = l === n) && (u = i[(c = i[4]) ? 5 : (c = 3, 3)], i[4] = i[5] = e) : i[0] <= d && ((o = r < 2 && d < i[1]) ? (c = 0, G.v = n, G.n = i[1]) : d < l && (o = r < 3 || i[0] > n || n > l) && (i[4] = r, i[5] = n, G.n = l, c = 0));
						}
						if (o || r > 1) return a;
						throw y = !0, n;
					}
					return function(o, p, l) {
						if (f > 1) throw TypeError("Generator is already running");
						for (y && 1 === p && d(p, l), c = p, u = l; (t = c < 2 ? e : u) || !y;) {
							i || (c ? c < 3 ? (c > 1 && (G.n = -1), d(c, u)) : G.n = u : G.v = u);
							try {
								if (f = 2, i) {
									if (c || (o = "next"), t = i[o]) {
										if (!(t = t.call(i, u))) throw TypeError("iterator result is not an object");
										if (!t.done) return t;
										u = t.value, c < 2 && (c = 0);
									} else 1 === c && (t = i["return"]) && t.call(i), c < 2 && (u = TypeError("The iterator does not provide a '" + o + "' method"), c = 1);
									i = e;
								} else if ((t = (y = G.n < 0) ? u : r.call(n, G)) !== a) break;
							} catch (t) {
								i = e, c = 1, u = t;
							} finally {
								f = 1;
							}
						}
						return {
							value: t,
							done: y
						};
					};
				}(r, o, i), !0), u;
			}
			var a = {};
			function Generator() {}
			function GeneratorFunction() {}
			function GeneratorFunctionPrototype() {}
			t = Object.getPrototypeOf;
			var c = [][n] ? t(t([][n]())) : (regeneratorDefine(t = {}, n, function() {
				return this;
			}), t);
			var u = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(c);
			function f(e) {
				return Object.setPrototypeOf ? Object.setPrototypeOf(e, GeneratorFunctionPrototype) : (e.__proto__ = GeneratorFunctionPrototype, regeneratorDefine(e, o, "GeneratorFunction")), e.prototype = Object.create(u), e;
			}
			return GeneratorFunction.prototype = GeneratorFunctionPrototype, regeneratorDefine(u, "constructor", GeneratorFunctionPrototype), regeneratorDefine(GeneratorFunctionPrototype, "constructor", GeneratorFunction), GeneratorFunction.displayName = "GeneratorFunction", regeneratorDefine(GeneratorFunctionPrototype, o, "GeneratorFunction"), regeneratorDefine(u), regeneratorDefine(u, o, "Generator"), regeneratorDefine(u, n, function() {
				return this;
			}), regeneratorDefine(u, "toString", function() {
				return "[object Generator]";
			}), (module.exports = _regenerator = function _regenerator() {
				return {
					w: i,
					m: f
				};
			}, module.exports.__esModule = true, module.exports["default"] = module.exports)();
		}
		module.exports = _regenerator, module.exports.__esModule = true, module.exports["default"] = module.exports;
	}));

//#endregion
//#region node_modules/@babel/runtime/helpers/regeneratorAsyncIterator.js
	var require_regeneratorAsyncIterator = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		var OverloadYield = require_OverloadYield();
		var regeneratorDefine = require_regeneratorDefine();
		function AsyncIterator(t, e) {
			function n(r, o, i, f) {
				try {
					var c = t[r](o);
					var u = c.value;
					return u instanceof OverloadYield ? e.resolve(u.v).then(function(t) {
						n("next", t, i, f);
					}, function(t) {
						n("throw", t, i, f);
					}) : e.resolve(u).then(function(t) {
						c.value = t, i(c);
					}, function(t) {
						return n("throw", t, i, f);
					});
				} catch (t) {
					f(t);
				}
			}
			var r;
			this.next || (regeneratorDefine(AsyncIterator.prototype), regeneratorDefine(AsyncIterator.prototype, "function" == typeof Symbol && Symbol.asyncIterator || "@asyncIterator", function() {
				return this;
			})), regeneratorDefine(this, "_invoke", function(t, o, i) {
				function f() {
					return new e(function(e, r) {
						n(t, i, e, r);
					});
				}
				return r = r ? r.then(f, f) : f();
			}, !0);
		}
		module.exports = AsyncIterator, module.exports.__esModule = true, module.exports["default"] = module.exports;
	}));

//#endregion
//#region node_modules/@babel/runtime/helpers/regeneratorAsyncGen.js
	var require_regeneratorAsyncGen = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		var regenerator = require_regenerator$1();
		var regeneratorAsyncIterator = require_regeneratorAsyncIterator();
		function _regeneratorAsyncGen(r, e, t, o, n) {
			return new regeneratorAsyncIterator(regenerator().w(r, e, t, o), n || Promise);
		}
		module.exports = _regeneratorAsyncGen, module.exports.__esModule = true, module.exports["default"] = module.exports;
	}));

//#endregion
//#region node_modules/@babel/runtime/helpers/regeneratorAsync.js
	var require_regeneratorAsync = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		var regeneratorAsyncGen = require_regeneratorAsyncGen();
		function _regeneratorAsync(n, e, r, t, o) {
			var a = regeneratorAsyncGen(n, e, r, t, o);
			return a.next().then(function(n) {
				return n.done ? n.value : a.next();
			});
		}
		module.exports = _regeneratorAsync, module.exports.__esModule = true, module.exports["default"] = module.exports;
	}));

//#endregion
//#region node_modules/@babel/runtime/helpers/regeneratorKeys.js
	var require_regeneratorKeys = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		function _regeneratorKeys(e) {
			var n = Object(e);
			var r = [];
			for (var t in n) r.unshift(t);
			return function e() {
				for (; r.length;) if ((t = r.pop()) in n) return e.value = t, e.done = !1, e;
				return e.done = !0, e;
			};
		}
		module.exports = _regeneratorKeys, module.exports.__esModule = true, module.exports["default"] = module.exports;
	}));

//#endregion
//#region node_modules/@babel/runtime/helpers/typeof.js
	var require_typeof = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		function _typeof(o) {
			"@babel/helpers - typeof";
			return module.exports = _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o) {
				return typeof o;
			} : function(o) {
				return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
			}, module.exports.__esModule = true, module.exports["default"] = module.exports, _typeof(o);
		}
		module.exports = _typeof, module.exports.__esModule = true, module.exports["default"] = module.exports;
	}));

//#endregion
//#region node_modules/@babel/runtime/helpers/regeneratorValues.js
	var require_regeneratorValues = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		var _typeof = require_typeof()["default"];
		function _regeneratorValues(e) {
			if (null != e) {
				var t = e["function" == typeof Symbol && Symbol.iterator || "@@iterator"];
				var r = 0;
				if (t) return t.call(e);
				if ("function" == typeof e.next) return e;
				if (!isNaN(e.length)) return { next: function next() {
					return e && r >= e.length && (e = void 0), {
						value: e && e[r++],
						done: !e
					};
				} };
			}
			throw new TypeError(_typeof(e) + " is not iterable");
		}
		module.exports = _regeneratorValues, module.exports.__esModule = true, module.exports["default"] = module.exports;
	}));

//#endregion
//#region node_modules/@babel/runtime/helpers/regeneratorRuntime.js
	var require_regeneratorRuntime = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		var OverloadYield = require_OverloadYield();
		var regenerator = require_regenerator$1();
		var regeneratorAsync = require_regeneratorAsync();
		var regeneratorAsyncGen = require_regeneratorAsyncGen();
		var regeneratorAsyncIterator = require_regeneratorAsyncIterator();
		var regeneratorKeys = require_regeneratorKeys();
		var regeneratorValues = require_regeneratorValues();
		function _regeneratorRuntime() {
			"use strict";
			var r = regenerator();
			var e = r.m(_regeneratorRuntime);
			var t = (Object.getPrototypeOf ? Object.getPrototypeOf(e) : e.__proto__).constructor;
			function n(r) {
				var e = "function" == typeof r && r.constructor;
				return !!e && (e === t || "GeneratorFunction" === (e.displayName || e.name));
			}
			var o = {
				"throw": 1,
				"return": 2,
				"break": 3,
				"continue": 3
			};
			function a(r) {
				var e;
				var t;
				return function(n) {
					e || (e = {
						stop: function stop() {
							return t(n.a, 2);
						},
						"catch": function _catch() {
							return n.v;
						},
						abrupt: function abrupt(r, e) {
							return t(n.a, o[r], e);
						},
						delegateYield: function delegateYield(r, o, a) {
							return e.resultName = o, t(n.d, regeneratorValues(r), a);
						},
						finish: function finish(r) {
							return t(n.f, r);
						}
					}, t = function t(r, _t, o) {
						n.p = e.prev, n.n = e.next;
						try {
							return r(_t, o);
						} finally {
							e.next = n.n;
						}
					}), e.resultName && (e[e.resultName] = n.v, e.resultName = void 0), e.sent = n.v, e.next = n.n;
					try {
						return r.call(this, e);
					} finally {
						n.p = e.prev, n.n = e.next;
					}
				};
			}
			return (module.exports = _regeneratorRuntime = function _regeneratorRuntime() {
				return {
					wrap: function wrap(e, t, n, o) {
						return r.w(a(e), t, n, o && o.reverse());
					},
					isGeneratorFunction: n,
					mark: r.m,
					awrap: function awrap(r, e) {
						return new OverloadYield(r, e);
					},
					AsyncIterator: regeneratorAsyncIterator,
					async: function async(r, e, t, o, u) {
						return (n(e) ? regeneratorAsyncGen : regeneratorAsync)(a(r), e, t, o, u);
					},
					keys: regeneratorKeys,
					values: regeneratorValues
				};
			}, module.exports.__esModule = true, module.exports["default"] = module.exports)();
		}
		module.exports = _regeneratorRuntime, module.exports.__esModule = true, module.exports["default"] = module.exports;
	}));

//#endregion
//#region node_modules/@babel/runtime/regenerator/index.js
	var require_regenerator = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		var runtime = require_regeneratorRuntime()();
		module.exports = runtime;
		try {
			regeneratorRuntime = runtime;
		} catch (accidentalStrictMode) {
			if (typeof globalThis === "object") globalThis.regeneratorRuntime = runtime;
			else Function("r", "regeneratorRuntime = r")(runtime);
		}
	}));

//#endregion
//#region node_modules/@babel/runtime/helpers/esm/objectWithoutPropertiesLoose.js
	function _objectWithoutPropertiesLoose$1(r, e) {
		if (null == r) return {};
		var t = {};
		for (var n in r) if ({}.hasOwnProperty.call(r, n)) {
			if (-1 !== e.indexOf(n)) continue;
			t[n] = r[n];
		}
		return t;
	}
	var init_objectWithoutPropertiesLoose$1 = __esmMin((() => {
		__name(_objectWithoutPropertiesLoose$1, "_objectWithoutPropertiesLoose");
	}));

//#endregion
//#region node_modules/@babel/runtime/helpers/esm/objectWithoutProperties.js
	function _objectWithoutProperties$1(e, t) {
		if (null == e) return {};
		var o;
		var r;
		var i = _objectWithoutPropertiesLoose$1(e, t);
		if (Object.getOwnPropertySymbols) {
			var n = Object.getOwnPropertySymbols(e);
			for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
		}
		return i;
	}
	var init_objectWithoutProperties$1 = __esmMin((() => {
		init_objectWithoutPropertiesLoose$1();
		__name(_objectWithoutProperties$1, "_objectWithoutProperties");
	}));

//#endregion
//#region modules/ai/assets/js/editor/components/loader.js
	function ownKeys$32(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	function _objectSpread$31(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$32(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$32(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	var import_prop_types$12, _excluded$33, Loader;
	var init_loader = __esmMin((() => {
		init_extends();
		init_defineProperty$1();
		init_objectWithoutProperties$1();
		import_prop_types$12 = /* @__PURE__ */ __toESM(require_prop_types());
		_excluded$33 = ["sx", "BoxProps"];
		__name(ownKeys$32, "ownKeys");
		__name(_objectSpread$31, "_objectSpread");
		Loader = function Loader(_ref) {
			var _ref$sx = _ref.sx;
			var sx = _ref$sx === void 0 ? {} : _ref$sx;
			var _ref$BoxProps = _ref.BoxProps;
			var BoxProps = _ref$BoxProps === void 0 ? {} : _ref$BoxProps;
			var props = _objectWithoutProperties$1(_ref, _excluded$33);
			return /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, _extends$1({
				width: "100%",
				display: "flex",
				alignItems: "center"
			}, BoxProps, { sx: _objectSpread$31({
				px: 1.5,
				minHeight: function minHeight(theme) {
					return theme.spacing(5);
				}
			}, BoxProps.sx || {}) }), /*#__PURE__*/ react.default.createElement(_elementor_ui.LinearProgress, _extends$1({ color: "secondary" }, props, { sx: _objectSpread$31({ width: "100%" }, sx) })));
		};
		Loader.propTypes = {
			sx: import_prop_types$12.default.object,
			BoxProps: import_prop_types$12.default.object
		};
	}));

//#endregion
//#region modules/ai/assets/js/editor/components/ui/search-field.js
	var import_prop_types$11, StyledTextField$1, SearchField;
	var init_search_field = __esmMin((() => {
		init_extends();
		import_prop_types$11 = /* @__PURE__ */ __toESM(require_prop_types());
		StyledTextField$1 = (0, _elementor_ui.styled)(_elementor_ui.TextField)(function() {
			return { ".wp-admin & .MuiInputBase-input, & .MuiInputBase-input:focus": {
				backgroundColor: "initial",
				boxShadow: "none",
				border: 0,
				color: "inherit",
				outline: 0,
				/**
				* TODO:
				* Find a better solution to get the component padding value dynamically or fix the global CSS override.
				* These values are taken from Material UI's source code (the left padding reset was added to match the component style).
				*/
				padding: "16.5px 14px 16.5px 0",
				"&.MuiInputBase-inputSizeSmall": { padding: "8.5px 14px 8.5px 0" }
			} };
		});
		SearchField = (0, react.forwardRef)(function(props, ref) {
			return /*#__PURE__*/ react.default.createElement(StyledTextField$1, _extends$1({
				autoFocus: true,
				fullWidth: true,
				required: true,
				size: "small",
				color: "secondary",
				InputProps: {
					autoComplete: "off",
					startAdornment: /*#__PURE__*/ react.default.createElement(_elementor_ui.InputAdornment, { position: "start" }, /*#__PURE__*/ react.default.createElement(_elementor_icons.SearchIcon, null))
				},
				placeholder: props.placeholder,
				name: props.name,
				value: props.value,
				onChange: props.onChange
			}, props, { inputRef: ref }));
		});
		SearchField.propTypes = {
			placeholder: import_prop_types$11.default.string,
			name: import_prop_types$11.default.string,
			value: import_prop_types$11.default.string,
			onChange: import_prop_types$11.default.func
		};
	}));

//#endregion
//#region modules/ai/assets/js/editor/components/prompt-search.js
	var PromptSearch;
	var init_prompt_search = __esmMin((() => {
		init_extends();
		init_search_field();
		PromptSearch = (0, react.forwardRef)(function(props, ref) {
			return /*#__PURE__*/ react.default.createElement(SearchField, _extends$1({
				name: "prompt",
				placeholder: (0, _wordpress_i18n.__)("Describe the text and tone you want to use", "elementor") + "..."
			}, props, { ref }));
		});
	}));

//#endregion
//#region modules/ai/assets/js/editor/components/textarea.js
	var import_prop_types$10, Textarea;
	var init_textarea = __esmMin((() => {
		init_extends();
		import_prop_types$10 = /* @__PURE__ */ __toESM(require_prop_types());
		Textarea = (0, react.forwardRef)(function(props, ref) {
			return /*#__PURE__*/ react.default.createElement(_elementor_ui.TextField, _extends$1({
				inputRef: ref,
				multiline: true,
				minRows: 4,
				maxRows: 20,
				color: "secondary"
			}, props));
		});
		Textarea.propTypes = {
			value: import_prop_types$10.default.string,
			onChange: import_prop_types$10.default.func,
			helperText: import_prop_types$10.default.string
		};
	}));

//#endregion
//#region modules/ai/assets/js/editor/components/prompt-suggestions.js
	var import_prop_types$9, PromptSuggestions;
	var init_prompt_suggestions = __esmMin((() => {
		import_prop_types$9 = /* @__PURE__ */ __toESM(require_prop_types());
		PromptSuggestions = function PromptSuggestions(props) {
			var _props$suggestions;
			return /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, null, /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, {
				variant: "subtitle1",
				color: "text.secondary"
			}, (0, _wordpress_i18n.__)("Suggested prompts", "elementor") + ":"), /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
				direction: "column",
				alignItems: "flex-start",
				gap: 1,
				sx: { my: 1 }
			}, (_props$suggestions = props.suggestions) === null || _props$suggestions === void 0 ? void 0 : _props$suggestions.map(function(option, index) {
				var _props$suggestionFilt;
				return /*#__PURE__*/ react.default.createElement(_elementor_ui.Chip, {
					key: index,
					variant: "outlined",
					size: "large",
					color: "secondary",
					label: ((_props$suggestionFilt = props.suggestionFilter) === null || _props$suggestionFilt === void 0 ? void 0 : _props$suggestionFilt.call(props, option)) || option,
					onClick: function onClick() {
						return props.onSelect(option);
					}
				});
			})), props.children);
		};
		PromptSuggestions.propTypes = {
			suggestions: import_prop_types$9.default.arrayOf(import_prop_types$9.default.string),
			onSelect: import_prop_types$9.default.func.isRequired,
			suggestionFilter: import_prop_types$9.default.func,
			children: import_prop_types$9.default.node
		};
	}));

//#endregion
//#region modules/ai/assets/js/editor/components/prompt-library-link.js
	var import_prop_types$8, PromptLibraryLink;
	var init_prompt_library_link = __esmMin((() => {
		import_prop_types$8 = /* @__PURE__ */ __toESM(require_prop_types());
		PromptLibraryLink = function PromptLibraryLink(props) {
			return /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, {
				variant: "body2",
				color: "text.secondary"
			}, (0, _wordpress_i18n.__)("For more suggestions, explore our"), " ", /*#__PURE__*/ react.default.createElement(_elementor_ui.Link, {
				href: props.libraryLink,
				className: "elementor-clickable",
				target: "_blank"
			}, (0, _wordpress_i18n.__)("prompt library")));
		};
		PromptLibraryLink.propTypes = { libraryLink: import_prop_types$8.default.string };
	}));

//#endregion
//#region modules/ai/assets/js/editor/components/prompt-action-selection.js
init_requests_ids();
init_asyncToGenerator();
var import_regenerator$3 = /* @__PURE__ */ __toESM(require_regenerator());
init_loader();
init_prompt_search();
init_textarea();
init_prompt_suggestions();
init_prompt_library_link();
	var import_prop_types$7 = /* @__PURE__ */ __toESM(require_prop_types());
	var labelToDashCash = function labelToDashCash(str) {
		return str.toLowerCase().replace(/ /g, "-");
	};
	var PromptActionSelection = function PromptActionSelection(props) {
		var actionId = labelToDashCash(props.label);
		var _props$wrapperStyle = props.wrapperStyle;
		var wrapperStyle = _props$wrapperStyle === void 0 ? { maxWidth: 138 } : _props$wrapperStyle;
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.FormControl, {
			size: "small",
			color: "secondary",
			fullWidth: true,
			sx: wrapperStyle
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.InputLabel, { id: actionId }, props.label), /*#__PURE__*/ react.default.createElement(_elementor_ui.Select, {
			id: actionId,
			labelId: actionId,
			label: props.label,
			value: props.value || "",
			onChange: props.onChange,
			disabled: props.disabled,
			sx: { "&.Mui-disabled .MuiSelect-nativeInput": {
				backgroundColor: "initial",
				opacity: 0
			} }
		}, props.options.map(function(option) {
			var _option$value;
			return /*#__PURE__*/ react.default.createElement(_elementor_ui.MenuItem, {
				dense: true,
				key: option.label,
				value: (_option$value = option.value) !== null && _option$value !== void 0 ? _option$value : option.label
			}, option.label);
		})));
	};
	PromptActionSelection.propTypes = {
		label: import_prop_types$7.default.string.isRequired,
		options: import_prop_types$7.default.arrayOf(import_prop_types$7.default.shape({
			label: import_prop_types$7.default.string.isRequired,
			value: import_prop_types$7.default.string
		})).isRequired,
		onChange: import_prop_types$7.default.func.isRequired,
		value: import_prop_types$7.default.string,
		wrapperStyle: import_prop_types$7.default.object,
		disabled: import_prop_types$7.default.bool
	};

//#endregion
//#region modules/ai/assets/js/editor/components/generate-button.js
	var GenerateButton$1;
	var init_generate_button = __esmMin((() => {
		init_extends();
		GenerateButton$1 = function GenerateButton(props) {
			return /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, _extends$1({
				variant: "contained",
				endIcon: /*#__PURE__*/ react.default.createElement(_elementor_icons.AIIcon, { fontSize: "small" }),
				disabled: !prompt,
				"aria-label": (0, _wordpress_i18n.__)("search", "elementor"),
				type: "submit",
				size: "small"
			}, props));
		};
	}));

//#endregion
//#region modules/ai/assets/js/editor/components/prompt-action.js
init_generate_button();
	init_extends();
	var PromptAction = function PromptAction(props) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Chip, _extends$1({
			size: "large",
			color: "secondary",
			variant: "outlined"
		}, props));
	};

//#endregion
//#region modules/ai/assets/js/editor/components/prompt-error-message.js
	var import_prop_types$6, _excluded$32, PromptErrorMessage;
	var init_prompt_error_message = __esmMin((() => {
		init_extends();
		init_objectWithoutProperties$1();
		import_prop_types$6 = /* @__PURE__ */ __toESM(require_prop_types());
		_excluded$32 = [
			"error",
			"onRetry",
			"actionPosition"
		];
		PromptErrorMessage = function PromptErrorMessage(_ref) {
			var error = _ref.error;
			var _ref$onRetry = _ref.onRetry;
			var onRetry = _ref$onRetry === void 0 ? function() {} : _ref$onRetry;
			var _ref$actionPosition = _ref.actionPosition;
			var actionPosition = _ref$actionPosition === void 0 ? "default" : _ref$actionPosition;
			var props = _objectWithoutProperties$1(_ref, _excluded$32);
			function getQuotaReachedTrailMessage(featureName) {
				if (!featureName) return {
					text: /*#__PURE__*/ react.default.createElement(_elementor_ui.AlertTitle, null, (0, _wordpress_i18n.__)("It's time to upgrade.", "elementor")),
					description: (0, _wordpress_i18n.__)("Enjoy the free trial? Upgrade now for unlimited access to built-in image, text and custom code generators.", "elementor"),
					buttonText: (0, _wordpress_i18n.__)("Upgrade", "elementor"),
					buttonAction: function buttonAction() {
						return window.open("https://go.elementor.com/ai-popup-purchase-limit-reached/", "_blank");
					}
				};
				return {
					text: /*#__PURE__*/ react.default.createElement(_elementor_ui.AlertTitle, null, (0, _wordpress_i18n.sprintf)((0, _wordpress_i18n.__)("You've used all AI credits for %s.", "elementor"), featureName.toLowerCase())),
					description: (0, _wordpress_i18n.__)("Upgrade now to keep using this feature. You still have credits for other AI features (Text, Code, Images, Containers, etc.)", "elementor"),
					buttonText: (0, _wordpress_i18n.__)("Upgrade now", "elementor"),
					buttonAction: function buttonAction() {
						return window.open("https://go.elementor.com/ai-popup-purchase-limit-reached/", "_blank");
					}
				};
			}
			function getErrorMessage() {
				var _error$extra_data;
				var errMsg = error.message || error;
				var featureName = (_error$extra_data = error.extra_data) === null || _error$extra_data === void 0 ? void 0 : _error$extra_data.featureName;
				var messages = {
					default: {
						text: /*#__PURE__*/ react.default.createElement(_elementor_ui.AlertTitle, null, (0, _wordpress_i18n.__)("There was a glitch.", "elementor")),
						description: (0, _wordpress_i18n.__)("Wait a moment and give it another go, or try tweaking the prompt.", "elementor"),
						buttonText: (0, _wordpress_i18n.__)("Try again", "elementor"),
						buttonAction: onRetry
					},
					service_outage_internal: {
						text: /*#__PURE__*/ react.default.createElement(_elementor_ui.AlertTitle, null, (0, _wordpress_i18n.__)("There was a glitch.", "elementor")),
						description: (0, _wordpress_i18n.__)("Wait a moment and give it another go.", "elementor"),
						buttonText: (0, _wordpress_i18n.__)("Try again", "elementor"),
						buttonAction: onRetry
					},
					invalid_connect_data: {
						text: /*#__PURE__*/ react.default.createElement(_elementor_ui.AlertTitle, null, (0, _wordpress_i18n.__)("There was a glitch.", "elementor")),
						description: /*#__PURE__*/ react.default.createElement(react.default.Fragment, null, (0, _wordpress_i18n.__)("Try exiting Elementor and sign in again.", "elementor"), " ", /*#__PURE__*/ react.default.createElement("a", {
							href: "https://elementor.com/help/disconnecting-reconnecting-your-elementor-account/",
							target: "_blank",
							rel: "noreferrer"
						}, (0, _wordpress_i18n.__)("Show me how", "elementor"))),
						buttonText: (0, _wordpress_i18n.__)("Reconnect", "elementor"),
						buttonAction: function buttonAction() {
							return window.open(window.ElementorAiConfig.connect_url);
						}
					},
					not_connected: {
						text: /*#__PURE__*/ react.default.createElement(_elementor_ui.AlertTitle, null, (0, _wordpress_i18n.__)("You aren't connected to Elementor AI.", "elementor")),
						description: (0, _wordpress_i18n.__)("Elementor AI is just a few clicks away. Connect your account to instantly create texts and custom code.", "elementor"),
						buttonText: (0, _wordpress_i18n.__)("Connect", "elementor"),
						buttonAction: function buttonAction() {
							return window.open(window.ElementorAiConfig.connect_url);
						}
					},
					quota_reached_trail: getQuotaReachedTrailMessage(featureName),
					quota_reached_subscription: {
						text: /*#__PURE__*/ react.default.createElement(_elementor_ui.AlertTitle, null, (0, _wordpress_i18n.__)("Looks like you're out of credits.", "elementor")),
						description: (0, _wordpress_i18n.__)("Ready to take it to the next level?", "elementor"),
						buttonText: (0, _wordpress_i18n.__)("Upgrade now", "elementor"),
						buttonAction: function buttonAction() {
							return window.open("https://go.elementor.com/ai-popup-purchase-limit-reached/", "_blank");
						}
					},
					rate_limit_network: {
						text: /*#__PURE__*/ react.default.createElement(_elementor_ui.AlertTitle, null, (0, _wordpress_i18n.__)("Whoa! Slow down there.", "elementor")),
						description: (0, _wordpress_i18n.__)("We can’t process that many requests so fast. Try again in 15 minutes.", "elementor")
					},
					invalid_prompts: {
						text: /*#__PURE__*/ react.default.createElement(_elementor_ui.AlertTitle, null, (0, _wordpress_i18n.__)("We were unable to generate that prompt.", "elementor")),
						description: (0, _wordpress_i18n.__)("Seems like the prompt contains words that could generate harmful content. Write a different prompt to continue.", "elementor")
					},
					service_unavailable: {
						text: /*#__PURE__*/ react.default.createElement(_elementor_ui.AlertTitle, null, (0, _wordpress_i18n.__)("There was a glitch.", "elementor")),
						description: (0, _wordpress_i18n.__)("Wait a moment and give it another go, or try tweaking the prompt.", "elementor"),
						buttonText: (0, _wordpress_i18n.__)("Try again", "elementor"),
						buttonAction: onRetry
					},
					request_timeout_error: {
						text: /*#__PURE__*/ react.default.createElement(_elementor_ui.AlertTitle, null, (0, _wordpress_i18n.__)("There was a glitch.", "elementor")),
						description: (0, _wordpress_i18n.__)("Wait a moment and give it another go, or try tweaking the prompt.", "elementor"),
						buttonText: (0, _wordpress_i18n.__)("Try again", "elementor"),
						buttonAction: onRetry
					},
					invalid_token: {
						text: /*#__PURE__*/ react.default.createElement(_elementor_ui.AlertTitle, null, (0, _wordpress_i18n.__)("Try again", "elementor")),
						description: (0, _wordpress_i18n.__)("Try exiting Elementor and sign in again.", "elementor"),
						buttonText: (0, _wordpress_i18n.__)("Reconnect", "elementor"),
						buttonAction: onRetry
					},
					file_too_large: {
						text: /*#__PURE__*/ react.default.createElement(_elementor_ui.AlertTitle, null, (0, _wordpress_i18n.__)("The file is too large.", "elementor")),
						description: (0, _wordpress_i18n.__)("Please upload a file that is less than 4MB.", "elementor")
					},
					image_resolution_maximum_exceeded: {
						text: /*#__PURE__*/ react.default.createElement(_elementor_ui.AlertTitle, null, (0, _wordpress_i18n.__)("The image resolution exceeds the maximum allowed size.", "elementor")),
						description: (0, _wordpress_i18n.__)("Please upload a file with dimensions less than 2048x2048 pixels.", "elementor")
					},
					external_service_unavailable: {
						text: /*#__PURE__*/ react.default.createElement(_elementor_ui.AlertTitle, null, (0, _wordpress_i18n.__)("Temporary external service issue", "elementor")),
						description: (0, _wordpress_i18n.__)("It seems that one of our partner services is temporarily unavailable. Please try again in a few minutes.", "elementor"),
						buttonText: (0, _wordpress_i18n.__)("Try Again", "elementor"),
						buttonAction: onRetry
					}
				};
				return messages[errMsg] || messages.default;
			}
			var message = getErrorMessage();
			var action = (message === null || message === void 0 ? void 0 : message.buttonText) && /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, {
				color: "inherit",
				size: "small",
				variant: "outlined",
				onClick: message.buttonAction
			}, message.buttonText);
			return /*#__PURE__*/ react.default.createElement(_elementor_ui.Alert, _extends$1({
				severity: message.severity || "error",
				action: "default" === actionPosition && action
			}, props), message.text, message.description, "bottom" === actionPosition && /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, { sx: { mt: 1 } }, action));
		};
		PromptErrorMessage.propTypes = {
			error: import_prop_types$6.default.oneOfType([import_prop_types$6.default.object, import_prop_types$6.default.string]),
			onRetry: import_prop_types$6.default.func,
			actionPosition: import_prop_types$6.default.oneOf(["default", "bottom"])
		};
	}));

//#endregion
//#region modules/ai/assets/js/editor/api/index.js
	function ownKeys$31(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	function _objectSpread$30(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$31(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$31(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	var request, getUserInformation, getRemoteConfig, getCompletionText, getEditText, getCustomCode, getCustomCSS, setGetStarted, setStatusFeedback, getTextToImageGeneration, getImageToImageGeneration, getImageToImageMaskCleanup, getImageToImageMaskGeneration, getImageToImageOutPainting, getImageToImageUpscale, getImageToImageRemoveBackground, getImageToImageIsolateObjects, getImageToImageReplaceBackground, getImagePromptEnhanced, getProductImageUnification, getAnimation, uploadImage, generateLayout, getLayoutPromptEnhanced, getHistory, deleteHistoryItem;
	var init_api = __esmMin((() => {
		init_defineProperty$1();
		__name(ownKeys$31, "ownKeys");
		__name(_objectSpread$30, "_objectSpread");
		request = function request(endpoint) {
			var data = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
			var immediately = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : false;
			var signal = arguments.length > 3 ? arguments[3] : void 0;
			if (Object.keys(data).length) if (window.elementorAiCurrentContext) data.context = window.elementorAiCurrentContext;
			else data.context = window.elementorWpAiCurrentContext;
			return new Promise(function(resolve, reject) {
				var ajaxData = elementorCommon.ajax.addRequest(endpoint, {
					success: resolve,
					error: reject,
					data,
					unique_id: data.unique_id
				}, immediately);
				if (signal && ajaxData.jqXhr) signal.addEventListener("abort", ajaxData.jqXhr.abort);
			});
		};
		getUserInformation = function getUserInformation(immediately) {
			return request("ai_get_user_information", void 0, immediately);
		};
		getRemoteConfig = function getRemoteConfig() {
			return request("ai_get_remote_config");
		};
		getCompletionText = function getCompletionText(payload) {
			return request("ai_get_completion_text", { payload });
		};
		getEditText = function getEditText(payload) {
			return request("ai_get_edit_text", { payload });
		};
		getCustomCode = function getCustomCode(payload) {
			return request("ai_get_custom_code", { payload });
		};
		getCustomCSS = function getCustomCSS(payload) {
			return request("ai_get_custom_css", { payload });
		};
		setGetStarted = function setGetStarted() {
			return request("ai_set_get_started");
		};
		setStatusFeedback = function setStatusFeedback(responseId) {
			return request("ai_set_status_feedback", { response_id: responseId }, true);
		};
		getTextToImageGeneration = function getTextToImageGeneration(payload) {
			return request("ai_get_text_to_image", { payload });
		};
		getImageToImageGeneration = function getImageToImageGeneration(payload) {
			return request("ai_get_image_to_image", { payload });
		};
		getImageToImageMaskCleanup = function getImageToImageMaskCleanup(payload) {
			return request("ai_get_image_to_image_mask_cleanup", { payload });
		};
		getImageToImageMaskGeneration = function getImageToImageMaskGeneration(payload) {
			return request("ai_get_image_to_image_mask", { payload });
		};
		getImageToImageOutPainting = function getImageToImageOutPainting(payload) {
			return request("ai_get_image_to_image_outpainting", { payload });
		};
		getImageToImageUpscale = function getImageToImageUpscale(payload) {
			return request("ai_get_image_to_image_upscale", { payload });
		};
		getImageToImageRemoveBackground = function getImageToImageRemoveBackground(payload) {
			return request("ai_get_image_to_image_remove_background", { payload });
		};
		getImageToImageIsolateObjects = function getImageToImageIsolateObjects(payload) {
			return request("ai_get_image_to_image_isolate_objects", { payload });
		};
		getImageToImageReplaceBackground = function getImageToImageReplaceBackground(payload) {
			return request("ai_get_image_to_image_replace_background", { payload });
		};
		getImagePromptEnhanced = function getImagePromptEnhanced(prompt) {
			return request("ai_get_image_prompt_enhancer", { prompt });
		};
		getProductImageUnification = function getProductImageUnification(payload, immediately) {
			return request("ai_get_product_image_unification", { payload }, immediately);
		};
		getAnimation = function getAnimation(payload) {
			return request("ai_get_animation", { payload });
		};
		uploadImage = function uploadImage(image) {
			return request("ai_upload_image", _objectSpread$30(_objectSpread$30({}, image), {}, {
				editor_post_id: image.image.editor_post_id,
				unique_id: image.image.unique_id
			}));
		};
		generateLayout = function generateLayout(requestBody, signal) {
			return request("ai_generate_layout", requestBody, true, signal);
		};
		getLayoutPromptEnhanced = function getLayoutPromptEnhanced(prompt, enhanceType) {
			return request("ai_get_layout_prompt_enhancer", {
				prompt,
				enhance_type: enhanceType
			});
		};
		getHistory = function getHistory(type, page, limit) {
			return request("ai_get_history", {
				type,
				page,
				limit
			});
		};
		deleteHistoryItem = function deleteHistoryItem(id) {
			return request("ai_delete_history_item", { id });
		};
	}));

//#endregion
//#region modules/ai/assets/js/editor/hooks/use-prompt.js
	function ownKeys$30(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	function _objectSpread$29(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$30(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$30(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	var _excluded$31, normalizeResponse, usePrompt;
	var init_use_prompt = __esmMin((() => {
		init_defineProperty$1();
		init_slicedToArray();
		init_objectWithoutProperties$1();
		init_api();
		_excluded$31 = [
			"text",
			"response_id",
			"usage",
			"images"
		];
		__name(ownKeys$30, "ownKeys");
		__name(_objectSpread$29, "_objectSpread");
		normalizeResponse = function normalizeResponse(_ref) {
			var text = _ref.text;
			var responseId = _ref.response_id;
			var usage = _ref.usage;
			var images = _ref.images;
			var optional = _objectWithoutProperties$1(_ref, _excluded$31);
			var creditsData = usage ? usage.quota - usage.usedQuota : 0;
			var normalized = {
				result: text || images,
				responseId,
				credits: Math.max(creditsData, 0),
				usagePercentage: usage === null || usage === void 0 ? void 0 : usage.usagePercentage
			};
			if (optional.base_template_id) normalized.baseTemplateId = optional.base_template_id;
			normalized.type = optional.template_type;
			return normalized;
		};
		usePrompt = function usePrompt(fetchData, initialState) {
			var _useState2 = _slicedToArray((0, react.useState)(false), 2);
			var isLoading = _useState2[0];
			var setIsLoading = _useState2[1];
			var _useState4 = _slicedToArray((0, react.useState)(""), 2);
			var error = _useState4[0];
			var setError = _useState4[1];
			var _useState6 = _slicedToArray((0, react.useState)(initialState), 2);
			var data = _useState6[0];
			var setData = _useState6[1];
			var _useRequestIds = useRequestIds();
			var updateUsagePercentage = _useRequestIds.updateUsagePercentage;
			var usagePercentage = _useRequestIds.usagePercentage;
			var send = (0, react.useRef)(/*#__PURE__*/ function() {
				var _ref2 = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$3.default.mark(function _callee(payload) {
					return import_regenerator$3.default.wrap(function(_context) {
						while (1) switch (_context.prev = _context.next) {
							case 0: return _context.abrupt("return", payload);
							case 1:
							case "end": return _context.stop();
						}
					}, _callee);
				}));
				return function(_x) {
					return _ref2.apply(this, arguments);
				};
			}());
			var sendUsageData = (0, react.useRef)(function() {});
			(0, react.useEffect)(function() {
				var newUsageValue = data === null || data === void 0 ? void 0 : data.usagePercentage;
				if (newUsageValue && newUsageValue !== usagePercentage) updateUsagePercentage(newUsageValue);
			}, [
				data,
				usagePercentage,
				updateUsagePercentage
			]);
			var _useRequestIds2 = useRequestIds();
			var setRequest = _useRequestIds2.setRequest;
			var editorSessionId = _useRequestIds2.editorSessionId;
			var sessionId = _useRequestIds2.sessionId;
			var generateId = _useRequestIds2.generateId;
			var batchId = _useRequestIds2.batchId;
			send.current = (0, react.useCallback)(/*#__PURE__*/ function() {
				var _ref3 = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$3.default.mark(function _callee2(payload) {
					return import_regenerator$3.default.wrap(function(_context2) {
						while (1) switch (_context2.prev = _context2.next) {
							case 0: return _context2.abrupt("return", new Promise(function(resolve, reject) {
								setError("");
								setIsLoading(true);
								var requestId = setRequest();
								var requestIds = {
									editorSessionId: editorSessionId.current,
									sessionId: sessionId.current,
									generateId: generateId.current,
									batchId: batchId.current,
									requestId: requestId.current
								};
								payload = _objectSpread$29(_objectSpread$29({}, payload), {}, { requestIds });
								fetchData(payload).then(function(result) {
									var normalizedData = normalizeResponse(result);
									setData(normalizedData);
									resolve(normalizedData);
								}).catch(function(err) {
									var finalError = (err === null || err === void 0 ? void 0 : err.responseText) || err;
									setError(finalError);
									reject(finalError);
								}).finally(function() {
									return setIsLoading(false);
								});
							}));
							case 1:
							case "end": return _context2.stop();
						}
					}, _callee2);
				}));
				return function(_x2) {
					return _ref3.apply(this, arguments);
				};
			}(), [
				batchId,
				editorSessionId,
				fetchData,
				generateId,
				sessionId,
				setRequest
			]);
			sendUsageData.current = (0, react.useCallback)(function() {
				var usageData = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : data;
				return usageData.responseId && setStatusFeedback(usageData.responseId);
			}, [data]);
			return {
				isLoading,
				error,
				data,
				setResult: function setResult(result) {
					var responseId = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : null;
					var updatedResult = _objectSpread$29({}, data);
					updatedResult.result = result;
					if (responseId) updatedResult.responseId = responseId;
					setData(updatedResult);
				},
				reset: function reset() {
					setData(function(_ref4) {
						return {
							credits: _ref4.credits,
							result: "",
							responseId: ""
						};
					});
					setError("");
					setIsLoading(false);
				},
				send: send.current,
				sendUsageData: sendUsageData.current
			};
		};
	}));

//#endregion
//#region modules/ai/assets/js/editor/hooks/use-text-prompt.js
init_prompt_error_message();
	init_asyncToGenerator();
	var import_regenerator$2 = /* @__PURE__ */ __toESM(require_regenerator());
	init_api();
	init_use_prompt();
	var getTextResult = /*#__PURE__*/ function() {
		var _ref = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee(payload) {
			return import_regenerator$2.default.wrap(function(_context) {
				while (1) switch (_context.prev = _context.next) {
					case 0:
						if (!(payload !== null && payload !== void 0 && payload.instruction)) {
							_context.next = 1;
							break;
						}
						return _context.abrupt("return", getEditText(payload));
					case 1: return _context.abrupt("return", getCompletionText(payload));
					case 2:
					case "end": return _context.stop();
				}
			}, _callee);
		}));
		return function getTextResult(_x) {
			return _ref.apply(this, arguments);
		};
	}();
	var useTextPrompt = function useTextPrompt(initialValue) {
		return usePrompt(getTextResult, initialValue);
	};

//#endregion
//#region modules/ai/assets/js/editor/actions-data.js
	var textAutocomplete, textareaAutocomplete, codeHtmlAutocomplete, codeCssAutocomplete, vocalTones, translateLanguages, motionEffectAutocomplete, hoverEffectAutocomplete;
	var init_actions_data = __esmMin((() => {
		textAutocomplete = [
			(0, _wordpress_i18n.__)("Craft a catchy slogan for", "elementor"),
			(0, _wordpress_i18n.__)("Write a strong section header for", "elementor"),
			(0, _wordpress_i18n.__)("Create a blog title for", "elementor"),
			(0, _wordpress_i18n.__)("Suggest a 4 word headline for", "elementor"),
			(0, _wordpress_i18n.__)("Write an attention grabbing title for", "elementor")
		];
		textareaAutocomplete = [
			(0, _wordpress_i18n.__)("Write a product description for", "elementor"),
			(0, _wordpress_i18n.__)("Create a blog post for", "elementor"),
			(0, _wordpress_i18n.__)("Craft an about us section for", "elementor"),
			(0, _wordpress_i18n.__)("Offer a few sentences to invite visitors to", "elementor"),
			(0, _wordpress_i18n.__)("Write microcopy for a submission form that includes", "elementor")
		];
		codeHtmlAutocomplete = [
			(0, _wordpress_i18n.__)("GA event fired on every button element click (assume GA was loaded)", "elementor"),
			(0, _wordpress_i18n.__)("Iframe code to embed a Spotify widget on my web page. My ID is 1234567890", "elementor"),
			(0, _wordpress_i18n.__)("Write an embed code of Google Analytics", "elementor"),
			(0, _wordpress_i18n.__)("Write an embed code of Facebook Pixel", "elementor"),
			(0, _wordpress_i18n.__)("HTML code that embeds the following PDF into a webpage: link to PDF", "elementor")
		];
		codeCssAutocomplete = [
			(0, _wordpress_i18n.__)("On hover, animate 20% bigger, rotate 10 deg CCW", "elementor"),
			(0, _wordpress_i18n.__)("Add a circle-shaped clip mask, semi transparent on hover", "elementor"),
			(0, _wordpress_i18n.__)("Rainbow colored animated text gradient", "elementor"),
			(0, _wordpress_i18n.__)("Add a 3D shadow to the text", "elementor")
		];
		vocalTones = [
			{ label: (0, _wordpress_i18n.__)("Casual", "elementor") },
			{ label: (0, _wordpress_i18n.__)("Confidence", "elementor") },
			{ label: (0, _wordpress_i18n.__)("Formal", "elementor") },
			{ label: (0, _wordpress_i18n.__)("Friendly", "elementor") },
			{ label: (0, _wordpress_i18n.__)("Inspirational", "elementor") },
			{ label: (0, _wordpress_i18n.__)("Motivational", "elementor") },
			{ label: (0, _wordpress_i18n.__)("Nostalgic", "elementor") },
			{ label: (0, _wordpress_i18n.__)("Playful", "elementor") },
			{ label: (0, _wordpress_i18n.__)("Professional", "elementor") },
			{ label: (0, _wordpress_i18n.__)("Scientific", "elementor") },
			{ label: (0, _wordpress_i18n.__)("Straightforward", "elementor") },
			{ label: (0, _wordpress_i18n.__)("Witty", "elementor") }
		];
		translateLanguages = [
			{
				label: (0, _wordpress_i18n.__)("Arabic", "elementor"),
				value: "ar-SA"
			},
			{
				label: (0, _wordpress_i18n.__)("Chinese", "elementor"),
				value: "zh-CN"
			},
			{
				label: (0, _wordpress_i18n.__)("Czech", "elementor"),
				value: "cs-CZ"
			},
			{
				label: (0, _wordpress_i18n.__)("Danish", "elementor"),
				value: "da-DK"
			},
			{
				label: (0, _wordpress_i18n.__)("Dutch", "elementor"),
				value: "nl-NL"
			},
			{
				label: (0, _wordpress_i18n.__)("English", "elementor"),
				value: "en-US"
			},
			{
				label: (0, _wordpress_i18n.__)("Finnish", "elementor"),
				value: "fi-FI"
			},
			{
				label: (0, _wordpress_i18n.__)("French", "elementor"),
				value: "fr-FR"
			},
			{
				label: (0, _wordpress_i18n.__)("German", "elementor"),
				value: "de-DE"
			},
			{
				label: (0, _wordpress_i18n.__)("Greek", "elementor"),
				value: "el-GR"
			},
			{
				label: (0, _wordpress_i18n.__)("Hebrew", "elementor"),
				value: "he-IL"
			},
			{
				label: (0, _wordpress_i18n.__)("Hungarian", "elementor"),
				value: "hu-HU"
			},
			{
				label: (0, _wordpress_i18n.__)("Indonesian", "elementor"),
				value: "id-ID"
			},
			{
				label: (0, _wordpress_i18n.__)("Italian", "elementor"),
				value: "it-IT"
			},
			{
				label: (0, _wordpress_i18n.__)("Japanese", "elementor"),
				value: "ja-JP"
			},
			{
				label: (0, _wordpress_i18n.__)("Korean", "elementor"),
				value: "ko-KR"
			},
			{
				label: (0, _wordpress_i18n.__)("Persian", "elementor"),
				value: "fa-IR"
			},
			{
				label: (0, _wordpress_i18n.__)("Polish", "elementor"),
				value: "pl-PL"
			},
			{
				label: (0, _wordpress_i18n.__)("Portuguese", "elementor"),
				value: "pt-PT"
			},
			{
				label: (0, _wordpress_i18n.__)("Russian", "elementor"),
				value: "ru-RU"
			},
			{
				label: (0, _wordpress_i18n.__)("Spanish", "elementor"),
				value: "es-ES"
			},
			{
				label: (0, _wordpress_i18n.__)("Swedish", "elementor"),
				value: "sv-SE"
			},
			{
				label: (0, _wordpress_i18n.__)("Thai", "elementor"),
				value: "th-TH"
			},
			{
				label: (0, _wordpress_i18n.__)("Turkish", "elementor"),
				value: "tr-TR"
			},
			{
				label: (0, _wordpress_i18n.__)("Vietnamese", "elementor"),
				value: "vi-VN"
			}
		];
		motionEffectAutocomplete = [
			(0, _wordpress_i18n.__)("Create Fade out animation on scroll", "elementor"),
			(0, _wordpress_i18n.__)("Create a Parallax effect on scroll", "elementor"),
			(0, _wordpress_i18n.__)("On scroll, fade in with slight scaling up", "elementor"),
			(0, _wordpress_i18n.__)("Move and tilt opposite to mouse position", "elementor"),
			(0, _wordpress_i18n.__)("Slide in from the left on load", "elementor")
		];
		hoverEffectAutocomplete = [
			(0, _wordpress_i18n.__)("Move down on hover", "elementor"),
			(0, _wordpress_i18n.__)("Slightly shift right on hover", "elementor"),
			(0, _wordpress_i18n.__)("Apply a subtle 3D tilt", "elementor"),
			(0, _wordpress_i18n.__)("Rotate gently clockwise on hover", "elementor"),
			(0, _wordpress_i18n.__)("Scale down and move up on hover", "elementor")
		];
	}));

//#endregion
//#region modules/ai/assets/js/editor/components/prompt-history/history-types.js
	var HISTORY_TYPES, TRANSLATED_HISTORY_TYPES_FALLBACK, TRANSLATED_HISTORY_TYPES, getTranslatedPromptHistoryType;
	var init_history_types = __esmMin((() => {
		init_defineProperty$1();
		HISTORY_TYPES = Object.freeze({
			ALL: "all",
			TEXT: "text",
			CODE: "code",
			IMAGE: "images",
			BLOCK: "blocks"
		});
		TRANSLATED_HISTORY_TYPES_FALLBACK = (0, _wordpress_i18n.__)("things", "elementor");
		TRANSLATED_HISTORY_TYPES = Object.freeze(_defineProperty$1(_defineProperty$1(_defineProperty$1(_defineProperty$1({}, HISTORY_TYPES.TEXT, (0, _wordpress_i18n.__)("texts", "elementor")), HISTORY_TYPES.CODE, (0, _wordpress_i18n.__)("code", "elementor")), HISTORY_TYPES.IMAGE, (0, _wordpress_i18n.__)("images", "elementor")), HISTORY_TYPES.BLOCK, (0, _wordpress_i18n.__)("blocks", "elementor")));
		getTranslatedPromptHistoryType = function getTranslatedPromptHistoryType(historyType) {
			if (TRANSLATED_HISTORY_TYPES[historyType]) return TRANSLATED_HISTORY_TYPES[historyType];
			return TRANSLATED_HISTORY_TYPES_FALLBACK;
		};
	}));

//#endregion
//#region modules/ai/assets/js/editor/helpers/index.js
	var vh;
	var init_helpers = __esmMin((() => {
		vh = function vh(value) {
			return value * Math.max(document.documentElement.clientHeight, window.innerHeight || 0) / 100;
		};
	}));

//#endregion
//#region modules/ai/assets/js/editor/components/prompt-history/context/prompt-history-context.js
	var import_prop_types$5, PromptHistoryContext, DIALOG_CONTAINER_SELECTOR, getContainer, getContainerHeight, getContentHeight, setContentHeight, PromptHistoryProvider, usePromptHistoryContext;
	var init_prompt_history_context = __esmMin((() => {
		init_slicedToArray();
		import_prop_types$5 = /* @__PURE__ */ __toESM(require_prop_types());
		init_history_types();
		init_helpers();
		PromptHistoryContext = (0, react.createContext)({});
		DIALOG_CONTAINER_SELECTOR = ".e-ai-dialog-content";
		getContainer = function getContainer() {
			return document.querySelector(DIALOG_CONTAINER_SELECTOR);
		};
		getContainerHeight = function getContainerHeight() {
			var contentContainer = document.querySelector(DIALOG_CONTAINER_SELECTOR);
			return contentContainer === null || contentContainer === void 0 ? void 0 : contentContainer.clientHeight;
		};
		getContentHeight = function getContentHeight() {
			return document.querySelector(DIALOG_CONTAINER_SELECTOR).children[0].clientHeight + 50;
		};
		setContentHeight = function setContentHeight(height) {
			var contentContainer = document.querySelector(DIALOG_CONTAINER_SELECTOR);
			if ("auto" === height) {
				contentContainer.style.height = "auto";
				return;
			}
			contentContainer.style.height = "".concat(height, "px");
		};
		PromptHistoryProvider = function PromptHistoryProvider(_ref) {
			var historyType = _ref.historyType;
			var children = _ref.children;
			var showHistoryState = (0, react.useState)(false);
			return /*#__PURE__*/ react.default.createElement(PromptHistoryContext.Provider, { value: {
				historyType,
				showHistoryState
			} }, children);
		};
		PromptHistoryProvider.propTypes = {
			historyType: import_prop_types$5.default.oneOf(Object.values(HISTORY_TYPES)).isRequired,
			children: import_prop_types$5.default.node
		};
		usePromptHistoryContext = function usePromptHistoryContext() {
			var _useContext = (0, react.useContext)(PromptHistoryContext);
			var historyType = _useContext.historyType;
			var showHistoryState = _useContext.showHistoryState;
			var _showHistoryState = _slicedToArray(showHistoryState, 2);
			var showHistory = _showHistoryState[0];
			var setShowHistory = _showHistoryState[1];
			var _useState2 = _slicedToArray((0, react.useState)(false), 2);
			var showModalWrapper = _useState2[0];
			var setShowModalWrapper = _useState2[1];
			var _useState4 = _slicedToArray((0, react.useState)(false), 2);
			var showPromptHistory = _useState4[0];
			var setShowPromptHistory = _useState4[1];
			var handleFullScreenHistoryState = function handleFullScreenHistoryState() {
				if (showHistory) {
					setShowModalWrapper(true);
					setTimeout(function() {
						return setShowPromptHistory(true);
					}, 200);
					return;
				}
				setShowPromptHistory(false);
				setTimeout(function() {
					return setShowModalWrapper(false);
				}, 500);
			};
			var handleDefaultHistoryState = function handleDefaultHistoryState() {
				if (showHistory) {
					var currentHeight = getContentHeight();
					setContentHeight(currentHeight);
					setTimeout(function() {
						if (currentHeight < vh(61)) {
							setContentHeight(vh(61));
							setTimeout(function() {
								return setShowModalWrapper(true);
							}, 200);
							setTimeout(function() {
								return setShowPromptHistory(true);
							}, 300);
							return;
						}
						setShowModalWrapper(true);
						setShowPromptHistory(true);
					}, 20);
					return;
				}
				setShowPromptHistory(false);
				setTimeout(function() {
					setShowModalWrapper(false);
					setContentHeight(getContentHeight());
					setTimeout(function() {
						return setContentHeight("auto");
					}, 300);
				}, 500);
			};
			(0, react.useEffect)(function() {
				if (showHistory === showModalWrapper && showHistory === showPromptHistory) return;
				if (historyType === HISTORY_TYPES.IMAGE) return handleFullScreenHistoryState();
				handleDefaultHistoryState();
			}, [showHistory]);
			return {
				historyType,
				isOpen: showPromptHistory,
				isModalOpen: showModalWrapper,
				showHistory,
				onOpen: function onOpen() {
					setShowHistory(true);
				},
				onClose: function onClose() {
					setShowHistory(false);
				},
				getContainer,
				getContainerHeight
			};
		};
	}));

//#endregion
//#region modules/ai/assets/js/editor/components/prompt-history/context/prompt-history-action-context.js
	function _createForOfIteratorHelper$1(r, e) {
		var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"];
		if (!t) {
			if (Array.isArray(r) || (t = _unsupportedIterableToArray$1(r)) || e && r && "number" == typeof r.length) {
				t && (r = t);
				var _n = 0;
				var F = function F() {};
				return {
					s: F,
					n: function n() {
						return _n >= r.length ? { done: !0 } : {
							done: !1,
							value: r[_n++]
						};
					},
					e: function e(r) {
						throw r;
					},
					f: F
				};
			}
			throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
		}
		var o;
		var a = !0;
		var u = !1;
		return {
			s: function s() {
				t = t.call(r);
			},
			n: function n() {
				var r = t.next();
				return a = r.done, r;
			},
			e: function e(r) {
				u = !0, o = r;
			},
			f: function f() {
				try {
					a || null == t.return || t.return();
				} finally {
					if (u) throw o;
				}
			}
		};
	}
	function _unsupportedIterableToArray$1(r, a) {
		if (r) {
			if ("string" == typeof r) return _arrayLikeToArray$1(r, a);
			var t = {}.toString.call(r).slice(8, -1);
			return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray$1(r, a) : void 0;
		}
	}
	function _arrayLikeToArray$1(r, a) {
		(null == a || a > r.length) && (a = r.length);
		for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e];
		return n;
	}
	var import_prop_types$4, PromptHistoryActionContext, initialActionValue, ACTION_TYPES, ENABLED_ACTIONS_BY_TYPE, PromptHistoryActionProvider, usePromptHistoryAction, useSubscribeOnPromptHistoryAction;
	var init_prompt_history_action_context = __esmMin((() => {
		init_slicedToArray();
		init_defineProperty$1();
		import_prop_types$4 = /* @__PURE__ */ __toESM(require_prop_types());
		init_history_types();
		init_prompt_history_context();
		__name(_createForOfIteratorHelper$1, "_createForOfIteratorHelper");
		__name(_unsupportedIterableToArray$1, "_unsupportedIterableToArray");
		__name(_arrayLikeToArray$1, "_arrayLikeToArray");
		PromptHistoryActionContext = (0, react.createContext)({});
		initialActionValue = {
			type: "",
			id: "",
			data: null
		};
		ACTION_TYPES = Object.freeze({
			REUSE: "reuse",
			EDIT: "edit",
			RESTORE: "restore",
			REMOVE: "remove"
		});
		ENABLED_ACTIONS_BY_TYPE = _defineProperty$1(_defineProperty$1(_defineProperty$1({}, HISTORY_TYPES.TEXT, _defineProperty$1(_defineProperty$1(_defineProperty$1({}, ACTION_TYPES.REUSE, true), ACTION_TYPES.EDIT, true), ACTION_TYPES.REMOVE, true)), HISTORY_TYPES.CODE, _defineProperty$1(_defineProperty$1({}, ACTION_TYPES.REUSE, true), ACTION_TYPES.REMOVE, true)), HISTORY_TYPES.IMAGE, _defineProperty$1(_defineProperty$1({}, ACTION_TYPES.RESTORE, true), ACTION_TYPES.REMOVE, true));
		PromptHistoryActionProvider = function PromptHistoryActionProvider(_ref) {
			var children = _ref.children;
			var _useState2 = _slicedToArray((0, react.useState)(initialActionValue), 2);
			var state = _useState2[0];
			var dispatch = _useState2[1];
			return /*#__PURE__*/ react.default.createElement(PromptHistoryActionContext.Provider, { value: {
				state,
				dispatch
			} }, children);
		};
		PromptHistoryActionProvider.propTypes = { children: import_prop_types$4.default.node };
		usePromptHistoryAction = function usePromptHistoryAction() {
			var _useContext = (0, react.useContext)(PromptHistoryActionContext);
			var state = _useContext.state;
			var dispatch = _useContext.dispatch;
			var _usePromptHistoryCont = usePromptHistoryContext();
			var historyType = _usePromptHistoryCont.historyType;
			var onClose = _usePromptHistoryCont.onClose;
			var closeModalAfterAction = function closeModalAfterAction(action) {
				return function() {
					action.apply(void 0, arguments);
					onClose();
				};
			};
			return {
				promptHistoryAction: state,
				onPromptReuse: closeModalAfterAction(function onPromptReuse(id, data) {
					return dispatch({
						type: ACTION_TYPES.REUSE,
						id,
						data
					});
				}),
				onResultEdit: closeModalAfterAction(function onResultEdit(id, data) {
					return dispatch({
						type: ACTION_TYPES.EDIT,
						id,
						data
					});
				}),
				onImagesRestore: closeModalAfterAction(function onImagesRestore(id, data) {
					return dispatch({
						type: ACTION_TYPES.RESTORE,
						id,
						data
					});
				}),
				getAllowedActions: function getAllowedActions() {
					return ENABLED_ACTIONS_BY_TYPE[historyType] || {};
				}
			};
		};
		useSubscribeOnPromptHistoryAction = function useSubscribeOnPromptHistoryAction(actions) {
			var state = (0, react.useContext)(PromptHistoryActionContext).state;
			(0, react.useEffect)(function() {
				if (!state.type) return;
				var _iterator = _createForOfIteratorHelper$1(actions);
				var _step;
				try {
					for (_iterator.s(); !(_step = _iterator.n()).done;) {
						var action = _step.value;
						if (state.type === (action === null || action === void 0 ? void 0 : action.type)) action === null || action === void 0 || action.handler(state);
					}
				} catch (err) {
					_iterator.e(err);
				} finally {
					_iterator.f();
				}
			}, [state]);
		};
	}));

//#endregion
//#region modules/ai/assets/js/editor/icons/bulb-icon.js
	var BulbIcon;
	var init_bulb_icon = __esmMin((() => {
		init_extends();
		BulbIcon = react.default.forwardRef(function(props, ref) {
			return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({}, props, { ref }), /*#__PURE__*/ react.default.createElement("svg", {
				width: "22",
				height: "22",
				viewBox: "0 0 22 22",
				fill: "none",
				xmlns: "http://www.w3.org/2000/svg"
			}, /*#__PURE__*/ react.default.createElement("g", { clipPath: "url(#clip0_10743_8902)" }, /*#__PURE__*/ react.default.createElement("path", {
				d: "M2.75 10.0833H3.66667M11 2.75V3.66667M18.3333 10.0833H19.25M5.13333 5.13333L5.775 5.775M16.8667 5.13333L16.225 5.775",
				stroke: "#2563EB",
				strokeWidth: "1.5",
				strokeLinecap: "round",
				strokeLinejoin: "round"
			}), /*#__PURE__*/ react.default.createElement("path", {
				d: "M9.16675 16.041C8.70841 15.1243 6.91205 13.2842 6.62523 12.366C6.3384 11.4477 6.34775 10.4626 6.65195 9.54997C6.95615 8.63738 7.53978 7.84362 8.32016 7.28116C9.10054 6.71869 10.0381 6.41602 11.0001 6.41602C11.962 6.41602 12.8996 6.71869 13.68 7.28116C14.4604 7.84362 15.044 8.63738 15.3482 9.54997C15.6524 10.4626 15.6618 11.4477 15.3749 12.366C15.0881 13.2842 13.2917 15.1243 12.8334 16.041C12.8334 16.041 12.7597 17.3762 12.8334 17.8743C12.8334 18.3606 12.6403 18.8269 12.2964 19.1707C11.9526 19.5145 11.4863 19.7077 11.0001 19.7077C10.5139 19.7077 10.0475 19.5145 9.70372 19.1707C9.3599 18.8269 9.16675 18.3606 9.16675 17.8743C9.2405 17.3762 9.16675 16.041 9.16675 16.041Z",
				stroke: "#2563EB",
				strokeWidth: "1.5",
				strokeLinecap: "round",
				strokeLinejoin: "round"
			}), /*#__PURE__*/ react.default.createElement("path", {
				d: "M10.0833 16.5H11.9166",
				stroke: "#2563EB",
				strokeWidth: "1.5",
				strokeLinecap: "round",
				strokeLinejoin: "round"
			})), /*#__PURE__*/ react.default.createElement("defs", null, /*#__PURE__*/ react.default.createElement("clipPath", { id: "clip0_10743_8902" }, /*#__PURE__*/ react.default.createElement("rect", {
				width: "22",
				height: "22",
				fill: "white"
			})))));
		});
	}));

//#endregion
//#region modules/ai/assets/js/editor/hooks/use-introduction.js
	function useIntroduction(key) {
		var _window$elementor$con;
		var _window$elementorAdmi;
		var _globalConfig$introdu;
		var globalConfig = window.elementor ? (_window$elementor$con = window.elementor.config) === null || _window$elementor$con === void 0 ? void 0 : _window$elementor$con.user : (_window$elementorAdmi = window.elementorAdmin) === null || _window$elementorAdmi === void 0 || (_window$elementorAdmi = _window$elementorAdmi.config) === null || _window$elementorAdmi === void 0 ? void 0 : _window$elementorAdmi.user;
		var _useState2 = _slicedToArray((0, react.useState)(!!(globalConfig !== null && globalConfig !== void 0 && (_globalConfig$introdu = globalConfig.introduction) !== null && _globalConfig$introdu !== void 0 && _globalConfig$introdu[key])), 2);
		var isViewed = _useState2[0];
		var setIsViewed = _useState2[1];
		function markAsViewed() {
			if (!key) return Promise.reject();
			return new Promise(function(resolve, reject) {
				if (isViewed) reject();
				setIsViewed(true);
				elementorCommon.ajax.addRequest("introduction_viewed", {
					data: { introductionKey: key },
					error: function error() {
						setIsViewed(false);
						reject();
					},
					success: function success() {
						setIsViewed(true);
						if (globalConfig !== null && globalConfig !== void 0 && globalConfig.introduction) globalConfig.introduction[key] = true;
						resolve();
					}
				});
			});
		}
		return {
			isViewed,
			markAsViewed
		};
	}
	var init_use_introduction = __esmMin((() => {
		init_slicedToArray();
	}));

//#endregion
//#region modules/ai/assets/js/editor/components/voice-promotion-alert.js
	function ownKeys$29(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	function _objectSpread$28(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$29(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$29(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	var import_prop_types$3, VoicePromotionAlert;
	var init_voice_promotion_alert = __esmMin((() => {
		init_defineProperty$1();
		init_bulb_icon();
		init_use_introduction();
		import_prop_types$3 = /* @__PURE__ */ __toESM(require_prop_types());
		__name(ownKeys$29, "ownKeys");
		__name(_objectSpread$28, "_objectSpread");
		VoicePromotionAlert = function VoicePromotionAlert(props) {
			var _useIntroduction = useIntroduction(props.introductionKey);
			var isViewed = _useIntroduction.isViewed;
			var markAsViewed = _useIntroduction.markAsViewed;
			if (isViewed) return null;
			return /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, {
				sx: _objectSpread$28({ mt: 2 }, props.sx),
				alignItems: "top"
			}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Alert, {
				severity: "info",
				variant: "standard",
				icon: /*#__PURE__*/ react.default.createElement(BulbIcon, { sx: { alignSelf: "flex-start" } }),
				onClose: markAsViewed
			}, (0, _wordpress_i18n.__)("Get improved results from AI by adding personal context.", "elementor"), /*#__PURE__*/ react.default.createElement(_elementor_ui.Link, {
				onClick: function onClick() {
					return $e.route("panel/global/menu");
				},
				className: "elementor-clickable",
				style: { textDecoration: "none" },
				color: "info.main",
				href: "#"
			}, (0, _wordpress_i18n.__)("Let’s do it", "elementor"))));
		};
		VoicePromotionAlert.propTypes = {
			sx: import_prop_types$3.default.object,
			introductionKey: import_prop_types$3.default.string
		};
	}));

//#endregion
//#region modules/ai/assets/js/editor/pages/form-text/index.js
	init_asyncToGenerator();
	init_slicedToArray();
	init_actions_data();
	init_prompt_history_action_context();
	init_requests_ids();
	init_voice_promotion_alert();
	var promptActions = [
		{
			label: (0, _wordpress_i18n.__)("Simplify language", "elementor"),
			icon: /*#__PURE__*/ react.default.createElement(_elementor_icons.MessageIcon, { fontSize: "small" }),
			value: "Simplify the language of the following message"
		},
		{
			label: (0, _wordpress_i18n.__)("Make it longer", "elementor"),
			icon: /*#__PURE__*/ react.default.createElement(_elementor_icons.ExpandIcon, { fontSize: "small" }),
			value: "Make the following message longer"
		},
		{
			label: (0, _wordpress_i18n.__)("Make it shorter", "elementor"),
			icon: /*#__PURE__*/ react.default.createElement(_elementor_icons.ShrinkIcon, { fontSize: "small" }),
			value: "Make the following message shorter"
		},
		{
			label: (0, _wordpress_i18n.__)("Fix spelling & grammar", "elementor"),
			icon: /*#__PURE__*/ react.default.createElement(_elementor_icons.AIIcon, { fontSize: "small" }),
			value: "Fix the spelling and grammar of the following message"
		}
	];
	var promptInstructions = [{
		label: (0, _wordpress_i18n.__)("Change tone", "elementor"),
		options: vocalTones,
		getInstruction: function getInstruction(value) {
			return "Change the tone of the following message to ".concat(value);
		}
	}, {
		label: (0, _wordpress_i18n.__)("Translate to", "elementor"),
		options: translateLanguages,
		getInstruction: function getInstruction(value) {
			return "Translate the following message to ".concat(value);
		}
	}];
	var FormText = function FormText(_ref) {
		var _initialValue$result;
		var _initialValue$credits;
		var _additionalOptions$in;
		var type = _ref.type;
		var onClose = _ref.onClose;
		var getControlValue = _ref.getControlValue;
		var setControlValue = _ref.setControlValue;
		var additionalOptions = _ref.additionalOptions;
		var credits = _ref.credits;
		var children = _ref.children;
		var initialValue = getControlValue() === (additionalOptions === null || additionalOptions === void 0 ? void 0 : additionalOptions.defaultValue) ? "" : getControlValue();
		var _useTextPrompt = useTextPrompt({
			result: (_initialValue$result = initialValue === null || initialValue === void 0 ? void 0 : initialValue.result) !== null && _initialValue$result !== void 0 ? _initialValue$result : initialValue,
			credits: (_initialValue$credits = initialValue === null || initialValue === void 0 ? void 0 : initialValue.credits) !== null && _initialValue$credits !== void 0 ? _initialValue$credits : credits,
			responseId: initialValue === null || initialValue === void 0 ? void 0 : initialValue.responseId
		});
		var data = _useTextPrompt.data;
		var isLoading = _useTextPrompt.isLoading;
		var txtGenErr = _useTextPrompt.error;
		var setResult = _useTextPrompt.setResult;
		var reset = _useTextPrompt.reset;
		var send = _useTextPrompt.send;
		var sendUsageData = _useTextPrompt.sendUsageData;
		var error = txtGenErr || (additionalOptions === null || additionalOptions === void 0 ? void 0 : additionalOptions.initError);
		var _useState2 = _slicedToArray((0, react.useState)(""), 2);
		var prompt = _useState2[0];
		var setPrompt = _useState2[1];
		var setGenerate = useRequestIds().setGenerate;
		useSubscribeOnPromptHistoryAction([{
			type: ACTION_TYPES.REUSE,
			handler: function handler(action) {
				reset();
				setPrompt(action.data);
			}
		}, {
			type: ACTION_TYPES.EDIT,
			handler: function handler(action) {
				setResult(action.data);
			}
		}]);
		var searchField = (0, react.useRef)(null);
		var resultField = (0, react.useRef)(null);
		var lastRun = (0, react.useRef)((_additionalOptions$in = additionalOptions.initRetry) !== null && _additionalOptions$in !== void 0 ? _additionalOptions$in : function() {});
		var autocompleteItems = "textarea" === type ? textareaAutocomplete : textAutocomplete;
		var showSuggestions = !prompt;
		var handleSubmit = function handleSubmit(event) {
			event.preventDefault();
			setGenerate();
			lastRun.current = function() {
				return send({ prompt });
			};
			lastRun.current();
		};
		var handleCustomInstruction = /*#__PURE__*/ function() {
			var _ref2 = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee(instruction) {
				return import_regenerator$2.default.wrap(function(_context) {
					while (1) switch (_context.prev = _context.next) {
						case 0:
							setGenerate();
							lastRun.current = function() {
								return send({
									input: resultField.current.value,
									instruction
								});
							};
							lastRun.current();
						case 1:
						case "end": return _context.stop();
					}
				}, _callee);
			}));
			return function handleCustomInstruction(_x) {
				return _ref2.apply(this, arguments);
			};
		}();
		var handleSuggestion = function handleSuggestion(suggestion) {
			setPrompt(suggestion + " ");
			searchField.current.focus();
		};
		var applyPrompt = function applyPrompt() {
			sendUsageData();
			setControlValue(resultField.current.value);
			onClose();
		};
		if (isLoading) return /*#__PURE__*/ react.default.createElement(Loader, null);
		return /*#__PURE__*/ react.default.createElement(react.default.Fragment, null, error && /*#__PURE__*/ react.default.createElement(PromptErrorMessage, {
			error,
			onRetry: lastRun.current,
			sx: { mb: 2.5 }
		}), children, !data.result && /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, {
			component: "form",
			onSubmit: handleSubmit
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, { sx: { mb: 2.5 } }, /*#__PURE__*/ react.default.createElement(PromptSearch, {
			ref: searchField,
			placeholder: (0, _wordpress_i18n.__)("Describe the text and tone you want to use...", "elementor"),
			name: "prompt",
			value: prompt,
			onChange: function onChange(event) {
				return setPrompt(event.target.value);
			}
		})), showSuggestions && /*#__PURE__*/ react.default.createElement(PromptSuggestions, {
			suggestions: autocompleteItems,
			onSelect: handleSuggestion,
			suggestionFilter: function suggestionFilter(suggestion) {
				return suggestion + "...";
			}
		}, /*#__PURE__*/ react.default.createElement(PromptLibraryLink, { libraryLink: "https://go.elementor.com/ai-prompt-library-text/" })), additionalOptions.hideAiContext ? null : /*#__PURE__*/ react.default.createElement(VoicePromotionAlert, {
			introductionKey: "ai-context-text-promotion",
			sx: { mb: 2 }
		}), /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			direction: "row",
			alignItems: "center",
			sx: {
				pb: 1.5,
				mt: 2
			}
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			direction: "row",
			justifyContent: "flex-end",
			flexGrow: 1
		}, /*#__PURE__*/ react.default.createElement(GenerateButton$1, null, (0, _wordpress_i18n.__)("Generate text", "elementor"))))), data.result && /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, { sx: { mt: 1 } }, /*#__PURE__*/ react.default.createElement(Textarea, {
			fullWidth: true,
			ref: resultField,
			value: data.result,
			helperText: (0, _wordpress_i18n.__)("Text generated by AI may be inaccurate or offensive.", "elementor"),
			onChange: function onChange(event) {
				return setResult(event.target.value);
			}
		}), /*#__PURE__*/ react.default.createElement(_elementor_ui.Grid, {
			container: true,
			spacing: 1,
			sx: { mt: 2.5 }
		}, promptActions.map(function(_ref3) {
			var label = _ref3.label;
			var icon = _ref3.icon;
			var value = _ref3.value;
			return /*#__PURE__*/ react.default.createElement(_elementor_ui.Grid, {
				item: true,
				key: label
			}, /*#__PURE__*/ react.default.createElement(PromptAction, {
				label,
				icon,
				onClick: function onClick() {
					return handleCustomInstruction(value);
				}
			}));
		})), /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			direction: "row",
			alignItems: "center",
			spacing: 1,
			sx: { mt: 2.5 }
		}, promptInstructions.map(function(_ref4) {
			var label = _ref4.label;
			var options = _ref4.options;
			var getInstruction = _ref4.getInstruction;
			return /*#__PURE__*/ react.default.createElement(PromptActionSelection, {
				key: label,
				label,
				options,
				onChange: function onChange(event) {
					return handleCustomInstruction(getInstruction(event.target.value));
				}
			});
		})), additionalOptions.hideAiContext ? null : /*#__PURE__*/ react.default.createElement(VoicePromotionAlert, {
			introductionKey: "ai-context-text-promotion",
			sx: { mb: 2 }
		}), /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			direction: "row",
			alignItems: "center",
			sx: { my: 1 }
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			direction: "row",
			gap: 1,
			justifyContent: "flex-end",
			flexGrow: 1
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, {
			size: "small",
			color: "secondary",
			variant: "text",
			onClick: reset
		}, (0, _wordpress_i18n.__)("New prompt", "elementor")), /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, {
			size: "small",
			variant: "contained",
			color: "primary",
			onClick: applyPrompt
		}, (0, _wordpress_i18n.__)("Use text", "elementor"))))));
	};
	FormText.propTypes = {
		type: import_prop_types$7.default.string.isRequired,
		controlType: import_prop_types$7.default.string,
		onClose: import_prop_types$7.default.func.isRequired,
		getControlValue: import_prop_types$7.default.func.isRequired,
		setControlValue: import_prop_types$7.default.func.isRequired,
		additionalOptions: import_prop_types$7.default.object,
		credits: import_prop_types$7.default.number,
		usagePercentage: import_prop_types$7.default.number,
		children: import_prop_types$7.default.node
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/connect/index.js
	var Connect = function Connect(_ref) {
		var connectUrl = _ref.connectUrl;
		var onSuccess = _ref.onSuccess;
		var approveButtonRef = (0, react.useRef)();
		(0, react.useEffect)(function() {
			if (!jQuery.fn.elementorConnect) return;
			jQuery(approveButtonRef.current).elementorConnect({
				success: function success(_, data) {
					return onSuccess(data);
				},
				error: function error() {
					throw new Error("Elementor AI: Failed to connect.");
				}
			});
		}, []);
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			alignItems: "center",
			gap: 2
		}, /*#__PURE__*/ react.default.createElement(_elementor_icons.AIIcon, { sx: {
			color: "text.primary",
			fontSize: "60px",
			mb: 1
		} }), /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, {
			variant: "h4",
			sx: { color: "text.primary" }
		}, (0, _wordpress_i18n.__)("Step into the future with Elementor AI", "elementor")), /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, { variant: "body2" }, (0, _wordpress_i18n.__)("Create smarter with AI text and code generators built right into the editor.", "elementor")), /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, {
			variant: "caption",
			sx: {
				maxWidth: 520,
				textAlign: "center"
			}
		}, "       ", (0, _wordpress_i18n.__)("By clicking \"Connect\", I approve the ", "elementor"), /*#__PURE__*/ react.default.createElement(_elementor_ui.Link, {
			href: "https://go.elementor.com/ai-terms/",
			target: "_blank",
			color: "info.main"
		}, (0, _wordpress_i18n.__)("Terms of Service", "elementor")), " & ", /*#__PURE__*/ react.default.createElement(_elementor_ui.Link, {
			href: "https://go.elementor.com/ai-privacy-policy/",
			target: "_blank",
			color: "info.main"
		}, (0, _wordpress_i18n.__)("Privacy Policy", "elementor")), (0, _wordpress_i18n.__)(" of the Elementor AI service.", "elementor")), /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, {
			ref: approveButtonRef,
			href: connectUrl,
			variant: "contained",
			sx: {
				mt: 1,
				"&:hover": { color: "primary.contrastText" }
			}
		}, (0, _wordpress_i18n.__)("Connect", "elementor")));
	};
	Connect.propTypes = {
		connectUrl: import_prop_types$7.default.string.isRequired,
		onSuccess: import_prop_types$7.default.func.isRequired
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/get-started/index.js
	init_asyncToGenerator();
	init_slicedToArray();
	init_api();
	var GetStarted = function GetStarted(_ref) {
		var onSuccess = _ref.onSuccess;
		var _useState2 = _slicedToArray((0, react.useState)(false), 2);
		var isTermsChecked = _useState2[0];
		var setIsTermsChecked = _useState2[1];
		var onGetStartedClick = /*#__PURE__*/ function() {
			var _ref2 = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee() {
				return import_regenerator$2.default.wrap(function(_context) {
					while (1) switch (_context.prev = _context.next) {
						case 0:
							_context.next = 1;
							return setGetStarted();
						case 1: onSuccess();
						case 2:
						case "end": return _context.stop();
					}
				}, _callee);
			}));
			return function onGetStartedClick() {
				return _ref2.apply(this, arguments);
			};
		}();
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			alignItems: "center",
			gap: 1.5
		}, /*#__PURE__*/ react.default.createElement(_elementor_icons.AIIcon, { sx: {
			color: "text.primary",
			fontSize: "60px",
			mb: 1
		} }), /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, {
			variant: "h4",
			sx: { color: "text.primary" }
		}, (0, _wordpress_i18n.__)("Step into the future with Elementor AI", "elementor")), /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, { variant: "body2" }, (0, _wordpress_i18n.__)("Create smarter with AI text and code generators built right into the editor.", "elementor")), /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			direction: "row",
			gap: 1.5,
			alignItems: "flex-start"
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Checkbox, {
			id: "e-ai-terms-approval",
			color: "secondary",
			checked: isTermsChecked,
			onClick: function onClick() {
				return setIsTermsChecked(function(prevState) {
					return !prevState;
				});
			}
		}), /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, null, /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, {
			variant: "caption",
			sx: { maxWidth: 520 },
			component: "label",
			htmlFor: "e-ai-terms-approval"
		}, "       ", (0, _wordpress_i18n.__)("I approve the ", "elementor"), /*#__PURE__*/ react.default.createElement(_elementor_ui.Link, {
			href: "https://go.elementor.com/ai-terms/",
			target: "_blank",
			color: "info.main"
		}, (0, _wordpress_i18n.__)("Terms of Service", "elementor")), " & ", /*#__PURE__*/ react.default.createElement(_elementor_ui.Link, {
			href: "https://go.elementor.com/ai-privacy-policy/",
			target: "_blank",
			color: "info.main"
		}, (0, _wordpress_i18n.__)("Privacy Policy", "elementor")), (0, _wordpress_i18n.__)(" of the Elementor AI service.", "elementor"), /*#__PURE__*/ react.default.createElement("br", null), (0, _wordpress_i18n.__)("This includes consenting to the collection and use of data to improve user experience.", "elementor")))), /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, {
			disabled: !isTermsChecked,
			variant: "contained",
			onClick: onGetStartedClick,
			sx: {
				mt: 1,
				"&:hover": { color: "primary.contrastText" }
			}
		}, (0, _wordpress_i18n.__)("Get Started", "elementor")));
	};
	GetStarted.propTypes = { onSuccess: import_prop_types$7.default.func.isRequired };

//#endregion
//#region modules/ai/assets/js/editor/hooks/use-user-info.js
	init_defineProperty$1();
	init_asyncToGenerator();
	init_slicedToArray();
	init_api();
	function ownKeys$28(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	__name(ownKeys$28, "ownKeys");
	function _objectSpread$27(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$28(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$28(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	__name(_objectSpread$27, "_objectSpread");
	var useUserInfo = function useUserInfo() {
		var immediately = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : false;
		var _useState2 = _slicedToArray((0, react.useState)(false), 2);
		var isLoaded = _useState2[0];
		var setIsLoaded = _useState2[1];
		var _useState4 = _slicedToArray((0, react.useState)(false), 2);
		var isLoading = _useState4[0];
		var setIsLoading = _useState4[1];
		var _useState6 = _slicedToArray((0, react.useState)({
			is_connected: false,
			is_get_started: false,
			connect_url: "",
			usage: {
				hasAiSubscription: false,
				quota: 0,
				usedQuota: 0
			}
		}), 2);
		var userInfo = _useState6[0];
		var setUserInfo = _useState6[1];
		var credits = userInfo.usage.quota - userInfo.usage.usedQuota;
		var usagePercentage = userInfo.usage.quota ? userInfo.usage.usedQuota / userInfo.usage.quota * 100 : 0;
		var fetchData = /*#__PURE__*/ function() {
			var _ref = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee() {
				var userInfoResult;
				return import_regenerator$2.default.wrap(function(_context) {
					while (1) switch (_context.prev = _context.next) {
						case 0:
							setIsLoading(true);
							_context.next = 1;
							return getUserInformation(immediately);
						case 1:
							userInfoResult = _context.sent;
							setUserInfo(function(prevState) {
								return _objectSpread$27(_objectSpread$27({}, prevState), userInfoResult);
							});
							setIsLoaded(true);
							setIsLoading(false);
						case 2:
						case "end": return _context.stop();
					}
				}, _callee);
			}));
			return function fetchData() {
				return _ref.apply(this, arguments);
			};
		}();
		if (!isLoaded && !isLoading) fetchData();
		return {
			isLoading,
			isLoaded,
			isConnected: userInfo.is_connected,
			isGetStarted: userInfo.is_get_started,
			connectUrl: userInfo.connect_url,
			builderUrl: userInfo.usage.builderUrl,
			hasSubscription: userInfo.usage.hasAiSubscription,
			credits: credits < 0 ? 0 : credits,
			usagePercentage: Math.round(usagePercentage),
			fetchData
		};
	};
	useUserInfo.propTypes = { immediately: import_prop_types$7.default.bool };

//#endregion
//#region modules/ai/assets/js/editor/components/dialog-header.js
	init_extends();
	var StyledElementorLogo = (0, _elementor_ui.styled)(function ElementorLogo(props) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 32 32" }, props), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M2.69648 24.8891C0.938383 22.2579 0 19.1645 0 16C0 11.7566 1.68571 7.68687 4.68629 4.68629C7.68687 1.68571 11.7566 0 16 0C19.1645 0 22.2579 0.938383 24.8891 2.69648C27.5203 4.45459 29.5711 6.95344 30.7821 9.87706C31.9931 12.8007 32.3099 16.0177 31.6926 19.1214C31.0752 22.2251 29.5514 25.0761 27.3137 27.3137C25.0761 29.5514 22.2251 31.0752 19.1214 31.6926C16.0177 32.3099 12.8007 31.9931 9.87706 30.7821C6.95344 29.5711 4.45459 27.5203 2.69648 24.8891ZM12.0006 9.33281H9.33437V22.6665H12.0006V9.33281ZM22.6657 9.33281H14.6669V11.9991H22.6657V9.33281ZM22.6657 14.6654H14.6669V17.3316H22.6657V14.6654ZM22.6657 20.0003H14.6669V22.6665H22.6657V20.0003Z"
		}));
	})(function(_ref) {
		var theme = _ref.theme;
		return {
			width: theme.spacing(3),
			height: theme.spacing(3),
			"& path": { fill: theme.palette.text.primary }
		};
	});
	var DialogHeader$1 = function DialogHeader(props) {
		var hideAiBetaLogo = props.hideAiBetaLogo;
		var onClose = props.onClose;
		var children = props.children;
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.AppBar, {
			sx: { fontWeight: "normal" },
			color: "transparent",
			position: "relative"
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Toolbar, { variant: "dense" }, !hideAiBetaLogo && /*#__PURE__*/ react.default.createElement(react.default.Fragment, null, /*#__PURE__*/ react.default.createElement(StyledElementorLogo, { sx: { mr: 1 } }), /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, {
			component: "span",
			variant: "subtitle2",
			sx: {
				fontWeight: "bold",
				textTransform: "uppercase"
			}
		}, (0, _wordpress_i18n.__)("AI", "elementor")), /*#__PURE__*/ react.default.createElement(_elementor_ui.Chip, {
			label: (0, _wordpress_i18n.__)("Beta", "elementor"),
			color: "default",
			size: "small",
			sx: { ml: 1 }
		})), /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			direction: "row",
			spacing: 1,
			alignItems: "center",
			sx: { ml: hideAiBetaLogo ? 0 : "auto" }
		}, children, /*#__PURE__*/ react.default.createElement(_elementor_ui.IconButton, {
			size: "small",
			"aria-label": "close",
			onClick: onClose,
			sx: { "&.MuiButtonBase-root": { mr: -1 } }
		}, /*#__PURE__*/ react.default.createElement(_elementor_icons.XIcon, null)))));
	};
	DialogHeader$1.propTypes = {
		onClose: import_prop_types$7.default.func.isRequired,
		hideAiBetaLogo: import_prop_types$7.default.bool,
		children: import_prop_types$7.default.oneOfType([import_prop_types$7.default.arrayOf(import_prop_types$7.default.node), import_prop_types$7.default.node])
	};

//#endregion
//#region modules/ai/assets/js/editor/components/wizard-dialog.js
	init_extends();
	init_defineProperty$1();
	init_objectWithoutProperties$1();
	var _excluded$30 = ["sx"];
	function ownKeys$27(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	__name(ownKeys$27, "ownKeys");
	function _objectSpread$26(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$27(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$27(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	__name(_objectSpread$26, "_objectSpread");
	var WizardDialog = function WizardDialog(props) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Dialog, {
			open: true,
			onClose: props.onClose,
			fullWidth: true,
			hideBackdrop: true,
			maxWidth: "lg",
			PaperProps: { sx: { height: "88vh" } },
			sx: { zIndex: 9999 }
		}, props.children);
	};
	WizardDialog.propTypes = {
		onClose: import_prop_types$7.default.func.isRequired,
		children: import_prop_types$7.default.node.isRequired
	};
	var WizardDialogContent = function WizardDialogContent(_ref) {
		var _ref$sx = _ref.sx;
		var sx = _ref$sx === void 0 ? {} : _ref$sx;
		var props = _objectWithoutProperties$1(_ref, _excluded$30);
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.DialogContent, _extends$1({}, props, { sx: _objectSpread$26({
			display: "flex",
			flexDirection: "column",
			justifyContent: "center"
		}, sx) }));
	};
	WizardDialogContent.propTypes = { sx: import_prop_types$7.default.object };
	WizardDialog.Header = DialogHeader$1;
	WizardDialog.Content = WizardDialogContent;

//#endregion
//#region \0@oxc-project+runtime@0.140.0/helpers/esm/typeof.js
	function _typeof(o) {
		"@babel/helpers - typeof";
		return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o) {
			return typeof o;
		} : function(o) {
			return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
		}, _typeof(o);
	}
	var init_typeof = __esmMin((() => {}));

//#endregion
//#region \0@oxc-project+runtime@0.140.0/helpers/esm/toPrimitive.js
	function toPrimitive(t, r) {
		if ("object" != _typeof(t) || !t) return t;
		var e = t[Symbol.toPrimitive];
		if (void 0 !== e) {
			var i = e.call(t, r || "default");
			if ("object" != _typeof(i)) return i;
			throw new TypeError("@@toPrimitive must return a primitive value.");
		}
		return ("string" === r ? String : Number)(t);
	}
	var init_toPrimitive = __esmMin((() => {
		init_typeof();
	}));

//#endregion
//#region \0@oxc-project+runtime@0.140.0/helpers/esm/toPropertyKey.js
	function toPropertyKey(t) {
		var i = toPrimitive(t, "string");
		return "symbol" == _typeof(i) ? i : i + "";
	}
	var init_toPropertyKey = __esmMin((() => {
		init_typeof();
		init_toPrimitive();
	}));

//#endregion
//#region \0@oxc-project+runtime@0.140.0/helpers/esm/defineProperty.js
	function _defineProperty(e, r, t) {
		return (r = toPropertyKey(r)) in e ? Object.defineProperty(e, r, {
			value: t,
			enumerable: !0,
			configurable: !0,
			writable: !0
		}) : e[r] = t, e;
	}
	var init_defineProperty = __esmMin((() => {
		init_toPropertyKey();
	}));

//#endregion
//#region \0@oxc-project+runtime@0.140.0/helpers/esm/objectSpread2.js
	function ownKeys$26(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	function _objectSpread2(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$26(Object(t), !0).forEach(function(r) {
				_defineProperty(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$26(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	var init_objectSpread2 = __esmMin((() => {
		init_defineProperty();
		__name(ownKeys$26, "ownKeys");
	}));

//#endregion
//#region \0@oxc-project+runtime@0.140.0/helpers/esm/objectWithoutPropertiesLoose.js
	function _objectWithoutPropertiesLoose(r, e) {
		if (null == r) return {};
		var t = {};
		for (var n in r) if ({}.hasOwnProperty.call(r, n)) {
			if (e.includes(n)) continue;
			t[n] = r[n];
		}
		return t;
	}
	var init_objectWithoutPropertiesLoose = __esmMin((() => {}));

//#endregion
//#region \0@oxc-project+runtime@0.140.0/helpers/esm/objectWithoutProperties.js
	function _objectWithoutProperties(e, t) {
		if (null == e) return {};
		var o;
		var r;
		var i = _objectWithoutPropertiesLoose(e, t);
		if (Object.getOwnPropertySymbols) {
			var s = Object.getOwnPropertySymbols(e);
			for (r = 0; r < s.length; r++) o = s[r], t.includes(o) || {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]);
		}
		return i;
	}
	var init_objectWithoutProperties = __esmMin((() => {
		init_objectWithoutPropertiesLoose();
	}));

//#endregion
//#region node_modules/react-draggable/node_modules/clsx/dist/clsx.js
	var require_clsx = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		function r(e) {
			var o;
			var t;
			var f = "";
			if ("string" == typeof e || "number" == typeof e) f += e;
			else if ("object" == typeof e) if (Array.isArray(e)) {
				var n = e.length;
				for (o = 0; o < n; o++) e[o] && (t = r(e[o])) && (f && (f += " "), f += t);
			} else for (t in e) e[t] && (f && (f += " "), f += t);
			return f;
		}
		function e() {
			for (var e, o, t = 0, f = "", n = arguments.length; t < n; t++) (e = arguments[t]) && (o = r(e)) && (f && (f += " "), f += o);
			return f;
		}
		module.exports = e, module.exports.clsx = e;
	}));

//#endregion
//#region node_modules/react-draggable/build/cjs/utils/shims.js
	var require_shims = /* @__PURE__ */ __commonJSMin(((exports) => {
		Object.defineProperty(exports, "__esModule", { value: true });
		exports.dontSetMe = dontSetMe;
		exports.findInArray = findInArray;
		exports.int = int;
		exports.isFunction = isFunction;
		exports.isNum = isNum;
		function findInArray(array, callback) {
			for (let i = 0, length = array.length; i < length; i++) if (callback.apply(callback, [
				array[i],
				i,
				array
			])) return array[i];
		}
		function isFunction(func) {
			return typeof func === "function" || Object.prototype.toString.call(func) === "[object Function]";
		}
		function isNum(num) {
			return typeof num === "number" && !isNaN(num);
		}
		function int(a) {
			return parseInt(a, 10);
		}
		function dontSetMe(props, propName, componentName) {
			if (props[propName]) return /* @__PURE__ */ new Error(`Invalid prop ${propName} passed to ${componentName} - do not set this, set it on the child.`);
		}
	}));

//#endregion
//#region node_modules/react-draggable/build/cjs/utils/getPrefix.js
	var require_getPrefix = /* @__PURE__ */ __commonJSMin(((exports) => {
		Object.defineProperty(exports, "__esModule", { value: true });
		exports.browserPrefixToKey = browserPrefixToKey;
		exports.browserPrefixToStyle = browserPrefixToStyle;
		exports.default = void 0;
		exports.getPrefix = getPrefix;
		var prefixes = [
			"Moz",
			"Webkit",
			"O",
			"ms"
		];
		function getPrefix() {
			var _window$document;
			let prop = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "transform";
			if (typeof window === "undefined") return "";
			const style = (_window$document = window.document) === null || _window$document === void 0 || (_window$document = _window$document.documentElement) === null || _window$document === void 0 ? void 0 : _window$document.style;
			if (!style) return "";
			if (prop in style) return "";
			for (let i = 0; i < prefixes.length; i++) if (browserPrefixToKey(prop, prefixes[i]) in style) return prefixes[i];
			return "";
		}
		function browserPrefixToKey(prop, prefix) {
			return prefix ? `${prefix}${kebabToTitleCase(prop)}` : prop;
		}
		function browserPrefixToStyle(prop, prefix) {
			return prefix ? `-${prefix.toLowerCase()}-${prop}` : prop;
		}
		function kebabToTitleCase(str) {
			let out = "";
			let shouldCapitalize = true;
			for (let i = 0; i < str.length; i++) if (shouldCapitalize) {
				out += str[i].toUpperCase();
				shouldCapitalize = false;
			} else if (str[i] === "-") shouldCapitalize = true;
			else out += str[i];
			return out;
		}
		exports.default = getPrefix();
	}));

//#endregion
//#region node_modules/react-draggable/build/cjs/utils/domFns.js
	var require_domFns = /* @__PURE__ */ __commonJSMin(((exports) => {
		init_objectSpread2();
		Object.defineProperty(exports, "__esModule", { value: true });
		exports.addClassName = addClassName;
		exports.addEvent = addEvent;
		exports.addUserSelectStyles = addUserSelectStyles;
		exports.createCSSTransform = createCSSTransform;
		exports.createSVGTransform = createSVGTransform;
		exports.getTouch = getTouch;
		exports.getTouchIdentifier = getTouchIdentifier;
		exports.getTranslation = getTranslation;
		exports.innerHeight = innerHeight;
		exports.innerWidth = innerWidth;
		exports.matchesSelector = matchesSelector;
		exports.matchesSelectorAndParentsTo = matchesSelectorAndParentsTo;
		exports.offsetXYFromParent = offsetXYFromParent;
		exports.outerHeight = outerHeight;
		exports.outerWidth = outerWidth;
		exports.removeClassName = removeClassName;
		exports.removeEvent = removeEvent;
		exports.scheduleRemoveUserSelectStyles = scheduleRemoveUserSelectStyles;
		var _shims = require_shims();
		var _getPrefix = _interopRequireWildcard(require_getPrefix());
		function _interopRequireWildcard(e, t) {
			if ("function" == typeof WeakMap) {
				var r = /* @__PURE__ */ new WeakMap();
				var n = /* @__PURE__ */ new WeakMap();
			}
			return (_interopRequireWildcard = function(e, t) {
				if (!t && e && e.__esModule) return e;
				var o;
				var i;
				var f = {
					__proto__: null,
					default: e
				};
				if (null === e || "object" != typeof e && "function" != typeof e) return f;
				if (o = t ? n : r) {
					if (o.has(e)) return o.get(e);
					o.set(e, f);
				}
				for (const t in e) "default" !== t && {}.hasOwnProperty.call(e, t) && ((i = (o = Object.defineProperty) && Object.getOwnPropertyDescriptor(e, t)) && (i.get || i.set) ? o(f, t, i) : f[t] = e[t]);
				return f;
			})(e, t);
		}
		var matchesSelectorFunc = "";
		function matchesSelector(el, selector) {
			if (!matchesSelectorFunc) matchesSelectorFunc = (0, _shims.findInArray)([
				"matches",
				"webkitMatchesSelector",
				"mozMatchesSelector",
				"msMatchesSelector",
				"oMatchesSelector"
			], function(method) {
				return (0, _shims.isFunction)(el[method]);
			});
			if (!(0, _shims.isFunction)(el[matchesSelectorFunc])) return false;
			return el[matchesSelectorFunc](selector);
		}
		function matchesSelectorAndParentsTo(el, selector, baseNode) {
			let node = el;
			do {
				if (matchesSelector(node, selector)) return true;
				if (node === baseNode) return false;
				node = node.parentNode;
			} while (node);
			return false;
		}
		function addEvent(el, event, handler, inputOptions) {
			if (!el) return;
			const options = _objectSpread2({ capture: true }, inputOptions);
			if (el.addEventListener) el.addEventListener(event, handler, options);
			else if (el.attachEvent) el.attachEvent("on" + event, handler);
			else el["on" + event] = handler;
		}
		function removeEvent(el, event, handler, inputOptions) {
			if (!el) return;
			const options = _objectSpread2({ capture: true }, inputOptions);
			if (el.removeEventListener) el.removeEventListener(event, handler, options);
			else if (el.detachEvent) el.detachEvent("on" + event, handler);
			else el["on" + event] = null;
		}
		function outerHeight(node) {
			let height = node.clientHeight;
			const computedStyle = node.ownerDocument.defaultView.getComputedStyle(node);
			height += (0, _shims.int)(computedStyle.borderTopWidth);
			height += (0, _shims.int)(computedStyle.borderBottomWidth);
			return height;
		}
		function outerWidth(node) {
			let width = node.clientWidth;
			const computedStyle = node.ownerDocument.defaultView.getComputedStyle(node);
			width += (0, _shims.int)(computedStyle.borderLeftWidth);
			width += (0, _shims.int)(computedStyle.borderRightWidth);
			return width;
		}
		function innerHeight(node) {
			let height = node.clientHeight;
			const computedStyle = node.ownerDocument.defaultView.getComputedStyle(node);
			height -= (0, _shims.int)(computedStyle.paddingTop);
			height -= (0, _shims.int)(computedStyle.paddingBottom);
			return height;
		}
		function innerWidth(node) {
			let width = node.clientWidth;
			const computedStyle = node.ownerDocument.defaultView.getComputedStyle(node);
			width -= (0, _shims.int)(computedStyle.paddingLeft);
			width -= (0, _shims.int)(computedStyle.paddingRight);
			return width;
		}
		function offsetXYFromParent(evt, offsetParent, scale) {
			const offsetParentRect = offsetParent === offsetParent.ownerDocument.body ? {
				left: 0,
				top: 0
			} : offsetParent.getBoundingClientRect();
			return {
				x: (evt.clientX + offsetParent.scrollLeft - offsetParentRect.left) / scale,
				y: (evt.clientY + offsetParent.scrollTop - offsetParentRect.top) / scale
			};
		}
		function createCSSTransform(controlPos, positionOffset) {
			const translation = getTranslation(controlPos, positionOffset, "px");
			return { [(0, _getPrefix.browserPrefixToKey)("transform", _getPrefix.default)]: translation };
		}
		function createSVGTransform(controlPos, positionOffset) {
			return getTranslation(controlPos, positionOffset, "");
		}
		function getTranslation(_ref, positionOffset, unitSuffix) {
			let { x, y } = _ref;
			let translation = `translate(${x}${unitSuffix},${y}${unitSuffix})`;
			if (positionOffset) translation = `translate(${`${typeof positionOffset.x === "string" ? positionOffset.x : positionOffset.x + unitSuffix}`}, ${`${typeof positionOffset.y === "string" ? positionOffset.y : positionOffset.y + unitSuffix}`})` + translation;
			return translation;
		}
		function getTouch(e, identifier) {
			return e.targetTouches && (0, _shims.findInArray)(e.targetTouches, (t) => identifier === t.identifier) || e.changedTouches && (0, _shims.findInArray)(e.changedTouches, (t) => identifier === t.identifier);
		}
		function getTouchIdentifier(e) {
			if (e.targetTouches && e.targetTouches[0]) return e.targetTouches[0].identifier;
			if (e.changedTouches && e.changedTouches[0]) return e.changedTouches[0].identifier;
		}
		function addUserSelectStyles(doc) {
			if (!doc) return;
			let styleEl = doc.getElementById("react-draggable-style-el");
			if (!styleEl) {
				styleEl = doc.createElement("style");
				styleEl.type = "text/css";
				styleEl.id = "react-draggable-style-el";
				styleEl.innerHTML = ".react-draggable-transparent-selection *::-moz-selection {all: inherit;}\n";
				styleEl.innerHTML += ".react-draggable-transparent-selection *::selection {all: inherit;}\n";
				doc.getElementsByTagName("head")[0].appendChild(styleEl);
			}
			if (doc.body) addClassName(doc.body, "react-draggable-transparent-selection");
		}
		function scheduleRemoveUserSelectStyles(doc) {
			if (window.requestAnimationFrame) window.requestAnimationFrame(() => {
				removeUserSelectStyles(doc);
			});
			else removeUserSelectStyles(doc);
		}
		function removeUserSelectStyles(doc) {
			if (!doc) return;
			try {
				if (doc.body) removeClassName(doc.body, "react-draggable-transparent-selection");
				if (doc.selection) doc.selection.empty();
				else {
					const selection = (doc.defaultView || window).getSelection();
					if (selection && selection.type !== "Caret") selection.removeAllRanges();
				}
			} catch (e) {}
		}
		function addClassName(el, className) {
			if (el.classList) el.classList.add(className);
			else if (!el.className.match(new RegExp(`(?:^|\\s)${className}(?!\\S)`))) el.className += ` ${className}`;
		}
		function removeClassName(el, className) {
			if (el.classList) el.classList.remove(className);
			else el.className = el.className.replace(new RegExp(`(?:^|\\s)${className}(?!\\S)`, "g"), "");
		}
	}));

//#endregion
//#region node_modules/react-draggable/build/cjs/utils/positionFns.js
	var require_positionFns = /* @__PURE__ */ __commonJSMin(((exports) => {
		Object.defineProperty(exports, "__esModule", { value: true });
		exports.canDragX = canDragX;
		exports.canDragY = canDragY;
		exports.createCoreData = createCoreData;
		exports.createDraggableData = createDraggableData;
		exports.getBoundPosition = getBoundPosition;
		exports.getControlPosition = getControlPosition;
		exports.snapToGrid = snapToGrid;
		var _shims = require_shims();
		var _domFns = require_domFns();
		function getBoundPosition(draggable, x, y) {
			if (!draggable.props.bounds) return [x, y];
			let { bounds } = draggable.props;
			bounds = typeof bounds === "string" ? bounds : cloneBounds(bounds);
			const node = findDOMNode(draggable);
			if (typeof bounds === "string") {
				const { ownerDocument } = node;
				const ownerWindow = ownerDocument.defaultView;
				let boundNode;
				if (bounds === "parent") boundNode = node.parentNode;
				else boundNode = node.getRootNode().querySelector(bounds);
				if (!(boundNode instanceof ownerWindow.HTMLElement)) throw new Error("Bounds selector \"" + bounds + "\" could not find an element.");
				const boundNodeEl = boundNode;
				const nodeStyle = ownerWindow.getComputedStyle(node);
				const boundNodeStyle = ownerWindow.getComputedStyle(boundNodeEl);
				bounds = {
					left: -node.offsetLeft + (0, _shims.int)(boundNodeStyle.paddingLeft) + (0, _shims.int)(nodeStyle.marginLeft),
					top: -node.offsetTop + (0, _shims.int)(boundNodeStyle.paddingTop) + (0, _shims.int)(nodeStyle.marginTop),
					right: (0, _domFns.innerWidth)(boundNodeEl) - (0, _domFns.outerWidth)(node) - node.offsetLeft + (0, _shims.int)(boundNodeStyle.paddingRight) - (0, _shims.int)(nodeStyle.marginRight),
					bottom: (0, _domFns.innerHeight)(boundNodeEl) - (0, _domFns.outerHeight)(node) - node.offsetTop + (0, _shims.int)(boundNodeStyle.paddingBottom) - (0, _shims.int)(nodeStyle.marginBottom)
				};
			}
			if ((0, _shims.isNum)(bounds.right)) x = Math.min(x, bounds.right);
			if ((0, _shims.isNum)(bounds.bottom)) y = Math.min(y, bounds.bottom);
			if ((0, _shims.isNum)(bounds.left)) x = Math.max(x, bounds.left);
			if ((0, _shims.isNum)(bounds.top)) y = Math.max(y, bounds.top);
			return [x, y];
		}
		function snapToGrid(grid, pendingX, pendingY) {
			return [Math.round(pendingX / grid[0]) * grid[0], Math.round(pendingY / grid[1]) * grid[1]];
		}
		function canDragX(draggable) {
			return draggable.props.axis === "both" || draggable.props.axis === "x";
		}
		function canDragY(draggable) {
			return draggable.props.axis === "both" || draggable.props.axis === "y";
		}
		function getControlPosition(e, touchIdentifier, draggableCore) {
			const touchObj = typeof touchIdentifier === "number" ? (0, _domFns.getTouch)(e, touchIdentifier) : null;
			if (typeof touchIdentifier === "number" && !touchObj) return null;
			const node = findDOMNode(draggableCore);
			const offsetParent = draggableCore.props.offsetParent || node.offsetParent || node.ownerDocument.body;
			return (0, _domFns.offsetXYFromParent)(touchObj || e, offsetParent, draggableCore.props.scale);
		}
		function createCoreData(draggable, x, y) {
			const isStart = !(0, _shims.isNum)(draggable.lastX);
			const node = findDOMNode(draggable);
			if (isStart) return {
				node,
				deltaX: 0,
				deltaY: 0,
				lastX: x,
				lastY: y,
				x,
				y
			};
			else return {
				node,
				deltaX: x - draggable.lastX,
				deltaY: y - draggable.lastY,
				lastX: draggable.lastX,
				lastY: draggable.lastY,
				x,
				y
			};
		}
		function createDraggableData(draggable, coreData) {
			const scale = draggable.props.scale;
			return {
				node: coreData.node,
				x: draggable.state.x + coreData.deltaX / scale,
				y: draggable.state.y + coreData.deltaY / scale,
				deltaX: coreData.deltaX / scale,
				deltaY: coreData.deltaY / scale,
				lastX: draggable.state.x,
				lastY: draggable.state.y
			};
		}
		function cloneBounds(bounds) {
			return {
				left: bounds.left,
				top: bounds.top,
				right: bounds.right,
				bottom: bounds.bottom
			};
		}
		function findDOMNode(draggable) {
			const node = draggable.findDOMNode();
			if (!node) throw new Error("<DraggableCore>: Unmounted during event!");
			return node;
		}
	}));

//#endregion
//#region node_modules/react-draggable/build/cjs/utils/log.js
	var require_log = /* @__PURE__ */ __commonJSMin(((exports) => {
		Object.defineProperty(exports, "__esModule", { value: true });
		exports.default = log;
		function log() {}
	}));

//#endregion
//#region node_modules/react-draggable/build/cjs/DraggableCore.js
	var require_DraggableCore = /* @__PURE__ */ __commonJSMin(((exports) => {
		Object.defineProperty(exports, "__esModule", { value: true });
		exports.default = void 0;
		var React$133 = _interopRequireWildcard((globalThis.React));
		var _propTypes = _interopRequireDefault(require_prop_types());
		var _reactDom$1 = _interopRequireDefault((globalThis.ReactDOM));
		var _domFns = require_domFns();
		var _positionFns = require_positionFns();
		var _shims = require_shims();
		var _log = _interopRequireDefault(require_log());
		function _interopRequireDefault(e) {
			return e && e.__esModule ? e : { default: e };
		}
		function _interopRequireWildcard(e, t) {
			if ("function" == typeof WeakMap) {
				var r = /* @__PURE__ */ new WeakMap();
				var n = /* @__PURE__ */ new WeakMap();
			}
			return (_interopRequireWildcard = function(e, t) {
				if (!t && e && e.__esModule) return e;
				var o;
				var i;
				var f = {
					__proto__: null,
					default: e
				};
				if (null === e || "object" != typeof e && "function" != typeof e) return f;
				if (o = t ? n : r) {
					if (o.has(e)) return o.get(e);
					o.set(e, f);
				}
				for (const t in e) "default" !== t && {}.hasOwnProperty.call(e, t) && ((i = (o = Object.defineProperty) && Object.getOwnPropertyDescriptor(e, t)) && (i.get || i.set) ? o(f, t, i) : f[t] = e[t]);
				return f;
			})(e, t);
		}
		function _defineProperty(e, r, t) {
			return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, {
				value: t,
				enumerable: !0,
				configurable: !0,
				writable: !0
			}) : e[r] = t, e;
		}
		function _toPropertyKey(t) {
			var i = _toPrimitive(t, "string");
			return "symbol" == typeof i ? i : i + "";
		}
		function _toPrimitive(t, r) {
			if ("object" != typeof t || !t) return t;
			var e = t[Symbol.toPrimitive];
			if (void 0 !== e) {
				var i = e.call(t, r || "default");
				if ("object" != typeof i) return i;
				throw new TypeError("@@toPrimitive must return a primitive value.");
			}
			return ("string" === r ? String : Number)(t);
		}
		var eventsFor = {
			touch: {
				start: "touchstart",
				move: "touchmove",
				stop: "touchend"
			},
			mouse: {
				start: "mousedown",
				move: "mousemove",
				stop: "mouseup"
			}
		};
		var dragEventFor = eventsFor.mouse;
		var DraggableCore = class extends React$133.Component {
			constructor() {
				super(...arguments);
				_defineProperty(this, "dragging", false);
				_defineProperty(this, "lastX", NaN);
				_defineProperty(this, "lastY", NaN);
				_defineProperty(this, "touchIdentifier", null);
				_defineProperty(this, "mounted", false);
				_defineProperty(this, "handleDragStart", (e) => {
					this.props.onMouseDown(e);
					if (!this.props.allowAnyClick && typeof e.button === "number" && e.button !== 0) return false;
					const thisNode = this.findDOMNode();
					if (!thisNode || !thisNode.ownerDocument || !thisNode.ownerDocument.body) throw new Error("<DraggableCore> not mounted on DragStart!");
					const { ownerDocument } = thisNode;
					if (this.props.disabled || !(e.target instanceof ownerDocument.defaultView.Node) || this.props.handle && !(0, _domFns.matchesSelectorAndParentsTo)(e.target, this.props.handle, thisNode) || this.props.cancel && (0, _domFns.matchesSelectorAndParentsTo)(e.target, this.props.cancel, thisNode)) return;
					if (e.type === "touchstart" && !this.props.allowMobileScroll) e.preventDefault();
					const touchIdentifier = (0, _domFns.getTouchIdentifier)(e);
					this.touchIdentifier = touchIdentifier;
					const position = (0, _positionFns.getControlPosition)(e, touchIdentifier, this);
					if (position == null) return;
					const { x, y } = position;
					const coreEvent = (0, _positionFns.createCoreData)(this, x, y);
					(0, _log.default)("DraggableCore: handleDragStart: %j", coreEvent);
					(0, _log.default)("calling", this.props.onStart);
					if (this.props.onStart(e, coreEvent) === false || this.mounted === false) return;
					if (this.props.enableUserSelectHack) (0, _domFns.addUserSelectStyles)(ownerDocument);
					this.dragging = true;
					this.lastX = x;
					this.lastY = y;
					(0, _domFns.addEvent)(ownerDocument, dragEventFor.move, this.handleDrag);
					(0, _domFns.addEvent)(ownerDocument, dragEventFor.stop, this.handleDragStop);
				});
				_defineProperty(this, "handleDrag", (e) => {
					const position = (0, _positionFns.getControlPosition)(e, this.touchIdentifier, this);
					if (position == null) return;
					let { x, y } = position;
					if (Array.isArray(this.props.grid)) {
						let deltaX = x - this.lastX;
						let deltaY = y - this.lastY;
						[deltaX, deltaY] = (0, _positionFns.snapToGrid)(this.props.grid, deltaX, deltaY);
						if (!deltaX && !deltaY) return;
						x = this.lastX + deltaX, y = this.lastY + deltaY;
					}
					const coreEvent = (0, _positionFns.createCoreData)(this, x, y);
					(0, _log.default)("DraggableCore: handleDrag: %j", coreEvent);
					if (this.props.onDrag(e, coreEvent) === false || this.mounted === false) {
						try {
							this.handleDragStop(new MouseEvent("mouseup"));
						} catch (err) {
							const event = document.createEvent("MouseEvents");
							event.initMouseEvent("mouseup", true, true, window, 0, 0, 0, 0, 0, false, false, false, false, 0, null);
							this.handleDragStop(event);
						}
						return;
					}
					this.lastX = x;
					this.lastY = y;
				});
				_defineProperty(this, "handleDragStop", (e) => {
					if (!this.dragging) return;
					const position = (0, _positionFns.getControlPosition)(e, this.touchIdentifier, this);
					if (position == null) return;
					let { x, y } = position;
					if (Array.isArray(this.props.grid)) {
						let deltaX = x - this.lastX || 0;
						let deltaY = y - this.lastY || 0;
						[deltaX, deltaY] = (0, _positionFns.snapToGrid)(this.props.grid, deltaX, deltaY);
						x = this.lastX + deltaX, y = this.lastY + deltaY;
					}
					const coreEvent = (0, _positionFns.createCoreData)(this, x, y);
					if (this.props.onStop(e, coreEvent) === false || this.mounted === false) return false;
					const thisNode = this.findDOMNode();
					if (thisNode) {
						if (this.props.enableUserSelectHack) (0, _domFns.scheduleRemoveUserSelectStyles)(thisNode.ownerDocument);
					}
					(0, _log.default)("DraggableCore: handleDragStop: %j", coreEvent);
					this.dragging = false;
					this.lastX = NaN;
					this.lastY = NaN;
					if (thisNode) {
						(0, _log.default)("DraggableCore: Removing handlers");
						(0, _domFns.removeEvent)(thisNode.ownerDocument, dragEventFor.move, this.handleDrag);
						(0, _domFns.removeEvent)(thisNode.ownerDocument, dragEventFor.stop, this.handleDragStop);
					}
				});
				_defineProperty(this, "onMouseDown", (e) => {
					dragEventFor = eventsFor.mouse;
					return this.handleDragStart(e);
				});
				_defineProperty(this, "onMouseUp", (e) => {
					dragEventFor = eventsFor.mouse;
					return this.handleDragStop(e);
				});
				_defineProperty(this, "onTouchStart", (e) => {
					dragEventFor = eventsFor.touch;
					return this.handleDragStart(e);
				});
				_defineProperty(this, "onTouchEnd", (e) => {
					dragEventFor = eventsFor.touch;
					return this.handleDragStop(e);
				});
			}
			componentDidMount() {
				this.mounted = true;
				const thisNode = this.findDOMNode();
				if (thisNode) (0, _domFns.addEvent)(thisNode, eventsFor.touch.start, this.onTouchStart, { passive: false });
			}
			componentWillUnmount() {
				this.mounted = false;
				const thisNode = this.findDOMNode();
				if (thisNode) {
					const { ownerDocument } = thisNode;
					(0, _domFns.removeEvent)(ownerDocument, eventsFor.mouse.move, this.handleDrag);
					(0, _domFns.removeEvent)(ownerDocument, eventsFor.touch.move, this.handleDrag);
					(0, _domFns.removeEvent)(ownerDocument, eventsFor.mouse.stop, this.handleDragStop);
					(0, _domFns.removeEvent)(ownerDocument, eventsFor.touch.stop, this.handleDragStop);
					(0, _domFns.removeEvent)(thisNode, eventsFor.touch.start, this.onTouchStart, { passive: false });
					if (this.props.enableUserSelectHack) (0, _domFns.scheduleRemoveUserSelectStyles)(ownerDocument);
				}
			}
			findDOMNode() {
				var _this$props;
				var _this$props2;
				return ((_this$props = this.props) === null || _this$props === void 0 ? void 0 : _this$props.nodeRef) ? (_this$props2 = this.props) === null || _this$props2 === void 0 || (_this$props2 = _this$props2.nodeRef) === null || _this$props2 === void 0 ? void 0 : _this$props2.current : _reactDom$1.default.findDOMNode(this);
			}
			render() {
				return /*#__PURE__*/ React$133.cloneElement(React$133.Children.only(this.props.children), {
					onMouseDown: this.onMouseDown,
					onMouseUp: this.onMouseUp,
					onTouchEnd: this.onTouchEnd
				});
			}
		};
		exports.default = DraggableCore;
		_defineProperty(DraggableCore, "displayName", "DraggableCore");
		_defineProperty(DraggableCore, "propTypes", {
			/**
			* `allowAnyClick` allows dragging using any mouse button.
			* By default, we only accept the left button.
			*
			* Defaults to `false`.
			*/
			allowAnyClick: _propTypes.default.bool,
			/**
			* `allowMobileScroll` turns off cancellation of the 'touchstart' event
			* on mobile devices. Only enable this if you are having trouble with click
			* events. Prefer using 'handle' / 'cancel' instead.
			*
			* Defaults to `false`.
			*/
			allowMobileScroll: _propTypes.default.bool,
			children: _propTypes.default.node.isRequired,
			/**
			* `disabled`, if true, stops the <Draggable> from dragging. All handlers,
			* with the exception of `onMouseDown`, will not fire.
			*/
			disabled: _propTypes.default.bool,
			/**
			* By default, we add 'user-select:none' attributes to the document body
			* to prevent ugly text selection during drag. If this is causing problems
			* for your app, set this to `false`.
			*/
			enableUserSelectHack: _propTypes.default.bool,
			/**
			* `offsetParent`, if set, uses the passed DOM node to compute drag offsets
			* instead of using the parent node.
			*/
			offsetParent: function(props, propName) {
				if (props[propName] && props[propName].nodeType !== 1) throw new Error("Draggable's offsetParent must be a DOM Node.");
			},
			/**
			* `grid` specifies the x and y that dragging should snap to.
			*/
			grid: _propTypes.default.arrayOf(_propTypes.default.number),
			/**
			* `handle` specifies a selector to be used as the handle that initiates drag.
			*
			* Example:
			*
			* ```jsx
			*   let App = React.createClass({
			*       render: function () {
			*         return (
			*            <Draggable handle=".handle">
			*              <div>
			*                  <div className="handle">Click me to drag</div>
			*                  <div>This is some other content</div>
			*              </div>
			*           </Draggable>
			*         );
			*       }
			*   });
			* ```
			*/
			handle: _propTypes.default.string,
			/**
			* `cancel` specifies a selector to be used to prevent drag initialization.
			*
			* Example:
			*
			* ```jsx
			*   let App = React.createClass({
			*       render: function () {
			*           return(
			*               <Draggable cancel=".cancel">
			*                   <div>
			*                     <div className="cancel">You can't drag from here</div>
			*                     <div>Dragging here works fine</div>
			*                   </div>
			*               </Draggable>
			*           );
			*       }
			*   });
			* ```
			*/
			cancel: _propTypes.default.string,
			nodeRef: _propTypes.default.object,
			/**
			* Called when dragging starts.
			* If this function returns the boolean false, dragging will be canceled.
			*/
			onStart: _propTypes.default.func,
			/**
			* Called while dragging.
			* If this function returns the boolean false, dragging will be canceled.
			*/
			onDrag: _propTypes.default.func,
			/**
			* Called when dragging stops.
			* If this function returns the boolean false, the drag will remain active.
			*/
			onStop: _propTypes.default.func,
			/**
			* A workaround option which can be passed if onMouseDown needs to be accessed,
			* since it'll always be blocked (as there is internal use of onMouseDown)
			*/
			onMouseDown: _propTypes.default.func,
			/**
			* `scale`, if set, applies scaling while dragging an element
			*/
			scale: _propTypes.default.number,
			/**
			* These properties should be defined on the child, not here.
			*/
			className: _shims.dontSetMe,
			style: _shims.dontSetMe,
			transform: _shims.dontSetMe
		});
		_defineProperty(DraggableCore, "defaultProps", {
			allowAnyClick: false,
			allowMobileScroll: false,
			disabled: false,
			enableUserSelectHack: true,
			onStart: function() {},
			onDrag: function() {},
			onStop: function() {},
			onMouseDown: function() {},
			scale: 1
		});
	}));

//#endregion
//#region node_modules/react-draggable/build/cjs/Draggable.js
	var require_Draggable = /* @__PURE__ */ __commonJSMin(((exports) => {
		init_objectSpread2();
		init_objectWithoutProperties();
		var _excluded = [
			"axis",
			"bounds",
			"children",
			"defaultPosition",
			"defaultClassName",
			"defaultClassNameDragging",
			"defaultClassNameDragged",
			"position",
			"positionOffset",
			"scale"
		];
		Object.defineProperty(exports, "__esModule", { value: true });
		Object.defineProperty(exports, "DraggableCore", {
			enumerable: true,
			get: function() {
				return _DraggableCore.default;
			}
		});
		exports.default = void 0;
		var React$132 = _interopRequireWildcard((globalThis.React));
		var _propTypes = _interopRequireDefault(require_prop_types());
		var _reactDom = _interopRequireDefault((globalThis.ReactDOM));
		var _clsx = require_clsx();
		var _domFns = require_domFns();
		var _positionFns = require_positionFns();
		var _shims = require_shims();
		var _DraggableCore = _interopRequireDefault(require_DraggableCore());
		var _log = _interopRequireDefault(require_log());
		function _interopRequireDefault(e) {
			return e && e.__esModule ? e : { default: e };
		}
		function _interopRequireWildcard(e, t) {
			if ("function" == typeof WeakMap) {
				var r = /* @__PURE__ */ new WeakMap();
				var n = /* @__PURE__ */ new WeakMap();
			}
			return (_interopRequireWildcard = function(e, t) {
				if (!t && e && e.__esModule) return e;
				var o;
				var i;
				var f = {
					__proto__: null,
					default: e
				};
				if (null === e || "object" != typeof e && "function" != typeof e) return f;
				if (o = t ? n : r) {
					if (o.has(e)) return o.get(e);
					o.set(e, f);
				}
				for (const t in e) "default" !== t && {}.hasOwnProperty.call(e, t) && ((i = (o = Object.defineProperty) && Object.getOwnPropertyDescriptor(e, t)) && (i.get || i.set) ? o(f, t, i) : f[t] = e[t]);
				return f;
			})(e, t);
		}
		function _extends() {
			return _extends = Object.assign ? Object.assign.bind() : function(n) {
				for (var e = 1; e < arguments.length; e++) {
					var t = arguments[e];
					for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]);
				}
				return n;
			}, _extends.apply(null, arguments);
		}
		function _defineProperty(e, r, t) {
			return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, {
				value: t,
				enumerable: !0,
				configurable: !0,
				writable: !0
			}) : e[r] = t, e;
		}
		function _toPropertyKey(t) {
			var i = _toPrimitive(t, "string");
			return "symbol" == typeof i ? i : i + "";
		}
		function _toPrimitive(t, r) {
			if ("object" != typeof t || !t) return t;
			var e = t[Symbol.toPrimitive];
			if (void 0 !== e) {
				var i = e.call(t, r || "default");
				if ("object" != typeof i) return i;
				throw new TypeError("@@toPrimitive must return a primitive value.");
			}
			return ("string" === r ? String : Number)(t);
		}
		var Draggable = class extends React$132.Component {
			static getDerivedStateFromProps(_ref, _ref2) {
				let { position } = _ref;
				let { prevPropsPosition } = _ref2;
				if (position && (!prevPropsPosition || position.x !== prevPropsPosition.x || position.y !== prevPropsPosition.y)) {
					(0, _log.default)("Draggable: getDerivedStateFromProps %j", {
						position,
						prevPropsPosition
					});
					return {
						x: position.x,
						y: position.y,
						prevPropsPosition: _objectSpread2({}, position)
					};
				}
				return null;
			}
			constructor(props) {
				super(props);
				_defineProperty(this, "onDragStart", (e, coreData) => {
					(0, _log.default)("Draggable: onDragStart: %j", coreData);
					if (this.props.onStart(e, (0, _positionFns.createDraggableData)(this, coreData)) === false) return false;
					this.setState({
						dragging: true,
						dragged: true
					});
				});
				_defineProperty(this, "onDrag", (e, coreData) => {
					if (!this.state.dragging) return false;
					(0, _log.default)("Draggable: onDrag: %j", coreData);
					const uiData = (0, _positionFns.createDraggableData)(this, coreData);
					const newState = {
						x: uiData.x,
						y: uiData.y,
						slackX: 0,
						slackY: 0
					};
					if (this.props.bounds) {
						const { x, y } = newState;
						newState.x += this.state.slackX;
						newState.y += this.state.slackY;
						const [newStateX, newStateY] = (0, _positionFns.getBoundPosition)(this, newState.x, newState.y);
						newState.x = newStateX;
						newState.y = newStateY;
						newState.slackX = this.state.slackX + (x - newState.x);
						newState.slackY = this.state.slackY + (y - newState.y);
						uiData.x = newState.x;
						uiData.y = newState.y;
						uiData.deltaX = newState.x - this.state.x;
						uiData.deltaY = newState.y - this.state.y;
					}
					if (this.props.onDrag(e, uiData) === false) return false;
					this.setState(newState);
				});
				_defineProperty(this, "onDragStop", (e, coreData) => {
					if (!this.state.dragging) return false;
					if (this.props.onStop(e, (0, _positionFns.createDraggableData)(this, coreData)) === false) return false;
					(0, _log.default)("Draggable: onDragStop: %j", coreData);
					const newState = {
						dragging: false,
						slackX: 0,
						slackY: 0
					};
					if (Boolean(this.props.position)) {
						const { x, y } = this.props.position;
						newState.x = x;
						newState.y = y;
					}
					this.setState(newState);
				});
				this.state = {
					dragging: false,
					dragged: false,
					x: props.position ? props.position.x : props.defaultPosition.x,
					y: props.position ? props.position.y : props.defaultPosition.y,
					prevPropsPosition: _objectSpread2({}, props.position),
					slackX: 0,
					slackY: 0,
					isElementSVG: false
				};
				if (props.position && !(props.onDrag || props.onStop)) console.warn("A `position` was applied to this <Draggable>, without drag handlers. This will make this component effectively undraggable. Please attach `onDrag` or `onStop` handlers so you can adjust the `position` of this element.");
			}
			componentDidMount() {
				if (typeof window.SVGElement !== "undefined" && this.findDOMNode() instanceof window.SVGElement) this.setState({ isElementSVG: true });
			}
			componentWillUnmount() {
				if (this.state.dragging) this.setState({ dragging: false });
			}
			findDOMNode() {
				var _this$props$nodeRef$c;
				var _this$props;
				return (_this$props$nodeRef$c = (_this$props = this.props) === null || _this$props === void 0 || (_this$props = _this$props.nodeRef) === null || _this$props === void 0 ? void 0 : _this$props.current) !== null && _this$props$nodeRef$c !== void 0 ? _this$props$nodeRef$c : _reactDom.default.findDOMNode(this);
			}
			render() {
				const _this$props2 = this.props, { axis, bounds, children, defaultPosition, defaultClassName, defaultClassNameDragging, defaultClassNameDragged, position, positionOffset, scale } = _this$props2, draggableCoreProps = _objectWithoutProperties(_this$props2, _excluded);
				let style = {};
				let svgTransform = null;
				const draggable = !Boolean(position) || this.state.dragging;
				const validPosition = position || defaultPosition;
				const transformOpts = {
					x: (0, _positionFns.canDragX)(this) && draggable ? this.state.x : validPosition.x,
					y: (0, _positionFns.canDragY)(this) && draggable ? this.state.y : validPosition.y
				};
				if (this.state.isElementSVG) svgTransform = (0, _domFns.createSVGTransform)(transformOpts, positionOffset);
				else style = (0, _domFns.createCSSTransform)(transformOpts, positionOffset);
				const className = (0, _clsx.clsx)(children.props.className || "", defaultClassName, {
					[defaultClassNameDragging]: this.state.dragging,
					[defaultClassNameDragged]: this.state.dragged
				});
				return /*#__PURE__*/ React$132.createElement(_DraggableCore.default, _extends({}, draggableCoreProps, {
					onStart: this.onDragStart,
					onDrag: this.onDrag,
					onStop: this.onDragStop
				}), /*#__PURE__*/ React$132.cloneElement(React$132.Children.only(children), {
					className,
					style: _objectSpread2(_objectSpread2({}, children.props.style), style),
					transform: svgTransform
				}));
			}
		};
		exports.default = Draggable;
		_defineProperty(Draggable, "displayName", "Draggable");
		_defineProperty(Draggable, "propTypes", _objectSpread2(_objectSpread2({}, _DraggableCore.default.propTypes), {}, {
			/**
			* `axis` determines which axis the draggable can move.
			*
			*  Note that all callbacks will still return data as normal. This only
			*  controls flushing to the DOM.
			*
			* 'both' allows movement horizontally and vertically.
			* 'x' limits movement to horizontal axis.
			* 'y' limits movement to vertical axis.
			* 'none' limits all movement.
			*
			* Defaults to 'both'.
			*/
			axis: _propTypes.default.oneOf([
				"both",
				"x",
				"y",
				"none"
			]),
			/**
			* `bounds` determines the range of movement available to the element.
			* Available values are:
			*
			* 'parent' restricts movement within the Draggable's parent node.
			*
			* Alternatively, pass an object with the following properties, all of which are optional:
			*
			* {left: LEFT_BOUND, right: RIGHT_BOUND, bottom: BOTTOM_BOUND, top: TOP_BOUND}
			*
			* All values are in px.
			*
			* Example:
			*
			* ```jsx
			*   let App = React.createClass({
			*       render: function () {
			*         return (
			*            <Draggable bounds={{right: 300, bottom: 300}}>
			*              <div>Content</div>
			*           </Draggable>
			*         );
			*       }
			*   });
			* ```
			*/
			bounds: _propTypes.default.oneOfType([
				_propTypes.default.shape({
					left: _propTypes.default.number,
					right: _propTypes.default.number,
					top: _propTypes.default.number,
					bottom: _propTypes.default.number
				}),
				_propTypes.default.string,
				_propTypes.default.oneOf([false])
			]),
			defaultClassName: _propTypes.default.string,
			defaultClassNameDragging: _propTypes.default.string,
			defaultClassNameDragged: _propTypes.default.string,
			/**
			* `defaultPosition` specifies the x and y that the dragged item should start at
			*
			* Example:
			*
			* ```jsx
			*      let App = React.createClass({
			*          render: function () {
			*              return (
			*                  <Draggable defaultPosition={{x: 25, y: 25}}>
			*                      <div>I start with transformX: 25px and transformY: 25px;</div>
			*                  </Draggable>
			*              );
			*          }
			*      });
			* ```
			*/
			defaultPosition: _propTypes.default.shape({
				x: _propTypes.default.number,
				y: _propTypes.default.number
			}),
			positionOffset: _propTypes.default.shape({
				x: _propTypes.default.oneOfType([_propTypes.default.number, _propTypes.default.string]),
				y: _propTypes.default.oneOfType([_propTypes.default.number, _propTypes.default.string])
			}),
			/**
			* `position`, if present, defines the current position of the element.
			*
			*  This is similar to how form elements in React work - if no `position` is supplied, the component
			*  is uncontrolled.
			*
			* Example:
			*
			* ```jsx
			*      let App = React.createClass({
			*          render: function () {
			*              return (
			*                  <Draggable position={{x: 25, y: 25}}>
			*                      <div>I start with transformX: 25px and transformY: 25px;</div>
			*                  </Draggable>
			*              );
			*          }
			*      });
			* ```
			*/
			position: _propTypes.default.shape({
				x: _propTypes.default.number,
				y: _propTypes.default.number
			}),
			/**
			* These properties should be defined on the child, not here.
			*/
			className: _shims.dontSetMe,
			style: _shims.dontSetMe,
			transform: _shims.dontSetMe
		}));
		_defineProperty(Draggable, "defaultProps", _objectSpread2(_objectSpread2({}, _DraggableCore.default.defaultProps), {}, {
			axis: "both",
			bounds: false,
			defaultClassName: "react-draggable",
			defaultClassNameDragging: "react-draggable-dragging",
			defaultClassNameDragged: "react-draggable-dragged",
			defaultPosition: {
				x: 0,
				y: 0
			},
			scale: 1
		}));
	}));

//#endregion
//#region node_modules/react-draggable/build/cjs/cjs.js
	var require_cjs = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		var { default: Draggable, DraggableCore } = require_Draggable();
		module.exports = Draggable;
		module.exports.default = Draggable;
		module.exports.DraggableCore = DraggableCore;
	}));

//#endregion
//#region modules/ai/assets/js/editor/components/prompt-dialog.js
	init_extends();
	init_defineProperty$1();
	init_slicedToArray();
	var import_cjs = /* @__PURE__ */ __toESM(require_cjs());
	function ownKeys$25(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	__name(ownKeys$25, "ownKeys");
	function _objectSpread$25(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$25(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$25(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	__name(_objectSpread$25, "_objectSpread");
	var DraggablePaper = function DraggablePaper(props) {
		var _useState2 = _slicedToArray((0, react.useState)({
			x: 0,
			y: 0
		}), 2);
		var position = _useState2[0];
		var setPosition = _useState2[1];
		var paperRef = (0, react.useRef)(null);
		var timeout = (0, react.useRef)(null);
		var onDrag = function onDrag(_e, _ref) {
			var x = _ref.x;
			var y = _ref.y;
			return setPosition({
				x,
				y
			});
		};
		var handlePositionBoundaries = function handlePositionBoundaries() {
			clearTimeout(timeout.current);
			timeout.current = setTimeout(function() {
				var _paperRef$current;
				var dialogTop = (_paperRef$current = paperRef.current) === null || _paperRef$current === void 0 ? void 0 : _paperRef$current.getBoundingClientRect().top;
				if (dialogTop < 0) setPosition(function(prev) {
					return _objectSpread$25(_objectSpread$25({}, prev), {}, { y: prev.y - dialogTop });
				});
			}, 50);
		};
		(0, react.useEffect)(function() {
			var resizeObserver = new ResizeObserver(handlePositionBoundaries);
			resizeObserver.observe(paperRef.current);
			return function() {
				resizeObserver.disconnect();
			};
		}, []);
		return /*#__PURE__*/ react.default.createElement(import_cjs.default, {
			position,
			onDrag,
			handle: ".MuiAppBar-root",
			cancel: "[class*=\"MuiDialogContent-root\"]",
			bounds: "parent"
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Paper, _extends$1({}, props, { ref: paperRef })));
	};
	var PromptDialog = function PromptDialog(props) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Dialog, _extends$1({
			scroll: "paper",
			open: true,
			fullWidth: true,
			hideBackdrop: true,
			PaperComponent: DraggablePaper,
			disableScrollLock: true,
			sx: { "& .MuiDialog-container": {
				alignItems: "flex-start",
				mt: "18vh"
			} },
			PaperProps: { sx: {
				m: 0,
				maxHeight: "76vh"
			} }
		}, props), props.children);
	};
	PromptDialog.propTypes = {
		onClose: import_prop_types$7.default.func.isRequired,
		children: import_prop_types$7.default.node,
		maxWidth: import_prop_types$7.default.oneOf([
			"xs",
			"sm",
			"md",
			"lg",
			"xl",
			false
		])
	};
	PromptDialog.Header = DialogHeader$1;
	PromptDialog.Content = _elementor_ui.DialogContent;

//#endregion
//#region modules/ai/assets/js/editor/components/upgrade-chip.js
	init_slicedToArray();
	var popoverId$1 = "e-ai-upgrade-popover";
	var StyledContent$3 = (0, _elementor_ui.styled)(_elementor_ui.Paper)(function(_ref) {
		var theme = _ref.theme;
		return {
			position: "relative",
			"[data-popper-placement=\"top\"] &": { marginBottom: theme.spacing(2.5) },
			"[data-popper-placement=\"bottom\"] &": { marginTop: theme.spacing(2.5) },
			padding: theme.spacing(3),
			boxShadow: theme.shadows[4],
			zIndex: "9999"
		};
	});
	var StyledArrow$1 = (0, _elementor_ui.styled)(_elementor_ui.Box)(function(_ref2) {
		var theme = _ref2.theme;
		return {
			width: theme.spacing(5),
			height: theme.spacing(2.5),
			position: "absolute",
			overflow: "hidden",
			left: "50% !important",
			transform: "translateX(-50%) rotate(var(--rotate, 0deg)) !important",
			"[data-popper-placement=\"top\"] &": { top: "100%" },
			"[data-popper-placement=\"bottom\"] &": {
				"--rotate": "180deg",
				top: "calc(".concat(theme.spacing(2.5), " * -1)")
			},
			"&::after": {
				backgroundColor: theme.palette.background.paper,
				content: "\"\"",
				display: "block",
				position: "absolute",
				width: theme.spacing(2.5),
				height: theme.spacing(2.5),
				top: 0,
				left: "50%",
				transform: "translateX(-50%) translateY(-50%) rotate(45deg)",
				boxShadow: "1px 1px 5px 0px rgba(0, 0, 0, 0.2)",
				backgroundImage: "linear-gradient(rgba(255, 255, 255, 0.05), rgba(255, 255, 255, 0.05))"
			}
		};
	});
	var upgradeBullets = [
		(0, _wordpress_i18n.__)("Get spot-on suggestions from AI Copilot and AI Context with appropriate designs, layouts, and content for your business.", "elementor"),
		(0, _wordpress_i18n.__)("Generate professional texts about any topic, in any tone.", "elementor"),
		(0, _wordpress_i18n.__)("Effortlessly create or enhance stunning images and bring your ideas to life.", "elementor"),
		(0, _wordpress_i18n.__)("Unleash infinite possibilities with the custom code generator.", "elementor"),
		(0, _wordpress_i18n.__)("Access 30-days of AI History with the AI Starter plan and 90-days with the Power plan.", "elementor")
	];
	var Chip$2 = (0, _elementor_ui.styled)(_elementor_ui.Chip)(function() {
		return {
			"& .MuiChip-label": { lineHeight: 1.5 },
			"& .MuiSvgIcon-root.MuiChip-icon": { fontSize: "1.25rem" }
		};
	});
	var UpgradeChip = function UpgradeChip(_ref3) {
		var _ref3$hasSubscription = _ref3.hasSubscription;
		var hasSubscription = _ref3$hasSubscription === void 0 ? false : _ref3$hasSubscription;
		var _ref3$usagePercentage = _ref3.usagePercentage;
		var usagePercentage = _ref3$usagePercentage === void 0 ? 0 : _ref3$usagePercentage;
		var _useState2 = _slicedToArray((0, react.useState)(false), 2);
		var isPopoverOpen = _useState2[0];
		var setIsPopoverOpen = _useState2[1];
		var anchorEl = (0, react.useRef)(null);
		var arrowEl = (0, react.useRef)(null);
		var showPopover = function showPopover() {
			return setIsPopoverOpen(true);
		};
		var hidePopover = function hidePopover() {
			return setIsPopoverOpen(false);
		};
		var actionUrl = "https://go.elementor.com/ai-popup-purchase-dropdown/";
		if (hasSubscription) actionUrl = usagePercentage >= 100 ? "https://go.elementor.com/ai-popup-upgrade-limit-reached/" : "https://go.elementor.com/ai-popup-upgrade-limit-reached-80-percent/";
		var actionLabel = hasSubscription ? (0, _wordpress_i18n.__)("Upgrade Elementor AI", "elementor") : (0, _wordpress_i18n.__)("Get Elementor AI", "elementor");
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, {
			component: "span",
			"aria-owns": isPopoverOpen ? popoverId$1 : void 0,
			"aria-haspopup": "true",
			onMouseEnter: showPopover,
			onMouseLeave: hidePopover,
			ref: anchorEl,
			display: "flex",
			alignItems: "center"
		}, /*#__PURE__*/ react.default.createElement(Chip$2, {
			color: "promotion",
			label: (0, _wordpress_i18n.__)("Upgrade", "elementor"),
			icon: /*#__PURE__*/ react.default.createElement(_elementor_icons.AIIcon, null),
			size: "small"
		}), /*#__PURE__*/ react.default.createElement(_elementor_ui.Popper, {
			open: isPopoverOpen,
			anchorEl: anchorEl.current,
			sx: {
				zIndex: "170001",
				maxWidth: 300
			},
			modifiers: [{
				name: "arrow",
				enabled: true,
				options: { element: arrowEl.current }
			}]
		}, /*#__PURE__*/ react.default.createElement(StyledContent$3, null, /*#__PURE__*/ react.default.createElement(StyledArrow$1, { ref: arrowEl }), /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, {
			variant: "h5",
			color: "text.primary"
		}, (0, _wordpress_i18n.__)("Unlimited access to Elementor AI", "elementor")), /*#__PURE__*/ react.default.createElement(_elementor_ui.List, { sx: { mb: 1 } }, upgradeBullets.map(function(bullet, index) {
			return /*#__PURE__*/ react.default.createElement(_elementor_ui.ListItem, {
				key: index,
				disableGutters: true,
				sx: { alignItems: "flex-start" }
			}, /*#__PURE__*/ react.default.createElement(_elementor_ui.ListItemIcon, null, /*#__PURE__*/ react.default.createElement(_elementor_icons.CheckedCircleIcon, null)), /*#__PURE__*/ react.default.createElement(_elementor_ui.ListItemText, { sx: { m: 0 } }, /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, { variant: "body2" }, bullet)));
		})), /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, {
			variant: "contained",
			color: "promotion",
			size: "small",
			href: actionUrl,
			target: "_blank",
			startIcon: /*#__PURE__*/ react.default.createElement(_elementor_icons.AIIcon, null),
			sx: { "&:hover": { color: "promotion.contrastText" } }
		}, actionLabel))));
	};
	UpgradeChip.propTypes = {
		hasSubscription: import_prop_types$7.default.bool,
		usagePercentage: import_prop_types$7.default.number
	};

//#endregion
//#region node_modules/@babel/runtime/helpers/esm/arrayWithoutHoles.js
	init_arrayLikeToArray();
	function _arrayWithoutHoles(r) {
		if (Array.isArray(r)) return _arrayLikeToArray$2(r);
	}

//#endregion
//#region node_modules/@babel/runtime/helpers/esm/iterableToArray.js
	function _iterableToArray(r) {
		if ("undefined" != typeof Symbol && null != r[Symbol.iterator] || null != r["@@iterator"]) return Array.from(r);
	}

//#endregion
//#region node_modules/@babel/runtime/helpers/esm/nonIterableSpread.js
	function _nonIterableSpread() {
		throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
	}

//#endregion
//#region node_modules/@babel/runtime/helpers/esm/toConsumableArray.js
	init_unsupportedIterableToArray();
	function _toConsumableArray(r) {
		return _arrayWithoutHoles(r) || _iterableToArray(r) || _unsupportedIterableToArray$2(r) || _nonIterableSpread();
	}

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/context/location-context.js
	init_slicedToArray();
	init_defineProperty$1();
	function ownKeys$24(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	__name(ownKeys$24, "ownKeys");
	function _objectSpread$24(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$24(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$24(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	__name(_objectSpread$24, "_objectSpread");
	var RoutesContext = (0, react.createContext)(null);
	var NAVIGATE = "NAVIGATE";
	var BACK = "BACK";
	var initialValue$1 = {
		current: "",
		history: []
	};
	var reducer$2 = function reducer(state, _ref) {
		var type = _ref.type;
		var payload = _ref.payload;
		switch (type) {
			case NAVIGATE: return _objectSpread$24(_objectSpread$24({}, state), {}, {
				current: payload,
				history: [].concat(_toConsumableArray(state.history), [payload])
			});
			case BACK: return _objectSpread$24(_objectSpread$24({}, state), {}, {
				current: state.history[state.history.length - 2],
				history: state.history.slice(0, state.history.length - 1)
			});
			default: return state;
		}
	};
	var LocationProvider = function LocationProvider(_ref2) {
		var children = _ref2.children;
		var _useReducer2 = _slicedToArray((0, react.useReducer)(reducer$2, initialValue$1), 2);
		var state = _useReducer2[0];
		var dispatch = _useReducer2[1];
		return /*#__PURE__*/ react.default.createElement(RoutesContext.Provider, { value: {
			state,
			dispatch
		} }, children);
	};
	LocationProvider.propTypes = { children: import_prop_types$7.default.node };
	var useLocation = function useLocation() {
		var current = (arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {}).current;
		var _useContext = (0, react.useContext)(RoutesContext);
		var state = _useContext.state;
		var dispatch = _useContext.dispatch;
		var navigate = function navigate(location) {
			dispatch({
				type: NAVIGATE,
				payload: location
			});
		};
		var back = function back() {
			dispatch({ type: BACK });
		};
		(0, react.useEffect)(function() {
			if (current) navigate(current);
		}, []);
		return {
			current: state.current,
			history: state.history,
			navigate,
			back
		};
	};

//#endregion
//#region modules/ai/assets/js/editor/components/ui/panel.js
	init_extends();
	init_defineProperty$1();
	init_objectWithoutProperties$1();
	var _excluded$29 = ["sx"];
	function ownKeys$23(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	__name(ownKeys$23, "ownKeys");
	function _objectSpread$23(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$23(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$23(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	__name(_objectSpread$23, "_objectSpread");
	var Panel = function Panel(_ref) {
		var _ref$sx = _ref.sx;
		var sx = _ref$sx === void 0 ? {} : _ref$sx;
		var props = _objectWithoutProperties$1(_ref, _excluded$29);
		return /*#__PURE__*/ react.default.createElement(react.default.Fragment, null, /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, _extends$1({ sx: _objectSpread$23({
			p: 4,
			width: 360,
			flexShrink: 0,
			height: "100%",
			overflowY: "auto"
		}, sx) }, props), props.children), /*#__PURE__*/ react.default.createElement(_elementor_ui.Divider, { orientation: "vertical" }));
	};
	Panel.propTypes = {
		children: import_prop_types$7.default.node,
		sx: import_prop_types$7.default.object
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/components/generate-loader.js
	var GenerateLoader = function GenerateLoader() {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			alignItems: "center",
			justifyContent: "center",
			gap: 2,
			width: "100%"
		}, /*#__PURE__*/ react.default.createElement(_elementor_icons.AIIcon, { sx: {
			color: "text.primary",
			fontSize: "60px",
			mb: 1
		} }), /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, {
			variant: "h5",
			sx: { color: "text.primary" }
		}, (0, _wordpress_i18n.__)("Bringing your vision to life...", "elementor")), /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, { variant: "body1" }, (0, _wordpress_i18n.__)("Hold tight, painting dreams might take a moment.", "elementor")), /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, { sx: {
			px: 1.5,
			py: 2.5,
			width: "100%",
			maxWidth: 600
		} }, /*#__PURE__*/ react.default.createElement(_elementor_ui.LinearProgress, { color: "inherit" })));
	};

//#endregion
//#region modules/ai/assets/js/editor/icons/chevron-left-icon.js
	init_extends();
	var ChevronLeftIcon = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 24 24" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M15.5303 18.2803C15.8232 17.9874 15.8232 17.5126 15.5303 17.2197L10.0607 11.75L15.5303 6.28033C15.8232 5.98744 15.8232 5.51256 15.5303 5.21967C15.2374 4.92678 14.7626 4.92678 14.4697 5.21967L8.46967 11.2197C8.17678 11.5126 8.17678 11.9874 8.46967 12.2803L14.4697 18.2803C14.7626 18.5732 15.2374 18.5732 15.5303 18.2803Z"
		}));
	});

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/components/back-button.js
	init_extends();
	init_objectWithoutProperties$1();
	var _excluded$28 = ["children"];
	var StyledChevronLeftIcon$1 = (0, _elementor_ui.withDirection)(ChevronLeftIcon);
	var BackButton$1 = function BackButton(_ref) {
		var children = _ref.children;
		var props = _objectWithoutProperties$1(_ref, _excluded$28);
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, _extends$1({
			size: "small",
			variant: "text",
			color: "secondary",
			startIcon: /*#__PURE__*/ react.default.createElement(StyledChevronLeftIcon$1, null)
		}, props), children || (0, _wordpress_i18n.__)("Back", "elementor"));
	};
	BackButton$1.propTypes = { children: import_prop_types$7.default.node };

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/context/global-settings-context.js
	var GlobalSettingsContext = (0, react.createContext)({});
	var GlobalSettingsProvider = function GlobalSettingsProvider(_ref) {
		var settings = _ref.settings;
		var children = _ref.children;
		return /*#__PURE__*/ react.default.createElement(GlobalSettingsContext.Provider, { value: settings }, children);
	};
	GlobalSettingsProvider.propTypes = {
		settings: import_prop_types$7.default.object,
		children: import_prop_types$7.default.node
	};
	var useGlobalSettings = function useGlobalSettings() {
		return (0, react.useContext)(GlobalSettingsContext);
	};

//#endregion
//#region modules/ai/assets/js/editor/components/upgrade-banner.js
	init_extends();
	init_objectWithoutProperties$1();
	var _excluded$27 = ["onClose"];
	var UpgradeBanner = function UpgradeBanner(_ref) {
		var onClose = _ref.onClose;
		var props = _objectWithoutProperties$1(_ref, _excluded$27);
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Alert, _extends$1({
			icon: false,
			action: /*#__PURE__*/ react.default.createElement(_elementor_ui.AlertAction, { onClick: function onClick() {
				return window.open("https://go.elementor.com/ai-banner-free-upgrade/", "_blank");
			} }, (0, _wordpress_i18n.__)("Upgrade", "elementor")),
			variant: "filled",
			color: "promotion",
			onClose
		}, props), (0, _wordpress_i18n.__)("You’re using a limited license. Get maximum access to Elementor AI.", "elementor"));
	};
	UpgradeBanner.propTypes = {
		onClose: import_prop_types$7.default.func,
		sx: import_prop_types$7.default.object
	};

//#endregion
//#region modules/ai/assets/js/editor/helpers/features-enum.js
	var FREE_TRIAL_FEATURES_NAMES = {
		TEXT: (0, _wordpress_i18n.__)("Text", "elementor"),
		IMAGES: (0, _wordpress_i18n.__)("Images", "elementor"),
		CODE: (0, _wordpress_i18n.__)("Code", "elementor"),
		CONTAINERS: (0, _wordpress_i18n.__)("Containers", "elementor"),
		ANIMATION: (0, _wordpress_i18n.__)("Animation", "elementor")
	};

//#endregion
//#region modules/ai/assets/js/editor/components/usage-limit-alert.js
	init_objectWithoutProperties$1();
	init_extends();
	init_defineProperty$1();
	var _excluded$26 = [
		"onClose",
		"usagePercentage",
		"hasSubscription",
		"feature"
	];
	var KEY_SUBSCRIPTION = "subscription";
	var KEY_NO_SUBSCRIPTION = "noSubscription";
	var getUsageTitle = function getUsageTitle(percentage) {
		return (0, _wordpress_i18n.sprintf)((0, _wordpress_i18n.__)("You’ve used %s of credits for this AI feature.", "elementor"), percentage);
	};
	var CREDITS_95_USAGE_TITLE = getUsageTitle("95%");
	var CREDITS_80_USAGE_TITLE = getUsageTitle("80%");
	var CREDITS_75_USAGE_TITLE = getUsageTitle("75%");
	var DESCRIPTION_SUBSCRIPTION = (0, _wordpress_i18n.__)("Get maximum access.", "elementor");
	var FEATURES = Object.keys(FREE_TRIAL_FEATURES_NAMES);
	var getDescriptionNoSubscription = function getDescriptionNoSubscription(excludedFeature) {
		var featuresList = FEATURES.filter(function(feature) {
			return feature !== excludedFeature;
		}).map(function(feature) {
			return FREE_TRIAL_FEATURES_NAMES[feature];
		}).join(", ");
		return (0, _wordpress_i18n.sprintf)((0, _wordpress_i18n.__)("Upgrade now to keep using this feature. You still have credits for other AI features (%s, etc.)", "elementor"), featuresList);
	};
	var alertConfigs = [
		{
			threshold: 95,
			title: _defineProperty$1(_defineProperty$1({}, KEY_SUBSCRIPTION, CREDITS_95_USAGE_TITLE), KEY_NO_SUBSCRIPTION, CREDITS_95_USAGE_TITLE),
			url: _defineProperty$1(_defineProperty$1({}, KEY_SUBSCRIPTION, "https://go.elementor.com/ai-banner-paid-95-limit-reach/"), KEY_NO_SUBSCRIPTION, "https://go.elementor.com/ai-banner-free-95-limit-reach/"),
			color: "error"
		},
		{
			threshold: 80,
			title: _defineProperty$1(_defineProperty$1({}, KEY_SUBSCRIPTION, CREDITS_80_USAGE_TITLE), KEY_NO_SUBSCRIPTION, CREDITS_80_USAGE_TITLE),
			url: _defineProperty$1(_defineProperty$1({}, KEY_SUBSCRIPTION, "https://go.elementor.com/ai-banner-paid-80-limit-reach/"), KEY_NO_SUBSCRIPTION, "https://go.elementor.com/ai-banner-free-80-limit-reach/"),
			color: "warning"
		},
		{
			threshold: 75,
			title: _defineProperty$1(_defineProperty$1({}, KEY_SUBSCRIPTION, CREDITS_75_USAGE_TITLE), KEY_NO_SUBSCRIPTION, CREDITS_75_USAGE_TITLE),
			url: _defineProperty$1(_defineProperty$1({}, KEY_SUBSCRIPTION, "https://go.elementor.com/ai-banner-paid-80-limit-reach/"), KEY_NO_SUBSCRIPTION, "https://go.elementor.com/ai-banner-free-80-limit-reach/"),
			color: "warning"
		}
	];
	var UpgradeButton = function UpgradeButton(props) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, _extends$1({
			color: "inherit",
			variant: "outlined",
			sx: { border: "2px solid" }
		}, props), (0, _wordpress_i18n.__)("Upgrade now", "elementor"));
	};
	var UsageLimitAlert = function UsageLimitAlert(_ref) {
		_ref.onClose;
		var usagePercentage = _ref.usagePercentage;
		var hasSubscription = _ref.hasSubscription;
		var feature = _ref.feature;
		var props = _objectWithoutProperties$1(_ref, _excluded$26);
		var config = alertConfigs.find(function(_ref2) {
			return usagePercentage >= _ref2.threshold;
		});
		if (!config) return null;
		var subscriptionType = hasSubscription ? KEY_SUBSCRIPTION : KEY_NO_SUBSCRIPTION;
		var description = _defineProperty$1(_defineProperty$1({}, KEY_SUBSCRIPTION, DESCRIPTION_SUBSCRIPTION), KEY_NO_SUBSCRIPTION, getDescriptionNoSubscription(feature));
		var title = config.title;
		var url = config.url;
		var color = config.color;
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Alert, _extends$1({
			severity: "warning",
			action: /*#__PURE__*/ react.default.createElement(UpgradeButton, { onClick: function handleUpgradeClick() {
				return window.open(url[subscriptionType], "_blank");
			} }),
			color
		}, props), /*#__PURE__*/ react.default.createElement(_elementor_ui.AlertTitle, null, title[subscriptionType]), description[subscriptionType]);
	};
	UsageLimitAlert.propTypes = {
		onClose: import_prop_types$7.default.func,
		usagePercentage: import_prop_types$7.default.number,
		hasSubscription: import_prop_types$7.default.bool,
		feature: import_prop_types$7.default.string
	};

//#endregion
//#region modules/ai/assets/js/editor/hooks/use-upgrade-message.js
	init_use_introduction();
	var USAGE_PERCENTAGE_THRESHOLD = 75;
	var useUpgradeMessage = function useUpgradeMessage(_ref) {
		var usagePercentage = _ref.usagePercentage;
		var hasSubscription = _ref.hasSubscription;
		var _useIntroduction = useIntroduction("e-ai-upgrade-message");
		var isBannerViewed = _useIntroduction.isViewed;
		var markBannerAsViewed = _useIntroduction.markAsViewed;
		var isFreeUser = !hasSubscription;
		return {
			showBadge: usagePercentage >= 75 || isFreeUser,
			showBanner: !isBannerViewed && isFreeUser && usagePercentage < 75,
			markBannerAsViewed
		};
	};

//#endregion
//#region modules/ai/assets/js/editor/components/usage-messages.js
	var UsageMessages = function UsageMessages(_ref) {
		var hasSubscription = _ref.hasSubscription;
		var usagePercentage = _ref.usagePercentage;
		var sx = _ref.sx;
		var feature = _ref.feature;
		var _useUpgradeMessage = useUpgradeMessage({
			usagePercentage,
			hasSubscription
		});
		var showBanner = _useUpgradeMessage.showBanner;
		var markBannerAsViewed = _useUpgradeMessage.markBannerAsViewed;
		return /*#__PURE__*/ react.default.createElement(react.default.Fragment, null, showBanner && /*#__PURE__*/ react.default.createElement(UpgradeBanner, {
			onClose: markBannerAsViewed,
			sx
		}), /*#__PURE__*/ react.default.createElement(UsageLimitAlert, {
			hasSubscription,
			usagePercentage,
			sx,
			feature
		}));
	};
	UsageMessages.propTypes = {
		hasSubscription: import_prop_types$7.default.bool,
		usagePercentage: import_prop_types$7.default.number,
		sx: import_prop_types$7.default.object,
		feature: import_prop_types$7.default.string
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/components/view.js
	init_extends();
	init_defineProperty$1();
	init_objectWithoutProperties$1();
	init_loader();
	init_prompt_error_message();
	var _excluded$25 = ["sx"];
	var _excluded2$4 = ["primary", "secondary"];
	var _excluded3$2 = ["primary", "secondary"];
	var _excluded4$1 = ["children"];
	var _excluded5$1 = [
		"isLoading",
		"isGenerating",
		"children"
	];
	var _excluded6 = ["sx"];
	function ownKeys$22(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	__name(ownKeys$22, "ownKeys");
	function _objectSpread$22(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$22(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$22(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	__name(_objectSpread$22, "_objectSpread");
	var ViewBackButton = function ViewBackButton(_ref) {
		var _ref$sx = _ref.sx;
		var sx = _ref$sx === void 0 ? {} : _ref$sx;
		var props = _objectWithoutProperties$1(_ref, _excluded$25);
		var back = useLocation().back;
		if (useGlobalSettings().hideBackButton) return null;
		return /*#__PURE__*/ react.default.createElement(BackButton$1, _extends$1({ onClick: function onClick(e) {
			e.preventDefault();
			back();
		} }, props, { sx: _objectSpread$22({
			mb: 1,
			alignSelf: "flex-start"
		}, sx) }));
	};
	ViewBackButton.propTypes = { sx: import_prop_types$7.default.object };
	var PanelHeading = function PanelHeading(_ref2) {
		var primary = _ref2.primary;
		var secondary = _ref2.secondary;
		var props = _objectWithoutProperties$1(_ref2, _excluded2$4);
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, _extends$1({
			spacing: 1,
			sx: { mb: 3 }
		}, props), /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, { variant: "h4" }, primary), secondary && /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, {
			variant: "body1",
			color: "secondary"
		}, secondary));
	};
	PanelHeading.propTypes = {
		primary: import_prop_types$7.default.string.isRequired,
		secondary: import_prop_types$7.default.string
	};
	var ContentHeading = function ContentHeading(_ref3) {
		var primary = _ref3.primary;
		var secondary = _ref3.secondary;
		var props = _objectWithoutProperties$1(_ref3, _excluded3$2);
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, _extends$1({
			gap: 1.5,
			sx: { mb: 3 }
		}, props), /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, { variant: "h6" }, primary), secondary && /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, {
			variant: "subtitle1",
			color: "secondary"
		}, secondary));
	};
	ContentHeading.propTypes = {
		primary: import_prop_types$7.default.string.isRequired,
		secondary: import_prop_types$7.default.string
	};
	var View = function View(_ref4) {
		var children = _ref4.children;
		var props = _objectWithoutProperties$1(_ref4, _excluded4$1);
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, _extends$1({
			className: "e-ai-dialog-content",
			display: "flex",
			sx: {
				overflowY: "auto",
				position: "relative"
			},
			height: "100%"
		}, props), children);
	};
	var Content = function Content(_ref5) {
		var _ref5$isLoading = _ref5.isLoading;
		var isLoading = _ref5$isLoading === void 0 ? false : _ref5$isLoading;
		var _ref5$isGenerating = _ref5.isGenerating;
		var isGenerating = _ref5$isGenerating === void 0 ? false : _ref5$isGenerating;
		var children = _ref5.children;
		var props = _objectWithoutProperties$1(_ref5, _excluded5$1);
		if (isGenerating) return /*#__PURE__*/ react.default.createElement(GenerateLoader, null);
		if (isLoading) return /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, { sx: {
			width: "100%",
			maxWidth: 600,
			margin: "0 auto",
			alignSelf: "center"
		} }, /*#__PURE__*/ react.default.createElement(Loader, { color: "inherit" }));
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, _extends$1({
			sx: {
				overflowY: "auto",
				p: 4
			},
			flexGrow: 1
		}, props), children);
	};
	Content.propTypes = {
		children: import_prop_types$7.default.node,
		isLoading: import_prop_types$7.default.bool,
		isGenerating: import_prop_types$7.default.bool
	};
	var ErrorMessage = function ErrorMessage(_ref6) {
		var _ref6$sx = _ref6.sx;
		var sx = _ref6$sx === void 0 ? {} : _ref6$sx;
		var props = _objectWithoutProperties$1(_ref6, _excluded6);
		return /*#__PURE__*/ react.default.createElement(PromptErrorMessage, _extends$1({ actionPosition: "bottom" }, props, { sx: _objectSpread$22({ mb: 2.5 }, sx) }));
	};
	ErrorMessage.propTypes = { sx: import_prop_types$7.default.object };
	var ViewPanel = function ViewPanel(props) {
		var _useGlobalSettings2 = useGlobalSettings();
		var hasSubscription = _useGlobalSettings2.hasSubscription;
		var usagePercentage = _useGlobalSettings2.usagePercentage;
		return /*#__PURE__*/ react.default.createElement(Panel, null, /*#__PURE__*/ react.default.createElement(UsageMessages, {
			hasSubscription,
			usagePercentage,
			sx: { mb: 4 },
			feature: FREE_TRIAL_FEATURES_NAMES.IMAGES
		}), props.children);
	};
	ViewPanel.propTypes = { children: import_prop_types$7.default.node };
	View.Panel = ViewPanel;
	View.Content = Content;
	View.BackButton = ViewBackButton;
	View.ErrorMessage = ErrorMessage;
	View.PanelHeading = PanelHeading;
	View.ContentHeading = ContentHeading;
	View.propTypes = { children: import_prop_types$7.default.node };

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/constants/index.js
	var LOCATIONS = {
		RESIZE: "resize",
		GENERATE: "generate",
		VARIATIONS: "variations",
		IMAGE_TOOLS: "image-tools",
		IN_PAINTING: "in-painting",
		OUT_PAINTING: "out-painting",
		REMOVE_BACKGROUND: "remove-background",
		REPLACE_BACKGROUND: "replace-background",
		REMOVE_TEXT: "remove-text",
		CLEANUP: "cleanup",
		PRODUCT_IMAGE_UNIFICATION: "product-image-unification",
		ISOLATE_OBJECT: "isolate-objects"
	};
	var IMAGE_PROMPT_SETTINGS = {
		IMAGE_TYPE: "image_type",
		IMAGE_STYLE: "style_preset",
		IMAGE_STRENGTH: "image_strength",
		IMAGE_RATIO: "ratio",
		IMAGE_ZOOM: "zoom",
		IMAGE_UPSCALE: "upscale_to",
		IMAGE_BACKGROUND_COLOR: "background_color"
	};
	var FEATURE_IDENTIFIER = { ISOLATE_OBJECT: "isolate-object" };
	var IMAGE_PROMPT_CATEGORIES = {
		"": {
			label: (0, _wordpress_i18n.__)("None", "elementor"),
			subCategories: {}
		},
		photographic: {
			label: (0, _wordpress_i18n.__)("Photographic", "elementor"),
			subCategories: {
				"": (0, _wordpress_i18n.__)("None", "elementor"),
				landscape: (0, _wordpress_i18n.__)("Landscape", "elementor"),
				macro: (0, _wordpress_i18n.__)("Macro", "elementor"),
				portrait: (0, _wordpress_i18n.__)("Portrait", "elementor"),
				"long-exposure": (0, _wordpress_i18n.__)("Long Exposure", "elementor"),
				product: (0, _wordpress_i18n.__)("Product", "elementor"),
				photorealistic: (0, _wordpress_i18n.__)("Photorealistic", "elementor")
			}
		},
		vector: {
			label: (0, _wordpress_i18n.__)("Vector / Logo", "elementor"),
			subCategories: {
				"": (0, _wordpress_i18n.__)("None", "elementor"),
				"typographic-logo": (0, _wordpress_i18n.__)("Typographic Logo", "elementor"),
				"shape-logo": (0, _wordpress_i18n.__)("Shape Logo", "elementor"),
				"handwritten-logo": (0, _wordpress_i18n.__)("Handwritten Logo", "elementor"),
				"line-art-logo": (0, _wordpress_i18n.__)("Line Art Logo", "elementor"),
				"old-school-logo": (0, _wordpress_i18n.__)("Old School Logo", "elementor")
			}
		},
		background: {
			label: (0, _wordpress_i18n.__)("Background", "elementor"),
			subCategories: {
				"": (0, _wordpress_i18n.__)("None", "elementor"),
				floral: (0, _wordpress_i18n.__)("Floral", "elementor"),
				gradient: (0, _wordpress_i18n.__)("Gradient", "elementor"),
				mosaic: (0, _wordpress_i18n.__)("Mosaic", "elementor"),
				neon: (0, _wordpress_i18n.__)("Neon", "elementor"),
				bokeh: (0, _wordpress_i18n.__)("Bokeh", "elementor")
			}
		},
		handmade: {
			label: (0, _wordpress_i18n.__)("Handmade", "elementor"),
			subCategories: {
				"": (0, _wordpress_i18n.__)("None", "elementor"),
				doodle: (0, _wordpress_i18n.__)("Doodle", "elementor"),
				"line-art": (0, _wordpress_i18n.__)("Line Art", "elementor"),
				"oil-painting": (0, _wordpress_i18n.__)("Oil Painting", "elementor"),
				"pencil-drawing": (0, _wordpress_i18n.__)("Pencil Drawing", "elementor"),
				watercolor: (0, _wordpress_i18n.__)("Watercolor", "elementor")
			}
		},
		"digital-art": {
			label: (0, _wordpress_i18n.__)("Digital Art", "elementor"),
			subCategories: {
				"": (0, _wordpress_i18n.__)("None", "elementor"),
				amine: (0, _wordpress_i18n.__)("Anime", "elementor"),
				cartoon: (0, _wordpress_i18n.__)("Cartoon", "elementor"),
				cinematic: (0, _wordpress_i18n.__)("Cinematic", "elementor"),
				"comic-book": (0, _wordpress_i18n.__)("Comic Book", "elementor"),
				"fantasy-art": (0, _wordpress_i18n.__)("Fantasy Art", "elementor"),
				isometric: (0, _wordpress_i18n.__)("Isometric", "elementor"),
				vector: (0, _wordpress_i18n.__)("Vector", "elementor"),
				"pixel-art": (0, _wordpress_i18n.__)("Pixel Art", "elementor"),
				"low-poly": (0, _wordpress_i18n.__)("Low Poly", "elementor"),
				"neon-punk": (0, _wordpress_i18n.__)("Neon Punk", "elementor")
			}
		},
		"3d": {
			label: (0, _wordpress_i18n.__)("3D", "elementor"),
			subCategories: {
				"": (0, _wordpress_i18n.__)("None", "elementor"),
				clay: (0, _wordpress_i18n.__)("Clay", "elementor"),
				digital: (0, _wordpress_i18n.__)("Digital", "elementor"),
				origami: (0, _wordpress_i18n.__)("Origami", "elementor"),
				stone: (0, _wordpress_i18n.__)("Stone", "elementor"),
				wood: (0, _wordpress_i18n.__)("Wood", "elementor")
			}
		}
	};
	var IMAGE_ASPECT_RATIO = {
		"1:1": {
			width: 512,
			height: 512,
			label: (0, _wordpress_i18n.__)("Square", "elementor") + " (1:1)"
		},
		"3:2": {
			width: 640,
			height: 448,
			label: (0, _wordpress_i18n.__)("Landscape", "elementor") + " (3:2)"
		},
		"4:3": {
			width: 640,
			height: 448,
			label: (0, _wordpress_i18n.__)("Landscape", "elementor") + " (4:3)"
		},
		"16:9": {
			width: 704,
			height: 384,
			label: (0, _wordpress_i18n.__)("Landscape", "elementor") + " (16:9)"
		},
		"2:3": {
			width: 448,
			height: 640,
			label: (0, _wordpress_i18n.__)("Portrait", "elementor") + " (2:3)"
		},
		"3:4": {
			width: 448,
			height: 640,
			label: (0, _wordpress_i18n.__)("Portrait", "elementor") + " (3:4)"
		},
		"9:16": {
			width: 384,
			height: 704,
			label: (0, _wordpress_i18n.__)("Portrait", "elementor") + " (9:16)"
		}
	};
	var IMAGE_PLACEHOLDERS_HOSTS = { WIREFRAME: "gene-\\d+\\.live\\.strattic\\.io" };

//#endregion
//#region modules/ai/assets/js/editor/components/ui/overlay.js
	var Overlay = (0, _elementor_ui.styled)(_elementor_ui.Box)(function(_ref) {
		var theme = _ref.theme;
		return {
			width: "100%",
			height: "100%",
			position: "absolute",
			backgroundColor: "rgba(0,0,0,0.5)",
			transition: "opacity ".concat(theme.transitions.duration.short, "ms ").concat(theme.transitions.easing.easeInOut),
			opacity: 0,
			"&:hover": { opacity: 1 }
		};
	});

//#endregion
//#region modules/ai/assets/js/editor/components/ui/overlay-bar.js
	var OverlayBar = (0, _elementor_ui.styled)(_elementor_ui.Stack)(function(_ref) {
		var theme = _ref.theme;
		var position = _ref.position;
		var style = {
			width: "100%",
			position: "absolute",
			left: 0,
			padding: theme.spacing(2.5)
		};
		if ("top" === position) style.top = 0;
		else if ("bottom" === position) style.bottom = 0;
		return style;
	});

//#endregion
//#region modules/ai/assets/js/editor/components/ui/overlay-bar-text.js
	init_extends();
	init_objectWithoutProperties$1();
	var _excluded$24 = ["children"];
	var OverlayBarText = function OverlayBarText(_ref) {
		var children = _ref.children;
		var props = _objectWithoutProperties$1(_ref, _excluded$24);
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, _extends$1({
			variant: "caption",
			color: "common.white"
		}, props), children);
	};
	OverlayBarText.propTypes = { children: import_prop_types$7.default.node };

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/components/gallery.js
	init_defineProperty$1();
	init_slicedToArray();
	init_extends();
	init_objectWithoutProperties$1();
	var _excluded$23 = ["children"];
	var _excluded2$3 = [
		"alt",
		"src",
		"text",
		"children",
		"aspectRatio",
		"variant",
		"OverlayBarProps",
		"numImagesInRow",
		"overlay",
		"onSelectChange",
		"checkboxColor",
		"isLoading",
		"initialChecked"
	];
	function ownKeys$21(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	__name(ownKeys$21, "ownKeys");
	function _objectSpread$21(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$21(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$21(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	__name(_objectSpread$21, "_objectSpread");
	var aspectRatios = Object.keys(IMAGE_ASPECT_RATIO);
	var Gallery = function Gallery(_ref) {
		var children = _ref.children;
		var props = _objectWithoutProperties$1(_ref, _excluded$23);
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.ImageList, _extends$1({
			width: "100%",
			cols: 2,
			gap: 24
		}, props), children);
	};
	Gallery.propTypes = { children: import_prop_types$7.default.node };
	var GalleryImage = function GalleryImage(_ref2) {
		var _elementorCommon$conf;
		var _elementorCommon;
		var alt = _ref2.alt;
		var src = _ref2.src;
		var text = _ref2.text;
		var children = _ref2.children;
		var _ref2$aspectRatio = _ref2.aspectRatio;
		var aspectRatio = _ref2$aspectRatio === void 0 ? "1:1" : _ref2$aspectRatio;
		var _ref2$variant = _ref2.variant;
		var variant = _ref2$variant === void 0 ? "contained" : _ref2$variant;
		var _ref2$OverlayBarProps = _ref2.OverlayBarProps;
		var OverlayBarProps = _ref2$OverlayBarProps === void 0 ? {} : _ref2$OverlayBarProps;
		var _ref2$numImagesInRow = _ref2.numImagesInRow;
		var numImagesInRow = _ref2$numImagesInRow === void 0 ? 2 : _ref2$numImagesInRow;
		var _ref2$overlay = _ref2.overlay;
		var overlay = _ref2$overlay === void 0 ? true : _ref2$overlay;
		var _ref2$onSelectChange = _ref2.onSelectChange;
		var onSelectChange = _ref2$onSelectChange === void 0 ? null : _ref2$onSelectChange;
		var _ref2$checkboxColor = _ref2.checkboxColor;
		var checkboxColor = _ref2$checkboxColor === void 0 ? "rgba(0, 0, 0, 0.54)" : _ref2$checkboxColor;
		var _ref2$isLoading = _ref2.isLoading;
		var isLoading = _ref2$isLoading === void 0 ? false : _ref2$isLoading;
		var _ref2$initialChecked = _ref2.initialChecked;
		var initialChecked = _ref2$initialChecked === void 0 ? false : _ref2$initialChecked;
		var props = _objectWithoutProperties$1(_ref2, _excluded2$3);
		var _useState2 = _slicedToArray((0, react.useState)(initialChecked), 2);
		var isChecked = _useState2[0];
		var setIsChecked = _useState2[1];
		var style = {};
		var isRTL = (_elementorCommon$conf = (_elementorCommon = elementorCommon) === null || _elementorCommon === void 0 || (_elementorCommon = _elementorCommon.config) === null || _elementorCommon === void 0 ? void 0 : _elementorCommon.isRTL) !== null && _elementorCommon$conf !== void 0 ? _elementorCommon$conf : true;
		(0, react.useEffect)(function() {
			setIsChecked(initialChecked);
		}, [initialChecked]);
		if ("thumbnail" === variant) {
			style.width = "100%";
			style.maxHeight = "238px";
			style.minWidth = "230px";
		} else {
			style.width = "auto";
			style.height = "auto";
			style.maxWidth = "100%";
			style.maxHeight = "1:1" === aspectRatio ? "initial" : "100%";
			style.objectFit = "contain";
			style.aspectRatio = aspectRatio.replace(":", " / ");
		}
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.ImageListItem, props, /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, {
			display: "flex",
			justifyContent: "center",
			alignItems: "center",
			sx: {
				bgcolor: "action.selected",
				height: "contained" === variant && numImagesInRow <= 2 ? 336 : "auto",
				position: "relative",
				overflow: "hidden"
			}
		}, /*#__PURE__*/ react.default.createElement(react.default.Fragment, null, isLoading ? /*#__PURE__*/ react.default.createElement(_elementor_ui.Skeleton, {
			sx: _objectSpread$21(_objectSpread$21({}, style), {}, {
				width: "100%",
				height: "100% "
			}),
			animation: "wave",
			variant: "rounded"
		}, /*#__PURE__*/ react.default.createElement("img", {
			alt,
			src,
			style: _objectSpread$21(_objectSpread$21({}, style), {}, { visibility: "hidden" })
		})) : /*#__PURE__*/ react.default.createElement(react.default.Fragment, null, onSelectChange && /*#__PURE__*/ react.default.createElement(_elementor_ui.Checkbox, {
			onClick: function onClick() {
				var newVal = !isChecked;
				setIsChecked(newVal);
				onSelectChange(newVal);
			},
			checked: isChecked,
			sx: {
				position: "absolute",
				top: 0,
				left: isRTL ? void 0 : 0,
				right: isRTL ? 0 : void 0,
				"& .MuiSvgIcon-root": { color: checkboxColor }
			}
		}), /*#__PURE__*/ react.default.createElement("img", {
			alt,
			src,
			style
		})))), overlay && children && /*#__PURE__*/ react.default.createElement(Overlay, null, /*#__PURE__*/ react.default.createElement(OverlayBar, _extends$1({
			gap: 1,
			position: "bottom"
		}, OverlayBarProps), text && /*#__PURE__*/ react.default.createElement(OverlayBarText, null, text), children)));
	};
	GalleryImage.propTypes = {
		alt: import_prop_types$7.default.string,
		src: import_prop_types$7.default.string,
		text: import_prop_types$7.default.string,
		children: import_prop_types$7.default.node,
		BoxProps: import_prop_types$7.default.object,
		OverlayBarProps: import_prop_types$7.default.object,
		aspectRatio: import_prop_types$7.default.oneOf(aspectRatios),
		variant: import_prop_types$7.default.oneOf(["contained", "thumbnail"]),
		numImagesInRow: import_prop_types$7.default.number,
		overlay: import_prop_types$7.default.bool,
		onSelectChange: import_prop_types$7.default.func,
		checkboxColor: import_prop_types$7.default.string,
		initialChecked: import_prop_types$7.default.bool,
		isLoading: import_prop_types$7.default.bool
	};
	Gallery.Image = GalleryImage;

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/components/image-form.js
	init_extends();
	init_objectWithoutProperties$1();
	var _excluded$22 = ["children"];
	var ImageForm = function ImageForm(_ref) {
		var children = _ref.children;
		var props = _objectWithoutProperties$1(_ref, _excluded$22);
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, _extends$1({ component: "form" }, props), /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, { spacing: 2.5 }, children));
	};
	ImageForm.propTypes = { children: import_prop_types$7.default.node };

//#endregion
//#region modules/ai/assets/js/editor/icons/wand-icon.js
	init_extends();
	var WandIcon = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 24 24" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M9 2.25C9.41421 2.25 9.75 2.58579 9.75 3C9.75 3.33152 9.8817 3.64946 10.1161 3.88388C10.3505 4.1183 10.6685 4.25 11 4.25C11.4142 4.25 11.75 4.58579 11.75 5C11.75 5.41421 11.4142 5.75 11 5.75C10.6685 5.75 10.3505 5.8817 10.1161 6.11612C9.8817 6.35054 9.75 6.66848 9.75 7C9.75 7.41421 9.41421 7.75 9 7.75C8.58579 7.75 8.25 7.41421 8.25 7C8.25 6.66848 8.1183 6.35054 7.88388 6.11612C7.64946 5.8817 7.33152 5.75 7 5.75C6.58579 5.75 6.25 5.41421 6.25 5C6.25 4.58579 6.58579 4.25 7 4.25C7.33152 4.25 7.64946 4.1183 7.88388 3.88388C8.1183 3.64946 8.25 3.33152 8.25 3C8.25 2.58579 8.58579 2.25 9 2.25ZM9 4.88746C8.98182 4.90673 8.96333 4.92576 8.94454 4.94454C8.92576 4.96333 8.90673 4.98182 8.88746 5C8.90673 5.01818 8.92576 5.03667 8.94454 5.05546C8.96333 5.07424 8.98182 5.09327 9 5.11254C9.01818 5.09327 9.03667 5.07424 9.05546 5.05546C9.07424 5.03667 9.09327 5.01818 9.11254 5C9.09327 4.98182 9.07424 4.96333 9.05546 4.94454C9.03667 4.92576 9.01818 4.90673 9 4.88746Z"
		}), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M18.5303 2.46967C18.2374 2.17678 17.7626 2.17678 17.4697 2.46967L2.46967 17.4697C2.17678 17.7626 2.17678 18.2374 2.46967 18.5303L5.46967 21.5303C5.76256 21.8232 6.23744 21.8232 6.53033 21.5303L21.5303 6.53033C21.8232 6.23744 21.8232 5.76256 21.5303 5.46967L18.5303 2.46967ZM18 7.93934L19.9393 6L18 4.06066L16.0607 6L18 7.93934ZM15 7.06066L16.9393 9L6 19.9393L4.06066 18L15 7.06066Z"
		}), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M19.75 13C19.75 12.5858 19.4142 12.25 19 12.25C18.5858 12.25 18.25 12.5858 18.25 13C18.25 13.3315 18.1183 13.6495 17.8839 13.8839C17.6495 14.1183 17.3315 14.25 17 14.25C16.5858 14.25 16.25 14.5858 16.25 15C16.25 15.4142 16.5858 15.75 17 15.75C17.3315 15.75 17.6495 15.8817 17.8839 16.1161C18.1183 16.3505 18.25 16.6685 18.25 17C18.25 17.4142 18.5858 17.75 19 17.75C19.4142 17.75 19.75 17.4142 19.75 17C19.75 16.6685 19.8817 16.3505 20.1161 16.1161C20.3505 15.8817 20.6685 15.75 21 15.75C21.4142 15.75 21.75 15.4142 21.75 15C21.75 14.5858 21.4142 14.25 21 14.25C20.6685 14.25 20.3505 14.1183 20.1161 13.8839C19.8817 13.6495 19.75 13.3315 19.75 13ZM18.9445 14.9445C18.9633 14.9258 18.9818 14.9067 19 14.8875C19.0182 14.9067 19.0367 14.9258 19.0555 14.9445C19.0742 14.9633 19.0933 14.9818 19.1125 15C19.0933 15.0182 19.0742 15.0367 19.0555 15.0555C19.0367 15.0742 19.0182 15.0933 19 15.1125C18.9818 15.0933 18.9633 15.0742 18.9445 15.0555C18.9258 15.0367 18.9067 15.0182 18.8875 15C18.9067 14.9818 18.9258 14.9633 18.9445 14.9445Z"
		}));
	});

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/components/enhance-button.js
	init_extends();
	init_objectWithoutProperties$1();
	var _excluded$21 = ["isLoading"];
	var StyledWandIcon = (0, _elementor_ui.withDirection)(WandIcon);
	var EnhanceButton = function EnhanceButton(_ref) {
		var isLoading = _ref.isLoading;
		var props = _objectWithoutProperties$1(_ref, _excluded$21);
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Tooltip, { title: (0, _wordpress_i18n.__)("Enhance prompt", "elementor") }, /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, {
			component: "span",
			sx: { cursor: props.disabled ? "default" : "pointer" }
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.IconButton, _extends$1({
			size: "small",
			color: "secondary"
		}, props), isLoading ? /*#__PURE__*/ react.default.createElement(_elementor_ui.CircularProgress, {
			color: "secondary",
			size: 20
		}) : /*#__PURE__*/ react.default.createElement(StyledWandIcon, { fontSize: "small" }))));
	};
	EnhanceButton.propTypes = {
		disabled: import_prop_types$7.default.bool,
		isLoading: import_prop_types$7.default.bool
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-layout/context/config.js
	var MODE_LAYOUT = "layout";
	var MODE_VARIATION = "variation";
	var LAYOUT_APP_MODES = [MODE_LAYOUT, MODE_VARIATION];
	var ConfigContext = react.default.createContext({});
	var useConfig = function useConfig() {
		return react.default.useContext(ConfigContext);
	};
	var ConfigProvider = function ConfigProvider(props) {
		return /*#__PURE__*/ react.default.createElement(ConfigContext.Provider, { value: {
			mode: props.mode,
			attachmentsTypes: props.attachmentsTypes,
			onClose: props.onClose,
			onConnect: props.onConnect,
			onData: props.onData,
			onInsert: props.onInsert,
			onSelect: props.onSelect,
			onGenerate: props.onGenerate,
			currentContext: props.currentContext,
			hasPro: props.hasPro
		} }, props.children);
	};
	ConfigProvider.propTypes = {
		mode: import_prop_types$7.default.oneOf(LAYOUT_APP_MODES).isRequired,
		children: import_prop_types$7.default.node.isRequired,
		attachmentsTypes: import_prop_types$7.default.object.isRequired,
		onClose: import_prop_types$7.default.func.isRequired,
		onConnect: import_prop_types$7.default.func.isRequired,
		onData: import_prop_types$7.default.func.isRequired,
		onInsert: import_prop_types$7.default.func.isRequired,
		onSelect: import_prop_types$7.default.func.isRequired,
		onGenerate: import_prop_types$7.default.func.isRequired,
		currentContext: import_prop_types$7.default.object,
		hasPro: import_prop_types$7.default.bool
	};

//#endregion
//#region modules/ai/assets/js/editor/hooks/use-prompt-enhancer.js
	init_api();
	init_use_prompt();
	var enhancePromptMap = /* @__PURE__ */ new Map([["media", getImagePromptEnhanced], ["layout", getLayoutPromptEnhanced]]);
	var getResult = function getResult(prompt, type, enhanceType) {
		if (!enhancePromptMap.has(type)) throw new Error("Invalid prompt type: ".concat(type));
		return enhancePromptMap.get(type)(prompt, enhanceType);
	};
	/**
	* Hook to enhance a prompt.
	*
	* @param {string}             prompt
	* @param {'media' | 'layout'} type
	* @return {{enhancedPrompt: string | undefined, isEnhancing: boolean, enhance: (function(...[*]): Promise)}}
	*/
	var usePromptEnhancer = function usePromptEnhancer(prompt, type) {
		var mode = useConfig().mode;
		var _usePrompt = usePrompt(function() {
			return getResult(prompt, type, mode);
		}, prompt);
		var enhancedData = _usePrompt.data;
		var isEnhancing = _usePrompt.isLoading;
		return {
			enhance: _usePrompt.send,
			isEnhancing,
			enhancedPrompt: enhancedData === null || enhancedData === void 0 ? void 0 : enhancedData.result
		};
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/components/prompt-field.js
	init_extends();
	init_objectWithoutProperties$1();
	init_textarea();
	var _excluded$20 = ["disabled", "onChange"];
	var PromptField = function PromptField(_ref) {
		var disabled = _ref.disabled;
		var _onChange = _ref.onChange;
		var props = _objectWithoutProperties$1(_ref, _excluded$20);
		var _usePromptEnhancer = usePromptEnhancer(props.value, "media");
		var enhance = _usePromptEnhancer.enhance;
		var isEnhancing = _usePromptEnhancer.isEnhancing;
		return /*#__PURE__*/ react.default.createElement(Textarea, _extends$1({
			minRows: 3,
			maxRows: 6,
			InputProps: { endAdornment: /*#__PURE__*/ react.default.createElement(_elementor_ui.InputAdornment, {
				position: "end",
				sx: {
					position: "absolute",
					bottom: "24px",
					right: "8px"
				}
			}, /*#__PURE__*/ react.default.createElement(EnhanceButton, {
				isLoading: isEnhancing,
				disabled: disabled || isEnhancing || !props.value,
				onClick: function onClick() {
					enhance(prompt).then(function(_ref2) {
						var result = _ref2.result;
						return _onChange(result);
					});
				}
			})) },
			sx: { "& .MuiInputBase-input.MuiOutlinedInput-input.MuiInputBase-inputMultiline": {
				pb: 5,
				width: "89%"
			} },
			onKeyDown: function onKeyDown(event) {
				if ("Tab" === event.key) {
					event.preventDefault();
					_onChange(props.placeholder);
				}
			}
		}, props, {
			onChange: function onChange(event) {
				return _onChange(event.target.value);
			},
			disabled: disabled || isEnhancing
		}));
	};
	PromptField.propTypes = {
		value: import_prop_types$7.default.string,
		disabled: import_prop_types$7.default.bool,
		placeholder: import_prop_types$7.default.string,
		onChange: import_prop_types$7.default.func.isRequired
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/components/image-type-select.js
	init_extends();
	init_slicedToArray();
	init_use_introduction();
	var StyledNewChip = (0, _elementor_ui.styled)(_elementor_ui.Chip)(function() {
		return { "& .MuiChip-label": {
			fontSize: "0.75rem",
			fontWeight: "normal"
		} };
	});
	var showLabel = function showLabel(label, key, isViewed, markAsViewed) {
		if (isViewed) return {
			label,
			value: key
		};
		return {
			label: /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
				direction: "row",
				width: "100%",
				justifyContent: "space-between",
				alignItems: "center"
			}, label, /*#__PURE__*/ react.default.createElement(StyledNewChip, {
				label: "New",
				color: "info",
				variant: "standard",
				size: "tiny"
			})),
			value: key,
			onClick: function onClick() {
				return markAsViewed();
			}
		};
	};
	var changeImageType = function changeImageType(e, props, markVectorAsViewed) {
		if ("vector" === e.target.value) markVectorAsViewed();
		if (props.onChange) props.onChange(e);
	};
	var ImageTypeSelect = function ImageTypeSelect(props) {
		var _useIntroduction = useIntroduction("e-ai-image-vector-option");
		var isVectorViewed = _useIntroduction.isViewed;
		var markVectorAsViewed = _useIntroduction.markAsViewed;
		var imageTypesWithNewChip = Object.entries(IMAGE_PROMPT_CATEGORIES).map(function(_ref) {
			var _ref2 = _slicedToArray(_ref, 2);
			var key = _ref2[0];
			var label = _ref2[1].label;
			if ("vector" === key) return showLabel(label, key, isVectorViewed, markVectorAsViewed);
			return {
				label,
				value: key
			};
		});
		return /*#__PURE__*/ react.default.createElement(PromptActionSelection, _extends$1({
			options: imageTypesWithNewChip,
			wrapperStyle: { width: "100%" },
			label: (0, _wordpress_i18n.__)("Image type", "elementor")
		}, props, { onChange: function onChange(e) {
			changeImageType(e, props, markVectorAsViewed);
		} }));
	};
	ImageTypeSelect.propTypes = {
		onChange: import_prop_types$7.default.func,
		value: import_prop_types$7.default.string,
		disabled: import_prop_types$7.default.bool
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/components/image-style-select.js
	init_extends();
	init_slicedToArray();
	init_objectWithoutProperties$1();
	var _excluded$19 = ["type"];
	var ImageStyleSelect = function ImageStyleSelect(_ref) {
		var _IMAGE_PROMPT_CATEGOR;
		var _ref$type = _ref.type;
		var type = _ref$type === void 0 ? "" : _ref$type;
		var props = _objectWithoutProperties$1(_ref, _excluded$19);
		var imageStyles = Object.entries(((_IMAGE_PROMPT_CATEGOR = IMAGE_PROMPT_CATEGORIES[type]) === null || _IMAGE_PROMPT_CATEGOR === void 0 ? void 0 : _IMAGE_PROMPT_CATEGOR.subCategories) || {}).map(function(_ref2) {
			var _ref3 = _slicedToArray(_ref2, 2);
			var value = _ref3[0];
			return {
				label: _ref3[1],
				value
			};
		});
		return /*#__PURE__*/ react.default.createElement(PromptActionSelection, _extends$1({
			options: imageStyles,
			wrapperStyle: { width: "100%" },
			label: (0, _wordpress_i18n.__)("Style", "elementor")
		}, props));
	};
	ImageStyleSelect.propTypes = { type: import_prop_types$7.default.string.isRequired };

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/components/image-ratio-select.js
	init_extends();
	init_slicedToArray();
	var imageRatios = Object.entries(IMAGE_ASPECT_RATIO).map(function(_ref) {
		var _ref2 = _slicedToArray(_ref, 2);
		var value = _ref2[0];
		return {
			label: _ref2[1].label,
			value
		};
	});
	var ImageRatioSelect = function ImageRatioSelect(props) {
		return /*#__PURE__*/ react.default.createElement(PromptActionSelection, _extends$1({
			options: imageRatios,
			wrapperStyle: { width: "100%" },
			label: (0, _wordpress_i18n.__)("Aspect ratio", "elementor")
		}, props));
	};

//#endregion
//#region modules/ai/assets/js/editor/icons/refresh-icon.js
	init_extends();
	var RefreshIcon = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 24 24" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M7.55012 4.45178C9.23098 3.48072 11.1845 3.08925 13.1097 3.33767C15.035 3.58609 16.8251 4.46061 18.2045 5.82653C19.5838 7.19245 20.4757 8.97399 20.743 10.8967C20.8 11.307 20.5136 11.6858 20.1033 11.7428C19.6931 11.7998 19.3142 11.5135 19.2572 11.1032C19.0353 9.50635 18.2945 8.02677 17.149 6.89236C16.0035 5.75795 14.5167 5.03165 12.9178 4.82534C11.3189 4.61902 9.69644 4.94414 8.30047 5.75061C7.24361 6.36117 6.36093 7.22198 5.72541 8.24995H8.00009C8.41431 8.24995 8.75009 8.58574 8.75009 8.99995C8.75009 9.41417 8.41431 9.74995 8.00009 9.74995H4.51686C4.5055 9.75021 4.49412 9.75021 4.48272 9.74995H4.00009C3.58588 9.74995 3.25009 9.41417 3.25009 8.99995V4.99995C3.25009 4.58574 3.58588 4.24995 4.00009 4.24995C4.41431 4.24995 4.75009 4.58574 4.75009 4.99995V7.00691C5.48358 5.96916 6.43655 5.0951 7.55012 4.45178Z"
		}), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M3.89686 12.2571C4.30713 12.2001 4.68594 12.4864 4.74295 12.8967C4.96487 14.4936 5.70565 15.9731 6.85119 17.1075C7.99673 18.242 9.48347 18.9683 11.0824 19.1746C12.6813 19.3809 14.3037 19.0558 15.6997 18.2493C16.7566 17.6387 17.6393 16.7779 18.2748 15.75H16.0001C15.5859 15.75 15.2501 15.4142 15.2501 15C15.2501 14.5857 15.5859 14.25 16.0001 14.25H19.4833C19.4947 14.2497 19.5061 14.2497 19.5175 14.25H20.0001C20.4143 14.25 20.7501 14.5857 20.7501 15V19C20.7501 19.4142 20.4143 19.75 20.0001 19.75C19.5859 19.75 19.2501 19.4142 19.2501 19V16.993C18.5166 18.0307 17.5636 18.9048 16.4501 19.5481C14.7692 20.5192 12.8157 20.9107 10.8904 20.6622C8.9652 20.4138 7.17504 19.5393 5.79572 18.1734C4.4164 16.8074 3.52443 15.0259 3.25723 13.1032C3.20022 12.6929 3.48658 12.3141 3.89686 12.2571Z"
		}));
	});

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/components/generate-again-submit.js
	init_extends();
	var GenerateAgainSubmit = function GenerateAgainSubmit(props) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, _extends$1({
			fullWidth: true,
			type: "submit",
			variant: "contained",
			color: "secondary",
			startIcon: /*#__PURE__*/ react.default.createElement(RefreshIcon, null),
			sx: { color: "background.paper" }
		}, props), (0, _wordpress_i18n.__)("Generate again", "elementor"));
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/components/new-prompt-button.js
	init_extends();
	var NewPromptButton = function NewPromptButton(props) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, _extends$1({
			fullWidth: true,
			variant: "text",
			color: "secondary"
		}, props), (0, _wordpress_i18n.__)("New prompt", "elementor"));
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/components/generate-images-submit.js
	init_extends();
	init_generate_button();
	init_voice_promotion_alert();
	var GenerateImagesSubmit = function GenerateImagesSubmit(props) {
		return /*#__PURE__*/ react.default.createElement(react.default.Fragment, null, /*#__PURE__*/ react.default.createElement(GenerateButton$1, _extends$1({
			size: "medium",
			fullWidth: true
		}, props), (0, _wordpress_i18n.__)("Generate images", "elementor")), /*#__PURE__*/ react.default.createElement(VoicePromotionAlert, { introductionKey: "ai-context-media-promotion" }));
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/hooks/use-image-size.js
	var useImageSize = function useImageSize(aspectRatio) {
		var _IMAGE_ASPECT_RATIO$a = IMAGE_ASPECT_RATIO[aspectRatio];
		return {
			width: _IMAGE_ASPECT_RATIO$a.width,
			height: _IMAGE_ASPECT_RATIO$a.height
		};
	};

//#endregion
//#region modules/ai/assets/js/editor/hooks/use-image-navigation.js
	init_slicedToArray();
	var initialIndex = -1;
	var useImageNavigation = function useImageNavigation(images) {
		var _useState2 = _slicedToArray((0, react.useState)(initialIndex), 2);
		var zoomIndex = _useState2[0];
		var setZoomIndex = _useState2[1];
		return {
			zoomIndex,
			setZoomIndex,
			actions: {
				reset: function reset() {
					return setZoomIndex(initialIndex);
				},
				prev: function prev() {
					var prevImage = zoomIndex + 1;
					if (prevImage >= images.length) prevImage = 0;
					setZoomIndex(prevImage);
				},
				next: function next() {
					var nextImage = zoomIndex - 1;
					if (nextImage < 0) nextImage = images.length - 1;
					setZoomIndex(nextImage);
				}
			}
		};
	};

//#endregion
//#region modules/ai/assets/js/editor/icons/chevron-right-icon.js
	init_extends();
	var ChevronRightIcon = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 24 24" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M8.46967 18.2803C8.17678 17.9874 8.17678 17.5126 8.46967 17.2197L13.9393 11.75L8.46967 6.28033C8.17678 5.98744 8.17678 5.51256 8.46967 5.21967C8.76256 4.92678 9.23744 4.92678 9.53033 5.21967L15.5303 11.2197C15.8232 11.5126 15.8232 11.9874 15.5303 12.2803L9.53033 18.2803C9.23744 18.5732 8.76256 18.5732 8.46967 18.2803Z"
		}));
	});

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/components/image-slider.js
	init_defineProperty$1();
	init_extends();
	init_objectWithoutProperties$1();
	var _excluded$18 = [
		"onPrev",
		"onNext",
		"children"
	];
	var _excluded2$2 = [
		"children",
		"startAction",
		"sx"
	];
	var _excluded3$1 = ["src", "alt"];
	function ownKeys$20(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	__name(ownKeys$20, "ownKeys");
	function _objectSpread$20(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$20(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$20(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	__name(_objectSpread$20, "_objectSpread");
	var StyledChevronLeftIcon = (0, _elementor_ui.withDirection)(ChevronLeftIcon);
	var StyledChevronRightIcon = (0, _elementor_ui.withDirection)(ChevronRightIcon);
	var ImageSlider = function ImageSlider(_ref) {
		var onPrev = _ref.onPrev;
		var onNext = _ref.onNext;
		var children = _ref.children;
		var props = _objectWithoutProperties$1(_ref, _excluded$18);
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, _extends$1({
			alignItems: "flex-start",
			spacing: .5
		}, props), /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			direction: "row",
			spacing: 2.5,
			alignSelf: "center",
			alignItems: "center"
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.IconButton, {
			onClick: onPrev,
			size: "large",
			color: "secondary"
		}, /*#__PURE__*/ react.default.createElement(StyledChevronLeftIcon, null)), /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			spacing: .5,
			justifyContent: "space-around",
			alignItems: "center"
		}, children), /*#__PURE__*/ react.default.createElement(_elementor_ui.IconButton, {
			onClick: onNext,
			size: "large",
			color: "secondary"
		}, /*#__PURE__*/ react.default.createElement(StyledChevronRightIcon, null))));
	};
	ImageSlider.propTypes = {
		onPrev: import_prop_types$7.default.func.isRequired,
		onNext: import_prop_types$7.default.func.isRequired,
		children: import_prop_types$7.default.node.isRequired
	};
	var Actions$1 = function Actions(_ref2) {
		var children = _ref2.children;
		var startAction = _ref2.startAction;
		var _ref2$sx = _ref2.sx;
		var sx = _ref2$sx === void 0 ? {} : _ref2$sx;
		var props = _objectWithoutProperties$1(_ref2, _excluded2$2);
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, _extends$1({
			display: "flex",
			justifyContent: "flex-end",
			alignItems: "center",
			width: "100%"
		}, props, { sx: _objectSpread$20({ mb: 1.5 }, sx) }), startAction, /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			direction: "row",
			spacing: 2,
			justifyContent: "flex-end",
			flexGrow: 1,
			width: "100%"
		}, children));
	};
	Actions$1.propTypes = {
		sx: import_prop_types$7.default.object,
		startAction: import_prop_types$7.default.node,
		children: import_prop_types$7.default.node.isRequired
	};
	var Image$2 = function Image(_ref3) {
		var src = _ref3.src;
		var _ref3$alt = _ref3.alt;
		var alt = _ref3$alt === void 0 ? "" : _ref3$alt;
		var props = _objectWithoutProperties$1(_ref3, _excluded3$1);
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, {
			display: "flex",
			justifyContent: "center",
			alignItems: "center"
		}, /*#__PURE__*/ react.default.createElement("img", _extends$1({
			src,
			alt
		}, props)));
	};
	Image$2.propTypes = {
		alt: import_prop_types$7.default.string,
		src: import_prop_types$7.default.string.isRequired
	};
	ImageSlider.Actions = Actions$1;
	ImageSlider.Image = Image$2;

//#endregion
//#region modules/ai/assets/js/editor/icons/copy-icon.js
	init_extends();
	var CopyIcon$1 = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 24 24" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M9 3.75C8.30964 3.75 7.75 4.30964 7.75 5V15C7.75 15.6904 8.30964 16.25 9 16.25H19C19.6904 16.25 20.25 15.6904 20.25 15V5C20.25 4.30964 19.6904 3.75 19 3.75H9ZM6.25 5C6.25 3.48122 7.48122 2.25 9 2.25H19C20.5188 2.25 21.75 3.48122 21.75 5V15C21.75 16.5188 20.5188 17.75 19 17.75H9C7.48122 17.75 6.25 16.5188 6.25 15V5Z"
		}), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M5 7.75C4.66848 7.75 4.35054 7.8817 4.11612 8.11612C3.8817 8.35054 3.75 8.66848 3.75 9V19C3.75 19.3315 3.8817 19.6495 4.11612 19.8839C4.35054 20.1183 4.66848 20.25 5 20.25H15C15.3315 20.25 15.6495 20.1183 15.8839 19.8839C16.1183 19.6495 16.25 19.3315 16.25 19V17C16.25 16.5858 16.5858 16.25 17 16.25C17.4142 16.25 17.75 16.5858 17.75 17V19C17.75 19.7293 17.4603 20.4288 16.9445 20.9445C16.4288 21.4603 15.7293 21.75 15 21.75H5C4.27065 21.75 3.57118 21.4603 3.05546 20.9445C2.53973 20.4288 2.25 19.7293 2.25 19V9C2.25 8.27065 2.53973 7.57118 3.05546 7.05546C3.57118 6.53973 4.27065 6.25 5 6.25H7C7.41421 6.25 7.75 6.58579 7.75 7C7.75 7.41421 7.41421 7.75 7 7.75H5Z"
		}));
	});

//#endregion
//#region modules/ai/assets/js/editor/icons/edit-icon.js
	init_extends();
	var EditIcon$1 = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 24 24" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M13.9697 4.96967C14.6408 4.29858 15.5509 3.92157 16.5 3.92157C17.4491 3.92157 18.3592 4.29858 19.0303 4.96967C19.7014 5.64075 20.0784 6.55094 20.0784 7.5C20.0784 8.44905 19.7014 9.35924 19.0303 10.0303L8.53033 20.5303C8.38968 20.671 8.19891 20.75 8 20.75H4C3.58579 20.75 3.25 20.4142 3.25 20V16C3.25 15.8011 3.32902 15.6103 3.46967 15.4697L13.9697 4.96967ZM16.5 5.42157C15.9488 5.42157 15.4201 5.64055 15.0303 6.03033L4.75 16.3107V19.25H7.68934L17.9697 8.96967C18.3595 8.57989 18.5784 8.05123 18.5784 7.5C18.5784 6.94876 18.3595 6.42011 17.9697 6.03033C17.5799 5.64055 17.0512 5.42157 16.5 5.42157Z"
		}), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M12.9697 5.96967C13.2626 5.67677 13.7374 5.67677 14.0303 5.96967L18.0303 9.96967C18.3232 10.2626 18.3232 10.7374 18.0303 11.0303C17.7374 11.3232 17.2626 11.3232 16.9697 11.0303L12.9697 7.03033C12.6768 6.73743 12.6768 6.26256 12.9697 5.96967Z"
		}));
	});

//#endregion
//#region modules/ai/assets/js/editor/icons/download-icon.js
	init_extends();
	var DownloadIcon = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 24 24" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M4 16.25C4.41421 16.25 4.75 16.5858 4.75 17V19C4.75 19.3315 4.8817 19.6495 5.11612 19.8839C5.35054 20.1183 5.66848 20.25 6 20.25H18C18.3315 20.25 18.6495 20.1183 18.8839 19.8839C19.1183 19.6495 19.25 19.3315 19.25 19V17C19.25 16.5858 19.5858 16.25 20 16.25C20.4142 16.25 20.75 16.5858 20.75 17V19C20.75 19.7293 20.4603 20.4288 19.9445 20.9445C19.4288 21.4603 18.7293 21.75 18 21.75H6C5.27065 21.75 4.57118 21.4603 4.05546 20.9445C3.53973 20.4288 3.25 19.7293 3.25 19V17C3.25 16.5858 3.58579 16.25 4 16.25Z"
		}), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M6.46967 10.4697C6.76256 10.1768 7.23744 10.1768 7.53033 10.4697L12 14.9393L16.4697 10.4697C16.7626 10.1768 17.2374 10.1768 17.5303 10.4697C17.8232 10.7626 17.8232 11.2374 17.5303 11.5303L12.5303 16.5303C12.2374 16.8232 11.7626 16.8232 11.4697 16.5303L6.46967 11.5303C6.17678 11.2374 6.17678 10.7626 6.46967 10.4697Z"
		}), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M12 3.25C12.4142 3.25 12.75 3.58579 12.75 4V16C12.75 16.4142 12.4142 16.75 12 16.75C11.5858 16.75 11.25 16.4142 11.25 16V4C11.25 3.58579 11.5858 3.25 12 3.25Z"
		}));
	});

//#endregion
//#region modules/ai/assets/js/editor/icons/zoom-in-icon.js
	init_extends();
	var ZoomInIcon = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 24 24" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M10 3.75C6.54822 3.75 3.75 6.54822 3.75 10C3.75 13.4518 6.54822 16.25 10 16.25C13.4518 16.25 16.25 13.4518 16.25 10C16.25 6.54822 13.4518 3.75 10 3.75ZM2.25 10C2.25 5.71979 5.71979 2.25 10 2.25C14.2802 2.25 17.75 5.71979 17.75 10C17.75 14.2802 14.2802 17.75 10 17.75C5.71979 17.75 2.25 14.2802 2.25 10Z"
		}), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M6.25 10C6.25 9.58579 6.58579 9.25 7 9.25H13C13.4142 9.25 13.75 9.58579 13.75 10C13.75 10.4142 13.4142 10.75 13 10.75H7C6.58579 10.75 6.25 10.4142 6.25 10Z"
		}), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M10 6.25C10.4142 6.25 10.75 6.58579 10.75 7V13C10.75 13.4142 10.4142 13.75 10 13.75C9.58579 13.75 9.25 13.4142 9.25 13V7C9.25 6.58579 9.58579 6.25 10 6.25Z"
		}), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M14.4697 14.4697C14.7626 14.1768 15.2374 14.1768 15.5303 14.4697L21.5303 20.4697C21.8232 20.7626 21.8232 21.2374 21.5303 21.5303C21.2374 21.8232 20.7626 21.8232 20.4697 21.5303L14.4697 15.5303C14.1768 15.2374 14.1768 14.7626 14.4697 14.4697Z"
		}));
	});

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/components/image-actions.js
	init_defineProperty$1();
	init_objectWithoutProperties$1();
	init_extends();
	var _excluded$17 = ["children"];
	var _excluded2$1 = ["sx"];
	var _excluded3 = ["sx"];
	var _excluded4 = ["sx"];
	var _excluded5 = ["sx"];
	function ownKeys$19(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	__name(ownKeys$19, "ownKeys");
	function _objectSpread$19(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$19(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$19(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	__name(_objectSpread$19, "_objectSpread");
	var StyledEditSvgIcon = (0, _elementor_ui.withDirection)(EditIcon$1);
	var StyledCopySvgIcon = (0, _elementor_ui.withDirection)(CopyIcon$1);
	var StyledZoomInIcon = (0, _elementor_ui.withDirection)(ZoomInIcon);
	var Tooltip$5 = function Tooltip(props) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Tooltip, _extends$1({ PopperProps: { sx: { "&.MuiTooltip-popper .MuiTooltip-tooltip.MuiTooltip-tooltipPlacementBottom": { mt: 1 } } } }, props));
	};
	var ImageActions = function ImageActions(_ref) {
		var children = _ref.children;
		var props = _objectWithoutProperties$1(_ref, _excluded$17);
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, _extends$1({
			display: "flex",
			justifyContent: "space-between",
			alignItems: "center",
			width: "100%",
			gap: 1
		}, props), children);
	};
	ImageActions.propTypes = { children: import_prop_types$7.default.node };
	var UseImage = function UseImage(_ref2) {
		var sx = _ref2.sx;
		var props = _objectWithoutProperties$1(_ref2, _excluded2$1);
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, _extends$1({
			size: "small",
			variant: "contained",
			startIcon: /*#__PURE__*/ react.default.createElement(DownloadIcon, null),
			sx
		}, props), (0, _wordpress_i18n.__)("Use Image", "elementor"));
	};
	UseImage.propTypes = { sx: import_prop_types$7.default.object };
	var EditImage = function EditImage(props) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, _extends$1({
			size: "small",
			color: "secondary",
			startIcon: /*#__PURE__*/ react.default.createElement(StyledEditSvgIcon, null)
		}, props), (0, _wordpress_i18n.__)("Edit", "elementor"));
	};
	var EditIcon = function EditIcon(_ref3) {
		var _ref3$sx = _ref3.sx;
		var sx = _ref3$sx === void 0 ? {} : _ref3$sx;
		var props = _objectWithoutProperties$1(_ref3, _excluded3);
		return /*#__PURE__*/ react.default.createElement(Tooltip$5, { title: (0, _wordpress_i18n.__)("Edit", "elementor") }, /*#__PURE__*/ react.default.createElement(_elementor_ui.IconButton, _extends$1({ sx: _objectSpread$19({
			mr: -1.5,
			ml: .5,
			color: "common.white",
			"&:hover": { color: "common.white" }
		}, sx) }, props), /*#__PURE__*/ react.default.createElement(StyledEditSvgIcon, null)));
	};
	EditIcon.propTypes = { sx: import_prop_types$7.default.object };
	var ZoomIcon = function ZoomIcon(_ref4) {
		var _ref4$sx = _ref4.sx;
		var sx = _ref4$sx === void 0 ? {} : _ref4$sx;
		var props = _objectWithoutProperties$1(_ref4, _excluded4);
		return /*#__PURE__*/ react.default.createElement(Tooltip$5, { title: (0, _wordpress_i18n.__)("Zoom", "elementor") }, /*#__PURE__*/ react.default.createElement(_elementor_ui.IconButton, _extends$1({
			color: "secondary",
			"aria-label": (0, _wordpress_i18n.__)("Zoom", "elementor"),
			sx: _objectSpread$19({
				color: "common.white",
				"&:hover": { color: "common.white" }
			}, sx)
		}, props), /*#__PURE__*/ react.default.createElement(StyledZoomInIcon, null)));
	};
	ZoomIcon.propTypes = { sx: import_prop_types$7.default.object };
	var CopyIcon = function CopyIcon(_ref5) {
		var _ref5$sx = _ref5.sx;
		var sx = _ref5$sx === void 0 ? {} : _ref5$sx;
		var props = _objectWithoutProperties$1(_ref5, _excluded5);
		return /*#__PURE__*/ react.default.createElement(Tooltip$5, { title: (0, _wordpress_i18n.__)("Copy prompt", "elementor") }, /*#__PURE__*/ react.default.createElement(_elementor_ui.IconButton, _extends$1({ sx: _objectSpread$19({
			mr: -1.5,
			ml: .5,
			color: "common.white",
			"&:hover": { color: "common.white" }
		}, sx) }, props), /*#__PURE__*/ react.default.createElement(StyledCopySvgIcon, null)));
	};
	CopyIcon.propTypes = { sx: import_prop_types$7.default.object };
	ImageActions.UseImage = UseImage;
	ImageActions.EditImage = EditImage;
	ImageActions.EditIcon = EditIcon;
	ImageActions.ZoomIcon = ZoomIcon;
	ImageActions.CopyIcon = CopyIcon;

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/components/single-image-preview.js
	init_defineProperty$1();
	init_extends();
	init_objectWithoutProperties$1();
	var _excluded$16 = ["children"];
	var _excluded2 = ["children", "sx"];
	function ownKeys$18(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	__name(ownKeys$18, "ownKeys");
	function _objectSpread$18(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$18(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$18(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	__name(_objectSpread$18, "_objectSpread");
	var SingleImagePreview = function SingleImagePreview(_ref) {
		var children = _ref.children;
		var props = _objectWithoutProperties$1(_ref, _excluded$16);
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, _extends$1({ display: "flex" }, props), children);
	};
	SingleImagePreview.propTypes = { children: import_prop_types$7.default.node.isRequired };
	var Actions = function Actions(_ref2) {
		var children = _ref2.children;
		var _ref2$sx = _ref2.sx;
		var sx = _ref2$sx === void 0 ? {} : _ref2$sx;
		var props = _objectWithoutProperties$1(_ref2, _excluded2);
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, _extends$1({
			display: "flex",
			width: "100%",
			justifyContent: "flex-end",
			alignItems: "center"
		}, props, { sx: _objectSpread$18({ mb: 1.5 }, sx) }), /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			direction: "row",
			spacing: 2,
			justifyContent: "flex-end",
			width: "100%"
		}, children));
	};
	Actions.propTypes = {
		sx: import_prop_types$7.default.object,
		children: import_prop_types$7.default.node.isRequired
	};
	var Image$1 = function Image(_ref3) {
		var src = _ref3.src;
		var alt = _ref3.alt;
		var _ref3$style = _ref3.style;
		var style = _ref3$style === void 0 ? {} : _ref3$style;
		var _ref3$isLoading = _ref3.isLoading;
		var isLoading = _ref3$isLoading === void 0 ? false : _ref3$isLoading;
		var children = _ref3.children;
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, { margin: "0 auto" }, children, isLoading ? /*#__PURE__*/ react.default.createElement(_elementor_ui.Skeleton, {
			sx: _objectSpread$18({
				maxWidth: "100%",
				width: "auto",
				maxHeight: "100%"
			}, style),
			animation: "wave",
			variant: "rounded"
		}) : /*#__PURE__*/ react.default.createElement("img", {
			src,
			alt,
			style: _objectSpread$18({
				maxWidth: "100%",
				width: "auto",
				maxHeight: "100%"
			}, style)
		}));
	};
	Image$1.propTypes = {
		style: import_prop_types$7.default.object,
		children: import_prop_types$7.default.node,
		src: import_prop_types$7.default.string.isRequired,
		alt: import_prop_types$7.default.string.isRequired,
		isLoading: import_prop_types$7.default.bool
	};
	SingleImagePreview.Actions = Actions;
	SingleImagePreview.Image = Image$1;

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/components/images-display.js
	init_extends();
	init_defineProperty$1();
	init_objectWithoutProperties$1();
	var _excluded$15 = ["children", "sx"];
	function ownKeys$17(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	__name(ownKeys$17, "ownKeys");
	function _objectSpread$17(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$17(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$17(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	__name(_objectSpread$17, "_objectSpread");
	var Container = function Container(_ref) {
		var children = _ref.children;
		var _ref$sx = _ref.sx;
		var sx = _ref$sx === void 0 ? {} : _ref$sx;
		var props = _objectWithoutProperties$1(_ref, _excluded$15);
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, _extends$1({ flexGrow: 1 }, props, { sx: _objectSpread$17({ overflowY: "auto" }, sx) }), children);
	};
	Container.propTypes = {
		sx: import_prop_types$7.default.object,
		children: import_prop_types$7.default.node
	};
	var ImagesDisplay = function ImagesDisplay(_ref2) {
		var images = _ref2.images;
		var _ref2$aspectRatio = _ref2.aspectRatio;
		var aspectRatio = _ref2$aspectRatio === void 0 ? "1:1" : _ref2$aspectRatio;
		var _ref2$onUseImage = _ref2.onUseImage;
		var onUseImage = _ref2$onUseImage === void 0 ? null : _ref2$onUseImage;
		var _ref2$onEditImage = _ref2.onEditImage;
		var onEditImage = _ref2$onEditImage === void 0 ? null : _ref2$onEditImage;
		var _ref2$transparentCont = _ref2.transparentContainer;
		var transparentContainer = _ref2$transparentCont === void 0 ? false : _ref2$transparentCont;
		var _ref2$cols = _ref2.cols;
		var cols = _ref2$cols === void 0 ? 2 : _ref2$cols;
		var _ref2$overlay = _ref2.overlay;
		var overlay = _ref2$overlay === void 0 ? true : _ref2$overlay;
		var _ref2$onSelectChange = _ref2.onSelectChange;
		var onSelectChange = _ref2$onSelectChange === void 0 ? null : _ref2$onSelectChange;
		var _useImageNavigation = useImageNavigation(images);
		var zoomIndex = _useImageNavigation.zoomIndex;
		var setZoomIndex = _useImageNavigation.setZoomIndex;
		var actions = _useImageNavigation.actions;
		var _useImageSize = useImageSize(aspectRatio);
		var width = _useImageSize.width;
		var height = _useImageSize.height;
		if (zoomIndex > -1) {
			var currentImage = images[zoomIndex];
			return /*#__PURE__*/ react.default.createElement(Container, null, /*#__PURE__*/ react.default.createElement(ImageSlider, {
				onPrev: actions.prev,
				onNext: actions.next
			}, /*#__PURE__*/ react.default.createElement(ImageSlider.Actions, { startAction: /*#__PURE__*/ react.default.createElement(BackButton$1, { onClick: actions.reset }) }, onEditImage && /*#__PURE__*/ react.default.createElement(ImageActions.EditImage, { onClick: function onClick() {
				return onEditImage(currentImage);
			} }), onUseImage && /*#__PURE__*/ react.default.createElement(ImageActions.UseImage, { onClick: function onClick() {
				return onUseImage(currentImage);
			} })), /*#__PURE__*/ react.default.createElement(ImageSlider.Image, {
				src: currentImage.image_url,
				style: {
					maxWidth: "630px",
					width: "100%",
					height: "auto"
				}
			})));
		}
		if (1 === images.length) {
			var image = images[0];
			var singleImageStyle = {
				width,
				height
			};
			if (transparentContainer) {
				singleImageStyle.backgroundImage = "linear-gradient(45deg, #bbb 25%, transparent 25%), linear-gradient(-45deg, #bbb 25%, transparent 25%), linear-gradient(45deg, transparent 75%, #bbb 75%), linear-gradient(-45deg, transparent 75%, #bbb 75%)";
				singleImageStyle.backgroundSize = "20px 20px";
				singleImageStyle.backgroundPosition = "0 0, 0 10px, 10px -10px, -10px 0px";
			}
			return /*#__PURE__*/ react.default.createElement(Container, { flexDirection: "column" }, /*#__PURE__*/ react.default.createElement(SingleImagePreview, null, /*#__PURE__*/ react.default.createElement(SingleImagePreview.Image, {
				src: image.image_url || image.url,
				style: singleImageStyle,
				alt: "generated-image",
				isLoading: image.isLoading
			}, /*#__PURE__*/ react.default.createElement(SingleImagePreview.Actions, null, onEditImage && /*#__PURE__*/ react.default.createElement(ImageActions.EditImage, { onClick: function onClick() {
				return onEditImage(image);
			} }), onUseImage && /*#__PURE__*/ react.default.createElement(ImageActions.UseImage, { onClick: function onClick() {
				return onUseImage(image);
			} })))));
		}
		return /*#__PURE__*/ react.default.createElement(Container, null, /*#__PURE__*/ react.default.createElement(Gallery, { cols }, images.map(function(image, index) {
			return /*#__PURE__*/ react.default.createElement(Gallery.Image, {
				key: "result-".concat(image.seed),
				alt: "generated-".concat(index),
				src: image.image_url,
				aspectRatio,
				"data-testid": "e-gallery-image",
				numImagesInRow: cols,
				overlay,
				onSelectChange: onSelectChange ? function(isChecked) {
					return onSelectChange(image.productId, isChecked);
				} : null,
				checkboxColor: image.checkboxColor,
				initialChecked: image.isChecked,
				isLoading: image.isLoading
			}, /*#__PURE__*/ react.default.createElement(ImageActions, null, onUseImage && /*#__PURE__*/ react.default.createElement(ImageActions.UseImage, {
				onClick: function onClick() {
					return onUseImage(image);
				},
				size: "medium",
				fullWidth: true
			}), /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
				direction: "row",
				spacing: .25,
				alignItems: "center"
			}, /*#__PURE__*/ react.default.createElement(ImageActions.ZoomIcon, {
				onClick: function onClick() {
					return setZoomIndex(index);
				},
				size: "medium"
			}), onEditImage && /*#__PURE__*/ react.default.createElement(ImageActions.EditIcon, {
				onClick: function onClick() {
					return onEditImage(image);
				},
				size: "medium"
			}))));
		})));
	};
	ImagesDisplay.propTypes = {
		images: import_prop_types$7.default.array,
		aspectRatio: import_prop_types$7.default.string,
		onUseImage: import_prop_types$7.default.func,
		onEditImage: import_prop_types$7.default.func,
		transparentContainer: import_prop_types$7.default.bool,
		cols: import_prop_types$7.default.number,
		overlay: import_prop_types$7.default.bool,
		onSelectChange: import_prop_types$7.default.func
	};
	ImagesDisplay.Container = Container;

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/hooks/use-prompt-settings.js
	init_defineProperty$1();
	init_slicedToArray();
	function ownKeys$16(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	__name(ownKeys$16, "ownKeys");
	function _objectSpread$16(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$16(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$16(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	__name(_objectSpread$16, "_objectSpread");
	var IMAGE_TYPE = IMAGE_PROMPT_SETTINGS.IMAGE_TYPE;
	var IMAGE_STYLE = IMAGE_PROMPT_SETTINGS.IMAGE_STYLE;
	var IMAGE_STRENGTH = IMAGE_PROMPT_SETTINGS.IMAGE_STRENGTH;
	var IMAGE_RATIO = IMAGE_PROMPT_SETTINGS.IMAGE_RATIO;
	var IMAGE_ZOOM = IMAGE_PROMPT_SETTINGS.IMAGE_ZOOM;
	var IMAGE_UPSCALE = IMAGE_PROMPT_SETTINGS.IMAGE_UPSCALE;
	var IMAGE_BACKGROUND_COLOR = IMAGE_PROMPT_SETTINGS.IMAGE_BACKGROUND_COLOR;
	var DEFAULT_TYPE = "";
	var DEFAULT_STYLE = "";
	var DEFAULT_STRENGTH = 0;
	var DEFAULT_RATIO = "1:1";
	var DEFAULT_ZOOM = "1";
	var DEFAULT_UPSCALE = "512";
	var DEFAULT_COLOR = "#FFFFFF";
	var usePromptSettings = function usePromptSettings() {
		var _ref = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
		var _ref$type = _ref.type;
		var type = _ref$type === void 0 ? DEFAULT_TYPE : _ref$type;
		var _ref$style = _ref.style;
		var style = _ref$style === void 0 ? DEFAULT_STYLE : _ref$style;
		var _ref$imageWeight = _ref.imageWeight;
		var imageWeight = _ref$imageWeight === void 0 ? DEFAULT_STRENGTH : _ref$imageWeight;
		var _ref$aspectRatio = _ref.aspectRatio;
		var aspectRatio = _ref$aspectRatio === void 0 ? DEFAULT_RATIO : _ref$aspectRatio;
		var _ref$zoom = _ref.zoom;
		var zoom = _ref$zoom === void 0 ? DEFAULT_ZOOM : _ref$zoom;
		var _ref$upScaleTo = _ref.upScaleTo;
		var upScaleTo = _ref$upScaleTo === void 0 ? DEFAULT_UPSCALE : _ref$upScaleTo;
		var _ref$bgColor = _ref.bgColor;
		var bgColor = _ref$bgColor === void 0 ? DEFAULT_COLOR : _ref$bgColor;
		var _useState2 = _slicedToArray((0, react.useState)(_defineProperty$1(_defineProperty$1(_defineProperty$1(_defineProperty$1(_defineProperty$1(_defineProperty$1(_defineProperty$1({}, IMAGE_TYPE, type), IMAGE_STYLE, style), IMAGE_STRENGTH, imageWeight), IMAGE_RATIO, aspectRatio), IMAGE_ZOOM, zoom), IMAGE_UPSCALE, upScaleTo), IMAGE_BACKGROUND_COLOR, bgColor)), 2);
		var settings = _useState2[0];
		var setSettings = _useState2[1];
		return {
			settings,
			updateSettings: function updateSettings(updated) {
				return setSettings(function(prev) {
					return _objectSpread$16(_objectSpread$16({}, prev), updated);
				});
			},
			resetSettings: function resetSettings() {
				setSettings(_defineProperty$1(_defineProperty$1(_defineProperty$1(_defineProperty$1(_defineProperty$1(_defineProperty$1(_defineProperty$1({}, IMAGE_TYPE, type), IMAGE_STYLE, DEFAULT_STYLE), IMAGE_STRENGTH, DEFAULT_STRENGTH), IMAGE_RATIO, aspectRatio), IMAGE_ZOOM, DEFAULT_ZOOM), IMAGE_UPSCALE, DEFAULT_UPSCALE), IMAGE_BACKGROUND_COLOR, bgColor));
			}
		};
	};

//#endregion
//#region modules/ai/assets/js/editor/hooks/use-images-preload.js
	init_slicedToArray();
	var loadImage$1 = function loadImage(url) {
		return new Promise(function(resolve, reject) {
			var loadImg = new Image();
			loadImg.src = url;
			loadImg.onload = function() {
				return resolve(url);
			};
			loadImg.onerror = function(err) {
				return reject(err);
			};
		});
	};
	var useImagesPreload = function useImagesPreload() {
		var _useState2 = _slicedToArray((0, react.useState)(false), 2);
		var ready = _useState2[0];
		var setReady = _useState2[1];
		return {
			ready,
			preloadImages: function preloadImages(images) {
				setReady(false);
				Promise.all(images.map(function(url) {
					return loadImage$1(url);
				})).then(function() {
					return setReady(true);
				}).catch(function(err) {
					return console.log("Failed to load images", err);
				});
			}
		};
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/generate/hooks/use-suggested-images.js
	init_slicedToArray();
	var shuffleImages = function shuffleImages(images) {
		return images.map(function(image) {
			return [Math.random(), image];
		}).sort(function(_ref, _ref2) {
			return _slicedToArray(_ref, 1)[0] - _slicedToArray(_ref2, 1)[0];
		}).map(function(_ref5) {
			return _slicedToArray(_ref5, 2)[1];
		});
	};
	var checkImageTypeIgnoreStyle = function checkImageTypeIgnoreStyle(imageType, selectedType) {
		return _slicedToArray(imageType.split("/"), 1)[0] === selectedType;
	};
	var useSuggestedImages = function useSuggestedImages(_ref7) {
		var selectedType = _ref7.selectedType;
		var _useState2 = _slicedToArray((0, react.useState)(false), 2);
		var isLoading = _useState2[0];
		var setIsLoading = _useState2[1];
		var _useState4 = _slicedToArray((0, react.useState)({ images: [] }), 2);
		var data = _useState4[0];
		var setImagesState = _useState4[1];
		var _useImagesPreload = useImagesPreload();
		var ready = _useImagesPreload.ready;
		var preloadImages = _useImagesPreload.preloadImages;
		var imagesData = (0, react.useMemo)(function() {
			var shuffledImages = shuffleImages(data.images);
			if (!selectedType) return shuffledImages;
			var categoryImages = shuffledImages.filter(function(_ref8) {
				var imageType = _ref8.imageType;
				return checkImageTypeIgnoreStyle(imageType, selectedType);
			});
			return categoryImages.length ? categoryImages : shuffledImages;
		}, [selectedType, data]);
		var fetchImages = function fetchImages() {
			setIsLoading(true);
			fetch("https://my.elementor.com/ai/images-prompt-gallery/ai-gallery.json").then(function(response) {
				return response.json();
			}).then(function(json) {
				return setImagesState(json);
			}).catch(function(e) {
				return console.log(e.message);
			}).finally(function() {
				return setIsLoading(false);
			});
		};
		(0, react.useEffect)(function() {
			if (0 === (data === null || data === void 0 ? void 0 : data.images.length)) {
				fetchImages();
				return;
			}
			preloadImages(data.images.map(function(_ref9) {
				return _ref9.thumbnailUrl;
			}));
		}, [data]);
		return {
			imagesData,
			isLoading: !ready || isLoading
		};
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/utils/index.js
	init_asyncToGenerator();
	init_defineProperty$1();
	init_slicedToArray();
	function ownKeys$15(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	__name(ownKeys$15, "ownKeys");
	function _objectSpread$15(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$15(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$15(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	__name(_objectSpread$15, "_objectSpread");
	var getAspectRatioSizes = function getAspectRatioSizes(width, height) {
		var aspectRatios = Object.keys(IMAGE_ASPECT_RATIO);
		var targetRatio = width / height;
		var closestRatio = aspectRatios[0];
		var minDiff = Infinity;
		aspectRatios.forEach(function(ratio) {
			var _ratio$split$map2 = _slicedToArray(ratio.split(":").map(Number), 2);
			var w = _ratio$split$map2[0];
			var h = _ratio$split$map2[1];
			var diff = Math.abs(targetRatio - w / h);
			if (diff < minDiff) {
				minDiff = diff;
				closestRatio = ratio;
			}
		});
		return _objectSpread$15({ ratio: closestRatio }, IMAGE_ASPECT_RATIO[closestRatio]);
	};
	var fetchImageAsBase64 = /*#__PURE__*/ function() {
		var _ref = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee(url) {
			var response;
			var blob;
			var reader;
			var _t;
			return import_regenerator$2.default.wrap(function(_context) {
				while (1) switch (_context.prev = _context.next) {
					case 0:
						_context.prev = 0;
						_context.next = 1;
						return fetch(url);
					case 1:
						response = _context.sent;
						_context.next = 2;
						return response.blob();
					case 2:
						blob = _context.sent;
						reader = new FileReader();
						return _context.abrupt("return", new Promise(function(resolve, reject) {
							reader.onloadend = function() {
								resolve(reader.result);
							};
							reader.onerror = reject;
							reader.readAsDataURL(blob);
						}));
					case 3:
						_context.prev = 3;
						_t = _context["catch"](0);
						throw _t;
					case 4:
					case "end": return _context.stop();
				}
			}, _callee, null, [[0, 3]]);
		}));
		return function fetchImageAsBase64(_x) {
			return _ref.apply(this, arguments);
		};
	}();

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/context/edit-image-context.js
	init_slicedToArray();
	init_defineProperty$1();
	function ownKeys$14(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	__name(ownKeys$14, "ownKeys");
	function _objectSpread$14(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$14(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$14(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	__name(_objectSpread$14, "_objectSpread");
	var EditImageContext = (0, react.createContext)({});
	var SET_STATE = "SET_STATE";
	var SET_ASPECT_RATIO = "SET_ASPECT_RATIO";
	var SET_LOADING = "SET_LOADING";
	var RESET = "RESET";
	var initialValue = {
		id: "",
		url: "",
		isLoading: false,
		aspectRatio: "1:1"
	};
	var reducer$1 = function reducer(state, _ref) {
		var type = _ref.type;
		var payload = _ref.payload;
		switch (type) {
			case SET_STATE: return _objectSpread$14(_objectSpread$14({}, state), payload);
			case SET_ASPECT_RATIO: return _objectSpread$14(_objectSpread$14({}, state), {}, { aspectRatio: payload });
			case SET_LOADING: return _objectSpread$14(_objectSpread$14({}, state), {}, { isLoading: payload });
			case RESET: return initialValue;
			default: throw Error("Unknown action.");
		}
	};
	var EditImageProvider = function EditImageProvider(_ref2) {
		var children = _ref2.children;
		var imageData = _ref2.imageData;
		var _useReducer2 = _slicedToArray((0, react.useReducer)(reducer$1, _objectSpread$14(_objectSpread$14({}, initialValue), imageData)), 2);
		var state = _useReducer2[0];
		var dispatch = _useReducer2[1];
		return /*#__PURE__*/ react.default.createElement(EditImageContext.Provider, { value: {
			state,
			dispatch
		} }, children);
	};
	EditImageProvider.propTypes = {
		children: import_prop_types$7.default.node,
		imageData: import_prop_types$7.default.object
	};
	var useEditImage = function useEditImage() {
		var _useContext = (0, react.useContext)(EditImageContext);
		var state = _useContext.state;
		var dispatch = _useContext.dispatch;
		var id = state.id;
		var url = state.url;
		var aspectRatio = state.aspectRatio;
		var isLoading = state.isLoading;
		var _useImageSize = useImageSize(aspectRatio);
		var width = _useImageSize.width;
		var height = _useImageSize.height;
		var setEditImage = function setEditImage(payload) {
			return dispatch({
				type: SET_STATE,
				payload
			});
		};
		var setAspectRatio = function setAspectRatio(payload) {
			return dispatch({
				type: SET_ASPECT_RATIO,
				payload
			});
		};
		var reset = function reset() {
			return dispatch({ type: RESET });
		};
		(0, react.useEffect)(function() {
			if (!id) return;
			dispatch({
				type: SET_LOADING,
				payload: true
			});
			var img = new Image();
			img.src = url;
			img.onload = function() {
				var ratio = getAspectRatioSizes(img.width, img.height).ratio;
				dispatch({
					type: SET_STATE,
					payload: {
						aspectRatio: ratio,
						isLoading: false
					}
				});
			};
		}, [id]);
		return {
			editImage: state,
			isLoading,
			aspectRatio,
			width,
			height,
			setEditImage,
			setAspectRatio,
			reset
		};
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/hooks/use-image-upload.js
	init_slicedToArray();
	init_api();
	var useImageUpload = function useImageUpload() {
		var _useState2 = _slicedToArray((0, react.useState)(false), 2);
		var isUploading = _useState2[0];
		var setIsUploading = _useState2[1];
		var _useState4 = _slicedToArray((0, react.useState)(""), 2);
		var uploadError = _useState4[0];
		var setUploadError = _useState4[1];
		var _useState6 = _slicedToArray((0, react.useState)({}), 2);
		var attachmentData = _useState6[0];
		var setAttachmentData = _useState6[1];
		return {
			upload: function upload(_ref) {
				var image = _ref.image;
				var prompt = _ref.prompt;
				return new Promise(function(resolve, reject) {
					setUploadError("");
					setAttachmentData({});
					setIsUploading(true);
					uploadImage({
						image,
						prompt
					}).then(function(result) {
						setAttachmentData(result);
						resolve(result);
					}).catch(function(err) {
						var error = (err === null || err === void 0 ? void 0 : err.responseText) || err;
						setUploadError(error);
						reject(error);
					}).finally(function() {
						return setIsUploading(false);
					});
				});
			},
			resetUpload: function resetUpload() {
				setUploadError("");
				setAttachmentData({});
				setIsUploading(false);
			},
			isUploading,
			uploadError,
			attachmentData
		};
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/context/global-actions-context.js
	var GlobalActionsContext = (0, react.createContext)({});
	var GlobalActionsProvider = function GlobalActionsProvider(_ref) {
		var actions = _ref.actions;
		var children = _ref.children;
		return /*#__PURE__*/ react.default.createElement(GlobalActionsContext.Provider, { value: actions }, children);
	};
	GlobalActionsProvider.propTypes = {
		actions: import_prop_types$7.default.object,
		children: import_prop_types$7.default.node
	};
	var useGlobalActions = function useGlobalActions() {
		return (0, react.useContext)(GlobalActionsContext);
	};

//#endregion
//#region node_modules/@babel/runtime/helpers/esm/classCallCheck.js
	function _classCallCheck(a, n) {
		if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function");
	}

//#endregion
//#region node_modules/@babel/runtime/helpers/esm/createClass.js
	init_toPropertyKey$1();
	function _defineProperties(e, r) {
		for (var t = 0; t < r.length; t++) {
			var o = r[t];
			o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, toPropertyKey$1(o.key), o);
		}
	}
	function _createClass(e, r, t) {
		return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e;
	}

//#endregion
//#region assets/dev/js/editor/utils/files-upload-handler.js
	var FilesUploadHandler = /*#__PURE__*/ function() {
		function FilesUploadHandler() {
			_classCallCheck(this, FilesUploadHandler);
		}
		return _createClass(FilesUploadHandler, null, [
			{
				key: "isUploadEnabled",
				value: function isUploadEnabled(mediaType) {
					if (!["svg", "application/json"].includes(mediaType)) return true;
					return elementorCommon.config.filesUpload.unfilteredFiles;
				}
			},
			{
				key: "setUploadTypeCaller",
				value: function setUploadTypeCaller(frame) {
					frame.uploader.uploader.param("uploadTypeCaller", "elementor-wp-media-upload");
				}
			},
			{
				key: "getUnfilteredFilesNonAdminDialog",
				value: function getUnfilteredFilesNonAdminDialog() {
					return elementorCommon.dialogsManager.createWidget("alert", {
						id: "e-unfiltered-files-disabled-dialog",
						headerMessage: (0, _wordpress_i18n.__)("Sorry, you can't upload that file yet", "elementor"),
						message: (0, _wordpress_i18n.__)("This is because JSON files may pose a security risk.", "elementor") + "<br><br>" + (0, _wordpress_i18n.__)("To upload them anyway, ask the site administrator to enable unfiltered file uploads.", "elementor"),
						strings: { confirm: (0, _wordpress_i18n.__)("Got it", "elementor") }
					});
				}
			},
			{
				key: "getUnfilteredFilesNotEnabledDialog",
				value: function getUnfilteredFilesNotEnabledDialog(callback) {
					var elementorInstance = window.elementorAdmin || window.elementor;
					if (!elementorInstance.config.user.is_administrator) return this.getUnfilteredFilesNonAdminDialog();
					return elementorInstance.helpers.getSimpleDialog("e-enable-unfiltered-files-dialog", (0, _wordpress_i18n.__)("Enable Unfiltered File Uploads", "elementor"), (0, _wordpress_i18n.__)("Before you enable unfiltered files upload, note that such files include a security risk. Elementor does run a process to remove possible malicious code, but there is still risk involved when using such files.", "elementor"), (0, _wordpress_i18n.__)("Enable", "elementor"), function onConfirm() {
						elementorCommon.ajax.addRequest("enable_unfiltered_files_upload", {}, true);
						elementorCommon.config.filesUpload.unfilteredFiles = true;
						callback();
					});
				}
			},
			{
				key: "getUnfilteredFilesNotEnabledImportTemplateDialog",
				value: function getUnfilteredFilesNotEnabledImportTemplateDialog(callback) {
					if (!(window.elementorAdmin || window.elementor).config.user.is_administrator) return this.getUnfilteredFilesNonAdminDialog();
					return elementorCommon.dialogsManager.createWidget("confirm", {
						id: "e-enable-unfiltered-files-dialog-import-template",
						headerMessage: (0, _wordpress_i18n.__)("Enable Unfiltered File Uploads", "elementor"),
						message: (0, _wordpress_i18n.__)("Before you enable unfiltered files upload, note that such files include a security risk. Elementor does run a process to remove possible malicious code, but there is still risk involved when using such files.", "elementor") + "<br /><br />" + (0, _wordpress_i18n.__)("If you do not enable uploading unfiltered files, any SVG or JSON (including lottie) files used in the uploaded template will not be imported.", "elementor"),
						position: {
							my: "center center",
							at: "center center"
						},
						strings: {
							confirm: (0, _wordpress_i18n.__)("Enable and Import", "elementor"),
							cancel: (0, _wordpress_i18n.__)("Import Without Enabling", "elementor")
						},
						onConfirm: function onConfirm() {
							elementorCommon.ajax.addRequest("enable_unfiltered_files_upload", { success: function success() {
								elementorCommon.config.filesUpload.unfilteredFiles = true;
								callback();
							} }, true);
						},
						onCancel: function onCancel() {
							return callback();
						}
					});
				}
			}
		]);
	}();

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/hooks/use-image-actions.js
	init_objectWithoutProperties$1();
	init_asyncToGenerator();
	init_defineProperty$1();
	var _excluded$14 = ["image"];
	function ownKeys$13(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	__name(ownKeys$13, "ownKeys");
	function _objectSpread$13(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$13(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$13(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	__name(_objectSpread$13, "_objectSpread");
	var normalizeImageData = function normalizeImageData(imageToUpload) {
		if (!(imageToUpload !== null && imageToUpload !== void 0 && imageToUpload.imageUrl)) return imageToUpload;
		return _objectSpread$13(_objectSpread$13({}, imageToUpload), {}, {
			image_url: imageToUpload.imageUrl,
			use_gallery_image: true
		});
	};
	var useImageActions = function useImageActions() {
		var _useEditImage = useEditImage();
		var editImage = _useEditImage.editImage;
		var setEditImage = _useEditImage.setEditImage;
		var _useGlobalActions = useGlobalActions();
		var setControlValue = _useGlobalActions.setControlValue;
		var saveAndClose = _useGlobalActions.saveAndClose;
		var _useImageUpload = useImageUpload();
		var attachmentData = _useImageUpload.attachmentData;
		var isUploading = _useImageUpload.isUploading;
		var uploadError = _useImageUpload.uploadError;
		var uploadImage = _useImageUpload.upload;
		var resetUpload = _useImageUpload.resetUpload;
		var ensureSVGUploading = function ensureSVGUploading(imageUrl) {
			if (!imageUrl) return true;
			var imageExtension = new URL(imageUrl).pathname.split(".").pop();
			var isUploadAllowed = window._wpPluploadSettings.defaults.filters.mime_types[0].extensions.split(",").includes(imageExtension) || elementorCommon.config.filesUpload.unfilteredFiles;
			if (!isUploadAllowed) {
				var dialog = FilesUploadHandler.getUnfilteredFilesNotEnabledDialog(function() {});
				dialog.getElements("widget").css("z-index", "170001");
				dialog.show();
			}
			return isUploadAllowed;
		};
		var upload = function upload(imageToUpload, prompt) {
			if (!ensureSVGUploading(imageToUpload.image_url)) return Promise.reject(/* @__PURE__ */ new Error("SVG Uploading is not allowed"));
			return uploadImage({
				image: normalizeImageData(imageToUpload),
				prompt: prompt || imageToUpload.prompt
			});
		};
		var getFinalImage = /*#__PURE__*/ function() {
			var _ref = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee(imageToUpload, prompt) {
				var isImageAlreadyUploaded;
				var _result;
				var result;
				return import_regenerator$2.default.wrap(function(_context) {
					while (1) switch (_context.prev = _context.next) {
						case 0:
							isImageAlreadyUploaded = editImage.url === imageToUpload.url;
							if (!isImageAlreadyUploaded) {
								_context.next = 1;
								break;
							}
							editImage.image, _result = _objectWithoutProperties$1(editImage, _excluded$14);
							return _context.abrupt("return", _result);
						case 1:
							_context.next = 2;
							return upload(imageToUpload, prompt);
						case 2:
							result = _context.sent;
							return _context.abrupt("return", result === null || result === void 0 ? void 0 : result.image);
						case 3:
						case "end": return _context.stop();
					}
				}, _callee);
			}));
			return function getFinalImage(_x, _x2) {
				return _ref.apply(this, arguments);
			};
		}();
		return {
			use: /* @__PURE__ */ function() {
				var _ref3 = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee3(imageToUpload, prompt) {
					var result;
					return import_regenerator$2.default.wrap(function(_context3) {
						while (1) switch (_context3.prev = _context3.next) {
							case 0:
								_context3.next = 1;
								return getFinalImage(imageToUpload, prompt);
							case 1:
								result = _context3.sent;
								setControlValue(result);
								saveAndClose();
							case 2:
							case "end": return _context3.stop();
						}
					}, _callee3);
				}));
				return function use(_x5, _x6) {
					return _ref3.apply(this, arguments);
				};
			}(),
			edit: /* @__PURE__ */ function() {
				var _ref2 = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee2(imageToUpload, prompt) {
					var result;
					return import_regenerator$2.default.wrap(function(_context2) {
						while (1) switch (_context2.prev = _context2.next) {
							case 0:
								_context2.next = 1;
								return upload(imageToUpload, prompt);
							case 1:
								result = _context2.sent;
								setEditImage(result.image);
							case 2:
							case "end": return _context2.stop();
						}
					}, _callee2);
				}));
				return function edit(_x3, _x4) {
					return _ref2.apply(this, arguments);
				};
			}(),
			reset: resetUpload,
			error: uploadError,
			data: attachmentData,
			isLoading: isUploading,
			useMultipleImages: /* @__PURE__ */ function() {
				var _ref4 = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee6(imagesToUpload) {
					var results;
					return import_regenerator$2.default.wrap(function(_context6) {
						while (1) switch (_context6.prev = _context6.next) {
							case 0:
								_context6.next = 1;
								return Promise.all(imagesToUpload.map(/*#__PURE__*/ function() {
									var _ref5 = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee4(img) {
										return import_regenerator$2.default.wrap(function(_context4) {
											while (1) switch (_context4.prev = _context4.next) {
												case 0:
													_context4.next = 1;
													return getFinalImage(img, null).catch(function(_) {
														return null;
													});
												case 1: return _context4.abrupt("return", _context4.sent);
												case 2:
												case "end": return _context4.stop();
											}
										}, _callee4);
									}));
									return function(_x8) {
										return _ref5.apply(this, arguments);
									};
								}()));
							case 1:
								results = _context6.sent;
								_context6.next = 2;
								return Promise.all(results.filter(function(result) {
									return result;
								}).map(/*#__PURE__*/ function() {
									var _ref6 = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee5(result) {
										return import_regenerator$2.default.wrap(function(_context5) {
											while (1) switch (_context5.prev = _context5.next) {
												case 0:
													_context5.next = 1;
													return setControlValue(result);
												case 1: return _context5.abrupt("return", _context5.sent);
												case 2:
												case "end": return _context5.stop();
											}
										}, _callee5);
									}));
									return function(_x9) {
										return _ref6.apply(this, arguments);
									};
								}()));
							case 2:
							case "end": return _context6.stop();
						}
					}, _callee6);
				}));
				return function useMultipleImages(_x7) {
					return _ref4.apply(this, arguments);
				};
			}()
		};
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/variations/components/images-placeholder.js
	var ImagesPlaceholder = function ImagesPlaceholder() {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, {
			sx: { overflowY: "scroll" },
			flexGrow: 1
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, {
			display: "grid",
			gridTemplateColumns: "repeat(2, 1fr)",
			gap: 3
		}, Array(4).fill(true).map(function(_, index) {
			return /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, {
				key: "placeholder-".concat(index),
				display: "flex",
				justifyContent: "center",
				alignItems: "center",
				sx: {
					bgcolor: "action.selected",
					height: 336
				}
			}, /*#__PURE__*/ react.default.createElement(_elementor_icons.AIIcon, {
				color: "secondary",
				sx: { fontSize: 36 }
			}));
		})));
	};
	ImagesPlaceholder.propTypes = {};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/generate/index.js
	init_defineProperty$1();
	init_slicedToArray();
	init_prompt_history_action_context();
	init_prompt_library_link();
	init_requests_ids();
	function ownKeys$12(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	__name(ownKeys$12, "ownKeys");
	function _objectSpread$12(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$12(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$12(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	__name(_objectSpread$12, "_objectSpread");
	var getPromptPlaceholder = function getPromptPlaceholder(images) {
		if (!(images !== null && images !== void 0 && images.length)) return (0, _wordpress_i18n.__)("describe your image", "elementor");
		return images[Math.floor(Math.random() * images.length)].prompt;
	};
	var Generate = function Generate(_ref) {
		var textToImageHook = _ref.textToImageHook;
		var _ref$predefinedPrompt = _ref.predefinedPrompt;
		var predefinedPrompt = _ref$predefinedPrompt === void 0 ? "" : _ref$predefinedPrompt;
		var _ref$initialSettings = _ref.initialSettings;
		var initialSettings = _ref$initialSettings === void 0 ? {} : _ref$initialSettings;
		var _useState2 = _slicedToArray((0, react.useState)(predefinedPrompt), 2);
		var prompt = _useState2[0];
		var setPrompt = _useState2[1];
		var setGenerate = useRequestIds().setGenerate;
		var initialImageType = useGlobalSettings().initialImageType;
		var _usePromptSettings = usePromptSettings(_objectSpread$12({ type: initialImageType }, initialSettings));
		var settings = _usePromptSettings.settings;
		var updateSettings = _usePromptSettings.updateSettings;
		var resetSettings = _usePromptSettings.resetSettings;
		var _useSuggestedImages = useSuggestedImages({ selectedType: settings[IMAGE_TYPE] });
		var suggestedImages = _useSuggestedImages.imagesData;
		var isPreloading = _useSuggestedImages.isLoading;
		var promptPlaceholder = getPromptPlaceholder(suggestedImages);
		var _textToImageHook = textToImageHook({});
		var generatedImages = _textToImageHook.data;
		var setResult = _textToImageHook.setResult;
		var send = _textToImageHook.send;
		var isGenerating = _textToImageHook.isLoading;
		var error = _textToImageHook.error;
		var reset = _textToImageHook.reset;
		var _useImageActions = useImageActions();
		var use = _useImageActions.use;
		var edit = _useImageActions.edit;
		var isUploading = _useImageActions.isLoading;
		var isLoading = isPreloading || isGenerating || isUploading;
		var generateAspectRatio = (0, react.useMemo)(function() {
			return settings[IMAGE_RATIO];
		}, [generatedImages === null || generatedImages === void 0 ? void 0 : generatedImages.result]);
		var handleSubmit = function handleSubmit(event) {
			event.preventDefault();
			setGenerate();
			if (!settings[IMAGE_STYLE]) settings[IMAGE_STYLE] = "general";
			send({
				prompt,
				settings
			});
		};
		var handleCopyPrompt = function handleCopyPrompt(_ref2) {
			var selectedPrompt = _ref2.prompt;
			var imageType = _ref2.imageType;
			setPrompt(selectedPrompt);
			var _imageType$split2 = _slicedToArray(imageType.split("/"), 2);
			var type = _imageType$split2[0];
			var style = _imageType$split2[1];
			updateSettings(_defineProperty$1(_defineProperty$1({}, IMAGE_TYPE, type), IMAGE_STYLE, style));
		};
		useSubscribeOnPromptHistoryAction([{
			type: ACTION_TYPES.RESTORE,
			handler: function handler(action) {
				var _action$data;
				var _action$data2;
				var _action$data3;
				var _action$data4;
				handleCopyPrompt({
					prompt: (_action$data = action.data) === null || _action$data === void 0 ? void 0 : _action$data.prompt,
					imageType: ((_action$data2 = action.data) === null || _action$data2 === void 0 ? void 0 : _action$data2.imageType) || ""
				});
				updateSettings(_defineProperty$1({}, IMAGE_RATIO, (_action$data3 = action.data) === null || _action$data3 === void 0 ? void 0 : _action$data3.ratio));
				setResult((_action$data4 = action.data) === null || _action$data4 === void 0 ? void 0 : _action$data4.images, action.id);
			}
		}]);
		return /*#__PURE__*/ react.default.createElement(View, null, /*#__PURE__*/ react.default.createElement(View.Panel, null, /*#__PURE__*/ react.default.createElement(View.PanelHeading, {
			primary: (0, _wordpress_i18n.__)("Imagine anything create everything", "elementor"),
			secondary: (0, _wordpress_i18n.__)("Generate images by selecting the desired type and style, and entering a prompt.", "elementor")
		}), error && /*#__PURE__*/ react.default.createElement(View.ErrorMessage, {
			error,
			onRetry: handleSubmit
		}), /*#__PURE__*/ react.default.createElement(ImageForm, { onSubmit: handleSubmit }, /*#__PURE__*/ react.default.createElement(PromptField, {
			"data-testid": "e-image-prompt",
			value: prompt,
			disabled: isLoading,
			placeholder: promptPlaceholder,
			onChange: setPrompt
		}), /*#__PURE__*/ react.default.createElement(ImageTypeSelect, {
			disabled: isLoading,
			value: settings[IMAGE_TYPE],
			onChange: function onChange(event) {
				return updateSettings(_defineProperty$1({}, IMAGE_TYPE, event.target.value));
			}
		}), /*#__PURE__*/ react.default.createElement(ImageStyleSelect, {
			type: settings[IMAGE_TYPE],
			value: settings[IMAGE_STYLE],
			disabled: isLoading || !settings[IMAGE_TYPE] || false,
			onChange: function onChange(event) {
				return updateSettings(_defineProperty$1({}, IMAGE_STYLE, event.target.value));
			}
		}), /*#__PURE__*/ react.default.createElement(ImageRatioSelect, {
			disabled: isLoading,
			value: settings[IMAGE_RATIO],
			onChange: function onChange(event) {
				return updateSettings(_defineProperty$1({}, IMAGE_RATIO, event.target.value));
			}
		}), generatedImages !== null && generatedImages !== void 0 && generatedImages.result ? /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			gap: 2,
			sx: { my: 2.5 }
		}, /*#__PURE__*/ react.default.createElement(GenerateAgainSubmit, { disabled: isLoading || "" === prompt }), /*#__PURE__*/ react.default.createElement(NewPromptButton, {
			disabled: isLoading,
			onClick: function onClick() {
				resetSettings();
				setPrompt("");
				reset();
			}
		})) : /*#__PURE__*/ react.default.createElement(GenerateImagesSubmit, { disabled: isLoading || "" === prompt }))), /*#__PURE__*/ react.default.createElement(View.Content, {
			isLoading: isPreloading || isUploading,
			isGenerating
		}, generatedImages !== null && generatedImages !== void 0 && generatedImages.result ? /*#__PURE__*/ react.default.createElement(ImagesDisplay, {
			images: generatedImages === null || generatedImages === void 0 ? void 0 : generatedImages.result,
			aspectRatio: generateAspectRatio,
			onUseImage: use,
			onEditImage: edit
		}) : /*#__PURE__*/ react.default.createElement(react.default.Fragment, null, predefinedPrompt ? /*#__PURE__*/ react.default.createElement(ImagesPlaceholder, null) : /*#__PURE__*/ react.default.createElement(react.default.Fragment, null, /*#__PURE__*/ react.default.createElement(View.ContentHeading, { primary: (0, _wordpress_i18n.__)("Spark your imagination with images generated by our community", "elementor") }), /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, { sx: { mb: 3 } }, /*#__PURE__*/ react.default.createElement(PromptLibraryLink, { libraryLink: "https://go.elementor.com/ai-prompt-library-image/" })), /*#__PURE__*/ react.default.createElement(Gallery, { cols: 3 }, suggestedImages === null || suggestedImages === void 0 ? void 0 : suggestedImages.map(function(suggestedPrompt) {
			return /*#__PURE__*/ react.default.createElement(Gallery.Image, {
				variant: "thumbnail",
				key: suggestedPrompt.prompt,
				alt: suggestedPrompt.prompt,
				text: suggestedPrompt.prompt,
				src: suggestedPrompt.thumbnailUrl
			}, /*#__PURE__*/ react.default.createElement(ImageActions, null, /*#__PURE__*/ react.default.createElement(ImageActions.UseImage, {
				onClick: function onClick() {
					return use(suggestedPrompt);
				},
				fullWidth: true
			}), /*#__PURE__*/ react.default.createElement(ImageActions.CopyIcon, { onClick: function onClick() {
				return handleCopyPrompt(suggestedPrompt);
			} }), /*#__PURE__*/ react.default.createElement(ImageActions.EditIcon, { onClick: function onClick() {
				return edit(suggestedPrompt);
			} })));
		}))))));
	};
	Generate.propTypes = {
		predefinedPrompt: import_prop_types$7.default.string,
		textToImageHook: import_prop_types$7.default.func,
		initialSettings: import_prop_types$7.default.object
	};

//#endregion
//#region modules/ai/assets/js/editor/icons/expand-icon.js
	init_extends();
	var ExpandIcon$1 = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 24 24" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M16 3.25C15.5858 3.25 15.25 3.58579 15.25 4C15.25 4.41421 15.5858 4.75 16 4.75H18.1893L13.4697 9.46967C13.1768 9.76256 13.1768 10.2374 13.4697 10.5303C13.7626 10.8232 14.2374 10.8232 14.5303 10.5303L19.25 5.81066V8C19.25 8.41421 19.5858 8.75 20 8.75C20.4142 8.75 20.75 8.41421 20.75 8V4C20.75 3.80806 20.6768 3.61612 20.5303 3.46967C20.4584 3.39776 20.3755 3.34351 20.2871 3.30691C20.1987 3.27024 20.1017 3.25 20 3.25H16ZM4 15.25C4.41421 15.25 4.75 15.5858 4.75 16V18.1893L9.46967 13.4697C9.76256 13.1768 10.2374 13.1768 10.5303 13.4697C10.8232 13.7626 10.8232 14.2374 10.5303 14.5303L5.81066 19.25H8C8.41421 19.25 8.75 19.5858 8.75 20C8.75 20.4142 8.41421 20.75 8 20.75H4C3.80806 20.75 3.61612 20.6768 3.46967 20.5303C3.39776 20.4584 3.34351 20.3755 3.30691 20.2871C3.27024 20.1987 3.25 20.1017 3.25 20V16C3.25 15.5858 3.58579 15.25 4 15.25ZM20.75 16V20C20.75 20.1017 20.7298 20.1987 20.6931 20.2871C20.6565 20.3755 20.6022 20.4584 20.5303 20.5303C20.4584 20.6022 20.3755 20.6565 20.2871 20.6931C20.2099 20.7251 20.1262 20.7446 20.0386 20.749C20.0258 20.7497 20.0129 20.75 20 20.75H16C15.5858 20.75 15.25 20.4142 15.25 20C15.25 19.5858 15.5858 19.25 16 19.25H18.1893L13.4697 14.5303C13.1768 14.2374 13.1768 13.7626 13.4697 13.4697C13.7626 13.1768 14.2374 13.1768 14.5303 13.4697L19.25 18.1893V16C19.25 15.5858 19.5858 15.25 20 15.25C20.4142 15.25 20.75 15.5858 20.75 16ZM3.71291 3.30691C3.80134 3.27024 3.89831 3.25 4 3.25H8C8.41421 3.25 8.75 3.58579 8.75 4C8.75 4.41421 8.41421 4.75 8 4.75H5.81066L10.5303 9.46967C10.8232 9.76256 10.8232 10.2374 10.5303 10.5303C10.2374 10.8232 9.76256 10.8232 9.46967 10.5303L4.75 5.81066V8C4.75 8.41421 4.41421 8.75 4 8.75C3.58579 8.75 3.25 8.41421 3.25 8V4C3.25 3.7937 3.33329 3.60686 3.46808 3.47126C3.46861 3.47073 3.46914 3.4702 3.46967 3.46967C3.4702 3.46914 3.47073 3.46861 3.47126 3.46808C3.5428 3.39696 3.62511 3.34324 3.71291 3.30691Z"
		}));
	});

//#endregion
//#region modules/ai/assets/js/editor/icons/brush-icon.js
	init_extends();
	var BrushIcon = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 24 24" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M20.8815 2.25919C20.9203 2.25301 20.9599 2.24983 21 2.24988C21.0827 2.24978 21.1629 2.26335 21.2384 2.2887C21.367 2.33162 21.4784 2.40776 21.5642 2.5058C21.6895 2.64891 21.76 2.83868 21.749 3.03992C21.7477 3.06492 21.7452 3.08966 21.7414 3.11407C21.3311 6.09743 20.124 8.91507 18.2472 11.2703C16.5079 13.4529 14.2532 15.1635 11.6906 16.2509C11.8289 17.1168 11.7249 18.0054 11.3884 18.8177C11.0289 19.6857 10.4201 20.4275 9.63896 20.9495C8.85782 21.4714 7.93946 21.75 7 21.75H3C2.58579 21.75 2.25 21.4142 2.25 21V17C2.25 16.0605 2.52858 15.1421 3.05052 14.361C3.57246 13.5799 4.3143 12.9711 5.18225 12.6115C5.99455 12.2751 6.88314 12.1711 7.74905 12.3094C8.83643 9.74682 10.547 7.49212 12.7296 5.75287C15.0837 3.87693 17.8998 2.67012 20.8815 2.25919ZM10.0984 16.0649C10.077 16.0082 10.0629 15.9506 10.0557 15.893C9.89413 15.4471 9.63624 15.04 9.2981 14.7019C8.96001 14.3638 8.55301 14.1059 8.10721 13.9444C8.04953 13.9372 7.99178 13.9231 7.93499 13.9016C7.90509 13.8903 7.87632 13.8773 7.84877 13.8628C7.77794 13.8436 7.70633 13.8268 7.63404 13.8124C7.0036 13.687 6.35014 13.7514 5.75628 13.9974C5.16242 14.2433 4.65484 14.6599 4.29772 15.1944C3.94061 15.7288 3.75 16.3572 3.75 17V20.25H7C7.64279 20.25 8.27114 20.0594 8.8056 19.7022C9.34006 19.3451 9.75662 18.8376 10.0026 18.2437C10.2486 17.6498 10.313 16.9964 10.1876 16.3659C10.1732 16.2935 10.1563 16.2218 10.1371 16.1509C10.1226 16.1234 10.1097 16.0947 10.0984 16.0649ZM10.3588 13.6412C10.7069 13.9894 10.9969 14.3876 11.2204 14.8204C12.2258 14.3839 13.1782 13.8417 14.0621 13.2048C13.3066 11.827 12.173 10.6933 10.7952 9.93782C10.1583 10.8218 9.61603 11.7741 9.17957 12.7795C9.61238 13.0031 10.0106 13.293 10.3588 13.6412ZM11.7434 8.75107C13.1943 9.59789 14.4021 10.8057 15.2489 12.2565C15.9093 11.6727 16.5204 11.0304 17.0741 10.3355C18.5698 8.45849 19.5983 6.25889 20.0821 3.9179C17.7411 4.40165 15.5415 5.43018 13.6644 6.92595C12.9696 7.47964 12.3273 8.0907 11.7434 8.75107Z"
		}));
	});

//#endregion
//#region modules/ai/assets/js/editor/icons/evolve-icon.js
	init_extends();
	var EvolveIcon = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 24 24" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M2.30691 2.71291C2.27024 2.80134 2.25 2.89831 2.25 3V8C2.25 8.41421 2.58579 8.75 3 8.75C3.41421 8.75 3.75 8.41421 3.75 8V4.81066L10.0056 11.0663L10.0057 11.0663C10.8022 11.8631 11.2497 12.9434 11.25 14.07V14.0702V15V21C11.25 21.4142 11.5858 21.75 12 21.75C12.4142 21.75 12.75 21.4142 12.75 21V15V14.0702V14.07C12.7503 12.9434 13.1979 11.863 13.9944 11.0663L20.25 4.81066V8C20.25 8.41421 20.5858 8.75 21 8.75C21.4142 8.75 21.75 8.41421 21.75 8V3C21.75 2.98706 21.7497 2.97419 21.749 2.96141C21.7446 2.87376 21.7251 2.79009 21.6931 2.71291C21.6565 2.62445 21.6022 2.54158 21.5303 2.46967C21.4584 2.39776 21.3755 2.34351 21.2871 2.30691C21.1987 2.27024 21.1017 2.25 21 2.25H16C15.5858 2.25 15.25 2.58579 15.25 3C15.25 3.41421 15.5858 3.75 16 3.75H19.1893L12.9337 10.0057L12.9336 10.0057C12.5656 10.3739 12.2526 10.7867 12 11.2316C11.7474 10.7867 11.4344 10.3739 11.0664 10.0057L11.0663 10.0057L4.81066 3.75H8C8.41421 3.75 8.75 3.41421 8.75 3C8.75 2.58579 8.41421 2.25 8 2.25H3C2.7937 2.25 2.60686 2.33329 2.47126 2.46808C2.47073 2.46861 2.4702 2.46914 2.46967 2.46967C2.46914 2.4702 2.46861 2.47073 2.46808 2.47126C2.39696 2.5428 2.34324 2.62511 2.30691 2.71291Z"
		}));
	});

//#endregion
//#region modules/ai/assets/js/editor/icons/enlarger-icon.js
	init_extends();
	var ExpandIcon = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 24 24" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M5.11612 5.11612C5.35054 4.8817 5.66848 4.75 6 4.75H18C18.3315 4.75 18.6495 4.8817 18.8839 5.11612C19.1183 5.35054 19.25 5.66848 19.25 6V18C19.25 18.3315 19.1183 18.6495 18.8839 18.8839C18.6495 19.1183 18.3315 19.25 18 19.25H11.7335C11.7444 19.1709 11.75 19.0908 11.75 19.01V14.01C11.75 13.8051 11.7141 13.604 11.6457 13.4149L16.25 8.81066V11C16.25 11.4142 16.5858 11.75 17 11.75C17.4142 11.75 17.75 11.4142 17.75 11V7C17.75 6.89831 17.7298 6.80134 17.6931 6.71291C17.6565 6.62445 17.6022 6.54158 17.5303 6.46967C17.4584 6.39776 17.3755 6.34351 17.2871 6.30691C17.1993 6.27051 17.1031 6.2503 17.0022 6.25C17.0015 6.25 17.0007 6.25 17 6.25H16.9997H13C12.5858 6.25 12.25 6.58579 12.25 7C12.25 7.41421 12.5858 7.75 13 7.75H15.1893L10.5803 12.359C10.3956 12.2941 10.1995 12.26 10 12.26H5C4.91585 12.26 4.83232 12.2661 4.75 12.278V6C4.75 5.66848 4.8817 5.35054 5.11612 5.11612ZM4.75 14.01V18C4.75 18.3315 4.8817 18.6495 5.11612 18.8839C5.35054 19.1183 5.66848 19.25 6 19.25H10.07C10.11 19.2383 10.1468 19.2168 10.1768 19.1868C10.2237 19.1399 10.25 19.0763 10.25 19.01V14.01C10.25 13.9437 10.2237 13.8801 10.1768 13.8332C10.1299 13.7863 10.0663 13.76 10 13.76H5C4.9337 13.76 4.87011 13.7863 4.82322 13.8332C4.77634 13.8801 4.75 13.9437 4.75 14.01ZM3.25 14.01V6C3.25 5.27065 3.53973 4.57118 4.05546 4.05546C4.57118 3.53973 5.27065 3.25 6 3.25H18C18.7293 3.25 19.4288 3.53973 19.9445 4.05546C20.4603 4.57118 20.75 5.27065 20.75 6V18C20.75 18.7293 20.4603 19.4288 19.9445 19.9445C19.4288 20.4603 18.7293 20.75 18 20.75H10.187C10.1251 20.7566 10.0627 20.76 10 20.76H5C4.53587 20.76 4.09075 20.5756 3.76256 20.2474C3.43438 19.9193 3.25 19.4741 3.25 19.01V18V14.01Z"
		}));
	});

//#endregion
//#region modules/ai/assets/js/editor/icons/remove-bg.js
	init_extends();
	var RemoveBackgroundIcon = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 16 16" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", { d: "M5.74996 2.50038C5.74996 2.2242 5.52607 2.00031 5.24989 2.00031C4.9737 2.00031 4.74981 2.2242 4.74981 2.50038V2.50705C4.74981 2.78323 4.9737 3.00712 5.24989 3.00712C5.52607 3.00712 5.74996 2.78323 5.74996 2.50705V2.50038Z" }), /*#__PURE__*/ react.default.createElement("path", { d: "M7.9997 2.00031C8.27588 2.00031 8.49978 2.2242 8.49978 2.50038V2.50705C8.49978 2.78323 8.27588 3.00712 7.9997 3.00712C7.72352 3.00712 7.49963 2.78323 7.49963 2.50705V2.50038C7.49963 2.2242 7.72352 2.00031 7.9997 2.00031Z" }), /*#__PURE__*/ react.default.createElement("path", { d: "M11.2496 2.50038C11.2496 2.2242 11.0257 2.00031 10.7495 2.00031C10.4733 2.00031 10.2494 2.2242 10.2494 2.50038V2.50705C10.2494 2.78323 10.4733 3.00712 10.7495 3.00712C11.0257 3.00712 11.2496 2.78323 11.2496 2.50705V2.50038Z" }), /*#__PURE__*/ react.default.createElement("path", { d: "M13.4993 2.00031C13.7755 2.00031 13.9994 2.2242 13.9994 2.50038V2.50705C13.9994 2.78323 13.7755 3.00712 13.4993 3.00712C13.2231 3.00712 12.9993 2.78323 12.9993 2.50705V2.50038C12.9993 2.2242 13.2231 2.00031 13.4993 2.00031Z" }), /*#__PURE__*/ react.default.createElement("path", { d: "M13.9994 5.25019C13.9994 4.97401 13.7755 4.75012 13.4993 4.75012C13.2231 4.75012 12.9993 4.97401 12.9993 5.25019V5.25686C12.9993 5.53304 13.2231 5.75694 13.4993 5.75694C13.7755 5.75694 13.9994 5.53304 13.9994 5.25686V5.25019Z" }), /*#__PURE__*/ react.default.createElement("path", { d: "M13.9994 8.00001C13.9994 7.72382 13.7755 7.49993 13.4993 7.49993C13.2231 7.49993 12.9993 7.72382 12.9993 8.00001V8.00667C12.9993 8.28286 13.2231 8.50675 13.4993 8.50675C13.7755 8.50675 13.9994 8.28286 13.9994 8.00667V8.00001Z" }), /*#__PURE__*/ react.default.createElement("path", { d: "M2.50007 7.49993C2.77626 7.49993 3.00015 7.72382 3.00015 8.00001V8.00667C3.00015 8.28286 2.77626 8.50675 2.50007 8.50675C2.22389 8.50675 2 8.28286 2 8.00667V8.00001C2 7.72382 2.22389 7.49993 2.50007 7.49993Z" }), /*#__PURE__*/ react.default.createElement("path", { d: "M3.00015 5.25019C3.00015 4.97401 2.77626 4.75012 2.50007 4.75012C2.22389 4.75012 2 4.97401 2 5.25019V5.25686C2 5.53304 2.22389 5.75694 2.50007 5.75694C2.77626 5.75694 3.00015 5.53304 3.00015 5.25686V5.25019Z" }), /*#__PURE__*/ react.default.createElement("path", { d: "M2.50007 2.00031C2.77626 2.00031 3.00015 2.2242 3.00015 2.50038V2.50705C3.00015 2.78323 2.77626 3.00712 2.50007 3.00712C2.22389 3.00712 2 2.78323 2 2.50705V2.50038C2 2.2242 2.22389 2.00031 2.50007 2.00031Z" }), /*#__PURE__*/ react.default.createElement("path", { d: "M9.37461 7.31255C9.94411 7.31255 10.4058 6.85088 10.4058 6.28137C10.4058 5.71187 9.94411 5.25019 9.37461 5.25019C8.8051 5.25019 8.34343 5.71187 8.34343 6.28137C8.34343 6.85088 8.8051 7.31255 9.37461 7.31255Z" }), /*#__PURE__*/ react.default.createElement("path", { d: "M9.37461 9.21061L7.6724 7.44357L7.66567 7.43672C7.27331 7.04478 6.79598 6.81248 6.28107 6.81248C5.76615 6.81248 5.28883 7.04478 4.89647 7.43672L4.88973 7.44358L2.13992 10.2982C2.05015 10.3913 2 10.5157 2 10.6451V13.4996C2 13.7758 2.22389 13.9997 2.50007 13.9997H13.4993C13.7755 13.9997 13.9994 13.7758 13.9994 13.4996V10.6451C13.9994 10.6365 13.9992 10.6279 13.9988 10.6194C13.9958 10.562 13.9833 10.5072 13.9627 10.4566C13.9409 10.403 13.9093 10.3523 13.8681 10.3073C13.8651 10.304 13.8621 10.3008 13.859 10.2976L12.4846 8.87085L12.4778 8.86399C12.0855 8.47205 11.6082 8.23975 11.0932 8.23975C10.5783 8.23975 10.101 8.47205 9.70864 8.86399L9.70191 8.87084L9.37461 9.21061ZM5.60649 8.14113L3.00015 10.8468V12.9996H12.9993V10.8468L11.7678 9.5684C11.5234 9.32549 11.2897 9.2399 11.0932 9.2399C10.8968 9.2399 10.6631 9.32549 10.4187 9.5684L10.069 9.93142L11.1097 11.0118C11.3013 11.2107 11.2954 11.5272 11.0965 11.7188C10.8975 11.9104 10.581 11.9045 10.3894 11.7056L9.01604 10.28C9.01482 10.2787 9.01361 10.2775 9.01241 10.2762L6.95564 8.14113C6.7112 7.89822 6.4775 7.81263 6.28107 7.81263C6.08463 7.81263 5.85094 7.89822 5.60649 8.14113Z" }));
	});

//#endregion
//#region modules/ai/assets/js/editor/icons/replace-bg.js
	init_extends();
	var ReplaceBackgroundIcon = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 16 16" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", { d: "M4.35355 1.64645C4.54882 1.84171 4.54882 2.15829 4.35355 2.35355L2.35355 4.35355C2.15829 4.54882 1.84171 4.54882 1.64645 4.35355C1.45118 4.15829 1.45118 3.84171 1.64645 3.64645L3.64645 1.64645C3.84171 1.45118 4.15829 1.45118 4.35355 1.64645Z" }), /*#__PURE__*/ react.default.createElement("path", { d: "M7.68689 1.64645C7.88215 1.84171 7.88215 2.15829 7.68689 2.35355L2.35355 7.68689C2.15829 7.88215 1.84171 7.88215 1.64645 7.68689C1.45118 7.49162 1.45118 7.17504 1.64645 6.97978L6.97978 1.64645C7.17504 1.45118 7.49162 1.45118 7.68689 1.64645Z" }), /*#__PURE__*/ react.default.createElement("path", { d: "M11.0202 1.64645C11.2155 1.84171 11.2155 2.15829 11.0202 2.35355L6.68689 6.68689C6.49162 6.88215 6.17504 6.88215 5.97978 6.68689C5.78452 6.49162 5.78452 6.17504 5.97978 5.97978L10.3131 1.64645C10.5084 1.45118 10.825 1.45118 11.0202 1.64645Z" }), /*#__PURE__*/ react.default.createElement("path", { d: "M14.3536 1.64645C14.5488 1.84171 14.5488 2.15829 14.3536 2.35355L11.0202 5.68689C10.825 5.88215 10.5084 5.88215 10.3131 5.68689C10.1179 5.49162 10.1179 5.17504 10.3131 4.97978L13.6464 1.64645C13.8417 1.45118 14.1583 1.45118 14.3536 1.64645Z" }), /*#__PURE__*/ react.default.createElement("path", { d: "M14.3536 4.97978C14.5488 5.17504 14.5488 5.49162 14.3536 5.68689L12.0202 8.02022C11.825 8.21548 11.5084 8.21548 11.3131 8.02022C11.1179 7.82496 11.1179 7.50838 11.3131 7.31311L13.6464 4.97978C13.8417 4.78452 14.1583 4.78452 14.3536 4.97978Z" }), /*#__PURE__*/ react.default.createElement("path", { d: "M14.3536 8.31311C14.5488 8.50838 14.5488 8.82496 14.3536 9.02022L14.0202 9.35355C13.825 9.54882 13.5084 9.54882 13.3131 9.35355C13.1179 9.15829 13.1179 8.84171 13.3131 8.64645L13.6464 8.31311C13.8417 8.11785 14.1583 8.11785 14.3536 8.31311Z" }), /*#__PURE__*/ react.default.createElement("path", { d: "M10.3333 7C10.3333 7.55228 9.88562 8 9.33333 8C8.78105 8 8.33333 7.55228 8.33333 7C8.33333 6.44772 8.78105 6 9.33333 6C9.88562 6 10.3333 6.44772 10.3333 7Z" }), /*#__PURE__*/ react.default.createElement("path", { d: "M6.33333 8.5C6.14679 8.5 5.92267 8.58153 5.6867 8.81725C5.67587 8.82806 5.66455 8.83838 5.65278 8.84816L2.5 11.4665V13.5H13.5V11.2318C13.5 10.9556 13.7239 10.7318 14 10.7318C14.2761 10.7318 14.5 10.9556 14.5 11.2318V14C14.5 14.2761 14.2761 14.5 14 14.5H2C1.72386 14.5 1.5 14.2761 1.5 14V11.2318C1.5 11.0831 1.56617 10.9421 1.68056 10.8471L4.99717 8.09274C5.37661 7.72073 5.83674 7.5 6.33333 7.5C6.83746 7.5 7.304 7.72747 7.6867 8.10976L7.69343 8.11662L11.0268 11.5769C11.2183 11.7758 11.2124 12.0923 11.0136 12.2839C10.8147 12.4755 10.4981 12.4696 10.3066 12.2707L6.97679 8.81408C6.74199 8.5808 6.51904 8.5 6.33333 8.5Z" }), /*#__PURE__*/ react.default.createElement("path", { d: "M10.3564 10.1984L9.69343 10.8866C9.50185 11.0854 9.18532 11.0914 8.98645 10.8998C8.78757 10.7082 8.78166 10.3917 8.97324 10.1928L9.6399 9.50073L9.64663 9.49387C10.4992 8.64223 11.843 8.83788 12.6387 9.45172C12.6435 9.45538 12.6482 9.45913 12.6528 9.46297L14.3194 10.8471C14.5319 11.0235 14.5611 11.3388 14.3846 11.5512C14.2082 11.7636 13.893 11.7928 13.6806 11.6164L12.0213 10.2384C11.4855 9.82976 10.7405 9.81862 10.3564 10.1984Z" }));
	});

//#endregion
//#region modules/ai/assets/js/editor/icons/cleanup-icon.js
	init_extends();
	var Cleanup$1 = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 16 16" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M10.6668 11.8334H3.66679C3.53235 11.8334 3.40357 11.7792 3.30952 11.6832L0.505518 8.81921L0.504041 8.81772C0.287672 8.59928 0.16626 8.30423 0.16626 7.99671C0.16626 7.68849 0.288225 7.3928 0.505518 7.17421L0.506568 7.17316L7.17323 0.506492L7.17429 0.505442C7.39288 0.288149 7.68857 0.166183 7.99679 0.166183C8.30501 0.166183 8.6007 0.288149 8.81929 0.505442L8.82034 0.50649L12.1547 3.84088C12.372 4.05947 12.494 4.35516 12.494 4.66338C12.494 4.97134 12.3722 5.26679 12.1553 5.48533L12.1547 5.48588L6.86473 10.8334H10.6668C10.9429 10.8334 11.1668 11.0572 11.1668 11.3334C11.1668 11.6095 10.9429 11.8334 10.6668 11.8334ZM7.87959 1.21435C7.91079 1.18349 7.9529 1.16618 7.99679 1.16618C8.04067 1.16618 8.08278 1.18349 8.11398 1.21434L11.4455 4.54588C11.4763 4.57707 11.494 4.61951 11.494 4.66338C11.494 4.70741 11.4766 4.74965 11.4455 4.78088L11.4447 4.78174L10.0411 6.20059L6.46721 2.62673L7.87959 1.21435ZM5.7601 3.33383L1.21473 7.87921C1.18387 7.91041 1.16626 7.95282 1.16626 7.99671C1.16626 8.04074 1.18368 8.08298 1.21473 8.11421L1.2174 8.11691L3.877 10.8334H5.4581L9.33779 6.91152L5.7601 3.33383Z"
		}));
	});

//#endregion
//#region modules/ai/assets/js/editor/icons/isolate-object-icon.js
	init_extends();
	var IsolateObjectIcon = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 16 16" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M2.6665 2.16699C2.94265 2.16699 3.1665 2.39085 3.1665 2.66699V2.67366C3.1665 2.9498 2.94265 3.17366 2.6665 3.17366C2.39036 3.17366 2.1665 2.9498 2.1665 2.67366V2.66699C2.1665 2.39085 2.39036 2.16699 2.6665 2.16699ZM5.33317 2.16699C5.60931 2.16699 5.83317 2.39085 5.83317 2.66699V2.67366C5.83317 2.9498 5.60931 3.17366 5.33317 3.17366C5.05703 3.17366 4.83317 2.9498 4.83317 2.67366V2.66699C4.83317 2.39085 5.05703 2.16699 5.33317 2.16699ZM7.99984 2.16699C8.27598 2.16699 8.49984 2.39085 8.49984 2.66699V2.67366C8.49984 2.9498 8.27598 3.17366 7.99984 3.17366C7.72369 3.17366 7.49984 2.9498 7.49984 2.67366V2.66699C7.49984 2.39085 7.72369 2.16699 7.99984 2.16699ZM10.6665 2.16699C10.9426 2.16699 11.1665 2.39085 11.1665 2.66699V2.67366C11.1665 2.9498 10.9426 3.17366 10.6665 3.17366C10.3904 3.17366 10.1665 2.9498 10.1665 2.67366V2.66699C10.1665 2.39085 10.3904 2.16699 10.6665 2.16699ZM13.3332 2.16699C13.6093 2.16699 13.8332 2.39085 13.8332 2.66699V2.67366C13.8332 2.9498 13.6093 3.17366 13.3332 3.17366C13.057 3.17366 12.8332 2.9498 12.8332 2.67366V2.66699C12.8332 2.39085 13.057 2.16699 13.3332 2.16699ZM2.6665 4.83366C2.94265 4.83366 3.1665 5.05752 3.1665 5.33366V5.34033C3.1665 5.61647 2.94265 5.84033 2.6665 5.84033C2.39036 5.84033 2.1665 5.61647 2.1665 5.34033V5.33366C2.1665 5.05752 2.39036 4.83366 2.6665 4.83366ZM4.83317 5.33366C4.83317 5.05752 5.05703 4.83366 5.33317 4.83366H10.6665C10.9426 4.83366 11.1665 5.05752 11.1665 5.33366V10.667C11.1665 10.9431 10.9426 11.167 10.6665 11.167H5.33317C5.05703 11.167 4.83317 10.9431 4.83317 10.667V5.33366ZM5.83317 5.83366V10.167H10.1665V5.83366H5.83317ZM13.3332 4.83366C13.6093 4.83366 13.8332 5.05752 13.8332 5.33366V5.34033C13.8332 5.61647 13.6093 5.84033 13.3332 5.84033C13.057 5.84033 12.8332 5.61647 12.8332 5.34033V5.33366C12.8332 5.05752 13.057 4.83366 13.3332 4.83366ZM2.6665 7.50033C2.94265 7.50033 3.1665 7.72418 3.1665 8.00033V8.00699C3.1665 8.28313 2.94265 8.50699 2.6665 8.50699C2.39036 8.50699 2.1665 8.28313 2.1665 8.00699V8.00033C2.1665 7.72418 2.39036 7.50033 2.6665 7.50033ZM13.3332 7.50033C13.6093 7.50033 13.8332 7.72418 13.8332 8.00033V8.00699C13.8332 8.28313 13.6093 8.50699 13.3332 8.50699C13.057 8.50699 12.8332 8.28313 12.8332 8.00699V8.00033C12.8332 7.72418 13.057 7.50033 13.3332 7.50033ZM2.6665 10.167C2.94265 10.167 3.1665 10.3908 3.1665 10.667V10.6737C3.1665 10.9498 2.94265 11.1737 2.6665 11.1737C2.39036 11.1737 2.1665 10.9498 2.1665 10.6737V10.667C2.1665 10.3908 2.39036 10.167 2.6665 10.167ZM13.3332 10.167C13.6093 10.167 13.8332 10.3908 13.8332 10.667V10.6737C13.8332 10.9498 13.6093 11.1737 13.3332 11.1737C13.057 11.1737 12.8332 10.9498 12.8332 10.6737V10.667C12.8332 10.3908 13.057 10.167 13.3332 10.167ZM2.6665 12.8337C2.94265 12.8337 3.1665 13.0575 3.1665 13.3337V13.3403C3.1665 13.6165 2.94265 13.8403 2.6665 13.8403C2.39036 13.8403 2.1665 13.6165 2.1665 13.3403V13.3337C2.1665 13.0575 2.39036 12.8337 2.6665 12.8337ZM5.33317 12.8337C5.60931 12.8337 5.83317 13.0575 5.83317 13.3337V13.3403C5.83317 13.6165 5.60931 13.8403 5.33317 13.8403C5.05703 13.8403 4.83317 13.6165 4.83317 13.3403V13.3337C4.83317 13.0575 5.05703 12.8337 5.33317 12.8337ZM7.99984 12.8337C8.27598 12.8337 8.49984 13.0575 8.49984 13.3337V13.3403C8.49984 13.6165 8.27598 13.8403 7.99984 13.8403C7.72369 13.8403 7.49984 13.6165 7.49984 13.3403V13.3337C7.49984 13.0575 7.72369 12.8337 7.99984 12.8337ZM10.6665 12.8337C10.9426 12.8337 11.1665 13.0575 11.1665 13.3337V13.3403C11.1665 13.6165 10.9426 13.8403 10.6665 13.8403C10.3904 13.8403 10.1665 13.6165 10.1665 13.3403V13.3337C10.1665 13.0575 10.3904 12.8337 10.6665 12.8337ZM13.3332 12.8337C13.6093 12.8337 13.8332 13.0575 13.8332 13.3337V13.3403C13.8332 13.6165 13.6093 13.8403 13.3332 13.8403C13.057 13.8403 12.8332 13.6165 12.8332 13.3403V13.3337C12.8332 13.0575 13.057 12.8337 13.3332 12.8337Z"
		}));
	});

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/image-tools/image-tools-panel.js
	init_use_introduction();
	var TeaserDrawing = function TeaserDrawing() {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, {
			viewBox: "0 0 184 80",
			sx: {
				width: 184,
				height: 80
			}
		}, /*#__PURE__*/ react.default.createElement("g", { clipPath: "url(#clip0_3127_96542)" }, /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M49.6014 1.36364C49.6014 0.610521 48.9907 0 48.2373 0C47.4839 0 46.8731 0.610521 46.8731 1.36364C46.8731 1.9664 46.6336 2.54448 46.2072 2.9707C45.7808 3.39692 45.2025 3.63636 44.5995 3.63636C43.8461 3.63636 43.2354 4.24689 43.2354 5C43.2354 5.75311 43.8461 6.36364 44.5995 6.36364C45.2025 6.36364 45.7808 6.60308 46.2072 7.0293C46.6336 7.45552 46.8731 8.0336 46.8731 8.63636C46.8731 9.38948 47.4839 10 48.2373 10C48.9907 10 49.6014 9.38948 49.6014 8.63636C49.6014 8.0336 49.8409 7.45552 50.2673 7.0293C50.6937 6.60308 51.272 6.36364 51.875 6.36364C52.6284 6.36364 53.2392 5.75311 53.2392 5C53.2392 4.24689 52.6284 3.63636 51.875 3.63636C51.272 3.63636 50.6937 3.39692 50.2673 2.9707C49.8409 2.54448 49.6014 1.9664 49.6014 1.36364ZM48.1364 4.89917C48.1706 4.86501 48.2042 4.83041 48.2373 4.79538C48.2703 4.83041 48.304 4.86501 48.3381 4.89917C48.3723 4.93333 48.4069 4.96694 48.4419 5C48.4069 5.03306 48.3723 5.06667 48.3381 5.10083C48.304 5.13499 48.2703 5.16959 48.2373 5.20462C48.2042 5.16959 48.1706 5.13499 48.1364 5.10083C48.1022 5.06667 48.0676 5.03306 48.0326 5C48.0676 4.96694 48.1022 4.93333 48.1364 4.89917Z",
			fill: "#69727D"
		}), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M66.6927 22.3677C66.6927 21.6177 66.082 21.0098 65.3286 21.0098C64.5752 21.0098 63.9644 21.6177 63.9644 22.3677C63.9644 22.9679 63.7249 23.5436 63.2985 23.968C62.8721 24.3924 62.2938 24.6309 61.6908 24.6309C60.9374 24.6309 60.3267 25.2388 60.3267 25.9888C60.3267 26.7387 60.9374 27.3467 61.6908 27.3467C62.2938 27.3467 62.8721 27.5851 63.2985 28.0096C63.7249 28.434 63.9644 29.0096 63.9644 29.6099C63.9644 30.3598 64.5752 30.9678 65.3286 30.9678C66.082 30.9678 66.6927 30.3598 66.6927 29.6099C66.6927 29.0096 66.9323 28.434 67.3586 28.0096C67.785 27.5851 68.3633 27.3467 68.9663 27.3467C69.7197 27.3467 70.3305 26.7387 70.3305 25.9888C70.3305 25.2388 69.7197 24.6309 68.9663 24.6309C68.3633 24.6309 67.785 24.3924 67.3586 23.968C66.9323 23.5436 66.6927 22.9679 66.6927 22.3677ZM65.2277 25.8884C65.2619 25.8544 65.2955 25.8199 65.3286 25.785C65.3616 25.8199 65.3953 25.8544 65.4294 25.8884C65.4636 25.9224 65.4982 25.9559 65.5333 25.9888C65.4982 26.0217 65.4636 26.0552 65.4294 26.0892C65.3953 26.1232 65.3616 26.1577 65.3286 26.1925C65.2955 26.1577 65.2619 26.1232 65.2277 26.0892C65.1935 26.0552 65.1589 26.0217 65.1239 25.9888C65.1589 25.9559 65.1935 25.9224 65.2277 25.8884Z",
			fill: "#69727D"
		}), /*#__PURE__*/ react.default.createElement("path", {
			d: "M53.9717 12L57.2198 17.0456L3.24796 51.4731L-0.000164873 46.4275L53.9717 12Z",
			fill: "#69727D"
		}), /*#__PURE__*/ react.default.createElement("path", {
			d: "M64.9761 5L68.2242 10.0456L57.2612 17.0387L54.013 11.9931L64.9761 5Z",
			fill: "#E6E8EA"
		}), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M65.7284 9.49923L64.432 7.48537L56.5088 12.5394L57.8052 14.5533L65.7284 9.49923ZM64.9761 5L54.013 11.9931L57.2612 17.0387L68.2242 10.0456L64.9761 5Z",
			fill: "#69727D"
		}), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M128 41C128 43.7687 121.671 46.2089 112.052 47.6451L114.482 74.7976C114.494 74.8647 114.5 74.9322 114.5 75C114.5 75.0001 114.5 75.0002 114.5 75.0003C114.513 75.1456 114.487 75.2779 114.43 75.3982C113.514 77.9735 103.8 80 91.95 80C79.496 80 69.4 77.7614 69.4 75C69.4 74.8146 69.4456 74.6315 69.5343 74.4513L71.2141 47.5326C62.0087 46.0835 56 43.6977 56 41C56 36.5817 72.1177 33 92 33C111.882 33 128 36.5817 128 41Z",
			fill: "#69727D"
		}), /*#__PURE__*/ react.default.createElement("path", {
			d: "M106.685 3.08259C112.562 16.564 109.338 34.1761 106.713 41.4407C106.713 41.4407 105.151 41.9127 100.894 41.9715C96.6371 42.0303 95.0251 41.9866 95.0251 41.4558C95.0252 40.9249 94.6272 34.6461 96.4177 22.3308C98.0343 11.2116 102.692 5.02553 105.448 2.75675C105.865 2.4137 106.47 2.58777 106.685 3.08259Z",
			fill: "#E6E8EA"
		}), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M104.883 4.66783C105.347 4.17219 106.14 4.35208 106.38 4.9873C108.716 11.1748 109.254 18.1507 108.889 24.5355C108.513 31.1152 107.184 36.9856 105.954 40.5842C105.778 40.6166 105.55 40.6541 105.265 40.6927C104.388 40.8119 102.974 40.9423 100.88 40.9712C98.7385 41.0008 97.3196 41.0031 96.4377 40.9371C96.2614 40.9239 96.1203 40.9088 96.0088 40.8935C95.9567 39.0573 95.8782 32.9919 97.4073 22.4743C98.766 13.1288 102.302 7.42438 104.883 4.66783ZM106.713 41.4403L107.002 42.3975C107.305 42.3062 107.546 42.0773 107.653 41.7802C109 38.0524 110.481 31.7376 110.886 24.6496C111.29 17.5654 110.626 9.61894 107.602 2.68255C107.147 1.63949 105.792 1.17814 104.812 1.98425C101.838 4.43294 97.0752 10.8574 95.4281 22.1866C93.7715 33.5811 93.975 39.865 94.0187 41.2138C94.0225 41.3324 94.0251 41.4128 94.0251 41.4552C94.025 42.261 94.695 42.5983 95.0067 42.7098C95.3591 42.8358 95.8079 42.8956 96.2886 42.9316C97.2769 43.0055 98.7924 43.0002 100.908 42.971C103.071 42.9411 104.566 42.806 105.534 42.6745C106.018 42.6088 106.371 42.5438 106.61 42.4932C106.729 42.4679 106.82 42.4461 106.885 42.4296C106.917 42.4214 106.943 42.4144 106.962 42.409L106.987 42.4021L106.996 42.3995L106.999 42.3984L107.001 42.398C107.001 42.398 107.002 42.3975 106.713 41.4403ZM106.426 40.4822L106.428 40.4817L106.43 40.4812C106.43 40.4812 106.429 40.4814 106.426 40.4822Z",
			fill: "#69727D"
		}), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M104.22 13.8372C104.787 15.7536 104.996 18.3439 104.953 21.2589C104.907 24.4148 104.569 27.8559 104.134 31.0529C103.7 34.2469 103.172 37.1799 102.753 39.3152C102.544 40.3824 102.361 41.2491 102.232 41.8481C102.22 41.9009 102.209 41.9516 102.199 42.0001H100.503C100.526 38.5025 100.676 33.4168 101.183 28.2866C101.724 22.8227 102.66 17.4299 104.22 13.8372ZM103 43.0001C103.974 43.2266 103.974 43.2264 103.974 43.2262L103.978 43.2093L103.989 43.1613C103.999 43.1192 104.013 43.0571 104.031 42.976C104.067 42.814 104.12 42.5763 104.186 42.2713C104.318 41.6613 104.503 40.7819 104.716 39.7006C105.14 37.5391 105.675 34.5658 106.116 31.3223C106.556 28.0818 106.905 24.5541 106.953 21.2881C107 18.0456 106.753 14.9635 105.943 12.6671C105.815 12.3045 105.676 11.9539 105.512 11.6797C105.394 11.4809 105.045 10.9388 104.371 10.9076C103.702 10.8766 103.301 11.3772 103.156 11.5722C102.959 11.8385 102.774 12.1912 102.594 12.5778C100.767 16.4945 99.7565 22.3931 99.1929 28.0897C98.6258 33.8211 98.5 39.4812 98.5 43.0001V44.0001H103.794L103.974 43.2262L103 43.0001Z",
			fill: "#69727D"
		}), /*#__PURE__*/ react.default.createElement("path", {
			d: "M79.2952 3.08259C73.4181 16.564 76.6423 34.1761 79.2676 41.4407C79.2676 41.4407 80.8299 41.9127 85.0867 41.9715C89.3434 42.0303 90.9554 41.9866 90.9554 41.4558C90.9553 40.9249 91.3533 34.6461 89.5628 22.3308C87.9462 11.2116 83.2888 5.02553 80.5325 2.75675C80.1157 2.4137 79.511 2.58777 79.2952 3.08259Z",
			fill: "#E6E8EA"
		}), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M81.0978 4.66783C80.6337 4.17219 79.8403 4.35208 79.6005 4.9873C77.2644 11.1748 76.7267 18.1507 77.0914 24.5355C77.4671 31.1152 78.7963 36.9856 80.0267 40.5842C80.2028 40.6166 80.4308 40.6541 80.7155 40.6927C81.5929 40.8119 83.0069 40.9423 85.1005 40.9712C87.242 41.0008 88.6609 41.0031 89.5427 40.9371C89.7191 40.9239 89.8602 40.9088 89.9716 40.8935C90.0238 39.0573 90.1023 32.9919 88.5732 22.4743C87.2145 13.1288 83.6789 7.42438 81.0978 4.66783ZM79.2676 41.4403L78.9784 42.3975C78.676 42.3062 78.4345 42.0773 78.3271 41.7802C76.98 38.0524 75.4994 31.7376 75.0946 24.6496C74.69 17.5654 75.3547 9.61894 78.3786 2.68255C78.8333 1.63949 80.1887 1.17814 81.168 1.98425C84.1429 4.43294 88.9053 10.8574 90.5524 22.1866C92.209 33.5811 92.0055 39.865 91.9618 41.2138C91.958 41.3324 91.9554 41.4128 91.9554 41.4552C91.9554 42.261 91.2855 42.5983 90.9737 42.7098C90.6214 42.8358 90.1726 42.8956 89.6919 42.9316C88.7036 43.0055 87.1881 43.0002 85.0728 42.971C82.9096 42.9411 81.4141 42.806 80.4462 42.6745C79.9621 42.6088 79.6094 42.5438 79.3707 42.4932C79.2514 42.4679 79.1605 42.4461 79.0958 42.4296C79.0635 42.4214 79.0377 42.4144 79.0182 42.409L78.9937 42.4021L78.9849 42.3995L78.9814 42.3984L78.9798 42.398C78.9798 42.398 78.9784 42.3975 79.2676 41.4403ZM79.5541 40.4822L79.5526 40.4817L79.5507 40.4812C79.5507 40.4812 79.5514 40.4814 79.5541 40.4822Z",
			fill: "#69727D"
		}), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M81.7604 13.8372C81.1939 15.7536 80.9847 18.3439 81.0272 21.2589C81.0733 24.4148 81.4118 27.8559 81.8464 31.0529C82.2805 34.2469 82.8081 37.1799 83.2274 39.3152C83.4369 40.3824 83.6192 41.2491 83.7488 41.8481C83.7603 41.9009 83.7713 41.9516 83.7819 42.0001H85.4771C85.4542 38.5025 85.3049 33.4168 84.7973 28.2866C84.2567 22.8227 83.32 17.4299 81.7604 13.8372ZM82.9805 43.0001C82.0065 43.2266 82.0064 43.2264 82.0064 43.2262L82.0025 43.2093L81.9914 43.1613C81.9818 43.1192 81.9677 43.0571 81.9494 42.976C81.913 42.814 81.8602 42.5763 81.7941 42.2713C81.6621 41.6613 81.4772 40.7819 81.2648 39.7006C80.8404 37.5391 80.3054 34.5658 79.8646 31.3223C79.4241 28.0818 79.0751 24.5541 79.0275 21.2881C78.9801 18.0456 79.227 14.9635 80.0375 12.6671C80.1655 12.3045 80.305 11.9539 80.4683 11.6797C80.5868 11.4809 80.9351 10.9388 81.6092 10.9076C82.2782 10.8766 82.6793 11.3772 82.8241 11.5722C83.0217 11.8385 83.2064 12.1912 83.3867 12.5778C85.2136 16.4945 86.224 22.3931 86.7876 28.0897C87.3546 33.8211 87.4805 39.4812 87.4805 43.0001V44.0001H82.1863L82.0064 43.2262L82.9805 43.0001Z",
			fill: "#69727D"
		})), /*#__PURE__*/ react.default.createElement("defs", null, /*#__PURE__*/ react.default.createElement("clipPath", { id: "clip0_3127_96542" }, /*#__PURE__*/ react.default.createElement("rect", {
			width: "184",
			height: "80",
			fill: "white"
		}))));
	};
	var ImageToolsContainer = (0, _elementor_ui.styled)(_elementor_ui.Box)(function(_ref) {
		var theme = _ref.theme;
		return { height: "calc(100% - ".concat(theme.spacing(13), ")") };
	});
	var ToolsTeaserContainer = (0, _elementor_ui.styled)(_elementor_ui.Box)(function(_ref2) {
		return {
			position: "sticky",
			top: "100%",
			padding: _ref2.theme.spacing(4, 0, 1)
		};
	});
	var StyledChip = (0, _elementor_ui.styled)(_elementor_ui.Chip)(function() {
		return {
			position: "absolute",
			top: 8,
			right: 8,
			"& .MuiChip-label": {
				fontSize: "0.75rem",
				fontWeight: "normal"
			}
		};
	});
	var ImageToolsPanel = function ImageToolsPanel() {
		var navigate = useLocation().navigate;
		var _useIntroduction = useIntroduction("e-ai-image-isolate-tool");
		var isIsolateViewed = _useIntroduction.isViewed;
		var markIsolateAsViewed = _useIntroduction.markAsViewed;
		var tools = [
			{
				label: (0, _wordpress_i18n.__)("Expand Image", "elementor"),
				Icon: ExpandIcon$1,
				onClick: function onClick() {
					return navigate(LOCATIONS.OUT_PAINTING);
				}
			},
			{
				label: (0, _wordpress_i18n.__)("Generative Fill", "elementor"),
				Icon: BrushIcon,
				onClick: function onClick() {
					return navigate(LOCATIONS.IN_PAINTING);
				}
			},
			{
				label: (0, _wordpress_i18n.__)("Resize", "elementor"),
				Icon: ExpandIcon,
				onClick: function onClick() {
					return navigate(LOCATIONS.RESIZE);
				}
			},
			{
				label: (0, _wordpress_i18n.__)("Variations", "elementor"),
				Icon: EvolveIcon,
				onClick: function onClick() {
					return navigate(LOCATIONS.VARIATIONS);
				}
			},
			{
				label: (0, _wordpress_i18n.__)("Remove Background", "elementor"),
				Icon: RemoveBackgroundIcon,
				onClick: function onClick() {
					return navigate(LOCATIONS.REMOVE_BACKGROUND);
				}
			},
			{
				label: (0, _wordpress_i18n.__)("Replace Background", "elementor"),
				Icon: ReplaceBackgroundIcon,
				onClick: function onClick() {
					return navigate(LOCATIONS.REPLACE_BACKGROUND);
				}
			},
			{
				label: (0, _wordpress_i18n.__)("Cleanup", "elementor"),
				Icon: Cleanup$1,
				onClick: function onClick() {
					return navigate(LOCATIONS.CLEANUP);
				}
			},
			{
				label: (0, _wordpress_i18n.__)("Isolate object", "elementor"),
				Icon: IsolateObjectIcon,
				ChipParam: !isIsolateViewed ? /*#__PURE__*/ react.default.createElement(StyledChip, {
					label: "New",
					color: "info",
					variant: "standard",
					size: "tiny"
				}) : null,
				onClick: function onClick() {
					markIsolateAsViewed();
					navigate(LOCATIONS.ISOLATE_OBJECT);
				}
			}
		];
		return /*#__PURE__*/ react.default.createElement(ImageToolsContainer, null, /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, {
			display: "grid",
			gridTemplateColumns: "repeat(2, 1fr)",
			gap: 1,
			justifyContent: "center"
		}, tools.map(function(_ref3) {
			var label = _ref3.label;
			var Icon = _ref3.Icon;
			var ChipParam = _ref3.ChipParam;
			var onClick = _ref3.onClick;
			return /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, {
				onClick,
				key: label,
				variant: "outlined",
				color: "secondary",
				disabled: false,
				sx: {
					py: 3,
					fontSize: "12px",
					height: "auto",
					borderRadius: "4px"
				}
			}, ChipParam, /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, {
				display: "flex",
				justifyContent: "center",
				alignItems: "center",
				flexDirection: "column"
			}, /*#__PURE__*/ react.default.createElement(Icon, { sx: { mb: .5 } }), label));
		})), /*#__PURE__*/ react.default.createElement(ToolsTeaserContainer, {
			display: "flex",
			flexDirection: "column",
			alignItems: "center"
		}, /*#__PURE__*/ react.default.createElement(TeaserDrawing, null), /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, {
			variant: "body2",
			align: "center",
			color: "secondary",
			sx: { mt: 2 }
		}, (0, _wordpress_i18n.__)("Stay tuned! More incredible AI tools are coming your way soon.", "elementor"))));
	};
	ImageToolsPanel.propTypes = {
		generateNewPrompt: import_prop_types$7.default.func,
		panelActive: import_prop_types$7.default.bool
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/image-tools/image-tools-content.js
	var ImageToolsContent = function ImageToolsContent() {
		var _useEditImage = useEditImage();
		var editImage = _useEditImage.editImage;
		var width = _useEditImage.width;
		var height = _useEditImage.height;
		var use = useImageActions().use;
		return /*#__PURE__*/ react.default.createElement(SingleImagePreview, null, /*#__PURE__*/ react.default.createElement(SingleImagePreview.Image, {
			src: editImage.url,
			style: {
				width,
				height
			},
			alt: editImage.alt || (0, _wordpress_i18n.__)("Image preview", "elementor")
		}, /*#__PURE__*/ react.default.createElement(SingleImagePreview.Actions, null, /*#__PURE__*/ react.default.createElement(ImageActions.UseImage, { onClick: function onClick() {
			return use(editImage);
		} }))));
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/image-tools/index.js
	var ImageTools = function ImageTools() {
		var navigate = useLocation().navigate;
		return /*#__PURE__*/ react.default.createElement(View, null, /*#__PURE__*/ react.default.createElement(View.Panel, null, /*#__PURE__*/ react.default.createElement(View.BackButton, { onClick: function onClick() {
			return navigate(LOCATIONS.GENERATE);
		} }, (0, _wordpress_i18n.__)("Generate with a prompt", "elementor")), /*#__PURE__*/ react.default.createElement(View.PanelHeading, { primary: (0, _wordpress_i18n.__)("Edit with AI", "elementor") }), /*#__PURE__*/ react.default.createElement(ImageToolsPanel, null)), /*#__PURE__*/ react.default.createElement(View.Content, null, /*#__PURE__*/ react.default.createElement(ImageToolsContent, null)));
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/components/generate-submit.js
	init_extends();
	var GenerateSubmit = function GenerateSubmit(props) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, _extends$1({
			fullWidth: true,
			size: "medium",
			type: "submit",
			variant: "contained"
		}, props), props.children || (0, _wordpress_i18n.__)("Generate", "elementor"));
	};
	GenerateSubmit.propTypes = { children: import_prop_types$7.default.node };

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/hooks/use-image-prompt.js
	init_use_prompt();
	var useImagePrompt = function useImagePrompt() {
		var promptData = usePrompt.apply(void 0, arguments);
		var _useGlobalActions = useGlobalActions();
		var setHasUnsavedChanges = _useGlobalActions.setHasUnsavedChanges;
		var state = _useGlobalActions.state;
		(0, react.useEffect)(function() {
			var _promptData$data;
			if ((_promptData$data = promptData.data) !== null && _promptData$data !== void 0 && _promptData$data.result) setHasUnsavedChanges(true);
		}, [promptData.data]);
		(0, react.useEffect)(function() {
			if (state.isAllSaved) promptData.sendUsageData();
		}, [state.isAllSaved]);
		return promptData;
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/resize/hooks/use-resize.js
	init_api();
	var useResize = function useResize(initialValue) {
		return useImagePrompt(function fetchAction(payload) {
			return getImageToImageUpscale(payload);
		}, initialValue);
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/resize/index.js
	init_defineProperty$1();
	init_requests_ids();
	var Resize = function Resize() {
		var _useEditImage = useEditImage();
		var editImage = _useEditImage.editImage;
		var initialEditImageWidth = _useEditImage.width;
		var setGenerate = useRequestIds().setGenerate;
		var _useImageActions = useImageActions();
		var use = _useImageActions.use;
		var edit = _useImageActions.edit;
		var isUploading = _useImageActions.isLoading;
		var _usePromptSettings = usePromptSettings();
		var settings = _usePromptSettings.settings;
		var updateSettings = _usePromptSettings.updateSettings;
		var _useResize = useResize();
		var data = _useResize.data;
		var send = _useResize.send;
		var isGenerating = _useResize.isLoading;
		var error = _useResize.error;
		var isLoading = isGenerating || isUploading;
		var handleSubmit = function handleSubmit(event) {
			event.preventDefault();
			setGenerate();
			send({
				promptSettings: settings,
				image: editImage
			});
		};
		return /*#__PURE__*/ react.default.createElement(View, null, /*#__PURE__*/ react.default.createElement(View.Panel, null, /*#__PURE__*/ react.default.createElement(View.BackButton, null), /*#__PURE__*/ react.default.createElement(View.PanelHeading, {
			primary: (0, _wordpress_i18n.__)("Resize", "elementor"),
			secondary: (0, _wordpress_i18n.__)("Make an image larger and improve it’s resolution.", "elementor")
		}), error && /*#__PURE__*/ react.default.createElement(View.ErrorMessage, {
			error,
			onRetry: handleSubmit
		}), /*#__PURE__*/ react.default.createElement(ImageForm, { onSubmit: handleSubmit }, /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, { spacing: .2 }, /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, {
			display: "flex",
			justifyContent: "space-between",
			alignItems: "center"
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, { variant: "caption" }, "512px"), /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, { variant: "caption" }, "2048px")), /*#__PURE__*/ react.default.createElement(_elementor_ui.Slider, {
			marks: true,
			step: 64,
			min: 512,
			max: 2048,
			id: "upscale_to",
			color: "secondary",
			name: "upscale_to",
			disabled: isLoading,
			defaultValue: initialEditImageWidth,
			valueLabelDisplay: "auto",
			"aria-label": (0, _wordpress_i18n.__)("Upscale to", "elementor"),
			onChange: function onChange(_, value) {
				return updateSettings(_defineProperty$1({}, IMAGE_UPSCALE, value));
			}
		}), /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, {
			display: "flex",
			justifyContent: "space-between",
			alignItems: "center"
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, { variant: "caption" }, (0, _wordpress_i18n.__)("Current", "elementor")), /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, { variant: "caption" }, (0, _wordpress_i18n.__)("Output", "elementor")))), /*#__PURE__*/ react.default.createElement(GenerateSubmit, { disabled: isLoading }))), /*#__PURE__*/ react.default.createElement(View.Content, { isGenerating: isLoading }, data !== null && data !== void 0 && data.result ? /*#__PURE__*/ react.default.createElement(ImagesDisplay, {
			onUseImage: use,
			onEditImage: edit,
			images: data.result,
			aspectRatio: editImage.aspectRatio
		}) : /*#__PURE__*/ react.default.createElement(SingleImagePreview, null, /*#__PURE__*/ react.default.createElement(SingleImagePreview.Image, {
			src: editImage.url,
			alt: editImage.alt,
			style: {
				width: editImage.width,
				height: editImage.height
			}
		}))));
	};

//#endregion
//#region node_modules/react-sketch-canvas/dist/react-sketch-canvas.esm.js
	function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) {
		try {
			var info = gen[key](arg);
			var value = info.value;
		} catch (error) {
			reject(error);
			return;
		}
		if (info.done) resolve(value);
		else Promise.resolve(value).then(_next, _throw);
	}
	function _asyncToGenerator(fn) {
		return function() {
			var self = this;
			var args = arguments;
			return new Promise(function(resolve, reject) {
				var gen = fn.apply(self, args);
				function _next(value) {
					asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value);
				}
				function _throw(err) {
					asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err);
				}
				_next(void 0);
			});
		};
	}
	function _extends() {
		_extends = Object.assign || function(target) {
			for (var i = 1; i < arguments.length; i++) {
				var source = arguments[i];
				for (var key in source) if (Object.prototype.hasOwnProperty.call(source, key)) target[key] = source[key];
			}
			return target;
		};
		return _extends.apply(this, arguments);
	}
	var runtime = { exports: {} };
	/**
	* Copyright (c) 2014-present, Facebook, Inc.
	*
	* This source code is licensed under the MIT license found in the
	* LICENSE file in the root directory of this source tree.
	*/
	(function(module) {
		var runtime = function(exports) {
			var Op = Object.prototype;
			var hasOwn = Op.hasOwnProperty;
			var undefined$1;
			var $Symbol = typeof Symbol === "function" ? Symbol : {};
			var iteratorSymbol = $Symbol.iterator || "@@iterator";
			var asyncIteratorSymbol = $Symbol.asyncIterator || "@@asyncIterator";
			var toStringTagSymbol = $Symbol.toStringTag || "@@toStringTag";
			function define(obj, key, value) {
				Object.defineProperty(obj, key, {
					value,
					enumerable: true,
					configurable: true,
					writable: true
				});
				return obj[key];
			}
			try {
				define({}, "");
			} catch (err) {
				define = function(obj, key, value) {
					return obj[key] = value;
				};
			}
			function wrap(innerFn, outerFn, self, tryLocsList) {
				var protoGenerator = outerFn && outerFn.prototype instanceof Generator ? outerFn : Generator;
				var generator = Object.create(protoGenerator.prototype);
				generator._invoke = makeInvokeMethod(innerFn, self, new Context(tryLocsList || []));
				return generator;
			}
			exports.wrap = wrap;
			function tryCatch(fn, obj, arg) {
				try {
					return {
						type: "normal",
						arg: fn.call(obj, arg)
					};
				} catch (err) {
					return {
						type: "throw",
						arg: err
					};
				}
			}
			var GenStateSuspendedStart = "suspendedStart";
			var GenStateSuspendedYield = "suspendedYield";
			var GenStateExecuting = "executing";
			var GenStateCompleted = "completed";
			var ContinueSentinel = {};
			function Generator() {}
			function GeneratorFunction() {}
			function GeneratorFunctionPrototype() {}
			var IteratorPrototype = {};
			define(IteratorPrototype, iteratorSymbol, function() {
				return this;
			});
			var getProto = Object.getPrototypeOf;
			var NativeIteratorPrototype = getProto && getProto(getProto(values([])));
			if (NativeIteratorPrototype && NativeIteratorPrototype !== Op && hasOwn.call(NativeIteratorPrototype, iteratorSymbol)) IteratorPrototype = NativeIteratorPrototype;
			var Gp = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(IteratorPrototype);
			GeneratorFunction.prototype = GeneratorFunctionPrototype;
			define(Gp, "constructor", GeneratorFunctionPrototype);
			define(GeneratorFunctionPrototype, "constructor", GeneratorFunction);
			GeneratorFunction.displayName = define(GeneratorFunctionPrototype, toStringTagSymbol, "GeneratorFunction");
			function defineIteratorMethods(prototype) {
				[
					"next",
					"throw",
					"return"
				].forEach(function(method) {
					define(prototype, method, function(arg) {
						return this._invoke(method, arg);
					});
				});
			}
			exports.isGeneratorFunction = function(genFun) {
				var ctor = typeof genFun === "function" && genFun.constructor;
				return ctor ? ctor === GeneratorFunction || (ctor.displayName || ctor.name) === "GeneratorFunction" : false;
			};
			exports.mark = function(genFun) {
				if (Object.setPrototypeOf) Object.setPrototypeOf(genFun, GeneratorFunctionPrototype);
				else {
					genFun.__proto__ = GeneratorFunctionPrototype;
					define(genFun, toStringTagSymbol, "GeneratorFunction");
				}
				genFun.prototype = Object.create(Gp);
				return genFun;
			};
			exports.awrap = function(arg) {
				return { __await: arg };
			};
			function AsyncIterator(generator, PromiseImpl) {
				function invoke(method, arg, resolve, reject) {
					var record = tryCatch(generator[method], generator, arg);
					if (record.type === "throw") reject(record.arg);
					else {
						var result = record.arg;
						var value = result.value;
						if (value && typeof value === "object" && hasOwn.call(value, "__await")) return PromiseImpl.resolve(value.__await).then(function(value) {
							invoke("next", value, resolve, reject);
						}, function(err) {
							invoke("throw", err, resolve, reject);
						});
						return PromiseImpl.resolve(value).then(function(unwrapped) {
							result.value = unwrapped;
							resolve(result);
						}, function(error) {
							return invoke("throw", error, resolve, reject);
						});
					}
				}
				var previousPromise;
				function enqueue(method, arg) {
					function callInvokeWithMethodAndArg() {
						return new PromiseImpl(function(resolve, reject) {
							invoke(method, arg, resolve, reject);
						});
					}
					return previousPromise = previousPromise ? previousPromise.then(callInvokeWithMethodAndArg, callInvokeWithMethodAndArg) : callInvokeWithMethodAndArg();
				}
				this._invoke = enqueue;
			}
			defineIteratorMethods(AsyncIterator.prototype);
			define(AsyncIterator.prototype, asyncIteratorSymbol, function() {
				return this;
			});
			exports.AsyncIterator = AsyncIterator;
			exports.async = function(innerFn, outerFn, self, tryLocsList, PromiseImpl) {
				if (PromiseImpl === void 0) PromiseImpl = Promise;
				var iter = new AsyncIterator(wrap(innerFn, outerFn, self, tryLocsList), PromiseImpl);
				return exports.isGeneratorFunction(outerFn) ? iter : iter.next().then(function(result) {
					return result.done ? result.value : iter.next();
				});
			};
			function makeInvokeMethod(innerFn, self, context) {
				var state = GenStateSuspendedStart;
				return function invoke(method, arg) {
					if (state === GenStateExecuting) throw new Error("Generator is already running");
					if (state === GenStateCompleted) {
						if (method === "throw") throw arg;
						return doneResult();
					}
					context.method = method;
					context.arg = arg;
					while (true) {
						var delegate = context.delegate;
						if (delegate) {
							var delegateResult = maybeInvokeDelegate(delegate, context);
							if (delegateResult) {
								if (delegateResult === ContinueSentinel) continue;
								return delegateResult;
							}
						}
						if (context.method === "next") context.sent = context._sent = context.arg;
						else if (context.method === "throw") {
							if (state === GenStateSuspendedStart) {
								state = GenStateCompleted;
								throw context.arg;
							}
							context.dispatchException(context.arg);
						} else if (context.method === "return") context.abrupt("return", context.arg);
						state = GenStateExecuting;
						var record = tryCatch(innerFn, self, context);
						if (record.type === "normal") {
							state = context.done ? GenStateCompleted : GenStateSuspendedYield;
							if (record.arg === ContinueSentinel) continue;
							return {
								value: record.arg,
								done: context.done
							};
						} else if (record.type === "throw") {
							state = GenStateCompleted;
							context.method = "throw";
							context.arg = record.arg;
						}
					}
				};
			}
			function maybeInvokeDelegate(delegate, context) {
				var method = delegate.iterator[context.method];
				if (method === undefined$1) {
					context.delegate = null;
					if (context.method === "throw") {
						if (delegate.iterator["return"]) {
							context.method = "return";
							context.arg = undefined$1;
							maybeInvokeDelegate(delegate, context);
							if (context.method === "throw") return ContinueSentinel;
						}
						context.method = "throw";
						context.arg = /* @__PURE__ */ new TypeError("The iterator does not provide a 'throw' method");
					}
					return ContinueSentinel;
				}
				var record = tryCatch(method, delegate.iterator, context.arg);
				if (record.type === "throw") {
					context.method = "throw";
					context.arg = record.arg;
					context.delegate = null;
					return ContinueSentinel;
				}
				var info = record.arg;
				if (!info) {
					context.method = "throw";
					context.arg = /* @__PURE__ */ new TypeError("iterator result is not an object");
					context.delegate = null;
					return ContinueSentinel;
				}
				if (info.done) {
					context[delegate.resultName] = info.value;
					context.next = delegate.nextLoc;
					if (context.method !== "return") {
						context.method = "next";
						context.arg = undefined$1;
					}
				} else return info;
				context.delegate = null;
				return ContinueSentinel;
			}
			defineIteratorMethods(Gp);
			define(Gp, toStringTagSymbol, "Generator");
			define(Gp, iteratorSymbol, function() {
				return this;
			});
			define(Gp, "toString", function() {
				return "[object Generator]";
			});
			function pushTryEntry(locs) {
				var entry = { tryLoc: locs[0] };
				if (1 in locs) entry.catchLoc = locs[1];
				if (2 in locs) {
					entry.finallyLoc = locs[2];
					entry.afterLoc = locs[3];
				}
				this.tryEntries.push(entry);
			}
			function resetTryEntry(entry) {
				var record = entry.completion || {};
				record.type = "normal";
				delete record.arg;
				entry.completion = record;
			}
			function Context(tryLocsList) {
				this.tryEntries = [{ tryLoc: "root" }];
				tryLocsList.forEach(pushTryEntry, this);
				this.reset(true);
			}
			exports.keys = function(object) {
				var keys = [];
				for (var key in object) keys.push(key);
				keys.reverse();
				return function next() {
					while (keys.length) {
						var key = keys.pop();
						if (key in object) {
							next.value = key;
							next.done = false;
							return next;
						}
					}
					next.done = true;
					return next;
				};
			};
			function values(iterable) {
				if (iterable) {
					var iteratorMethod = iterable[iteratorSymbol];
					if (iteratorMethod) return iteratorMethod.call(iterable);
					if (typeof iterable.next === "function") return iterable;
					if (!isNaN(iterable.length)) {
						var i = -1;
						var next = function next() {
							while (++i < iterable.length) if (hasOwn.call(iterable, i)) {
								next.value = iterable[i];
								next.done = false;
								return next;
							}
							next.value = undefined$1;
							next.done = true;
							return next;
						};
						return next.next = next;
					}
				}
				return { next: doneResult };
			}
			exports.values = values;
			function doneResult() {
				return {
					value: undefined$1,
					done: true
				};
			}
			Context.prototype = {
				constructor: Context,
				reset: function(skipTempReset) {
					this.prev = 0;
					this.next = 0;
					this.sent = this._sent = undefined$1;
					this.done = false;
					this.delegate = null;
					this.method = "next";
					this.arg = undefined$1;
					this.tryEntries.forEach(resetTryEntry);
					if (!skipTempReset) {
						for (var name in this) if (name.charAt(0) === "t" && hasOwn.call(this, name) && !isNaN(+name.slice(1))) this[name] = undefined$1;
					}
				},
				stop: function() {
					this.done = true;
					var rootRecord = this.tryEntries[0].completion;
					if (rootRecord.type === "throw") throw rootRecord.arg;
					return this.rval;
				},
				dispatchException: function(exception) {
					if (this.done) throw exception;
					var context = this;
					function handle(loc, caught) {
						record.type = "throw";
						record.arg = exception;
						context.next = loc;
						if (caught) {
							context.method = "next";
							context.arg = undefined$1;
						}
						return !!caught;
					}
					for (var i = this.tryEntries.length - 1; i >= 0; --i) {
						var entry = this.tryEntries[i];
						var record = entry.completion;
						if (entry.tryLoc === "root") return handle("end");
						if (entry.tryLoc <= this.prev) {
							var hasCatch = hasOwn.call(entry, "catchLoc");
							var hasFinally = hasOwn.call(entry, "finallyLoc");
							if (hasCatch && hasFinally) {
								if (this.prev < entry.catchLoc) return handle(entry.catchLoc, true);
								else if (this.prev < entry.finallyLoc) return handle(entry.finallyLoc);
							} else if (hasCatch) {
								if (this.prev < entry.catchLoc) return handle(entry.catchLoc, true);
							} else if (hasFinally) {
								if (this.prev < entry.finallyLoc) return handle(entry.finallyLoc);
							} else throw new Error("try statement without catch or finally");
						}
					}
				},
				abrupt: function(type, arg) {
					for (var i = this.tryEntries.length - 1; i >= 0; --i) {
						var entry = this.tryEntries[i];
						if (entry.tryLoc <= this.prev && hasOwn.call(entry, "finallyLoc") && this.prev < entry.finallyLoc) {
							var finallyEntry = entry;
							break;
						}
					}
					if (finallyEntry && (type === "break" || type === "continue") && finallyEntry.tryLoc <= arg && arg <= finallyEntry.finallyLoc) finallyEntry = null;
					var record = finallyEntry ? finallyEntry.completion : {};
					record.type = type;
					record.arg = arg;
					if (finallyEntry) {
						this.method = "next";
						this.next = finallyEntry.finallyLoc;
						return ContinueSentinel;
					}
					return this.complete(record);
				},
				complete: function(record, afterLoc) {
					if (record.type === "throw") throw record.arg;
					if (record.type === "break" || record.type === "continue") this.next = record.arg;
					else if (record.type === "return") {
						this.rval = this.arg = record.arg;
						this.method = "return";
						this.next = "end";
					} else if (record.type === "normal" && afterLoc) this.next = afterLoc;
					return ContinueSentinel;
				},
				finish: function(finallyLoc) {
					for (var i = this.tryEntries.length - 1; i >= 0; --i) {
						var entry = this.tryEntries[i];
						if (entry.finallyLoc === finallyLoc) {
							this.complete(entry.completion, entry.afterLoc);
							resetTryEntry(entry);
							return ContinueSentinel;
						}
					}
				},
				"catch": function(tryLoc) {
					for (var i = this.tryEntries.length - 1; i >= 0; --i) {
						var entry = this.tryEntries[i];
						if (entry.tryLoc === tryLoc) {
							var record = entry.completion;
							if (record.type === "throw") {
								var thrown = record.arg;
								resetTryEntry(entry);
							}
							return thrown;
						}
					}
					throw new Error("illegal catch attempt");
				},
				delegateYield: function(iterable, resultName, nextLoc) {
					this.delegate = {
						iterator: values(iterable),
						resultName,
						nextLoc
					};
					if (this.method === "next") this.arg = undefined$1;
					return ContinueSentinel;
				}
			};
			return exports;
		}(module.exports);
		try {
			regeneratorRuntime = runtime;
		} catch (accidentalStrictMode) {
			if (typeof globalThis === "object") globalThis.regeneratorRuntime = runtime;
			else Function("r", "regeneratorRuntime = r")(runtime);
		}
	})(runtime);
	var _regeneratorRuntime$19 = runtime.exports;
	/**
	* Generate SVG Path tag from the given points
	*/
	var SvgPath = function SvgPath(_ref) {
		var paths = _ref.paths;
		var id = _ref.id;
		var strokeWidth = _ref.strokeWidth;
		var strokeColor = _ref.strokeColor;
		var _ref$command = _ref.command;
		var command = _ref$command === void 0 ? bezierCommand : _ref$command;
		if (paths.length === 1) {
			var _paths$ = paths[0];
			var x = _paths$.x;
			var y = _paths$.y;
			var radius = strokeWidth / 2;
			return react.createElement("circle", {
				key: id,
				id,
				cx: x,
				cy: y,
				r: radius,
				stroke: strokeColor,
				fill: strokeColor
			});
		}
		var d = paths.reduce(function(acc, point, i, a) {
			return i === 0 ? "M " + point.x + "," + point.y : acc + " " + command(point, i, a);
		}, "");
		return react.createElement("path", {
			key: id,
			id,
			d,
			fill: "none",
			strokeLinecap: "round",
			stroke: strokeColor,
			strokeWidth
		});
	};
	var line = function line(pointA, pointB) {
		var lengthX = pointB.x - pointA.x;
		var lengthY = pointB.y - pointA.y;
		return {
			length: Math.sqrt(Math.pow(lengthX, 2) + Math.pow(lengthY, 2)),
			angle: Math.atan2(lengthY, lengthX)
		};
	};
	var controlPoint = function controlPoint(controlPoints) {
		var current = controlPoints.current;
		var next = controlPoints.next;
		var previous = controlPoints.previous;
		var reverse = controlPoints.reverse;
		var p = previous || current;
		var n = next || current;
		var smoothing = .2;
		var o = line(p, n);
		var angle = o.angle + (reverse ? Math.PI : 0);
		var length = o.length * smoothing;
		return [current.x + Math.cos(angle) * length, current.y + Math.sin(angle) * length];
	};
	var bezierCommand = function bezierCommand(point, i, a) {
		var cpsX = null;
		var cpsY = null;
		switch (i) {
			case 0:
				var _controlPoint = controlPoint({ current: point });
				cpsX = _controlPoint[0];
				cpsY = _controlPoint[1];
				break;
			case 1:
				var _controlPoint2 = controlPoint({
					current: a[i - 1],
					next: point
				});
				cpsX = _controlPoint2[0];
				cpsY = _controlPoint2[1];
				break;
			default:
				var _controlPoint3 = controlPoint({
					current: a[i - 1],
					previous: a[i - 2],
					next: point
				});
				cpsX = _controlPoint3[0];
				cpsY = _controlPoint3[1];
				break;
		}
		var _controlPoint4 = controlPoint({
			current: point,
			previous: a[i - 1],
			next: a[i + 1],
			reverse: true
		});
		var cpeX = _controlPoint4[0];
		var cpeY = _controlPoint4[1];
		return "C " + cpsX + "," + cpsY + " " + cpeX + "," + cpeY + " " + point.x + ", " + point.y;
	};
	var Paths = function Paths(_ref2) {
		var id = _ref2.id;
		var paths = _ref2.paths;
		return react.createElement(react.Fragment, null, paths.map(function(path, index) {
			return react.createElement(SvgPath, {
				key: id + "__" + index,
				paths: path.paths,
				id: id + "__" + index,
				strokeWidth: path.strokeWidth,
				strokeColor: path.strokeColor,
				command: bezierCommand
			});
		}));
	};
	var loadImage = function loadImage(url) {
		return new Promise(function(resolve, reject) {
			var img = new Image();
			img.addEventListener("load", function() {
				if (img.width > 0) resolve(img);
				reject("Image not found");
			});
			img.addEventListener("error", function(err) {
				return reject(err);
			});
			img.src = url;
			img.setAttribute("crossorigin", "anonymous");
		});
	};
	function getCanvasWithViewBox(canvas) {
		var _canvas$firstChild;
		var svgCanvas = (_canvas$firstChild = canvas.firstChild) == null ? void 0 : _canvas$firstChild.cloneNode(true);
		var width = canvas.offsetWidth;
		var height = canvas.offsetHeight;
		svgCanvas.setAttribute("viewBox", "0 0 " + width + " " + height);
		svgCanvas.setAttribute("width", width.toString());
		svgCanvas.setAttribute("height", height.toString());
		return {
			svgCanvas,
			width,
			height
		};
	}
	var Canvas = /*#__PURE__*/ react.forwardRef(function(props, ref) {
		var paths = props.paths;
		var isDrawing = props.isDrawing;
		var onPointerDown = props.onPointerDown;
		var onPointerMove = props.onPointerMove;
		var onPointerUp = props.onPointerUp;
		var _props$id = props.id;
		var id = _props$id === void 0 ? "react-sketch-canvas" : _props$id;
		var _props$width = props.width;
		var width = _props$width === void 0 ? "100%" : _props$width;
		var _props$height = props.height;
		var height = _props$height === void 0 ? "100%" : _props$height;
		var _props$className = props.className;
		var className = _props$className === void 0 ? "react-sketch-canvas" : _props$className;
		var _props$canvasColor = props.canvasColor;
		var canvasColor = _props$canvasColor === void 0 ? "red" : _props$canvasColor;
		var _props$backgroundImag = props.backgroundImage;
		var backgroundImage = _props$backgroundImag === void 0 ? "" : _props$backgroundImag;
		var _props$exportWithBack = props.exportWithBackgroundImage;
		var exportWithBackgroundImage = _props$exportWithBack === void 0 ? false : _props$exportWithBack;
		var _props$preserveBackgr = props.preserveBackgroundImageAspectRatio;
		var preserveBackgroundImageAspectRatio = _props$preserveBackgr === void 0 ? "none" : _props$preserveBackgr;
		var _props$allowOnlyPoint = props.allowOnlyPointerType;
		var allowOnlyPointerType = _props$allowOnlyPoint === void 0 ? "all" : _props$allowOnlyPoint;
		var _props$style = props.style;
		var style = _props$style === void 0 ? {
			border: "0.0625rem solid #9c9c9c",
			borderRadius: "0.25rem"
		} : _props$style;
		var _props$svgStyle = props.svgStyle;
		var svgStyle = _props$svgStyle === void 0 ? {} : _props$svgStyle;
		var canvasRef = react.useRef(null);
		var getCoordinates = function getCoordinates(pointerEvent) {
			var _canvasRef$current;
			var _window$scrollX;
			var _window$scrollY;
			var boundingArea = (_canvasRef$current = canvasRef.current) == null ? void 0 : _canvasRef$current.getBoundingClientRect();
			var scrollLeft = (_window$scrollX = window.scrollX) != null ? _window$scrollX : 0;
			var scrollTop = (_window$scrollY = window.scrollY) != null ? _window$scrollY : 0;
			if (!boundingArea) return {
				x: 0,
				y: 0
			};
			return {
				x: pointerEvent.pageX - boundingArea.left - scrollLeft,
				y: pointerEvent.pageY - boundingArea.top - scrollTop
			};
		};
		var handlePointerDown = function handlePointerDown(event) {
			if (allowOnlyPointerType !== "all" && event.pointerType !== allowOnlyPointerType) return;
			if (event.pointerType === "mouse" && event.button !== 0) return;
			onPointerDown(getCoordinates(event));
		};
		var handlePointerMove = function handlePointerMove(event) {
			if (!isDrawing) return;
			if (allowOnlyPointerType !== "all" && event.pointerType !== allowOnlyPointerType) return;
			onPointerMove(getCoordinates(event));
		};
		var handlePointerUp = function handlePointerUp(event) {
			if (event.pointerType === "mouse" && event.button !== 0) return;
			if (allowOnlyPointerType !== "all" && event.pointerType !== allowOnlyPointerType) return;
			onPointerUp();
		};
		react.useImperativeHandle(ref, function() {
			return {
				exportImage: function exportImage(imageType) {
					return new Promise(/*#__PURE__*/ function() {
						var _ref = _asyncToGenerator(/*#__PURE__*/ _regeneratorRuntime$19.mark(function _callee(resolve, reject) {
							var canvas;
							var _getCanvasWithViewBox;
							var svgCanvas;
							var _width;
							var _height;
							var canvasSketch;
							var loadImagePromises;
							var img;
							return _regeneratorRuntime$19.wrap(function _callee$(_context) {
								while (1) switch (_context.prev = _context.next) {
									case 0:
										_context.prev = 0;
										canvas = canvasRef.current;
										if (canvas) {
											_context.next = 4;
											break;
										}
										throw Error("Canvas not rendered yet");
									case 4:
										_getCanvasWithViewBox = getCanvasWithViewBox(canvas), svgCanvas = _getCanvasWithViewBox.svgCanvas, _width = _getCanvasWithViewBox.width, _height = _getCanvasWithViewBox.height;
										canvasSketch = "data:image/svg+xml;base64," + btoa(svgCanvas.outerHTML);
										_context.next = 8;
										return loadImage(canvasSketch);
									case 8:
										_context.t0 = _context.sent;
										loadImagePromises = [_context.t0];
										if (!exportWithBackgroundImage) {
											_context.next = 21;
											break;
										}
										_context.prev = 11;
										_context.next = 14;
										return loadImage(backgroundImage);
									case 14:
										img = _context.sent;
										loadImagePromises.push(img);
										_context.next = 21;
										break;
									case 18:
										_context.prev = 18;
										_context.t1 = _context["catch"](11);
										console.warn("exportWithBackgroundImage props is set without a valid background image URL. This option is ignored");
									case 21:
										Promise.all(loadImagePromises).then(function(images) {
											var renderCanvas = document.createElement("canvas");
											renderCanvas.setAttribute("width", _width.toString());
											renderCanvas.setAttribute("height", _height.toString());
											var context = renderCanvas.getContext("2d");
											if (!context) throw Error("Canvas not rendered yet");
											images.reverse().forEach(function(image) {
												context.drawImage(image, 0, 0);
											});
											resolve(renderCanvas.toDataURL("image/" + imageType));
										})["catch"](function(e) {
											throw e;
										});
										_context.next = 27;
										break;
									case 24:
										_context.prev = 24;
										_context.t2 = _context["catch"](0);
										reject(_context.t2);
									case 27:
									case "end": return _context.stop();
								}
							}, _callee, null, [[0, 24], [11, 18]]);
						}));
						return function(_x, _x2) {
							return _ref.apply(this, arguments);
						};
					}());
				},
				exportSvg: function exportSvg() {
					return new Promise(function(resolve, reject) {
						try {
							var _canvasRef$current2;
							var canvas = (_canvasRef$current2 = canvasRef.current) != null ? _canvasRef$current2 : null;
							if (canvas !== null) {
								var _svgCanvas$querySelec;
								var _svgCanvas$querySelec2;
								var svgCanvas = getCanvasWithViewBox(canvas).svgCanvas;
								if (exportWithBackgroundImage) {
									resolve(svgCanvas.outerHTML);
									return;
								}
								(_svgCanvas$querySelec = svgCanvas.querySelector("#" + id + "__background")) == null || _svgCanvas$querySelec.remove();
								(_svgCanvas$querySelec2 = svgCanvas.querySelector("#" + id + "__canvas-background")) == null || _svgCanvas$querySelec2.setAttribute("fill", canvasColor);
								resolve(svgCanvas.outerHTML);
							}
							reject(/* @__PURE__ */ new Error("Canvas not loaded"));
						} catch (e) {
							reject(e);
						}
					});
				}
			};
		});
		react.useEffect(function() {
			document.addEventListener("pointerup", handlePointerUp);
			return function() {
				document.removeEventListener("pointerup", handlePointerUp);
			};
		}, [handlePointerUp]);
		var eraserPaths = paths.filter(function(path) {
			return !path.drawMode;
		});
		var currentGroup = 0;
		var pathGroups = paths.reduce(function(arrayGroup, path) {
			if (!path.drawMode) {
				currentGroup += 1;
				return arrayGroup;
			}
			if (arrayGroup[currentGroup] === void 0) arrayGroup[currentGroup] = [];
			arrayGroup[currentGroup].push(path);
			return arrayGroup;
		}, [[]]);
		return react.createElement("div", {
			role: "presentation",
			ref: canvasRef,
			className,
			style: _extends({
				touchAction: "none",
				width,
				height
			}, style),
			"touch-action": "none",
			onPointerDown: handlePointerDown,
			onPointerMove: handlePointerMove,
			onPointerUp: handlePointerUp
		}, react.createElement("svg", {
			version: "1.1",
			baseProfile: "full",
			xmlns: "http://www.w3.org/2000/svg",
			xmlnsXlink: "http://www.w3.org/1999/xlink",
			style: _extends({
				width: "100%",
				height: "100%"
			}, svgStyle),
			id
		}, react.createElement("g", {
			id: id + "__eraser-stroke-group",
			display: "none"
		}, react.createElement("rect", {
			id: id + "__mask-background",
			x: "0",
			y: "0",
			width: "100%",
			height: "100%",
			fill: "white"
		}), eraserPaths.map(function(eraserPath, i) {
			return react.createElement(SvgPath, {
				key: id + "__eraser-" + i,
				id: id + "__eraser-" + i,
				paths: eraserPath.paths,
				strokeColor: "#000000",
				strokeWidth: eraserPath.strokeWidth
			});
		})), react.createElement("defs", null, backgroundImage && react.createElement("pattern", {
			id: id + "__background",
			x: "0",
			y: "0",
			width: "100%",
			height: "100%",
			patternUnits: "userSpaceOnUse"
		}, react.createElement("image", {
			x: "0",
			y: "0",
			width: "100%",
			height: "100%",
			xlinkHref: backgroundImage,
			preserveAspectRatio: preserveBackgroundImageAspectRatio
		})), eraserPaths.map(function(_, i) {
			return react.createElement("mask", {
				id: id + "__eraser-mask-" + i,
				key: id + "__eraser-mask-" + i,
				maskUnits: "userSpaceOnUse"
			}, react.createElement("use", { href: "#" + id + "__mask-background" }), Array.from({ length: eraserPaths.length - i }, function(_, j) {
				return j + i;
			}).map(function(k) {
				return react.createElement("use", {
					key: k.toString(),
					href: "#" + id + "__eraser-" + k.toString()
				});
			}));
		})), react.createElement("g", { id: id + "__canvas-background-group" }, react.createElement("rect", {
			id: id + "__canvas-background",
			x: "0",
			y: "0",
			width: "100%",
			height: "100%",
			fill: backgroundImage ? "url(#" + id + "__background)" : canvasColor
		})), pathGroups.map(function(pathGroup, i) {
			return react.createElement("g", {
				id: id + "__stroke-group-" + i,
				key: id + "__stroke-group-" + i,
				mask: "url(#" + id + "__eraser-mask-" + i + ")"
			}, react.createElement(Paths, {
				id,
				paths: pathGroup
			}));
		})));
	});
	var ReactSketchCanvas = /*#__PURE__*/ react.forwardRef(function(props, ref) {
		var _props$id = props.id;
		var id = _props$id === void 0 ? "react-sketch-canvas" : _props$id;
		var _props$width = props.width;
		var width = _props$width === void 0 ? "100%" : _props$width;
		var _props$height = props.height;
		var height = _props$height === void 0 ? "100%" : _props$height;
		var _props$className = props.className;
		var className = _props$className === void 0 ? "" : _props$className;
		var _props$canvasColor = props.canvasColor;
		var canvasColor = _props$canvasColor === void 0 ? "white" : _props$canvasColor;
		var _props$strokeColor = props.strokeColor;
		var strokeColor = _props$strokeColor === void 0 ? "red" : _props$strokeColor;
		var _props$backgroundImag = props.backgroundImage;
		var backgroundImage = _props$backgroundImag === void 0 ? "" : _props$backgroundImag;
		var _props$exportWithBack = props.exportWithBackgroundImage;
		var exportWithBackgroundImage = _props$exportWithBack === void 0 ? false : _props$exportWithBack;
		var _props$preserveBackgr = props.preserveBackgroundImageAspectRatio;
		var preserveBackgroundImageAspectRatio = _props$preserveBackgr === void 0 ? "none" : _props$preserveBackgr;
		var _props$strokeWidth = props.strokeWidth;
		var strokeWidth = _props$strokeWidth === void 0 ? 4 : _props$strokeWidth;
		var _props$eraserWidth = props.eraserWidth;
		var eraserWidth = _props$eraserWidth === void 0 ? 8 : _props$eraserWidth;
		var _props$allowOnlyPoint = props.allowOnlyPointerType;
		var allowOnlyPointerType = _props$allowOnlyPoint === void 0 ? "all" : _props$allowOnlyPoint;
		var _props$style = props.style;
		var style = _props$style === void 0 ? {
			border: "0.0625rem solid #9c9c9c",
			borderRadius: "0.25rem"
		} : _props$style;
		var _props$svgStyle = props.svgStyle;
		var svgStyle = _props$svgStyle === void 0 ? {} : _props$svgStyle;
		var _props$onChange = props.onChange;
		var onChange = _props$onChange === void 0 ? function(_paths) {} : _props$onChange;
		var _props$onStroke = props.onStroke;
		var onStroke = _props$onStroke === void 0 ? function(_path, _isEraser) {} : _props$onStroke;
		var _props$withTimestamp = props.withTimestamp;
		var withTimestamp = _props$withTimestamp === void 0 ? false : _props$withTimestamp;
		var svgCanvas = react.createRef();
		var _React$useState = react.useState(true);
		var drawMode = _React$useState[0];
		var setDrawMode = _React$useState[1];
		var _React$useState2 = react.useState(false);
		var isDrawing = _React$useState2[0];
		var setIsDrawing = _React$useState2[1];
		var _React$useState3 = react.useState([]);
		var resetStack = _React$useState3[0];
		var setResetStack = _React$useState3[1];
		var _React$useState4 = react.useState([]);
		var undoStack = _React$useState4[0];
		var setUndoStack = _React$useState4[1];
		var _React$useState5 = react.useState([]);
		var currentPaths = _React$useState5[0];
		var setCurrentPaths = _React$useState5[1];
		var liftStrokeUp = react.useCallback(function() {
			var _currentPaths$slice$;
			var _currentPaths$slice;
			var lastStroke = (_currentPaths$slice$ = (_currentPaths$slice = currentPaths.slice(-1)) == null ? void 0 : _currentPaths$slice[0]) != null ? _currentPaths$slice$ : null;
			if (lastStroke === null) {
				console.warn("No stroke found!");
				return;
			}
			onStroke(lastStroke, !lastStroke.drawMode);
		}, [isDrawing]);
		react.useEffect(function() {
			liftStrokeUp();
		}, [isDrawing]);
		react.useEffect(function() {
			onChange(currentPaths);
		}, [currentPaths]);
		react.useImperativeHandle(ref, function() {
			return {
				eraseMode: function eraseMode(erase) {
					setDrawMode(!erase);
				},
				clearCanvas: function clearCanvas() {
					setResetStack([].concat(currentPaths));
					setCurrentPaths([]);
				},
				undo: function undo() {
					if (resetStack.length !== 0) {
						setCurrentPaths([].concat(resetStack));
						setResetStack([]);
						return;
					}
					setUndoStack(function(undoStack) {
						return [].concat(undoStack, currentPaths.slice(-1));
					});
					setCurrentPaths(function(currentPaths) {
						return currentPaths.slice(0, -1);
					});
				},
				redo: function redo() {
					if (undoStack.length === 0) return;
					setCurrentPaths(function(currentPaths) {
						return [].concat(currentPaths, undoStack.slice(-1));
					});
					setUndoStack(function(undoStack) {
						return undoStack.slice(0, -1);
					});
				},
				exportImage: function exportImage(imageType) {
					var _svgCanvas$current;
					var exportImage = (_svgCanvas$current = svgCanvas.current) == null ? void 0 : _svgCanvas$current.exportImage;
					if (!exportImage) throw Error("Export function called before canvas loaded");
					else return exportImage(imageType);
				},
				exportSvg: function exportSvg() {
					return new Promise(function(resolve, reject) {
						var _svgCanvas$current2;
						var exportSvg = (_svgCanvas$current2 = svgCanvas.current) == null ? void 0 : _svgCanvas$current2.exportSvg;
						if (!exportSvg) reject(Error("Export function called before canvas loaded"));
						else exportSvg().then(function(data) {
							resolve(data);
						})["catch"](function(e) {
							reject(e);
						});
					});
				},
				exportPaths: function exportPaths() {
					return new Promise(function(resolve, reject) {
						try {
							resolve(currentPaths);
						} catch (e) {
							reject(e);
						}
					});
				},
				loadPaths: function loadPaths(paths) {
					setCurrentPaths(function(currentPaths) {
						return [].concat(currentPaths, paths);
					});
				},
				getSketchingTime: function getSketchingTime() {
					return new Promise(function(resolve, reject) {
						if (!withTimestamp) reject(/* @__PURE__ */ new Error("Set 'withTimestamp' prop to get sketching time"));
						try {
							resolve(currentPaths.reduce(function(totalSketchingTime, path) {
								var _path$startTimestamp;
								var _path$endTimestamp;
								var startTimestamp = (_path$startTimestamp = path.startTimestamp) != null ? _path$startTimestamp : 0;
								return totalSketchingTime + (((_path$endTimestamp = path.endTimestamp) != null ? _path$endTimestamp : 0) - startTimestamp);
							}, 0));
						} catch (e) {
							reject(e);
						}
					});
				},
				resetCanvas: function resetCanvas() {
					setResetStack([]);
					setUndoStack([]);
					setCurrentPaths([]);
				}
			};
		});
		return react.createElement(Canvas, {
			ref: svgCanvas,
			id,
			width,
			height,
			className,
			canvasColor,
			backgroundImage,
			exportWithBackgroundImage,
			preserveBackgroundImageAspectRatio,
			allowOnlyPointerType,
			style,
			svgStyle,
			paths: currentPaths,
			isDrawing,
			onPointerDown: function handlePointerDown(point) {
				setIsDrawing(true);
				setUndoStack([]);
				var stroke = {
					drawMode,
					strokeColor: drawMode ? strokeColor : "#000000",
					strokeWidth: drawMode ? strokeWidth : eraserWidth,
					paths: [point]
				};
				if (withTimestamp) stroke = _extends({}, stroke, {
					startTimestamp: Date.now(),
					endTimestamp: 0
				});
				setCurrentPaths(function(currentPaths) {
					return [].concat(currentPaths, [stroke]);
				});
			},
			onPointerMove: function handlePointerMove(point) {
				if (!isDrawing) return;
				var currentStroke = currentPaths.slice(-1)[0];
				var updatedStroke = _extends({}, currentStroke, { paths: [].concat(currentStroke.paths, [point]) });
				setCurrentPaths(function(currentPaths) {
					return [].concat(currentPaths.slice(0, -1), [updatedStroke]);
				});
			},
			onPointerUp: function handlePointerUp() {
				var _currentPaths$slice$2;
				var _currentPaths$slice2;
				if (!isDrawing) return;
				setIsDrawing(false);
				if (!withTimestamp) return;
				var currentStroke = (_currentPaths$slice$2 = (_currentPaths$slice2 = currentPaths.slice(-1)) == null ? void 0 : _currentPaths$slice2[0]) != null ? _currentPaths$slice$2 : null;
				if (currentStroke === null) return;
				var updatedStroke = _extends({}, currentStroke, { endTimestamp: Date.now() });
				setCurrentPaths(function(currentPaths) {
					return [].concat(currentPaths.slice(0, -1), [updatedStroke]);
				});
			}
		});
	});

//#endregion
//#region modules/ai/assets/js/editor/icons/undo-icon.js
	init_extends();
	var UndoIcon = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 24 24" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", { d: "M9.53033 6.53033C9.82322 6.23744 9.82322 5.76256 9.53033 5.46967C9.23744 5.17678 8.76256 5.17678 8.46967 5.46967L4.46967 9.46967C4.39776 9.54158 4.34351 9.62445 4.30691 9.71291C4.27024 9.80134 4.25 9.89831 4.25 10C4.25 10.1017 4.27024 10.1987 4.30691 10.2871C4.34351 10.3755 4.39776 10.4584 4.46967 10.5303L8.46967 14.5303C8.76256 14.8232 9.23744 14.8232 9.53033 14.5303C9.82322 14.2374 9.82322 13.7626 9.53033 13.4697L6.81066 10.75H16C16.862 10.75 17.6886 11.0924 18.2981 11.7019C18.9076 12.3114 19.25 13.138 19.25 14C19.25 14.862 18.9076 15.6886 18.2981 16.2981C17.6886 16.9076 16.862 17.25 16 17.25H15C14.5858 17.25 14.25 17.5858 14.25 18C14.25 18.4142 14.5858 18.75 15 18.75H16C17.2598 18.75 18.468 18.2496 19.3588 17.3588C20.2496 16.468 20.75 15.2598 20.75 14C20.75 12.7402 20.2496 11.532 19.3588 10.6412C18.468 9.75044 17.2598 9.25 16 9.25H6.81066L9.53033 6.53033Z" }));
	});

//#endregion
//#region modules/ai/assets/js/editor/icons/redo-icon.js
	init_extends();
	var RedoIcon = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 24 24" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", { d: "M15.5303 5.46967C15.2374 5.17678 14.7626 5.17678 14.4697 5.46967C14.1768 5.76256 14.1768 6.23744 14.4697 6.53033L17.1893 9.25H8C6.74022 9.25 5.53204 9.75044 4.64124 10.6412C3.75044 11.532 3.25 12.7402 3.25 14C3.25 15.2598 3.75044 16.468 4.64124 17.3588C5.53204 18.2496 6.74022 18.75 8 18.75H9C9.41421 18.75 9.75 18.4142 9.75 18C9.75 17.5858 9.41421 17.25 9 17.25H8C7.13805 17.25 6.3114 16.9076 5.7019 16.2981C5.09241 15.6886 4.75 14.862 4.75 14C4.75 13.138 5.09241 12.3114 5.7019 11.7019C6.3114 11.0924 7.13805 10.75 8 10.75H17.1893L14.4697 13.4697C14.1768 13.7626 14.1768 14.2374 14.4697 14.5303C14.7626 14.8232 15.2374 14.8232 15.5303 14.5303L19.5303 10.5303C19.6768 10.3839 19.75 10.1919 19.75 10C19.75 9.89831 19.7298 9.80134 19.6931 9.71291C19.6565 9.62445 19.6022 9.54158 19.5303 9.46967L15.5303 5.46967Z" }));
	});

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/in-painting/in-painting-content.js
	init_asyncToGenerator();
	init_slicedToArray();
	var STROKE_SELECT_WIDTH$1 = 120;
	var BRUSH_COLOR$1 = "rgba(255, 255, 255)";
	var CANVAS_COLOR$1 = "rgba(0, 0, 0)";
	var StyledUndoIcon$1 = (0, _elementor_ui.withDirection)(UndoIcon);
	var StyledRedoIcon$1 = (0, _elementor_ui.withDirection)(RedoIcon);
	var BrushCursor$1 = (0, _elementor_ui.styled)(_elementor_ui.Box, { shouldForwardProp: function shouldForwardProp(prop) {
		return "size" === prop;
	} })(function(_ref) {
		var theme = _ref.theme;
		var size = _ref.size;
		return {
			position: "absolute",
			width: size,
			height: size,
			borderRadius: "50%",
			backgroundColor: BRUSH_COLOR$1,
			pointerEvents: "none",
			transform: "translate(".concat("rtl" === theme.direction ? "50%" : "-50%", ", -50%)"),
			border: "1px solid #fff"
		};
	});
	var BrishSizeIcon$1 = (0, _elementor_ui.styled)(_elementor_ui.Box, { shouldForwardProp: function shouldForwardProp(prop) {
		return "size" === prop;
	} })(function(_ref2) {
		var theme = _ref2.theme;
		var size = _ref2.size;
		return {
			width: size / 2,
			height: size / 2,
			borderRadius: "50%",
			backgroundColor: theme.palette.secondary.main
		};
	});
	var InPaintingContent = function InPaintingContent(_ref3) {
		var editImage = _ref3.editImage;
		var setMask = _ref3.setMask;
		var setIsCanvasChanged = _ref3.setIsCanvasChanged;
		var canvasWidth = _ref3.width;
		var canvasHeight = _ref3.height;
		var sketchRef = (0, react.useRef)();
		var _useState2 = _slicedToArray((0, react.useState)(30), 2);
		var stroke = _useState2[0];
		var setStroke = _useState2[1];
		var brushCursorRef = (0, react.useRef)();
		(0, react.useEffect)(function() {
			var canvas = document.querySelector(".eui-in-painting-canvas");
			var positionElement = function positionElement(e) {
				var _canvas$getBoundingCl = canvas.getBoundingClientRect();
				var left = _canvas$getBoundingCl.left;
				var top = _canvas$getBoundingCl.top;
				var width = _canvas$getBoundingCl.width;
				var height = _canvas$getBoundingCl.height;
				var x = e.clientX - left;
				var y = e.clientY - top;
				if (x > 0 && x < width && y > 0 && y < height) {
					brushCursorRef.current.style.left = "".concat(x, "px");
					brushCursorRef.current.style.top = "".concat(y, "px");
				}
			};
			window.addEventListener("mousemove", positionElement);
			return function() {
				window.removeEventListener("mousemove", positionElement);
			};
		}, [stroke]);
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			alignItems: "flex-start",
			spacing: .5,
			flexGrow: 1
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			width: "100%",
			direction: "row",
			spacing: 3,
			alignSelf: "center",
			justifyContent: "center",
			sx: { marginBottom: "20px !important" }
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			direction: "row",
			gap: 1
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Tooltip, { title: (0, _wordpress_i18n.__)("Undo", "elementor") }, /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, {
			variant: "outlined",
			color: "secondary",
			onClick: function onClick() {
				return sketchRef.current.undo();
			},
			sx: { px: 0 }
		}, /*#__PURE__*/ react.default.createElement(StyledUndoIcon$1, null))), /*#__PURE__*/ react.default.createElement(_elementor_ui.Tooltip, { title: (0, _wordpress_i18n.__)("Redo", "elementor") }, /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, {
			variant: "outlined",
			color: "secondary",
			onClick: function onClick() {
				return sketchRef.current.redo();
			},
			sx: { px: 0 }
		}, /*#__PURE__*/ react.default.createElement(StyledRedoIcon$1, null)))), /*#__PURE__*/ react.default.createElement(_elementor_ui.FormControl, {
			size: "small",
			color: "secondary",
			sx: { minWidth: STROKE_SELECT_WIDTH$1 }
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.InputLabel, { id: "stroke" }, "Stroke"), /*#__PURE__*/ react.default.createElement(_elementor_ui.Select, {
			autoWidth: true,
			label: "Stroke",
			value: stroke,
			id: "demo-simple-select",
			labelId: "demo-simple-select-label",
			onChange: function onChange(e) {
				return setStroke(e.target.value);
			},
			MenuProps: {
				PaperProps: { sx: { maxWidth: STROKE_SELECT_WIDTH$1 } },
				MenuListProps: { sx: { minWidth: STROKE_SELECT_WIDTH$1 } }
			},
			sx: { "& .MuiSelect-select .MuiListItemIcon-root": {
				mr: .25,
				width: "initial",
				minWidth: "initial",
				justifyContent: "flex-start"
			} }
		}, [
			10,
			20,
			30,
			40,
			50
		].map(function(value) {
			return /*#__PURE__*/ react.default.createElement(_elementor_ui.MenuItem, {
				key: "stroke-width-option-" + value,
				value
			}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
				direction: "row",
				alignItems: "center",
				gap: 1
			}, /*#__PURE__*/ react.default.createElement(_elementor_ui.ListItemIcon, { sx: {
				width: 30,
				display: "flex",
				justifyContent: "center"
			} }, /*#__PURE__*/ react.default.createElement(BrishSizeIcon$1, { size: value })), /*#__PURE__*/ react.default.createElement(_elementor_ui.ListItemText, null, value)));
		})))), /*#__PURE__*/ react.default.createElement("div", { style: {
			margin: "0 auto",
			position: "relative",
			cursor: "none",
			overflow: "hidden"
		} }, /*#__PURE__*/ react.default.createElement(BrushCursor$1, {
			ref: brushCursorRef,
			size: stroke
		}), /*#__PURE__*/ react.default.createElement(ReactSketchCanvas, {
			className: "eui-in-painting-canvas",
			withViewBox: true,
			ref: sketchRef,
			height: canvasHeight + "px",
			width: canvasWidth + "px",
			strokeWidth: stroke,
			strokeColor: BRUSH_COLOR$1,
			canvasColor: CANVAS_COLOR$1,
			backgroundImage: editImage.url,
			onStroke: function onStroke() {
				return setIsCanvasChanged(true);
			},
			onChange: /*#__PURE__*/ _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee() {
				var svg;
				return import_regenerator$2.default.wrap(function(_context) {
					while (1) switch (_context.prev = _context.next) {
						case 0:
							_context.next = 1;
							return sketchRef.current.exportSvg();
						case 1:
							svg = _context.sent;
							setMask(svg);
						case 2:
						case "end": return _context.stop();
					}
				}, _callee);
			}))
		})));
	};
	InPaintingContent.propTypes = {
		setMask: import_prop_types$7.default.func.isRequired,
		setIsCanvasChanged: import_prop_types$7.default.func.isRequired,
		width: import_prop_types$7.default.number.isRequired,
		height: import_prop_types$7.default.number.isRequired,
		editImage: import_prop_types$7.default.object.isRequired
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/in-painting/hooks/use-in-painting.js
	init_api();
	var useInPainting = function useInPainting(initialValue) {
		return useImagePrompt(function fetchAction(payload) {
			return getImageToImageMaskGeneration(payload);
		}, initialValue);
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/in-painting/index.js
	init_asyncToGenerator();
	init_slicedToArray();
	init_requests_ids();
	var InPainting = function InPainting() {
		var _useState2 = _slicedToArray((0, react.useState)(""), 2);
		var prompt = _useState2[0];
		var setPrompt = _useState2[1];
		var setGenerate = useRequestIds().setGenerate;
		var _useState4 = _slicedToArray((0, react.useState)(""), 2);
		var mask = _useState4[0];
		var setMask = _useState4[1];
		var _useState6 = _slicedToArray((0, react.useState)(false), 2);
		var isCanvasChanged = _useState6[0];
		var setIsCanvasChanged = _useState6[1];
		var _usePromptSettings = usePromptSettings();
		var settings = _usePromptSettings.settings;
		var resetSettings = _usePromptSettings.resetSettings;
		var _useEditImage = useEditImage();
		var editImage = _useEditImage.editImage;
		var width = _useEditImage.width;
		var height = _useEditImage.height;
		var _useImageActions = useImageActions();
		var use = _useImageActions.use;
		var edit = _useImageActions.edit;
		var isUploading = _useImageActions.isLoading;
		var _useInPainting = useInPainting();
		var data = _useInPainting.data;
		var send = _useInPainting.send;
		var isGenerating = _useInPainting.isLoading;
		var error = _useInPainting.error;
		var reset = _useInPainting.reset;
		var isLoading = isGenerating || isUploading;
		var handleSubmit = /*#__PURE__*/ function() {
			var _ref = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee(event) {
				var finalPrompt;
				return import_regenerator$2.default.wrap(function(_context) {
					while (1) switch (_context.prev = _context.next) {
						case 0:
							event.preventDefault();
							finalPrompt = prompt || "Remove object and fill based on the surroundings";
							setGenerate();
							send({
								prompt: finalPrompt,
								settings,
								image: editImage,
								mask
							});
						case 1:
						case "end": return _context.stop();
					}
				}, _callee);
			}));
			return function handleSubmit(_x) {
				return _ref.apply(this, arguments);
			};
		}();
		return /*#__PURE__*/ react.default.createElement(View, null, /*#__PURE__*/ react.default.createElement(View.Panel, null, /*#__PURE__*/ react.default.createElement(View.BackButton, null), /*#__PURE__*/ react.default.createElement(View.PanelHeading, {
			primary: (0, _wordpress_i18n.__)("Generative Fill", "elementor"),
			secondary: (0, _wordpress_i18n.__)("Mark an area and edit it with a prompt.", "elementor")
		}), error && /*#__PURE__*/ react.default.createElement(View.ErrorMessage, {
			error,
			onRetry: handleSubmit
		}), /*#__PURE__*/ react.default.createElement(ImageForm, { onSubmit: handleSubmit }, /*#__PURE__*/ react.default.createElement(PromptField, {
			value: prompt,
			disabled: isLoading,
			onChange: setPrompt,
			placeholder: (0, _wordpress_i18n.__)("Describe what you want to generate in the marked area (English only)", "elementor")
		}), data !== null && data !== void 0 && data.result ? /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			gap: 2,
			sx: { my: 2.5 }
		}, /*#__PURE__*/ react.default.createElement(GenerateAgainSubmit, { disabled: isLoading }), /*#__PURE__*/ react.default.createElement(NewPromptButton, {
			disabled: isLoading,
			onClick: function onClick() {
				resetSettings();
				setPrompt("");
				reset();
			}
		})) : /*#__PURE__*/ react.default.createElement(GenerateSubmit, { disabled: isLoading || !prompt || !isCanvasChanged }))), /*#__PURE__*/ react.default.createElement(View.Content, { isGenerating: isLoading }, data !== null && data !== void 0 && data.result ? /*#__PURE__*/ react.default.createElement(ImagesDisplay, {
			images: data.result,
			aspectRatio: editImage.aspectRatio,
			onUseImage: use,
			onEditImage: edit
		}) : /*#__PURE__*/ react.default.createElement(InPaintingContent, {
			editImage,
			width,
			height,
			setMask,
			setIsCanvasChanged
		})));
	};

//#endregion
//#region node_modules/react-avatar-editor/dist/index.js
	var require_dist = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		(function(e, t) {
			"object" == typeof exports && "undefined" != typeof module ? module.exports = t(require_prop_types(), (globalThis.React)) : "function" == typeof define && define.amd ? define(["prop-types", "react"], t) : (e = "undefined" != typeof globalThis ? globalThis : e || self).AvatarEditor = t(e.PropTypes, e.React);
		})(exports, function(e, t) {
			"use strict";
			function o(e) {
				return e && "object" == typeof e && "default" in e ? e : { default: e };
			}
			var n = o(e);
			var i = o(t);
			function r(e, t) {
				for (var o = 0; o < t.length; o++) {
					var n = t[o];
					n.enumerable = n.enumerable || !1, n.configurable = !0, "value" in n && (n.writable = !0), Object.defineProperty(e, n.key, n);
				}
			}
			function s(e, t, o) {
				return t in e ? Object.defineProperty(e, t, {
					value: o,
					enumerable: !0,
					configurable: !0,
					writable: !0
				}) : e[t] = o, e;
			}
			function u() {
				return (u = Object.assign || function(e) {
					for (var t = 1; t < arguments.length; t++) {
						var o = arguments[t];
						for (var n in o) Object.prototype.hasOwnProperty.call(o, n) && (e[n] = o[n]);
					}
					return e;
				}).apply(this, arguments);
			}
			function a(t, e) {
				var o;
				var n = Object.keys(t);
				return Object.getOwnPropertySymbols && (o = Object.getOwnPropertySymbols(t), e && (o = o.filter(function(e) {
					return Object.getOwnPropertyDescriptor(t, e).enumerable;
				})), n.push.apply(n, o)), n;
			}
			function y(t) {
				for (var e = 1; e < arguments.length; e++) {
					var o = null != arguments[e] ? arguments[e] : {};
					e % 2 ? a(Object(o), !0).forEach(function(e) {
						s(t, e, o[e]);
					}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(t, Object.getOwnPropertyDescriptors(o)) : a(Object(o)).forEach(function(e) {
						Object.defineProperty(t, e, Object.getOwnPropertyDescriptor(o, e));
					});
				}
				return t;
			}
			function h(e) {
				return (h = Object.setPrototypeOf ? Object.getPrototypeOf : function(e) {
					return e.__proto__ || Object.getPrototypeOf(e);
				})(e);
			}
			function c(e, t) {
				return (c = Object.setPrototypeOf || function(e, t) {
					return e.__proto__ = t, e;
				})(e, t);
			}
			function l(e, t) {
				if (null == e) return {};
				var o;
				var n = function(e, t) {
					if (null == e) return {};
					for (var o, n = {}, a = Object.keys(e), r = 0; r < a.length; r++) o = a[r], 0 <= t.indexOf(o) || (n[o] = e[o]);
					return n;
				}(e, t);
				if (Object.getOwnPropertySymbols) for (var a = Object.getOwnPropertySymbols(e), r = 0; r < a.length; r++) o = a[r], 0 <= t.indexOf(o) || Object.prototype.propertyIsEnumerable.call(e, o) && (n[o] = e[o]);
				return n;
			}
			function d(e) {
				if (void 0 === e) throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
				return e;
			}
			function p(r) {
				var i = function() {
					if ("undefined" == typeof Reflect || !Reflect.construct) return !1;
					if (Reflect.construct.sham) return !1;
					if ("function" == typeof Proxy) return !0;
					try {
						return Date.prototype.toString.call(Reflect.construct(Date, [], function() {})), !0;
					} catch (e) {
						return !1;
					}
				}();
				return function() {
					var e;
					var t;
					var o;
					var n;
					var a = h(r);
					return t = i ? (e = h(this).constructor, Reflect.construct(a, arguments, e)) : a.apply(this, arguments), o = this, !(n = t) || "object" != typeof n && "function" != typeof n ? d(o) : n;
				};
			}
			function m(e, t) {
				return function(e) {
					if (Array.isArray(e)) return e;
				}(e) || function(e, t) {
					if ("undefined" == typeof Symbol || !(Symbol.iterator in Object(e))) return;
					var o = [];
					var n = !0;
					var a = !1;
					var r = void 0;
					try {
						for (var i, s = e[Symbol.iterator](); !(n = (i = s.next()).done) && (o.push(i.value), !t || o.length !== t); n = !0);
					} catch (e) {
						a = !0, r = e;
					} finally {
						try {
							n || null == s.return || s.return();
						} finally {
							if (a) throw r;
						}
					}
					return o;
				}(e, t) || g(e, t) || function() {
					throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
				}();
			}
			function g(e, t) {
				if (e) {
					if ("string" == typeof e) return f(e, t);
					var o = Object.prototype.toString.call(e).slice(8, -1);
					return "Object" === o && e.constructor && (o = e.constructor.name), "Map" === o || "Set" === o ? Array.from(e) : "Arguments" === o || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(o) ? f(e, t) : void 0;
				}
			}
			function f(e, t) {
				(null == t || t > e.length) && (t = e.length);
				for (var o = 0, n = new Array(t); o < t; o++) n[o] = e[o];
				return n;
			}
			function v(a, r) {
				return new Promise(function(e, t) {
					var o;
					var n = new Image();
					n.onload = function() {
						return e(n);
					}, n.onerror = t, !1 == (null !== (o = a) && !!o.match(/^\s*data:([a-z]+\/[a-z]+(;[a-z-]+=[a-z-]+)?)?(;base64)?,[a-z0-9!$&',()*+;=\-._~:@/?%\s]*\s*$/i)) && r && (n.crossOrigin = r), n.src = a;
				});
			}
			var b;
			var w = !("undefined" == typeof window || "undefined" == typeof navigator || !("ontouchstart" in window || 0 < navigator.msMaxTouchPoints));
			var M = "undefined" != typeof File;
			var O = {
				touch: {
					react: {
						down: "onTouchStart",
						mouseDown: "onMouseDown",
						drag: "onTouchMove",
						move: "onTouchMove",
						mouseMove: "onMouseMove",
						up: "onTouchEnd",
						mouseUp: "onMouseUp"
					},
					native: {
						down: "touchstart",
						mouseDown: "mousedown",
						drag: "touchmove",
						move: "touchmove",
						mouseMove: "mousemove",
						up: "touchend",
						mouseUp: "mouseup"
					}
				},
				desktop: {
					react: {
						down: "onMouseDown",
						drag: "onDragOver",
						move: "onMouseMove",
						up: "onMouseUp"
					},
					native: {
						down: "mousedown",
						drag: "dragStart",
						move: "mousemove",
						up: "mouseup"
					}
				}
			};
			var I = w ? O.touch : O.desktop;
			var P = "undefined" != typeof window && window.devicePixelRatio ? window.devicePixelRatio : 1;
			var C = {
				x: .5,
				y: .5
			};
			var x = function() {
				(function(e, t) {
					if ("function" != typeof t && null !== t) throw new TypeError("Super expression must either be null or a function");
					e.prototype = Object.create(t && t.prototype, { constructor: {
						value: e,
						writable: !0,
						configurable: !0
					} }), t && c(e, t);
				})(a, i["default"].Component);
				var e;
				var t;
				var o;
				var n = p(a);
				function a(e) {
					var v;
					return function(e, t) {
						if (!(e instanceof t)) throw new TypeError("Cannot call a class as a function");
					}(this, a), s(d(v = n.call(this, e)), "state", {
						drag: !1,
						my: null,
						mx: null,
						image: C
					}), s(d(v), "handleImageReady", function(e) {
						var t = v.getInitialSize(e.width, e.height);
						t.resource = e, t.x = .5, t.y = .5, t.backgroundColor = v.props.backgroundColor, v.setState({
							drag: !1,
							image: t
						}, v.props.onImageReady), v.props.onLoadSuccess(t);
					}), s(d(v), "clearImage", function() {
						v.canvas.getContext("2d").clearRect(0, 0, v.canvas.width, v.canvas.height), v.setState({ image: C });
					}), s(d(v), "handleMouseDown", function(e) {
						(e = e || window.event).preventDefault(), v.setState({
							drag: !0,
							mx: null,
							my: null
						});
					}), s(d(v), "handleMouseUp", function() {
						v.state.drag && (v.setState({ drag: !1 }), v.props.onMouseUp());
					}), s(d(v), "handleMouseMove", function(e) {
						var t;
						var o;
						var n;
						var a;
						var r;
						var i;
						var s;
						var u;
						var h;
						var c;
						var l;
						var d;
						var p;
						var g;
						var f;
						var m;
						e = e || window.event, !1 !== v.state.drag && (e.preventDefault(), n = {
							mx: t = e.targetTouches ? e.targetTouches[0].pageX : e.clientX,
							my: o = e.targetTouches ? e.targetTouches[0].pageY : e.clientY
						}, m = v.props.rotate, m = (m %= 360) < 0 ? m + 360 : m, v.state.mx && v.state.my && (a = v.state.mx - t, r = v.state.my - o, i = v.state.image.width * v.props.scale, s = v.state.image.height * v.props.scale, h = (u = v.getCroppingRect()).x, c = u.y, h *= i, c *= s, l = function(e) {
							return e * (Math.PI / 180);
						}, d = Math.cos(l(m)), g = c + -a * (p = Math.sin(l(m))) + r * d, f = {
							x: (h + a * d + r * p) / i + 1 / v.props.scale * v.getXScale() / 2,
							y: g / s + 1 / v.props.scale * v.getYScale() / 2
						}, v.props.onPositionChange(f), n.image = y(y({}, v.state.image), f)), v.setState(n), v.props.onMouseMove(e));
					}), s(d(v), "setCanvas", function(e) {
						v.canvas = e;
					}), v.canvas = null, v;
				}
				return e = a, (t = [
					{
						key: "componentDidMount",
						value: function() {
							this.props.disableHiDPIScaling && (P = 1);
							var e;
							var t;
							var o = this.canvas.getContext("2d");
							this.props.image && this.loadImage(this.props.image), this.paint(o), document && (e = !!function() {
								var t = !1;
								try {
									var e = Object.defineProperty({}, "passive", { get: function() {
										t = !0;
									} });
									window.addEventListener("test", e, e), window.removeEventListener("test", e, e);
								} catch (e) {
									t = !1;
								}
								return t;
							}() && { passive: !1 }, t = I.native, document.addEventListener(t.move, this.handleMouseMove, e), document.addEventListener(t.up, this.handleMouseUp, e), w && (document.addEventListener(t.mouseMove, this.handleMouseMove, e), document.addEventListener(t.mouseUp, this.handleMouseUp, e)));
						}
					},
					{
						key: "componentDidUpdate",
						value: function(e, t) {
							this.props.image && this.props.image !== e.image || this.props.width !== e.width || this.props.height !== e.height || this.props.backgroundColor !== e.backgroundColor ? this.loadImage(this.props.image) : this.props.image || t.image === C || this.clearImage();
							var o = this.canvas.getContext("2d");
							o.clearRect(0, 0, this.canvas.width, this.canvas.height), this.paint(o), this.paintImage(o, this.state.image, this.props.border), e.image === this.props.image && e.width === this.props.width && e.height === this.props.height && e.position === this.props.position && e.scale === this.props.scale && e.rotate === this.props.rotate && t.my === this.state.my && t.mx === this.state.mx && t.image.x === this.state.image.x && t.image.y === this.state.image.y && t.backgroundColor === this.state.backgroundColor || this.props.onImageChange();
						}
					},
					{
						key: "componentWillUnmount",
						value: function() {
							var e;
							document && (e = I.native, document.removeEventListener(e.move, this.handleMouseMove, !1), document.removeEventListener(e.up, this.handleMouseUp, !1), w && (document.removeEventListener(e.mouseMove, this.handleMouseMove, !1), document.removeEventListener(e.mouseUp, this.handleMouseUp, !1)));
						}
					},
					{
						key: "isVertical",
						value: function() {
							return !this.props.disableCanvasRotation && this.props.rotate % 180 != 0;
						}
					},
					{
						key: "getBorders",
						value: function(e) {
							var t = 0 < arguments.length && void 0 !== e ? e : this.props.border;
							return Array.isArray(t) ? t : [t, t];
						}
					},
					{
						key: "getDimensions",
						value: function() {
							var e = this.props;
							var t = e.width;
							var o = e.height;
							var n = e.rotate;
							var a = e.border;
							var r = {};
							var i = m(this.getBorders(a), 2);
							var s = i[0];
							var u = i[1];
							var h = t;
							var c = o;
							return this.isVertical() ? (r.width = c, r.height = h) : (r.width = h, r.height = c), r.width += 2 * s, r.height += 2 * u, {
								canvas: r,
								rotate: n,
								width: t,
								height: o,
								border: a
							};
						}
					},
					{
						key: "getImage",
						value: function() {
							var e = this.getCroppingRect();
							var t = this.state.image;
							e.x *= t.resource.width, e.y *= t.resource.height, e.width *= t.resource.width, e.height *= t.resource.height;
							var o = document.createElement("canvas");
							this.isVertical() ? (o.width = e.height, o.height = e.width) : (o.width = e.width, o.height = e.height);
							var n = o.getContext("2d");
							return n.translate(o.width / 2, o.height / 2), n.rotate(this.props.rotate * Math.PI / 180), n.translate(-o.width / 2, -o.height / 2), this.isVertical() && n.translate((o.width - o.height) / 2, (o.height - o.width) / 2), t.backgroundColor && (n.fillStyle = t.backgroundColor, n.fillRect(-e.x, -e.y, t.resource.width, t.resource.height)), n.drawImage(t.resource, -e.x, -e.y), o;
						}
					},
					{
						key: "getImageScaledToCanvas",
						value: function() {
							var e = this.getDimensions();
							var t = e.width;
							var o = e.height;
							var n = document.createElement("canvas");
							return this.isVertical() ? (n.width = o, n.height = t) : (n.width = t, n.height = o), this.paintImage(n.getContext("2d"), this.state.image, 0, 1), n;
						}
					},
					{
						key: "getXScale",
						value: function() {
							var e = this.props.width / this.props.height;
							var t = this.state.image.width / this.state.image.height;
							return Math.min(1, e / t);
						}
					},
					{
						key: "getYScale",
						value: function() {
							var e = this.props.height / this.props.width;
							var t = this.state.image.height / this.state.image.width;
							return Math.min(1, e / t);
						}
					},
					{
						key: "getCroppingRect",
						value: function() {
							var e = this.props.position || {
								x: this.state.image.x,
								y: this.state.image.y
							};
							var t = 1 / this.props.scale * this.getXScale();
							var o = 1 / this.props.scale * this.getYScale();
							var n = {
								x: e.x - t / 2,
								y: e.y - o / 2,
								width: t,
								height: o
							};
							var a = 0;
							var r = 1 - n.width;
							var i = 0;
							var s = 1 - n.height;
							return (this.props.disableBoundaryChecks || 1 < t || 1 < o) && (a = -n.width, i = -n.height, s = r = 1), y(y({}, n), {}, {
								x: Math.max(a, Math.min(n.x, r)),
								y: Math.max(i, Math.min(n.y, s))
							});
						}
					},
					{
						key: "loadImage",
						value: function(e) {
							var t;
							M && e instanceof File ? this.loadingImage = (t = e, new Promise(function(o, n) {
								var e = new FileReader();
								e.onload = function(e) {
									try {
										o(v(e.target.result));
									} catch (e) {
										n(e);
									}
								}, e.readAsDataURL(t);
							}).then(this.handleImageReady).catch(this.props.onLoadFailure)) : "string" == typeof e && (this.loadingImage = v(e, this.props.crossOrigin).then(this.handleImageReady).catch(this.props.onLoadFailure));
						}
					},
					{
						key: "getInitialSize",
						value: function(e, t) {
							var o;
							var n;
							var a = this.getDimensions();
							return t / e < a.height / a.width ? n = e * ((o = this.getDimensions().height) / t) : o = t * ((n = this.getDimensions().width) / e), {
								height: o,
								width: n
							};
						}
					},
					{
						key: "paintImage",
						value: function(e, t, o, n) {
							var a;
							var r = 3 < arguments.length && void 0 !== n ? n : P;
							t.resource && (a = this.calculatePosition(t, o), e.save(), e.translate(e.canvas.width / 2, e.canvas.height / 2), e.rotate(this.props.rotate * Math.PI / 180), e.translate(-e.canvas.width / 2, -e.canvas.height / 2), this.isVertical() && e.translate((e.canvas.width - e.canvas.height) / 2, (e.canvas.height - e.canvas.width) / 2), e.scale(r, r), e.globalCompositeOperation = "destination-over", e.drawImage(t.resource, a.x, a.y, a.width, a.height), t.backgroundColor && (e.fillStyle = t.backgroundColor, e.fillRect(a.x, a.y, a.width, a.height)), e.restore());
						}
					},
					{
						key: "calculatePosition",
						value: function(e, t) {
							e = e || this.state.image;
							var o = m(this.getBorders(t), 2);
							var n = o[0];
							var a = o[1];
							var r = this.getCroppingRect();
							var i = e.width * this.props.scale;
							var s = e.height * this.props.scale;
							var u = -r.x * i;
							var h = -r.y * s;
							return this.isVertical() ? (u += a, h += n) : (u += n, h += a), {
								x: u,
								y: h,
								height: s,
								width: i
							};
						}
					},
					{
						key: "paint",
						value: function(e) {
							e.save(), e.scale(P, P), e.translate(0, 0), e.fillStyle = "rgba(" + this.props.color.slice(0, 4).join(",") + ")";
							var t;
							var o;
							var n;
							var a;
							var r;
							var i;
							var s;
							var u;
							var h = this.props.borderRadius;
							var c = this.getDimensions();
							var l = m(this.getBorders(c.border), 2);
							var d = l[0];
							var p = l[1];
							var g = c.canvas.height;
							var f = c.canvas.width;
							var h = Math.max(h, 0);
							h = Math.min(h, f / 2 - d, g / 2 - p), e.beginPath(), t = e, a = f - 2 * (o = d), r = g - 2 * (n = p), 0 === (i = h) ? t.rect(o, n, a, r) : (s = a - i, u = r - i, t.translate(o, n), t.arc(i, i, i, Math.PI, 1.5 * Math.PI), t.lineTo(s, 0), t.arc(s, i, i, 1.5 * Math.PI, 2 * Math.PI), t.lineTo(a, u), t.arc(s, u, i, 2 * Math.PI, .5 * Math.PI), t.lineTo(i, r), t.arc(i, u, i, .5 * Math.PI, Math.PI), t.translate(-o, -n)), e.rect(f, 0, -f, g), e.fill("evenodd"), e.restore();
						}
					},
					{
						key: "render",
						value: function() {
							var e = this.props;
							var t = (e.scale, e.rotate, e.image, e.border, e.borderRadius, e.width, e.height, e.position, e.color, e.backgroundColor, e.style);
							var o = (e.crossOrigin, e.onLoadFailure, e.onLoadSuccess, e.onImageReady, e.onImageChange, e.onMouseUp, e.onMouseMove, e.onPositionChange, e.disableBoundaryChecks, e.disableHiDPIScaling, e.disableCanvasRotation, l(e, [
								"scale",
								"rotate",
								"image",
								"border",
								"borderRadius",
								"width",
								"height",
								"position",
								"color",
								"backgroundColor",
								"style",
								"crossOrigin",
								"onLoadFailure",
								"onLoadSuccess",
								"onImageReady",
								"onImageChange",
								"onMouseUp",
								"onMouseMove",
								"onPositionChange",
								"disableBoundaryChecks",
								"disableHiDPIScaling",
								"disableCanvasRotation"
							]));
							var n = this.getDimensions();
							var a = {
								width: n.canvas.width,
								height: n.canvas.height,
								cursor: this.state.drag ? "grabbing" : "grab",
								touchAction: "none"
							};
							var r = {
								width: n.canvas.width * P,
								height: n.canvas.height * P,
								style: y(y({}, a), t)
							};
							return r[I.react.down] = this.handleMouseDown, w && (r[I.react.mouseDown] = this.handleMouseDown), i.default.createElement("canvas", u({ ref: this.setCanvas }, r, o));
						}
					}
				]) && r(e.prototype, t), o && r(e, o), a;
			}();
			return s(x, "propTypes", {
				scale: n.default.number,
				rotate: n.default.number,
				image: n.default.oneOfType([n.default.string].concat(function(e) {
					if (Array.isArray(e)) return f(e);
				}(b = M ? [n.default.instanceOf(File)] : []) || function(e) {
					if ("undefined" != typeof Symbol && Symbol.iterator in Object(e)) return Array.from(e);
				}(b) || g(b) || function() {
					throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
				}())),
				border: n.default.oneOfType([n.default.number, n.default.arrayOf(n.default.number)]),
				borderRadius: n.default.number,
				width: n.default.number,
				height: n.default.number,
				position: n.default.shape({
					x: n.default.number,
					y: n.default.number
				}),
				color: n.default.arrayOf(n.default.number),
				backgroundColor: n.default.string,
				crossOrigin: n.default.oneOf([
					"",
					"anonymous",
					"use-credentials"
				]),
				onLoadFailure: n.default.func,
				onLoadSuccess: n.default.func,
				onImageReady: n.default.func,
				onImageChange: n.default.func,
				onMouseUp: n.default.func,
				onMouseMove: n.default.func,
				onPositionChange: n.default.func,
				disableBoundaryChecks: n.default.bool,
				disableHiDPIScaling: n.default.bool,
				disableCanvasRotation: n.default.bool
			}), s(x, "defaultProps", {
				scale: 1,
				rotate: 0,
				border: 25,
				borderRadius: 0,
				width: 200,
				height: 200,
				color: [
					0,
					0,
					0,
					.5
				],
				onLoadFailure: function() {},
				onLoadSuccess: function() {},
				onImageReady: function() {},
				onImageChange: function() {},
				onMouseUp: function() {},
				onMouseMove: function() {},
				onPositionChange: function() {},
				disableBoundaryChecks: !1,
				disableHiDPIScaling: !1,
				disableCanvasRotation: !0
			}), x;
		});
	}));

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/out-painting/out-painting-content.js
	init_asyncToGenerator();
	var import_dist = /* @__PURE__ */ __toESM(require_dist());
	var OutPaintingContent = function OutPaintingContent(_ref) {
		var scale = _ref.scale;
		var setMask = _ref.setMask;
		var editImage = _ref.editImage;
		var aspectRatio = _ref.aspectRatio;
		var setImageSize = _ref.setImageSize;
		var setPosition = _ref.setPosition;
		var cropperRef = (0, react.useRef)();
		var position = (0, react.useRef)({
			x: .5,
			y: .5
		});
		var _useImageSize = useImageSize(aspectRatio);
		var width = _useImageSize.width;
		var height = _useImageSize.height;
		var imageSize = {
			width,
			height
		};
		var updateMask = /*#__PURE__*/ function() {
			var _ref2 = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee() {
				var imageDataURL;
				return import_regenerator$2.default.wrap(function(_context) {
					while (1) switch (_context.prev = _context.next) {
						case 0:
							_context.next = 1;
							return cropperRef.current.getImageScaledToCanvas().toDataURL();
						case 1:
							imageDataURL = _context.sent;
							setMask(imageDataURL);
						case 2:
						case "end": return _context.stop();
					}
				}, _callee);
			}));
			return function updateMask() {
				return _ref2.apply(this, arguments);
			};
		}();
		var _onPositionChange = /*#__PURE__*/ function() {
			var _ref3 = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee2(args) {
				return import_regenerator$2.default.wrap(function(_context2) {
					while (1) switch (_context2.prev = _context2.next) {
						case 0:
							position.current = {
								x: args.x,
								y: args.y
							};
							_context2.next = 1;
							return updateMask();
						case 1: setPosition(position.current);
						case 2:
						case "end": return _context2.stop();
					}
				}, _callee2);
			}));
			return function onPositionChange(_x) {
				return _ref3.apply(this, arguments);
			};
		}();
		var onImageChange = /*#__PURE__*/ function() {
			var _ref4 = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee3() {
				return import_regenerator$2.default.wrap(function(_context3) {
					while (1) switch (_context3.prev = _context3.next) {
						case 0:
							imageSize = {
								width: imageSize.width * scale,
								height: imageSize.height * scale
							};
							_context3.next = 1;
							return updateMask();
						case 1: setImageSize(imageSize);
						case 2:
						case "end": return _context3.stop();
					}
				}, _callee3);
			}));
			return function onImageChange() {
				return _ref4.apply(this, arguments);
			};
		}();
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			alignItems: "center",
			spacing: .5,
			flexGrow: 1
		}, /*#__PURE__*/ react.default.createElement(import_dist.default, {
			ref: cropperRef,
			image: editImage.url,
			style: {
				backgroundImage: "linear-gradient(45deg, #bbb 25%, transparent 25%), linear-gradient(-45deg, #bbb 25%, transparent 25%), linear-gradient(45deg, transparent 75%, #bbb 75%), linear-gradient(-45deg, transparent 75%, #bbb 75%)",
				backgroundSize: "20px 20px",
				backgroundPosition: "0 0, 0 10px, 10px -10px, -10px 0px",
				outline: "2px dashed #000",
				marginTop: "12px"
			},
			color: [
				0,
				0,
				0,
				.9
			],
			rotate: 0,
			border: 0,
			allowZoomOut: true,
			backgroundColor: "transparent",
			showGrid: true,
			onImageChange,
			onPositionChange: function onPositionChange(args) {
				return _onPositionChange(args);
			},
			width,
			height,
			scale
		}));
	};
	OutPaintingContent.propTypes = {
		scale: import_prop_types$7.default.number.isRequired,
		setMask: import_prop_types$7.default.func.isRequired,
		editImage: import_prop_types$7.default.object.isRequired,
		aspectRatio: import_prop_types$7.default.string.isRequired,
		setImageSize: import_prop_types$7.default.func.isRequired,
		setPosition: import_prop_types$7.default.func.isRequired
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/out-painting/hooks/use-out-painting.js
	init_api();
	var useOutPainting = function useOutPainting(initialValue) {
		return useImagePrompt(function fetchAction(payload) {
			return getImageToImageOutPainting(payload);
		}, initialValue);
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/out-painting/index.js
	init_defineProperty$1();
	init_asyncToGenerator();
	init_slicedToArray();
	init_requests_ids();
	var OutPainting = function OutPainting() {
		var _useState2 = _slicedToArray((0, react.useState)({
			width: 0,
			height: 0
		}), 2);
		var imageSize = _useState2[0];
		var setImageSize = _useState2[1];
		var _useState4 = _slicedToArray((0, react.useState)({
			x: .5,
			y: .5
		}), 2);
		var position = _useState4[0];
		var setPosition = _useState4[1];
		var _useState6 = _slicedToArray((0, react.useState)(""), 2);
		var mask = _useState6[0];
		var setMask = _useState6[1];
		var setGenerate = useRequestIds().setGenerate;
		var _useEditImage = useEditImage();
		var editImage = _useEditImage.editImage;
		var initialAspectRatio = _useEditImage.aspectRatio;
		var _usePromptSettings = usePromptSettings({ aspectRatio: initialAspectRatio });
		var settings = _usePromptSettings.settings;
		var updateSettings = _usePromptSettings.updateSettings;
		var _useImageActions = useImageActions();
		var use = _useImageActions.use;
		var edit = _useImageActions.edit;
		var isUploading = _useImageActions.isLoading;
		var _useOutPainting = useOutPainting();
		var data = _useOutPainting.data;
		var send = _useOutPainting.send;
		var isGenerating = _useOutPainting.isLoading;
		var error = _useOutPainting.error;
		var isLoading = isGenerating || isUploading;
		var generatedAspectRatio = (0, react.useMemo)(function() {
			return settings[IMAGE_RATIO];
		}, [data === null || data === void 0 ? void 0 : data.result]);
		var hasGeneratedResult = !!(data !== null && data !== void 0 && data.result);
		var handleSubmit = /*#__PURE__*/ function() {
			var _ref = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee(event) {
				var imageBase64;
				return import_regenerator$2.default.wrap(function(_context) {
					while (1) switch (_context.prev = _context.next) {
						case 0:
							event.preventDefault();
							setGenerate();
							_context.next = 1;
							return fetchImageAsBase64(editImage.url);
						case 1:
							imageBase64 = _context.sent;
							send({
								settings,
								image: editImage,
								mask,
								size: imageSize,
								position,
								image_base64: imageBase64
							});
						case 2:
						case "end": return _context.stop();
					}
				}, _callee);
			}));
			return function handleSubmit(_x) {
				return _ref.apply(this, arguments);
			};
		}();
		return /*#__PURE__*/ react.default.createElement(View, null, /*#__PURE__*/ react.default.createElement(View.Panel, null, /*#__PURE__*/ react.default.createElement(View.BackButton, null), /*#__PURE__*/ react.default.createElement(View.PanelHeading, {
			primary: (0, _wordpress_i18n.__)("Expand Image", "elementor"),
			secondary: (0, _wordpress_i18n.__)("Position image in it’s new size to generate content around the edges.", "elementor")
		}), error && /*#__PURE__*/ react.default.createElement(View.ErrorMessage, {
			error,
			onRetry: handleSubmit
		}), /*#__PURE__*/ react.default.createElement(ImageForm, { onSubmit: handleSubmit }, /*#__PURE__*/ react.default.createElement(ImageRatioSelect, {
			disabled: isLoading || hasGeneratedResult,
			value: settings[IMAGE_RATIO],
			onChange: function onChange(event) {
				return updateSettings(_defineProperty$1({}, IMAGE_RATIO, event.target.value));
			}
		}), /*#__PURE__*/ react.default.createElement(_elementor_ui.FormControl, { sx: {
			width: "100%",
			mb: 2.5
		} }, /*#__PURE__*/ react.default.createElement(_elementor_ui.Slider, {
			marks: true,
			id: "zoom",
			name: "zoom",
			max: 1,
			min: .1,
			step: .1,
			color: "secondary",
			defaultValue: 1,
			disabled: isLoading || hasGeneratedResult,
			valueLabelDisplay: "auto",
			"aria-labelledby": "image-size-slider",
			"aria-label": (0, _wordpress_i18n.__)("Reference strength", "elementor"),
			onChange: function onChange(_, value) {
				return updateSettings(_defineProperty$1({}, IMAGE_ZOOM, value));
			}
		}), /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, {
			id: "image-size-slider",
			variant: "caption",
			gutterBottom: true
		}, (0, _wordpress_i18n.__)("Original image size", "elementor"))), data !== null && data !== void 0 && data.result ? /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			gap: 2,
			sx: { my: 2.5 }
		}, /*#__PURE__*/ react.default.createElement(GenerateAgainSubmit, { disabled: isLoading })) : /*#__PURE__*/ react.default.createElement(GenerateSubmit, { disabled: isLoading }))), /*#__PURE__*/ react.default.createElement(View.Content, { isGenerating: isLoading }, hasGeneratedResult ? /*#__PURE__*/ react.default.createElement(ImagesDisplay, {
			onUseImage: use,
			onEditImage: edit,
			images: data.result,
			aspectRatio: generatedAspectRatio
		}) : /*#__PURE__*/ react.default.createElement(OutPaintingContent, {
			setMask,
			editImage,
			scale: settings[IMAGE_ZOOM],
			aspectRatio: settings[IMAGE_RATIO],
			setImageSize,
			setPosition
		})));
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/variations/hooks/use-image-to-image.js
	init_api();
	var useImageToImage = function useImageToImage(initialValue) {
		return useImagePrompt(function fetchAction(payload) {
			return getImageToImageGeneration(payload);
		}, initialValue);
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/variations/index.js
	init_slicedToArray();
	init_requests_ids();
	var IMAGE_WEIGHT_DEFAULT = 45;
	var Variations = function Variations() {
		var _data$result;
		var prompt = _slicedToArray((0, react.useState)(""), 1)[0];
		var setGenerate = useRequestIds().setGenerate;
		var _useEditImage = useEditImage();
		var editImage = _useEditImage.editImage;
		var initialAspectRatio = _useEditImage.aspectRatio;
		var settings = usePromptSettings({
			aspectRatio: initialAspectRatio,
			imageWeight: IMAGE_WEIGHT_DEFAULT
		}).settings;
		var _useImageToImage = useImageToImage();
		var data = _useImageToImage.data;
		var send = _useImageToImage.send;
		var isGenerating = _useImageToImage.isLoading;
		var error = _useImageToImage.error;
		var _useImageActions = useImageActions();
		var use = _useImageActions.use;
		var edit = _useImageActions.edit;
		var isUploading = _useImageActions.isLoading;
		var isLoading = isGenerating || isUploading;
		var generatedAspectRatio = (0, react.useMemo)(function() {
			return settings[IMAGE_RATIO];
		}, [data === null || data === void 0 ? void 0 : data.result]);
		var handleSubmit = function handleSubmit(event) {
			event.preventDefault();
			setGenerate();
			send({
				prompt,
				settings,
				image: editImage
			});
		};
		return /*#__PURE__*/ react.default.createElement(View, null, /*#__PURE__*/ react.default.createElement(View.Panel, null, /*#__PURE__*/ react.default.createElement(View.BackButton, null), /*#__PURE__*/ react.default.createElement(View.PanelHeading, {
			primary: (0, _wordpress_i18n.__)("Variations", "elementor"),
			secondary: (0, _wordpress_i18n.__)("Create new versions of the original image.", "elementor")
		}), error && /*#__PURE__*/ react.default.createElement(View.ErrorMessage, {
			error,
			onRetry: handleSubmit
		}), /*#__PURE__*/ react.default.createElement(ImageForm, { onSubmit: handleSubmit }, /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, { sx: {
			display: "flex",
			alignItems: "center",
			justifyContent: "center",
			bgcolor: "action.selected"
		} }, /*#__PURE__*/ react.default.createElement("img", {
			src: (editImage === null || editImage === void 0 ? void 0 : editImage.image_url) || (editImage === null || editImage === void 0 ? void 0 : editImage.url),
			alt: prompt,
			style: {
				width: "auto",
				height: "auto",
				maxWidth: "100%",
				maxHeight: 166,
				objectFit: "contained"
			}
		})), /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			gap: 2,
			sx: { my: 2.5 }
		}, (data === null || data === void 0 || (_data$result = data.result) === null || _data$result === void 0 ? void 0 : _data$result.length) > 0 ? /*#__PURE__*/ react.default.createElement(GenerateAgainSubmit, { disabled: isLoading }) : /*#__PURE__*/ react.default.createElement(GenerateImagesSubmit, { disabled: isLoading })))), /*#__PURE__*/ react.default.createElement(View.Content, { isGenerating: isLoading }, data !== null && data !== void 0 && data.result ? /*#__PURE__*/ react.default.createElement(ImagesDisplay, {
			images: data.result,
			aspectRatio: generatedAspectRatio,
			onUseImage: use,
			onEditImage: edit
		}) : /*#__PURE__*/ react.default.createElement(ImagesPlaceholder, null)));
	};
	Variations.propTypes = {};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/replace-background/hooks/use-replace-background.js
	init_api();
	var useReplaceBackground = function useReplaceBackground(initialValue) {
		return useImagePrompt(function fetchAction(payload) {
			return getImageToImageReplaceBackground(payload);
		}, initialValue);
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/replace-background/index.js
	init_slicedToArray();
	init_requests_ids();
	var ReplaceBackground = function ReplaceBackground() {
		var _useState2 = _slicedToArray((0, react.useState)(""), 2);
		var prompt = _useState2[0];
		var setPrompt = _useState2[1];
		var setGenerate = useRequestIds().setGenerate;
		var editImage = useEditImage().editImage;
		var _useImageActions = useImageActions();
		var use = _useImageActions.use;
		var edit = _useImageActions.edit;
		var isUploading = _useImageActions.isLoading;
		var _useReplaceBackground = useReplaceBackground();
		var data = _useReplaceBackground.data;
		var send = _useReplaceBackground.send;
		var isGenerating = _useReplaceBackground.isLoading;
		var error = _useReplaceBackground.error;
		var navigate = useLocation().navigate;
		var isLoading = isGenerating || isUploading;
		var handleSubmit = function handleSubmit(event) {
			event.preventDefault();
			setGenerate();
			send({
				prompt,
				image: editImage
			});
		};
		return /*#__PURE__*/ react.default.createElement(View, null, /*#__PURE__*/ react.default.createElement(View.Panel, null, /*#__PURE__*/ react.default.createElement(View.BackButton, null), /*#__PURE__*/ react.default.createElement(View.PanelHeading, {
			primary: (0, _wordpress_i18n.__)("Replace Background", "elementor"),
			secondary: (0, _wordpress_i18n.__)("Generate a new background with a prompt.", "elementor")
		}), error && /*#__PURE__*/ react.default.createElement(View.ErrorMessage, {
			error,
			onRetry: handleSubmit
		}), /*#__PURE__*/ react.default.createElement(ImageForm, { onSubmit: handleSubmit }, /*#__PURE__*/ react.default.createElement(PromptField, {
			value: prompt,
			disabled: isLoading,
			onChange: setPrompt,
			placeholder: (0, _wordpress_i18n.__)("Describe what you want to generate in the background (English only)", "elementor")
		}), /*#__PURE__*/ react.default.createElement(GenerateSubmit, {
			disabled: isLoading || "" === prompt,
			color: data !== null && data !== void 0 && data.result ? "secondary" : "primary"
		}, data !== null && data !== void 0 && data.result ? (0, _wordpress_i18n.__)("Generate Again", "elementor") : (0, _wordpress_i18n.__)("Replace Background", "elementor")), (data === null || data === void 0 ? void 0 : data.result) && /*#__PURE__*/ react.default.createElement(NewPromptButton, {
			disabled: isLoading,
			onClick: function onClick() {
				return navigate(LOCATIONS.GENERATE);
			}
		}))), /*#__PURE__*/ react.default.createElement(View.Content, { isGenerating: isLoading }, data !== null && data !== void 0 && data.result ? /*#__PURE__*/ react.default.createElement(ImagesDisplay, {
			onUseImage: use,
			onEditImage: edit,
			images: data.result,
			aspectRatio: editImage.aspectRatio
		}) : /*#__PURE__*/ react.default.createElement(SingleImagePreview, null, /*#__PURE__*/ react.default.createElement(SingleImagePreview.Image, {
			src: editImage.url,
			alt: editImage.alt,
			style: {
				width: editImage.width,
				height: editImage.height
			}
		}))));
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/remove-background/hooks/use-remove-background.js
	init_api();
	var useRemoveBackground = function useRemoveBackground(initialValue) {
		return useImagePrompt(function fetchAction(payload) {
			return getImageToImageRemoveBackground(payload);
		}, initialValue);
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/remove-background/index.js
	init_requests_ids();
	var RemoveBackground = function RemoveBackground() {
		var editImage = useEditImage().editImage;
		var setGenerate = useRequestIds().setGenerate;
		var _useImageActions = useImageActions();
		var use = _useImageActions.use;
		var edit = _useImageActions.edit;
		var isUploading = _useImageActions.isLoading;
		var _useRemoveBackground = useRemoveBackground();
		var data = _useRemoveBackground.data;
		var send = _useRemoveBackground.send;
		var isGenerating = _useRemoveBackground.isLoading;
		var error = _useRemoveBackground.error;
		var navigate = useLocation().navigate;
		var isLoading = isGenerating || isUploading;
		var _useImageSize = useImageSize(editImage.aspectRatio);
		var width = _useImageSize.width;
		var height = _useImageSize.height;
		var handleSubmit = function handleSubmit(event) {
			event.preventDefault();
			setGenerate();
			send({ image: editImage });
		};
		return /*#__PURE__*/ react.default.createElement(View, null, /*#__PURE__*/ react.default.createElement(View.Panel, null, /*#__PURE__*/ react.default.createElement(View.BackButton, null), /*#__PURE__*/ react.default.createElement(View.PanelHeading, {
			primary: (0, _wordpress_i18n.__)("Remove Background", "elementor"),
			secondary: (0, _wordpress_i18n.__)("Create an image of the subject with a transparent background", "elementor")
		}), error && /*#__PURE__*/ react.default.createElement(View.ErrorMessage, {
			error,
			onRetry: handleSubmit
		}), /*#__PURE__*/ react.default.createElement(ImageForm, { onSubmit: handleSubmit }, data !== null && data !== void 0 && data.result ? /*#__PURE__*/ react.default.createElement(NewPromptButton, {
			variant: "contained",
			disabled: isLoading,
			onClick: function onClick() {
				return navigate(LOCATIONS.GENERATE);
			}
		}) : /*#__PURE__*/ react.default.createElement(GenerateSubmit, { disabled: isLoading }, (0, _wordpress_i18n.__)("Remove Background", "elementor")))), /*#__PURE__*/ react.default.createElement(View.Content, { isGenerating: isLoading }, data !== null && data !== void 0 && data.result ? /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, null, /*#__PURE__*/ react.default.createElement(ImagesDisplay, {
			transparentContainer: true,
			onUseImage: use,
			onEditImage: edit,
			images: data.result,
			aspectRatio: editImage.aspectRatio
		})) : /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, null, /*#__PURE__*/ react.default.createElement(SingleImagePreview, null, /*#__PURE__*/ react.default.createElement(SingleImagePreview.Image, {
			src: editImage.url,
			alt: editImage.alt,
			style: {
				width,
				height
			}
		})))));
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/cleanup/cleanup-content.js
	init_asyncToGenerator();
	init_slicedToArray();
	var STROKE_SELECT_WIDTH = 120;
	var BRUSH_COLOR = "rgba(255, 255, 255)";
	var CANVAS_COLOR = "rgba(0, 0, 0)";
	var StyledUndoIcon = (0, _elementor_ui.withDirection)(UndoIcon);
	var StyledRedoIcon = (0, _elementor_ui.withDirection)(RedoIcon);
	var BrushCursor = (0, _elementor_ui.styled)(_elementor_ui.Box, { shouldForwardProp: function shouldForwardProp(prop) {
		return "size" === prop;
	} })(function(_ref) {
		var theme = _ref.theme;
		var size = _ref.size;
		return {
			position: "absolute",
			width: size,
			height: size,
			borderRadius: "50%",
			backgroundColor: BRUSH_COLOR,
			pointerEvents: "none",
			transform: "translate(".concat("rtl" === theme.direction ? "50%" : "-50%", ", -50%)"),
			border: "1px solid #fff"
		};
	});
	var BrishSizeIcon = (0, _elementor_ui.styled)(_elementor_ui.Box, { shouldForwardProp: function shouldForwardProp(prop) {
		return "size" === prop;
	} })(function(_ref2) {
		var theme = _ref2.theme;
		var size = _ref2.size;
		return {
			width: size / 2,
			height: size / 2,
			borderRadius: "50%",
			backgroundColor: theme.palette.secondary.main
		};
	});
	var CleanupContent = function CleanupContent(_ref3) {
		var editImage = _ref3.editImage;
		var setMask = _ref3.setMask;
		var setIsCanvasChanged = _ref3.setIsCanvasChanged;
		var canvasWidth = _ref3.width;
		var canvasHeight = _ref3.height;
		var sketchRef = (0, react.useRef)();
		var _useState2 = _slicedToArray((0, react.useState)(30), 2);
		var stroke = _useState2[0];
		var setStroke = _useState2[1];
		var brushCursorRef = (0, react.useRef)();
		(0, react.useEffect)(function() {
			var canvas = document.querySelector(".eui-in-painting-canvas");
			var positionElement = function positionElement(e) {
				var _canvas$getBoundingCl = canvas.getBoundingClientRect();
				var left = _canvas$getBoundingCl.left;
				var top = _canvas$getBoundingCl.top;
				var width = _canvas$getBoundingCl.width;
				var height = _canvas$getBoundingCl.height;
				var x = e.clientX - left;
				var y = e.clientY - top;
				if (x > 0 && x < width && y > 0 && y < height) {
					brushCursorRef.current.style.left = "".concat(x, "px");
					brushCursorRef.current.style.top = "".concat(y, "px");
				}
			};
			window.addEventListener("mousemove", positionElement);
			return function() {
				window.removeEventListener("mousemove", positionElement);
			};
		}, [stroke]);
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			alignItems: "flex-start",
			spacing: .5,
			flexGrow: 1
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			width: "100%",
			direction: "row",
			spacing: 3,
			alignSelf: "center",
			justifyContent: "center",
			sx: { mb: 2.5 }
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			direction: "row",
			gap: 1
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Tooltip, { title: (0, _wordpress_i18n.__)("Undo", "elementor") }, /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, {
			variant: "outlined",
			color: "secondary",
			onClick: function onClick() {
				return sketchRef.current.undo();
			},
			sx: { px: 0 }
		}, /*#__PURE__*/ react.default.createElement(StyledUndoIcon, null))), /*#__PURE__*/ react.default.createElement(_elementor_ui.Tooltip, { title: (0, _wordpress_i18n.__)("Redo", "elementor") }, /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, {
			variant: "outlined",
			color: "secondary",
			onClick: function onClick() {
				return sketchRef.current.redo();
			},
			sx: { px: 0 }
		}, /*#__PURE__*/ react.default.createElement(StyledRedoIcon, null)))), /*#__PURE__*/ react.default.createElement(_elementor_ui.FormControl, {
			size: "small",
			color: "secondary",
			sx: { minWidth: STROKE_SELECT_WIDTH }
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.InputLabel, { id: "stroke" }, "Stroke"), /*#__PURE__*/ react.default.createElement(_elementor_ui.Select, {
			autoWidth: true,
			label: "Stroke",
			value: stroke,
			id: "demo-simple-select",
			labelId: "demo-simple-select-label",
			onChange: function onChange(e) {
				return setStroke(e.target.value);
			},
			MenuProps: {
				PaperProps: { sx: { maxWidth: STROKE_SELECT_WIDTH } },
				MenuListProps: { sx: { minWidth: STROKE_SELECT_WIDTH } }
			},
			sx: { "& .MuiSelect-select .MuiListItemIcon-root": {
				mr: .25,
				width: "initial",
				minWidth: "initial",
				justifyContent: "flex-start"
			} }
		}, [
			10,
			20,
			30,
			40,
			50
		].map(function(value) {
			return /*#__PURE__*/ react.default.createElement(_elementor_ui.MenuItem, {
				key: "stroke-width-option-" + value,
				value
			}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
				direction: "row",
				alignItems: "center",
				gap: 1
			}, /*#__PURE__*/ react.default.createElement(_elementor_ui.ListItemIcon, { sx: {
				width: 30,
				display: "flex",
				justifyContent: "center"
			} }, /*#__PURE__*/ react.default.createElement(BrishSizeIcon, { size: value })), /*#__PURE__*/ react.default.createElement(_elementor_ui.ListItemText, null, value)));
		})))), /*#__PURE__*/ react.default.createElement("div", { style: {
			margin: "0 auto",
			position: "relative",
			cursor: "none",
			overflow: "hidden"
		} }, /*#__PURE__*/ react.default.createElement(BrushCursor, {
			ref: brushCursorRef,
			size: stroke
		}), /*#__PURE__*/ react.default.createElement(ReactSketchCanvas, {
			className: "eui-in-painting-canvas",
			withViewBox: true,
			ref: sketchRef,
			height: canvasHeight + "px",
			width: canvasWidth + "px",
			strokeWidth: stroke,
			strokeColor: BRUSH_COLOR,
			canvasColor: CANVAS_COLOR,
			backgroundImage: editImage.url,
			onStroke: function onStroke() {
				return setIsCanvasChanged(true);
			},
			onChange: /*#__PURE__*/ _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee() {
				var svg;
				return import_regenerator$2.default.wrap(function(_context) {
					while (1) switch (_context.prev = _context.next) {
						case 0:
							_context.next = 1;
							return sketchRef.current.exportSvg();
						case 1:
							svg = _context.sent;
							setMask(svg);
						case 2:
						case "end": return _context.stop();
					}
				}, _callee);
			}))
		})));
	};
	CleanupContent.propTypes = {
		setMask: import_prop_types$7.default.func.isRequired,
		setIsCanvasChanged: import_prop_types$7.default.func.isRequired,
		width: import_prop_types$7.default.number.isRequired,
		height: import_prop_types$7.default.number.isRequired,
		editImage: import_prop_types$7.default.object.isRequired
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/cleanup/hooks/use-cleanup.js
	init_api();
	var useCleanup = function useCleanup(initialValue) {
		return useImagePrompt(function fetchAction(payload) {
			return getImageToImageMaskCleanup(payload);
		}, initialValue);
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/cleanup/index.js
	init_asyncToGenerator();
	init_slicedToArray();
	init_requests_ids();
	var Cleanup = function Cleanup() {
		var setGenerate = useRequestIds().setGenerate;
		var _useState2 = _slicedToArray((0, react.useState)(""), 2);
		var mask = _useState2[0];
		var setMask = _useState2[1];
		var _useState4 = _slicedToArray((0, react.useState)(false), 2);
		var isCanvasChanged = _useState4[0];
		var setIsCanvasChanged = _useState4[1];
		var _usePromptSettings = usePromptSettings();
		var settings = _usePromptSettings.settings;
		var resetSettings = _usePromptSettings.resetSettings;
		var _useEditImage = useEditImage();
		var editImage = _useEditImage.editImage;
		var width = _useEditImage.width;
		var height = _useEditImage.height;
		var _useImageActions = useImageActions();
		var use = _useImageActions.use;
		var edit = _useImageActions.edit;
		var isUploading = _useImageActions.isLoading;
		var _useCleanup = useCleanup();
		var data = _useCleanup.data;
		var send = _useCleanup.send;
		var isGenerating = _useCleanup.isLoading;
		var error = _useCleanup.error;
		var reset = _useCleanup.reset;
		var isLoading = isGenerating || isUploading;
		var handleSubmit = /*#__PURE__*/ function() {
			var _ref = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee(event) {
				return import_regenerator$2.default.wrap(function(_context) {
					while (1) switch (_context.prev = _context.next) {
						case 0:
							event.preventDefault();
							setGenerate();
							send({
								settings,
								image: editImage,
								mask
							});
						case 1:
						case "end": return _context.stop();
					}
				}, _callee);
			}));
			return function handleSubmit(_x) {
				return _ref.apply(this, arguments);
			};
		}();
		return /*#__PURE__*/ react.default.createElement(View, null, /*#__PURE__*/ react.default.createElement(View.Panel, null, /*#__PURE__*/ react.default.createElement(View.BackButton, null), /*#__PURE__*/ react.default.createElement(View.PanelHeading, {
			primary: (0, _wordpress_i18n.__)("Cleanup", "elementor"),
			secondary: (0, _wordpress_i18n.__)("Remove unwanted area from your image.", "elementor")
		}), error && /*#__PURE__*/ react.default.createElement(View.ErrorMessage, {
			error,
			onRetry: handleSubmit
		}), /*#__PURE__*/ react.default.createElement(ImageForm, { onSubmit: handleSubmit }, data !== null && data !== void 0 && data.result ? /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			gap: 2,
			sx: { my: 2.5 }
		}, /*#__PURE__*/ react.default.createElement(GenerateAgainSubmit, { disabled: isLoading }), /*#__PURE__*/ react.default.createElement(NewPromptButton, {
			disabled: isLoading,
			onClick: function onClick() {
				resetSettings();
				reset();
			}
		})) : /*#__PURE__*/ react.default.createElement(GenerateSubmit, { disabled: isLoading || !isCanvasChanged }))), /*#__PURE__*/ react.default.createElement(View.Content, { isGenerating: isLoading }, data !== null && data !== void 0 && data.result ? /*#__PURE__*/ react.default.createElement(ImagesDisplay, {
			images: data.result,
			aspectRatio: editImage.aspectRatio,
			onUseImage: use,
			onEditImage: edit
		}) : /*#__PURE__*/ react.default.createElement(CleanupContent, {
			editImage,
			width,
			height,
			setMask,
			setIsCanvasChanged
		})));
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/generate/hooks/use-text-to-image.js
	init_api();
	var useTextToImage = function useTextToImage(initialValue) {
		return useImagePrompt(getTextToImageGeneration, initialValue);
	};

//#endregion
//#region node_modules/react-colorful/dist/index.mjs
	function u() {
		return (u = Object.assign || function(e) {
			for (var r = 1; r < arguments.length; r++) {
				var t = arguments[r];
				for (var n in t) Object.prototype.hasOwnProperty.call(t, n) && (e[n] = t[n]);
			}
			return e;
		}).apply(this, arguments);
	}
	function c(e, r) {
		if (null == e) return {};
		var t;
		var n;
		var o = {};
		var a = Object.keys(e);
		for (n = 0; n < a.length; n++) r.indexOf(t = a[n]) >= 0 || (o[t] = e[t]);
		return o;
	}
	function i(e) {
		var t = (0, react$1.useRef)(e);
		var n = (0, react$1.useRef)(function(e) {
			t.current && t.current(e);
		});
		return t.current = e, n.current;
	}
	var s = function(e, r, t) {
		return void 0 === r && (r = 0), void 0 === t && (t = 1), e > t ? t : e < r ? r : e;
	};
	var f = function(e) {
		return "touches" in e;
	};
	var v = function(e) {
		return e && e.ownerDocument.defaultView || self;
	};
	var d = function(e, r, t) {
		var n = e.getBoundingClientRect();
		var o = f(r) ? function(e, r) {
			for (var t = 0; t < e.length; t++) if (e[t].identifier === r) return e[t];
			return e[0];
		}(r.touches, t) : r;
		return {
			left: s((o.pageX - (n.left + v(e).pageXOffset)) / n.width),
			top: s((o.pageY - (n.top + v(e).pageYOffset)) / n.height)
		};
	};
	var h = function(e) {
		!f(e) && e.preventDefault();
	};
	var m$1 = react$1.default.memo(function(o) {
		var a = o.onMove;
		var l = o.onKey;
		var s = c(o, ["onMove", "onKey"]);
		var m = (0, react$1.useRef)(null);
		var g = i(a);
		var p = i(l);
		var b = (0, react$1.useRef)(null);
		var _ = (0, react$1.useRef)(!1);
		var x = (0, react$1.useMemo)(function() {
			var e = function(e) {
				h(e), (f(e) ? e.touches.length > 0 : e.buttons > 0) && m.current ? g(d(m.current, e, b.current)) : t(!1);
			};
			var r = function() {
				return t(!1);
			};
			function t(t) {
				var n = _.current;
				var o = v(m.current);
				var a = t ? o.addEventListener : o.removeEventListener;
				a(n ? "touchmove" : "mousemove", e), a(n ? "touchend" : "mouseup", r);
			}
			return [
				function(e) {
					var r = e.nativeEvent;
					var n = m.current;
					if (n && (h(r), !function(e, r) {
						return r && !f(e);
					}(r, _.current) && n)) {
						if (f(r)) {
							_.current = !0;
							var o = r.changedTouches || [];
							o.length && (b.current = o[0].identifier);
						}
						n.focus(), g(d(n, r, b.current)), t(!0);
					}
				},
				function(e) {
					var r = e.which || e.keyCode;
					r < 37 || r > 40 || (e.preventDefault(), p({
						left: 39 === r ? .05 : 37 === r ? -.05 : 0,
						top: 40 === r ? .05 : 38 === r ? -.05 : 0
					}));
				},
				t
			];
		}, [p, g]);
		var C = x[0];
		var E = x[1];
		var H = x[2];
		return (0, react$1.useEffect)(function() {
			return H;
		}, [H]), react$1.default.createElement("div", u({}, s, {
			onTouchStart: C,
			onMouseDown: C,
			className: "react-colorful__interactive",
			ref: m,
			onKeyDown: E,
			tabIndex: 0,
			role: "slider"
		}));
	});
	var g = function(e) {
		return e.filter(Boolean).join(" ");
	};
	var p = function(r) {
		var t = r.color;
		var n = r.left;
		var o = r.top;
		var a = void 0 === o ? .5 : o;
		var l = g(["react-colorful__pointer", r.className]);
		return react$1.default.createElement("div", {
			className: l,
			style: {
				top: 100 * a + "%",
				left: 100 * n + "%"
			}
		}, react$1.default.createElement("div", {
			className: "react-colorful__pointer-fill",
			style: { backgroundColor: t }
		}));
	};
	var b = function(e, r, t) {
		return void 0 === r && (r = 0), void 0 === t && (t = Math.pow(10, r)), Math.round(t * e) / t;
	};
	var _ = {
		grad: .9,
		turn: 360,
		rad: 360 / (2 * Math.PI)
	};
	var x = function(e) {
		return L(C(e));
	};
	var C = function(e) {
		return "#" === e[0] && (e = e.substring(1)), e.length < 6 ? {
			r: parseInt(e[0] + e[0], 16),
			g: parseInt(e[1] + e[1], 16),
			b: parseInt(e[2] + e[2], 16),
			a: 4 === e.length ? b(parseInt(e[3] + e[3], 16) / 255, 2) : 1
		} : {
			r: parseInt(e.substring(0, 2), 16),
			g: parseInt(e.substring(2, 4), 16),
			b: parseInt(e.substring(4, 6), 16),
			a: 8 === e.length ? b(parseInt(e.substring(6, 8), 16) / 255, 2) : 1
		};
	};
	var w = function(e) {
		return K(I(e));
	};
	var y = function(e) {
		var r = e.s;
		var t = e.v;
		var n = e.a;
		var o = (200 - r) * t / 100;
		return {
			h: b(e.h),
			s: b(o > 0 && o < 200 ? r * t / 100 / (o <= 100 ? o : 200 - o) * 100 : 0),
			l: b(o / 2),
			a: b(n, 2)
		};
	};
	var q = function(e) {
		var r = y(e);
		return "hsl(" + r.h + ", " + r.s + "%, " + r.l + "%)";
	};
	var I = function(e) {
		var r = e.h;
		var t = e.s;
		var n = e.v;
		var o = e.a;
		r = r / 360 * 6, t /= 100, n /= 100;
		var a = Math.floor(r);
		var l = n * (1 - t);
		var u = n * (1 - (r - a) * t);
		var c = n * (1 - (1 - r + a) * t);
		var i = a % 6;
		return {
			r: b(255 * [
				n,
				u,
				l,
				l,
				c,
				n
			][i]),
			g: b(255 * [
				c,
				n,
				n,
				u,
				l,
				l
			][i]),
			b: b(255 * [
				l,
				l,
				c,
				n,
				n,
				u
			][i]),
			a: b(o, 2)
		};
	};
	var D = function(e) {
		var r = e.toString(16);
		return r.length < 2 ? "0" + r : r;
	};
	var K = function(e) {
		var r = e.r;
		var t = e.g;
		var n = e.b;
		var o = e.a;
		var a = o < 1 ? D(b(255 * o)) : "";
		return "#" + D(r) + D(t) + D(n) + a;
	};
	var L = function(e) {
		var r = e.r;
		var t = e.g;
		var n = e.b;
		var o = e.a;
		var a = Math.max(r, t, n);
		var l = a - Math.min(r, t, n);
		var u = l ? a === r ? (t - n) / l : a === t ? 2 + (n - r) / l : 4 + (r - t) / l : 0;
		return {
			h: b(60 * (u < 0 ? u + 6 : u)),
			s: b(a ? l / a * 100 : 0),
			v: b(a / 255 * 100),
			a: o
		};
	};
	var S = react$1.default.memo(function(r) {
		var t = r.hue;
		var n = r.onChange;
		var o = g(["react-colorful__hue", r.className]);
		return react$1.default.createElement("div", { className: o }, react$1.default.createElement(m$1, {
			onMove: function(e) {
				n({ h: 360 * e.left });
			},
			onKey: function(e) {
				n({ h: s(t + 360 * e.left, 0, 360) });
			},
			"aria-label": "Hue",
			"aria-valuenow": b(t),
			"aria-valuemax": "360",
			"aria-valuemin": "0"
		}, react$1.default.createElement(p, {
			className: "react-colorful__hue-pointer",
			left: t / 360,
			color: q({
				h: t,
				s: 100,
				v: 100,
				a: 1
			})
		})));
	});
	var T = react$1.default.memo(function(r) {
		var t = r.hsva;
		var n = r.onChange;
		var o = { backgroundColor: q({
			h: t.h,
			s: 100,
			v: 100,
			a: 1
		}) };
		return react$1.default.createElement("div", {
			className: "react-colorful__saturation",
			style: o
		}, react$1.default.createElement(m$1, {
			onMove: function(e) {
				n({
					s: 100 * e.left,
					v: 100 - 100 * e.top
				});
			},
			onKey: function(e) {
				n({
					s: s(t.s + 100 * e.left, 0, 100),
					v: s(t.v - 100 * e.top, 0, 100)
				});
			},
			"aria-label": "Color",
			"aria-valuetext": "Saturation " + b(t.s) + "%, Brightness " + b(t.v) + "%"
		}, react$1.default.createElement(p, {
			className: "react-colorful__saturation-pointer",
			top: 1 - t.v / 100,
			left: t.s / 100,
			color: q(t)
		})));
	});
	var F = function(e, r) {
		if (e === r) return !0;
		for (var t in e) if (e[t] !== r[t]) return !1;
		return !0;
	};
	var X = function(e, r) {
		return e.toLowerCase() === r.toLowerCase() || F(C(e), C(r));
	};
	function Y(e, t, l) {
		var u = i(l);
		var c = (0, react$1.useState)(function() {
			return e.toHsva(t);
		});
		var s = c[0];
		var f = c[1];
		var v = (0, react$1.useRef)({
			color: t,
			hsva: s
		});
		(0, react$1.useEffect)(function() {
			if (!e.equal(t, v.current.color)) {
				var r = e.toHsva(t);
				v.current = {
					hsva: r,
					color: t
				}, f(r);
			}
		}, [t, e]), (0, react$1.useEffect)(function() {
			var r;
			F(s, v.current.hsva) || e.equal(r = e.fromHsva(s), v.current.color) || (v.current = {
				hsva: s,
				color: r
			}, u(r));
		}, [
			s,
			e,
			u
		]);
		return [s, (0, react$1.useCallback)(function(e) {
			f(function(r) {
				return Object.assign({}, r, e);
			});
		}, [])];
	}
	var R;
	var V = "undefined" != typeof window ? react$1.useLayoutEffect : react$1.useEffect;
	var $ = function() {
		return R || ("undefined" != typeof __webpack_nonce__ ? __webpack_nonce__ : void 0);
	};
	var J = /* @__PURE__ */ new Map();
	var Q = function(e) {
		V(function() {
			var r = e.current ? e.current.ownerDocument : document;
			if (void 0 !== r && !J.has(r)) {
				var t = r.createElement("style");
				t.innerHTML = ".react-colorful{position:relative;display:flex;flex-direction:column;width:200px;height:200px;-webkit-user-select:none;-moz-user-select:none;-ms-user-select:none;user-select:none;cursor:default}.react-colorful__saturation{position:relative;flex-grow:1;border-color:transparent;border-bottom:12px solid #000;border-radius:8px 8px 0 0;background-image:linear-gradient(0deg,#000,transparent),linear-gradient(90deg,#fff,hsla(0,0%,100%,0))}.react-colorful__alpha-gradient,.react-colorful__pointer-fill{content:\"\";position:absolute;left:0;top:0;right:0;bottom:0;pointer-events:none;border-radius:inherit}.react-colorful__alpha-gradient,.react-colorful__saturation{box-shadow:inset 0 0 0 1px rgba(0,0,0,.05)}.react-colorful__alpha,.react-colorful__hue{position:relative;height:24px}.react-colorful__hue{background:linear-gradient(90deg,red 0,#ff0 17%,#0f0 33%,#0ff 50%,#00f 67%,#f0f 83%,red)}.react-colorful__last-control{border-radius:0 0 8px 8px}.react-colorful__interactive{position:absolute;left:0;top:0;right:0;bottom:0;border-radius:inherit;outline:none;touch-action:none}.react-colorful__pointer{position:absolute;z-index:1;box-sizing:border-box;width:28px;height:28px;transform:translate(-50%,-50%);background-color:#fff;border:2px solid #fff;border-radius:50%;box-shadow:0 2px 4px rgba(0,0,0,.2)}.react-colorful__interactive:focus .react-colorful__pointer{transform:translate(-50%,-50%) scale(1.1)}.react-colorful__alpha,.react-colorful__alpha-pointer{background-color:#fff;background-image:url('data:image/svg+xml;charset=utf-8,<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" fill-opacity=\".05\"><path d=\"M8 0h8v8H8zM0 8h8v8H0z\"/></svg>')}.react-colorful__saturation-pointer{z-index:3}.react-colorful__hue-pointer{z-index:2}", J.set(r, t);
				var n = $();
				n && t.setAttribute("nonce", n), r.head.appendChild(t);
			}
		}, []);
	};
	var U = function(t) {
		var n = t.className;
		var o = t.colorModel;
		var a = t.color;
		var l = void 0 === a ? o.defaultColor : a;
		var i = t.onChange;
		var s = c(t, [
			"className",
			"colorModel",
			"color",
			"onChange"
		]);
		var f = (0, react$1.useRef)(null);
		Q(f);
		var v = Y(o, l, i);
		var d = v[0];
		var h = v[1];
		var m = g(["react-colorful", n]);
		return react$1.default.createElement("div", u({}, s, {
			ref: f,
			className: m
		}), react$1.default.createElement(T, {
			hsva: d,
			onChange: h
		}), react$1.default.createElement(S, {
			hue: d.h,
			onChange: h,
			className: "react-colorful__last-control"
		}));
	};
	var W = {
		defaultColor: "000",
		toHsva: x,
		fromHsva: function(e) {
			return w({
				h: e.h,
				s: e.s,
				v: e.v,
				a: 1
			});
		},
		equal: X
	};
	var Z = function(r) {
		return react$1.default.createElement(U, u({}, r, { colorModel: W }));
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/components/color-picker.js
	init_defineProperty$1();
	init_slicedToArray();
	function ownKeys$11(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	__name(ownKeys$11, "ownKeys");
	function _objectSpread$11(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$11(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$11(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	__name(_objectSpread$11, "_objectSpread");
	var ColorInput = function ColorInput(_ref) {
		var _elementorCommon$conf;
		var _elementorCommon;
		var label = _ref.label;
		var color = _ref.color;
		var onChange = _ref.onChange;
		var disabled = _ref.disabled;
		var _useState2 = _slicedToArray((0, react.useState)(false), 2);
		var isOpened = _useState2[0];
		var setIsOpened = _useState2[1];
		var anchorEl = (0, react.useRef)();
		var _useState4 = _slicedToArray((0, react.useState)(170001), 2);
		var zIndex = _useState4[0];
		var setZIndex = _useState4[1];
		var isRTL = (_elementorCommon$conf = (_elementorCommon = elementorCommon) === null || _elementorCommon === void 0 || (_elementorCommon = _elementorCommon.config) === null || _elementorCommon === void 0 ? void 0 : _elementorCommon.isRTL) !== null && _elementorCommon$conf !== void 0 ? _elementorCommon$conf : true;
		(0, react.useEffect)(function() {
			if (anchorEl.current) {
				var element = anchorEl.current;
				var currentZIndex = 0;
				while (element && element !== document.body) {
					var computedZIndex = window.getComputedStyle(element).zIndex;
					if (computedZIndex !== "auto") {
						currentZIndex = parseInt(computedZIndex, 10) || 0;
						break;
					}
					element = element.parentElement;
				}
				setZIndex(currentZIndex > 0 ? currentZIndex + 1 : 170001);
			}
		}, [isOpened]);
		var handleClick = function handleClick() {
			return setIsOpened(true);
		};
		var handleClose = function handleClose() {
			setIsOpened(false);
		};
		var handleKeyDown = function handleKeyDown(event) {
			if ("Enter" === event.key || " " === event.key) {
				event.preventDefault();
				handleClick();
			}
		};
		var handleTextFieldChange = function handleTextFieldChange(event) {
			onChange("#".concat(event.target.value));
		};
		var handleColorPickerChange = function handleColorPickerChange(colorValue) {
			onChange(colorValue);
		};
		var id = isOpened ? "simple-popover" : void 0;
		return /*#__PURE__*/ react.default.createElement("div", {
			className: "el-ai-color-input-wrapper",
			style: _objectSpread$11({
				display: "flex",
				alignItems: "center"
			}, disabled ? {
				pointerEvents: "none",
				opacity: .5
			} : {})
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.FormLabel, { sx: {
			whiteSpace: "nowrap",
			flex: 4
		} }, label), /*#__PURE__*/ react.default.createElement("button", {
			type: "button",
			onClick: handleClick,
			onKeyDown: handleKeyDown,
			style: {
				cursor: "pointer",
				borderRadius: "4px",
				marginRight: "8px",
				marginLeft: "8px",
				background: color,
				width: "100%",
				height: "auto",
				borderColor: "rgba(0, 0, 0, 0.23)",
				borderWidth: "1px",
				borderStyle: "solid",
				aspectRatio: "1 / 1",
				flex: 1.1
			},
			"aria-describedby": id,
			ref: anchorEl
		}), /*#__PURE__*/ react.default.createElement(StyledTextField, {
			value: color.substring(1, 7),
			color: "secondary",
			fullWidth: true,
			InputProps: {
				autoComplete: "off",
				color: "secondary",
				sx: { height: "100%" },
				startAdornment: /*#__PURE__*/ react.default.createElement(_elementor_ui.InputAdornment, { position: "start" }, /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, {
					variant: "body1",
					color: "secondary"
				}, "#"))
			},
			onChange: handleTextFieldChange,
			sx: {
				flex: 3,
				height: "40px"
			}
		}), /*#__PURE__*/ react.default.createElement(_elementor_ui.Popover, {
			id,
			anchorOrigin: {
				vertical: "bottom",
				horizontal: isRTL ? "right" : "left"
			},
			transformOrigin: {
				vertical: "top",
				horizontal: isRTL ? "right" : "left"
			},
			onClose: handleClose,
			open: isOpened,
			anchorEl: anchorEl.current,
			style: {
				zIndex,
				overflow: "hidden"
			}
		}, /*#__PURE__*/ react.default.createElement("div", {
			className: "el-ai-custom-color-picker",
			style: {
				height: "min-content",
				overflow: "hidden"
			}
		}, /*#__PURE__*/ react.default.createElement(Z, {
			color,
			onChange: handleColorPickerChange
		}))));
	};
	var StyledTextField = (0, _elementor_ui.styled)(_elementor_ui_TextField.default)(function() {
		return { ".wp-admin & .MuiInputBase-input, & .MuiInputBase-input:focus": {
			backgroundColor: "initial",
			boxShadow: "none",
			border: 0,
			color: "inherit",
			outline: 0,
			padding: 0,
			fontFamily: "inherit",
			minHeight: "2.4375em"
		} };
	});
	ColorInput.propTypes = {
		label: import_prop_types$7.default.string,
		color: import_prop_types$7.default.string,
		onChange: import_prop_types$7.default.func.isRequired,
		disabled: import_prop_types$7.default.bool
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/product-image-unification/hooks/use-product-image-unification.js
	init_api();
	var useProductImageUnification = function useProductImageUnification(initialValue) {
		return useImagePrompt((0, react.useRef)(function(payload) {
			return getProductImageUnification(payload, true);
		}).current, initialValue);
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/product-image-unification/components/product-image.js
	init_defineProperty$1();
	var ProductImage = function ProductImage(_ref) {
		var productId = _ref.productId;
		var ratio = _ref.ratio;
		var bgColor = _ref.bgColor;
		var image = _ref.image;
		var onUpdate = _ref.onUpdate;
		var isUploading = useImageActions().isLoading;
		var _useProductImageUnifi = useProductImageUnification(_defineProperty$1(_defineProperty$1(_defineProperty$1({ productId }, IMAGE_RATIO, ratio), IMAGE_BACKGROUND_COLOR, bgColor), "image", image));
		var data = _useProductImageUnifi.data;
		var isGenerating = _useProductImageUnifi.isLoading;
		var error = _useProductImageUnifi.error;
		var send = _useProductImageUnifi.send;
		var sendUsageData = _useProductImageUnifi.sendUsageData;
		(0, react.useEffect)(function() {
			if (onUpdate) onUpdate(data, isGenerating || isUploading, error, send, productId, ratio, bgColor, image, sendUsageData);
		}, [
			data,
			isGenerating,
			error,
			send,
			productId,
			ratio,
			bgColor,
			image,
			isUploading,
			onUpdate,
			sendUsageData
		]);
		return /*#__PURE__*/ react.default.createElement("div", { style: { visibility: "hidden" } });
	};
	ProductImage.propTypes = {
		productId: import_prop_types$7.default.number,
		ratio: import_prop_types$7.default.string,
		bgColor: import_prop_types$7.default.string,
		image: import_prop_types$7.default.object,
		onUpdate: import_prop_types$7.default.func
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/product-image-unification/index.js
	init_defineProperty$1();
	init_asyncToGenerator();
	init_slicedToArray();
	init_requests_ids();
	function ownKeys$10(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	__name(ownKeys$10, "ownKeys");
	function _objectSpread$10(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$10(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$10(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	__name(_objectSpread$10, "_objectSpread");
	var ProductImageUnification = function ProductImageUnification(_ref) {
		var _products$images$leng;
		var _products$images;
		var _Object$values;
		var _products$images$leng2;
		var _products$images2;
		var _products$images$leng3;
		var _products$images3;
		var _errorlessProducts$le;
		var _products$images4;
		var onClose = _ref.onClose;
		var setGenerate = useRequestIds().setGenerate;
		var products = useEditImage().editImage;
		var _usePromptSettings = usePromptSettings();
		var settings = _usePromptSettings.settings;
		var updateSettings = _usePromptSettings.updateSettings;
		var _useState2 = _slicedToArray((0, react.useState)({}), 2);
		var productsData = _useState2[0];
		var setProductsData = _useState2[1];
		var _useState4 = _slicedToArray((0, react.useState)({}), 2);
		var loadingMap = _useState4[0];
		var setLoadingMap = _useState4[1];
		var _useState6 = _slicedToArray((0, react.useState)({}), 2);
		var errorMap = _useState6[0];
		var setErrorMap = _useState6[1];
		var _useState8 = _slicedToArray((0, react.useState)({}), 2);
		var checkboxColorMap = _useState8[0];
		var setCheckboxColorMap = _useState8[1];
		var _useState0 = _slicedToArray((0, react.useState)(false), 2);
		var wasGeneratedOnce = _useState0[0];
		var setWasGeneratedOnce = _useState0[1];
		var useMultipleImages = useImageActions().useMultipleImages;
		var _useState10 = _slicedToArray((0, react.useState)(false), 2);
		var isSavingImages = _useState10[0];
		var setIsSavingImages = _useState10[1];
		var errorlessProducts = Object.values(productsData).filter(function(_ref2) {
			var _errorMap$productId;
			return !((_errorMap$productId = errorMap[_ref2.productId]) !== null && _errorMap$productId !== void 0 && _errorMap$productId.errorGenerating);
		});
		var use = (0, react.useCallback)(/*#__PURE__*/ _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee() {
			var generatedImages;
			var imagesToSave;
			return import_regenerator$2.default.wrap(function(_context) {
				while (1) switch (_context.prev = _context.next) {
					case 0:
						setIsSavingImages(true);
						generatedImages = errorlessProducts.filter(function(_ref4) {
							var data = _ref4.data;
							return _ref4.wasGenerated && (data === null || data === void 0 ? void 0 : data.isChecked) && (data === null || data === void 0 ? void 0 : data.productId) !== void 0;
						});
						imagesToSave = generatedImages.map(function(_ref5) {
							var data = _ref5.data;
							return _objectSpread$10(_objectSpread$10({}, data), {}, {
								editor_post_id: data.productId,
								unique_id: "ai-product-unification-".concat(data.productId)
							});
						});
						_context.next = 1;
						return useMultipleImages(imagesToSave);
					case 1:
						generatedImages.forEach(function(_ref6) {
							var sendUsageData = _ref6.sendUsageData;
							sendUsageData();
						});
						onClose();
					case 2:
					case "end": return _context.stop();
				}
			}, _callee);
		})), [
			onClose,
			errorlessProducts,
			useMultipleImages
		]);
		var generatedAspectRatio = (0, react.useMemo)(function() {
			return settings[IMAGE_RATIO];
		}, [settings]);
		var generatedBgColor = (0, react.useMemo)(function() {
			return settings[IMAGE_BACKGROUND_COLOR];
		}, [settings]);
		var onProductUpdate = (0, react.useCallback)(function(res, isLoadingResult, errorGenerating, _req, productId, ratio, bgColor, image, sendUsageData) {
			setLoadingMap(function(prevState) {
				return _objectSpread$10(_objectSpread$10({}, prevState), {}, _defineProperty$1({}, productId, !!isLoadingResult));
			});
			setErrorMap(function(prevState) {
				return _objectSpread$10(_objectSpread$10({}, prevState), {}, _defineProperty$1({}, productId, { errorGenerating }));
			});
			setProductsData(function(prevState) {
				var _res$result;
				var _ref7;
				var _res$result$;
				var _res$result2;
				var _prevState$productId;
				var _prevState$productId$;
				var _prevState$productId2;
				var _checkboxColorMap$pro;
				return _objectSpread$10(_objectSpread$10({}, prevState), {}, _defineProperty$1({}, productId, _objectSpread$10(_objectSpread$10({}, prevState[productId]), {}, {
					productId,
					res,
					req: function req() {
						return _req({
							postId: productId,
							settings: _defineProperty$1(_defineProperty$1({}, IMAGE_RATIO, ratio), IMAGE_BACKGROUND_COLOR, bgColor),
							image
						});
					},
					sendUsageData,
					wasGenerated: (res === null || res === void 0 || (_res$result = res.result) === null || _res$result === void 0 ? void 0 : _res$result[0]) !== void 0,
					data: _objectSpread$10(_objectSpread$10({ productId }, !errorGenerating ? (_ref7 = (_res$result$ = res === null || res === void 0 || (_res$result2 = res.result) === null || _res$result2 === void 0 ? void 0 : _res$result2[0]) !== null && _res$result$ !== void 0 ? _res$result$ : image) !== null && _ref7 !== void 0 ? _ref7 : (_prevState$productId = prevState[productId]) === null || _prevState$productId === void 0 ? void 0 : _prevState$productId.image : {}), {}, {
						seed: productId,
						isChecked: (_prevState$productId$ = (_prevState$productId2 = prevState[productId]) === null || _prevState$productId2 === void 0 || (_prevState$productId2 = _prevState$productId2.data) === null || _prevState$productId2 === void 0 ? void 0 : _prevState$productId2.isChecked) !== null && _prevState$productId$ !== void 0 ? _prevState$productId$ : true,
						isLoading: isLoadingResult,
						checkboxColor: (_checkboxColorMap$pro = checkboxColorMap[productId]) !== null && _checkboxColorMap$pro !== void 0 ? _checkboxColorMap$pro : "rgba(0, 0, 0, 0.54)"
					})
				})));
			});
		}, [checkboxColorMap]);
		var isLoading = errorlessProducts.some(function(_ref8) {
			return loadingMap[_ref8.productId];
		});
		var isError = Object.values(errorMap).length && Object.values(errorMap).every(function(_ref9) {
			return _ref9.errorGenerating;
		});
		var thumbnailThreshold = 10;
		var exceedThumbnailThreshold = ((_products$images$leng = products === null || products === void 0 || (_products$images = products.images) === null || _products$images === void 0 ? void 0 : _products$images.length) !== null && _products$images$leng !== void 0 ? _products$images$leng : 0) - thumbnailThreshold > 0;
		var selectedProducts = errorlessProducts.filter(function(_ref0) {
			var data = _ref0.data;
			return data.isChecked && !data.isLoading;
		});
		var handleSubmit = (0, react.useCallback)(function(event) {
			event.preventDefault();
			setGenerate();
			setWasGeneratedOnce(true);
			var isLightColor = function isLightColor(color) {
				var rgb = parseInt(color.slice(1), 16);
				var r = rgb >> 16 & 255;
				var g = rgb >> 8 & 255;
				var b = rgb >> 0 & 255;
				return .299 * r + .587 * g + .114 * b > 128;
			};
			var productsToUnify = (errorlessProducts.length ? errorlessProducts : Object.values(productsData)).filter(function(product) {
				var _product$data;
				return ((_product$data = product.data) === null || _product$data === void 0 ? void 0 : _product$data.isChecked) && product.req;
			});
			var newCheckboxColor = isLightColor(generatedBgColor) ? "rgba(0, 0, 0, 0.54)" : "rgba( 255, 255, 255, 0.7 )";
			if (productsToUnify.find(function(product) {
				return checkboxColorMap[product.productId] !== newCheckboxColor;
			})) setCheckboxColorMap(function(prevState) {
				var newColorMap = _objectSpread$10({}, prevState);
				productsToUnify.forEach(function(product) {
					newColorMap[product.productId] = newCheckboxColor;
				});
				return newColorMap;
			});
			productsToUnify.forEach(function(product) {
				return product.req().catch(function() {});
			});
		}, [
			checkboxColorMap,
			errorlessProducts,
			generatedBgColor,
			productsData,
			setGenerate
		]);
		return /*#__PURE__*/ react.default.createElement(View, null, /*#__PURE__*/ react.default.createElement(View.Panel, null, /*#__PURE__*/ react.default.createElement(View.PanelHeading, {
			primary: (0, _wordpress_i18n.__)("Unify images", "elementor"),
			secondary: (0, _wordpress_i18n.__)("Select a set of parameters and AI will automate your adjustments:", "elementor")
		}), isError && /*#__PURE__*/ react.default.createElement(View.ErrorMessage, {
			error: (_Object$values = Object.values(errorMap)) === null || _Object$values === void 0 || (_Object$values = _Object$values[0]) === null || _Object$values === void 0 ? void 0 : _Object$values.errorGenerating,
			onRetry: handleSubmit
		}), /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, { gap: 2 }, /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, { sx: {
			display: "flex",
			flexWrap: "wrap",
			width: "100%",
			"& .MuiAvatar-root": { margin: .5 }
		} }, products === null || products === void 0 ? void 0 : products.images.slice(0, exceedThumbnailThreshold ? thumbnailThreshold - 1 : thumbnailThreshold).map(function(img) {
			return /*#__PURE__*/ react.default.createElement(_elementor_ui.Avatar, {
				key: img.productId,
				alt: img.productId + "",
				src: img.image_url,
				variant: "square",
				sx: {
					width: 50,
					height: 50
				}
			});
		}), exceedThumbnailThreshold && /*#__PURE__*/ react.default.createElement(_elementor_ui.Avatar, {
			variant: "square",
			sx: {
				bgcolor: "lightgray",
				width: 50,
				height: 50
			}
		}, ((_products$images$leng2 = products === null || products === void 0 || (_products$images2 = products.images) === null || _products$images2 === void 0 ? void 0 : _products$images2.length) !== null && _products$images$leng2 !== void 0 ? _products$images$leng2 : 0) - (thumbnailThreshold - 1))), /*#__PURE__*/ react.default.createElement(ImageForm, { onSubmit: handleSubmit }, /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			gap: 2,
			sx: { my: 2.5 }
		}, /*#__PURE__*/ react.default.createElement(ColorInput, {
			label: (0, _wordpress_i18n.__)("Background Color", "elementor"),
			color: generatedBgColor,
			onChange: function onChange(color) {
				return updateSettings(_defineProperty$1({}, IMAGE_BACKGROUND_COLOR, color));
			},
			disabled: isLoading
		}), /*#__PURE__*/ react.default.createElement(ImageRatioSelect, {
			disabled: true,
			value: generatedAspectRatio,
			onChange: function onChange(event) {
				return updateSettings(_defineProperty$1({}, IMAGE_RATIO, event.target.value));
			}
		}), /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			gap: 2,
			sx: { my: 2.5 }
		}, wasGeneratedOnce ? /*#__PURE__*/ react.default.createElement(GenerateAgainSubmit, { disabled: isLoading }) : /*#__PURE__*/ react.default.createElement(GenerateSubmit, { disabled: isLoading })))))), /*#__PURE__*/ react.default.createElement(View.Content, { isGenerating: isSavingImages }, /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, { sx: {
			display: "flex",
			flexDirection: "column",
			height: "100%",
			alignItems: "center",
			justifyContent: "center",
			minHeight: "76vh"
		} }, /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, { sx: {
			flex: 1,
			overflowY: "auto",
			display: "flex",
			flexDirection: "column",
			height: "100%",
			alignItems: "center",
			justifyContent: "center",
			overflowX: "hidden"
		} }, wasGeneratedOnce ? /*#__PURE__*/ react.default.createElement(react.default.Fragment, null, errorlessProducts.length > 1 && errorlessProducts.some(function(_ref1) {
			return !_ref1.data.isLoading;
		}) && /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, { sx: {
			display: "flex",
			flexDirection: "row",
			alignItems: "inherit",
			justifyContent: "space-between",
			width: "100%"
		} }, /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, {
			variant: "body2",
			color: "secondary"
		}, (0, _wordpress_i18n.sprintf)((0, _wordpress_i18n.__)("%1$d/%2$d selected", "elementor"), selectedProducts.length, (_products$images$leng3 = products === null || products === void 0 || (_products$images3 = products.images) === null || _products$images3 === void 0 ? void 0 : _products$images3.length) !== null && _products$images$leng3 !== void 0 ? _products$images$leng3 : 0)), /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, {
			variant: "text",
			color: "secondary",
			onClick: function onClick() {
				var isChecked = !selectedProducts.some(function(product) {
					var _product$data2;
					return (_product$data2 = product.data) === null || _product$data2 === void 0 ? void 0 : _product$data2.isChecked;
				});
				setProductsData(function(prevState) {
					var newState = _objectSpread$10({}, prevState);
					Object.values(newState).forEach(function(product) {
						var _product$data3;
						if (isChecked !== ((_product$data3 = product.data) === null || _product$data3 === void 0 ? void 0 : _product$data3.isChecked)) newState[product.productId] = _objectSpread$10(_objectSpread$10({}, product), {}, { data: _objectSpread$10(_objectSpread$10({}, product.data), {}, { isChecked }) });
					});
					return newState;
				});
			}
		}, selectedProducts.some(function(product) {
			var _product$data4;
			return (_product$data4 = product.data) === null || _product$data4 === void 0 ? void 0 : _product$data4.isChecked;
		}) ? (0, _wordpress_i18n.__)("Clear all", "elementor") : (0, _wordpress_i18n.__)("Select all", "elementor"))), /*#__PURE__*/ react.default.createElement(ImagesDisplay, {
			images: errorlessProducts.map(function(product) {
				return product.data;
			}),
			cols: function getCols() {
				var dataLength = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 1;
				return Math.min(Math.ceil(Math.sqrt(dataLength !== null && dataLength !== void 0 ? dataLength : 1)), 4);
			}((_errorlessProducts$le = errorlessProducts.length) !== null && _errorlessProducts$le !== void 0 ? _errorlessProducts$le : 1),
			overlay: false,
			onSelectChange: function onSelectChange(productId, isChecked) {
				return setProductsData(function(prevState) {
					var _prevState$productId3;
					if ((_prevState$productId3 = prevState[productId]) !== null && _prevState$productId3 !== void 0 && _prevState$productId3.data) prevState[productId].data.isChecked = isChecked;
					return _objectSpread$10({}, prevState);
				});
			}
		})) : /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, {
			component: "img",
			src: window.UnifyProductImagesConfig.placeholder,
			alt: (0, _wordpress_i18n.__)("Example GIF", "elementor")
		})), wasGeneratedOnce && !isError && errorlessProducts.length && errorlessProducts.some(function(_ref10) {
			var data = _ref10.data;
			var errorGenerating = _ref10.errorGenerating;
			var wasGenerated = _ref10.wasGenerated;
			return (data === null || data === void 0 ? void 0 : data.isChecked) && !(data !== null && data !== void 0 && data.isLoadingResult) && !errorGenerating && wasGenerated;
		}) && /*#__PURE__*/ react.default.createElement(ImageActions.UseImage, {
			onClick: use,
			sx: {
				alignSelf: "flex-end",
				mt: 2
			}
		}))), (_products$images4 = products.images) === null || _products$images4 === void 0 ? void 0 : _products$images4.filter(function(img) {
			return img.productId;
		}).map(function(img) {
			return /*#__PURE__*/ react.default.createElement(ProductImage, {
				key: "product-".concat(img.productId),
				productId: img.productId,
				ratio: settings[IMAGE_RATIO],
				bgColor: settings[IMAGE_BACKGROUND_COLOR],
				image: img,
				onUpdate: onProductUpdate
			});
		}));
	};
	ProductImageUnification.propTypes = { onClose: import_prop_types$7.default.func };

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/isolate-objects/hooks/use-isolate-objects.js
	init_api();
	var useIsolateObject = function useIsolateObject(initialValue) {
		return useImagePrompt(function fetchAction(payload) {
			return getImageToImageIsolateObjects(payload);
		}, initialValue);
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/views/isolate-objects/index.js
	init_defineProperty$1();
	init_requests_ids();
	var IsolateObject = function IsolateObject() {
		var editImage = useEditImage().editImage;
		var setGenerate = useRequestIds().setGenerate;
		var _useImageActions = useImageActions();
		var use = _useImageActions.use;
		var edit = _useImageActions.edit;
		var isUploading = _useImageActions.isLoading;
		var _usePromptSettings = usePromptSettings();
		var settings = _usePromptSettings.settings;
		var updateSettings = _usePromptSettings.updateSettings;
		var _useIsolateObject = useIsolateObject();
		var data = _useIsolateObject.data;
		var send = _useIsolateObject.send;
		var isGenerating = _useIsolateObject.isLoading;
		var error = _useIsolateObject.error;
		var isLoading = isGenerating || isUploading;
		var _useImageSize = useImageSize(editImage.aspectRatio);
		var width = _useImageSize.width;
		var height = _useImageSize.height;
		var generatedAspectRatio = (0, react.useMemo)(function() {
			return settings[IMAGE_RATIO];
		}, [settings]);
		var generatedBgColor = (0, react.useMemo)(function() {
			return settings[IMAGE_BACKGROUND_COLOR];
		}, [settings]);
		var handleSubmit = function handleSubmit(event) {
			event.preventDefault();
			setGenerate();
			send({
				image: editImage,
				settings: _defineProperty$1(_defineProperty$1({}, IMAGE_RATIO, generatedAspectRatio), IMAGE_BACKGROUND_COLOR, generatedBgColor),
				featureIdentifier: FEATURE_IDENTIFIER.ISOLATE_OBJECT
			});
		};
		return /*#__PURE__*/ react.default.createElement(View, null, /*#__PURE__*/ react.default.createElement(View.Panel, null, /*#__PURE__*/ react.default.createElement(View.BackButton, null), /*#__PURE__*/ react.default.createElement(View.PanelHeading, {
			primary: (0, _wordpress_i18n.__)("Isolate Object", "elementor"),
			secondary: (0, _wordpress_i18n.__)("Remove the background, center your object, and add a drop shadow for a clean look.", "elementor")
		}), error && /*#__PURE__*/ react.default.createElement(View.ErrorMessage, {
			error,
			onRetry: handleSubmit
		}), /*#__PURE__*/ react.default.createElement(ImageForm, { onSubmit: handleSubmit }, /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			gap: 2,
			sx: { my: 2.5 }
		}, /*#__PURE__*/ react.default.createElement(ColorInput, {
			label: (0, _wordpress_i18n.__)("Background Color", "elementor"),
			color: generatedBgColor,
			onChange: function onChange(color) {
				return updateSettings(_defineProperty$1({}, IMAGE_BACKGROUND_COLOR, color));
			},
			disabled: isLoading
		}), /*#__PURE__*/ react.default.createElement(GenerateSubmit, {
			disabled: isLoading,
			loading: isLoading
		}, (0, _wordpress_i18n.__)("Isolate Object", "elementor"))))), /*#__PURE__*/ react.default.createElement(View.Content, { isGenerating: isLoading }, data !== null && data !== void 0 && data.result ? /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, null, /*#__PURE__*/ react.default.createElement(ImagesDisplay, {
			transparentContainer: true,
			onUseImage: use,
			onEditImage: edit,
			images: data.result,
			aspectRatio: editImage.aspectRatio
		})) : /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, null, /*#__PURE__*/ react.default.createElement(SingleImagePreview, null, /*#__PURE__*/ react.default.createElement(SingleImagePreview.Image, {
			src: editImage.url,
			alt: editImage.alt,
			style: {
				width,
				height
			}
		})))));
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/media-outlet.js
	init_loader();
	init_prompt_history_action_context();
	var MediaOutlet = function MediaOutlet(_ref) {
		var _ref$additionalOption = _ref.additionalOptions;
		var additionalOptions = _ref$additionalOption === void 0 ? null : _ref$additionalOption;
		var _ref$onClose = _ref.onClose;
		var onClose = _ref$onClose === void 0 ? null : _ref$onClose;
		var editImage = useEditImage().editImage;
		var _useLocation = useLocation({ current: (additionalOptions === null || additionalOptions === void 0 ? void 0 : additionalOptions.location) || LOCATIONS.GENERATE });
		var current = _useLocation.current;
		var navigate = _useLocation.navigate;
		(0, react.useEffect)(function() {
			if ((additionalOptions === null || additionalOptions === void 0 ? void 0 : additionalOptions.location) && additionalOptions.location !== LOCATIONS.GENERATE) {
				navigate(additionalOptions.location);
				return;
			}
			if (editImage.url) if (new RegExp(IMAGE_PLACEHOLDERS_HOSTS.WIREFRAME).test(new URL(editImage.url).host)) navigate(LOCATIONS.GENERATE);
			else if ("url" === editImage.source || editImage.id) navigate(LOCATIONS.IMAGE_TOOLS);
			else navigate(LOCATIONS.GENERATE);
		}, [
			editImage.id,
			editImage.url,
			editImage.source,
			additionalOptions === null || additionalOptions === void 0 ? void 0 : additionalOptions.location
		]);
		useSubscribeOnPromptHistoryAction([{
			type: ACTION_TYPES.RESTORE,
			handler: function handler() {
				if (current !== LOCATIONS.GENERATE) navigate(LOCATIONS.GENERATE);
			}
		}]);
		if (!current) return /*#__PURE__*/ react.default.createElement(View, { alignItems: "center" }, /*#__PURE__*/ react.default.createElement(Loader, null));
		return /*#__PURE__*/ react.default.createElement(react.default.Fragment, null, current === LOCATIONS.GENERATE && /*#__PURE__*/ react.default.createElement(Generate, {
			textToImageHook: additionalOptions !== null && additionalOptions !== void 0 && additionalOptions.textToImageHook ? additionalOptions === null || additionalOptions === void 0 ? void 0 : additionalOptions.textToImageHook : useTextToImage,
			predefinedPrompt: additionalOptions === null || additionalOptions === void 0 ? void 0 : additionalOptions.predefinedPrompt,
			initialSettings: additionalOptions === null || additionalOptions === void 0 ? void 0 : additionalOptions.initialSettings
		}), current === LOCATIONS.IMAGE_TOOLS && /*#__PURE__*/ react.default.createElement(ImageTools, null), current === LOCATIONS.VARIATIONS && /*#__PURE__*/ react.default.createElement(Variations, null), current === LOCATIONS.IN_PAINTING && /*#__PURE__*/ react.default.createElement(InPainting, null), current === LOCATIONS.OUT_PAINTING && /*#__PURE__*/ react.default.createElement(OutPainting, null), current === LOCATIONS.RESIZE && /*#__PURE__*/ react.default.createElement(Resize, null), current === LOCATIONS.REPLACE_BACKGROUND && /*#__PURE__*/ react.default.createElement(ReplaceBackground, null), current === LOCATIONS.REMOVE_BACKGROUND && /*#__PURE__*/ react.default.createElement(RemoveBackground, null), current === LOCATIONS.CLEANUP && /*#__PURE__*/ react.default.createElement(Cleanup, null), current === LOCATIONS.PRODUCT_IMAGE_UNIFICATION && /*#__PURE__*/ react.default.createElement(ProductImageUnification, { onClose }), current === LOCATIONS.ISOLATE_OBJECT && /*#__PURE__*/ react.default.createElement(IsolateObject, null));
	};
	MediaOutlet.propTypes = {
		additionalOptions: import_prop_types$7.default.object,
		onClose: import_prop_types$7.default.func
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/components/unsaved-changes-alert.js
	init_extends();
	init_objectWithoutProperties$1();
	var _excluded$13 = ["onClose", "onCancel"];
	var UnsavedChangesAlert$1 = function UnsavedChangesAlert(_ref) {
		var onClose = _ref.onClose;
		var onCancel = _ref.onCancel;
		var props = _objectWithoutProperties$1(_ref, _excluded$13);
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Dialog, _extends$1({
			sx: { zIndex: 2e5 },
			"aria-labelledby": "unsaved-changes-alert-title",
			"aria-describedby": "unsaved-changes-alert-description"
		}, props), /*#__PURE__*/ react.default.createElement(_elementor_ui.DialogTitle, { id: "unsaved-changes-alert-title" }, (0, _wordpress_i18n.__)("Leave Elementor AI?", "elementor")), /*#__PURE__*/ react.default.createElement(_elementor_ui.DialogContent, null, /*#__PURE__*/ react.default.createElement(_elementor_ui.DialogContentText, { id: "unsaved-changes-alert-description" }, (0, _wordpress_i18n.__)("Images will be gone forever and we won’t be able to recover them.", "elementor"))), /*#__PURE__*/ react.default.createElement(_elementor_ui.DialogActions, null, /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, {
			onClick: onCancel,
			color: "secondary"
		}, (0, _wordpress_i18n.__)("Cancel", "elementor")), /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, {
			onClick: onClose,
			color: "error",
			variant: "contained"
		}, (0, _wordpress_i18n.__)("Yes, leave", "elementor"))));
	};
	UnsavedChangesAlert$1.propTypes = {
		onClose: import_prop_types$7.default.func,
		onCancel: import_prop_types$7.default.func
	};

//#endregion
//#region modules/ai/assets/js/editor/components/prompt-history/history-button.js
	init_extends();
	init_objectWithoutProperties$1();
	var _excluded$12 = ["isActive"];
	var _templateObject$8;
	var StyledHistoryButton = (0, _elementor_ui.styled)(_elementor_ui.IconButton)(_templateObject$8 || (_templateObject$8 = _taggedTemplateLiteral([
		"\n  margin-right: ",
		";\n\n  ",
		"\n"
	])), function(_ref) {
		return _ref.theme.spacing(2);
	}, function(_ref2) {
		var isActive = _ref2.isActive;
		var theme = _ref2.theme;
		return isActive && "\n    background-color: ".concat(theme.palette.action.hover, ";\n    color: ").concat(theme.palette.text.tertiary, ";\n  ");
	});
	var PromptHistoryButton = function PromptHistoryButton(_ref3) {
		var isActive = _ref3.isActive;
		var props = _objectWithoutProperties$1(_ref3, _excluded$12);
		return /*#__PURE__*/ react.default.createElement(StyledHistoryButton, _extends$1({
			"aria-label": (0, _wordpress_i18n.__)("Show prompt history", "elementor"),
			type: "button",
			size: "small",
			isActive
		}, props), /*#__PURE__*/ react.default.createElement(_elementor_icons.HistoryIcon, null));
	};
	PromptHistoryButton.propTypes = { isActive: import_prop_types$7.default.bool.isRequired };

//#endregion
//#region modules/ai/assets/js/editor/hooks/use-prompt-history.js
	init_asyncToGenerator();
	init_slicedToArray();
	init_defineProperty$1();
	init_api();
	function ownKeys$9(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	__name(ownKeys$9, "ownKeys");
	function _objectSpread$9(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$9(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$9(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	__name(_objectSpread$9, "_objectSpread");
	var getImageThumbnailURL = function getImageThumbnailURL(base, imageURL) {
		if (!base) return imageURL;
		return "".concat(base, "/?o=").concat(btoa(imageURL, false));
	};
	var normalizeItems = function normalizeItems(response) {
		var meta = response.meta;
		var items = response.items;
		return items === null || items === void 0 ? void 0 : items.map(function(item) {
			var _item$request;
			var _item$request2;
			var _item$request3;
			var _item$response;
			var _item$response2;
			var _item$response3;
			return {
				id: item === null || item === void 0 ? void 0 : item.elementorApiId,
				date: item === null || item === void 0 ? void 0 : item.createdAt,
				action: item === null || item === void 0 ? void 0 : item.action,
				prompt: item === null || item === void 0 || (_item$request = item.request) === null || _item$request === void 0 ? void 0 : _item$request.prompt,
				imageType: item === null || item === void 0 || (_item$request2 = item.request) === null || _item$request2 === void 0 ? void 0 : _item$request2.image_type,
				ratio: item === null || item === void 0 || (_item$request3 = item.request) === null || _item$request3 === void 0 ? void 0 : _item$request3.ratio,
				text: item === null || item === void 0 || (_item$response = item.response) === null || _item$response === void 0 || (_item$response = _item$response.results) === null || _item$response === void 0 ? void 0 : _item$response.text,
				images: item === null || item === void 0 || (_item$response2 = item.response) === null || _item$response2 === void 0 || (_item$response2 = _item$response2.results) === null || _item$response2 === void 0 ? void 0 : _item$response2.images,
				thumbnails: item === null || item === void 0 || (_item$response3 = item.response) === null || _item$response3 === void 0 || (_item$response3 = _item$response3.results) === null || _item$response3 === void 0 || (_item$response3 = _item$response3.images) === null || _item$response3 === void 0 ? void 0 : _item$response3.map(function(image) {
					return _objectSpread$9(_objectSpread$9({}, image), {}, { image_url: getImageThumbnailURL(meta.thumbnailUrl, image.image_url) });
				})
			};
		});
	};
	var usePromptHistory = function usePromptHistory(historyType) {
		var _useState2 = _slicedToArray((0, react.useState)(false), 2);
		var isLoading = _useState2[0];
		var setIsLoading = _useState2[1];
		var _useState4 = _slicedToArray((0, react.useState)({}), 2);
		var data = _useState4[0];
		var setData = _useState4[1];
		var _useState6 = _slicedToArray((0, react.useState)(""), 2);
		var error = _useState6[0];
		var setError = _useState6[1];
		var fetchData = /*#__PURE__*/ function() {
			var _ref2 = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee(_ref) {
				var page;
				var limit;
				return import_regenerator$2.default.wrap(function(_context) {
					while (1) switch (_context.prev = _context.next) {
						case 0:
							page = _ref.page, limit = _ref.limit;
							return _context.abrupt("return", new Promise(function(resolve, reject) {
								setIsLoading(true);
								getHistory(historyType, page, limit).then(function(response) {
									var clone = JSON.parse(JSON.stringify(data));
									clone.meta = response.meta;
									clone.items = [].concat(_toConsumableArray((clone === null || clone === void 0 ? void 0 : clone.items) || []), _toConsumableArray(normalizeItems(response)));
									setData(clone);
									resolve(true);
								}).catch(function(err) {
									var finalError = (err === null || err === void 0 ? void 0 : err.responseText) || err;
									setError(finalError);
									reject(finalError);
								}).finally(function() {
									return setIsLoading(false);
								});
							}));
						case 1:
						case "end": return _context.stop();
					}
				}, _callee);
			}));
			return function fetchData(_x) {
				return _ref2.apply(this, arguments);
			};
		}();
		return {
			isLoading,
			error,
			items: data.items,
			meta: data.meta,
			fetchData,
			deleteItemById: function deleteItemById(id) {
				var itemIndex = data.items.findIndex(function(item) {
					return item.id === id;
				});
				if (-1 === itemIndex) return false;
				var clone = JSON.parse(JSON.stringify(data));
				clone.items.splice(itemIndex, 1);
				setData(clone);
				return true;
			}
		};
	};

//#endregion
//#region modules/ai/assets/js/editor/components/prompt-history/parts/modal-header.js
	var StyledContent$2 = (0, _elementor_ui.styled)(_elementor_ui.Box)(function(_ref) {
		var theme = _ref.theme;
		return {
			marginTop: 0,
			padding: theme.spacing(1.5),
			borderBottom: "1px solid ".concat(theme.palette.action.focus),
			display: "flex",
			alignItems: "center",
			justifyContent: "space-between"
		};
	});
	var PromptHistoryModalHeader = function PromptHistoryModalHeader(_ref2) {
		var onClose = _ref2.onClose;
		return /*#__PURE__*/ react.default.createElement(StyledContent$2, null, /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, {
			fontWeight: "bolder",
			variant: "h6",
			sx: { userSelect: "none" }
		}, (0, _wordpress_i18n.__)("History", "elementor")), /*#__PURE__*/ react.default.createElement(_elementor_ui.IconButton, {
			size: "small",
			"aria-label": (0, _wordpress_i18n.__)("Hide prompt history", "elementor"),
			onClick: onClose
		}, /*#__PURE__*/ react.default.createElement(_elementor_icons.XIcon, { fontSize: "small" })));
	};
	PromptHistoryModalHeader.propTypes = { onClose: import_prop_types$7.default.func.isRequired };

//#endregion
//#region modules/ai/assets/js/editor/hooks/use-delete-prompt-history-item.js
	init_asyncToGenerator();
	init_slicedToArray();
	init_api();
	var useDeletePromptHistoryItem = function useDeletePromptHistoryItem() {
		var _useState2 = _slicedToArray((0, react.useState)(false), 2);
		var isLoading = _useState2[0];
		var setIsLoading = _useState2[1];
		var _useState4 = _slicedToArray((0, react.useState)(""), 2);
		var error = _useState4[0];
		var setError = _useState4[1];
		return {
			isLoading,
			error,
			deleteItem: /* @__PURE__ */ function() {
				var _ref = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee(id) {
					return import_regenerator$2.default.wrap(function(_context) {
						while (1) switch (_context.prev = _context.next) {
							case 0: return _context.abrupt("return", new Promise(function(resolve, reject) {
								setError("");
								setIsLoading(true);
								deleteHistoryItem(id).then(function() {
									resolve(true);
								}).catch(function(err) {
									var finalError = (err === null || err === void 0 ? void 0 : err.responseText) || err;
									setError(finalError);
									reject(finalError);
								}).finally(function() {
									return setIsLoading(false);
								});
							}));
							case 1:
							case "end": return _context.stop();
						}
					}, _callee);
				}));
				return function deleteItem(_x) {
					return _ref.apply(this, arguments);
				};
			}()
		};
	};

//#endregion
//#region modules/ai/assets/js/editor/helpers/date-helpers.js
/**
	* Formats a date (like wp_date() in PHP), translating it into site's locale.
	*
	* @param {string}                       format   PHP-style formatting string.
	* @param {Date | string | undefined}    date     Date object
	* @param {string | number | undefined=} timezone Timezone to output result in or a UTC offset. Defaults to timezone from site.
	* @return {string}
	*/
	var translateDate = function translateDate(format, date, timezone) {
		var _window$wp;
		if ((_window$wp = window.wp) !== null && _window$wp !== void 0 && _window$wp.date) {
			var _window$wp2;
			var dateI18n = ((_window$wp2 = window.wp) === null || _window$wp2 === void 0 ? void 0 : _window$wp2.date).dateI18n;
			return dateI18n(format, date, timezone);
		}
		return date;
	};
	/**
	* Returns difference in days between two dates
	*
	* @param {Date} firstDate
	* @param {Date} secondDate
	* @return {number} Positive or negative number in days
	*/
	var daysDiff = function daysDiff(firstDate, secondDate) {
		var MS_PER_DAY = 1e3 * 60 * 60 * 24;
		var firstUTC = Date.UTC(firstDate.getFullYear(), firstDate.getMonth(), firstDate.getDate());
		var secondUTC = Date.UTC(secondDate.getFullYear(), secondDate.getMonth(), secondDate.getDate());
		return Math.floor((secondUTC - firstUTC) / MS_PER_DAY);
	};

//#endregion
//#region modules/ai/assets/js/editor/icons/restore-icon.js
	init_extends();
	var RestoreIcon = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 24 24" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			d: "M11.913 3.22a8.781 8.781 0 0 1 3.367.634.75.75 0 1 1-.56 1.392A7.281 7.281 0 0 0 8.25 18.24V15a.75.75 0 0 1 1.5 0v4.433a.73.73 0 0 1 0 .034V20a.75.75 0 0 1-.75.75H4a.75.75 0 0 1 0-1.5h3.045a8.782 8.782 0 0 1 4.868-16.03ZM18.37 6.41a.75.75 0 0 1 .75.75v.01a.75.75 0 0 1-1.5 0v-.01a.75.75 0 0 1 .75-.75ZM13 19.19a.75.75 0 0 1 .75.75v.01a.75.75 0 0 1-1.5 0v-.01a.75.75 0 0 1 .75-.75ZM16.84 17.62a.75.75 0 0 1 .75.75v.01a.75.75 0 0 1-1.5 0v-.01a.75.75 0 0 1 .75-.75ZM19.37 14.35a.75.75 0 0 1 .75.75v.01a.75.75 0 0 1-1.5 0v-.01a.75.75 0 0 1 .75-.75ZM19.94 10.25a.75.75 0 0 1 .75.75v.01a.75.75 0 0 1-1.5 0V11a.75.75 0 0 1 .75-.75Z",
			clipRule: "evenodd"
		}));
	});

//#endregion
//#region modules/ai/assets/js/editor/components/tooltip.js
	init_extends();
	init_objectWithoutProperties$1();
	var _excluded$11 = ["children"];
	var Tooltip$2 = function Tooltip(_ref) {
		var children = _ref.children;
		var props = _objectWithoutProperties$1(_ref, _excluded$11);
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Tooltip, _extends$1({ componentsProps: { tooltip: { sx: {
			"&.MuiTooltip-tooltip[class*=\"MuiTooltip-tooltipPlacement\"]": {
				m: 0,
				fontSize: "10px",
				fontWeight: 500,
				lineHeight: "1.4em"
			},
			py: .5,
			px: 1
		} } } }, props), children);
	};
	Tooltip$2.propTypes = { children: import_prop_types$7.default.oneOfType([import_prop_types$7.default.arrayOf(import_prop_types$7.default.node), import_prop_types$7.default.node]) };

//#endregion
//#region modules/ai/assets/js/editor/components/prompt-history/parts/modal-item-action-button.js
	init_extends();
	init_objectWithoutProperties$1();
	var _excluded$10 = ["tooltipTitle"];
	var ActionButton = function ActionButton(_ref) {
		var tooltipTitle = _ref.tooltipTitle;
		var props = _objectWithoutProperties$1(_ref, _excluded$10);
		return /*#__PURE__*/ react.default.createElement(Tooltip$2, {
			title: tooltipTitle,
			placement: "top"
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.IconButton, _extends$1({
			type: "button",
			size: "small",
			disableRipple: true,
			disableFocusRipple: true,
			disableTouchRipple: true
		}, props)));
	};
	ActionButton.propTypes = { tooltipTitle: import_prop_types$7.default.string.isRequired };

//#endregion
//#region modules/ai/assets/js/editor/icons/text-icon.js
	init_extends();
	var TextIcon = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 24 24" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			d: "M4.25 5A.75.75 0 0 1 5 4.25h14a.75.75 0 0 1 .75.75v2a.75.75 0 0 1-1.5 0V5.75H5.75V7a.75.75 0 0 1-1.5 0V5Z",
			clipRule: "evenodd"
		}), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			d: "M12 4.25a.75.75 0 0 1 .75.75v14a.75.75 0 0 1-1.5 0V5a.75.75 0 0 1 .75-.75Z",
			clipRule: "evenodd"
		}), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			d: "M9.25 19a.75.75 0 0 1 .75-.75h4a.75.75 0 0 1 0 1.5h-4a.75.75 0 0 1-.75-.75Z",
			clipRule: "evenodd"
		}));
	});

//#endregion
//#region modules/ai/assets/js/editor/icons/code-icon.js
	init_extends();
	var CodeIcon = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 24 24" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			d: "M7.53 7.47a.75.75 0 0 1 0 1.06L4.06 12l3.47 3.47a.75.75 0 1 1-1.06 1.06l-4-4a.75.75 0 0 1 0-1.06l4-4a.75.75 0 0 1 1.06 0ZM16.47 7.47a.75.75 0 0 1 1.06 0l4 4a.75.75 0 0 1 0 1.06l-4 4a.75.75 0 1 1-1.06-1.06L19.94 12l-3.47-3.47a.75.75 0 0 1 0-1.06ZM14.182 3.272a.75.75 0 0 1 .546.91l-4 16a.75.75 0 0 1-1.456-.364l4-16a.75.75 0 0 1 .91-.546Z",
			clipRule: "evenodd"
		}));
	});

//#endregion
//#region modules/ai/assets/js/editor/components/prompt-history/parts/action-icon.js
	var FALLBACK_ICON = /*#__PURE__*/ react.default.createElement(_elementor_icons.AIIcon, { "data-testid": "e-ph-fi" });
	var ICONS_BY_ACTIONS = Object.freeze({
		"enhance-image-prompt": /*#__PURE__*/ react.default.createElement(TextIcon, null),
		"get-text": /*#__PURE__*/ react.default.createElement(TextIcon, null),
		"edit-text": /*#__PURE__*/ react.default.createElement(TextIcon, null),
		"custom-css": /*#__PURE__*/ react.default.createElement(CodeIcon, null),
		"custom-code": /*#__PURE__*/ react.default.createElement(CodeIcon, null)
	});
	var PromptHistoryActionIcon = function PromptHistoryActionIcon(_ref) {
		var action = _ref.action;
		if (ICONS_BY_ACTIONS[action]) return ICONS_BY_ACTIONS[action];
		return FALLBACK_ICON;
	};
	PromptHistoryActionIcon.propTypes = { action: import_prop_types$7.default.string.isRequired };

//#endregion
//#region modules/ai/assets/js/editor/components/prompt-history/parts/modal-item-title.js
	var _templateObject$7;
	var StyledTitle = (0, _elementor_ui.styled)(_elementor_ui.Typography)(_templateObject$7 || (_templateObject$7 = _taggedTemplateLiteral(["\n  margin-bottom: ", ";\n"])), function(_ref) {
		return _ref.theme.spacing(.5);
	});
	var PromptHistoryItemTitle = function PromptHistoryItemTitle(_ref2) {
		var prompt = _ref2.prompt;
		return /*#__PURE__*/ react.default.createElement(Tooltip$2, {
			title: prompt,
			arrow: false,
			placement: "bottom-start"
		}, /*#__PURE__*/ react.default.createElement(StyledTitle, {
			variant: "body2",
			noWrap: true,
			paragraph: true
		}, prompt));
	};
	PromptHistoryItemTitle.propTypes = { prompt: import_prop_types$7.default.string.isRequired };

//#endregion
//#region modules/ai/assets/js/editor/components/prompt-history/parts/modal-item-secondary-content.js
	var _templateObject$6;
	var _templateObject2$3;
	var StyledDateSubtitle = (0, _elementor_ui.styled)(_elementor_ui.Typography)(_templateObject$6 || (_templateObject$6 = _taggedTemplateLiteral(["\n  color: ", ";\n"])), function(_ref) {
		return _ref.theme.palette.secondary.light;
	});
	var StyledImage = (0, _elementor_ui.styled)("img")(_templateObject2$3 || (_templateObject2$3 = _taggedTemplateLiteral(["\n  height: 72px;\n  width: 72px;\n  object-fit: cover;\n  margin-right: ", ";\n"])), function(_ref2) {
		return _ref2.theme.spacing(.5);
	});
	var PromptHistoryItemSecondaryContent = function PromptHistoryItemSecondaryContent(_ref3) {
		var date = _ref3.date;
		var thumbnails = _ref3.thumbnails;
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			direction: "column",
			width: "90%"
		}, /*#__PURE__*/ react.default.createElement(StyledDateSubtitle, { variant: "caption" }, date), (thumbnails === null || thumbnails === void 0 ? void 0 : thumbnails.length) > 0 && /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			flexDirection: "row",
			mt: 1
		}, thumbnails.map(
			/**
			* @param {Object} thumb
			* @return {React.ReactNode}
			*/
			function(thumb) {
				return /*#__PURE__*/ react.default.createElement(StyledImage, {
					key: "thumbnail-".concat(thumb.seed),
					alt: "",
					src: thumb.image_url
				});
			}
		)));
	};
	PromptHistoryItemSecondaryContent.propTypes = {
		date: import_prop_types$7.default.string.isRequired,
		thumbnails: import_prop_types$7.default.array
	};

//#endregion
//#region modules/ai/assets/js/editor/components/prompt-history/parts/modal-item.js
	init_objectWithoutProperties$1();
	init_prompt_history_action_context();
	var _excluded$9 = [
		"id",
		"date",
		"onHistoryItemDelete"
	];
	var _templateObject$5;
	var _templateObject2$2;
	var _templateObject3;
	var _templateObject4;
	var StyledListItem = (0, _elementor_ui.styled)(_elementor_ui.ListItem)(_templateObject$5 || (_templateObject$5 = _taggedTemplateLiteral(["\n  & .e-prompt-history-item-actions {\n    position: absolute;\n    top: -9999px;\n  }\n\n  &:hover,\n  &:focus-visible,\n  &:focus-within {\n    .MuiTypography-body2 {\n      font-weight: 700;\n      max-width: ", ";\n    }\n\n    .e-prompt-history-item-actions {\n      position: relative;\n      top: -3px;\n    }\n  }\n"])), function(_ref) {
		var actionsCount = _ref.actionsCount;
		return "calc(100% - ".concat(actionsCount, " * 26px)");
	});
	var StyledListItemButton = (0, _elementor_ui.styled)(_elementor_ui.ListItemButton)(_templateObject2$2 || (_templateObject2$2 = _taggedTemplateLiteral(["\n  padding: ", ";\n  align-items: flex-start;\n  cursor: inherit;\n"])), function(_ref2) {
		return _ref2.theme.spacing(.5, 2);
	});
	var StyledListItemIcon = (0, _elementor_ui.styled)(_elementor_ui.ListItemIcon)(_templateObject3 || (_templateObject3 = _taggedTemplateLiteral([
		"\n  min-width: auto;\n\n  & > .MuiSvgIcon-root {\n    margin-right: ",
		";\n    font-size: 1rem;\n    position: relative;\n    top: ",
		";\n  }\n"
	])), function(_ref3) {
		return _ref3.theme.spacing(1.5);
	}, function(_ref4) {
		return _ref4.theme.spacing(.5);
	});
	var StyledButtonsWrapper = (0, _elementor_ui.styled)(_elementor_ui.Box)(_templateObject4 || (_templateObject4 = _taggedTemplateLiteral(["\n  & .MuiSvgIcon-root {\n    font-size: 1rem;\n  }\n"])));
	var PromptHistoryItem = function PromptHistoryItem(_ref5) {
		var id = _ref5.id;
		var date = _ref5.date;
		var onHistoryItemDelete = _ref5.onHistoryItemDelete;
		var props = _objectWithoutProperties$1(_ref5, _excluded$9);
		var _usePromptHistoryActi = usePromptHistoryAction();
		var getAllowedActions = _usePromptHistoryActi.getAllowedActions;
		var onPromptReuse = _usePromptHistoryActi.onPromptReuse;
		var onResultEdit = _usePromptHistoryActi.onResultEdit;
		var onImagesRestore = _usePromptHistoryActi.onImagesRestore;
		var action = props.action;
		var prompt = props.prompt;
		var text = props.text;
		var images = props.images;
		var thumbnails = props.thumbnails;
		var imageType = props.imageType;
		var ratio = props.ratio;
		var allowedActions = getAllowedActions();
		return /*#__PURE__*/ react.default.createElement(StyledListItem, {
			tabIndex: "0",
			"data-testid": "e-ph-i",
			disableGutters: true,
			disablePadding: true,
			actionsCount: Object.keys(allowedActions).length
		}, /*#__PURE__*/ react.default.createElement(StyledListItemButton, {
			component: "div",
			role: void 0
		}, /*#__PURE__*/ react.default.createElement(StyledListItemIcon, null, /*#__PURE__*/ react.default.createElement(PromptHistoryActionIcon, { action })), /*#__PURE__*/ react.default.createElement(_elementor_ui.ListItemText, {
			disableTypography: true,
			primary: /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
				direction: "row",
				justifyContent: "space-between",
				alignItems: "center",
				height: "16px"
			}, /*#__PURE__*/ react.default.createElement(PromptHistoryItemTitle, { prompt }), /*#__PURE__*/ react.default.createElement(StyledButtonsWrapper, { className: "e-prompt-history-item-actions" }, allowedActions[ACTION_TYPES.REMOVE] && /*#__PURE__*/ react.default.createElement(ActionButton, {
				onClick: function onClick() {
					return onHistoryItemDelete(id);
				},
				"aria-label": (0, _wordpress_i18n.__)("Remove item", "elementor"),
				tooltipTitle: (0, _wordpress_i18n.__)("Remove", "elementor")
			}, /*#__PURE__*/ react.default.createElement(_elementor_icons.TrashIcon, null)), allowedActions[ACTION_TYPES.REUSE] && /*#__PURE__*/ react.default.createElement(ActionButton, {
				onClick: function onClick() {
					return onPromptReuse(id, prompt);
				},
				"aria-label": (0, _wordpress_i18n.__)("Reuse prompt", "elementor"),
				tooltipTitle: (0, _wordpress_i18n.__)("Reuse prompt", "elementor")
			}, /*#__PURE__*/ react.default.createElement(RefreshIcon, null)), allowedActions[ACTION_TYPES.RESTORE] && /*#__PURE__*/ react.default.createElement(ActionButton, {
				onClick: function onClick() {
					return onImagesRestore(id, {
						prompt,
						images,
						imageType,
						ratio
					});
				},
				"aria-label": (0, _wordpress_i18n.__)("Restore", "elementor"),
				tooltipTitle: (0, _wordpress_i18n.__)("Restore", "elementor")
			}, /*#__PURE__*/ react.default.createElement(RestoreIcon, null)), allowedActions[ACTION_TYPES.EDIT] && /*#__PURE__*/ react.default.createElement(ActionButton, {
				onClick: function onClick() {
					return onResultEdit(id, text);
				},
				"aria-label": (0, _wordpress_i18n.__)("Edit result", "elementor"),
				tooltipTitle: (0, _wordpress_i18n.__)("Edit", "elementor")
			}, /*#__PURE__*/ react.default.createElement(EditIcon$1, null)))),
			secondary: /*#__PURE__*/ react.default.createElement(PromptHistoryItemSecondaryContent, {
				date,
				thumbnails
			})
		})));
	};
	PromptHistoryItem.propTypes = {
		id: import_prop_types$7.default.string.isRequired,
		action: import_prop_types$7.default.string.isRequired,
		prompt: import_prop_types$7.default.string.isRequired,
		date: import_prop_types$7.default.string.isRequired,
		onHistoryItemDelete: import_prop_types$7.default.func.isRequired,
		text: import_prop_types$7.default.string,
		images: import_prop_types$7.default.array,
		thumbnails: import_prop_types$7.default.array,
		imageType: import_prop_types$7.default.string,
		ratio: import_prop_types$7.default.string
	};

//#endregion
//#region modules/ai/assets/js/editor/components/prompt-history/parts/modal-period.js
	init_extends();
	init_objectWithoutProperties$1();
	var _excluded$8 = ["id", "date"];
	var StyledPeriodList = (0, _elementor_ui.styled)(_elementor_ui.List)(function(_ref) {
		var theme = _ref.theme;
		return { borderBottom: "1px solid ".concat(theme.palette.action.focus) };
	});
	var StyledPeriodTitle = (0, _elementor_ui.styled)(_elementor_ui.Typography)(function(_ref2) {
		var theme = _ref2.theme;
		return {
			padding: theme.spacing(1.5, 2),
			color: theme.palette.secondary.light
		};
	});
	var DATE_FORMAT = (0, _wordpress_i18n.__)("F j, g:i A", "elementor");
	var PromptHistoryPeriod = function PromptHistoryPeriod(_ref3) {
		var periodTitle = _ref3.periodTitle;
		var historyItems = _ref3.historyItems;
		var _onHistoryItemDelete = _ref3.onHistoryItemDelete;
		return /*#__PURE__*/ react.default.createElement(StyledPeriodList, {
			"data-testid": "e-ph-p",
			subheader: /*#__PURE__*/ react.default.createElement(_elementor_ui.ListSubheader, {
				disableSticky: true,
				disableGutters: true
			}, /*#__PURE__*/ react.default.createElement(StyledPeriodTitle, {
				variant: "body2",
				role: "heading",
				"aria-level": "6"
			}, periodTitle))
		}, historyItems.map(function(_ref4) {
			var id = _ref4.id;
			var date = _ref4.date;
			var props = _objectWithoutProperties$1(_ref4, _excluded$8);
			return /*#__PURE__*/ react.default.createElement(PromptHistoryItem, _extends$1({
				key: id,
				date: translateDate(DATE_FORMAT, date),
				onHistoryItemDelete: function onHistoryItemDelete() {
					return _onHistoryItemDelete(id);
				},
				id
			}, props));
		}));
	};
	PromptHistoryPeriod.propTypes = {
		periodTitle: import_prop_types$7.default.string.isRequired,
		historyItems: import_prop_types$7.default.array.isRequired,
		onHistoryItemDelete: import_prop_types$7.default.func.isRequired
	};

//#endregion
//#region modules/ai/assets/js/editor/components/prompt-history/helpers/history-period-helpers.js
	init_defineProperty$1();
	function _createForOfIteratorHelper(r, e) {
		var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"];
		if (!t) {
			if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) {
				t && (r = t);
				var _n = 0;
				var F = function F() {};
				return {
					s: F,
					n: function n() {
						return _n >= r.length ? { done: !0 } : {
							done: !1,
							value: r[_n++]
						};
					},
					e: function e(r) {
						throw r;
					},
					f: F
				};
			}
			throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
		}
		var o;
		var a = !0;
		var u = !1;
		return {
			s: function s() {
				t = t.call(r);
			},
			n: function n() {
				var r = t.next();
				return a = r.done, r;
			},
			e: function e(r) {
				u = !0, o = r;
			},
			f: function f() {
				try {
					a || null == t.return || t.return();
				} finally {
					if (u) throw o;
				}
			}
		};
	}
	function _unsupportedIterableToArray(r, a) {
		if (r) {
			if ("string" == typeof r) return _arrayLikeToArray(r, a);
			var t = {}.toString.call(r).slice(8, -1);
			return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0;
		}
	}
	function _arrayLikeToArray(r, a) {
		(null == a || a > r.length) && (a = r.length);
		for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e];
		return n;
	}
	var DATE_KEY = "date";
	var LAST_7_DAYS_KEY = "7-days";
	var LAST_30_DAYS_KEY = "30-days";
	var MONTH_FORMAT = (0, _wordpress_i18n.__)("F", "elementor");
	/**
	* Sorts items in ascending order by date.
	*
	* @param {Array} historyData
	* @return {Array}
	*/
	var sortPromptHistoryData = function sortPromptHistoryData(historyData) {
		return _toConsumableArray(historyData).sort(function(a, b) {
			return new Date(b[DATE_KEY]) - new Date(a[DATE_KEY]);
		});
	};
	/**
	* Groups prompt history data by time periods.
	*
	* @param {Array} historyData
	* @return {Object.<string, {label: string, items: any[]}>}
	*/
	var groupPromptHistoryData = function groupPromptHistoryData(historyData) {
		var data = sortPromptHistoryData(historyData);
		var currentDate = /* @__PURE__ */ new Date();
		var result = _defineProperty$1(_defineProperty$1({}, LAST_7_DAYS_KEY, {
			label: (0, _wordpress_i18n.__)("Last 7 days", "elementor"),
			items: []
		}), LAST_30_DAYS_KEY, {
			label: (0, _wordpress_i18n.__)("Last 30 days", "elementor"),
			items: []
		});
		var _iterator = _createForOfIteratorHelper(data);
		var _step;
		try {
			for (_iterator.s(); !(_step = _iterator.n()).done;) {
				var item = _step.value;
				var date = new Date(item[DATE_KEY]);
				var diff = daysDiff(date, currentDate);
				if (diff <= 7) {
					result[LAST_7_DAYS_KEY].items.push(item);
					continue;
				}
				if (diff <= 30) {
					result[LAST_30_DAYS_KEY].items.push(item);
					continue;
				}
				var month = date.getMonth();
				if (!result[month]) result[month] = {
					label: translateDate(MONTH_FORMAT, date),
					items: []
				};
				result[month].items.push(item);
			}
		} catch (err) {
			_iterator.e(err);
		} finally {
			_iterator.f();
		}
		return result;
	};
	var renderPeriods = function renderPeriods(_ref) {
		var _groupData$LAST_7_DAY;
		var _groupData$LAST_30_DA;
		var items = _ref.items;
		var onDelete = _ref.onDelete;
		var groupData = groupPromptHistoryData(items);
		var periods = [];
		if ((_groupData$LAST_7_DAY = groupData["7-days"]) !== null && _groupData$LAST_7_DAY !== void 0 && (_groupData$LAST_7_DAY = _groupData$LAST_7_DAY.items) !== null && _groupData$LAST_7_DAY !== void 0 && _groupData$LAST_7_DAY.length) periods.push(/*#__PURE__*/ react.default.createElement(PromptHistoryPeriod, {
			periodTitle: groupData[LAST_7_DAYS_KEY].label,
			onHistoryItemDelete: onDelete,
			historyItems: groupData[LAST_7_DAYS_KEY].items
		}));
		if ((_groupData$LAST_30_DA = groupData["30-days"]) !== null && _groupData$LAST_30_DA !== void 0 && (_groupData$LAST_30_DA = _groupData$LAST_30_DA.items) !== null && _groupData$LAST_30_DA !== void 0 && _groupData$LAST_30_DA.length) periods.push(/*#__PURE__*/ react.default.createElement(PromptHistoryPeriod, {
			periodTitle: groupData[LAST_30_DAYS_KEY].label,
			onHistoryItemDelete: onDelete,
			historyItems: groupData[LAST_30_DAYS_KEY].items
		}));
		for (var i = 12; i >= 0; i--) if (groupData[i]) periods.push(/*#__PURE__*/ react.default.createElement(PromptHistoryPeriod, {
			periodTitle: groupData[i].label,
			onHistoryItemDelete: onDelete,
			historyItems: groupData[i].items
		}));
		return periods;
	};

//#endregion
//#region modules/ai/assets/js/editor/components/prompt-history/parts/modal-empty.js
	init_history_types();
	var PromptHistoryEmpty = function PromptHistoryEmpty(_ref) {
		var historyType = _ref.historyType;
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			justifyContent: "center",
			sx: {
				height: "100%",
				textAlign: "center"
			},
			"data-testid": "e-ph-empty"
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, { variant: "h6" }, (0, _wordpress_i18n.__)("Prepare to be amazed", "elementor")), /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, { variant: "body1" }, (0, _wordpress_i18n.sprintf)((0, _wordpress_i18n.__)("This is where you'll find all the %s you've generated using Elementor AI.", "elementor"), getTranslatedPromptHistoryType(historyType))));
	};
	PromptHistoryEmpty.propTypes = { historyType: import_prop_types$7.default.oneOf(Object.values(HISTORY_TYPES)).isRequired };

//#endregion
//#region node_modules/react-infinite-scroller/src/InfiniteScroll.js
	init_objectWithoutProperties();
	init_defineProperty();
	var _excluded$7 = [
		"children",
		"element",
		"hasMore",
		"initialLoad",
		"isReverse",
		"loader",
		"loadMore",
		"pageStart",
		"ref",
		"threshold",
		"useCapture",
		"useWindow",
		"getScrollParent"
	];
	var InfiniteScroll = class extends react.Component {
		constructor(props) {
			super(props);
			this.scrollListener = this.scrollListener.bind(this);
			this.eventListenerOptions = this.eventListenerOptions.bind(this);
			this.mousewheelListener = this.mousewheelListener.bind(this);
		}
		componentDidMount() {
			this.pageLoaded = this.props.pageStart;
			this.options = this.eventListenerOptions();
			this.attachScrollListener();
		}
		componentDidUpdate() {
			if (this.props.isReverse && this.loadMore) {
				const parentElement = this.getParentElement(this.scrollComponent);
				parentElement.scrollTop = parentElement.scrollHeight - this.beforeScrollHeight + this.beforeScrollTop;
				this.loadMore = false;
			}
			this.attachScrollListener();
		}
		componentWillUnmount() {
			this.detachScrollListener();
			this.detachMousewheelListener();
		}
		isPassiveSupported() {
			let passive = false;
			const testOptions = { get passive() {
				passive = true;
			} };
			try {
				document.addEventListener("test", null, testOptions);
				document.removeEventListener("test", null, testOptions);
			} catch (e) {}
			return passive;
		}
		eventListenerOptions() {
			let options = this.props.useCapture;
			if (this.isPassiveSupported()) options = {
				useCapture: this.props.useCapture,
				passive: true
			};
			else options = { passive: false };
			return options;
		}
		setDefaultLoader(loader) {
			this.defaultLoader = loader;
		}
		detachMousewheelListener() {
			let scrollEl = window;
			if (this.props.useWindow === false) scrollEl = this.scrollComponent.parentNode;
			scrollEl.removeEventListener("mousewheel", this.mousewheelListener, this.options ? this.options : this.props.useCapture);
		}
		detachScrollListener() {
			let scrollEl = window;
			if (this.props.useWindow === false) scrollEl = this.getParentElement(this.scrollComponent);
			scrollEl.removeEventListener("scroll", this.scrollListener, this.options ? this.options : this.props.useCapture);
			scrollEl.removeEventListener("resize", this.scrollListener, this.options ? this.options : this.props.useCapture);
		}
		getParentElement(el) {
			const scrollParent = this.props.getScrollParent && this.props.getScrollParent();
			if (scrollParent != null) return scrollParent;
			return el && el.parentNode;
		}
		filterProps(props) {
			return props;
		}
		attachScrollListener() {
			const parentElement = this.getParentElement(this.scrollComponent);
			if (!this.props.hasMore || !parentElement) return;
			let scrollEl = window;
			if (this.props.useWindow === false) scrollEl = parentElement;
			scrollEl.addEventListener("mousewheel", this.mousewheelListener, this.options ? this.options : this.props.useCapture);
			scrollEl.addEventListener("scroll", this.scrollListener, this.options ? this.options : this.props.useCapture);
			scrollEl.addEventListener("resize", this.scrollListener, this.options ? this.options : this.props.useCapture);
			if (this.props.initialLoad) this.scrollListener();
		}
		mousewheelListener(e) {
			if (e.deltaY === 1 && !this.isPassiveSupported()) e.preventDefault();
		}
		scrollListener() {
			const el = this.scrollComponent;
			const scrollEl = window;
			const parentNode = this.getParentElement(el);
			let offset;
			if (this.props.useWindow) {
				const doc = document.documentElement || document.body.parentNode || document.body;
				const scrollTop = scrollEl.pageYOffset !== void 0 ? scrollEl.pageYOffset : doc.scrollTop;
				if (this.props.isReverse) offset = scrollTop;
				else offset = this.calculateOffset(el, scrollTop);
			} else if (this.props.isReverse) offset = parentNode.scrollTop;
			else offset = el.scrollHeight - parentNode.scrollTop - parentNode.clientHeight;
			if (offset < Number(this.props.threshold) && el && el.offsetParent !== null) {
				this.detachScrollListener();
				this.beforeScrollHeight = parentNode.scrollHeight;
				this.beforeScrollTop = parentNode.scrollTop;
				if (typeof this.props.loadMore === "function") {
					this.props.loadMore(this.pageLoaded += 1);
					this.loadMore = true;
				}
			}
		}
		calculateOffset(el, scrollTop) {
			if (!el) return 0;
			return this.calculateTopPosition(el) + (el.offsetHeight - scrollTop - window.innerHeight);
		}
		calculateTopPosition(el) {
			if (!el) return 0;
			return el.offsetTop + this.calculateTopPosition(el.offsetParent);
		}
		render() {
			const renderProps = this.filterProps(this.props);
			const { children, element, hasMore, initialLoad, isReverse, loader, loadMore, pageStart, ref, threshold, useCapture, useWindow, getScrollParent } = renderProps, props = _objectWithoutProperties(renderProps, _excluded$7);
			props.ref = (node) => {
				this.scrollComponent = node;
				if (ref) ref(node);
			};
			const childrenArray = [children];
			if (hasMore) {
				if (loader) isReverse ? childrenArray.unshift(loader) : childrenArray.push(loader);
				else if (this.defaultLoader) isReverse ? childrenArray.unshift(this.defaultLoader) : childrenArray.push(this.defaultLoader);
			}
			return react.default.createElement(element, props, childrenArray);
		}
	};
	_defineProperty(InfiniteScroll, "propTypes", {
		children: import_prop_types$7.default.node.isRequired,
		element: import_prop_types$7.default.node,
		hasMore: import_prop_types$7.default.bool,
		initialLoad: import_prop_types$7.default.bool,
		isReverse: import_prop_types$7.default.bool,
		loader: import_prop_types$7.default.node,
		loadMore: import_prop_types$7.default.func.isRequired,
		pageStart: import_prop_types$7.default.number,
		ref: import_prop_types$7.default.func,
		getScrollParent: import_prop_types$7.default.func,
		threshold: import_prop_types$7.default.number,
		useCapture: import_prop_types$7.default.bool,
		useWindow: import_prop_types$7.default.bool
	});
	_defineProperty(InfiniteScroll, "defaultProps", {
		element: "div",
		hasMore: false,
		initialLoad: true,
		pageStart: 0,
		ref: null,
		threshold: 250,
		useWindow: true,
		isReverse: false,
		useCapture: false,
		loader: null,
		getScrollParent: null
	});

//#endregion
//#region modules/ai/assets/js/editor/components/prompt-history/parts/modal-upgrade.js
	init_defineProperty$1();
	init_history_types();
	var VARIANT_FULL = "full";
	var VARIANT_SMALL = "small";
	var messages = _defineProperty$1(_defineProperty$1({}, VARIANT_FULL, (0, _wordpress_i18n.__)("Want to see your %s generation history for as far as the past 90 days?", "elementor")), VARIANT_SMALL, (0, _wordpress_i18n.__)("Want to see your %s generation history for the past 90 days?", "elementor"));
	var actionUrl = "https://go.elementor.com/ai-popup-purchase-dropdown/";
	var getMessage = function getMessage(variant, historyType) {
		return (0, _wordpress_i18n.sprintf)(messages[variant] || messages[VARIANT_FULL], getTranslatedPromptHistoryType(historyType));
	};
	var PromptHistoryUpgrade = function PromptHistoryUpgrade(_ref) {
		var variant = _ref.variant;
		var historyType = _ref.historyType;
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			justifyContent: "center",
			sx: {
				height: VARIANT_SMALL === variant ? "auto" : "100%",
				textAlign: "center",
				p: 2
			},
			"data-testid": "e-ph-upgrade-".concat(variant)
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, {
			variant: "body1",
			sx: { marginBottom: 2 }
		}, getMessage(variant, historyType)), /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, {
			variant: "contained",
			color: "promotion",
			size: "small",
			href: actionUrl,
			target: "_blank",
			rel: "noopener noreferrer",
			startIcon: /*#__PURE__*/ react.default.createElement(_elementor_icons.UpgradeIcon, null),
			sx: {
				width: "50%",
				alignSelf: "center",
				"&:hover": { color: "promotion.contrastText" }
			}
		}, (0, _wordpress_i18n.__)("Upgrade now", "elementor")));
	};
	PromptHistoryUpgrade.propTypes = {
		variant: import_prop_types$7.default.oneOf([VARIANT_FULL, VARIANT_SMALL]).isRequired,
		historyType: import_prop_types$7.default.oneOf(Object.values(HISTORY_TYPES)).isRequired
	};

//#endregion
//#region modules/ai/assets/js/editor/components/prompt-history/parts/modal-container.js
	init_extends();
	init_objectWithoutProperties$1();
	init_prompt_history_context();
	var _excluded$6 = ["children"];
	var _templateObject$4;
	var StyledContent$1 = (0, _elementor_ui.styled)(_elementor_ui.Box)(_templateObject$4 || (_templateObject$4 = _taggedTemplateLiteral([
		"\n  width: 360px;\n  position: relative;\n  margin-top: ",
		";\n  margin-right: ",
		";\n  background-color: ",
		";\n  border-radius: ",
		";\n  height: ",
		";\n\n  @media screen and (max-width: 456px) {\n    width: 320px;\n  }\n\n  @media screen and (max-width: 420px) {\n    width: 230px;\n  }\n"
	])), function(_ref) {
		return _ref.theme.spacing(2);
	}, function(_ref2) {
		return _ref2.theme.spacing(2);
	}, function(_ref3) {
		return _ref3.theme.palette.background.paper;
	}, function(_ref4) {
		var theme = _ref4.theme;
		return "".concat(theme.shape.borderRadius, "px");
	}, function(_ref5) {
		var height = _ref5.height;
		return "calc( ".concat(height, "px - 32px )");
	});
	var ModalContainer = function ModalContainer(_ref6) {
		var children = _ref6.children;
		var props = _objectWithoutProperties$1(_ref6, _excluded$6);
		var _usePromptHistoryCont = usePromptHistoryContext();
		var isOpen = _usePromptHistoryCont.isOpen;
		var isModalOpen = _usePromptHistoryCont.isModalOpen;
		var onClose = _usePromptHistoryCont.onClose;
		var getContainerHeight = _usePromptHistoryCont.getContainerHeight;
		var getContainer = _usePromptHistoryCont.getContainer;
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Modal, _extends$1({
			container: getContainer(),
			open: isModalOpen,
			hideBackdrop: true,
			onClose,
			sx: { position: "absolute" }
		}, props), /*#__PURE__*/ react.default.createElement(_elementor_ui.Backdrop, {
			open: true,
			sx: {
				position: "absolute",
				justifyContent: "flex-end",
				alignItems: "flex-start"
			},
			"aria-hidden": false
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Slide, {
			direction: "left",
			in: isOpen,
			timeout: 500,
			easing: "ease-in-out"
		}, /*#__PURE__*/ react.default.createElement(StyledContent$1, {
			"aria-label": (0, _wordpress_i18n.__)("Prompt history modal", "elementor"),
			height: getContainerHeight()
		}, children))));
	};
	ModalContainer.propTypes = {
		isOpen: import_prop_types$7.default.bool.isRequired,
		isModalOpen: import_prop_types$7.default.bool.isRequired,
		onClose: import_prop_types$7.default.func.isRequired,
		children: import_prop_types$7.default.oneOfType([import_prop_types$7.default.arrayOf(import_prop_types$7.default.node), import_prop_types$7.default.node])
	};

//#endregion
//#region modules/ai/assets/js/editor/components/prompt-history/history-modal.js
	init_asyncToGenerator();
	init_prompt_error_message();
	init_prompt_history_context();
	var ITEMS_LIMIT = 10;
	var FREE_PLAN_ERRORS = ["invalid_connect_data", "no_subscription"];
	var PromptHistoryModal = function PromptHistoryModal(props) {
		var lastRun = (0, react.useRef)(function() {});
		var scrollContainer = (0, react.useRef)(null);
		var _usePromptHistoryCont = usePromptHistoryContext();
		var historyType = _usePromptHistoryCont.historyType;
		var onClose = _usePromptHistoryCont.onClose;
		var _usePromptHistory = usePromptHistory(historyType);
		var items = _usePromptHistory.items;
		var meta = _usePromptHistory.meta;
		var isHistoryFetchingInProgress = _usePromptHistory.isLoading;
		var historyFetchingError = _usePromptHistory.error;
		var fetchData = _usePromptHistory.fetchData;
		var deleteItemFromState = _usePromptHistory.deleteItemById;
		var _useDeletePromptHisto = useDeletePromptHistoryItem();
		var isDeletingInProgress = _useDeletePromptHisto.isLoading;
		var historyDeletingError = _useDeletePromptHisto.error;
		var deleteItem = _useDeletePromptHisto.deleteItem;
		var error = historyFetchingError || historyDeletingError;
		var isLoading = isHistoryFetchingInProgress || isDeletingInProgress;
		var isLastPage = meta && (meta === null || meta === void 0 ? void 0 : meta.currentPage) === (meta === null || meta === void 0 ? void 0 : meta.totalPages);
		var showUpgrade = (items === null || items === void 0 ? void 0 : items.length) > 0 && (meta === null || meta === void 0 ? void 0 : meta.allowedDays) < 90 && isLastPage;
		(0, react.useEffect)(function() {
			lastRun.current = /*#__PURE__*/ _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee() {
				return import_regenerator$2.default.wrap(function(_context) {
					while (1) switch (_context.prev = _context.next) {
						case 0: return _context.abrupt("return", fetchData({
							page: 1,
							limit: ITEMS_LIMIT
						}));
						case 1:
						case "end": return _context.stop();
					}
				}, _callee);
			}));
			lastRun.current();
		}, []);
		var loadNext = function loadNext() {
			if (isLoading || isLastPage) return;
			lastRun.current = /*#__PURE__*/ _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee2() {
				return import_regenerator$2.default.wrap(function(_context2) {
					while (1) switch (_context2.prev = _context2.next) {
						case 0: return _context2.abrupt("return", fetchData({
							page: meta.currentPage + 1,
							limit: ITEMS_LIMIT
						}));
						case 1:
						case "end": return _context2.stop();
					}
				}, _callee2);
			}));
			lastRun.current();
		};
		var onHistoryItemDelete = /*#__PURE__*/ function() {
			var _ref3 = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee4(id) {
				return import_regenerator$2.default.wrap(function(_context4) {
					while (1) switch (_context4.prev = _context4.next) {
						case 0:
							lastRun.current = /*#__PURE__*/ _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee3() {
								return import_regenerator$2.default.wrap(function(_context3) {
									while (1) switch (_context3.prev = _context3.next) {
										case 0:
											_context3.next = 1;
											return deleteItem(id);
										case 1: return _context3.abrupt("return", _context3.sent);
										case 2:
										case "end": return _context3.stop();
									}
								}, _callee3);
							}));
							_context4.next = 1;
							return lastRun.current();
						case 1: if (!historyDeletingError) deleteItemFromState(id);
						case 2:
						case "end": return _context4.stop();
					}
				}, _callee4);
			}));
			return function onHistoryItemDelete(_x) {
				return _ref3.apply(this, arguments);
			};
		}();
		return /*#__PURE__*/ react.default.createElement(ModalContainer, props, /*#__PURE__*/ react.default.createElement(PromptHistoryModalHeader, { onClose }), error && !FREE_PLAN_ERRORS.includes(error) && /*#__PURE__*/ react.default.createElement(PromptErrorMessage, {
			error,
			onRetry: lastRun.current,
			sx: {
				position: "absolute",
				zIndex: 1,
				marginTop: isLoading ? .5 : "revert"
			}
		}), isLoading && /*#__PURE__*/ react.default.createElement(_elementor_ui.LinearProgress, {
			role: "progressbar",
			"aria-label": (0, _wordpress_i18n.__)("Loading", "elementor"),
			color: "secondary"
		}), /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, {
			sx: {
				overflowY: "scroll",
				height: "85%"
			},
			ref: scrollContainer
		}, error && FREE_PLAN_ERRORS.includes(error) && /*#__PURE__*/ react.default.createElement(PromptHistoryUpgrade, {
			variant: "full",
			historyType
		}), !error && 0 === (items === null || items === void 0 ? void 0 : items.length) && /*#__PURE__*/ react.default.createElement(PromptHistoryEmpty, { historyType }), (items === null || items === void 0 ? void 0 : items.length) > 0 && /*#__PURE__*/ react.default.createElement(InfiniteScroll, {
			loadMore: loadNext,
			getScrollParent: function getScrollParent() {
				return scrollContainer.current;
			},
			useWindow: false,
			isReverse: false,
			threshold: 30,
			initialLoad: false,
			hasMore: !isLastPage
		}, renderPeriods({
			items,
			onDelete: onHistoryItemDelete
		})), showUpgrade && /*#__PURE__*/ react.default.createElement(PromptHistoryUpgrade, {
			variant: "small",
			historyType
		})));
	};

//#endregion
//#region modules/ai/assets/js/editor/components/prompt-history/index.js
	init_prompt_history_context();
	var PROMPT_HISTORY_MODAL_ID = "prompt-history-modal";
	var PromptHistory = function PromptHistory() {
		var _usePromptHistoryCont = usePromptHistoryContext();
		var isOpen = _usePromptHistoryCont.isOpen;
		var isModalOpen = _usePromptHistoryCont.isModalOpen;
		var onOpen = _usePromptHistoryCont.onOpen;
		var onClose = _usePromptHistoryCont.onClose;
		return /*#__PURE__*/ react.default.createElement(react.default.Fragment, null, /*#__PURE__*/ react.default.createElement(PromptHistoryButton, {
			isActive: isOpen,
			onClick: function onClick() {
				return isOpen ? onClose() : onOpen();
			},
			"aria-haspopup": "dialog",
			"aria-controls": PROMPT_HISTORY_MODAL_ID
		}), isModalOpen && /*#__PURE__*/ react.default.createElement(PromptHistoryModal, { id: PROMPT_HISTORY_MODAL_ID }));
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-media/index.js
	init_extends();
	init_slicedToArray();
	init_defineProperty$1();
	function ownKeys$8(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	__name(ownKeys$8, "ownKeys");
	function _objectSpread$8(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$8(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$8(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	__name(_objectSpread$8, "_objectSpread");
	var initialData = {
		isAllSaved: false,
		hasUnsavedChanges: false
	};
	var SET_UNSAVED_CHANGES = "SET_UNSAVED_CHANGES";
	var SAVE_AND_CLOSE = "SAVE_AND_CLOSE";
	var reducer = function reducer(state, _ref) {
		var type = _ref.type;
		var payload = _ref.payload;
		switch (type) {
			case SET_UNSAVED_CHANGES: return _objectSpread$8(_objectSpread$8({}, state), {}, {
				hasUnsavedChanges: payload,
				isAllSaved: payload ? false : state.isAllSaved
			});
			case SAVE_AND_CLOSE: return _objectSpread$8(_objectSpread$8({}, state), {}, {
				isAllSaved: true,
				hasUnsavedChanges: false
			});
			default: throw Error("Unknown action.");
		}
	};
	var FormMedia = function FormMedia(_ref2) {
		var onClose = _ref2.onClose;
		var DialogProps = _ref2.DialogProps;
		var getControlValue = _ref2.getControlValue;
		var setControlValue = _ref2.setControlValue;
		var additionalOptions = _ref2.additionalOptions;
		var maybeRenderUpgradeChip = _ref2.maybeRenderUpgradeChip;
		var hasSubscription = _ref2.hasSubscription;
		var usagePercentage = _ref2.usagePercentage;
		var _useReducer2 = _slicedToArray((0, react.useReducer)(reducer, initialData), 2);
		var state = _useReducer2[0];
		var dispatch = _useReducer2[1];
		var _useState2 = _slicedToArray((0, react.useState)(false), 2);
		var showUnsavedChangeAlert = _useState2[0];
		var setShowUnsavedChangeAlert = _useState2[1];
		var setHasUnsavedChanges = function setHasUnsavedChanges(payload) {
			return dispatch({
				type: SET_UNSAVED_CHANGES,
				payload
			});
		};
		var saveAndClose = function saveAndClose() {
			return dispatch({ type: SAVE_AND_CLOSE });
		};
		var onCloseIntent = function onCloseIntent() {
			if (state.hasUnsavedChanges) {
				setShowUnsavedChangeAlert(true);
				return;
			}
			onClose();
		};
		var editImageInitialData = getControlValue() === (additionalOptions === null || additionalOptions === void 0 ? void 0 : additionalOptions.defaultValue) ? {} : getControlValue();
		var globalSettings = {
			initialImageType: (additionalOptions === null || additionalOptions === void 0 ? void 0 : additionalOptions.defaultImageType) || "",
			hasSubscription,
			usagePercentage,
			hideBackButton: additionalOptions === null || additionalOptions === void 0 ? void 0 : additionalOptions.hideBackButton
		};
		var globalActions = {
			state,
			getControlValue,
			setControlValue,
			saveAndClose,
			close: onCloseIntent,
			setHasUnsavedChanges
		};
		(0, react.useEffect)(function() {
			if (state.isAllSaved) onClose();
		}, [state.isAllSaved]);
		return /*#__PURE__*/ react.default.createElement(react.default.Fragment, null, /*#__PURE__*/ react.default.createElement(PromptDialog, _extends$1({
			id: "e-form-media",
			onClose: function onClose() {
				return onCloseIntent();
			},
			maxWidth: "lg"
		}, DialogProps), /*#__PURE__*/ react.default.createElement(PromptDialog.Header, {
			onClose: function onClose() {
				return onCloseIntent();
			},
			hideAiBetaLogo: additionalOptions === null || additionalOptions === void 0 ? void 0 : additionalOptions.hideAiBetaLogo
		}, !additionalOptions.withoutHistory && /*#__PURE__*/ react.default.createElement(PromptHistory, null), maybeRenderUpgradeChip()), /*#__PURE__*/ react.default.createElement(_elementor_ui.Divider, null), /*#__PURE__*/ react.default.createElement(GlobalSettingsProvider, { settings: globalSettings }, /*#__PURE__*/ react.default.createElement(GlobalActionsProvider, { actions: globalActions }, /*#__PURE__*/ react.default.createElement(LocationProvider, null, /*#__PURE__*/ react.default.createElement(EditImageProvider, { imageData: editImageInitialData }, /*#__PURE__*/ react.default.createElement(MediaOutlet, {
			additionalOptions,
			onClose
		})))))), showUnsavedChangeAlert && /*#__PURE__*/ react.default.createElement(UnsavedChangesAlert$1, {
			onClose,
			onCancel: function onCancel() {
				return setShowUnsavedChangeAlert(false);
			},
			open: true
		}));
	};
	FormMedia.propTypes = {
		onClose: import_prop_types$7.default.func.isRequired,
		DialogProps: import_prop_types$7.default.object,
		getControlValue: import_prop_types$7.default.func.isRequired,
		setControlValue: import_prop_types$7.default.func.isRequired,
		additionalOptions: import_prop_types$7.default.object,
		credits: import_prop_types$7.default.number,
		maybeRenderUpgradeChip: import_prop_types$7.default.func,
		hasSubscription: import_prop_types$7.default.bool,
		usagePercentage: import_prop_types$7.default.number
	};

//#endregion
//#region modules/ai/assets/js/editor/hooks/use-animation-prompt.js
	init_typeof$1();
	init_slicedToArray();
	init_defineProperty$1();
	init_asyncToGenerator();
	init_api();
	init_use_prompt();
	function ownKeys$7(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	__name(ownKeys$7, "ownKeys");
	function _objectSpread$7(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$7(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$7(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	__name(_objectSpread$7, "_objectSpread");
	var getAnimationResult = /*#__PURE__*/ function() {
		var _ref = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee(payload, motionEffectType) {
			return import_regenerator$2.default.wrap(function(_context) {
				while (1) switch (_context.prev = _context.next) {
					case 0: return _context.abrupt("return", getAnimation(_objectSpread$7(_objectSpread$7({}, payload), {}, { motionEffectType })));
					case 1:
					case "end": return _context.stop();
				}
			}, _callee);
		}));
		return function getAnimationResult(_x, _x2) {
			return _ref.apply(this, arguments);
		};
	}();
	var useAnimationPrompt = function useAnimationPrompt(motionEffectType, widgetType, initialValue) {
		var _usePrompt = usePrompt(function(payload) {
			return getAnimationResult(payload, motionEffectType);
		}, initialValue);
		var data = _usePrompt.data;
		var isLoading = _usePrompt.isLoading;
		var error = _usePrompt.error;
		var reset = _usePrompt.reset;
		var send = _usePrompt.send;
		var sendUsageData = _usePrompt.sendUsageData;
		var getControlData = function getControlData(controlKey) {
			var _elementor$widgetsCac;
			var _elementor$widgetsCac2;
			return {
				label: (_elementor$widgetsCac = elementor.widgetsCache) === null || _elementor$widgetsCac === void 0 || (_elementor$widgetsCac = _elementor$widgetsCac[widgetType !== null && widgetType !== void 0 ? widgetType : ""]) === null || _elementor$widgetsCac === void 0 || (_elementor$widgetsCac = _elementor$widgetsCac.controls[controlKey]) === null || _elementor$widgetsCac === void 0 ? void 0 : _elementor$widgetsCac.label,
				innerTab: (_elementor$widgetsCac2 = elementor.widgetsCache) === null || _elementor$widgetsCac2 === void 0 || (_elementor$widgetsCac2 = _elementor$widgetsCac2[widgetType !== null && widgetType !== void 0 ? widgetType : ""]) === null || _elementor$widgetsCac2 === void 0 || (_elementor$widgetsCac2 = _elementor$widgetsCac2.controls[controlKey]) === null || _elementor$widgetsCac2 === void 0 ? void 0 : _elementor$widgetsCac2.inner_tab
			};
		};
		var processMotionEffects = function processMotionEffects(jsonObject) {
			var result = {};
			var firstChild = jsonObject[Object.keys(jsonObject)[0]];
			var firstEnableControlKey = Object.keys(firstChild)[0];
			result[firstEnableControlKey] = {
				isParent: true,
				label: getControlData(firstEnableControlKey).label,
				value: jsonObject.enabled[firstEnableControlKey],
				tabs: 0
			};
			var processOtherProps = function processOtherProps(props) {
				for (var _i = 0, _Object$entries = Object.entries(props); _i < _Object$entries.length; _i++) {
					var _Object$entries$_i = _slicedToArray(_Object$entries[_i], 2);
					var key = _Object$entries$_i[0];
					var value = _Object$entries$_i[1];
					if ("enabled" === key && value) {
						var enableControlKey = Object.keys(value)[0];
						result[enableControlKey] = {
							isParent: false,
							label: getControlData(enableControlKey).label,
							value: value[enableControlKey],
							tabs: 1
						};
					} else {
						if ("none" === value || "normal" === value) continue;
						result[key] = { value };
					}
				}
			};
			for (var _i2 = 0, _Object$entries2 = Object.entries(jsonObject); _i2 < _Object$entries2.length; _i2++) {
				var _Object$entries2$_i = _slicedToArray(_Object$entries2[_i2], 2);
				var propKey = _Object$entries2$_i[0];
				var propValue = _Object$entries2$_i[1];
				if (propKey !== "enabled" && "object" === _typeof$1(propValue)) processOtherProps(propValue);
			}
			return result;
		};
		var processEntranceEffects = function processEntranceEffects(jsonObject) {
			var result = {};
			var firstChildKey = Object.keys(jsonObject)[0];
			var firstChild = jsonObject[firstChildKey];
			result[firstChildKey] = {
				isParent: true,
				label: getControlData(firstChildKey).label,
				value: firstChild,
				tabs: 0
			};
			result[firstChild] = {
				isParent: false,
				label: firstChild,
				tabs: 1
			};
			var processProps = function processProps(props) {
				for (var _i3 = 0, _Object$entries3 = Object.entries(props); _i3 < _Object$entries3.length; _i3++) {
					var _Object$entries3$_i = _slicedToArray(_Object$entries3[_i3], 2);
					var key = _Object$entries3$_i[0];
					var value = _Object$entries3$_i[1];
					if ("none" === value || "normal" === value) continue;
					result[key] = { value };
				}
			};
			for (var _i4 = 0, _Object$entries4 = Object.entries(jsonObject); _i4 < _Object$entries4.length; _i4++) {
				var _Object$entries4$_i = _slicedToArray(_Object$entries4[_i4], 2);
				var propKey = _Object$entries4$_i[0];
				var propValue = _Object$entries4$_i[1];
				if (propKey !== firstChildKey && "object" === _typeof$1(propValue)) processProps(propValue);
			}
			return result;
		};
		var processHoverEffects = function processHoverEffects(jsonObject) {
			var result = {};
			var isHoverLabelAdded = false;
			var processProps = function processProps(props) {
				for (var _i5 = 0, _Object$entries5 = Object.entries(props); _i5 < _Object$entries5.length; _i5++) {
					var _Object$entries5$_i = _slicedToArray(_Object$entries5[_i5], 2);
					var key = _Object$entries5$_i[0];
					var value = _Object$entries5$_i[1];
					if ("enabled" === key && value) {
						var enableControlKey = Object.keys(value)[0];
						var _getControlData = getControlData(enableControlKey);
						var label = _getControlData.label;
						var innerTab = _getControlData.innerTab;
						if (!isHoverLabelAdded) {
							result[innerTab] = {
								isParent: true,
								label: getControlData(innerTab).label,
								tabs: 0
							};
							isHoverLabelAdded = true;
						}
						result[enableControlKey] = {
							isParent: false,
							label,
							value: value[enableControlKey],
							tabs: 1
						};
					} else {
						if ("none" === value || "normal" === value) continue;
						result[key] = { value };
					}
				}
			};
			for (var _i6 = 0, _Object$entries6 = Object.entries(jsonObject); _i6 < _Object$entries6.length; _i6++) {
				var propValue = _slicedToArray(_Object$entries6[_i6], 2)[1];
				if ("object" === _typeof$1(propValue)) processProps(propValue);
			}
			return result;
		};
		var digestResult = function digestResult(jsonString) {
			var result = {};
			function traverse(currentObj) {
				if (!currentObj || _typeof$1(currentObj) !== "object") return;
				for (var key in currentObj) {
					if (!currentObj.hasOwnProperty(key)) continue;
					if (["scrolling_effects", "mouse_effects"].includes(key)) {
						result = _objectSpread$7(_objectSpread$7({}, result), processMotionEffects(currentObj[key]));
						continue;
					}
					if ("entrance_effects" === key) {
						result = _objectSpread$7(_objectSpread$7({}, result), processEntranceEffects(currentObj[key]));
						continue;
					}
					if ("hover_effects" === key) {
						result = _objectSpread$7(_objectSpread$7({}, result), processHoverEffects(currentObj[key]));
						return;
					}
					traverse(currentObj[key]);
				}
			}
			traverse(JSON.parse(jsonString));
			return result;
		};
		(0, react.useEffect)(function() {
			if (data.result) data.result = digestResult(data.result);
		}, [data]);
		return {
			data,
			isLoading,
			error,
			reset,
			send,
			sendUsageData
		};
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-animation/index.js
	init_asyncToGenerator();
	init_slicedToArray();
	init_actions_data();
	init_loader();
	init_prompt_search();
	init_prompt_suggestions();
	init_generate_button();
	init_prompt_error_message();
	init_requests_ids();
	init_voice_promotion_alert();
	var CodeDisplayWrapper$1 = (0, _elementor_ui.styled)(_elementor_ui.Box)(function() {
		return {
			"& p": {
				mb: "10px",
				fontSize: "14px",
				lineHeight: "1.5"
			},
			"& pre": { position: "relative" },
			"& textarea": {
				fontSize: "13px",
				lineHeight: "1.7"
			}
		};
	});
	var FormAnimation = function FormAnimation(_ref) {
		var onClose = _ref.onClose;
		var getControlValue = _ref.getControlValue;
		var setControlValue = _ref.setControlValue;
		var additionalOptions = _ref.additionalOptions;
		var credits = _ref.credits;
		var children = _ref.children;
		var _useAnimationPrompt = useAnimationPrompt(additionalOptions.animationType, additionalOptions.widgetType, credits);
		var data = _useAnimationPrompt.data;
		var isLoading = _useAnimationPrompt.isLoading;
		var error = _useAnimationPrompt.error;
		var reset = _useAnimationPrompt.reset;
		var send = _useAnimationPrompt.send;
		var sendUsageData = _useAnimationPrompt.sendUsageData;
		var _useState2 = _slicedToArray((0, react.useState)(""), 2);
		var prompt = _useState2[0];
		var setPrompt = _useState2[1];
		var setGenerate = useRequestIds().setGenerate;
		var _useState4 = _slicedToArray((0, react.useState)(""), 2);
		var animationSummary = _useState4[0];
		var setAnimationSummary = _useState4[1];
		var _useState6 = _slicedToArray((0, react.useState)(), 2);
		var prevControlValue = _useState6[0];
		var setPrevControlValue = _useState6[1];
		var lastRun = (0, react.useRef)(function() {});
		var autocompleteItems = "hover" === additionalOptions.animationType ? hoverEffectAutocomplete : motionEffectAutocomplete;
		var showSuggestions = !prompt;
		var handleSubmit = /*#__PURE__*/ function() {
			var _ref2 = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee(event) {
				return import_regenerator$2.default.wrap(function(_context) {
					while (1) switch (_context.prev = _context.next) {
						case 0:
							event.preventDefault();
							setGenerate();
							lastRun.current = function() {
								return send({ prompt });
							};
							_context.next = 1;
							return lastRun.current();
						case 1:
						case "end": return _context.stop();
					}
				}, _callee);
			}));
			return function handleSubmit(_x) {
				return _ref2.apply(this, arguments);
			};
		}();
		(0, react.useEffect)(function() {
			if (data.result) showPreview(data.result);
		}, [data]);
		(0, react.useEffect)(function() {
			return function() {
				restorePrevSettings();
			};
		}, []);
		var showPreview = function showPreview(result) {
			var summary = Object.keys(result).filter(function(key) {
				return result[key].label;
			}).map(function(key) {
				var item = result[key];
				var tabs = "&nbsp;".repeat(item.tabs);
				var bulletIcon = item.tabs > 0 ? "&#8226; " : "";
				var label = item.isParent ? "<br/><b>".concat(item.label, ":</b>") : "".concat(bulletIcon, " ").concat(item.label);
				return "".concat(tabs).concat(label);
			}).join("<br/>");
			var title = "".concat((0, _wordpress_i18n.__)("Here’s a breakdown of what was done for creating the animation", "elementor"));
			setAnimationSummary("<b>".concat(title, ":</b><br/>").concat(summary));
			setPrevControlValue(getControlValue());
			elementor.documents.getCurrent().history.setActive(false);
			setControlValue(getValidElementor(result));
		};
		var restorePrevSettings = function restorePrevSettings() {
			setControlValue(prevControlValue);
			elementor.documents.getCurrent().history.setActive(true);
		};
		var getValidElementor = function getValidElementor(result) {
			return Object.entries(result).filter(function(_ref3) {
				return _slicedToArray(_ref3, 2)[1].hasOwnProperty("value");
			}).reduce(function(previousValue, _ref5) {
				var _ref6 = _slicedToArray(_ref5, 2);
				var key = _ref6[0];
				previousValue[key] = _ref6[1].value;
				return previousValue;
			}, {});
		};
		var applyPrompt = function applyPrompt(result) {
			sendUsageData();
			restorePrevSettings();
			setControlValue(getValidElementor(result));
			onClose();
		};
		if (isLoading) return /*#__PURE__*/ react.default.createElement(Loader, null);
		return /*#__PURE__*/ react.default.createElement(react.default.Fragment, null, error && /*#__PURE__*/ react.default.createElement(PromptErrorMessage, {
			error,
			onRetry: lastRun.current,
			sx: { mb: 2.5 }
		}), children, !data.result && /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, {
			component: "form",
			onSubmit: handleSubmit
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, { sx: { pb: 1.5 } }, /*#__PURE__*/ react.default.createElement(PromptSearch, {
			placeholder: (0, _wordpress_i18n.__)("Describe the animation you want to create...", "elementor"),
			name: "prompt",
			value: prompt,
			color: "secondary",
			onChange: function onChange(event) {
				return setPrompt(event.target.value);
			}
		})), showSuggestions && /*#__PURE__*/ react.default.createElement(PromptSuggestions, {
			suggestions: autocompleteItems,
			onSelect: setPrompt
		}), /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			direction: "row",
			alignItems: "center",
			sx: {
				py: 1.5,
				mt: 4
			}
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			direction: "row",
			justifyContent: "flex-end",
			flexGrow: 1
		}, /*#__PURE__*/ react.default.createElement(GenerateButton$1, null, (0, _wordpress_i18n.__)("Generate Animation", "elementor"))))), data.result && animationSummary && /*#__PURE__*/ react.default.createElement(CodeDisplayWrapper$1, null, /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, { sx: {
			backgroundColor: "rgba(0, 0, 0, 0.06); ",
			padding: "1em",
			borderRadius: "4px",
			width: "100%",
			height: "260px",
			overflowX: "auto"
		} }, /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, {
			variant: "body1",
			dangerouslySetInnerHTML: { __html: animationSummary }
		})), /*#__PURE__*/ react.default.createElement(VoicePromotionAlert, { introductionKey: "ai-context-animation-promotion" }), /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			direction: "row",
			alignItems: "center",
			sx: { mt: 4 }
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			direction: "row",
			gap: 1,
			justifyContent: "flex-end",
			flexGrow: 1
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, {
			size: "small",
			color: "secondary",
			variant: "text",
			onClick: function onClick() {
				restorePrevSettings();
				reset();
			}
		}, (0, _wordpress_i18n.__)("New prompt", "elementor")), /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, {
			size: "small",
			variant: "contained",
			color: "primary",
			onClick: function onClick() {
				return applyPrompt(data.result);
			}
		}, (0, _wordpress_i18n.__)("Use animation", "elementor"))))));
	};
	FormAnimation.propTypes = {
		onClose: import_prop_types$7.default.func.isRequired,
		getControlValue: import_prop_types$7.default.func.isRequired,
		setControlValue: import_prop_types$7.default.func.isRequired,
		additionalOptions: import_prop_types$7.default.shape({
			animationType: import_prop_types$7.default.string,
			widgetType: import_prop_types$7.default.string
		}),
		credits: import_prop_types$7.default.number,
		children: import_prop_types$7.default.node
	};

//#endregion
//#region \0vite/preload-helper.js
	var scriptRel, assetsURL, seen, __vitePreload;
	var init_preload_helper = __esmMin((() => {
		scriptRel = "modulepreload";
		assetsURL = function(dep) {
			return "/" + dep;
		};
		seen = {};
		__vitePreload = function preload(baseModule, deps, importerUrl) {
			let promise = Promise.resolve();
			if (false              && deps && deps.length > 0) {
				const links = document.getElementsByTagName("link");
				const cspNonceMeta = document.querySelector("meta[property=csp-nonce]");
				const cspNonce = (cspNonceMeta === null || cspNonceMeta === void 0 ? void 0 : cspNonceMeta.nonce) || (cspNonceMeta === null || cspNonceMeta === void 0 ? void 0 : cspNonceMeta.getAttribute("nonce"));
				function allSettled(promises) {
					return Promise.all(promises.map((p) => Promise.resolve(p).then((value) => ({
						status: "fulfilled",
						value
					}), (reason) => ({
						status: "rejected",
						reason
					}))));
				}
				function importMetaResolve(specifier) {
					if ({}.resolve) return {}.resolve(specifier);
					return new URL(
						specifier,
						/** #__KEEP__ */
						{}.url
					).href;
				}
				promise = allSettled(deps.map((dep) => {
					dep = assetsURL(dep, importerUrl);
					dep = importMetaResolve(dep);
					if (dep in seen) return;
					seen[dep] = true;
					const isCss = dep.endsWith(".css");
					for (let i = links.length - 1; i >= 0; i--) {
						const link = links[i];
						if (link.href === dep && (!isCss || link.rel === "stylesheet")) return;
					}
					const link = document.createElement("link");
					link.rel = isCss ? "stylesheet" : scriptRel;
					if (!isCss) link.as = "script";
					link.crossOrigin = "";
					link.href = dep;
					if (cspNonce) link.setAttribute("nonce", cspNonce);
					document.head.appendChild(link);
					if (isCss) return new Promise((res, rej) => {
						link.addEventListener("load", res);
						link.addEventListener("error", () => rej(/* @__PURE__ */ new Error(`Unable to preload CSS for ${dep}`)));
					});
				}));
			}
			function handlePreloadError(err) {
				const e = new Event("vite:preloadError", { cancelable: true });
				e.payload = err;
				window.dispatchEvent(e);
				if (!e.defaultPrevented) throw err;
			}
			return promise.then((res) => {
				for (const item of res || []) {
					if (item.status !== "rejected") continue;
					handlePreloadError(item.reason);
				}
				return baseModule().catch(handlePreloadError);
			});
		};
	}));

//#endregion
//#region modules/ai/assets/js/editor/pages/form-code/code-block.js
	var import_prop_types$2, _excluded$5, CodeBlock;
	var init_code_block = __esmMin((() => {
		init_extends();
		init_objectWithoutProperties$1();
		import_prop_types$2 = /* @__PURE__ */ __toESM(require_prop_types());
		init_textarea();
		_excluded$5 = [
			"node",
			"inline",
			"children",
			"defaultValue",
			"onInsert"
		];
		CodeBlock = function CodeBlock(_ref) {
			_ref.node;
			var inline = _ref.inline;
			var children = _ref.children;
			var defaultValue = _ref.defaultValue;
			var onInsert = _ref.onInsert;
			var props = _objectWithoutProperties$1(_ref, _excluded$5);
			var codeBlockInput = (0, react.useRef)(null);
			if (inline) return /*#__PURE__*/ react.default.createElement("code", props);
			return /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, {
				sx: { position: "relative" },
				dir: "ltr"
			}, /*#__PURE__*/ react.default.createElement(Textarea, _extends$1({
				fullWidth: true,
				ref: codeBlockInput,
				defaultValue: children[0],
				sx: { mb: 1 },
				helperText: (0, _wordpress_i18n.__)("Code generated by AI may be inaccurate.", "elementor")
			}, props)), /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, {
				size: "small",
				variant: "contained",
				onClick: function onClick() {
					return onInsert(defaultValue + "\n" + codeBlockInput.current.value);
				},
				sx: {
					position: "absolute",
					right: "11px /* @noflip */",
					bottom: "44px"
				}
			}, (0, _wordpress_i18n.__)("Insert", "elementor")));
		};
		CodeBlock.propTypes = {
			node: import_prop_types$2.default.object,
			inline: import_prop_types$2.default.bool,
			children: import_prop_types$2.default.arrayOf(import_prop_types$2.default.node).isRequired,
			defaultValue: import_prop_types$2.default.string,
			onInsert: import_prop_types$2.default.func.isRequired
		};
	}));

//#endregion
//#region modules/ai/assets/js/editor/hooks/use-code-prompt.js
	function ownKeys$6(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	function _objectSpread$6(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$6(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$6(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	var import_regenerator$1, getCodeResult, useCodePrompt;
	var init_use_code_prompt = __esmMin((() => {
		init_defineProperty$1();
		init_asyncToGenerator();
		import_regenerator$1 = /* @__PURE__ */ __toESM(require_regenerator());
		init_api();
		init_use_prompt();
		__name(ownKeys$6, "ownKeys");
		__name(_objectSpread$6, "_objectSpread");
		getCodeResult = /*#__PURE__*/ function() {
			var _ref2 = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$1.default.mark(function _callee(payload, _ref) {
				var codeLanguage;
				var htmlMarkup;
				var elementId;
				return import_regenerator$1.default.wrap(function(_context) {
					while (1) switch (_context.prev = _context.next) {
						case 0:
							codeLanguage = _ref.codeLanguage, htmlMarkup = _ref.htmlMarkup, elementId = _ref.elementId;
							if (!("css" === codeLanguage)) {
								_context.next = 1;
								break;
							}
							return _context.abrupt("return", getCustomCSS(_objectSpread$6(_objectSpread$6({}, payload), {}, {
								html_markup: htmlMarkup,
								element_id: elementId
							})));
						case 1: return _context.abrupt("return", getCustomCode(_objectSpread$6(_objectSpread$6({}, payload), {}, { language: codeLanguage })));
						case 2:
						case "end": return _context.stop();
					}
				}, _callee);
			}));
			return function getCodeResult(_x, _x2) {
				return _ref2.apply(this, arguments);
			};
		}();
		useCodePrompt = function useCodePrompt(_ref3) {
			var codeLanguage = _ref3.codeLanguage;
			var htmlMarkup = _ref3.htmlMarkup;
			var elementId = _ref3.elementId;
			var credits = _ref3.initialCredits;
			return usePrompt(function(payload) {
				return getCodeResult(payload, {
					codeLanguage,
					htmlMarkup,
					elementId
				});
			}, { credits });
		};
	}));

//#endregion
//#region modules/ai/assets/js/editor/pages/form-code/splitTextResult.js
	function splitText(inputText) {
		if (!inputText) return {};
		var codeMatch = inputText.match(/```([\s\S]*?)```/);
		var code = codeMatch ? "```".concat(codeMatch[1], "```").trim() : "";
		var detailsMatch = inputText.match(/```[\s\S]*?```([\s\S]*)/);
		return {
			code,
			details: detailsMatch === null || detailsMatch === void 0 ? void 0 : detailsMatch[1].trim()
		};
	}
	var init_splitTextResult = __esmMin((() => {}));

//#endregion
//#region node_modules/react-markdown/lib/uri-transformer.js
/**
	* @param {string} uri
	* @returns {string}
	*/
	function uriTransformer(uri) {
		const url = (uri || "").trim();
		const first = url.charAt(0);
		if (first === "#" || first === "/") return url;
		const colon = url.indexOf(":");
		if (colon === -1) return url;
		let index = -1;
		while (++index < protocols.length) {
			const protocol = protocols[index];
			if (colon === protocol.length && url.slice(0, protocol.length).toLowerCase() === protocol) return url;
		}
		index = url.indexOf("?");
		if (index !== -1 && colon > index) return url;
		index = url.indexOf("#");
		if (index !== -1 && colon > index) return url;
		return "javascript:void(0)";
	}
	var protocols;
	var init_uri_transformer = __esmMin((() => {
		protocols = [
			"http",
			"https",
			"mailto",
			"tel"
		];
	}));

//#endregion
//#region node_modules/vfile/node_modules/is-buffer/index.js
	var require_is_buffer$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		/*!
		* Determine if an object is a Buffer
		*
		* @author   Feross Aboukhadijeh <https://feross.org>
		* @license  MIT
		*/
		module.exports = function isBuffer(obj) {
			return obj != null && obj.constructor != null && typeof obj.constructor.isBuffer === "function" && obj.constructor.isBuffer(obj);
		};
	}));

//#endregion
//#region node_modules/unist-util-stringify-position/lib/index.js
/**
	* @typedef {import('unist').Node} Node
	* @typedef {import('unist').Point} Point
	* @typedef {import('unist').Position} Position
	*/
	/**
	* @typedef NodeLike
	* @property {string} type
	* @property {PositionLike | null | undefined} [position]
	*
	* @typedef PositionLike
	* @property {PointLike | null | undefined} [start]
	* @property {PointLike | null | undefined} [end]
	*
	* @typedef PointLike
	* @property {number | null | undefined} [line]
	* @property {number | null | undefined} [column]
	* @property {number | null | undefined} [offset]
	*/
	/**
	* Serialize the positional info of a point, position (start and end points),
	* or node.
	*
	* @param {Node | NodeLike | Position | PositionLike | Point | PointLike | null | undefined} [value]
	*   Node, position, or point.
	* @returns {string}
	*   Pretty printed positional info of a node (`string`).
	*
	*   In the format of a range `ls:cs-le:ce` (when given `node` or `position`)
	*   or a point `l:c` (when given `point`), where `l` stands for line, `c` for
	*   column, `s` for `start`, and `e` for end.
	*   An empty string (`''`) is returned if the given value is neither `node`,
	*   `position`, nor `point`.
	*/
	function stringifyPosition(value) {
		if (!value || typeof value !== "object") return "";
		if ("position" in value || "type" in value) return position$1(value.position);
		if ("start" in value || "end" in value) return position$1(value);
		if ("line" in value || "column" in value) return point$2(value);
		return "";
	}
	/**
	* @param {Point | PointLike | null | undefined} point
	* @returns {string}
	*/
	function point$2(point) {
		return index(point && point.line) + ":" + index(point && point.column);
	}
	/**
	* @param {Position | PositionLike | null | undefined} pos
	* @returns {string}
	*/
	function position$1(pos) {
		return point$2(pos && pos.start) + "-" + point$2(pos && pos.end);
	}
	/**
	* @param {number | null | undefined} value
	* @returns {number}
	*/
	function index(value) {
		return value && typeof value === "number" ? value : 1;
	}
	var init_lib$15 = __esmMin((() => {
		__name(point$2, "point");
		__name(position$1, "position");
	}));

//#endregion
//#region node_modules/unist-util-stringify-position/index.js
	var init_unist_util_stringify_position = __esmMin((() => {
		init_lib$15();
	}));

//#endregion
//#region node_modules/vfile-message/lib/index.js
	var VFileMessage;
	var init_lib$14 = __esmMin((() => {
		init_unist_util_stringify_position();
		VFileMessage = class extends Error {
			/**
			* Create a message for `reason` at `place` from `origin`.
			*
			* When an error is passed in as `reason`, the `stack` is copied.
			*
			* @param {string | Error | VFileMessage} reason
			*   Reason for message, uses the stack and message of the error if given.
			*
			*   > 👉 **Note**: you should use markdown.
			* @param {Node | NodeLike | Position | Point | null | undefined} [place]
			*   Place in file where the message occurred.
			* @param {string | null | undefined} [origin]
			*   Place in code where the message originates (example:
			*   `'my-package:my-rule'` or `'my-rule'`).
			* @returns
			*   Instance of `VFileMessage`.
			*/
			constructor(reason, place, origin) {
				/** @type {[string | null, string | null]} */
				const parts = [null, null];
				/** @type {Position} */
				let position = {
					start: {
						line: null,
						column: null
					},
					end: {
						line: null,
						column: null
					}
				};
				super();
				if (typeof place === "string") {
					origin = place;
					place = void 0;
				}
				if (typeof origin === "string") {
					const index = origin.indexOf(":");
					if (index === -1) parts[1] = origin;
					else {
						parts[0] = origin.slice(0, index);
						parts[1] = origin.slice(index + 1);
					}
				}
				if (place) {
					if ("type" in place || "position" in place) {
						if (place.position) position = place.position;
					} else if ("start" in place || "end" in place) position = place;
					else if ("line" in place || "column" in place) position.start = place;
				}
				/**
				* Serialized positional info of error.
				*
				* On normal errors, this would be something like `ParseError`, buit in
				* `VFile` messages we use this space to show where an error happened.
				*/
				this.name = stringifyPosition(place) || "1:1";
				/**
				* Reason for message.
				*
				* @type {string}
				*/
				this.message = typeof reason === "object" ? reason.message : reason;
				/**
				* Stack of message.
				*
				* This is used by normal errors to show where something happened in
				* programming code, irrelevant for `VFile` messages,
				*
				* @type {string}
				*/
				this.stack = "";
				if (typeof reason === "object" && reason.stack) this.stack = reason.stack;
				/**
				* Reason for message.
				*
				* > 👉 **Note**: you should use markdown.
				*
				* @type {string}
				*/
				this.reason = this.message;
				/**
				* State of problem.
				*
				* * `true` — marks associated file as no longer processable (error)
				* * `false` — necessitates a (potential) change (warning)
				* * `null | undefined` — for things that might not need changing (info)
				*
				* @type {boolean | null | undefined}
				*/
				this.fatal;
				/**
				* Starting line of error.
				*
				* @type {number | null}
				*/
				this.line = position.start.line;
				/**
				* Starting column of error.
				*
				* @type {number | null}
				*/
				this.column = position.start.column;
				/**
				* Full unist position.
				*
				* @type {Position | null}
				*/
				this.position = position;
				/**
				* Namespace of message (example: `'my-package'`).
				*
				* @type {string | null}
				*/
				this.source = parts[0];
				/**
				* Category of message (example: `'my-rule'`).
				*
				* @type {string | null}
				*/
				this.ruleId = parts[1];
				/**
				* Path of a file (used throughout the `VFile` ecosystem).
				*
				* @type {string | null}
				*/
				this.file;
				/**
				* Specify the source value that’s being reported, which is deemed
				* incorrect.
				*
				* @type {string | null}
				*/
				this.actual;
				/**
				* Suggest acceptable values that can be used instead of `actual`.
				*
				* @type {Array<string> | null}
				*/
				this.expected;
				/**
				* Link to docs for the message.
				*
				* > 👉 **Note**: this must be an absolute URL that can be passed as `x`
				* > to `new URL(x)`.
				*
				* @type {string | null}
				*/
				this.url;
				/**
				* Long form description of the message (you should use markdown).
				*
				* @type {string | null}
				*/
				this.note;
			}
		};
		VFileMessage.prototype.file = "";
		VFileMessage.prototype.name = "";
		VFileMessage.prototype.reason = "";
		VFileMessage.prototype.message = "";
		VFileMessage.prototype.stack = "";
		VFileMessage.prototype.fatal = null;
		VFileMessage.prototype.column = null;
		VFileMessage.prototype.line = null;
		VFileMessage.prototype.source = null;
		VFileMessage.prototype.ruleId = null;
		VFileMessage.prototype.position = null;
	}));

//#endregion
//#region node_modules/vfile-message/index.js
	var init_vfile_message = __esmMin((() => {
		init_lib$14();
	}));

//#endregion
//#region node_modules/vfile/lib/minpath.browser.js
/**
	* Get the basename from a path.
	*
	* @param {string} path
	*   File path.
	* @param {string | undefined} [ext]
	*   Extension to strip.
	* @returns {string}
	*   Stem or basename.
	*/
	function basename(path, ext) {
		if (ext !== void 0 && typeof ext !== "string") throw new TypeError("\"ext\" argument must be a string");
		assertPath$1(path);
		let start = 0;
		let end = -1;
		let index = path.length;
		/** @type {boolean | undefined} */
		let seenNonSlash;
		if (ext === void 0 || ext.length === 0 || ext.length > path.length) {
			while (index--) if (path.charCodeAt(index) === 47) {
				if (seenNonSlash) {
					start = index + 1;
					break;
				}
			} else if (end < 0) {
				seenNonSlash = true;
				end = index + 1;
			}
			return end < 0 ? "" : path.slice(start, end);
		}
		if (ext === path) return "";
		let firstNonSlashEnd = -1;
		let extIndex = ext.length - 1;
		while (index--) if (path.charCodeAt(index) === 47) {
			if (seenNonSlash) {
				start = index + 1;
				break;
			}
		} else {
			if (firstNonSlashEnd < 0) {
				seenNonSlash = true;
				firstNonSlashEnd = index + 1;
			}
			if (extIndex > -1) if (path.charCodeAt(index) === ext.charCodeAt(extIndex--)) {
				if (extIndex < 0) end = index;
			} else {
				extIndex = -1;
				end = firstNonSlashEnd;
			}
		}
		if (start === end) end = firstNonSlashEnd;
		else if (end < 0) end = path.length;
		return path.slice(start, end);
	}
	/**
	* Get the dirname from a path.
	*
	* @param {string} path
	*   File path.
	* @returns {string}
	*   File path.
	*/
	function dirname(path) {
		assertPath$1(path);
		if (path.length === 0) return ".";
		let end = -1;
		let index = path.length;
		/** @type {boolean | undefined} */
		let unmatchedSlash;
		while (--index) if (path.charCodeAt(index) === 47) {
			if (unmatchedSlash) {
				end = index;
				break;
			}
		} else if (!unmatchedSlash) unmatchedSlash = true;
		return end < 0 ? path.charCodeAt(0) === 47 ? "/" : "." : end === 1 && path.charCodeAt(0) === 47 ? "//" : path.slice(0, end);
	}
	/**
	* Get an extname from a path.
	*
	* @param {string} path
	*   File path.
	* @returns {string}
	*   Extname.
	*/
	function extname(path) {
		assertPath$1(path);
		let index = path.length;
		let end = -1;
		let startPart = 0;
		let startDot = -1;
		let preDotState = 0;
		/** @type {boolean | undefined} */
		let unmatchedSlash;
		while (index--) {
			const code = path.charCodeAt(index);
			if (code === 47) {
				if (unmatchedSlash) {
					startPart = index + 1;
					break;
				}
				continue;
			}
			if (end < 0) {
				unmatchedSlash = true;
				end = index + 1;
			}
			if (code === 46) {
				if (startDot < 0) startDot = index;
				else if (preDotState !== 1) preDotState = 1;
			} else if (startDot > -1) preDotState = -1;
		}
		if (startDot < 0 || end < 0 || preDotState === 0 || preDotState === 1 && startDot === end - 1 && startDot === startPart + 1) return "";
		return path.slice(startDot, end);
	}
	/**
	* Join segments from a path.
	*
	* @param {Array<string>} segments
	*   Path segments.
	* @returns {string}
	*   File path.
	*/
	function join(...segments) {
		let index = -1;
		/** @type {string | undefined} */
		let joined;
		while (++index < segments.length) {
			assertPath$1(segments[index]);
			if (segments[index]) joined = joined === void 0 ? segments[index] : joined + "/" + segments[index];
		}
		return joined === void 0 ? "." : normalize$1(joined);
	}
	/**
	* Normalize a basic file path.
	*
	* @param {string} path
	*   File path.
	* @returns {string}
	*   File path.
	*/
	function normalize$1(path) {
		assertPath$1(path);
		const absolute = path.charCodeAt(0) === 47;
		let value = normalizeString(path, !absolute);
		if (value.length === 0 && !absolute) value = ".";
		if (value.length > 0 && path.charCodeAt(path.length - 1) === 47) value += "/";
		return absolute ? "/" + value : value;
	}
	/**
	* Resolve `.` and `..` elements in a path with directory names.
	*
	* @param {string} path
	*   File path.
	* @param {boolean} allowAboveRoot
	*   Whether `..` can move above root.
	* @returns {string}
	*   File path.
	*/
	function normalizeString(path, allowAboveRoot) {
		let result = "";
		let lastSegmentLength = 0;
		let lastSlash = -1;
		let dots = 0;
		let index = -1;
		/** @type {number | undefined} */
		let code;
		/** @type {number} */
		let lastSlashIndex;
		while (++index <= path.length) {
			if (index < path.length) code = path.charCodeAt(index);
			else if (code === 47) break;
			else code = 47;
			if (code === 47) {
				if (lastSlash === index - 1 || dots === 1) {} else if (lastSlash !== index - 1 && dots === 2) {
					if (result.length < 2 || lastSegmentLength !== 2 || result.charCodeAt(result.length - 1) !== 46 || result.charCodeAt(result.length - 2) !== 46) {
						if (result.length > 2) {
							lastSlashIndex = result.lastIndexOf("/");
							if (lastSlashIndex !== result.length - 1) {
								if (lastSlashIndex < 0) {
									result = "";
									lastSegmentLength = 0;
								} else {
									result = result.slice(0, lastSlashIndex);
									lastSegmentLength = result.length - 1 - result.lastIndexOf("/");
								}
								lastSlash = index;
								dots = 0;
								continue;
							}
						} else if (result.length > 0) {
							result = "";
							lastSegmentLength = 0;
							lastSlash = index;
							dots = 0;
							continue;
						}
					}
					if (allowAboveRoot) {
						result = result.length > 0 ? result + "/.." : "..";
						lastSegmentLength = 2;
					}
				} else {
					if (result.length > 0) result += "/" + path.slice(lastSlash + 1, index);
					else result = path.slice(lastSlash + 1, index);
					lastSegmentLength = index - lastSlash - 1;
				}
				lastSlash = index;
				dots = 0;
			} else if (code === 46 && dots > -1) dots++;
			else dots = -1;
		}
		return result;
	}
	/**
	* Make sure `path` is a string.
	*
	* @param {string} path
	*   File path.
	* @returns {asserts path is string}
	*   Nothing.
	*/
	function assertPath$1(path) {
		if (typeof path !== "string") throw new TypeError("Path must be a string. Received " + JSON.stringify(path));
	}
	var path;
	var init_minpath_browser = __esmMin((() => {
		path = {
			basename,
			dirname,
			extname,
			join,
			sep: "/"
		};
		__name(normalize$1, "normalize");
		__name(assertPath$1, "assertPath");
	}));

//#endregion
//#region node_modules/vfile/lib/minproc.browser.js
	function cwd() {
		return "/";
	}
	var proc;
	var init_minproc_browser = __esmMin((() => {
		proc = { cwd };
	}));

//#endregion
//#region node_modules/vfile/lib/minurl.shared.js
/**
	* @typedef URL
	* @property {string} hash
	* @property {string} host
	* @property {string} hostname
	* @property {string} href
	* @property {string} origin
	* @property {string} password
	* @property {string} pathname
	* @property {string} port
	* @property {string} protocol
	* @property {string} search
	* @property {any} searchParams
	* @property {string} username
	* @property {() => string} toString
	* @property {() => string} toJSON
	*/
	/**
	* Check if `fileUrlOrPath` looks like a URL.
	*
	* @param {unknown} fileUrlOrPath
	*   File path or URL.
	* @returns {fileUrlOrPath is URL}
	*   Whether it’s a URL.
	*/
	function isUrl(fileUrlOrPath) {
		return fileUrlOrPath !== null && typeof fileUrlOrPath === "object" && fileUrlOrPath.href && fileUrlOrPath.origin;
	}
	var init_minurl_shared = __esmMin((() => {}));

//#endregion
//#region node_modules/vfile/lib/minurl.browser.js
/**
	* @param {string | URL} path
	*   File URL.
	* @returns {string}
	*   File URL.
	*/
	function urlToPath(path) {
		if (typeof path === "string") path = new URL(path);
		else if (!isUrl(path)) {
			/** @type {NodeJS.ErrnoException} */
			const error = /* @__PURE__ */ new TypeError("The \"path\" argument must be of type string or an instance of URL. Received `" + path + "`");
			error.code = "ERR_INVALID_ARG_TYPE";
			throw error;
		}
		if (path.protocol !== "file:") {
			/** @type {NodeJS.ErrnoException} */
			const error = /* @__PURE__ */ new TypeError("The URL must be of scheme file");
			error.code = "ERR_INVALID_URL_SCHEME";
			throw error;
		}
		return getPathFromURLPosix(path);
	}
	/**
	* Get a path from a POSIX URL.
	*
	* @param {URL} url
	*   URL.
	* @returns {string}
	*   File path.
	*/
	function getPathFromURLPosix(url) {
		if (url.hostname !== "") {
			/** @type {NodeJS.ErrnoException} */
			const error = /* @__PURE__ */ new TypeError("File URL host must be \"localhost\" or empty on darwin");
			error.code = "ERR_INVALID_FILE_URL_HOST";
			throw error;
		}
		const pathname = url.pathname;
		let index = -1;
		while (++index < pathname.length) if (pathname.charCodeAt(index) === 37 && pathname.charCodeAt(index + 1) === 50) {
			const third = pathname.charCodeAt(index + 2);
			if (third === 70 || third === 102) {
				/** @type {NodeJS.ErrnoException} */
				const error = /* @__PURE__ */ new TypeError("File URL path must not include encoded / characters");
				error.code = "ERR_INVALID_FILE_URL_PATH";
				throw error;
			}
		}
		return decodeURIComponent(pathname);
	}
	var init_minurl_browser = __esmMin((() => {
		init_minurl_shared();
	}));

//#endregion
//#region node_modules/vfile/lib/index.js
/**
	* Assert that `part` is not a path (as in, does not contain `path.sep`).
	*
	* @param {string | null | undefined} part
	*   File path part.
	* @param {string} name
	*   Part name.
	* @returns {void}
	*   Nothing.
	*/
	function assertPart(part, name) {
		if (part && part.includes(path.sep)) throw new Error("`" + name + "` cannot be a path: did not expect `" + path.sep + "`");
	}
	/**
	* Assert that `part` is not empty.
	*
	* @param {string | undefined} part
	*   Thing.
	* @param {string} name
	*   Part name.
	* @returns {asserts part is string}
	*   Nothing.
	*/
	function assertNonEmpty(part, name) {
		if (!part) throw new Error("`" + name + "` cannot be empty");
	}
	/**
	* Assert `path` exists.
	*
	* @param {string | undefined} path
	*   Path.
	* @param {string} name
	*   Dependency name.
	* @returns {asserts path is string}
	*   Nothing.
	*/
	function assertPath(path, name) {
		if (!path) throw new Error("Setting `" + name + "` requires `path` to be set too");
	}
	/**
	* Assert `value` is a buffer.
	*
	* @param {unknown} value
	*   thing.
	* @returns {value is Buffer}
	*   Whether `value` is a Node.js buffer.
	*/
	function buffer(value) {
		return (0, import_is_buffer$1.default)(value);
	}
	var import_is_buffer$1, order, VFile;
	var init_lib$13 = __esmMin((() => {
		import_is_buffer$1 = /* @__PURE__ */ __toESM(require_is_buffer$1(), 1);
		init_vfile_message();
		init_minpath_browser();
		init_minproc_browser();
		init_minurl_browser();
		order = [
			"history",
			"path",
			"basename",
			"stem",
			"extname",
			"dirname"
		];
		VFile = class {
			/**
			* Create a new virtual file.
			*
			* `options` is treated as:
			*
			* *   `string` or `Buffer` — `{value: options}`
			* *   `URL` — `{path: options}`
			* *   `VFile` — shallow copies its data over to the new file
			* *   `object` — all fields are shallow copied over to the new file
			*
			* Path related fields are set in the following order (least specific to
			* most specific): `history`, `path`, `basename`, `stem`, `extname`,
			* `dirname`.
			*
			* You cannot set `dirname` or `extname` without setting either `history`,
			* `path`, `basename`, or `stem` too.
			*
			* @param {Compatible | null | undefined} [value]
			*   File value.
			* @returns
			*   New instance.
			*/
			constructor(value) {
				/** @type {Options | VFile} */
				let options;
				if (!value) options = {};
				else if (typeof value === "string" || buffer(value)) options = { value };
				else if (isUrl(value)) options = { path: value };
				else options = value;
				/**
				* Place to store custom information (default: `{}`).
				*
				* It’s OK to store custom data directly on the file but moving it to
				* `data` is recommended.
				*
				* @type {Data}
				*/
				this.data = {};
				/**
				* List of messages associated with the file.
				*
				* @type {Array<VFileMessage>}
				*/
				this.messages = [];
				/**
				* List of filepaths the file moved between.
				*
				* The first is the original path and the last is the current path.
				*
				* @type {Array<string>}
				*/
				this.history = [];
				/**
				* Base of `path` (default: `process.cwd()` or `'/'` in browsers).
				*
				* @type {string}
				*/
				this.cwd = proc.cwd();
				/**
				* Raw value.
				*
				* @type {Value}
				*/
				this.value;
				/**
				* Whether a file was saved to disk.
				*
				* This is used by vfile reporters.
				*
				* @type {boolean}
				*/
				this.stored;
				/**
				* Custom, non-string, compiled, representation.
				*
				* This is used by unified to store non-string results.
				* One example is when turning markdown into React nodes.
				*
				* @type {unknown}
				*/
				this.result;
				/**
				* Source map.
				*
				* This type is equivalent to the `RawSourceMap` type from the `source-map`
				* module.
				*
				* @type {Map | null | undefined}
				*/
				this.map;
				let index = -1;
				while (++index < order.length) {
					const prop = order[index];
					if (prop in options && options[prop] !== void 0 && options[prop] !== null) this[prop] = prop === "history" ? [...options[prop]] : options[prop];
				}
				/** @type {string} */
				let prop;
				for (prop in options) if (!order.includes(prop)) this[prop] = options[prop];
			}
			/**
			* Get the full path (example: `'~/index.min.js'`).
			*
			* @returns {string}
			*/
			get path() {
				return this.history[this.history.length - 1];
			}
			/**
			* Set the full path (example: `'~/index.min.js'`).
			*
			* Cannot be nullified.
			* You can set a file URL (a `URL` object with a `file:` protocol) which will
			* be turned into a path with `url.fileURLToPath`.
			*
			* @param {string | URL} path
			*/
			set path(path) {
				if (isUrl(path)) path = urlToPath(path);
				assertNonEmpty(path, "path");
				if (this.path !== path) this.history.push(path);
			}
			/**
			* Get the parent path (example: `'~'`).
			*/
			get dirname() {
				return typeof this.path === "string" ? path.dirname(this.path) : void 0;
			}
			/**
			* Set the parent path (example: `'~'`).
			*
			* Cannot be set if there’s no `path` yet.
			*/
			set dirname(dirname) {
				assertPath(this.basename, "dirname");
				this.path = path.join(dirname || "", this.basename);
			}
			/**
			* Get the basename (including extname) (example: `'index.min.js'`).
			*/
			get basename() {
				return typeof this.path === "string" ? path.basename(this.path) : void 0;
			}
			/**
			* Set basename (including extname) (`'index.min.js'`).
			*
			* Cannot contain path separators (`'/'` on unix, macOS, and browsers, `'\'`
			* on windows).
			* Cannot be nullified (use `file.path = file.dirname` instead).
			*/
			set basename(basename) {
				assertNonEmpty(basename, "basename");
				assertPart(basename, "basename");
				this.path = path.join(this.dirname || "", basename);
			}
			/**
			* Get the extname (including dot) (example: `'.js'`).
			*/
			get extname() {
				return typeof this.path === "string" ? path.extname(this.path) : void 0;
			}
			/**
			* Set the extname (including dot) (example: `'.js'`).
			*
			* Cannot contain path separators (`'/'` on unix, macOS, and browsers, `'\'`
			* on windows).
			* Cannot be set if there’s no `path` yet.
			*/
			set extname(extname) {
				assertPart(extname, "extname");
				assertPath(this.dirname, "extname");
				if (extname) {
					if (extname.charCodeAt(0) !== 46) throw new Error("`extname` must start with `.`");
					if (extname.includes(".", 1)) throw new Error("`extname` cannot contain multiple dots");
				}
				this.path = path.join(this.dirname, this.stem + (extname || ""));
			}
			/**
			* Get the stem (basename w/o extname) (example: `'index.min'`).
			*/
			get stem() {
				return typeof this.path === "string" ? path.basename(this.path, this.extname) : void 0;
			}
			/**
			* Set the stem (basename w/o extname) (example: `'index.min'`).
			*
			* Cannot contain path separators (`'/'` on unix, macOS, and browsers, `'\'`
			* on windows).
			* Cannot be nullified (use `file.path = file.dirname` instead).
			*/
			set stem(stem) {
				assertNonEmpty(stem, "stem");
				assertPart(stem, "stem");
				this.path = path.join(this.dirname || "", stem + (this.extname || ""));
			}
			/**
			* Serialize the file.
			*
			* @param {BufferEncoding | null | undefined} [encoding='utf8']
			*   Character encoding to understand `value` as when it’s a `Buffer`
			*   (default: `'utf8'`).
			* @returns {string}
			*   Serialized file.
			*/
			toString(encoding) {
				return (this.value || "").toString(encoding || void 0);
			}
			/**
			* Create a warning message associated with the file.
			*
			* Its `fatal` is set to `false` and `file` is set to the current file path.
			* Its added to `file.messages`.
			*
			* @param {string | Error | VFileMessage} reason
			*   Reason for message, uses the stack and message of the error if given.
			* @param {Node | NodeLike | Position | Point | null | undefined} [place]
			*   Place in file where the message occurred.
			* @param {string | null | undefined} [origin]
			*   Place in code where the message originates (example:
			*   `'my-package:my-rule'` or `'my-rule'`).
			* @returns {VFileMessage}
			*   Message.
			*/
			message(reason, place, origin) {
				const message = new VFileMessage(reason, place, origin);
				if (this.path) {
					message.name = this.path + ":" + message.name;
					message.file = this.path;
				}
				message.fatal = false;
				this.messages.push(message);
				return message;
			}
			/**
			* Create an info message associated with the file.
			*
			* Its `fatal` is set to `null` and `file` is set to the current file path.
			* Its added to `file.messages`.
			*
			* @param {string | Error | VFileMessage} reason
			*   Reason for message, uses the stack and message of the error if given.
			* @param {Node | NodeLike | Position | Point | null | undefined} [place]
			*   Place in file where the message occurred.
			* @param {string | null | undefined} [origin]
			*   Place in code where the message originates (example:
			*   `'my-package:my-rule'` or `'my-rule'`).
			* @returns {VFileMessage}
			*   Message.
			*/
			info(reason, place, origin) {
				const message = this.message(reason, place, origin);
				message.fatal = null;
				return message;
			}
			/**
			* Create a fatal error associated with the file.
			*
			* Its `fatal` is set to `true` and `file` is set to the current file path.
			* Its added to `file.messages`.
			*
			* > 👉 **Note**: a fatal error means that a file is no longer processable.
			*
			* @param {string | Error | VFileMessage} reason
			*   Reason for message, uses the stack and message of the error if given.
			* @param {Node | NodeLike | Position | Point | null | undefined} [place]
			*   Place in file where the message occurred.
			* @param {string | null | undefined} [origin]
			*   Place in code where the message originates (example:
			*   `'my-package:my-rule'` or `'my-rule'`).
			* @returns {never}
			*   Message.
			* @throws {VFileMessage}
			*   Message.
			*/
			fail(reason, place, origin) {
				const message = this.message(reason, place, origin);
				message.fatal = true;
				throw message;
			}
		};
	}));

//#endregion
//#region node_modules/vfile/index.js
	var init_vfile = __esmMin((() => {
		init_lib$13();
	}));

//#endregion
//#region node_modules/bail/index.js
/**
	* Throw a given error.
	*
	* @param {Error|null|undefined} [error]
	*   Maybe error.
	* @returns {asserts error is null|undefined}
	*/
	function bail(error) {
		if (error) throw error;
	}
	var init_bail = __esmMin((() => {}));

//#endregion
//#region node_modules/unified/node_modules/is-buffer/index.js
	var require_is_buffer = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		/*!
		* Determine if an object is a Buffer
		*
		* @author   Feross Aboukhadijeh <https://feross.org>
		* @license  MIT
		*/
		module.exports = function isBuffer(obj) {
			return obj != null && obj.constructor != null && typeof obj.constructor.isBuffer === "function" && obj.constructor.isBuffer(obj);
		};
	}));

//#endregion
//#region node_modules/extend/index.js
	var require_extend = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		var hasOwn = Object.prototype.hasOwnProperty;
		var toStr = Object.prototype.toString;
		var defineProperty = Object.defineProperty;
		var gOPD = Object.getOwnPropertyDescriptor;
		var isArray = function isArray(arr) {
			if (typeof Array.isArray === "function") return Array.isArray(arr);
			return toStr.call(arr) === "[object Array]";
		};
		var isPlainObject = function isPlainObject(obj) {
			if (!obj || toStr.call(obj) !== "[object Object]") return false;
			var hasOwnConstructor = hasOwn.call(obj, "constructor");
			var hasIsPrototypeOf = obj.constructor && obj.constructor.prototype && hasOwn.call(obj.constructor.prototype, "isPrototypeOf");
			if (obj.constructor && !hasOwnConstructor && !hasIsPrototypeOf) return false;
			var key;
			for (key in obj);
			return typeof key === "undefined" || hasOwn.call(obj, key);
		};
		var setProperty = function setProperty(target, options) {
			if (defineProperty && options.name === "__proto__") defineProperty(target, options.name, {
				enumerable: true,
				configurable: true,
				value: options.newValue,
				writable: true
			});
			else target[options.name] = options.newValue;
		};
		var getProperty = function getProperty(obj, name) {
			if (name === "__proto__") {
				if (!hasOwn.call(obj, name)) return;
				else if (gOPD) return gOPD(obj, name).value;
			}
			return obj[name];
		};
		module.exports = function extend() {
			var options;
			var name;
			var src;
			var copy;
			var copyIsArray;
			var clone;
			var target = arguments[0];
			var i = 1;
			var length = arguments.length;
			var deep = false;
			if (typeof target === "boolean") {
				deep = target;
				target = arguments[1] || {};
				i = 2;
			}
			if (target == null || typeof target !== "object" && typeof target !== "function") target = {};
			for (; i < length; ++i) {
				options = arguments[i];
				if (options != null) for (name in options) {
					src = getProperty(target, name);
					copy = getProperty(options, name);
					if (target !== copy) {
						if (deep && copy && (isPlainObject(copy) || (copyIsArray = isArray(copy)))) {
							if (copyIsArray) {
								copyIsArray = false;
								clone = src && isArray(src) ? src : [];
							} else clone = src && isPlainObject(src) ? src : {};
							setProperty(target, {
								name,
								newValue: extend(deep, clone, copy)
							});
						} else if (typeof copy !== "undefined") setProperty(target, {
							name,
							newValue: copy
						});
					}
				}
			}
			return target;
		};
	}));

//#endregion
//#region node_modules/is-plain-obj/index.js
	function isPlainObject(value) {
		if (typeof value !== "object" || value === null) return false;
		const prototype = Object.getPrototypeOf(value);
		return (prototype === null || prototype === Object.prototype || Object.getPrototypeOf(prototype) === null) && !(Symbol.toStringTag in value) && !(Symbol.iterator in value);
	}
	var init_is_plain_obj = __esmMin((() => {}));

//#endregion
//#region node_modules/trough/lib/index.js
/**
	* @typedef {(error?: Error | null | undefined, ...output: Array<any>) => void} Callback
	*   Callback.
	*
	* @typedef {(...input: Array<any>) => any} Middleware
	*   Ware.
	*
	* @typedef Pipeline
	*   Pipeline.
	* @property {Run} run
	*   Run the pipeline.
	* @property {Use} use
	*   Add middleware.
	*
	* @typedef {(...input: Array<any>) => void} Run
	*   Call all middleware.
	*
	*   Calls `done` on completion with either an error or the output of the
	*   last middleware.
	*
	*   > 👉 **Note**: as the length of input defines whether async functions get a
	*   > `next` function,
	*   > it’s recommended to keep `input` at one value normally.
	
	*
	* @typedef {(fn: Middleware) => Pipeline} Use
	*   Add middleware.
	*/
	/**
	* Create new middleware.
	*
	* @returns {Pipeline}
	*   Pipeline.
	*/
	function trough() {
		/** @type {Array<Middleware>} */
		const fns = [];
		/** @type {Pipeline} */
		const pipeline = {
			run,
			use
		};
		return pipeline;
		/** @type {Run} */
		function run(...values) {
			let middlewareIndex = -1;
			/** @type {Callback} */
			const callback = values.pop();
			if (typeof callback !== "function") throw new TypeError("Expected function as last argument, not " + callback);
			next(null, ...values);
			/**
			* Run the next `fn`, or we’re done.
			*
			* @param {Error | null | undefined} error
			* @param {Array<any>} output
			*/
			function next(error, ...output) {
				const fn = fns[++middlewareIndex];
				let index = -1;
				if (error) {
					callback(error);
					return;
				}
				while (++index < values.length) if (output[index] === null || output[index] === void 0) output[index] = values[index];
				values = output;
				if (fn) wrap$1(fn, next)(...output);
				else callback(null, ...output);
			}
		}
		/** @type {Use} */
		function use(middelware) {
			if (typeof middelware !== "function") throw new TypeError("Expected `middelware` to be a function, not " + middelware);
			fns.push(middelware);
			return pipeline;
		}
	}
	/**
	* Wrap `middleware` into a uniform interface.
	*
	* You can pass all input to the resulting function.
	* `callback` is then called with the output of `middleware`.
	*
	* If `middleware` accepts more arguments than the later given in input,
	* an extra `done` function is passed to it after that input,
	* which must be called by `middleware`.
	*
	* The first value in `input` is the main input value.
	* All other input values are the rest input values.
	* The values given to `callback` are the input values,
	* merged with every non-nullish output value.
	*
	* * if `middleware` throws an error,
	*   returns a promise that is rejected,
	*   or calls the given `done` function with an error,
	*   `callback` is called with that error
	* * if `middleware` returns a value or returns a promise that is resolved,
	*   that value is the main output value
	* * if `middleware` calls `done`,
	*   all non-nullish values except for the first one (the error) overwrite the
	*   output values
	*
	* @param {Middleware} middleware
	*   Function to wrap.
	* @param {Callback} callback
	*   Callback called with the output of `middleware`.
	* @returns {Run}
	*   Wrapped middleware.
	*/
	function wrap$1(middleware, callback) {
		/** @type {boolean} */
		let called;
		return wrapped;
		/**
		* Call `middleware`.
		* @this {any}
		* @param {Array<any>} parameters
		* @returns {void}
		*/
		function wrapped(...parameters) {
			const fnExpectsCallback = middleware.length > parameters.length;
			/** @type {any} */
			let result;
			if (fnExpectsCallback) parameters.push(done);
			try {
				result = middleware.apply(this, parameters);
			} catch (error) {
				const exception = error;
				if (fnExpectsCallback && called) throw exception;
				return done(exception);
			}
			if (!fnExpectsCallback) if (result && result.then && typeof result.then === "function") result.then(then, done);
			else if (result instanceof Error) done(result);
			else then(result);
		}
		/**
		* Call `callback`, only once.
		*
		* @type {Callback}
		*/
		function done(error, ...output) {
			if (!called) {
				called = true;
				callback(error, ...output);
			}
		}
		/**
		* Call `done` with one value.
		*
		* @param {any} [value]
		*/
		function then(value) {
			done(null, value);
		}
	}
	var init_lib$12 = __esmMin((() => {
		__name(wrap$1, "wrap");
	}));

//#endregion
//#region node_modules/trough/index.js
	var init_trough = __esmMin((() => {
		init_lib$12();
	}));

//#endregion
//#region node_modules/unified/lib/index.js
/**
	* @returns {Processor}
	*/
	function base() {
		const transformers = trough();
		/** @type {Processor['attachers']} */
		const attachers = [];
		/** @type {Record<string, unknown>} */
		let namespace = {};
		/** @type {boolean|undefined} */
		let frozen;
		let freezeIndex = -1;
		processor.data = data;
		processor.Parser = void 0;
		processor.Compiler = void 0;
		processor.freeze = freeze;
		processor.attachers = attachers;
		processor.use = use;
		processor.parse = parse;
		processor.stringify = stringify;
		processor.run = run;
		processor.runSync = runSync;
		processor.process = process;
		processor.processSync = processSync;
		return processor;
		/** @type {Processor} */
		function processor() {
			const destination = base();
			let index = -1;
			while (++index < attachers.length) destination.use(...attachers[index]);
			destination.data((0, import_extend.default)(true, {}, namespace));
			return destination;
		}
		/**
		* @param {string|Record<string, unknown>} [key]
		* @param {unknown} [value]
		* @returns {unknown}
		*/
		function data(key, value) {
			if (typeof key === "string") {
				if (arguments.length === 2) {
					assertUnfrozen("data", frozen);
					namespace[key] = value;
					return processor;
				}
				return own$6.call(namespace, key) && namespace[key] || null;
			}
			if (key) {
				assertUnfrozen("data", frozen);
				namespace = key;
				return processor;
			}
			return namespace;
		}
		/** @type {Processor['freeze']} */
		function freeze() {
			if (frozen) return processor;
			while (++freezeIndex < attachers.length) {
				const [attacher, ...options] = attachers[freezeIndex];
				if (options[0] === false) continue;
				if (options[0] === true) options[0] = void 0;
				/** @type {Transformer|void} */
				const transformer = attacher.call(processor, ...options);
				if (typeof transformer === "function") transformers.use(transformer);
			}
			frozen = true;
			freezeIndex = Number.POSITIVE_INFINITY;
			return processor;
		}
		/**
		* @param {Pluggable|null|undefined} [value]
		* @param {...unknown} options
		* @returns {Processor}
		*/
		function use(value, ...options) {
			/** @type {Record<string, unknown>|undefined} */
			let settings;
			assertUnfrozen("use", frozen);
			if (value === null || value === void 0) {} else if (typeof value === "function") addPlugin(value, ...options);
			else if (typeof value === "object") if (Array.isArray(value)) addList(value);
			else addPreset(value);
			else throw new TypeError("Expected usable value, not `" + value + "`");
			if (settings) namespace.settings = Object.assign(namespace.settings || {}, settings);
			return processor;
			/**
			* @param {import('..').Pluggable<unknown[]>} value
			* @returns {void}
			*/
			function add(value) {
				if (typeof value === "function") addPlugin(value);
				else if (typeof value === "object") if (Array.isArray(value)) {
					const [plugin, ...options] = value;
					addPlugin(plugin, ...options);
				} else addPreset(value);
				else throw new TypeError("Expected usable value, not `" + value + "`");
			}
			/**
			* @param {Preset} result
			* @returns {void}
			*/
			function addPreset(result) {
				addList(result.plugins);
				if (result.settings) settings = Object.assign(settings || {}, result.settings);
			}
			/**
			* @param {PluggableList|null|undefined} [plugins]
			* @returns {void}
			*/
			function addList(plugins) {
				let index = -1;
				if (plugins === null || plugins === void 0) {} else if (Array.isArray(plugins)) while (++index < plugins.length) {
					const thing = plugins[index];
					add(thing);
				}
				else throw new TypeError("Expected a list of plugins, not `" + plugins + "`");
			}
			/**
			* @param {Plugin} plugin
			* @param {...unknown} [value]
			* @returns {void}
			*/
			function addPlugin(plugin, value) {
				let index = -1;
				/** @type {Processor['attachers'][number]|undefined} */
				let entry;
				while (++index < attachers.length) if (attachers[index][0] === plugin) {
					entry = attachers[index];
					break;
				}
				if (entry) {
					if (isPlainObject(entry[1]) && isPlainObject(value)) value = (0, import_extend.default)(true, entry[1], value);
					entry[1] = value;
				} else attachers.push([...arguments]);
			}
		}
		/** @type {Processor['parse']} */
		function parse(doc) {
			processor.freeze();
			const file = vfile(doc);
			const Parser = processor.Parser;
			assertParser("parse", Parser);
			if (newable(Parser, "parse")) return new Parser(String(file), file).parse();
			return Parser(String(file), file);
		}
		/** @type {Processor['stringify']} */
		function stringify(node, doc) {
			processor.freeze();
			const file = vfile(doc);
			const Compiler = processor.Compiler;
			assertCompiler("stringify", Compiler);
			assertNode(node);
			if (newable(Compiler, "compile")) return new Compiler(node, file).compile();
			return Compiler(node, file);
		}
		/**
		* @param {Node} node
		* @param {VFileCompatible|RunCallback} [doc]
		* @param {RunCallback} [callback]
		* @returns {Promise<Node>|void}
		*/
		function run(node, doc, callback) {
			assertNode(node);
			processor.freeze();
			if (!callback && typeof doc === "function") {
				callback = doc;
				doc = void 0;
			}
			if (!callback) return new Promise(executor);
			executor(null, callback);
			/**
			* @param {null|((node: Node) => void)} resolve
			* @param {(error: Error) => void} reject
			* @returns {void}
			*/
			function executor(resolve, reject) {
				transformers.run(node, vfile(doc), done);
				/**
				* @param {Error|null} error
				* @param {Node} tree
				* @param {VFile} file
				* @returns {void}
				*/
				function done(error, tree, file) {
					tree = tree || node;
					if (error) reject(error);
					else if (resolve) resolve(tree);
					else callback(null, tree, file);
				}
			}
		}
		/** @type {Processor['runSync']} */
		function runSync(node, file) {
			/** @type {Node|undefined} */
			let result;
			/** @type {boolean|undefined} */
			let complete;
			processor.run(node, file, done);
			assertDone("runSync", "run", complete);
			return result;
			/**
			* @param {Error|null} [error]
			* @param {Node} [tree]
			* @returns {void}
			*/
			function done(error, tree) {
				bail(error);
				result = tree;
				complete = true;
			}
		}
		/**
		* @param {VFileCompatible} doc
		* @param {ProcessCallback} [callback]
		* @returns {Promise<VFile>|undefined}
		*/
		function process(doc, callback) {
			processor.freeze();
			assertParser("process", processor.Parser);
			assertCompiler("process", processor.Compiler);
			if (!callback) return new Promise(executor);
			executor(null, callback);
			/**
			* @param {null|((file: VFile) => void)} resolve
			* @param {(error?: Error|null|undefined) => void} reject
			* @returns {void}
			*/
			function executor(resolve, reject) {
				const file = vfile(doc);
				processor.run(processor.parse(file), file, (error, tree, file) => {
					if (error || !tree || !file) done(error);
					else {
						/** @type {unknown} */
						const result = processor.stringify(tree, file);
						if (result === void 0 || result === null) {} else if (looksLikeAVFileValue(result)) file.value = result;
						else file.result = result;
						done(error, file);
					}
				});
				/**
				* @param {Error|null|undefined} [error]
				* @param {VFile|undefined} [file]
				* @returns {void}
				*/
				function done(error, file) {
					if (error || !file) reject(error);
					else if (resolve) resolve(file);
					else callback(null, file);
				}
			}
		}
		/** @type {Processor['processSync']} */
		function processSync(doc) {
			/** @type {boolean|undefined} */
			let complete;
			processor.freeze();
			assertParser("processSync", processor.Parser);
			assertCompiler("processSync", processor.Compiler);
			const file = vfile(doc);
			processor.process(file, done);
			assertDone("processSync", "process", complete);
			return file;
			/**
			* @param {Error|null|undefined} [error]
			* @returns {void}
			*/
			function done(error) {
				complete = true;
				bail(error);
			}
		}
	}
	/**
	* Check if `value` is a constructor.
	*
	* @param {unknown} value
	* @param {string} name
	* @returns {boolean}
	*/
	function newable(value, name) {
		return typeof value === "function" && value.prototype && (keys(value.prototype) || name in value.prototype);
	}
	/**
	* Check if `value` is an object with keys.
	*
	* @param {Record<string, unknown>} value
	* @returns {boolean}
	*/
	function keys(value) {
		/** @type {string} */
		let key;
		for (key in value) if (own$6.call(value, key)) return true;
		return false;
	}
	/**
	* Assert a parser is available.
	*
	* @param {string} name
	* @param {unknown} value
	* @returns {asserts value is Parser}
	*/
	function assertParser(name, value) {
		if (typeof value !== "function") throw new TypeError("Cannot `" + name + "` without `Parser`");
	}
	/**
	* Assert a compiler is available.
	*
	* @param {string} name
	* @param {unknown} value
	* @returns {asserts value is Compiler}
	*/
	function assertCompiler(name, value) {
		if (typeof value !== "function") throw new TypeError("Cannot `" + name + "` without `Compiler`");
	}
	/**
	* Assert the processor is not frozen.
	*
	* @param {string} name
	* @param {unknown} frozen
	* @returns {asserts frozen is false}
	*/
	function assertUnfrozen(name, frozen) {
		if (frozen) throw new Error("Cannot call `" + name + "` on a frozen processor.\nCreate a new processor first, by calling it: use `processor()` instead of `processor`.");
	}
	/**
	* Assert `node` is a unist node.
	*
	* @param {unknown} node
	* @returns {asserts node is Node}
	*/
	function assertNode(node) {
		if (!isPlainObject(node) || typeof node.type !== "string") throw new TypeError("Expected node, got `" + node + "`");
	}
	/**
	* Assert that `complete` is `true`.
	*
	* @param {string} name
	* @param {string} asyncName
	* @param {unknown} complete
	* @returns {asserts complete is true}
	*/
	function assertDone(name, asyncName, complete) {
		if (!complete) throw new Error("`" + name + "` finished async. Use `" + asyncName + "` instead");
	}
	/**
	* @param {VFileCompatible} [value]
	* @returns {VFile}
	*/
	function vfile(value) {
		return looksLikeAVFile(value) ? value : new VFile(value);
	}
	/**
	* @param {VFileCompatible} [value]
	* @returns {value is VFile}
	*/
	function looksLikeAVFile(value) {
		return Boolean(value && typeof value === "object" && "message" in value && "messages" in value);
	}
	/**
	* @param {unknown} [value]
	* @returns {value is VFileValue}
	*/
	function looksLikeAVFileValue(value) {
		return typeof value === "string" || (0, import_is_buffer.default)(value);
	}
	var import_is_buffer, import_extend, unified, own$6;
	var init_lib$11 = __esmMin((() => {
		init_bail();
		import_is_buffer = /* @__PURE__ */ __toESM(require_is_buffer(), 1);
		import_extend = /* @__PURE__ */ __toESM(require_extend(), 1);
		init_is_plain_obj();
		init_trough();
		init_vfile();
		unified = base().freeze();
		own$6 = {}.hasOwnProperty;
	}));

//#endregion
//#region node_modules/unified/index.js
	var init_unified = __esmMin((() => {
		init_lib$11();
	}));

//#endregion
//#region node_modules/mdast-util-to-string/lib/index.js
/**
	* Get the text content of a node or list of nodes.
	*
	* Prefers the node’s plain-text fields, otherwise serializes its children,
	* and if the given value is an array, serialize the nodes in it.
	*
	* @param {unknown} value
	*   Thing to serialize, typically `Node`.
	* @param {Options | null | undefined} [options]
	*   Configuration (optional).
	* @returns {string}
	*   Serialized `value`.
	*/
	function toString(value, options) {
		const settings = options || emptyOptions;
		return one$1(value, typeof settings.includeImageAlt === "boolean" ? settings.includeImageAlt : true, typeof settings.includeHtml === "boolean" ? settings.includeHtml : true);
	}
	/**
	* One node or several nodes.
	*
	* @param {unknown} value
	*   Thing to serialize.
	* @param {boolean} includeImageAlt
	*   Include image `alt`s.
	* @param {boolean} includeHtml
	*   Include HTML.
	* @returns {string}
	*   Serialized node.
	*/
	function one$1(value, includeImageAlt, includeHtml) {
		if (node(value)) {
			if ("value" in value) return value.type === "html" && !includeHtml ? "" : value.value;
			if (includeImageAlt && "alt" in value && value.alt) return value.alt;
			if ("children" in value) return all$1(value.children, includeImageAlt, includeHtml);
		}
		if (Array.isArray(value)) return all$1(value, includeImageAlt, includeHtml);
		return "";
	}
	/**
	* Serialize a list of nodes.
	*
	* @param {Array<unknown>} values
	*   Thing to serialize.
	* @param {boolean} includeImageAlt
	*   Include image `alt`s.
	* @param {boolean} includeHtml
	*   Include HTML.
	* @returns {string}
	*   Serialized nodes.
	*/
	function all$1(values, includeImageAlt, includeHtml) {
		/** @type {Array<string>} */
		const result = [];
		let index = -1;
		while (++index < values.length) result[index] = one$1(values[index], includeImageAlt, includeHtml);
		return result.join("");
	}
	/**
	* Check if `value` looks like a node.
	*
	* @param {unknown} value
	*   Thing.
	* @returns {value is Node}
	*   Whether `value` is a node.
	*/
	function node(value) {
		return Boolean(value && typeof value === "object");
	}
	var emptyOptions;
	var init_lib$10 = __esmMin((() => {
		emptyOptions = {};
		__name(one$1, "one");
		__name(all$1, "all");
	}));

//#endregion
//#region node_modules/mdast-util-to-string/index.js
	var init_mdast_util_to_string = __esmMin((() => {
		init_lib$10();
	}));

//#endregion
//#region node_modules/micromark-util-chunked/index.js
/**
	* Like `Array#splice`, but smarter for giant arrays.
	*
	* `Array#splice` takes all items to be inserted as individual argument which
	* causes a stack overflow in V8 when trying to insert 100k items for instance.
	*
	* Otherwise, this does not return the removed items, and takes `items` as an
	* array instead of rest parameters.
	*
	* @template {unknown} T
	*   Item type.
	* @param {Array<T>} list
	*   List to operate on.
	* @param {number} start
	*   Index to remove/insert at (can be negative).
	* @param {number} remove
	*   Number of items to remove.
	* @param {Array<T>} items
	*   Items to inject into `list`.
	* @returns {void}
	*   Nothing.
	*/
	function splice(list, start, remove, items) {
		const end = list.length;
		let chunkStart = 0;
		/** @type {Array<unknown>} */
		let parameters;
		if (start < 0) start = -start > end ? 0 : end + start;
		else start = start > end ? end : start;
		remove = remove > 0 ? remove : 0;
		if (items.length < 1e4) {
			parameters = Array.from(items);
			parameters.unshift(start, remove);
			list.splice(...parameters);
		} else {
			if (remove) list.splice(start, remove);
			while (chunkStart < items.length) {
				parameters = items.slice(chunkStart, chunkStart + 1e4);
				parameters.unshift(start, 0);
				list.splice(...parameters);
				chunkStart += 1e4;
				start += 1e4;
			}
		}
	}
	/**
	* Append `items` (an array) at the end of `list` (another array).
	* When `list` was empty, returns `items` instead.
	*
	* This prevents a potentially expensive operation when `list` is empty,
	* and adds items in batches to prevent V8 from hanging.
	*
	* @template {unknown} T
	*   Item type.
	* @param {Array<T>} list
	*   List to operate on.
	* @param {Array<T>} items
	*   Items to add to `list`.
	* @returns {Array<T>}
	*   Either `list` or `items`.
	*/
	function push(list, items) {
		if (list.length > 0) {
			splice(list, list.length, 0, items);
			return list;
		}
		return items;
	}
	var init_micromark_util_chunked = __esmMin((() => {}));

//#endregion
//#region node_modules/micromark-util-combine-extensions/index.js
/**
	* Combine multiple syntax extensions into one.
	*
	* @param {Array<Extension>} extensions
	*   List of syntax extensions.
	* @returns {NormalizedExtension}
	*   A single combined extension.
	*/
	function combineExtensions(extensions) {
		/** @type {NormalizedExtension} */
		const all = {};
		let index = -1;
		while (++index < extensions.length) syntaxExtension(all, extensions[index]);
		return all;
	}
	/**
	* Merge `extension` into `all`.
	*
	* @param {NormalizedExtension} all
	*   Extension to merge into.
	* @param {Extension} extension
	*   Extension to merge.
	* @returns {void}
	*/
	function syntaxExtension(all, extension) {
		/** @type {keyof Extension} */
		let hook;
		for (hook in extension) {
			/** @type {Record<string, unknown>} */
			const left = (hasOwnProperty.call(all, hook) ? all[hook] : void 0) || (all[hook] = {});
			/** @type {Record<string, unknown> | undefined} */
			const right = extension[hook];
			/** @type {string} */
			let code;
			if (right) for (code in right) {
				if (!hasOwnProperty.call(left, code)) left[code] = [];
				const value = right[code];
				constructs(left[code], Array.isArray(value) ? value : value ? [value] : []);
			}
		}
	}
	/**
	* Merge `list` into `existing` (both lists of constructs).
	* Mutates `existing`.
	*
	* @param {Array<unknown>} existing
	* @param {Array<unknown>} list
	* @returns {void}
	*/
	function constructs(existing, list) {
		let index = -1;
		/** @type {Array<unknown>} */
		const before = [];
		while (++index < list.length) (list[index].add === "after" ? existing : before).push(list[index]);
		splice(existing, 0, 0, before);
	}
	var hasOwnProperty;
	var init_micromark_util_combine_extensions = __esmMin((() => {
		init_micromark_util_chunked();
		hasOwnProperty = {}.hasOwnProperty;
	}));

//#endregion
//#region node_modules/micromark-util-character/lib/unicode-punctuation-regex.js
	var unicodePunctuationRegex;
	var init_unicode_punctuation_regex = __esmMin((() => {
		unicodePunctuationRegex = /[!-\/:-@\[-`\{-~\xA1\xA7\xAB\xB6\xB7\xBB\xBF\u037E\u0387\u055A-\u055F\u0589\u058A\u05BE\u05C0\u05C3\u05C6\u05F3\u05F4\u0609\u060A\u060C\u060D\u061B\u061D-\u061F\u066A-\u066D\u06D4\u0700-\u070D\u07F7-\u07F9\u0830-\u083E\u085E\u0964\u0965\u0970\u09FD\u0A76\u0AF0\u0C77\u0C84\u0DF4\u0E4F\u0E5A\u0E5B\u0F04-\u0F12\u0F14\u0F3A-\u0F3D\u0F85\u0FD0-\u0FD4\u0FD9\u0FDA\u104A-\u104F\u10FB\u1360-\u1368\u1400\u166E\u169B\u169C\u16EB-\u16ED\u1735\u1736\u17D4-\u17D6\u17D8-\u17DA\u1800-\u180A\u1944\u1945\u1A1E\u1A1F\u1AA0-\u1AA6\u1AA8-\u1AAD\u1B5A-\u1B60\u1B7D\u1B7E\u1BFC-\u1BFF\u1C3B-\u1C3F\u1C7E\u1C7F\u1CC0-\u1CC7\u1CD3\u2010-\u2027\u2030-\u2043\u2045-\u2051\u2053-\u205E\u207D\u207E\u208D\u208E\u2308-\u230B\u2329\u232A\u2768-\u2775\u27C5\u27C6\u27E6-\u27EF\u2983-\u2998\u29D8-\u29DB\u29FC\u29FD\u2CF9-\u2CFC\u2CFE\u2CFF\u2D70\u2E00-\u2E2E\u2E30-\u2E4F\u2E52-\u2E5D\u3001-\u3003\u3008-\u3011\u3014-\u301F\u3030\u303D\u30A0\u30FB\uA4FE\uA4FF\uA60D-\uA60F\uA673\uA67E\uA6F2-\uA6F7\uA874-\uA877\uA8CE\uA8CF\uA8F8-\uA8FA\uA8FC\uA92E\uA92F\uA95F\uA9C1-\uA9CD\uA9DE\uA9DF\uAA5C-\uAA5F\uAADE\uAADF\uAAF0\uAAF1\uABEB\uFD3E\uFD3F\uFE10-\uFE19\uFE30-\uFE52\uFE54-\uFE61\uFE63\uFE68\uFE6A\uFE6B\uFF01-\uFF03\uFF05-\uFF0A\uFF0C-\uFF0F\uFF1A\uFF1B\uFF1F\uFF20\uFF3B-\uFF3D\uFF3F\uFF5B\uFF5D\uFF5F-\uFF65]/;
	}));

//#endregion
//#region node_modules/micromark-util-character/index.js
/**
	* Check whether a character code is an ASCII control character.
	*
	* An **ASCII control** is a character in the inclusive range U+0000 NULL (NUL)
	* to U+001F (US), or U+007F (DEL).
	*
	* @param {Code} code
	*   Code.
	* @returns {boolean}
	*   Whether it matches.
	*/
	function asciiControl(code) {
		return code !== null && (code < 32 || code === 127);
	}
	/**
	* Check whether a character code is a markdown line ending.
	*
	* A **markdown line ending** is the virtual characters M-0003 CARRIAGE RETURN
	* LINE FEED (CRLF), M-0004 LINE FEED (LF) and M-0005 CARRIAGE RETURN (CR).
	*
	* In micromark, the actual character U+000A LINE FEED (LF) and U+000D CARRIAGE
	* RETURN (CR) are replaced by these virtual characters depending on whether
	* they occurred together.
	*
	* @param {Code} code
	*   Code.
	* @returns {boolean}
	*   Whether it matches.
	*/
	function markdownLineEnding(code) {
		return code !== null && code < -2;
	}
	/**
	* Check whether a character code is a markdown line ending (see
	* `markdownLineEnding`) or markdown space (see `markdownSpace`).
	*
	* @param {Code} code
	*   Code.
	* @returns {boolean}
	*   Whether it matches.
	*/
	function markdownLineEndingOrSpace(code) {
		return code !== null && (code < 0 || code === 32);
	}
	/**
	* Check whether a character code is a markdown space.
	*
	* A **markdown space** is the concrete character U+0020 SPACE (SP) and the
	* virtual characters M-0001 VIRTUAL SPACE (VS) and M-0002 HORIZONTAL TAB (HT).
	*
	* In micromark, the actual character U+0009 CHARACTER TABULATION (HT) is
	* replaced by one M-0002 HORIZONTAL TAB (HT) and between 0 and 3 M-0001 VIRTUAL
	* SPACE (VS) characters, depending on the column at which the tab occurred.
	*
	* @param {Code} code
	*   Code.
	* @returns {boolean}
	*   Whether it matches.
	*/
	function markdownSpace(code) {
		return code === -2 || code === -1 || code === 32;
	}
	/**
	* Create a code check from a regex.
	*
	* @param {RegExp} regex
	* @returns {(code: Code) => boolean}
	*/
	function regexCheck(regex) {
		return check;
		/**
		* Check whether a code matches the bound regex.
		*
		* @param {Code} code
		*   Character code.
		* @returns {boolean}
		*   Whether the character code matches the bound regex.
		*/
		function check(code) {
			return code !== null && regex.test(String.fromCharCode(code));
		}
	}
	var asciiAlpha, asciiAlphanumeric, asciiAtext, asciiDigit, asciiHexDigit, asciiPunctuation, unicodePunctuation, unicodeWhitespace;
	var init_micromark_util_character = __esmMin((() => {
		init_unicode_punctuation_regex();
		asciiAlpha = regexCheck(/[A-Za-z]/);
		asciiAlphanumeric = regexCheck(/[\dA-Za-z]/);
		asciiAtext = regexCheck(/[#-'*+\--9=?A-Z^-~]/);
		asciiDigit = regexCheck(/\d/);
		asciiHexDigit = regexCheck(/[\dA-Fa-f]/);
		asciiPunctuation = regexCheck(/[!-/:-@[-`{-~]/);
		unicodePunctuation = regexCheck(unicodePunctuationRegex);
		unicodeWhitespace = regexCheck(/\s/);
	}));

//#endregion
//#region node_modules/micromark-factory-space/index.js
/**
	* Parse spaces and tabs.
	*
	* There is no `nok` parameter:
	*
	* *   spaces in markdown are often optional, in which case this factory can be
	*     used and `ok` will be switched to whether spaces were found or not
	* *   one line ending or space can be detected with `markdownSpace(code)` right
	*     before using `factorySpace`
	*
	* ###### Examples
	*
	* Where `␉` represents a tab (plus how much it expands) and `␠` represents a
	* single space.
	*
	* ```markdown
	* ␉
	* ␠␠␠␠
	* ␉␠
	* ```
	*
	* @param {Effects} effects
	*   Context.
	* @param {State} ok
	*   State switched to when successful.
	* @param {TokenType} type
	*   Type (`' \t'`).
	* @param {number | undefined} [max=Infinity]
	*   Max (exclusive).
	* @returns
	*   Start state.
	*/
	function factorySpace(effects, ok, type, max) {
		const limit = max ? max - 1 : Number.POSITIVE_INFINITY;
		let size = 0;
		return start;
		/** @type {State} */
		function start(code) {
			if (markdownSpace(code)) {
				effects.enter(type);
				return prefix(code);
			}
			return ok(code);
		}
		/** @type {State} */
		function prefix(code) {
			if (markdownSpace(code) && size++ < limit) {
				effects.consume(code);
				return prefix;
			}
			effects.exit(type);
			return ok(code);
		}
	}
	var init_micromark_factory_space = __esmMin((() => {
		init_micromark_util_character();
	}));

//#endregion
//#region node_modules/micromark/lib/initialize/content.js
/**
	* @this {TokenizeContext}
	* @type {Initializer}
	*/
	function initializeContent(effects) {
		const contentStart = effects.attempt(this.parser.constructs.contentInitial, afterContentStartConstruct, paragraphInitial);
		/** @type {Token} */
		let previous;
		return contentStart;
		/** @type {State} */
		function afterContentStartConstruct(code) {
			if (code === null) {
				effects.consume(code);
				return;
			}
			effects.enter("lineEnding");
			effects.consume(code);
			effects.exit("lineEnding");
			return factorySpace(effects, contentStart, "linePrefix");
		}
		/** @type {State} */
		function paragraphInitial(code) {
			effects.enter("paragraph");
			return lineStart(code);
		}
		/** @type {State} */
		function lineStart(code) {
			const token = effects.enter("chunkText", {
				contentType: "text",
				previous
			});
			if (previous) previous.next = token;
			previous = token;
			return data(code);
		}
		/** @type {State} */
		function data(code) {
			if (code === null) {
				effects.exit("chunkText");
				effects.exit("paragraph");
				effects.consume(code);
				return;
			}
			if (markdownLineEnding(code)) {
				effects.consume(code);
				effects.exit("chunkText");
				return lineStart;
			}
			effects.consume(code);
			return data;
		}
	}
	var content$1;
	var init_content$1 = __esmMin((() => {
		init_micromark_factory_space();
		init_micromark_util_character();
		content$1 = { tokenize: initializeContent };
	}));

//#endregion
//#region node_modules/micromark/lib/initialize/document.js
/**
	* @this {TokenizeContext}
	* @type {Initializer}
	*/
	function initializeDocument(effects) {
		const self = this;
		/** @type {Array<StackItem>} */
		const stack = [];
		let continued = 0;
		/** @type {TokenizeContext | undefined} */
		let childFlow;
		/** @type {Token | undefined} */
		let childToken;
		/** @type {number} */
		let lineStartOffset;
		return start;
		/** @type {State} */
		function start(code) {
			if (continued < stack.length) {
				const item = stack[continued];
				self.containerState = item[1];
				return effects.attempt(item[0].continuation, documentContinue, checkNewContainers)(code);
			}
			return checkNewContainers(code);
		}
		/** @type {State} */
		function documentContinue(code) {
			continued++;
			if (self.containerState._closeFlow) {
				self.containerState._closeFlow = void 0;
				if (childFlow) closeFlow();
				const indexBeforeExits = self.events.length;
				let indexBeforeFlow = indexBeforeExits;
				/** @type {Point | undefined} */
				let point;
				while (indexBeforeFlow--) if (self.events[indexBeforeFlow][0] === "exit" && self.events[indexBeforeFlow][1].type === "chunkFlow") {
					point = self.events[indexBeforeFlow][1].end;
					break;
				}
				exitContainers(continued);
				let index = indexBeforeExits;
				while (index < self.events.length) {
					self.events[index][1].end = Object.assign({}, point);
					index++;
				}
				splice(self.events, indexBeforeFlow + 1, 0, self.events.slice(indexBeforeExits));
				self.events.length = index;
				return checkNewContainers(code);
			}
			return start(code);
		}
		/** @type {State} */
		function checkNewContainers(code) {
			if (continued === stack.length) {
				if (!childFlow) return documentContinued(code);
				if (childFlow.currentConstruct && childFlow.currentConstruct.concrete) return flowStart(code);
				self.interrupt = Boolean(childFlow.currentConstruct && !childFlow._gfmTableDynamicInterruptHack);
			}
			self.containerState = {};
			return effects.check(containerConstruct, thereIsANewContainer, thereIsNoNewContainer)(code);
		}
		/** @type {State} */
		function thereIsANewContainer(code) {
			if (childFlow) closeFlow();
			exitContainers(continued);
			return documentContinued(code);
		}
		/** @type {State} */
		function thereIsNoNewContainer(code) {
			self.parser.lazy[self.now().line] = continued !== stack.length;
			lineStartOffset = self.now().offset;
			return flowStart(code);
		}
		/** @type {State} */
		function documentContinued(code) {
			self.containerState = {};
			return effects.attempt(containerConstruct, containerContinue, flowStart)(code);
		}
		/** @type {State} */
		function containerContinue(code) {
			continued++;
			stack.push([self.currentConstruct, self.containerState]);
			return documentContinued(code);
		}
		/** @type {State} */
		function flowStart(code) {
			if (code === null) {
				if (childFlow) closeFlow();
				exitContainers(0);
				effects.consume(code);
				return;
			}
			childFlow = childFlow || self.parser.flow(self.now());
			effects.enter("chunkFlow", {
				contentType: "flow",
				previous: childToken,
				_tokenizer: childFlow
			});
			return flowContinue(code);
		}
		/** @type {State} */
		function flowContinue(code) {
			if (code === null) {
				writeToChild(effects.exit("chunkFlow"), true);
				exitContainers(0);
				effects.consume(code);
				return;
			}
			if (markdownLineEnding(code)) {
				effects.consume(code);
				writeToChild(effects.exit("chunkFlow"));
				continued = 0;
				self.interrupt = void 0;
				return start;
			}
			effects.consume(code);
			return flowContinue;
		}
		/**
		* @param {Token} token
		* @param {boolean | undefined} [eof]
		* @returns {void}
		*/
		function writeToChild(token, eof) {
			const stream = self.sliceStream(token);
			if (eof) stream.push(null);
			token.previous = childToken;
			if (childToken) childToken.next = token;
			childToken = token;
			childFlow.defineSkip(token.start);
			childFlow.write(stream);
			if (self.parser.lazy[token.start.line]) {
				let index = childFlow.events.length;
				while (index--) if (childFlow.events[index][1].start.offset < lineStartOffset && (!childFlow.events[index][1].end || childFlow.events[index][1].end.offset > lineStartOffset)) return;
				const indexBeforeExits = self.events.length;
				let indexBeforeFlow = indexBeforeExits;
				/** @type {boolean | undefined} */
				let seen;
				/** @type {Point | undefined} */
				let point;
				while (indexBeforeFlow--) if (self.events[indexBeforeFlow][0] === "exit" && self.events[indexBeforeFlow][1].type === "chunkFlow") {
					if (seen) {
						point = self.events[indexBeforeFlow][1].end;
						break;
					}
					seen = true;
				}
				exitContainers(continued);
				index = indexBeforeExits;
				while (index < self.events.length) {
					self.events[index][1].end = Object.assign({}, point);
					index++;
				}
				splice(self.events, indexBeforeFlow + 1, 0, self.events.slice(indexBeforeExits));
				self.events.length = index;
			}
		}
		/**
		* @param {number} size
		* @returns {void}
		*/
		function exitContainers(size) {
			let index = stack.length;
			while (index-- > size) {
				const entry = stack[index];
				self.containerState = entry[1];
				entry[0].exit.call(self, effects);
			}
			stack.length = size;
		}
		function closeFlow() {
			childFlow.write([null]);
			childToken = void 0;
			childFlow = void 0;
			self.containerState._closeFlow = void 0;
		}
	}
	/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeContainer(effects, ok, nok) {
		return factorySpace(effects, effects.attempt(this.parser.constructs.document, ok, nok), "linePrefix", this.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4);
	}
	var document$2, containerConstruct;
	var init_document = __esmMin((() => {
		init_micromark_factory_space();
		init_micromark_util_character();
		init_micromark_util_chunked();
		document$2 = { tokenize: initializeDocument };
		containerConstruct = { tokenize: tokenizeContainer };
	}));

//#endregion
//#region node_modules/micromark-util-classify-character/index.js
/**
	* Classify whether a code represents whitespace, punctuation, or something
	* else.
	*
	* Used for attention (emphasis, strong), whose sequences can open or close
	* based on the class of surrounding characters.
	*
	* > 👉 **Note**: eof (`null`) is seen as whitespace.
	*
	* @param {Code} code
	*   Code.
	* @returns {typeof constants.characterGroupWhitespace | typeof constants.characterGroupPunctuation | undefined}
	*   Group.
	*/
	function classifyCharacter(code) {
		if (code === null || markdownLineEndingOrSpace(code) || unicodeWhitespace(code)) return 1;
		if (unicodePunctuation(code)) return 2;
	}
	var init_micromark_util_classify_character = __esmMin((() => {
		init_micromark_util_character();
	}));

//#endregion
//#region node_modules/micromark-util-resolve-all/index.js
/**
	* @typedef {import('micromark-util-types').Event} Event
	* @typedef {import('micromark-util-types').Resolver} Resolver
	* @typedef {import('micromark-util-types').TokenizeContext} TokenizeContext
	*/
	/**
	* Call all `resolveAll`s.
	*
	* @param {Array<{resolveAll?: Resolver | undefined}>} constructs
	*   List of constructs, optionally with `resolveAll`s.
	* @param {Array<Event>} events
	*   List of events.
	* @param {TokenizeContext} context
	*   Context used by `tokenize`.
	* @returns {Array<Event>}
	*   Changed events.
	*/
	function resolveAll(constructs, events, context) {
		/** @type {Array<Resolver>} */
		const called = [];
		let index = -1;
		while (++index < constructs.length) {
			const resolve = constructs[index].resolveAll;
			if (resolve && !called.includes(resolve)) {
				events = resolve(events, context);
				called.push(resolve);
			}
		}
		return events;
	}
	var init_micromark_util_resolve_all = __esmMin((() => {}));

//#endregion
//#region node_modules/micromark-core-commonmark/lib/attention.js
/**
	* Take all events and resolve attention to emphasis or strong.
	*
	* @type {Resolver}
	*/
	function resolveAllAttention(events, context) {
		let index = -1;
		/** @type {number} */
		let open;
		/** @type {Token} */
		let group;
		/** @type {Token} */
		let text;
		/** @type {Token} */
		let openingSequence;
		/** @type {Token} */
		let closingSequence;
		/** @type {number} */
		let use;
		/** @type {Array<Event>} */
		let nextEvents;
		/** @type {number} */
		let offset;
		while (++index < events.length) if (events[index][0] === "enter" && events[index][1].type === "attentionSequence" && events[index][1]._close) {
			open = index;
			while (open--) if (events[open][0] === "exit" && events[open][1].type === "attentionSequence" && events[open][1]._open && context.sliceSerialize(events[open][1]).charCodeAt(0) === context.sliceSerialize(events[index][1]).charCodeAt(0)) {
				if ((events[open][1]._close || events[index][1]._open) && (events[index][1].end.offset - events[index][1].start.offset) % 3 && !((events[open][1].end.offset - events[open][1].start.offset + events[index][1].end.offset - events[index][1].start.offset) % 3)) continue;
				use = events[open][1].end.offset - events[open][1].start.offset > 1 && events[index][1].end.offset - events[index][1].start.offset > 1 ? 2 : 1;
				const start = Object.assign({}, events[open][1].end);
				const end = Object.assign({}, events[index][1].start);
				movePoint(start, -use);
				movePoint(end, use);
				openingSequence = {
					type: use > 1 ? "strongSequence" : "emphasisSequence",
					start,
					end: Object.assign({}, events[open][1].end)
				};
				closingSequence = {
					type: use > 1 ? "strongSequence" : "emphasisSequence",
					start: Object.assign({}, events[index][1].start),
					end
				};
				text = {
					type: use > 1 ? "strongText" : "emphasisText",
					start: Object.assign({}, events[open][1].end),
					end: Object.assign({}, events[index][1].start)
				};
				group = {
					type: use > 1 ? "strong" : "emphasis",
					start: Object.assign({}, openingSequence.start),
					end: Object.assign({}, closingSequence.end)
				};
				events[open][1].end = Object.assign({}, openingSequence.start);
				events[index][1].start = Object.assign({}, closingSequence.end);
				nextEvents = [];
				if (events[open][1].end.offset - events[open][1].start.offset) nextEvents = push(nextEvents, [[
					"enter",
					events[open][1],
					context
				], [
					"exit",
					events[open][1],
					context
				]]);
				nextEvents = push(nextEvents, [
					[
						"enter",
						group,
						context
					],
					[
						"enter",
						openingSequence,
						context
					],
					[
						"exit",
						openingSequence,
						context
					],
					[
						"enter",
						text,
						context
					]
				]);
				nextEvents = push(nextEvents, resolveAll(context.parser.constructs.insideSpan.null, events.slice(open + 1, index), context));
				nextEvents = push(nextEvents, [
					[
						"exit",
						text,
						context
					],
					[
						"enter",
						closingSequence,
						context
					],
					[
						"exit",
						closingSequence,
						context
					],
					[
						"exit",
						group,
						context
					]
				]);
				if (events[index][1].end.offset - events[index][1].start.offset) {
					offset = 2;
					nextEvents = push(nextEvents, [[
						"enter",
						events[index][1],
						context
					], [
						"exit",
						events[index][1],
						context
					]]);
				} else offset = 0;
				splice(events, open - 1, index - open + 3, nextEvents);
				index = open + nextEvents.length - offset - 2;
				break;
			}
		}
		index = -1;
		while (++index < events.length) if (events[index][1].type === "attentionSequence") events[index][1].type = "data";
		return events;
	}
	/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeAttention(effects, ok) {
		const attentionMarkers = this.parser.constructs.attentionMarkers.null;
		const previous = this.previous;
		const before = classifyCharacter(previous);
		/** @type {NonNullable<Code>} */
		let marker;
		return start;
		/**
		* Before a sequence.
		*
		* ```markdown
		* > | **
		*     ^
		* ```
		*
		* @type {State}
		*/
		function start(code) {
			marker = code;
			effects.enter("attentionSequence");
			return inside(code);
		}
		/**
		* In a sequence.
		*
		* ```markdown
		* > | **
		*     ^^
		* ```
		*
		* @type {State}
		*/
		function inside(code) {
			if (code === marker) {
				effects.consume(code);
				return inside;
			}
			const token = effects.exit("attentionSequence");
			const after = classifyCharacter(code);
			const open = !after || after === 2 && before || attentionMarkers.includes(code);
			const close = !before || before === 2 && after || attentionMarkers.includes(previous);
			token._open = Boolean(marker === 42 ? open : open && (before || !close));
			token._close = Boolean(marker === 42 ? close : close && (after || !open));
			return ok(code);
		}
	}
	/**
	* Move a point a bit.
	*
	* Note: `move` only works inside lines! It’s not possible to move past other
	* chunks (replacement characters, tabs, or line endings).
	*
	* @param {Point} point
	* @param {number} offset
	* @returns {void}
	*/
	function movePoint(point, offset) {
		point.column += offset;
		point.offset += offset;
		point._bufferIndex += offset;
	}
	var attention;
	var init_attention = __esmMin((() => {
		init_micromark_util_chunked();
		init_micromark_util_classify_character();
		init_micromark_util_resolve_all();
		attention = {
			name: "attention",
			tokenize: tokenizeAttention,
			resolveAll: resolveAllAttention
		};
	}));

//#endregion
//#region node_modules/micromark-core-commonmark/lib/autolink.js
/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeAutolink(effects, ok, nok) {
		let size = 0;
		return start;
		/**
		* Start of an autolink.
		*
		* ```markdown
		* > | a<https://example.com>b
		*      ^
		* > | a<user@example.com>b
		*      ^
		* ```
		*
		* @type {State}
		*/
		function start(code) {
			effects.enter("autolink");
			effects.enter("autolinkMarker");
			effects.consume(code);
			effects.exit("autolinkMarker");
			effects.enter("autolinkProtocol");
			return open;
		}
		/**
		* After `<`, at protocol or atext.
		*
		* ```markdown
		* > | a<https://example.com>b
		*       ^
		* > | a<user@example.com>b
		*       ^
		* ```
		*
		* @type {State}
		*/
		function open(code) {
			if (asciiAlpha(code)) {
				effects.consume(code);
				return schemeOrEmailAtext;
			}
			return emailAtext(code);
		}
		/**
		* At second byte of protocol or atext.
		*
		* ```markdown
		* > | a<https://example.com>b
		*        ^
		* > | a<user@example.com>b
		*        ^
		* ```
		*
		* @type {State}
		*/
		function schemeOrEmailAtext(code) {
			if (code === 43 || code === 45 || code === 46 || asciiAlphanumeric(code)) {
				size = 1;
				return schemeInsideOrEmailAtext(code);
			}
			return emailAtext(code);
		}
		/**
		* In ambiguous protocol or atext.
		*
		* ```markdown
		* > | a<https://example.com>b
		*        ^
		* > | a<user@example.com>b
		*        ^
		* ```
		*
		* @type {State}
		*/
		function schemeInsideOrEmailAtext(code) {
			if (code === 58) {
				effects.consume(code);
				size = 0;
				return urlInside;
			}
			if ((code === 43 || code === 45 || code === 46 || asciiAlphanumeric(code)) && size++ < 32) {
				effects.consume(code);
				return schemeInsideOrEmailAtext;
			}
			size = 0;
			return emailAtext(code);
		}
		/**
		* After protocol, in URL.
		*
		* ```markdown
		* > | a<https://example.com>b
		*             ^
		* ```
		*
		* @type {State}
		*/
		function urlInside(code) {
			if (code === 62) {
				effects.exit("autolinkProtocol");
				effects.enter("autolinkMarker");
				effects.consume(code);
				effects.exit("autolinkMarker");
				effects.exit("autolink");
				return ok;
			}
			if (code === null || code === 32 || code === 60 || asciiControl(code)) return nok(code);
			effects.consume(code);
			return urlInside;
		}
		/**
		* In email atext.
		*
		* ```markdown
		* > | a<user.name@example.com>b
		*              ^
		* ```
		*
		* @type {State}
		*/
		function emailAtext(code) {
			if (code === 64) {
				effects.consume(code);
				return emailAtSignOrDot;
			}
			if (asciiAtext(code)) {
				effects.consume(code);
				return emailAtext;
			}
			return nok(code);
		}
		/**
		* In label, after at-sign or dot.
		*
		* ```markdown
		* > | a<user.name@example.com>b
		*                 ^       ^
		* ```
		*
		* @type {State}
		*/
		function emailAtSignOrDot(code) {
			return asciiAlphanumeric(code) ? emailLabel(code) : nok(code);
		}
		/**
		* In label, where `.` and `>` are allowed.
		*
		* ```markdown
		* > | a<user.name@example.com>b
		*                   ^
		* ```
		*
		* @type {State}
		*/
		function emailLabel(code) {
			if (code === 46) {
				effects.consume(code);
				size = 0;
				return emailAtSignOrDot;
			}
			if (code === 62) {
				effects.exit("autolinkProtocol").type = "autolinkEmail";
				effects.enter("autolinkMarker");
				effects.consume(code);
				effects.exit("autolinkMarker");
				effects.exit("autolink");
				return ok;
			}
			return emailValue(code);
		}
		/**
		* In label, where `.` and `>` are *not* allowed.
		*
		* Though, this is also used in `emailLabel` to parse other values.
		*
		* ```markdown
		* > | a<user.name@ex-ample.com>b
		*                    ^
		* ```
		*
		* @type {State}
		*/
		function emailValue(code) {
			if ((code === 45 || asciiAlphanumeric(code)) && size++ < 63) {
				const next = code === 45 ? emailValue : emailLabel;
				effects.consume(code);
				return next;
			}
			return nok(code);
		}
	}
	var autolink;
	var init_autolink = __esmMin((() => {
		init_micromark_util_character();
		autolink = {
			name: "autolink",
			tokenize: tokenizeAutolink
		};
	}));

//#endregion
//#region node_modules/micromark-core-commonmark/lib/blank-line.js
/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeBlankLine(effects, ok, nok) {
		return start;
		/**
		* Start of blank line.
		*
		* > 👉 **Note**: `␠` represents a space character.
		*
		* ```markdown
		* > | ␠␠␊
		*     ^
		* > | ␊
		*     ^
		* ```
		*
		* @type {State}
		*/
		function start(code) {
			return markdownSpace(code) ? factorySpace(effects, after, "linePrefix")(code) : after(code);
		}
		/**
		* At eof/eol, after optional whitespace.
		*
		* > 👉 **Note**: `␠` represents a space character.
		*
		* ```markdown
		* > | ␠␠␊
		*       ^
		* > | ␊
		*     ^
		* ```
		*
		* @type {State}
		*/
		function after(code) {
			return code === null || markdownLineEnding(code) ? ok(code) : nok(code);
		}
	}
	var blankLine;
	var init_blank_line = __esmMin((() => {
		init_micromark_factory_space();
		init_micromark_util_character();
		blankLine = {
			tokenize: tokenizeBlankLine,
			partial: true
		};
	}));

//#endregion
//#region node_modules/micromark-core-commonmark/lib/block-quote.js
/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeBlockQuoteStart(effects, ok, nok) {
		const self = this;
		return start;
		/**
		* Start of block quote.
		*
		* ```markdown
		* > | > a
		*     ^
		* ```
		*
		* @type {State}
		*/
		function start(code) {
			if (code === 62) {
				const state = self.containerState;
				if (!state.open) {
					effects.enter("blockQuote", { _container: true });
					state.open = true;
				}
				effects.enter("blockQuotePrefix");
				effects.enter("blockQuoteMarker");
				effects.consume(code);
				effects.exit("blockQuoteMarker");
				return after;
			}
			return nok(code);
		}
		/**
		* After `>`, before optional whitespace.
		*
		* ```markdown
		* > | > a
		*      ^
		* ```
		*
		* @type {State}
		*/
		function after(code) {
			if (markdownSpace(code)) {
				effects.enter("blockQuotePrefixWhitespace");
				effects.consume(code);
				effects.exit("blockQuotePrefixWhitespace");
				effects.exit("blockQuotePrefix");
				return ok;
			}
			effects.exit("blockQuotePrefix");
			return ok(code);
		}
	}
	/**
	* Start of block quote continuation.
	*
	* ```markdown
	*   | > a
	* > | > b
	*     ^
	* ```
	*
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeBlockQuoteContinuation(effects, ok, nok) {
		const self = this;
		return contStart;
		/**
		* Start of block quote continuation.
		*
		* Also used to parse the first block quote opening.
		*
		* ```markdown
		*   | > a
		* > | > b
		*     ^
		* ```
		*
		* @type {State}
		*/
		function contStart(code) {
			if (markdownSpace(code)) return factorySpace(effects, contBefore, "linePrefix", self.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4)(code);
			return contBefore(code);
		}
		/**
		* At `>`, after optional whitespace.
		*
		* Also used to parse the first block quote opening.
		*
		* ```markdown
		*   | > a
		* > | > b
		*     ^
		* ```
		*
		* @type {State}
		*/
		function contBefore(code) {
			return effects.attempt(blockQuote, ok, nok)(code);
		}
	}
	/** @type {Exiter} */
	function exit(effects) {
		effects.exit("blockQuote");
	}
	var blockQuote;
	var init_block_quote = __esmMin((() => {
		init_micromark_factory_space();
		init_micromark_util_character();
		blockQuote = {
			name: "blockQuote",
			tokenize: tokenizeBlockQuoteStart,
			continuation: { tokenize: tokenizeBlockQuoteContinuation },
			exit
		};
	}));

//#endregion
//#region node_modules/micromark-core-commonmark/lib/character-escape.js
/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeCharacterEscape(effects, ok, nok) {
		return start;
		/**
		* Start of character escape.
		*
		* ```markdown
		* > | a\*b
		*      ^
		* ```
		*
		* @type {State}
		*/
		function start(code) {
			effects.enter("characterEscape");
			effects.enter("escapeMarker");
			effects.consume(code);
			effects.exit("escapeMarker");
			return inside;
		}
		/**
		* After `\`, at punctuation.
		*
		* ```markdown
		* > | a\*b
		*       ^
		* ```
		*
		* @type {State}
		*/
		function inside(code) {
			if (asciiPunctuation(code)) {
				effects.enter("characterEscapeValue");
				effects.consume(code);
				effects.exit("characterEscapeValue");
				effects.exit("characterEscape");
				return ok;
			}
			return nok(code);
		}
	}
	var characterEscape;
	var init_character_escape = __esmMin((() => {
		init_micromark_util_character();
		characterEscape = {
			name: "characterEscape",
			tokenize: tokenizeCharacterEscape
		};
	}));

//#endregion
//#region node_modules/decode-named-character-reference/index.dom.js
/**
	* @param {string} value
	* @returns {string | false}
	*/
	function decodeNamedCharacterReference(value) {
		const characterReference = "&" + value + ";";
		element.innerHTML = characterReference;
		const character = element.textContent;
		if (character.charCodeAt(character.length - 1) === 59 && value !== "semi") return false;
		return character === characterReference ? false : character;
	}
	var element;
	var init_index_dom = __esmMin((() => {
		element = document.createElement("i");
	}));

//#endregion
//#region node_modules/micromark-core-commonmark/lib/character-reference.js
/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeCharacterReference(effects, ok, nok) {
		const self = this;
		let size = 0;
		/** @type {number} */
		let max;
		/** @type {(code: Code) => boolean} */
		let test;
		return start;
		/**
		* Start of character reference.
		*
		* ```markdown
		* > | a&amp;b
		*      ^
		* > | a&#123;b
		*      ^
		* > | a&#x9;b
		*      ^
		* ```
		*
		* @type {State}
		*/
		function start(code) {
			effects.enter("characterReference");
			effects.enter("characterReferenceMarker");
			effects.consume(code);
			effects.exit("characterReferenceMarker");
			return open;
		}
		/**
		* After `&`, at `#` for numeric references or alphanumeric for named
		* references.
		*
		* ```markdown
		* > | a&amp;b
		*       ^
		* > | a&#123;b
		*       ^
		* > | a&#x9;b
		*       ^
		* ```
		*
		* @type {State}
		*/
		function open(code) {
			if (code === 35) {
				effects.enter("characterReferenceMarkerNumeric");
				effects.consume(code);
				effects.exit("characterReferenceMarkerNumeric");
				return numeric;
			}
			effects.enter("characterReferenceValue");
			max = 31;
			test = asciiAlphanumeric;
			return value(code);
		}
		/**
		* After `#`, at `x` for hexadecimals or digit for decimals.
		*
		* ```markdown
		* > | a&#123;b
		*        ^
		* > | a&#x9;b
		*        ^
		* ```
		*
		* @type {State}
		*/
		function numeric(code) {
			if (code === 88 || code === 120) {
				effects.enter("characterReferenceMarkerHexadecimal");
				effects.consume(code);
				effects.exit("characterReferenceMarkerHexadecimal");
				effects.enter("characterReferenceValue");
				max = 6;
				test = asciiHexDigit;
				return value;
			}
			effects.enter("characterReferenceValue");
			max = 7;
			test = asciiDigit;
			return value(code);
		}
		/**
		* After markers (`&#x`, `&#`, or `&`), in value, before `;`.
		*
		* The character reference kind defines what and how many characters are
		* allowed.
		*
		* ```markdown
		* > | a&amp;b
		*       ^^^
		* > | a&#123;b
		*        ^^^
		* > | a&#x9;b
		*         ^
		* ```
		*
		* @type {State}
		*/
		function value(code) {
			if (code === 59 && size) {
				const token = effects.exit("characterReferenceValue");
				if (test === asciiAlphanumeric && !decodeNamedCharacterReference(self.sliceSerialize(token))) return nok(code);
				effects.enter("characterReferenceMarker");
				effects.consume(code);
				effects.exit("characterReferenceMarker");
				effects.exit("characterReference");
				return ok;
			}
			if (test(code) && size++ < max) {
				effects.consume(code);
				return value;
			}
			return nok(code);
		}
	}
	var characterReference;
	var init_character_reference = __esmMin((() => {
		init_index_dom();
		init_micromark_util_character();
		characterReference = {
			name: "characterReference",
			tokenize: tokenizeCharacterReference
		};
	}));

//#endregion
//#region node_modules/micromark-core-commonmark/lib/code-fenced.js
/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeCodeFenced(effects, ok, nok) {
		const self = this;
		/** @type {Construct} */
		const closeStart = {
			tokenize: tokenizeCloseStart,
			partial: true
		};
		let initialPrefix = 0;
		let sizeOpen = 0;
		/** @type {NonNullable<Code>} */
		let marker;
		return start;
		/**
		* Start of code.
		*
		* ```markdown
		* > | ~~~js
		*     ^
		*   | alert(1)
		*   | ~~~
		* ```
		*
		* @type {State}
		*/
		function start(code) {
			return beforeSequenceOpen(code);
		}
		/**
		* In opening fence, after prefix, at sequence.
		*
		* ```markdown
		* > | ~~~js
		*     ^
		*   | alert(1)
		*   | ~~~
		* ```
		*
		* @type {State}
		*/
		function beforeSequenceOpen(code) {
			const tail = self.events[self.events.length - 1];
			initialPrefix = tail && tail[1].type === "linePrefix" ? tail[2].sliceSerialize(tail[1], true).length : 0;
			marker = code;
			effects.enter("codeFenced");
			effects.enter("codeFencedFence");
			effects.enter("codeFencedFenceSequence");
			return sequenceOpen(code);
		}
		/**
		* In opening fence sequence.
		*
		* ```markdown
		* > | ~~~js
		*      ^
		*   | alert(1)
		*   | ~~~
		* ```
		*
		* @type {State}
		*/
		function sequenceOpen(code) {
			if (code === marker) {
				sizeOpen++;
				effects.consume(code);
				return sequenceOpen;
			}
			if (sizeOpen < 3) return nok(code);
			effects.exit("codeFencedFenceSequence");
			return markdownSpace(code) ? factorySpace(effects, infoBefore, "whitespace")(code) : infoBefore(code);
		}
		/**
		* In opening fence, after the sequence (and optional whitespace), before info.
		*
		* ```markdown
		* > | ~~~js
		*        ^
		*   | alert(1)
		*   | ~~~
		* ```
		*
		* @type {State}
		*/
		function infoBefore(code) {
			if (code === null || markdownLineEnding(code)) {
				effects.exit("codeFencedFence");
				return self.interrupt ? ok(code) : effects.check(nonLazyContinuation, atNonLazyBreak, after)(code);
			}
			effects.enter("codeFencedFenceInfo");
			effects.enter("chunkString", { contentType: "string" });
			return info(code);
		}
		/**
		* In info.
		*
		* ```markdown
		* > | ~~~js
		*        ^
		*   | alert(1)
		*   | ~~~
		* ```
		*
		* @type {State}
		*/
		function info(code) {
			if (code === null || markdownLineEnding(code)) {
				effects.exit("chunkString");
				effects.exit("codeFencedFenceInfo");
				return infoBefore(code);
			}
			if (markdownSpace(code)) {
				effects.exit("chunkString");
				effects.exit("codeFencedFenceInfo");
				return factorySpace(effects, metaBefore, "whitespace")(code);
			}
			if (code === 96 && code === marker) return nok(code);
			effects.consume(code);
			return info;
		}
		/**
		* In opening fence, after info and whitespace, before meta.
		*
		* ```markdown
		* > | ~~~js eval
		*           ^
		*   | alert(1)
		*   | ~~~
		* ```
		*
		* @type {State}
		*/
		function metaBefore(code) {
			if (code === null || markdownLineEnding(code)) return infoBefore(code);
			effects.enter("codeFencedFenceMeta");
			effects.enter("chunkString", { contentType: "string" });
			return meta(code);
		}
		/**
		* In meta.
		*
		* ```markdown
		* > | ~~~js eval
		*           ^
		*   | alert(1)
		*   | ~~~
		* ```
		*
		* @type {State}
		*/
		function meta(code) {
			if (code === null || markdownLineEnding(code)) {
				effects.exit("chunkString");
				effects.exit("codeFencedFenceMeta");
				return infoBefore(code);
			}
			if (code === 96 && code === marker) return nok(code);
			effects.consume(code);
			return meta;
		}
		/**
		* At eol/eof in code, before a non-lazy closing fence or content.
		*
		* ```markdown
		* > | ~~~js
		*          ^
		* > | alert(1)
		*             ^
		*   | ~~~
		* ```
		*
		* @type {State}
		*/
		function atNonLazyBreak(code) {
			return effects.attempt(closeStart, after, contentBefore)(code);
		}
		/**
		* Before code content, not a closing fence, at eol.
		*
		* ```markdown
		*   | ~~~js
		* > | alert(1)
		*             ^
		*   | ~~~
		* ```
		*
		* @type {State}
		*/
		function contentBefore(code) {
			effects.enter("lineEnding");
			effects.consume(code);
			effects.exit("lineEnding");
			return contentStart;
		}
		/**
		* Before code content, not a closing fence.
		*
		* ```markdown
		*   | ~~~js
		* > | alert(1)
		*     ^
		*   | ~~~
		* ```
		*
		* @type {State}
		*/
		function contentStart(code) {
			return initialPrefix > 0 && markdownSpace(code) ? factorySpace(effects, beforeContentChunk, "linePrefix", initialPrefix + 1)(code) : beforeContentChunk(code);
		}
		/**
		* Before code content, after optional prefix.
		*
		* ```markdown
		*   | ~~~js
		* > | alert(1)
		*     ^
		*   | ~~~
		* ```
		*
		* @type {State}
		*/
		function beforeContentChunk(code) {
			if (code === null || markdownLineEnding(code)) return effects.check(nonLazyContinuation, atNonLazyBreak, after)(code);
			effects.enter("codeFlowValue");
			return contentChunk(code);
		}
		/**
		* In code content.
		*
		* ```markdown
		*   | ~~~js
		* > | alert(1)
		*     ^^^^^^^^
		*   | ~~~
		* ```
		*
		* @type {State}
		*/
		function contentChunk(code) {
			if (code === null || markdownLineEnding(code)) {
				effects.exit("codeFlowValue");
				return beforeContentChunk(code);
			}
			effects.consume(code);
			return contentChunk;
		}
		/**
		* After code.
		*
		* ```markdown
		*   | ~~~js
		*   | alert(1)
		* > | ~~~
		*        ^
		* ```
		*
		* @type {State}
		*/
		function after(code) {
			effects.exit("codeFenced");
			return ok(code);
		}
		/**
		* @this {TokenizeContext}
		* @type {Tokenizer}
		*/
		function tokenizeCloseStart(effects, ok, nok) {
			let size = 0;
			return startBefore;
			/**
			*
			*
			* @type {State}
			*/
			function startBefore(code) {
				effects.enter("lineEnding");
				effects.consume(code);
				effects.exit("lineEnding");
				return start;
			}
			/**
			* Before closing fence, at optional whitespace.
			*
			* ```markdown
			*   | ~~~js
			*   | alert(1)
			* > | ~~~
			*     ^
			* ```
			*
			* @type {State}
			*/
			function start(code) {
				effects.enter("codeFencedFence");
				return markdownSpace(code) ? factorySpace(effects, beforeSequenceClose, "linePrefix", self.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4)(code) : beforeSequenceClose(code);
			}
			/**
			* In closing fence, after optional whitespace, at sequence.
			*
			* ```markdown
			*   | ~~~js
			*   | alert(1)
			* > | ~~~
			*     ^
			* ```
			*
			* @type {State}
			*/
			function beforeSequenceClose(code) {
				if (code === marker) {
					effects.enter("codeFencedFenceSequence");
					return sequenceClose(code);
				}
				return nok(code);
			}
			/**
			* In closing fence sequence.
			*
			* ```markdown
			*   | ~~~js
			*   | alert(1)
			* > | ~~~
			*     ^
			* ```
			*
			* @type {State}
			*/
			function sequenceClose(code) {
				if (code === marker) {
					size++;
					effects.consume(code);
					return sequenceClose;
				}
				if (size >= sizeOpen) {
					effects.exit("codeFencedFenceSequence");
					return markdownSpace(code) ? factorySpace(effects, sequenceCloseAfter, "whitespace")(code) : sequenceCloseAfter(code);
				}
				return nok(code);
			}
			/**
			* After closing fence sequence, after optional whitespace.
			*
			* ```markdown
			*   | ~~~js
			*   | alert(1)
			* > | ~~~
			*        ^
			* ```
			*
			* @type {State}
			*/
			function sequenceCloseAfter(code) {
				if (code === null || markdownLineEnding(code)) {
					effects.exit("codeFencedFence");
					return ok(code);
				}
				return nok(code);
			}
		}
	}
	/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeNonLazyContinuation(effects, ok, nok) {
		const self = this;
		return start;
		/**
		*
		*
		* @type {State}
		*/
		function start(code) {
			if (code === null) return nok(code);
			effects.enter("lineEnding");
			effects.consume(code);
			effects.exit("lineEnding");
			return lineStart;
		}
		/**
		*
		*
		* @type {State}
		*/
		function lineStart(code) {
			return self.parser.lazy[self.now().line] ? nok(code) : ok(code);
		}
	}
	var nonLazyContinuation, codeFenced;
	var init_code_fenced = __esmMin((() => {
		init_micromark_factory_space();
		init_micromark_util_character();
		nonLazyContinuation = {
			tokenize: tokenizeNonLazyContinuation,
			partial: true
		};
		codeFenced = {
			name: "codeFenced",
			tokenize: tokenizeCodeFenced,
			concrete: true
		};
	}));

//#endregion
//#region node_modules/micromark-core-commonmark/lib/code-indented.js
/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeCodeIndented(effects, ok, nok) {
		const self = this;
		return start;
		/**
		* Start of code (indented).
		*
		* > **Parsing note**: it is not needed to check if this first line is a
		* > filled line (that it has a non-whitespace character), because blank lines
		* > are parsed already, so we never run into that.
		*
		* ```markdown
		* > |     aaa
		*     ^
		* ```
		*
		* @type {State}
		*/
		function start(code) {
			effects.enter("codeIndented");
			return factorySpace(effects, afterPrefix, "linePrefix", 5)(code);
		}
		/**
		* At start, after 1 or 4 spaces.
		*
		* ```markdown
		* > |     aaa
		*         ^
		* ```
		*
		* @type {State}
		*/
		function afterPrefix(code) {
			const tail = self.events[self.events.length - 1];
			return tail && tail[1].type === "linePrefix" && tail[2].sliceSerialize(tail[1], true).length >= 4 ? atBreak(code) : nok(code);
		}
		/**
		* At a break.
		*
		* ```markdown
		* > |     aaa
		*         ^  ^
		* ```
		*
		* @type {State}
		*/
		function atBreak(code) {
			if (code === null) return after(code);
			if (markdownLineEnding(code)) return effects.attempt(furtherStart, atBreak, after)(code);
			effects.enter("codeFlowValue");
			return inside(code);
		}
		/**
		* In code content.
		*
		* ```markdown
		* > |     aaa
		*         ^^^^
		* ```
		*
		* @type {State}
		*/
		function inside(code) {
			if (code === null || markdownLineEnding(code)) {
				effects.exit("codeFlowValue");
				return atBreak(code);
			}
			effects.consume(code);
			return inside;
		}
		/** @type {State} */
		function after(code) {
			effects.exit("codeIndented");
			return ok(code);
		}
	}
	/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeFurtherStart(effects, ok, nok) {
		const self = this;
		return furtherStart;
		/**
		* At eol, trying to parse another indent.
		*
		* ```markdown
		* > |     aaa
		*            ^
		*   |     bbb
		* ```
		*
		* @type {State}
		*/
		function furtherStart(code) {
			if (self.parser.lazy[self.now().line]) return nok(code);
			if (markdownLineEnding(code)) {
				effects.enter("lineEnding");
				effects.consume(code);
				effects.exit("lineEnding");
				return furtherStart;
			}
			return factorySpace(effects, afterPrefix, "linePrefix", 5)(code);
		}
		/**
		* At start, after 1 or 4 spaces.
		*
		* ```markdown
		* > |     aaa
		*         ^
		* ```
		*
		* @type {State}
		*/
		function afterPrefix(code) {
			const tail = self.events[self.events.length - 1];
			return tail && tail[1].type === "linePrefix" && tail[2].sliceSerialize(tail[1], true).length >= 4 ? ok(code) : markdownLineEnding(code) ? furtherStart(code) : nok(code);
		}
	}
	var codeIndented, furtherStart;
	var init_code_indented = __esmMin((() => {
		init_micromark_factory_space();
		init_micromark_util_character();
		codeIndented = {
			name: "codeIndented",
			tokenize: tokenizeCodeIndented
		};
		furtherStart = {
			tokenize: tokenizeFurtherStart,
			partial: true
		};
	}));

//#endregion
//#region node_modules/micromark-core-commonmark/lib/code-text.js
/** @type {Resolver} */
	function resolveCodeText(events) {
		let tailExitIndex = events.length - 4;
		let headEnterIndex = 3;
		/** @type {number} */
		let index;
		/** @type {number | undefined} */
		let enter;
		if ((events[headEnterIndex][1].type === "lineEnding" || events[headEnterIndex][1].type === "space") && (events[tailExitIndex][1].type === "lineEnding" || events[tailExitIndex][1].type === "space")) {
			index = headEnterIndex;
			while (++index < tailExitIndex) if (events[index][1].type === "codeTextData") {
				events[headEnterIndex][1].type = "codeTextPadding";
				events[tailExitIndex][1].type = "codeTextPadding";
				headEnterIndex += 2;
				tailExitIndex -= 2;
				break;
			}
		}
		index = headEnterIndex - 1;
		tailExitIndex++;
		while (++index <= tailExitIndex) if (enter === void 0) {
			if (index !== tailExitIndex && events[index][1].type !== "lineEnding") enter = index;
		} else if (index === tailExitIndex || events[index][1].type === "lineEnding") {
			events[enter][1].type = "codeTextData";
			if (index !== enter + 2) {
				events[enter][1].end = events[index - 1][1].end;
				events.splice(enter + 2, index - enter - 2);
				tailExitIndex -= index - enter - 2;
				index = enter + 2;
			}
			enter = void 0;
		}
		return events;
	}
	/**
	* @this {TokenizeContext}
	* @type {Previous}
	*/
	function previous(code) {
		return code !== 96 || this.events[this.events.length - 1][1].type === "characterEscape";
	}
	/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeCodeText(effects, ok, nok) {
		let sizeOpen = 0;
		/** @type {number} */
		let size;
		/** @type {Token} */
		let token;
		return start;
		/**
		* Start of code (text).
		*
		* ```markdown
		* > | `a`
		*     ^
		* > | \`a`
		*      ^
		* ```
		*
		* @type {State}
		*/
		function start(code) {
			effects.enter("codeText");
			effects.enter("codeTextSequence");
			return sequenceOpen(code);
		}
		/**
		* In opening sequence.
		*
		* ```markdown
		* > | `a`
		*     ^
		* ```
		*
		* @type {State}
		*/
		function sequenceOpen(code) {
			if (code === 96) {
				effects.consume(code);
				sizeOpen++;
				return sequenceOpen;
			}
			effects.exit("codeTextSequence");
			return between(code);
		}
		/**
		* Between something and something else.
		*
		* ```markdown
		* > | `a`
		*      ^^
		* ```
		*
		* @type {State}
		*/
		function between(code) {
			if (code === null) return nok(code);
			if (code === 32) {
				effects.enter("space");
				effects.consume(code);
				effects.exit("space");
				return between;
			}
			if (code === 96) {
				token = effects.enter("codeTextSequence");
				size = 0;
				return sequenceClose(code);
			}
			if (markdownLineEnding(code)) {
				effects.enter("lineEnding");
				effects.consume(code);
				effects.exit("lineEnding");
				return between;
			}
			effects.enter("codeTextData");
			return data(code);
		}
		/**
		* In data.
		*
		* ```markdown
		* > | `a`
		*      ^
		* ```
		*
		* @type {State}
		*/
		function data(code) {
			if (code === null || code === 32 || code === 96 || markdownLineEnding(code)) {
				effects.exit("codeTextData");
				return between(code);
			}
			effects.consume(code);
			return data;
		}
		/**
		* In closing sequence.
		*
		* ```markdown
		* > | `a`
		*       ^
		* ```
		*
		* @type {State}
		*/
		function sequenceClose(code) {
			if (code === 96) {
				effects.consume(code);
				size++;
				return sequenceClose;
			}
			if (size === sizeOpen) {
				effects.exit("codeTextSequence");
				effects.exit("codeText");
				return ok(code);
			}
			token.type = "codeTextData";
			return data(code);
		}
	}
	var codeText;
	var init_code_text = __esmMin((() => {
		init_micromark_util_character();
		codeText = {
			name: "codeText",
			tokenize: tokenizeCodeText,
			resolve: resolveCodeText,
			previous
		};
	}));

//#endregion
//#region node_modules/micromark-util-subtokenize/index.js
/**
	* Tokenize subcontent.
	*
	* @param {Array<Event>} events
	*   List of events.
	* @returns {boolean}
	*   Whether subtokens were found.
	*/
	function subtokenize(events) {
		/** @type {Record<string, number>} */
		const jumps = {};
		let index = -1;
		/** @type {Event} */
		let event;
		/** @type {number | undefined} */
		let lineIndex;
		/** @type {number} */
		let otherIndex;
		/** @type {Event} */
		let otherEvent;
		/** @type {Array<Event>} */
		let parameters;
		/** @type {Array<Event>} */
		let subevents;
		/** @type {boolean | undefined} */
		let more;
		while (++index < events.length) {
			while (index in jumps) index = jumps[index];
			event = events[index];
			if (index && event[1].type === "chunkFlow" && events[index - 1][1].type === "listItemPrefix") {
				subevents = event[1]._tokenizer.events;
				otherIndex = 0;
				if (otherIndex < subevents.length && subevents[otherIndex][1].type === "lineEndingBlank") otherIndex += 2;
				if (otherIndex < subevents.length && subevents[otherIndex][1].type === "content") while (++otherIndex < subevents.length) {
					if (subevents[otherIndex][1].type === "content") break;
					if (subevents[otherIndex][1].type === "chunkText") {
						subevents[otherIndex][1]._isInFirstContentOfListItem = true;
						otherIndex++;
					}
				}
			}
			if (event[0] === "enter") {
				if (event[1].contentType) {
					Object.assign(jumps, subcontent(events, index));
					index = jumps[index];
					more = true;
				}
			} else if (event[1]._container) {
				otherIndex = index;
				lineIndex = void 0;
				while (otherIndex--) {
					otherEvent = events[otherIndex];
					if (otherEvent[1].type === "lineEnding" || otherEvent[1].type === "lineEndingBlank") {
						if (otherEvent[0] === "enter") {
							if (lineIndex) events[lineIndex][1].type = "lineEndingBlank";
							otherEvent[1].type = "lineEnding";
							lineIndex = otherIndex;
						}
					} else break;
				}
				if (lineIndex) {
					event[1].end = Object.assign({}, events[lineIndex][1].start);
					parameters = events.slice(lineIndex, index);
					parameters.unshift(event);
					splice(events, lineIndex, index - lineIndex + 1, parameters);
				}
			}
		}
		return !more;
	}
	/**
	* Tokenize embedded tokens.
	*
	* @param {Array<Event>} events
	* @param {number} eventIndex
	* @returns {Record<string, number>}
	*/
	function subcontent(events, eventIndex) {
		const token = events[eventIndex][1];
		const context = events[eventIndex][2];
		let startPosition = eventIndex - 1;
		/** @type {Array<number>} */
		const startPositions = [];
		const tokenizer = token._tokenizer || context.parser[token.contentType](token.start);
		const childEvents = tokenizer.events;
		/** @type {Array<[number, number]>} */
		const jumps = [];
		/** @type {Record<string, number>} */
		const gaps = {};
		/** @type {Array<Chunk>} */
		let stream;
		/** @type {Token | undefined} */
		let previous;
		let index = -1;
		/** @type {Token | undefined} */
		let current = token;
		let adjust = 0;
		let start = 0;
		const breaks = [start];
		while (current) {
			while (events[++startPosition][1] !== current);
			startPositions.push(startPosition);
			if (!current._tokenizer) {
				stream = context.sliceStream(current);
				if (!current.next) stream.push(null);
				if (previous) tokenizer.defineSkip(current.start);
				if (current._isInFirstContentOfListItem) tokenizer._gfmTasklistFirstContentOfListItem = true;
				tokenizer.write(stream);
				if (current._isInFirstContentOfListItem) tokenizer._gfmTasklistFirstContentOfListItem = void 0;
			}
			previous = current;
			current = current.next;
		}
		current = token;
		while (++index < childEvents.length) if (childEvents[index][0] === "exit" && childEvents[index - 1][0] === "enter" && childEvents[index][1].type === childEvents[index - 1][1].type && childEvents[index][1].start.line !== childEvents[index][1].end.line) {
			start = index + 1;
			breaks.push(start);
			current._tokenizer = void 0;
			current.previous = void 0;
			current = current.next;
		}
		tokenizer.events = [];
		if (current) {
			current._tokenizer = void 0;
			current.previous = void 0;
		} else breaks.pop();
		index = breaks.length;
		while (index--) {
			const slice = childEvents.slice(breaks[index], breaks[index + 1]);
			const start = startPositions.pop();
			jumps.unshift([start, start + slice.length - 1]);
			splice(events, start, 2, slice);
		}
		index = -1;
		while (++index < jumps.length) {
			gaps[adjust + jumps[index][0]] = adjust + jumps[index][1];
			adjust += jumps[index][1] - jumps[index][0] - 1;
		}
		return gaps;
	}
	var init_micromark_util_subtokenize = __esmMin((() => {
		init_micromark_util_chunked();
	}));

//#endregion
//#region node_modules/micromark-core-commonmark/lib/content.js
/**
	* Content is transparent: it’s parsed right now. That way, definitions are also
	* parsed right now: before text in paragraphs (specifically, media) are parsed.
	*
	* @type {Resolver}
	*/
	function resolveContent(events) {
		subtokenize(events);
		return events;
	}
	/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeContent(effects, ok) {
		/** @type {Token | undefined} */
		let previous;
		return chunkStart;
		/**
		* Before a content chunk.
		*
		* ```markdown
		* > | abc
		*     ^
		* ```
		*
		* @type {State}
		*/
		function chunkStart(code) {
			effects.enter("content");
			previous = effects.enter("chunkContent", { contentType: "content" });
			return chunkInside(code);
		}
		/**
		* In a content chunk.
		*
		* ```markdown
		* > | abc
		*     ^^^
		* ```
		*
		* @type {State}
		*/
		function chunkInside(code) {
			if (code === null) return contentEnd(code);
			if (markdownLineEnding(code)) return effects.check(continuationConstruct, contentContinue, contentEnd)(code);
			effects.consume(code);
			return chunkInside;
		}
		/**
		*
		*
		* @type {State}
		*/
		function contentEnd(code) {
			effects.exit("chunkContent");
			effects.exit("content");
			return ok(code);
		}
		/**
		*
		*
		* @type {State}
		*/
		function contentContinue(code) {
			effects.consume(code);
			effects.exit("chunkContent");
			previous.next = effects.enter("chunkContent", {
				contentType: "content",
				previous
			});
			previous = previous.next;
			return chunkInside;
		}
	}
	/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeContinuation(effects, ok, nok) {
		const self = this;
		return startLookahead;
		/**
		*
		*
		* @type {State}
		*/
		function startLookahead(code) {
			effects.exit("chunkContent");
			effects.enter("lineEnding");
			effects.consume(code);
			effects.exit("lineEnding");
			return factorySpace(effects, prefixed, "linePrefix");
		}
		/**
		*
		*
		* @type {State}
		*/
		function prefixed(code) {
			if (code === null || markdownLineEnding(code)) return nok(code);
			const tail = self.events[self.events.length - 1];
			if (!self.parser.constructs.disable.null.includes("codeIndented") && tail && tail[1].type === "linePrefix" && tail[2].sliceSerialize(tail[1], true).length >= 4) return ok(code);
			return effects.interrupt(self.parser.constructs.flow, nok, ok)(code);
		}
	}
	var content, continuationConstruct;
	var init_content = __esmMin((() => {
		init_micromark_factory_space();
		init_micromark_util_character();
		init_micromark_util_subtokenize();
		content = {
			tokenize: tokenizeContent,
			resolve: resolveContent
		};
		continuationConstruct = {
			tokenize: tokenizeContinuation,
			partial: true
		};
	}));

//#endregion
//#region node_modules/micromark-factory-destination/index.js
/**
	* Parse destinations.
	*
	* ###### Examples
	*
	* ```markdown
	* <a>
	* <a\>b>
	* <a b>
	* <a)>
	* a
	* a\)b
	* a(b)c
	* a(b)
	* ```
	*
	* @param {Effects} effects
	*   Context.
	* @param {State} ok
	*   State switched to when successful.
	* @param {State} nok
	*   State switched to when unsuccessful.
	* @param {TokenType} type
	*   Type for whole (`<a>` or `b`).
	* @param {TokenType} literalType
	*   Type when enclosed (`<a>`).
	* @param {TokenType} literalMarkerType
	*   Type for enclosing (`<` and `>`).
	* @param {TokenType} rawType
	*   Type when not enclosed (`b`).
	* @param {TokenType} stringType
	*   Type for the value (`a` or `b`).
	* @param {number | undefined} [max=Infinity]
	*   Depth of nested parens (inclusive).
	* @returns {State}
	*   Start state.
	*/ function factoryDestination(effects, ok, nok, type, literalType, literalMarkerType, rawType, stringType, max) {
		const limit = max || Number.POSITIVE_INFINITY;
		let balance = 0;
		return start;
		/**
		* Start of destination.
		*
		* ```markdown
		* > | <aa>
		*     ^
		* > | aa
		*     ^
		* ```
		*
		* @type {State}
		*/
		function start(code) {
			if (code === 60) {
				effects.enter(type);
				effects.enter(literalType);
				effects.enter(literalMarkerType);
				effects.consume(code);
				effects.exit(literalMarkerType);
				return enclosedBefore;
			}
			if (code === null || code === 32 || code === 41 || asciiControl(code)) return nok(code);
			effects.enter(type);
			effects.enter(rawType);
			effects.enter(stringType);
			effects.enter("chunkString", { contentType: "string" });
			return raw(code);
		}
		/**
		* After `<`, at an enclosed destination.
		*
		* ```markdown
		* > | <aa>
		*      ^
		* ```
		*
		* @type {State}
		*/
		function enclosedBefore(code) {
			if (code === 62) {
				effects.enter(literalMarkerType);
				effects.consume(code);
				effects.exit(literalMarkerType);
				effects.exit(literalType);
				effects.exit(type);
				return ok;
			}
			effects.enter(stringType);
			effects.enter("chunkString", { contentType: "string" });
			return enclosed(code);
		}
		/**
		* In enclosed destination.
		*
		* ```markdown
		* > | <aa>
		*      ^
		* ```
		*
		* @type {State}
		*/
		function enclosed(code) {
			if (code === 62) {
				effects.exit("chunkString");
				effects.exit(stringType);
				return enclosedBefore(code);
			}
			if (code === null || code === 60 || markdownLineEnding(code)) return nok(code);
			effects.consume(code);
			return code === 92 ? enclosedEscape : enclosed;
		}
		/**
		* After `\`, at a special character.
		*
		* ```markdown
		* > | <a\*a>
		*        ^
		* ```
		*
		* @type {State}
		*/
		function enclosedEscape(code) {
			if (code === 60 || code === 62 || code === 92) {
				effects.consume(code);
				return enclosed;
			}
			return enclosed(code);
		}
		/**
		* In raw destination.
		*
		* ```markdown
		* > | aa
		*     ^
		* ```
		*
		* @type {State}
		*/
		function raw(code) {
			if (!balance && (code === null || code === 41 || markdownLineEndingOrSpace(code))) {
				effects.exit("chunkString");
				effects.exit(stringType);
				effects.exit(rawType);
				effects.exit(type);
				return ok(code);
			}
			if (balance < limit && code === 40) {
				effects.consume(code);
				balance++;
				return raw;
			}
			if (code === 41) {
				effects.consume(code);
				balance--;
				return raw;
			}
			if (code === null || code === 32 || code === 40 || asciiControl(code)) return nok(code);
			effects.consume(code);
			return code === 92 ? rawEscape : raw;
		}
		/**
		* After `\`, at special character.
		*
		* ```markdown
		* > | a\*a
		*       ^
		* ```
		*
		* @type {State}
		*/
		function rawEscape(code) {
			if (code === 40 || code === 41 || code === 92) {
				effects.consume(code);
				return raw;
			}
			return raw(code);
		}
	}
	var init_micromark_factory_destination = __esmMin((() => {
		init_micromark_util_character();
	}));

//#endregion
//#region node_modules/micromark-factory-label/index.js
/**
	* Parse labels.
	*
	* > 👉 **Note**: labels in markdown are capped at 999 characters in the string.
	*
	* ###### Examples
	*
	* ```markdown
	* [a]
	* [a
	* b]
	* [a\]b]
	* ```
	*
	* @this {TokenizeContext}
	*   Tokenize context.
	* @param {Effects} effects
	*   Context.
	* @param {State} ok
	*   State switched to when successful.
	* @param {State} nok
	*   State switched to when unsuccessful.
	* @param {TokenType} type
	*   Type of the whole label (`[a]`).
	* @param {TokenType} markerType
	*   Type for the markers (`[` and `]`).
	* @param {TokenType} stringType
	*   Type for the identifier (`a`).
	* @returns {State}
	*   Start state.
	*/ function factoryLabel(effects, ok, nok, type, markerType, stringType) {
		const self = this;
		let size = 0;
		/** @type {boolean} */
		let seen;
		return start;
		/**
		* Start of label.
		*
		* ```markdown
		* > | [a]
		*     ^
		* ```
		*
		* @type {State}
		*/
		function start(code) {
			effects.enter(type);
			effects.enter(markerType);
			effects.consume(code);
			effects.exit(markerType);
			effects.enter(stringType);
			return atBreak;
		}
		/**
		* In label, at something, before something else.
		*
		* ```markdown
		* > | [a]
		*      ^
		* ```
		*
		* @type {State}
		*/
		function atBreak(code) {
			if (size > 999 || code === null || code === 91 || code === 93 && !seen || code === 94 && !size && "_hiddenFootnoteSupport" in self.parser.constructs) return nok(code);
			if (code === 93) {
				effects.exit(stringType);
				effects.enter(markerType);
				effects.consume(code);
				effects.exit(markerType);
				effects.exit(type);
				return ok;
			}
			if (markdownLineEnding(code)) {
				effects.enter("lineEnding");
				effects.consume(code);
				effects.exit("lineEnding");
				return atBreak;
			}
			effects.enter("chunkString", { contentType: "string" });
			return labelInside(code);
		}
		/**
		* In label, in text.
		*
		* ```markdown
		* > | [a]
		*      ^
		* ```
		*
		* @type {State}
		*/
		function labelInside(code) {
			if (code === null || code === 91 || code === 93 || markdownLineEnding(code) || size++ > 999) {
				effects.exit("chunkString");
				return atBreak(code);
			}
			effects.consume(code);
			if (!seen) seen = !markdownSpace(code);
			return code === 92 ? labelEscape : labelInside;
		}
		/**
		* After `\`, at a special character.
		*
		* ```markdown
		* > | [a\*a]
		*        ^
		* ```
		*
		* @type {State}
		*/
		function labelEscape(code) {
			if (code === 91 || code === 92 || code === 93) {
				effects.consume(code);
				size++;
				return labelInside;
			}
			return labelInside(code);
		}
	}
	var init_micromark_factory_label = __esmMin((() => {
		init_micromark_util_character();
	}));

//#endregion
//#region node_modules/micromark-factory-title/index.js
/**
	* Parse titles.
	*
	* ###### Examples
	*
	* ```markdown
	* "a"
	* 'b'
	* (c)
	* "a
	* b"
	* 'a
	*     b'
	* (a\)b)
	* ```
	*
	* @param {Effects} effects
	*   Context.
	* @param {State} ok
	*   State switched to when successful.
	* @param {State} nok
	*   State switched to when unsuccessful.
	* @param {TokenType} type
	*   Type of the whole title (`"a"`, `'b'`, `(c)`).
	* @param {TokenType} markerType
	*   Type for the markers (`"`, `'`, `(`, and `)`).
	* @param {TokenType} stringType
	*   Type for the value (`a`).
	* @returns {State}
	*   Start state.
	*/ function factoryTitle(effects, ok, nok, type, markerType, stringType) {
		/** @type {NonNullable<Code>} */
		let marker;
		return start;
		/**
		* Start of title.
		*
		* ```markdown
		* > | "a"
		*     ^
		* ```
		*
		* @type {State}
		*/
		function start(code) {
			if (code === 34 || code === 39 || code === 40) {
				effects.enter(type);
				effects.enter(markerType);
				effects.consume(code);
				effects.exit(markerType);
				marker = code === 40 ? 41 : code;
				return begin;
			}
			return nok(code);
		}
		/**
		* After opening marker.
		*
		* This is also used at the closing marker.
		*
		* ```markdown
		* > | "a"
		*      ^
		* ```
		*
		* @type {State}
		*/
		function begin(code) {
			if (code === marker) {
				effects.enter(markerType);
				effects.consume(code);
				effects.exit(markerType);
				effects.exit(type);
				return ok;
			}
			effects.enter(stringType);
			return atBreak(code);
		}
		/**
		* At something, before something else.
		*
		* ```markdown
		* > | "a"
		*      ^
		* ```
		*
		* @type {State}
		*/
		function atBreak(code) {
			if (code === marker) {
				effects.exit(stringType);
				return begin(marker);
			}
			if (code === null) return nok(code);
			if (markdownLineEnding(code)) {
				effects.enter("lineEnding");
				effects.consume(code);
				effects.exit("lineEnding");
				return factorySpace(effects, atBreak, "linePrefix");
			}
			effects.enter("chunkString", { contentType: "string" });
			return inside(code);
		}
		/**
		*
		*
		* @type {State}
		*/
		function inside(code) {
			if (code === marker || code === null || markdownLineEnding(code)) {
				effects.exit("chunkString");
				return atBreak(code);
			}
			effects.consume(code);
			return code === 92 ? escape : inside;
		}
		/**
		* After `\`, at a special character.
		*
		* ```markdown
		* > | "a\*b"
		*      ^
		* ```
		*
		* @type {State}
		*/
		function escape(code) {
			if (code === marker || code === 92) {
				effects.consume(code);
				return inside;
			}
			return inside(code);
		}
	}
	var init_micromark_factory_title = __esmMin((() => {
		init_micromark_factory_space();
		init_micromark_util_character();
	}));

//#endregion
//#region node_modules/micromark-factory-whitespace/index.js
/**
	* Parse spaces and tabs.
	*
	* There is no `nok` parameter:
	*
	* *   line endings or spaces in markdown are often optional, in which case this
	*     factory can be used and `ok` will be switched to whether spaces were found
	*     or not
	* *   one line ending or space can be detected with
	*     `markdownLineEndingOrSpace(code)` right before using `factoryWhitespace`
	*
	* @param {Effects} effects
	*   Context.
	* @param {State} ok
	*   State switched to when successful.
	* @returns
	*   Start state.
	*/
	function factoryWhitespace(effects, ok) {
		/** @type {boolean} */
		let seen;
		return start;
		/** @type {State} */
		function start(code) {
			if (markdownLineEnding(code)) {
				effects.enter("lineEnding");
				effects.consume(code);
				effects.exit("lineEnding");
				seen = true;
				return start;
			}
			if (markdownSpace(code)) return factorySpace(effects, start, seen ? "linePrefix" : "lineSuffix")(code);
			return ok(code);
		}
	}
	var init_micromark_factory_whitespace = __esmMin((() => {
		init_micromark_factory_space();
		init_micromark_util_character();
	}));

//#endregion
//#region node_modules/micromark-util-normalize-identifier/index.js
/**
	* Normalize an identifier (as found in references, definitions).
	*
	* Collapses markdown whitespace, trim, and then lower- and uppercase.
	*
	* Some characters are considered “uppercase”, such as U+03F4 (`ϴ`), but if their
	* lowercase counterpart (U+03B8 (`θ`)) is uppercased will result in a different
	* uppercase character (U+0398 (`Θ`)).
	* So, to get a canonical form, we perform both lower- and uppercase.
	*
	* Using uppercase last makes sure keys will never interact with default
	* prototypal values (such as `constructor`): nothing in the prototype of
	* `Object` is uppercase.
	*
	* @param {string} value
	*   Identifier to normalize.
	* @returns {string}
	*   Normalized identifier.
	*/
	function normalizeIdentifier(value) {
		return value.replace(/[\t\n\r ]+/g, " ").replace(/^ | $/g, "").toLowerCase().toUpperCase();
	}
	var init_micromark_util_normalize_identifier = __esmMin((() => {}));

//#endregion
//#region node_modules/micromark-core-commonmark/lib/definition.js
/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeDefinition(effects, ok, nok) {
		const self = this;
		/** @type {string} */
		let identifier;
		return start;
		/**
		* At start of a definition.
		*
		* ```markdown
		* > | [a]: b "c"
		*     ^
		* ```
		*
		* @type {State}
		*/
		function start(code) {
			effects.enter("definition");
			return before(code);
		}
		/**
		* After optional whitespace, at `[`.
		*
		* ```markdown
		* > | [a]: b "c"
		*     ^
		* ```
		*
		* @type {State}
		*/
		function before(code) {
			return factoryLabel.call(self, effects, labelAfter, nok, "definitionLabel", "definitionLabelMarker", "definitionLabelString")(code);
		}
		/**
		* After label.
		*
		* ```markdown
		* > | [a]: b "c"
		*        ^
		* ```
		*
		* @type {State}
		*/
		function labelAfter(code) {
			identifier = normalizeIdentifier(self.sliceSerialize(self.events[self.events.length - 1][1]).slice(1, -1));
			if (code === 58) {
				effects.enter("definitionMarker");
				effects.consume(code);
				effects.exit("definitionMarker");
				return markerAfter;
			}
			return nok(code);
		}
		/**
		* After marker.
		*
		* ```markdown
		* > | [a]: b "c"
		*         ^
		* ```
		*
		* @type {State}
		*/
		function markerAfter(code) {
			return markdownLineEndingOrSpace(code) ? factoryWhitespace(effects, destinationBefore)(code) : destinationBefore(code);
		}
		/**
		* Before destination.
		*
		* ```markdown
		* > | [a]: b "c"
		*          ^
		* ```
		*
		* @type {State}
		*/
		function destinationBefore(code) {
			return factoryDestination(effects, destinationAfter, nok, "definitionDestination", "definitionDestinationLiteral", "definitionDestinationLiteralMarker", "definitionDestinationRaw", "definitionDestinationString")(code);
		}
		/**
		* After destination.
		*
		* ```markdown
		* > | [a]: b "c"
		*           ^
		* ```
		*
		* @type {State}
		*/
		function destinationAfter(code) {
			return effects.attempt(titleBefore, after, after)(code);
		}
		/**
		* After definition.
		*
		* ```markdown
		* > | [a]: b
		*           ^
		* > | [a]: b "c"
		*               ^
		* ```
		*
		* @type {State}
		*/
		function after(code) {
			return markdownSpace(code) ? factorySpace(effects, afterWhitespace, "whitespace")(code) : afterWhitespace(code);
		}
		/**
		* After definition, after optional whitespace.
		*
		* ```markdown
		* > | [a]: b
		*           ^
		* > | [a]: b "c"
		*               ^
		* ```
		*
		* @type {State}
		*/
		function afterWhitespace(code) {
			if (code === null || markdownLineEnding(code)) {
				effects.exit("definition");
				self.parser.defined.push(identifier);
				return ok(code);
			}
			return nok(code);
		}
	}
	/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeTitleBefore(effects, ok, nok) {
		return titleBefore;
		/**
		* After destination, at whitespace.
		*
		* ```markdown
		* > | [a]: b
		*           ^
		* > | [a]: b "c"
		*           ^
		* ```
		*
		* @type {State}
		*/
		function titleBefore(code) {
			return markdownLineEndingOrSpace(code) ? factoryWhitespace(effects, beforeMarker)(code) : nok(code);
		}
		/**
		* At title.
		*
		* ```markdown
		*   | [a]: b
		* > | "c"
		*     ^
		* ```
		*
		* @type {State}
		*/
		function beforeMarker(code) {
			return factoryTitle(effects, titleAfter, nok, "definitionTitle", "definitionTitleMarker", "definitionTitleString")(code);
		}
		/**
		* After title.
		*
		* ```markdown
		* > | [a]: b "c"
		*               ^
		* ```
		*
		* @type {State}
		*/
		function titleAfter(code) {
			return markdownSpace(code) ? factorySpace(effects, titleAfterOptionalWhitespace, "whitespace")(code) : titleAfterOptionalWhitespace(code);
		}
		/**
		* After title, after optional whitespace.
		*
		* ```markdown
		* > | [a]: b "c"
		*               ^
		* ```
		*
		* @type {State}
		*/
		function titleAfterOptionalWhitespace(code) {
			return code === null || markdownLineEnding(code) ? ok(code) : nok(code);
		}
	}
	var definition, titleBefore;
	var init_definition = __esmMin((() => {
		init_micromark_factory_destination();
		init_micromark_factory_label();
		init_micromark_factory_space();
		init_micromark_factory_title();
		init_micromark_factory_whitespace();
		init_micromark_util_character();
		init_micromark_util_normalize_identifier();
		definition = {
			name: "definition",
			tokenize: tokenizeDefinition
		};
		titleBefore = {
			tokenize: tokenizeTitleBefore,
			partial: true
		};
	}));

//#endregion
//#region node_modules/micromark-core-commonmark/lib/hard-break-escape.js
/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeHardBreakEscape(effects, ok, nok) {
		return start;
		/**
		* Start of a hard break (escape).
		*
		* ```markdown
		* > | a\
		*      ^
		*   | b
		* ```
		*
		* @type {State}
		*/
		function start(code) {
			effects.enter("hardBreakEscape");
			effects.consume(code);
			return after;
		}
		/**
		* After `\`, at eol.
		*
		* ```markdown
		* > | a\
		*       ^
		*   | b
		* ```
		*
		*  @type {State}
		*/
		function after(code) {
			if (markdownLineEnding(code)) {
				effects.exit("hardBreakEscape");
				return ok(code);
			}
			return nok(code);
		}
	}
	var hardBreakEscape;
	var init_hard_break_escape = __esmMin((() => {
		init_micromark_util_character();
		hardBreakEscape = {
			name: "hardBreakEscape",
			tokenize: tokenizeHardBreakEscape
		};
	}));

//#endregion
//#region node_modules/micromark-core-commonmark/lib/heading-atx.js
/** @type {Resolver} */
	function resolveHeadingAtx(events, context) {
		let contentEnd = events.length - 2;
		let contentStart = 3;
		/** @type {Token} */
		let content;
		/** @type {Token} */
		let text;
		if (events[contentStart][1].type === "whitespace") contentStart += 2;
		if (contentEnd - 2 > contentStart && events[contentEnd][1].type === "whitespace") contentEnd -= 2;
		if (events[contentEnd][1].type === "atxHeadingSequence" && (contentStart === contentEnd - 1 || contentEnd - 4 > contentStart && events[contentEnd - 2][1].type === "whitespace")) contentEnd -= contentStart + 1 === contentEnd ? 2 : 4;
		if (contentEnd > contentStart) {
			content = {
				type: "atxHeadingText",
				start: events[contentStart][1].start,
				end: events[contentEnd][1].end
			};
			text = {
				type: "chunkText",
				start: events[contentStart][1].start,
				end: events[contentEnd][1].end,
				contentType: "text"
			};
			splice(events, contentStart, contentEnd - contentStart + 1, [
				[
					"enter",
					content,
					context
				],
				[
					"enter",
					text,
					context
				],
				[
					"exit",
					text,
					context
				],
				[
					"exit",
					content,
					context
				]
			]);
		}
		return events;
	}
	/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeHeadingAtx(effects, ok, nok) {
		let size = 0;
		return start;
		/**
		* Start of a heading (atx).
		*
		* ```markdown
		* > | ## aa
		*     ^
		* ```
		*
		* @type {State}
		*/
		function start(code) {
			effects.enter("atxHeading");
			return before(code);
		}
		/**
		* After optional whitespace, at `#`.
		*
		* ```markdown
		* > | ## aa
		*     ^
		* ```
		*
		* @type {State}
		*/
		function before(code) {
			effects.enter("atxHeadingSequence");
			return sequenceOpen(code);
		}
		/**
		* In opening sequence.
		*
		* ```markdown
		* > | ## aa
		*     ^
		* ```
		*
		* @type {State}
		*/
		function sequenceOpen(code) {
			if (code === 35 && size++ < 6) {
				effects.consume(code);
				return sequenceOpen;
			}
			if (code === null || markdownLineEndingOrSpace(code)) {
				effects.exit("atxHeadingSequence");
				return atBreak(code);
			}
			return nok(code);
		}
		/**
		* After something, before something else.
		*
		* ```markdown
		* > | ## aa
		*       ^
		* ```
		*
		* @type {State}
		*/
		function atBreak(code) {
			if (code === 35) {
				effects.enter("atxHeadingSequence");
				return sequenceFurther(code);
			}
			if (code === null || markdownLineEnding(code)) {
				effects.exit("atxHeading");
				return ok(code);
			}
			if (markdownSpace(code)) return factorySpace(effects, atBreak, "whitespace")(code);
			effects.enter("atxHeadingText");
			return data(code);
		}
		/**
		* In further sequence (after whitespace).
		*
		* Could be normal “visible” hashes in the heading or a final sequence.
		*
		* ```markdown
		* > | ## aa ##
		*           ^
		* ```
		*
		* @type {State}
		*/
		function sequenceFurther(code) {
			if (code === 35) {
				effects.consume(code);
				return sequenceFurther;
			}
			effects.exit("atxHeadingSequence");
			return atBreak(code);
		}
		/**
		* In text.
		*
		* ```markdown
		* > | ## aa
		*        ^
		* ```
		*
		* @type {State}
		*/
		function data(code) {
			if (code === null || code === 35 || markdownLineEndingOrSpace(code)) {
				effects.exit("atxHeadingText");
				return atBreak(code);
			}
			effects.consume(code);
			return data;
		}
	}
	var headingAtx;
	var init_heading_atx = __esmMin((() => {
		init_micromark_factory_space();
		init_micromark_util_character();
		init_micromark_util_chunked();
		headingAtx = {
			name: "headingAtx",
			tokenize: tokenizeHeadingAtx,
			resolve: resolveHeadingAtx
		};
	}));

//#endregion
//#region node_modules/micromark-util-html-tag-name/index.js
	var htmlBlockNames, htmlRawNames;
	var init_micromark_util_html_tag_name = __esmMin((() => {
		htmlBlockNames = [
			"address",
			"article",
			"aside",
			"base",
			"basefont",
			"blockquote",
			"body",
			"caption",
			"center",
			"col",
			"colgroup",
			"dd",
			"details",
			"dialog",
			"dir",
			"div",
			"dl",
			"dt",
			"fieldset",
			"figcaption",
			"figure",
			"footer",
			"form",
			"frame",
			"frameset",
			"h1",
			"h2",
			"h3",
			"h4",
			"h5",
			"h6",
			"head",
			"header",
			"hr",
			"html",
			"iframe",
			"legend",
			"li",
			"link",
			"main",
			"menu",
			"menuitem",
			"nav",
			"noframes",
			"ol",
			"optgroup",
			"option",
			"p",
			"param",
			"search",
			"section",
			"summary",
			"table",
			"tbody",
			"td",
			"tfoot",
			"th",
			"thead",
			"title",
			"tr",
			"track",
			"ul"
		];
		htmlRawNames = [
			"pre",
			"script",
			"style",
			"textarea"
		];
	}));

//#endregion
//#region node_modules/micromark-core-commonmark/lib/html-flow.js
/** @type {Resolver} */
	function resolveToHtmlFlow(events) {
		let index = events.length;
		while (index--) if (events[index][0] === "enter" && events[index][1].type === "htmlFlow") break;
		if (index > 1 && events[index - 2][1].type === "linePrefix") {
			events[index][1].start = events[index - 2][1].start;
			events[index + 1][1].start = events[index - 2][1].start;
			events.splice(index - 2, 2);
		}
		return events;
	}
	/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeHtmlFlow(effects, ok, nok) {
		const self = this;
		/** @type {number} */
		let marker;
		/** @type {boolean} */
		let closingTag;
		/** @type {string} */
		let buffer;
		/** @type {number} */
		let index;
		/** @type {Code} */
		let markerB;
		return start;
		/**
		* Start of HTML (flow).
		*
		* ```markdown
		* > | <x />
		*     ^
		* ```
		*
		* @type {State}
		*/
		function start(code) {
			return before(code);
		}
		/**
		* At `<`, after optional whitespace.
		*
		* ```markdown
		* > | <x />
		*     ^
		* ```
		*
		* @type {State}
		*/
		function before(code) {
			effects.enter("htmlFlow");
			effects.enter("htmlFlowData");
			effects.consume(code);
			return open;
		}
		/**
		* After `<`, at tag name or other stuff.
		*
		* ```markdown
		* > | <x />
		*      ^
		* > | <!doctype>
		*      ^
		* > | <!--xxx-->
		*      ^
		* ```
		*
		* @type {State}
		*/
		function open(code) {
			if (code === 33) {
				effects.consume(code);
				return declarationOpen;
			}
			if (code === 47) {
				effects.consume(code);
				closingTag = true;
				return tagCloseStart;
			}
			if (code === 63) {
				effects.consume(code);
				marker = 3;
				return self.interrupt ? ok : continuationDeclarationInside;
			}
			if (asciiAlpha(code)) {
				effects.consume(code);
				buffer = String.fromCharCode(code);
				return tagName;
			}
			return nok(code);
		}
		/**
		* After `<!`, at declaration, comment, or CDATA.
		*
		* ```markdown
		* > | <!doctype>
		*       ^
		* > | <!--xxx-->
		*       ^
		* > | <![CDATA[>&<]]>
		*       ^
		* ```
		*
		* @type {State}
		*/
		function declarationOpen(code) {
			if (code === 45) {
				effects.consume(code);
				marker = 2;
				return commentOpenInside;
			}
			if (code === 91) {
				effects.consume(code);
				marker = 5;
				index = 0;
				return cdataOpenInside;
			}
			if (asciiAlpha(code)) {
				effects.consume(code);
				marker = 4;
				return self.interrupt ? ok : continuationDeclarationInside;
			}
			return nok(code);
		}
		/**
		* After `<!-`, inside a comment, at another `-`.
		*
		* ```markdown
		* > | <!--xxx-->
		*        ^
		* ```
		*
		* @type {State}
		*/
		function commentOpenInside(code) {
			if (code === 45) {
				effects.consume(code);
				return self.interrupt ? ok : continuationDeclarationInside;
			}
			return nok(code);
		}
		/**
		* After `<![`, inside CDATA, expecting `CDATA[`.
		*
		* ```markdown
		* > | <![CDATA[>&<]]>
		*        ^^^^^^
		* ```
		*
		* @type {State}
		*/
		function cdataOpenInside(code) {
			if (code === "CDATA[".charCodeAt(index++)) {
				effects.consume(code);
				if (index === 6) return self.interrupt ? ok : continuation;
				return cdataOpenInside;
			}
			return nok(code);
		}
		/**
		* After `</`, in closing tag, at tag name.
		*
		* ```markdown
		* > | </x>
		*       ^
		* ```
		*
		* @type {State}
		*/
		function tagCloseStart(code) {
			if (asciiAlpha(code)) {
				effects.consume(code);
				buffer = String.fromCharCode(code);
				return tagName;
			}
			return nok(code);
		}
		/**
		* In tag name.
		*
		* ```markdown
		* > | <ab>
		*      ^^
		* > | </ab>
		*       ^^
		* ```
		*
		* @type {State}
		*/
		function tagName(code) {
			if (code === null || code === 47 || code === 62 || markdownLineEndingOrSpace(code)) {
				const slash = code === 47;
				const name = buffer.toLowerCase();
				if (!slash && !closingTag && htmlRawNames.includes(name)) {
					marker = 1;
					return self.interrupt ? ok(code) : continuation(code);
				}
				if (htmlBlockNames.includes(buffer.toLowerCase())) {
					marker = 6;
					if (slash) {
						effects.consume(code);
						return basicSelfClosing;
					}
					return self.interrupt ? ok(code) : continuation(code);
				}
				marker = 7;
				return self.interrupt && !self.parser.lazy[self.now().line] ? nok(code) : closingTag ? completeClosingTagAfter(code) : completeAttributeNameBefore(code);
			}
			if (code === 45 || asciiAlphanumeric(code)) {
				effects.consume(code);
				buffer += String.fromCharCode(code);
				return tagName;
			}
			return nok(code);
		}
		/**
		* After closing slash of a basic tag name.
		*
		* ```markdown
		* > | <div/>
		*          ^
		* ```
		*
		* @type {State}
		*/
		function basicSelfClosing(code) {
			if (code === 62) {
				effects.consume(code);
				return self.interrupt ? ok : continuation;
			}
			return nok(code);
		}
		/**
		* After closing slash of a complete tag name.
		*
		* ```markdown
		* > | <x/>
		*        ^
		* ```
		*
		* @type {State}
		*/
		function completeClosingTagAfter(code) {
			if (markdownSpace(code)) {
				effects.consume(code);
				return completeClosingTagAfter;
			}
			return completeEnd(code);
		}
		/**
		* At an attribute name.
		*
		* At first, this state is used after a complete tag name, after whitespace,
		* where it expects optional attributes or the end of the tag.
		* It is also reused after attributes, when expecting more optional
		* attributes.
		*
		* ```markdown
		* > | <a />
		*        ^
		* > | <a :b>
		*        ^
		* > | <a _b>
		*        ^
		* > | <a b>
		*        ^
		* > | <a >
		*        ^
		* ```
		*
		* @type {State}
		*/
		function completeAttributeNameBefore(code) {
			if (code === 47) {
				effects.consume(code);
				return completeEnd;
			}
			if (code === 58 || code === 95 || asciiAlpha(code)) {
				effects.consume(code);
				return completeAttributeName;
			}
			if (markdownSpace(code)) {
				effects.consume(code);
				return completeAttributeNameBefore;
			}
			return completeEnd(code);
		}
		/**
		* In attribute name.
		*
		* ```markdown
		* > | <a :b>
		*         ^
		* > | <a _b>
		*         ^
		* > | <a b>
		*         ^
		* ```
		*
		* @type {State}
		*/
		function completeAttributeName(code) {
			if (code === 45 || code === 46 || code === 58 || code === 95 || asciiAlphanumeric(code)) {
				effects.consume(code);
				return completeAttributeName;
			}
			return completeAttributeNameAfter(code);
		}
		/**
		* After attribute name, at an optional initializer, the end of the tag, or
		* whitespace.
		*
		* ```markdown
		* > | <a b>
		*         ^
		* > | <a b=c>
		*         ^
		* ```
		*
		* @type {State}
		*/
		function completeAttributeNameAfter(code) {
			if (code === 61) {
				effects.consume(code);
				return completeAttributeValueBefore;
			}
			if (markdownSpace(code)) {
				effects.consume(code);
				return completeAttributeNameAfter;
			}
			return completeAttributeNameBefore(code);
		}
		/**
		* Before unquoted, double quoted, or single quoted attribute value, allowing
		* whitespace.
		*
		* ```markdown
		* > | <a b=c>
		*          ^
		* > | <a b="c">
		*          ^
		* ```
		*
		* @type {State}
		*/
		function completeAttributeValueBefore(code) {
			if (code === null || code === 60 || code === 61 || code === 62 || code === 96) return nok(code);
			if (code === 34 || code === 39) {
				effects.consume(code);
				markerB = code;
				return completeAttributeValueQuoted;
			}
			if (markdownSpace(code)) {
				effects.consume(code);
				return completeAttributeValueBefore;
			}
			return completeAttributeValueUnquoted(code);
		}
		/**
		* In double or single quoted attribute value.
		*
		* ```markdown
		* > | <a b="c">
		*           ^
		* > | <a b='c'>
		*           ^
		* ```
		*
		* @type {State}
		*/
		function completeAttributeValueQuoted(code) {
			if (code === markerB) {
				effects.consume(code);
				markerB = null;
				return completeAttributeValueQuotedAfter;
			}
			if (code === null || markdownLineEnding(code)) return nok(code);
			effects.consume(code);
			return completeAttributeValueQuoted;
		}
		/**
		* In unquoted attribute value.
		*
		* ```markdown
		* > | <a b=c>
		*          ^
		* ```
		*
		* @type {State}
		*/
		function completeAttributeValueUnquoted(code) {
			if (code === null || code === 34 || code === 39 || code === 47 || code === 60 || code === 61 || code === 62 || code === 96 || markdownLineEndingOrSpace(code)) return completeAttributeNameAfter(code);
			effects.consume(code);
			return completeAttributeValueUnquoted;
		}
		/**
		* After double or single quoted attribute value, before whitespace or the
		* end of the tag.
		*
		* ```markdown
		* > | <a b="c">
		*            ^
		* ```
		*
		* @type {State}
		*/
		function completeAttributeValueQuotedAfter(code) {
			if (code === 47 || code === 62 || markdownSpace(code)) return completeAttributeNameBefore(code);
			return nok(code);
		}
		/**
		* In certain circumstances of a complete tag where only an `>` is allowed.
		*
		* ```markdown
		* > | <a b="c">
		*             ^
		* ```
		*
		* @type {State}
		*/
		function completeEnd(code) {
			if (code === 62) {
				effects.consume(code);
				return completeAfter;
			}
			return nok(code);
		}
		/**
		* After `>` in a complete tag.
		*
		* ```markdown
		* > | <x>
		*        ^
		* ```
		*
		* @type {State}
		*/
		function completeAfter(code) {
			if (code === null || markdownLineEnding(code)) return continuation(code);
			if (markdownSpace(code)) {
				effects.consume(code);
				return completeAfter;
			}
			return nok(code);
		}
		/**
		* In continuation of any HTML kind.
		*
		* ```markdown
		* > | <!--xxx-->
		*          ^
		* ```
		*
		* @type {State}
		*/
		function continuation(code) {
			if (code === 45 && marker === 2) {
				effects.consume(code);
				return continuationCommentInside;
			}
			if (code === 60 && marker === 1) {
				effects.consume(code);
				return continuationRawTagOpen;
			}
			if (code === 62 && marker === 4) {
				effects.consume(code);
				return continuationClose;
			}
			if (code === 63 && marker === 3) {
				effects.consume(code);
				return continuationDeclarationInside;
			}
			if (code === 93 && marker === 5) {
				effects.consume(code);
				return continuationCdataInside;
			}
			if (markdownLineEnding(code) && (marker === 6 || marker === 7)) {
				effects.exit("htmlFlowData");
				return effects.check(blankLineBefore, continuationAfter, continuationStart)(code);
			}
			if (code === null || markdownLineEnding(code)) {
				effects.exit("htmlFlowData");
				return continuationStart(code);
			}
			effects.consume(code);
			return continuation;
		}
		/**
		* In continuation, at eol.
		*
		* ```markdown
		* > | <x>
		*        ^
		*   | asd
		* ```
		*
		* @type {State}
		*/
		function continuationStart(code) {
			return effects.check(nonLazyContinuationStart, continuationStartNonLazy, continuationAfter)(code);
		}
		/**
		* In continuation, at eol, before non-lazy content.
		*
		* ```markdown
		* > | <x>
		*        ^
		*   | asd
		* ```
		*
		* @type {State}
		*/
		function continuationStartNonLazy(code) {
			effects.enter("lineEnding");
			effects.consume(code);
			effects.exit("lineEnding");
			return continuationBefore;
		}
		/**
		* In continuation, before non-lazy content.
		*
		* ```markdown
		*   | <x>
		* > | asd
		*     ^
		* ```
		*
		* @type {State}
		*/
		function continuationBefore(code) {
			if (code === null || markdownLineEnding(code)) return continuationStart(code);
			effects.enter("htmlFlowData");
			return continuation(code);
		}
		/**
		* In comment continuation, after one `-`, expecting another.
		*
		* ```markdown
		* > | <!--xxx-->
		*             ^
		* ```
		*
		* @type {State}
		*/
		function continuationCommentInside(code) {
			if (code === 45) {
				effects.consume(code);
				return continuationDeclarationInside;
			}
			return continuation(code);
		}
		/**
		* In raw continuation, after `<`, at `/`.
		*
		* ```markdown
		* > | <script>console.log(1)<\/script>
		*                            ^
		* ```
		*
		* @type {State}
		*/
		function continuationRawTagOpen(code) {
			if (code === 47) {
				effects.consume(code);
				buffer = "";
				return continuationRawEndTag;
			}
			return continuation(code);
		}
		/**
		* In raw continuation, after `</`, in a raw tag name.
		*
		* ```markdown
		* > | <script>console.log(1)<\/script>
		*                             ^^^^^^
		* ```
		*
		* @type {State}
		*/
		function continuationRawEndTag(code) {
			if (code === 62) {
				const name = buffer.toLowerCase();
				if (htmlRawNames.includes(name)) {
					effects.consume(code);
					return continuationClose;
				}
				return continuation(code);
			}
			if (asciiAlpha(code) && buffer.length < 8) {
				effects.consume(code);
				buffer += String.fromCharCode(code);
				return continuationRawEndTag;
			}
			return continuation(code);
		}
		/**
		* In cdata continuation, after `]`, expecting `]>`.
		*
		* ```markdown
		* > | <![CDATA[>&<]]>
		*                  ^
		* ```
		*
		* @type {State}
		*/
		function continuationCdataInside(code) {
			if (code === 93) {
				effects.consume(code);
				return continuationDeclarationInside;
			}
			return continuation(code);
		}
		/**
		* In declaration or instruction continuation, at `>`.
		*
		* ```markdown
		* > | <!-->
		*         ^
		* > | <?>
		*       ^
		* > | <!q>
		*        ^
		* > | <!--ab-->
		*             ^
		* > | <![CDATA[>&<]]>
		*                   ^
		* ```
		*
		* @type {State}
		*/
		function continuationDeclarationInside(code) {
			if (code === 62) {
				effects.consume(code);
				return continuationClose;
			}
			if (code === 45 && marker === 2) {
				effects.consume(code);
				return continuationDeclarationInside;
			}
			return continuation(code);
		}
		/**
		* In closed continuation: everything we get until the eol/eof is part of it.
		*
		* ```markdown
		* > | <!doctype>
		*               ^
		* ```
		*
		* @type {State}
		*/
		function continuationClose(code) {
			if (code === null || markdownLineEnding(code)) {
				effects.exit("htmlFlowData");
				return continuationAfter(code);
			}
			effects.consume(code);
			return continuationClose;
		}
		/**
		* Done.
		*
		* ```markdown
		* > | <!doctype>
		*               ^
		* ```
		*
		* @type {State}
		*/
		function continuationAfter(code) {
			effects.exit("htmlFlow");
			return ok(code);
		}
	}
	/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeNonLazyContinuationStart(effects, ok, nok) {
		const self = this;
		return start;
		/**
		* At eol, before continuation.
		*
		* ```markdown
		* > | * ```js
		*            ^
		*   | b
		* ```
		*
		* @type {State}
		*/
		function start(code) {
			if (markdownLineEnding(code)) {
				effects.enter("lineEnding");
				effects.consume(code);
				effects.exit("lineEnding");
				return after;
			}
			return nok(code);
		}
		/**
		* A continuation.
		*
		* ```markdown
		*   | * ```js
		* > | b
		*     ^
		* ```
		*
		* @type {State}
		*/
		function after(code) {
			return self.parser.lazy[self.now().line] ? nok(code) : ok(code);
		}
	}
	/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeBlankLineBefore(effects, ok, nok) {
		return start;
		/**
		* Before eol, expecting blank line.
		*
		* ```markdown
		* > | <div>
		*          ^
		*   |
		* ```
		*
		* @type {State}
		*/
		function start(code) {
			effects.enter("lineEnding");
			effects.consume(code);
			effects.exit("lineEnding");
			return effects.attempt(blankLine, ok, nok);
		}
	}
	var htmlFlow, blankLineBefore, nonLazyContinuationStart;
	var init_html_flow = __esmMin((() => {
		init_micromark_util_character();
		init_micromark_util_html_tag_name();
		init_blank_line();
		htmlFlow = {
			name: "htmlFlow",
			tokenize: tokenizeHtmlFlow,
			resolveTo: resolveToHtmlFlow,
			concrete: true
		};
		blankLineBefore = {
			tokenize: tokenizeBlankLineBefore,
			partial: true
		};
		nonLazyContinuationStart = {
			tokenize: tokenizeNonLazyContinuationStart,
			partial: true
		};
	}));

//#endregion
//#region node_modules/micromark-core-commonmark/lib/html-text.js
/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeHtmlText(effects, ok, nok) {
		const self = this;
		/** @type {NonNullable<Code> | undefined} */
		let marker;
		/** @type {number} */
		let index;
		/** @type {State} */
		let returnState;
		return start;
		/**
		* Start of HTML (text).
		*
		* ```markdown
		* > | a <b> c
		*       ^
		* ```
		*
		* @type {State}
		*/
		function start(code) {
			effects.enter("htmlText");
			effects.enter("htmlTextData");
			effects.consume(code);
			return open;
		}
		/**
		* After `<`, at tag name or other stuff.
		*
		* ```markdown
		* > | a <b> c
		*        ^
		* > | a <!doctype> c
		*        ^
		* > | a <!--b--> c
		*        ^
		* ```
		*
		* @type {State}
		*/
		function open(code) {
			if (code === 33) {
				effects.consume(code);
				return declarationOpen;
			}
			if (code === 47) {
				effects.consume(code);
				return tagCloseStart;
			}
			if (code === 63) {
				effects.consume(code);
				return instruction;
			}
			if (asciiAlpha(code)) {
				effects.consume(code);
				return tagOpen;
			}
			return nok(code);
		}
		/**
		* After `<!`, at declaration, comment, or CDATA.
		*
		* ```markdown
		* > | a <!doctype> c
		*         ^
		* > | a <!--b--> c
		*         ^
		* > | a <![CDATA[>&<]]> c
		*         ^
		* ```
		*
		* @type {State}
		*/
		function declarationOpen(code) {
			if (code === 45) {
				effects.consume(code);
				return commentOpenInside;
			}
			if (code === 91) {
				effects.consume(code);
				index = 0;
				return cdataOpenInside;
			}
			if (asciiAlpha(code)) {
				effects.consume(code);
				return declaration;
			}
			return nok(code);
		}
		/**
		* In a comment, after `<!-`, at another `-`.
		*
		* ```markdown
		* > | a <!--b--> c
		*          ^
		* ```
		*
		* @type {State}
		*/
		function commentOpenInside(code) {
			if (code === 45) {
				effects.consume(code);
				return commentEnd;
			}
			return nok(code);
		}
		/**
		* In comment.
		*
		* ```markdown
		* > | a <!--b--> c
		*           ^
		* ```
		*
		* @type {State}
		*/
		function comment(code) {
			if (code === null) return nok(code);
			if (code === 45) {
				effects.consume(code);
				return commentClose;
			}
			if (markdownLineEnding(code)) {
				returnState = comment;
				return lineEndingBefore(code);
			}
			effects.consume(code);
			return comment;
		}
		/**
		* In comment, after `-`.
		*
		* ```markdown
		* > | a <!--b--> c
		*             ^
		* ```
		*
		* @type {State}
		*/
		function commentClose(code) {
			if (code === 45) {
				effects.consume(code);
				return commentEnd;
			}
			return comment(code);
		}
		/**
		* In comment, after `--`.
		*
		* ```markdown
		* > | a <!--b--> c
		*              ^
		* ```
		*
		* @type {State}
		*/
		function commentEnd(code) {
			return code === 62 ? end(code) : code === 45 ? commentClose(code) : comment(code);
		}
		/**
		* After `<![`, in CDATA, expecting `CDATA[`.
		*
		* ```markdown
		* > | a <![CDATA[>&<]]> b
		*          ^^^^^^
		* ```
		*
		* @type {State}
		*/
		function cdataOpenInside(code) {
			if (code === "CDATA[".charCodeAt(index++)) {
				effects.consume(code);
				return index === 6 ? cdata : cdataOpenInside;
			}
			return nok(code);
		}
		/**
		* In CDATA.
		*
		* ```markdown
		* > | a <![CDATA[>&<]]> b
		*                ^^^
		* ```
		*
		* @type {State}
		*/
		function cdata(code) {
			if (code === null) return nok(code);
			if (code === 93) {
				effects.consume(code);
				return cdataClose;
			}
			if (markdownLineEnding(code)) {
				returnState = cdata;
				return lineEndingBefore(code);
			}
			effects.consume(code);
			return cdata;
		}
		/**
		* In CDATA, after `]`, at another `]`.
		*
		* ```markdown
		* > | a <![CDATA[>&<]]> b
		*                    ^
		* ```
		*
		* @type {State}
		*/
		function cdataClose(code) {
			if (code === 93) {
				effects.consume(code);
				return cdataEnd;
			}
			return cdata(code);
		}
		/**
		* In CDATA, after `]]`, at `>`.
		*
		* ```markdown
		* > | a <![CDATA[>&<]]> b
		*                     ^
		* ```
		*
		* @type {State}
		*/
		function cdataEnd(code) {
			if (code === 62) return end(code);
			if (code === 93) {
				effects.consume(code);
				return cdataEnd;
			}
			return cdata(code);
		}
		/**
		* In declaration.
		*
		* ```markdown
		* > | a <!b> c
		*          ^
		* ```
		*
		* @type {State}
		*/
		function declaration(code) {
			if (code === null || code === 62) return end(code);
			if (markdownLineEnding(code)) {
				returnState = declaration;
				return lineEndingBefore(code);
			}
			effects.consume(code);
			return declaration;
		}
		/**
		* In instruction.
		*
		* ```markdown
		* > | a <?b?> c
		*         ^
		* ```
		*
		* @type {State}
		*/
		function instruction(code) {
			if (code === null) return nok(code);
			if (code === 63) {
				effects.consume(code);
				return instructionClose;
			}
			if (markdownLineEnding(code)) {
				returnState = instruction;
				return lineEndingBefore(code);
			}
			effects.consume(code);
			return instruction;
		}
		/**
		* In instruction, after `?`, at `>`.
		*
		* ```markdown
		* > | a <?b?> c
		*           ^
		* ```
		*
		* @type {State}
		*/
		function instructionClose(code) {
			return code === 62 ? end(code) : instruction(code);
		}
		/**
		* After `</`, in closing tag, at tag name.
		*
		* ```markdown
		* > | a </b> c
		*         ^
		* ```
		*
		* @type {State}
		*/
		function tagCloseStart(code) {
			if (asciiAlpha(code)) {
				effects.consume(code);
				return tagClose;
			}
			return nok(code);
		}
		/**
		* After `</x`, in a tag name.
		*
		* ```markdown
		* > | a </b> c
		*          ^
		* ```
		*
		* @type {State}
		*/
		function tagClose(code) {
			if (code === 45 || asciiAlphanumeric(code)) {
				effects.consume(code);
				return tagClose;
			}
			return tagCloseBetween(code);
		}
		/**
		* In closing tag, after tag name.
		*
		* ```markdown
		* > | a </b> c
		*          ^
		* ```
		*
		* @type {State}
		*/
		function tagCloseBetween(code) {
			if (markdownLineEnding(code)) {
				returnState = tagCloseBetween;
				return lineEndingBefore(code);
			}
			if (markdownSpace(code)) {
				effects.consume(code);
				return tagCloseBetween;
			}
			return end(code);
		}
		/**
		* After `<x`, in opening tag name.
		*
		* ```markdown
		* > | a <b> c
		*         ^
		* ```
		*
		* @type {State}
		*/
		function tagOpen(code) {
			if (code === 45 || asciiAlphanumeric(code)) {
				effects.consume(code);
				return tagOpen;
			}
			if (code === 47 || code === 62 || markdownLineEndingOrSpace(code)) return tagOpenBetween(code);
			return nok(code);
		}
		/**
		* In opening tag, after tag name.
		*
		* ```markdown
		* > | a <b> c
		*         ^
		* ```
		*
		* @type {State}
		*/
		function tagOpenBetween(code) {
			if (code === 47) {
				effects.consume(code);
				return end;
			}
			if (code === 58 || code === 95 || asciiAlpha(code)) {
				effects.consume(code);
				return tagOpenAttributeName;
			}
			if (markdownLineEnding(code)) {
				returnState = tagOpenBetween;
				return lineEndingBefore(code);
			}
			if (markdownSpace(code)) {
				effects.consume(code);
				return tagOpenBetween;
			}
			return end(code);
		}
		/**
		* In attribute name.
		*
		* ```markdown
		* > | a <b c> d
		*          ^
		* ```
		*
		* @type {State}
		*/
		function tagOpenAttributeName(code) {
			if (code === 45 || code === 46 || code === 58 || code === 95 || asciiAlphanumeric(code)) {
				effects.consume(code);
				return tagOpenAttributeName;
			}
			return tagOpenAttributeNameAfter(code);
		}
		/**
		* After attribute name, before initializer, the end of the tag, or
		* whitespace.
		*
		* ```markdown
		* > | a <b c> d
		*           ^
		* ```
		*
		* @type {State}
		*/
		function tagOpenAttributeNameAfter(code) {
			if (code === 61) {
				effects.consume(code);
				return tagOpenAttributeValueBefore;
			}
			if (markdownLineEnding(code)) {
				returnState = tagOpenAttributeNameAfter;
				return lineEndingBefore(code);
			}
			if (markdownSpace(code)) {
				effects.consume(code);
				return tagOpenAttributeNameAfter;
			}
			return tagOpenBetween(code);
		}
		/**
		* Before unquoted, double quoted, or single quoted attribute value, allowing
		* whitespace.
		*
		* ```markdown
		* > | a <b c=d> e
		*            ^
		* ```
		*
		* @type {State}
		*/
		function tagOpenAttributeValueBefore(code) {
			if (code === null || code === 60 || code === 61 || code === 62 || code === 96) return nok(code);
			if (code === 34 || code === 39) {
				effects.consume(code);
				marker = code;
				return tagOpenAttributeValueQuoted;
			}
			if (markdownLineEnding(code)) {
				returnState = tagOpenAttributeValueBefore;
				return lineEndingBefore(code);
			}
			if (markdownSpace(code)) {
				effects.consume(code);
				return tagOpenAttributeValueBefore;
			}
			effects.consume(code);
			return tagOpenAttributeValueUnquoted;
		}
		/**
		* In double or single quoted attribute value.
		*
		* ```markdown
		* > | a <b c="d"> e
		*             ^
		* ```
		*
		* @type {State}
		*/
		function tagOpenAttributeValueQuoted(code) {
			if (code === marker) {
				effects.consume(code);
				marker = void 0;
				return tagOpenAttributeValueQuotedAfter;
			}
			if (code === null) return nok(code);
			if (markdownLineEnding(code)) {
				returnState = tagOpenAttributeValueQuoted;
				return lineEndingBefore(code);
			}
			effects.consume(code);
			return tagOpenAttributeValueQuoted;
		}
		/**
		* In unquoted attribute value.
		*
		* ```markdown
		* > | a <b c=d> e
		*            ^
		* ```
		*
		* @type {State}
		*/
		function tagOpenAttributeValueUnquoted(code) {
			if (code === null || code === 34 || code === 39 || code === 60 || code === 61 || code === 96) return nok(code);
			if (code === 47 || code === 62 || markdownLineEndingOrSpace(code)) return tagOpenBetween(code);
			effects.consume(code);
			return tagOpenAttributeValueUnquoted;
		}
		/**
		* After double or single quoted attribute value, before whitespace or the end
		* of the tag.
		*
		* ```markdown
		* > | a <b c="d"> e
		*               ^
		* ```
		*
		* @type {State}
		*/
		function tagOpenAttributeValueQuotedAfter(code) {
			if (code === 47 || code === 62 || markdownLineEndingOrSpace(code)) return tagOpenBetween(code);
			return nok(code);
		}
		/**
		* In certain circumstances of a tag where only an `>` is allowed.
		*
		* ```markdown
		* > | a <b c="d"> e
		*               ^
		* ```
		*
		* @type {State}
		*/
		function end(code) {
			if (code === 62) {
				effects.consume(code);
				effects.exit("htmlTextData");
				effects.exit("htmlText");
				return ok;
			}
			return nok(code);
		}
		/**
		* At eol.
		*
		* > 👉 **Note**: we can’t have blank lines in text, so no need to worry about
		* > empty tokens.
		*
		* ```markdown
		* > | a <!--a
		*            ^
		*   | b-->
		* ```
		*
		* @type {State}
		*/
		function lineEndingBefore(code) {
			effects.exit("htmlTextData");
			effects.enter("lineEnding");
			effects.consume(code);
			effects.exit("lineEnding");
			return lineEndingAfter;
		}
		/**
		* After eol, at optional whitespace.
		*
		* > 👉 **Note**: we can’t have blank lines in text, so no need to worry about
		* > empty tokens.
		*
		* ```markdown
		*   | a <!--a
		* > | b-->
		*     ^
		* ```
		*
		* @type {State}
		*/
		function lineEndingAfter(code) {
			return markdownSpace(code) ? factorySpace(effects, lineEndingAfterPrefix, "linePrefix", self.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4)(code) : lineEndingAfterPrefix(code);
		}
		/**
		* After eol, after optional whitespace.
		*
		* > 👉 **Note**: we can’t have blank lines in text, so no need to worry about
		* > empty tokens.
		*
		* ```markdown
		*   | a <!--a
		* > | b-->
		*     ^
		* ```
		*
		* @type {State}
		*/
		function lineEndingAfterPrefix(code) {
			effects.enter("htmlTextData");
			return returnState(code);
		}
	}
	var htmlText;
	var init_html_text = __esmMin((() => {
		init_micromark_factory_space();
		init_micromark_util_character();
		htmlText = {
			name: "htmlText",
			tokenize: tokenizeHtmlText
		};
	}));

//#endregion
//#region node_modules/micromark-core-commonmark/lib/label-end.js
/** @type {Resolver} */
	function resolveAllLabelEnd(events) {
		let index = -1;
		while (++index < events.length) {
			const token = events[index][1];
			if (token.type === "labelImage" || token.type === "labelLink" || token.type === "labelEnd") {
				events.splice(index + 1, token.type === "labelImage" ? 4 : 2);
				token.type = "data";
				index++;
			}
		}
		return events;
	}
	/** @type {Resolver} */
	function resolveToLabelEnd(events, context) {
		let index = events.length;
		let offset = 0;
		/** @type {Token} */
		let token;
		/** @type {number | undefined} */
		let open;
		/** @type {number | undefined} */
		let close;
		/** @type {Array<Event>} */
		let media;
		while (index--) {
			token = events[index][1];
			if (open) {
				if (token.type === "link" || token.type === "labelLink" && token._inactive) break;
				if (events[index][0] === "enter" && token.type === "labelLink") token._inactive = true;
			} else if (close) {
				if (events[index][0] === "enter" && (token.type === "labelImage" || token.type === "labelLink") && !token._balanced) {
					open = index;
					if (token.type !== "labelLink") {
						offset = 2;
						break;
					}
				}
			} else if (token.type === "labelEnd") close = index;
		}
		const group = {
			type: events[open][1].type === "labelLink" ? "link" : "image",
			start: Object.assign({}, events[open][1].start),
			end: Object.assign({}, events[events.length - 1][1].end)
		};
		const label = {
			type: "label",
			start: Object.assign({}, events[open][1].start),
			end: Object.assign({}, events[close][1].end)
		};
		const text = {
			type: "labelText",
			start: Object.assign({}, events[open + offset + 2][1].end),
			end: Object.assign({}, events[close - 2][1].start)
		};
		media = [[
			"enter",
			group,
			context
		], [
			"enter",
			label,
			context
		]];
		media = push(media, events.slice(open + 1, open + offset + 3));
		media = push(media, [[
			"enter",
			text,
			context
		]]);
		media = push(media, resolveAll(context.parser.constructs.insideSpan.null, events.slice(open + offset + 4, close - 3), context));
		media = push(media, [
			[
				"exit",
				text,
				context
			],
			events[close - 2],
			events[close - 1],
			[
				"exit",
				label,
				context
			]
		]);
		media = push(media, events.slice(close + 1));
		media = push(media, [[
			"exit",
			group,
			context
		]]);
		splice(events, open, events.length, media);
		return events;
	}
	/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeLabelEnd(effects, ok, nok) {
		const self = this;
		let index = self.events.length;
		/** @type {Token} */
		let labelStart;
		/** @type {boolean} */
		let defined;
		while (index--) if ((self.events[index][1].type === "labelImage" || self.events[index][1].type === "labelLink") && !self.events[index][1]._balanced) {
			labelStart = self.events[index][1];
			break;
		}
		return start;
		/**
		* Start of label end.
		*
		* ```markdown
		* > | [a](b) c
		*       ^
		* > | [a][b] c
		*       ^
		* > | [a][] b
		*       ^
		* > | [a] b
		* ```
		*
		* @type {State}
		*/
		function start(code) {
			if (!labelStart) return nok(code);
			if (labelStart._inactive) return labelEndNok(code);
			defined = self.parser.defined.includes(normalizeIdentifier(self.sliceSerialize({
				start: labelStart.end,
				end: self.now()
			})));
			effects.enter("labelEnd");
			effects.enter("labelMarker");
			effects.consume(code);
			effects.exit("labelMarker");
			effects.exit("labelEnd");
			return after;
		}
		/**
		* After `]`.
		*
		* ```markdown
		* > | [a](b) c
		*       ^
		* > | [a][b] c
		*       ^
		* > | [a][] b
		*       ^
		* > | [a] b
		*       ^
		* ```
		*
		* @type {State}
		*/
		function after(code) {
			if (code === 40) return effects.attempt(resourceConstruct, labelEndOk, defined ? labelEndOk : labelEndNok)(code);
			if (code === 91) return effects.attempt(referenceFullConstruct, labelEndOk, defined ? referenceNotFull : labelEndNok)(code);
			return defined ? labelEndOk(code) : labelEndNok(code);
		}
		/**
		* After `]`, at `[`, but not at a full reference.
		*
		* > 👉 **Note**: we only get here if the label is defined.
		*
		* ```markdown
		* > | [a][] b
		*        ^
		* > | [a] b
		*        ^
		* ```
		*
		* @type {State}
		*/
		function referenceNotFull(code) {
			return effects.attempt(referenceCollapsedConstruct, labelEndOk, labelEndNok)(code);
		}
		/**
		* Done, we found something.
		*
		* ```markdown
		* > | [a](b) c
		*           ^
		* > | [a][b] c
		*           ^
		* > | [a][] b
		*          ^
		* > | [a] b
		*        ^
		* ```
		*
		* @type {State}
		*/
		function labelEndOk(code) {
			return ok(code);
		}
		/**
		* Done, it’s nothing.
		*
		* There was an okay opening, but we didn’t match anything.
		*
		* ```markdown
		* > | [a](b c
		*        ^
		* > | [a][b c
		*        ^
		* > | [a] b
		*        ^
		* ```
		*
		* @type {State}
		*/
		function labelEndNok(code) {
			labelStart._balanced = true;
			return nok(code);
		}
	}
	/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeResource(effects, ok, nok) {
		return resourceStart;
		/**
		* At a resource.
		*
		* ```markdown
		* > | [a](b) c
		*        ^
		* ```
		*
		* @type {State}
		*/
		function resourceStart(code) {
			effects.enter("resource");
			effects.enter("resourceMarker");
			effects.consume(code);
			effects.exit("resourceMarker");
			return resourceBefore;
		}
		/**
		* In resource, after `(`, at optional whitespace.
		*
		* ```markdown
		* > | [a](b) c
		*         ^
		* ```
		*
		* @type {State}
		*/
		function resourceBefore(code) {
			return markdownLineEndingOrSpace(code) ? factoryWhitespace(effects, resourceOpen)(code) : resourceOpen(code);
		}
		/**
		* In resource, after optional whitespace, at `)` or a destination.
		*
		* ```markdown
		* > | [a](b) c
		*         ^
		* ```
		*
		* @type {State}
		*/
		function resourceOpen(code) {
			if (code === 41) return resourceEnd(code);
			return factoryDestination(effects, resourceDestinationAfter, resourceDestinationMissing, "resourceDestination", "resourceDestinationLiteral", "resourceDestinationLiteralMarker", "resourceDestinationRaw", "resourceDestinationString", 32)(code);
		}
		/**
		* In resource, after destination, at optional whitespace.
		*
		* ```markdown
		* > | [a](b) c
		*          ^
		* ```
		*
		* @type {State}
		*/
		function resourceDestinationAfter(code) {
			return markdownLineEndingOrSpace(code) ? factoryWhitespace(effects, resourceBetween)(code) : resourceEnd(code);
		}
		/**
		* At invalid destination.
		*
		* ```markdown
		* > | [a](<<) b
		*         ^
		* ```
		*
		* @type {State}
		*/
		function resourceDestinationMissing(code) {
			return nok(code);
		}
		/**
		* In resource, after destination and whitespace, at `(` or title.
		*
		* ```markdown
		* > | [a](b ) c
		*           ^
		* ```
		*
		* @type {State}
		*/
		function resourceBetween(code) {
			if (code === 34 || code === 39 || code === 40) return factoryTitle(effects, resourceTitleAfter, nok, "resourceTitle", "resourceTitleMarker", "resourceTitleString")(code);
			return resourceEnd(code);
		}
		/**
		* In resource, after title, at optional whitespace.
		*
		* ```markdown
		* > | [a](b "c") d
		*              ^
		* ```
		*
		* @type {State}
		*/
		function resourceTitleAfter(code) {
			return markdownLineEndingOrSpace(code) ? factoryWhitespace(effects, resourceEnd)(code) : resourceEnd(code);
		}
		/**
		* In resource, at `)`.
		*
		* ```markdown
		* > | [a](b) d
		*          ^
		* ```
		*
		* @type {State}
		*/
		function resourceEnd(code) {
			if (code === 41) {
				effects.enter("resourceMarker");
				effects.consume(code);
				effects.exit("resourceMarker");
				effects.exit("resource");
				return ok;
			}
			return nok(code);
		}
	}
	/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeReferenceFull(effects, ok, nok) {
		const self = this;
		return referenceFull;
		/**
		* In a reference (full), at the `[`.
		*
		* ```markdown
		* > | [a][b] d
		*        ^
		* ```
		*
		* @type {State}
		*/
		function referenceFull(code) {
			return factoryLabel.call(self, effects, referenceFullAfter, referenceFullMissing, "reference", "referenceMarker", "referenceString")(code);
		}
		/**
		* In a reference (full), after `]`.
		*
		* ```markdown
		* > | [a][b] d
		*          ^
		* ```
		*
		* @type {State}
		*/
		function referenceFullAfter(code) {
			return self.parser.defined.includes(normalizeIdentifier(self.sliceSerialize(self.events[self.events.length - 1][1]).slice(1, -1))) ? ok(code) : nok(code);
		}
		/**
		* In reference (full) that was missing.
		*
		* ```markdown
		* > | [a][b d
		*        ^
		* ```
		*
		* @type {State}
		*/
		function referenceFullMissing(code) {
			return nok(code);
		}
	}
	/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeReferenceCollapsed(effects, ok, nok) {
		return referenceCollapsedStart;
		/**
		* In reference (collapsed), at `[`.
		*
		* > 👉 **Note**: we only get here if the label is defined.
		*
		* ```markdown
		* > | [a][] d
		*        ^
		* ```
		*
		* @type {State}
		*/
		function referenceCollapsedStart(code) {
			effects.enter("reference");
			effects.enter("referenceMarker");
			effects.consume(code);
			effects.exit("referenceMarker");
			return referenceCollapsedOpen;
		}
		/**
		* In reference (collapsed), at `]`.
		*
		* > 👉 **Note**: we only get here if the label is defined.
		*
		* ```markdown
		* > | [a][] d
		*         ^
		* ```
		*
		*  @type {State}
		*/
		function referenceCollapsedOpen(code) {
			if (code === 93) {
				effects.enter("referenceMarker");
				effects.consume(code);
				effects.exit("referenceMarker");
				effects.exit("reference");
				return ok;
			}
			return nok(code);
		}
	}
	var labelEnd, resourceConstruct, referenceFullConstruct, referenceCollapsedConstruct;
	var init_label_end = __esmMin((() => {
		init_micromark_factory_destination();
		init_micromark_factory_label();
		init_micromark_factory_title();
		init_micromark_factory_whitespace();
		init_micromark_util_character();
		init_micromark_util_chunked();
		init_micromark_util_normalize_identifier();
		init_micromark_util_resolve_all();
		labelEnd = {
			name: "labelEnd",
			tokenize: tokenizeLabelEnd,
			resolveTo: resolveToLabelEnd,
			resolveAll: resolveAllLabelEnd
		};
		resourceConstruct = { tokenize: tokenizeResource };
		referenceFullConstruct = { tokenize: tokenizeReferenceFull };
		referenceCollapsedConstruct = { tokenize: tokenizeReferenceCollapsed };
	}));

//#endregion
//#region node_modules/micromark-core-commonmark/lib/label-start-image.js
/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeLabelStartImage(effects, ok, nok) {
		const self = this;
		return start;
		/**
		* Start of label (image) start.
		*
		* ```markdown
		* > | a ![b] c
		*       ^
		* ```
		*
		* @type {State}
		*/
		function start(code) {
			effects.enter("labelImage");
			effects.enter("labelImageMarker");
			effects.consume(code);
			effects.exit("labelImageMarker");
			return open;
		}
		/**
		* After `!`, at `[`.
		*
		* ```markdown
		* > | a ![b] c
		*        ^
		* ```
		*
		* @type {State}
		*/
		function open(code) {
			if (code === 91) {
				effects.enter("labelMarker");
				effects.consume(code);
				effects.exit("labelMarker");
				effects.exit("labelImage");
				return after;
			}
			return nok(code);
		}
		/**
		* After `![`.
		*
		* ```markdown
		* > | a ![b] c
		*         ^
		* ```
		*
		* This is needed in because, when GFM footnotes are enabled, images never
		* form when started with a `^`.
		* Instead, links form:
		*
		* ```markdown
		* ![^a](b)
		*
		* ![^a][b]
		*
		* [b]: c
		* ```
		*
		* ```html
		* <p>!<a href=\"b\">^a</a></p>
		* <p>!<a href=\"c\">^a</a></p>
		* ```
		*
		* @type {State}
		*/
		function after(code) {
			/* c8 ignore next 3 */
			return code === 94 && "_hiddenFootnoteSupport" in self.parser.constructs ? nok(code) : ok(code);
		}
	}
	var labelStartImage;
	var init_label_start_image = __esmMin((() => {
		init_label_end();
		labelStartImage = {
			name: "labelStartImage",
			tokenize: tokenizeLabelStartImage,
			resolveAll: labelEnd.resolveAll
		};
	}));

//#endregion
//#region node_modules/micromark-core-commonmark/lib/label-start-link.js
/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeLabelStartLink(effects, ok, nok) {
		const self = this;
		return start;
		/**
		* Start of label (link) start.
		*
		* ```markdown
		* > | a [b] c
		*       ^
		* ```
		*
		* @type {State}
		*/
		function start(code) {
			effects.enter("labelLink");
			effects.enter("labelMarker");
			effects.consume(code);
			effects.exit("labelMarker");
			effects.exit("labelLink");
			return after;
		}
		/** @type {State} */
		function after(code) {
			/* c8 ignore next 3 */
			return code === 94 && "_hiddenFootnoteSupport" in self.parser.constructs ? nok(code) : ok(code);
		}
	}
	var labelStartLink;
	var init_label_start_link = __esmMin((() => {
		init_label_end();
		labelStartLink = {
			name: "labelStartLink",
			tokenize: tokenizeLabelStartLink,
			resolveAll: labelEnd.resolveAll
		};
	}));

//#endregion
//#region node_modules/micromark-core-commonmark/lib/line-ending.js
/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeLineEnding(effects, ok) {
		return start;
		/** @type {State} */
		function start(code) {
			effects.enter("lineEnding");
			effects.consume(code);
			effects.exit("lineEnding");
			return factorySpace(effects, ok, "linePrefix");
		}
	}
	var lineEnding;
	var init_line_ending = __esmMin((() => {
		init_micromark_factory_space();
		lineEnding = {
			name: "lineEnding",
			tokenize: tokenizeLineEnding
		};
	}));

//#endregion
//#region node_modules/micromark-core-commonmark/lib/thematic-break.js
/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeThematicBreak(effects, ok, nok) {
		let size = 0;
		/** @type {NonNullable<Code>} */
		let marker;
		return start;
		/**
		* Start of thematic break.
		*
		* ```markdown
		* > | ***
		*     ^
		* ```
		*
		* @type {State}
		*/
		function start(code) {
			effects.enter("thematicBreak");
			return before(code);
		}
		/**
		* After optional whitespace, at marker.
		*
		* ```markdown
		* > | ***
		*     ^
		* ```
		*
		* @type {State}
		*/
		function before(code) {
			marker = code;
			return atBreak(code);
		}
		/**
		* After something, before something else.
		*
		* ```markdown
		* > | ***
		*     ^
		* ```
		*
		* @type {State}
		*/
		function atBreak(code) {
			if (code === marker) {
				effects.enter("thematicBreakSequence");
				return sequence(code);
			}
			if (size >= 3 && (code === null || markdownLineEnding(code))) {
				effects.exit("thematicBreak");
				return ok(code);
			}
			return nok(code);
		}
		/**
		* In sequence.
		*
		* ```markdown
		* > | ***
		*     ^
		* ```
		*
		* @type {State}
		*/
		function sequence(code) {
			if (code === marker) {
				effects.consume(code);
				size++;
				return sequence;
			}
			effects.exit("thematicBreakSequence");
			return markdownSpace(code) ? factorySpace(effects, atBreak, "whitespace")(code) : atBreak(code);
		}
	}
	var thematicBreak$1;
	var init_thematic_break$1 = __esmMin((() => {
		init_micromark_factory_space();
		init_micromark_util_character();
		thematicBreak$1 = {
			name: "thematicBreak",
			tokenize: tokenizeThematicBreak
		};
	}));

//#endregion
//#region node_modules/micromark-core-commonmark/lib/list.js
/**
	* @type {Tokenizer}
	* @this {TokenizeContext}
	*/
	function tokenizeListStart(effects, ok, nok) {
		const self = this;
		const tail = self.events[self.events.length - 1];
		let initialSize = tail && tail[1].type === "linePrefix" ? tail[2].sliceSerialize(tail[1], true).length : 0;
		let size = 0;
		return start;
		/** @type {State} */
		function start(code) {
			const kind = self.containerState.type || (code === 42 || code === 43 || code === 45 ? "listUnordered" : "listOrdered");
			if (kind === "listUnordered" ? !self.containerState.marker || code === self.containerState.marker : asciiDigit(code)) {
				if (!self.containerState.type) {
					self.containerState.type = kind;
					effects.enter(kind, { _container: true });
				}
				if (kind === "listUnordered") {
					effects.enter("listItemPrefix");
					return code === 42 || code === 45 ? effects.check(thematicBreak$1, nok, atMarker)(code) : atMarker(code);
				}
				if (!self.interrupt || code === 49) {
					effects.enter("listItemPrefix");
					effects.enter("listItemValue");
					return inside(code);
				}
			}
			return nok(code);
		}
		/** @type {State} */
		function inside(code) {
			if (asciiDigit(code) && ++size < 10) {
				effects.consume(code);
				return inside;
			}
			if ((!self.interrupt || size < 2) && (self.containerState.marker ? code === self.containerState.marker : code === 41 || code === 46)) {
				effects.exit("listItemValue");
				return atMarker(code);
			}
			return nok(code);
		}
		/**
		* @type {State}
		**/
		function atMarker(code) {
			effects.enter("listItemMarker");
			effects.consume(code);
			effects.exit("listItemMarker");
			self.containerState.marker = self.containerState.marker || code;
			return effects.check(blankLine, self.interrupt ? nok : onBlank, effects.attempt(listItemPrefixWhitespaceConstruct, endOfPrefix, otherPrefix));
		}
		/** @type {State} */
		function onBlank(code) {
			self.containerState.initialBlankLine = true;
			initialSize++;
			return endOfPrefix(code);
		}
		/** @type {State} */
		function otherPrefix(code) {
			if (markdownSpace(code)) {
				effects.enter("listItemPrefixWhitespace");
				effects.consume(code);
				effects.exit("listItemPrefixWhitespace");
				return endOfPrefix;
			}
			return nok(code);
		}
		/** @type {State} */
		function endOfPrefix(code) {
			self.containerState.size = initialSize + self.sliceSerialize(effects.exit("listItemPrefix"), true).length;
			return ok(code);
		}
	}
	/**
	* @type {Tokenizer}
	* @this {TokenizeContext}
	*/
	function tokenizeListContinuation(effects, ok, nok) {
		const self = this;
		self.containerState._closeFlow = void 0;
		return effects.check(blankLine, onBlank, notBlank);
		/** @type {State} */
		function onBlank(code) {
			self.containerState.furtherBlankLines = self.containerState.furtherBlankLines || self.containerState.initialBlankLine;
			return factorySpace(effects, ok, "listItemIndent", self.containerState.size + 1)(code);
		}
		/** @type {State} */
		function notBlank(code) {
			if (self.containerState.furtherBlankLines || !markdownSpace(code)) {
				self.containerState.furtherBlankLines = void 0;
				self.containerState.initialBlankLine = void 0;
				return notInCurrentItem(code);
			}
			self.containerState.furtherBlankLines = void 0;
			self.containerState.initialBlankLine = void 0;
			return effects.attempt(indentConstruct, ok, notInCurrentItem)(code);
		}
		/** @type {State} */
		function notInCurrentItem(code) {
			self.containerState._closeFlow = true;
			self.interrupt = void 0;
			return factorySpace(effects, effects.attempt(list$1, ok, nok), "linePrefix", self.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4)(code);
		}
	}
	/**
	* @type {Tokenizer}
	* @this {TokenizeContext}
	*/
	function tokenizeIndent(effects, ok, nok) {
		const self = this;
		return factorySpace(effects, afterPrefix, "listItemIndent", self.containerState.size + 1);
		/** @type {State} */
		function afterPrefix(code) {
			const tail = self.events[self.events.length - 1];
			return tail && tail[1].type === "listItemIndent" && tail[2].sliceSerialize(tail[1], true).length === self.containerState.size ? ok(code) : nok(code);
		}
	}
	/**
	* @type {Exiter}
	* @this {TokenizeContext}
	*/
	function tokenizeListEnd(effects) {
		effects.exit(this.containerState.type);
	}
	/**
	* @type {Tokenizer}
	* @this {TokenizeContext}
	*/
	function tokenizeListItemPrefixWhitespace(effects, ok, nok) {
		const self = this;
		return factorySpace(effects, afterPrefix, "listItemPrefixWhitespace", self.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 5);
		/** @type {State} */
		function afterPrefix(code) {
			const tail = self.events[self.events.length - 1];
			return !markdownSpace(code) && tail && tail[1].type === "listItemPrefixWhitespace" ? ok(code) : nok(code);
		}
	}
	var list$1, listItemPrefixWhitespaceConstruct, indentConstruct;
	var init_list$1 = __esmMin((() => {
		init_micromark_factory_space();
		init_micromark_util_character();
		init_blank_line();
		init_thematic_break$1();
		list$1 = {
			name: "list",
			tokenize: tokenizeListStart,
			continuation: { tokenize: tokenizeListContinuation },
			exit: tokenizeListEnd
		};
		listItemPrefixWhitespaceConstruct = {
			tokenize: tokenizeListItemPrefixWhitespace,
			partial: true
		};
		indentConstruct = {
			tokenize: tokenizeIndent,
			partial: true
		};
	}));

//#endregion
//#region node_modules/micromark-core-commonmark/lib/setext-underline.js
/** @type {Resolver} */
	function resolveToSetextUnderline(events, context) {
		let index = events.length;
		/** @type {number | undefined} */
		let content;
		/** @type {number | undefined} */
		let text;
		/** @type {number | undefined} */
		let definition;
		while (index--) if (events[index][0] === "enter") {
			if (events[index][1].type === "content") {
				content = index;
				break;
			}
			if (events[index][1].type === "paragraph") text = index;
		} else {
			if (events[index][1].type === "content") events.splice(index, 1);
			if (!definition && events[index][1].type === "definition") definition = index;
		}
		const heading = {
			type: "setextHeading",
			start: Object.assign({}, events[text][1].start),
			end: Object.assign({}, events[events.length - 1][1].end)
		};
		events[text][1].type = "setextHeadingText";
		if (definition) {
			events.splice(text, 0, [
				"enter",
				heading,
				context
			]);
			events.splice(definition + 1, 0, [
				"exit",
				events[content][1],
				context
			]);
			events[content][1].end = Object.assign({}, events[definition][1].end);
		} else events[content][1] = heading;
		events.push([
			"exit",
			heading,
			context
		]);
		return events;
	}
	/**
	* @this {TokenizeContext}
	* @type {Tokenizer}
	*/
	function tokenizeSetextUnderline(effects, ok, nok) {
		const self = this;
		/** @type {NonNullable<Code>} */
		let marker;
		return start;
		/**
		* At start of heading (setext) underline.
		*
		* ```markdown
		*   | aa
		* > | ==
		*     ^
		* ```
		*
		* @type {State}
		*/
		function start(code) {
			let index = self.events.length;
			/** @type {boolean | undefined} */
			let paragraph;
			while (index--) if (self.events[index][1].type !== "lineEnding" && self.events[index][1].type !== "linePrefix" && self.events[index][1].type !== "content") {
				paragraph = self.events[index][1].type === "paragraph";
				break;
			}
			if (!self.parser.lazy[self.now().line] && (self.interrupt || paragraph)) {
				effects.enter("setextHeadingLine");
				marker = code;
				return before(code);
			}
			return nok(code);
		}
		/**
		* After optional whitespace, at `-` or `=`.
		*
		* ```markdown
		*   | aa
		* > | ==
		*     ^
		* ```
		*
		* @type {State}
		*/
		function before(code) {
			effects.enter("setextHeadingLineSequence");
			return inside(code);
		}
		/**
		* In sequence.
		*
		* ```markdown
		*   | aa
		* > | ==
		*     ^
		* ```
		*
		* @type {State}
		*/
		function inside(code) {
			if (code === marker) {
				effects.consume(code);
				return inside;
			}
			effects.exit("setextHeadingLineSequence");
			return markdownSpace(code) ? factorySpace(effects, after, "lineSuffix")(code) : after(code);
		}
		/**
		* After sequence, after optional whitespace.
		*
		* ```markdown
		*   | aa
		* > | ==
		*       ^
		* ```
		*
		* @type {State}
		*/
		function after(code) {
			if (code === null || markdownLineEnding(code)) {
				effects.exit("setextHeadingLine");
				return ok(code);
			}
			return nok(code);
		}
	}
	var setextUnderline;
	var init_setext_underline = __esmMin((() => {
		init_micromark_factory_space();
		init_micromark_util_character();
		setextUnderline = {
			name: "setextUnderline",
			tokenize: tokenizeSetextUnderline,
			resolveTo: resolveToSetextUnderline
		};
	}));

//#endregion
//#region node_modules/micromark-core-commonmark/index.js
	var init_micromark_core_commonmark = __esmMin((() => {
		init_attention();
		init_autolink();
		init_blank_line();
		init_block_quote();
		init_character_escape();
		init_character_reference();
		init_code_fenced();
		init_code_indented();
		init_code_text();
		init_content();
		init_definition();
		init_hard_break_escape();
		init_heading_atx();
		init_html_flow();
		init_html_text();
		init_label_end();
		init_label_start_image();
		init_label_start_link();
		init_line_ending();
		init_list$1();
		init_setext_underline();
		init_thematic_break$1();
	}));

//#endregion
//#region node_modules/micromark/lib/initialize/flow.js
/**
	* @this {TokenizeContext}
	* @type {Initializer}
	*/
	function initializeFlow(effects) {
		const self = this;
		const initial = effects.attempt(blankLine, atBlankEnding, effects.attempt(this.parser.constructs.flowInitial, afterConstruct, factorySpace(effects, effects.attempt(this.parser.constructs.flow, afterConstruct, effects.attempt(content, afterConstruct)), "linePrefix")));
		return initial;
		/** @type {State} */
		function atBlankEnding(code) {
			if (code === null) {
				effects.consume(code);
				return;
			}
			effects.enter("lineEndingBlank");
			effects.consume(code);
			effects.exit("lineEndingBlank");
			self.currentConstruct = void 0;
			return initial;
		}
		/** @type {State} */
		function afterConstruct(code) {
			if (code === null) {
				effects.consume(code);
				return;
			}
			effects.enter("lineEnding");
			effects.consume(code);
			effects.exit("lineEnding");
			self.currentConstruct = void 0;
			return initial;
		}
	}
	var flow$1;
	var init_flow = __esmMin((() => {
		init_micromark_core_commonmark();
		init_micromark_factory_space();
		flow$1 = { tokenize: initializeFlow };
	}));

//#endregion
//#region node_modules/micromark/lib/initialize/text.js
/**
	* @param {'string' | 'text'} field
	* @returns {InitialConstruct}
	*/
	function initializeFactory(field) {
		return {
			tokenize: initializeText,
			resolveAll: createResolver(field === "text" ? resolveAllLineSuffixes : void 0)
		};
		/**
		* @this {TokenizeContext}
		* @type {Initializer}
		*/
		function initializeText(effects) {
			const self = this;
			const constructs = this.parser.constructs[field];
			const text = effects.attempt(constructs, start, notText);
			return start;
			/** @type {State} */
			function start(code) {
				return atBreak(code) ? text(code) : notText(code);
			}
			/** @type {State} */
			function notText(code) {
				if (code === null) {
					effects.consume(code);
					return;
				}
				effects.enter("data");
				effects.consume(code);
				return data;
			}
			/** @type {State} */
			function data(code) {
				if (atBreak(code)) {
					effects.exit("data");
					return text(code);
				}
				effects.consume(code);
				return data;
			}
			/**
			* @param {Code} code
			* @returns {boolean}
			*/
			function atBreak(code) {
				if (code === null) return true;
				const list = constructs[code];
				let index = -1;
				if (list) while (++index < list.length) {
					const item = list[index];
					if (!item.previous || item.previous.call(self, self.previous)) return true;
				}
				return false;
			}
		}
	}
	/**
	* @param {Resolver | undefined} [extraResolver]
	* @returns {Resolver}
	*/
	function createResolver(extraResolver) {
		return resolveAllText;
		/** @type {Resolver} */
		function resolveAllText(events, context) {
			let index = -1;
			/** @type {number | undefined} */
			let enter;
			while (++index <= events.length) if (enter === void 0) {
				if (events[index] && events[index][1].type === "data") {
					enter = index;
					index++;
				}
			} else if (!events[index] || events[index][1].type !== "data") {
				if (index !== enter + 2) {
					events[enter][1].end = events[index - 1][1].end;
					events.splice(enter + 2, index - enter - 2);
					index = enter + 2;
				}
				enter = void 0;
			}
			return extraResolver ? extraResolver(events, context) : events;
		}
	}
	/**
	* A rather ugly set of instructions which again looks at chunks in the input
	* stream.
	* The reason to do this here is that it is *much* faster to parse in reverse.
	* And that we can’t hook into `null` to split the line suffix before an EOF.
	* To do: figure out if we can make this into a clean utility, or even in core.
	* As it will be useful for GFMs literal autolink extension (and maybe even
	* tables?)
	*
	* @type {Resolver}
	*/
	function resolveAllLineSuffixes(events, context) {
		let eventIndex = 0;
		while (++eventIndex <= events.length) if ((eventIndex === events.length || events[eventIndex][1].type === "lineEnding") && events[eventIndex - 1][1].type === "data") {
			const data = events[eventIndex - 1][1];
			const chunks = context.sliceStream(data);
			let index = chunks.length;
			let bufferIndex = -1;
			let size = 0;
			/** @type {boolean | undefined} */
			let tabs;
			while (index--) {
				const chunk = chunks[index];
				if (typeof chunk === "string") {
					bufferIndex = chunk.length;
					while (chunk.charCodeAt(bufferIndex - 1) === 32) {
						size++;
						bufferIndex--;
					}
					if (bufferIndex) break;
					bufferIndex = -1;
				} else if (chunk === -2) {
					tabs = true;
					size++;
				} else if (chunk === -1) {} else {
					index++;
					break;
				}
			}
			if (size) {
				const token = {
					type: eventIndex === events.length || tabs || size < 2 ? "lineSuffix" : "hardBreakTrailing",
					start: {
						line: data.end.line,
						column: data.end.column - size,
						offset: data.end.offset - size,
						_index: data.start._index + index,
						_bufferIndex: index ? bufferIndex : data.start._bufferIndex + bufferIndex
					},
					end: Object.assign({}, data.end)
				};
				data.end = Object.assign({}, token.start);
				if (data.start.offset === data.end.offset) Object.assign(data, token);
				else {
					events.splice(eventIndex, 0, [
						"enter",
						token,
						context
					], [
						"exit",
						token,
						context
					]);
					eventIndex += 2;
				}
			}
			eventIndex++;
		}
		return events;
	}
	var resolver, string$1, text$2;
	var init_text$1 = __esmMin((() => {
		resolver = { resolveAll: createResolver() };
		string$1 = initializeFactory("string");
		text$2 = initializeFactory("text");
	}));

//#endregion
//#region node_modules/micromark/lib/create-tokenizer.js
/**
	* Create a tokenizer.
	* Tokenizers deal with one type of data (e.g., containers, flow, text).
	* The parser is the object dealing with it all.
	* `initialize` works like other constructs, except that only its `tokenize`
	* function is used, in which case it doesn’t receive an `ok` or `nok`.
	* `from` can be given to set the point before the first character, although
	* when further lines are indented, they must be set with `defineSkip`.
	*
	* @param {ParseContext} parser
	* @param {InitialConstruct} initialize
	* @param {Omit<Point, '_bufferIndex' | '_index'> | undefined} [from]
	* @returns {TokenizeContext}
	*/
	function createTokenizer(parser, initialize, from) {
		/** @type {Point} */
		let point = Object.assign(from ? Object.assign({}, from) : {
			line: 1,
			column: 1,
			offset: 0
		}, {
			_index: 0,
			_bufferIndex: -1
		});
		/** @type {Record<string, number>} */
		const columnStart = {};
		/** @type {Array<Construct>} */
		const resolveAllConstructs = [];
		/** @type {Array<Chunk>} */
		let chunks = [];
		/** @type {Array<Token>} */
		let stack = [];
		/**
		* Tools used for tokenizing.
		*
		* @type {Effects}
		*/
		const effects = {
			consume,
			enter,
			exit,
			attempt: constructFactory(onsuccessfulconstruct),
			check: constructFactory(onsuccessfulcheck),
			interrupt: constructFactory(onsuccessfulcheck, { interrupt: true })
		};
		/**
		* State and tools for resolving and serializing.
		*
		* @type {TokenizeContext}
		*/
		const context = {
			previous: null,
			code: null,
			containerState: {},
			events: [],
			parser,
			sliceStream,
			sliceSerialize,
			now,
			defineSkip,
			write
		};
		/**
		* The state function.
		*
		* @type {State | void}
		*/
		let state = initialize.tokenize.call(context, effects);
		if (initialize.resolveAll) resolveAllConstructs.push(initialize);
		return context;
		/** @type {TokenizeContext['write']} */
		function write(slice) {
			chunks = push(chunks, slice);
			main();
			if (chunks[chunks.length - 1] !== null) return [];
			addResult(initialize, 0);
			context.events = resolveAll(resolveAllConstructs, context.events, context);
			return context.events;
		}
		/** @type {TokenizeContext['sliceSerialize']} */
		function sliceSerialize(token, expandTabs) {
			return serializeChunks(sliceStream(token), expandTabs);
		}
		/** @type {TokenizeContext['sliceStream']} */
		function sliceStream(token) {
			return sliceChunks(chunks, token);
		}
		/** @type {TokenizeContext['now']} */
		function now() {
			const { line, column, offset, _index, _bufferIndex } = point;
			return {
				line,
				column,
				offset,
				_index,
				_bufferIndex
			};
		}
		/** @type {TokenizeContext['defineSkip']} */
		function defineSkip(value) {
			columnStart[value.line] = value.column;
			accountForPotentialSkip();
		}
		/**
		* Main loop (note that `_index` and `_bufferIndex` in `point` are modified by
		* `consume`).
		* Here is where we walk through the chunks, which either include strings of
		* several characters, or numerical character codes.
		* The reason to do this in a loop instead of a call is so the stack can
		* drain.
		*
		* @returns {void}
		*/
		function main() {
			/** @type {number} */
			let chunkIndex;
			while (point._index < chunks.length) {
				const chunk = chunks[point._index];
				if (typeof chunk === "string") {
					chunkIndex = point._index;
					if (point._bufferIndex < 0) point._bufferIndex = 0;
					while (point._index === chunkIndex && point._bufferIndex < chunk.length) go(chunk.charCodeAt(point._bufferIndex));
				} else go(chunk);
			}
		}
		/**
		* Deal with one code.
		*
		* @param {Code} code
		* @returns {void}
		*/
		function go(code) {
			state = state(code);
		}
		/** @type {Effects['consume']} */
		function consume(code) {
			if (markdownLineEnding(code)) {
				point.line++;
				point.column = 1;
				point.offset += code === -3 ? 2 : 1;
				accountForPotentialSkip();
			} else if (code !== -1) {
				point.column++;
				point.offset++;
			}
			if (point._bufferIndex < 0) point._index++;
			else {
				point._bufferIndex++;
				if (point._bufferIndex === chunks[point._index].length) {
					point._bufferIndex = -1;
					point._index++;
				}
			}
			context.previous = code;
		}
		/** @type {Effects['enter']} */
		function enter(type, fields) {
			/** @type {Token} */
			const token = fields || {};
			token.type = type;
			token.start = now();
			context.events.push([
				"enter",
				token,
				context
			]);
			stack.push(token);
			return token;
		}
		/** @type {Effects['exit']} */
		function exit(type) {
			const token = stack.pop();
			token.end = now();
			context.events.push([
				"exit",
				token,
				context
			]);
			return token;
		}
		/**
		* Use results.
		*
		* @type {ReturnHandle}
		*/
		function onsuccessfulconstruct(construct, info) {
			addResult(construct, info.from);
		}
		/**
		* Discard results.
		*
		* @type {ReturnHandle}
		*/
		function onsuccessfulcheck(_, info) {
			info.restore();
		}
		/**
		* Factory to attempt/check/interrupt.
		*
		* @param {ReturnHandle} onreturn
		* @param {{interrupt?: boolean | undefined} | undefined} [fields]
		*/
		function constructFactory(onreturn, fields) {
			return hook;
			/**
			* Handle either an object mapping codes to constructs, a list of
			* constructs, or a single construct.
			*
			* @param {Array<Construct> | Construct | ConstructRecord} constructs
			* @param {State} returnState
			* @param {State | undefined} [bogusState]
			* @returns {State}
			*/
			function hook(constructs, returnState, bogusState) {
				/** @type {Array<Construct>} */
				let listOfConstructs;
				/** @type {number} */
				let constructIndex;
				/** @type {Construct} */
				let currentConstruct;
				/** @type {Info} */
				let info;
				return Array.isArray(constructs) ? handleListOfConstructs(constructs) : "tokenize" in constructs ? handleListOfConstructs([constructs]) : handleMapOfConstructs(constructs);
				/**
				* Handle a list of construct.
				*
				* @param {ConstructRecord} map
				* @returns {State}
				*/
				function handleMapOfConstructs(map) {
					return start;
					/** @type {State} */
					function start(code) {
						const def = code !== null && map[code];
						const all = code !== null && map.null;
						return handleListOfConstructs([...Array.isArray(def) ? def : def ? [def] : [], ...Array.isArray(all) ? all : all ? [all] : []])(code);
					}
				}
				/**
				* Handle a list of construct.
				*
				* @param {Array<Construct>} list
				* @returns {State}
				*/
				function handleListOfConstructs(list) {
					listOfConstructs = list;
					constructIndex = 0;
					if (list.length === 0) return bogusState;
					return handleConstruct(list[constructIndex]);
				}
				/**
				* Handle a single construct.
				*
				* @param {Construct} construct
				* @returns {State}
				*/
				function handleConstruct(construct) {
					return start;
					/** @type {State} */
					function start(code) {
						info = store();
						currentConstruct = construct;
						if (!construct.partial) context.currentConstruct = construct;
						if (construct.name && context.parser.constructs.disable.null.includes(construct.name)) return nok(code);
						return construct.tokenize.call(fields ? Object.assign(Object.create(context), fields) : context, effects, ok, nok)(code);
					}
				}
				/** @type {State} */
				function ok(code) {
					onreturn(currentConstruct, info);
					return returnState;
				}
				/** @type {State} */
				function nok(code) {
					info.restore();
					if (++constructIndex < listOfConstructs.length) return handleConstruct(listOfConstructs[constructIndex]);
					return bogusState;
				}
			}
		}
		/**
		* @param {Construct} construct
		* @param {number} from
		* @returns {void}
		*/
		function addResult(construct, from) {
			if (construct.resolveAll && !resolveAllConstructs.includes(construct)) resolveAllConstructs.push(construct);
			if (construct.resolve) splice(context.events, from, context.events.length - from, construct.resolve(context.events.slice(from), context));
			if (construct.resolveTo) context.events = construct.resolveTo(context.events, context);
		}
		/**
		* Store state.
		*
		* @returns {Info}
		*/
		function store() {
			const startPoint = now();
			const startPrevious = context.previous;
			const startCurrentConstruct = context.currentConstruct;
			const startEventsIndex = context.events.length;
			const startStack = Array.from(stack);
			return {
				restore,
				from: startEventsIndex
			};
			/**
			* Restore state.
			*
			* @returns {void}
			*/
			function restore() {
				point = startPoint;
				context.previous = startPrevious;
				context.currentConstruct = startCurrentConstruct;
				context.events.length = startEventsIndex;
				stack = startStack;
				accountForPotentialSkip();
			}
		}
		/**
		* Move the current point a bit forward in the line when it’s on a column
		* skip.
		*
		* @returns {void}
		*/
		function accountForPotentialSkip() {
			if (point.line in columnStart && point.column < 2) {
				point.column = columnStart[point.line];
				point.offset += columnStart[point.line] - 1;
			}
		}
	}
	/**
	* Get the chunks from a slice of chunks in the range of a token.
	*
	* @param {Array<Chunk>} chunks
	* @param {Pick<Token, 'end' | 'start'>} token
	* @returns {Array<Chunk>}
	*/
	function sliceChunks(chunks, token) {
		const startIndex = token.start._index;
		const startBufferIndex = token.start._bufferIndex;
		const endIndex = token.end._index;
		const endBufferIndex = token.end._bufferIndex;
		/** @type {Array<Chunk>} */
		let view;
		if (startIndex === endIndex) view = [chunks[startIndex].slice(startBufferIndex, endBufferIndex)];
		else {
			view = chunks.slice(startIndex, endIndex);
			if (startBufferIndex > -1) {
				const head = view[0];
				if (typeof head === "string") view[0] = head.slice(startBufferIndex);
				else view.shift();
			}
			if (endBufferIndex > 0) view.push(chunks[endIndex].slice(0, endBufferIndex));
		}
		return view;
	}
	/**
	* Get the string value of a slice of chunks.
	*
	* @param {Array<Chunk>} chunks
	* @param {boolean | undefined} [expandTabs=false]
	* @returns {string}
	*/
	function serializeChunks(chunks, expandTabs) {
		let index = -1;
		/** @type {Array<string>} */
		const result = [];
		/** @type {boolean | undefined} */
		let atTab;
		while (++index < chunks.length) {
			const chunk = chunks[index];
			/** @type {string} */
			let value;
			if (typeof chunk === "string") value = chunk;
			else switch (chunk) {
				case -5:
					value = "\r";
					break;
				case -4:
					value = "\n";
					break;
				case -3:
					value = "\r\n";
					break;
				case -2:
					value = expandTabs ? " " : "	";
					break;
				case -1:
					if (!expandTabs && atTab) continue;
					value = " ";
					break;
				default: value = String.fromCharCode(chunk);
			}
			atTab = chunk === -2;
			result.push(value);
		}
		return result.join("");
	}
	var init_create_tokenizer = __esmMin((() => {
		init_micromark_util_character();
		init_micromark_util_chunked();
		init_micromark_util_resolve_all();
	}));

//#endregion
//#region node_modules/micromark/lib/constructs.js
	var constructs_exports = /* @__PURE__ */ __exportAll({
		attentionMarkers: () => attentionMarkers,
		contentInitial: () => contentInitial,
		disable: () => disable,
		document: () => document$1,
		flow: () => flow,
		flowInitial: () => flowInitial,
		insideSpan: () => insideSpan,
		string: () => string,
		text: () => text$1
	});
	var document$1, contentInitial, flowInitial, flow, string, text$1, insideSpan, attentionMarkers, disable;
	var init_constructs = __esmMin((() => {
		init_micromark_core_commonmark();
		init_text$1();
		document$1 = {
			[42]: list$1,
			[43]: list$1,
			[45]: list$1,
			[48]: list$1,
			[49]: list$1,
			[50]: list$1,
			[51]: list$1,
			[52]: list$1,
			[53]: list$1,
			[54]: list$1,
			[55]: list$1,
			[56]: list$1,
			[57]: list$1,
			[62]: blockQuote
		};
		contentInitial = { [91]: definition };
		flowInitial = {
			[-2]: codeIndented,
			[-1]: codeIndented,
			[32]: codeIndented
		};
		flow = {
			[35]: headingAtx,
			[42]: thematicBreak$1,
			[45]: [setextUnderline, thematicBreak$1],
			[60]: htmlFlow,
			[61]: setextUnderline,
			[95]: thematicBreak$1,
			[96]: codeFenced,
			[126]: codeFenced
		};
		string = {
			[38]: characterReference,
			[92]: characterEscape
		};
		text$1 = {
			[-5]: lineEnding,
			[-4]: lineEnding,
			[-3]: lineEnding,
			[33]: labelStartImage,
			[38]: characterReference,
			[42]: attention,
			[60]: [autolink, htmlText],
			[91]: labelStartLink,
			[92]: [hardBreakEscape, characterEscape],
			[93]: labelEnd,
			[95]: attention,
			[96]: codeText
		};
		insideSpan = { null: [attention, resolver] };
		attentionMarkers = { null: [42, 95] };
		disable = { null: [] };
	}));

//#endregion
//#region node_modules/micromark/lib/parse.js
/**
	* @param {ParseOptions | null | undefined} [options]
	* @returns {ParseContext}
	*/
	function parse(options) {
		/** @type {ParseContext} */
		const parser = {
			defined: [],
			lazy: {},
			constructs: combineExtensions([constructs_exports, ...(options || {}).extensions || []]),
			content: create(content$1),
			document: create(document$2),
			flow: create(flow$1),
			string: create(string$1),
			text: create(text$2)
		};
		return parser;
		/**
		* @param {InitialConstruct} initial
		*/
		function create(initial) {
			return creator;
			/** @type {Create} */
			function creator(from) {
				return createTokenizer(parser, initial, from);
			}
		}
	}
	var init_parse = __esmMin((() => {
		init_micromark_util_combine_extensions();
		init_content$1();
		init_document();
		init_flow();
		init_text$1();
		init_create_tokenizer();
		init_constructs();
	}));

//#endregion
//#region node_modules/micromark/lib/preprocess.js
/**
	* @returns {Preprocessor}
	*/
	function preprocess() {
		let column = 1;
		let buffer = "";
		/** @type {boolean | undefined} */
		let start = true;
		/** @type {boolean | undefined} */
		let atCarriageReturn;
		return preprocessor;
		/** @type {Preprocessor} */
		function preprocessor(value, encoding, end) {
			/** @type {Array<Chunk>} */
			const chunks = [];
			/** @type {RegExpMatchArray | null} */
			let match;
			/** @type {number} */
			let next;
			/** @type {number} */
			let startPosition;
			/** @type {number} */
			let endPosition;
			/** @type {Code} */
			let code;
			value = buffer + value.toString(encoding);
			startPosition = 0;
			buffer = "";
			if (start) {
				if (value.charCodeAt(0) === 65279) startPosition++;
				start = void 0;
			}
			while (startPosition < value.length) {
				search.lastIndex = startPosition;
				match = search.exec(value);
				endPosition = match && match.index !== void 0 ? match.index : value.length;
				code = value.charCodeAt(endPosition);
				if (!match) {
					buffer = value.slice(startPosition);
					break;
				}
				if (code === 10 && startPosition === endPosition && atCarriageReturn) {
					chunks.push(-3);
					atCarriageReturn = void 0;
				} else {
					if (atCarriageReturn) {
						chunks.push(-5);
						atCarriageReturn = void 0;
					}
					if (startPosition < endPosition) {
						chunks.push(value.slice(startPosition, endPosition));
						column += endPosition - startPosition;
					}
					switch (code) {
						case 0:
							chunks.push(65533);
							column++;
							break;
						case 9:
							next = Math.ceil(column / 4) * 4;
							chunks.push(-2);
							while (column++ < next) chunks.push(-1);
							break;
						case 10:
							chunks.push(-4);
							column = 1;
							break;
						default:
							atCarriageReturn = true;
							column = 1;
					}
				}
				startPosition = endPosition + 1;
			}
			if (end) {
				if (atCarriageReturn) chunks.push(-5);
				if (buffer) chunks.push(buffer);
				chunks.push(null);
			}
			return chunks;
		}
	}
	var search;
	var init_preprocess = __esmMin((() => {
		search = /[\0\t\n\r]/g;
	}));

//#endregion
//#region node_modules/micromark/lib/postprocess.js
/**
	* @param {Array<Event>} events
	* @returns {Array<Event>}
	*/
	function postprocess(events) {
		while (!subtokenize(events));
		return events;
	}
	var init_postprocess = __esmMin((() => {
		init_micromark_util_subtokenize();
	}));

//#endregion
//#region node_modules/micromark-util-decode-numeric-character-reference/index.js
/**
	* Turn the number (in string form as either hexa- or plain decimal) coming from
	* a numeric character reference into a character.
	*
	* Sort of like `String.fromCharCode(Number.parseInt(value, base))`, but makes
	* non-characters and control characters safe.
	*
	* @param {string} value
	*   Value to decode.
	* @param {number} base
	*   Numeric base.
	* @returns {string}
	*   Character.
	*/
	function decodeNumericCharacterReference(value, base) {
		const code = Number.parseInt(value, base);
		if (code < 9 || code === 11 || code > 13 && code < 32 || code > 126 && code < 160 || code > 55295 && code < 57344 || code > 64975 && code < 65008 || (code & 65535) === 65535 || (code & 65535) === 65534 || code > 1114111) return "�";
		return String.fromCharCode(code);
	}
	var init_micromark_util_decode_numeric_character_reference = __esmMin((() => {}));

//#endregion
//#region node_modules/micromark-util-decode-string/index.js
/**
	* Decode markdown strings (which occur in places such as fenced code info
	* strings, destinations, labels, and titles).
	*
	* The “string” content type allows character escapes and -references.
	* This decodes those.
	*
	* @param {string} value
	*   Value to decode.
	* @returns {string}
	*   Decoded value.
	*/
	function decodeString(value) {
		return value.replace(characterEscapeOrReference, decode);
	}
	/**
	* @param {string} $0
	* @param {string} $1
	* @param {string} $2
	* @returns {string}
	*/
	function decode($0, $1, $2) {
		if ($1) return $1;
		if ($2.charCodeAt(0) === 35) {
			const head = $2.charCodeAt(1);
			const hex = head === 120 || head === 88;
			return decodeNumericCharacterReference($2.slice(hex ? 2 : 1), hex ? 16 : 10);
		}
		return decodeNamedCharacterReference($2) || $0;
	}
	var characterEscapeOrReference;
	var init_micromark_util_decode_string = __esmMin((() => {
		init_index_dom();
		init_micromark_util_decode_numeric_character_reference();
		characterEscapeOrReference = /\\([!-/:-@[-`{-~])|&(#(?:\d{1,7}|x[\da-f]{1,6})|[\da-z]{1,31});/gi;
	}));

//#endregion
//#region node_modules/mdast-util-from-markdown/lib/index.js
/**
	* Note this compiler only understand complete buffering, not streaming.
	*
	* @param {Options | null | undefined} [options]
	*/
	function compiler(options) {
		/** @type {Config} */
		const config = {
			transforms: [],
			canContainEols: [
				"emphasis",
				"fragment",
				"heading",
				"paragraph",
				"strong"
			],
			enter: {
				autolink: opener(link),
				autolinkProtocol: onenterdata,
				autolinkEmail: onenterdata,
				atxHeading: opener(heading),
				blockQuote: opener(blockQuote),
				characterEscape: onenterdata,
				characterReference: onenterdata,
				codeFenced: opener(codeFlow),
				codeFencedFenceInfo: buffer,
				codeFencedFenceMeta: buffer,
				codeIndented: opener(codeFlow, buffer),
				codeText: opener(codeText, buffer),
				codeTextData: onenterdata,
				data: onenterdata,
				codeFlowValue: onenterdata,
				definition: opener(definition),
				definitionDestinationString: buffer,
				definitionLabelString: buffer,
				definitionTitleString: buffer,
				emphasis: opener(emphasis),
				hardBreakEscape: opener(hardBreak),
				hardBreakTrailing: opener(hardBreak),
				htmlFlow: opener(html, buffer),
				htmlFlowData: onenterdata,
				htmlText: opener(html, buffer),
				htmlTextData: onenterdata,
				image: opener(image),
				label: buffer,
				link: opener(link),
				listItem: opener(listItem),
				listItemValue: onenterlistitemvalue,
				listOrdered: opener(list, onenterlistordered),
				listUnordered: opener(list),
				paragraph: opener(paragraph),
				reference: onenterreference,
				referenceString: buffer,
				resourceDestinationString: buffer,
				resourceTitleString: buffer,
				setextHeading: opener(heading),
				strong: opener(strong),
				thematicBreak: opener(thematicBreak)
			},
			exit: {
				atxHeading: closer(),
				atxHeadingSequence: onexitatxheadingsequence,
				autolink: closer(),
				autolinkEmail: onexitautolinkemail,
				autolinkProtocol: onexitautolinkprotocol,
				blockQuote: closer(),
				characterEscapeValue: onexitdata,
				characterReferenceMarkerHexadecimal: onexitcharacterreferencemarker,
				characterReferenceMarkerNumeric: onexitcharacterreferencemarker,
				characterReferenceValue: onexitcharacterreferencevalue,
				codeFenced: closer(onexitcodefenced),
				codeFencedFence: onexitcodefencedfence,
				codeFencedFenceInfo: onexitcodefencedfenceinfo,
				codeFencedFenceMeta: onexitcodefencedfencemeta,
				codeFlowValue: onexitdata,
				codeIndented: closer(onexitcodeindented),
				codeText: closer(onexitcodetext),
				codeTextData: onexitdata,
				data: onexitdata,
				definition: closer(),
				definitionDestinationString: onexitdefinitiondestinationstring,
				definitionLabelString: onexitdefinitionlabelstring,
				definitionTitleString: onexitdefinitiontitlestring,
				emphasis: closer(),
				hardBreakEscape: closer(onexithardbreak),
				hardBreakTrailing: closer(onexithardbreak),
				htmlFlow: closer(onexithtmlflow),
				htmlFlowData: onexitdata,
				htmlText: closer(onexithtmltext),
				htmlTextData: onexitdata,
				image: closer(onexitimage),
				label: onexitlabel,
				labelText: onexitlabeltext,
				lineEnding: onexitlineending,
				link: closer(onexitlink),
				listItem: closer(),
				listOrdered: closer(),
				listUnordered: closer(),
				paragraph: closer(),
				referenceString: onexitreferencestring,
				resourceDestinationString: onexitresourcedestinationstring,
				resourceTitleString: onexitresourcetitlestring,
				resource: onexitresource,
				setextHeading: closer(onexitsetextheading),
				setextHeadingLineSequence: onexitsetextheadinglinesequence,
				setextHeadingText: onexitsetextheadingtext,
				strong: closer(),
				thematicBreak: closer()
			}
		};
		configure(config, (options || {}).mdastExtensions || []);
		/** @type {CompileData} */
		const data = {};
		return compile;
		/**
		* Turn micromark events into an mdast tree.
		*
		* @param {Array<Event>} events
		*   Events.
		* @returns {Root}
		*   mdast tree.
		*/
		function compile(events) {
			/** @type {Root} */
			let tree = {
				type: "root",
				children: []
			};
			/** @type {Omit<CompileContext, 'sliceSerialize'>} */
			const context = {
				stack: [tree],
				tokenStack: [],
				config,
				enter,
				exit,
				buffer,
				resume,
				setData,
				getData
			};
			/** @type {Array<number>} */
			const listStack = [];
			let index = -1;
			while (++index < events.length) if (events[index][1].type === "listOrdered" || events[index][1].type === "listUnordered") if (events[index][0] === "enter") listStack.push(index);
			else index = prepareList(events, listStack.pop(), index);
			index = -1;
			while (++index < events.length) {
				const handler = config[events[index][0]];
				if (own$5.call(handler, events[index][1].type)) handler[events[index][1].type].call(Object.assign({ sliceSerialize: events[index][2].sliceSerialize }, context), events[index][1]);
			}
			if (context.tokenStack.length > 0) {
				const tail = context.tokenStack[context.tokenStack.length - 1];
				(tail[1] || defaultOnError).call(context, void 0, tail[0]);
			}
			tree.position = {
				start: point$1(events.length > 0 ? events[0][1].start : {
					line: 1,
					column: 1,
					offset: 0
				}),
				end: point$1(events.length > 0 ? events[events.length - 2][1].end : {
					line: 1,
					column: 1,
					offset: 0
				})
			};
			index = -1;
			while (++index < config.transforms.length) tree = config.transforms[index](tree) || tree;
			return tree;
		}
		/**
		* @param {Array<Event>} events
		* @param {number} start
		* @param {number} length
		* @returns {number}
		*/
		function prepareList(events, start, length) {
			let index = start - 1;
			let containerBalance = -1;
			let listSpread = false;
			/** @type {Token | undefined} */
			let listItem;
			/** @type {number | undefined} */
			let lineIndex;
			/** @type {number | undefined} */
			let firstBlankLineIndex;
			/** @type {boolean | undefined} */
			let atMarker;
			while (++index <= length) {
				const event = events[index];
				if (event[1].type === "listUnordered" || event[1].type === "listOrdered" || event[1].type === "blockQuote") {
					if (event[0] === "enter") containerBalance++;
					else containerBalance--;
					atMarker = void 0;
				} else if (event[1].type === "lineEndingBlank") {
					if (event[0] === "enter") {
						if (listItem && !atMarker && !containerBalance && !firstBlankLineIndex) firstBlankLineIndex = index;
						atMarker = void 0;
					}
				} else if (event[1].type === "linePrefix" || event[1].type === "listItemValue" || event[1].type === "listItemMarker" || event[1].type === "listItemPrefix" || event[1].type === "listItemPrefixWhitespace") {} else atMarker = void 0;
				if (!containerBalance && event[0] === "enter" && event[1].type === "listItemPrefix" || containerBalance === -1 && event[0] === "exit" && (event[1].type === "listUnordered" || event[1].type === "listOrdered")) {
					if (listItem) {
						let tailIndex = index;
						lineIndex = void 0;
						while (tailIndex--) {
							const tailEvent = events[tailIndex];
							if (tailEvent[1].type === "lineEnding" || tailEvent[1].type === "lineEndingBlank") {
								if (tailEvent[0] === "exit") continue;
								if (lineIndex) {
									events[lineIndex][1].type = "lineEndingBlank";
									listSpread = true;
								}
								tailEvent[1].type = "lineEnding";
								lineIndex = tailIndex;
							} else if (tailEvent[1].type === "linePrefix" || tailEvent[1].type === "blockQuotePrefix" || tailEvent[1].type === "blockQuotePrefixWhitespace" || tailEvent[1].type === "blockQuoteMarker" || tailEvent[1].type === "listItemIndent") {} else break;
						}
						if (firstBlankLineIndex && (!lineIndex || firstBlankLineIndex < lineIndex)) listItem._spread = true;
						listItem.end = Object.assign({}, lineIndex ? events[lineIndex][1].start : event[1].end);
						events.splice(lineIndex || index, 0, [
							"exit",
							listItem,
							event[2]
						]);
						index++;
						length++;
					}
					if (event[1].type === "listItemPrefix") {
						listItem = {
							type: "listItem",
							_spread: false,
							start: Object.assign({}, event[1].start),
							end: void 0
						};
						events.splice(index, 0, [
							"enter",
							listItem,
							event[2]
						]);
						index++;
						length++;
						firstBlankLineIndex = void 0;
						atMarker = true;
					}
				}
			}
			events[start][1]._spread = listSpread;
			return length;
		}
		/**
		* Set data.
		*
		* @template {keyof CompileData} Key
		*   Field type.
		* @param {Key} key
		*   Key of field.
		* @param {CompileData[Key]} [value]
		*   New value.
		* @returns {void}
		*   Nothing.
		*/
		function setData(key, value) {
			data[key] = value;
		}
		/**
		* Get data.
		*
		* @template {keyof CompileData} Key
		*   Field type.
		* @param {Key} key
		*   Key of field.
		* @returns {CompileData[Key]}
		*   Value.
		*/
		function getData(key) {
			return data[key];
		}
		/**
		* Create an opener handle.
		*
		* @param {(token: Token) => Node} create
		*   Create a node.
		* @param {Handle} [and]
		*   Optional function to also run.
		* @returns {Handle}
		*   Handle.
		*/
		function opener(create, and) {
			return open;
			/**
			* @this {CompileContext}
			* @param {Token} token
			* @returns {void}
			*/
			function open(token) {
				enter.call(this, create(token), token);
				if (and) and.call(this, token);
			}
		}
		/**
		* @this {CompileContext}
		* @returns {void}
		*/
		function buffer() {
			this.stack.push({
				type: "fragment",
				children: []
			});
		}
		/**
		* @template {Node} Kind
		*   Node type.
		* @this {CompileContext}
		*   Context.
		* @param {Kind} node
		*   Node to enter.
		* @param {Token} token
		*   Corresponding token.
		* @param {OnEnterError | undefined} [errorHandler]
		*   Handle the case where this token is open, but it is closed by something else.
		* @returns {Kind}
		*   The given node.
		*/
		function enter(node, token, errorHandler) {
			this.stack[this.stack.length - 1].children.push(node);
			this.stack.push(node);
			this.tokenStack.push([token, errorHandler]);
			node.position = { start: point$1(token.start) };
			return node;
		}
		/**
		* Create a closer handle.
		*
		* @param {Handle} [and]
		*   Optional function to also run.
		* @returns {Handle}
		*   Handle.
		*/
		function closer(and) {
			return close;
			/**
			* @this {CompileContext}
			* @param {Token} token
			* @returns {void}
			*/
			function close(token) {
				if (and) and.call(this, token);
				exit.call(this, token);
			}
		}
		/**
		* @this {CompileContext}
		*   Context.
		* @param {Token} token
		*   Corresponding token.
		* @param {OnExitError | undefined} [onExitError]
		*   Handle the case where another token is open.
		* @returns {Node}
		*   The closed node.
		*/
		function exit(token, onExitError) {
			const node = this.stack.pop();
			const open = this.tokenStack.pop();
			if (!open) throw new Error("Cannot close `" + token.type + "` (" + stringifyPosition({
				start: token.start,
				end: token.end
			}) + "): it’s not open");
			else if (open[0].type !== token.type) if (onExitError) onExitError.call(this, token, open[0]);
			else (open[1] || defaultOnError).call(this, token, open[0]);
			node.position.end = point$1(token.end);
			return node;
		}
		/**
		* @this {CompileContext}
		* @returns {string}
		*/
		function resume() {
			return toString(this.stack.pop());
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onenterlistordered() {
			setData("expectingFirstListItemValue", true);
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onenterlistitemvalue(token) {
			if (getData("expectingFirstListItemValue")) {
				const ancestor = this.stack[this.stack.length - 2];
				ancestor.start = Number.parseInt(this.sliceSerialize(token), 10);
				setData("expectingFirstListItemValue");
			}
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitcodefencedfenceinfo() {
			const data = this.resume();
			const node = this.stack[this.stack.length - 1];
			node.lang = data;
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitcodefencedfencemeta() {
			const data = this.resume();
			const node = this.stack[this.stack.length - 1];
			node.meta = data;
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitcodefencedfence() {
			if (getData("flowCodeInside")) return;
			this.buffer();
			setData("flowCodeInside", true);
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitcodefenced() {
			const data = this.resume();
			const node = this.stack[this.stack.length - 1];
			node.value = data.replace(/^(\r?\n|\r)|(\r?\n|\r)$/g, "");
			setData("flowCodeInside");
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitcodeindented() {
			const data = this.resume();
			const node = this.stack[this.stack.length - 1];
			node.value = data.replace(/(\r?\n|\r)$/g, "");
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitdefinitionlabelstring(token) {
			const label = this.resume();
			const node = this.stack[this.stack.length - 1];
			node.label = label;
			node.identifier = normalizeIdentifier(this.sliceSerialize(token)).toLowerCase();
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitdefinitiontitlestring() {
			const data = this.resume();
			const node = this.stack[this.stack.length - 1];
			node.title = data;
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitdefinitiondestinationstring() {
			const data = this.resume();
			const node = this.stack[this.stack.length - 1];
			node.url = data;
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitatxheadingsequence(token) {
			const node = this.stack[this.stack.length - 1];
			if (!node.depth) node.depth = this.sliceSerialize(token).length;
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitsetextheadingtext() {
			setData("setextHeadingSlurpLineEnding", true);
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitsetextheadinglinesequence(token) {
			const node = this.stack[this.stack.length - 1];
			node.depth = this.sliceSerialize(token).charCodeAt(0) === 61 ? 1 : 2;
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitsetextheading() {
			setData("setextHeadingSlurpLineEnding");
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onenterdata(token) {
			const node = this.stack[this.stack.length - 1];
			let tail = node.children[node.children.length - 1];
			if (!tail || tail.type !== "text") {
				tail = text();
				tail.position = { start: point$1(token.start) };
				node.children.push(tail);
			}
			this.stack.push(tail);
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitdata(token) {
			const tail = this.stack.pop();
			tail.value += this.sliceSerialize(token);
			tail.position.end = point$1(token.end);
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitlineending(token) {
			const context = this.stack[this.stack.length - 1];
			if (getData("atHardBreak")) {
				const tail = context.children[context.children.length - 1];
				tail.position.end = point$1(token.end);
				setData("atHardBreak");
				return;
			}
			if (!getData("setextHeadingSlurpLineEnding") && config.canContainEols.includes(context.type)) {
				onenterdata.call(this, token);
				onexitdata.call(this, token);
			}
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexithardbreak() {
			setData("atHardBreak", true);
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexithtmlflow() {
			const data = this.resume();
			const node = this.stack[this.stack.length - 1];
			node.value = data;
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexithtmltext() {
			const data = this.resume();
			const node = this.stack[this.stack.length - 1];
			node.value = data;
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitcodetext() {
			const data = this.resume();
			const node = this.stack[this.stack.length - 1];
			node.value = data;
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitlink() {
			const node = this.stack[this.stack.length - 1];
			if (getData("inReference")) {
				/** @type {ReferenceType} */
				const referenceType = getData("referenceType") || "shortcut";
				node.type += "Reference";
				node.referenceType = referenceType;
				delete node.url;
				delete node.title;
			} else {
				delete node.identifier;
				delete node.label;
			}
			setData("referenceType");
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitimage() {
			const node = this.stack[this.stack.length - 1];
			if (getData("inReference")) {
				/** @type {ReferenceType} */
				const referenceType = getData("referenceType") || "shortcut";
				node.type += "Reference";
				node.referenceType = referenceType;
				delete node.url;
				delete node.title;
			} else {
				delete node.identifier;
				delete node.label;
			}
			setData("referenceType");
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitlabeltext(token) {
			const string = this.sliceSerialize(token);
			const ancestor = this.stack[this.stack.length - 2];
			ancestor.label = decodeString(string);
			ancestor.identifier = normalizeIdentifier(string).toLowerCase();
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitlabel() {
			const fragment = this.stack[this.stack.length - 1];
			const value = this.resume();
			const node = this.stack[this.stack.length - 1];
			setData("inReference", true);
			if (node.type === "link") node.children = fragment.children;
			else node.alt = value;
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitresourcedestinationstring() {
			const data = this.resume();
			const node = this.stack[this.stack.length - 1];
			node.url = data;
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitresourcetitlestring() {
			const data = this.resume();
			const node = this.stack[this.stack.length - 1];
			node.title = data;
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitresource() {
			setData("inReference");
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onenterreference() {
			setData("referenceType", "collapsed");
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitreferencestring(token) {
			const label = this.resume();
			const node = this.stack[this.stack.length - 1];
			node.label = label;
			node.identifier = normalizeIdentifier(this.sliceSerialize(token)).toLowerCase();
			setData("referenceType", "full");
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitcharacterreferencemarker(token) {
			setData("characterReferenceType", token.type);
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitcharacterreferencevalue(token) {
			const data = this.sliceSerialize(token);
			const type = getData("characterReferenceType");
			/** @type {string} */
			let value;
			if (type) {
				value = decodeNumericCharacterReference(data, type === "characterReferenceMarkerNumeric" ? 10 : 16);
				setData("characterReferenceType");
			} else value = decodeNamedCharacterReference(data);
			const tail = this.stack.pop();
			tail.value += value;
			tail.position.end = point$1(token.end);
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitautolinkprotocol(token) {
			onexitdata.call(this, token);
			const node = this.stack[this.stack.length - 1];
			node.url = this.sliceSerialize(token);
		}
		/**
		* @this {CompileContext}
		* @type {Handle}
		*/
		function onexitautolinkemail(token) {
			onexitdata.call(this, token);
			const node = this.stack[this.stack.length - 1];
			node.url = "mailto:" + this.sliceSerialize(token);
		}
		/** @returns {Blockquote} */
		function blockQuote() {
			return {
				type: "blockquote",
				children: []
			};
		}
		/** @returns {Code} */
		function codeFlow() {
			return {
				type: "code",
				lang: null,
				meta: null,
				value: ""
			};
		}
		/** @returns {InlineCode} */
		function codeText() {
			return {
				type: "inlineCode",
				value: ""
			};
		}
		/** @returns {Definition} */
		function definition() {
			return {
				type: "definition",
				identifier: "",
				label: null,
				title: null,
				url: ""
			};
		}
		/** @returns {Emphasis} */
		function emphasis() {
			return {
				type: "emphasis",
				children: []
			};
		}
		/** @returns {Heading} */
		function heading() {
			return {
				type: "heading",
				depth: void 0,
				children: []
			};
		}
		/** @returns {Break} */
		function hardBreak() {
			return { type: "break" };
		}
		/** @returns {HTML} */
		function html() {
			return {
				type: "html",
				value: ""
			};
		}
		/** @returns {Image} */
		function image() {
			return {
				type: "image",
				title: null,
				url: "",
				alt: null
			};
		}
		/** @returns {Link} */
		function link() {
			return {
				type: "link",
				title: null,
				url: "",
				children: []
			};
		}
		/**
		* @param {Token} token
		* @returns {List}
		*/
		function list(token) {
			return {
				type: "list",
				ordered: token.type === "listOrdered",
				start: null,
				spread: token._spread,
				children: []
			};
		}
		/**
		* @param {Token} token
		* @returns {ListItem}
		*/
		function listItem(token) {
			return {
				type: "listItem",
				spread: token._spread,
				checked: null,
				children: []
			};
		}
		/** @returns {Paragraph} */
		function paragraph() {
			return {
				type: "paragraph",
				children: []
			};
		}
		/** @returns {Strong} */
		function strong() {
			return {
				type: "strong",
				children: []
			};
		}
		/** @returns {Text} */
		function text() {
			return {
				type: "text",
				value: ""
			};
		}
		/** @returns {ThematicBreak} */
		function thematicBreak() {
			return { type: "thematicBreak" };
		}
	}
	/**
	* Copy a point-like value.
	*
	* @param {Point} d
	*   Point-like value.
	* @returns {Point}
	*   unist point.
	*/
	function point$1(d) {
		return {
			line: d.line,
			column: d.column,
			offset: d.offset
		};
	}
	/**
	* @param {Config} combined
	* @param {Array<Extension | Array<Extension>>} extensions
	* @returns {void}
	*/
	function configure(combined, extensions) {
		let index = -1;
		while (++index < extensions.length) {
			const value = extensions[index];
			if (Array.isArray(value)) configure(combined, value);
			else extension(combined, value);
		}
	}
	/**
	* @param {Config} combined
	* @param {Extension} extension
	* @returns {void}
	*/
	function extension(combined, extension) {
		/** @type {keyof Extension} */
		let key;
		for (key in extension) if (own$5.call(extension, key)) {
			if (key === "canContainEols") {
				const right = extension[key];
				if (right) combined[key].push(...right);
			} else if (key === "transforms") {
				const right = extension[key];
				if (right) combined[key].push(...right);
			} else if (key === "enter" || key === "exit") {
				const right = extension[key];
				if (right) Object.assign(combined[key], right);
			}
		}
	}
	/** @type {OnEnterError} */
	function defaultOnError(left, right) {
		if (left) throw new Error("Cannot close `" + left.type + "` (" + stringifyPosition({
			start: left.start,
			end: left.end
		}) + "): a different token (`" + right.type + "`, " + stringifyPosition({
			start: right.start,
			end: right.end
		}) + ") is open");
		else throw new Error("Cannot close document, a token (`" + right.type + "`, " + stringifyPosition({
			start: right.start,
			end: right.end
		}) + ") is still open");
	}
	var own$5, fromMarkdown;
	var init_lib$9 = __esmMin((() => {
		init_mdast_util_to_string();
		init_parse();
		init_preprocess();
		init_postprocess();
		init_micromark_util_decode_numeric_character_reference();
		init_micromark_util_decode_string();
		init_micromark_util_normalize_identifier();
		init_index_dom();
		init_unist_util_stringify_position();
		own$5 = {}.hasOwnProperty;
		fromMarkdown = function(value, encoding, options) {
			if (typeof encoding !== "string") {
				options = encoding;
				encoding = void 0;
			}
			return compiler(options)(postprocess(parse(options).document().write(preprocess()(value, encoding, true))));
		};
		__name(point$1, "point");
	}));

//#endregion
//#region node_modules/mdast-util-from-markdown/index.js
	var init_mdast_util_from_markdown = __esmMin((() => {
		init_lib$9();
	}));

//#endregion
//#region node_modules/remark-parse/lib/index.js
/**
	* @this {import('unified').Processor}
	* @type {import('unified').Plugin<[Options?] | void[], string, Root>}
	*/
	function remarkParse(options) {
		/** @type {import('unified').ParserFunction<Root>} */
		const parser = (doc) => {
			const settings = this.data("settings");
			return fromMarkdown(doc, Object.assign({}, settings, options, {
				extensions: this.data("micromarkExtensions") || [],
				mdastExtensions: this.data("fromMarkdownExtensions") || []
			}));
		};
		Object.assign(this, { Parser: parser });
	}
	var init_lib$8 = __esmMin((() => {
		init_mdast_util_from_markdown();
	}));

//#endregion
//#region node_modules/remark-parse/index.js
	var init_remark_parse = __esmMin((() => {
		init_lib$8();
	}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/handlers/blockquote.js
/**
	* @typedef {import('hast').Element} Element
	* @typedef {import('mdast').Blockquote} Blockquote
	* @typedef {import('../state.js').State} State
	*/
	/**
	* Turn an mdast `blockquote` node into hast.
	*
	* @param {State} state
	*   Info passed around.
	* @param {Blockquote} node
	*   mdast node.
	* @returns {Element}
	*   hast node.
	*/
	function blockquote(state, node) {
		/** @type {Element} */
		const result = {
			type: "element",
			tagName: "blockquote",
			properties: {},
			children: state.wrap(state.all(node), true)
		};
		state.patch(node, result);
		return state.applyData(node, result);
	}
	var init_blockquote = __esmMin((() => {}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/handlers/break.js
/**
	* @typedef {import('hast').Element} Element
	* @typedef {import('hast').Text} Text
	* @typedef {import('mdast').Break} Break
	* @typedef {import('../state.js').State} State
	*/
	/**
	* Turn an mdast `break` node into hast.
	*
	* @param {State} state
	*   Info passed around.
	* @param {Break} node
	*   mdast node.
	* @returns {Array<Element | Text>}
	*   hast element content.
	*/
	function hardBreak(state, node) {
		/** @type {Element} */
		const result = {
			type: "element",
			tagName: "br",
			properties: {},
			children: []
		};
		state.patch(node, result);
		return [state.applyData(node, result), {
			type: "text",
			value: "\n"
		}];
	}
	var init_break = __esmMin((() => {}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/handlers/code.js
/**
	* @typedef {import('hast').Element} Element
	* @typedef {import('hast').Properties} Properties
	* @typedef {import('mdast').Code} Code
	* @typedef {import('../state.js').State} State
	
	*/
	/**
	* Turn an mdast `code` node into hast.
	*
	* @param {State} state
	*   Info passed around.
	* @param {Code} node
	*   mdast node.
	* @returns {Element}
	*   hast node.
	*/
	function code(state, node) {
		const value = node.value ? node.value + "\n" : "";
		const lang = node.lang ? node.lang.match(/^[^ \t]+(?=[ \t]|$)/) : null;
		/** @type {Properties} */
		const properties = {};
		if (lang) properties.className = ["language-" + lang];
		/** @type {Element} */
		let result = {
			type: "element",
			tagName: "code",
			properties,
			children: [{
				type: "text",
				value
			}]
		};
		if (node.meta) result.data = { meta: node.meta };
		state.patch(node, result);
		result = state.applyData(node, result);
		result = {
			type: "element",
			tagName: "pre",
			properties: {},
			children: [result]
		};
		state.patch(node, result);
		return result;
	}
	var init_code = __esmMin((() => {}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/handlers/delete.js
/**
	* @typedef {import('hast').Element} Element
	* @typedef {import('mdast').Delete} Delete
	* @typedef {import('../state.js').State} State
	
	*/
	/**
	* Turn an mdast `delete` node into hast.
	*
	* @param {State} state
	*   Info passed around.
	* @param {Delete} node
	*   mdast node.
	* @returns {Element}
	*   hast node.
	*/
	function strikethrough(state, node) {
		/** @type {Element} */
		const result = {
			type: "element",
			tagName: "del",
			properties: {},
			children: state.all(node)
		};
		state.patch(node, result);
		return state.applyData(node, result);
	}
	var init_delete = __esmMin((() => {}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/handlers/emphasis.js
/**
	* @typedef {import('hast').Element} Element
	* @typedef {import('mdast').Emphasis} Emphasis
	* @typedef {import('../state.js').State} State
	*/
	/**
	* Turn an mdast `emphasis` node into hast.
	*
	* @param {State} state
	*   Info passed around.
	* @param {Emphasis} node
	*   mdast node.
	* @returns {Element}
	*   hast node.
	*/
	function emphasis(state, node) {
		/** @type {Element} */
		const result = {
			type: "element",
			tagName: "em",
			properties: {},
			children: state.all(node)
		};
		state.patch(node, result);
		return state.applyData(node, result);
	}
	var init_emphasis = __esmMin((() => {}));

//#endregion
//#region node_modules/micromark-util-sanitize-uri/index.js
/**
	* Normalize a URL.
	*
	* Encode unsafe characters with percent-encoding, skipping already encoded
	* sequences.
	*
	* @param {string} value
	*   URI to normalize.
	* @returns {string}
	*   Normalized URI.
	*/
	function normalizeUri(value) {
		/** @type {Array<string>} */
		const result = [];
		let index = -1;
		let start = 0;
		let skip = 0;
		while (++index < value.length) {
			const code = value.charCodeAt(index);
			/** @type {string} */
			let replace = "";
			if (code === 37 && asciiAlphanumeric(value.charCodeAt(index + 1)) && asciiAlphanumeric(value.charCodeAt(index + 2))) skip = 2;
			else if (code < 128) {
				if (!/[!#$&-;=?-Z_a-z~]/.test(String.fromCharCode(code))) replace = String.fromCharCode(code);
			} else if (code > 55295 && code < 57344) {
				const next = value.charCodeAt(index + 1);
				if (code < 56320 && next > 56319 && next < 57344) {
					replace = String.fromCharCode(code, next);
					skip = 1;
				} else replace = "�";
			} else replace = String.fromCharCode(code);
			if (replace) {
				result.push(value.slice(start, index), encodeURIComponent(replace));
				start = index + skip + 1;
				replace = "";
			}
			if (skip) {
				index += skip;
				skip = 0;
			}
		}
		return result.join("") + value.slice(start);
	}
	var init_micromark_util_sanitize_uri = __esmMin((() => {
		init_micromark_util_character();
	}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/handlers/footnote-reference.js
/**
	* Turn an mdast `footnoteReference` node into hast.
	*
	* @param {State} state
	*   Info passed around.
	* @param {FootnoteReference} node
	*   mdast node.
	* @returns {Element}
	*   hast node.
	*/
	function footnoteReference(state, node) {
		const id = String(node.identifier).toUpperCase();
		const safeId = normalizeUri(id.toLowerCase());
		const index = state.footnoteOrder.indexOf(id);
		/** @type {number} */
		let counter;
		if (index === -1) {
			state.footnoteOrder.push(id);
			state.footnoteCounts[id] = 1;
			counter = state.footnoteOrder.length;
		} else {
			state.footnoteCounts[id]++;
			counter = index + 1;
		}
		const reuseCounter = state.footnoteCounts[id];
		/** @type {Element} */
		const link = {
			type: "element",
			tagName: "a",
			properties: {
				href: "#" + state.clobberPrefix + "fn-" + safeId,
				id: state.clobberPrefix + "fnref-" + safeId + (reuseCounter > 1 ? "-" + reuseCounter : ""),
				dataFootnoteRef: true,
				ariaDescribedBy: ["footnote-label"]
			},
			children: [{
				type: "text",
				value: String(counter)
			}]
		};
		state.patch(node, link);
		/** @type {Element} */
		const sup = {
			type: "element",
			tagName: "sup",
			properties: {},
			children: [link]
		};
		state.patch(node, sup);
		return state.applyData(node, sup);
	}
	var init_footnote_reference = __esmMin((() => {
		init_micromark_util_sanitize_uri();
	}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/handlers/footnote.js
/**
	* Turn an mdast `footnote` node into hast.
	*
	* @param {State} state
	*   Info passed around.
	* @param {Footnote} node
	*   mdast node.
	* @returns {Element}
	*   hast node.
	*/
	function footnote(state, node) {
		const footnoteById = state.footnoteById;
		let no = 1;
		while (no in footnoteById) no++;
		const identifier = String(no);
		footnoteById[identifier] = {
			type: "footnoteDefinition",
			identifier,
			children: [{
				type: "paragraph",
				children: node.children
			}],
			position: node.position
		};
		return footnoteReference(state, {
			type: "footnoteReference",
			identifier,
			position: node.position
		});
	}
	var init_footnote = __esmMin((() => {
		init_footnote_reference();
	}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/handlers/heading.js
/**
	* @typedef {import('hast').Element} Element
	* @typedef {import('mdast').Heading} Heading
	* @typedef {import('../state.js').State} State
	*/
	/**
	* Turn an mdast `heading` node into hast.
	*
	* @param {State} state
	*   Info passed around.
	* @param {Heading} node
	*   mdast node.
	* @returns {Element}
	*   hast node.
	*/
	function heading(state, node) {
		/** @type {Element} */
		const result = {
			type: "element",
			tagName: "h" + node.depth,
			properties: {},
			children: state.all(node)
		};
		state.patch(node, result);
		return state.applyData(node, result);
	}
	var init_heading = __esmMin((() => {}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/handlers/html.js
/**
	* @typedef {import('hast').Element} Element
	* @typedef {import('mdast').HTML} Html
	* @typedef {import('../state.js').State} State
	* @typedef {import('../../index.js').Raw} Raw
	*/
	/**
	* Turn an mdast `html` node into hast (`raw` node in dangerous mode, otherwise
	* nothing).
	*
	* @param {State} state
	*   Info passed around.
	* @param {Html} node
	*   mdast node.
	* @returns {Raw | Element | null}
	*   hast node.
	*/
	function html$2(state, node) {
		if (state.dangerous) {
			/** @type {Raw} */
			const result = {
				type: "raw",
				value: node.value
			};
			state.patch(node, result);
			return state.applyData(node, result);
		}
		return null;
	}
	var init_html$1 = __esmMin((() => {
		__name(html$2, "html");
	}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/revert.js
/**
	* @typedef {import('hast').ElementContent} ElementContent
	*
	* @typedef {import('mdast').Content} Content
	* @typedef {import('mdast').Reference} Reference
	* @typedef {import('mdast').Root} Root
	*
	* @typedef {import('./state.js').State} State
	*/
	/**
	* @typedef {Root | Content} Nodes
	* @typedef {Extract<Nodes, Reference>} References
	*/
	/**
	* Return the content of a reference without definition as plain text.
	*
	* @param {State} state
	*   Info passed around.
	* @param {References} node
	*   Reference node (image, link).
	* @returns {ElementContent | Array<ElementContent>}
	*   hast content.
	*/
	function revert(state, node) {
		const subtype = node.referenceType;
		let suffix = "]";
		if (subtype === "collapsed") suffix += "[]";
		else if (subtype === "full") suffix += "[" + (node.label || node.identifier) + "]";
		if (node.type === "imageReference") return {
			type: "text",
			value: "![" + node.alt + suffix
		};
		const contents = state.all(node);
		const head = contents[0];
		if (head && head.type === "text") head.value = "[" + head.value;
		else contents.unshift({
			type: "text",
			value: "["
		});
		const tail = contents[contents.length - 1];
		if (tail && tail.type === "text") tail.value += suffix;
		else contents.push({
			type: "text",
			value: suffix
		});
		return contents;
	}
	var init_revert = __esmMin((() => {}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/handlers/image-reference.js
/**
	* Turn an mdast `imageReference` node into hast.
	*
	* @param {State} state
	*   Info passed around.
	* @param {ImageReference} node
	*   mdast node.
	* @returns {ElementContent | Array<ElementContent>}
	*   hast node.
	*/
	function imageReference(state, node) {
		const def = state.definition(node.identifier);
		if (!def) return revert(state, node);
		/** @type {Properties} */
		const properties = {
			src: normalizeUri(def.url || ""),
			alt: node.alt
		};
		if (def.title !== null && def.title !== void 0) properties.title = def.title;
		/** @type {Element} */
		const result = {
			type: "element",
			tagName: "img",
			properties,
			children: []
		};
		state.patch(node, result);
		return state.applyData(node, result);
	}
	var init_image_reference = __esmMin((() => {
		init_micromark_util_sanitize_uri();
		init_revert();
	}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/handlers/image.js
/**
	* Turn an mdast `image` node into hast.
	*
	* @param {State} state
	*   Info passed around.
	* @param {Image} node
	*   mdast node.
	* @returns {Element}
	*   hast node.
	*/
	function image(state, node) {
		/** @type {Properties} */
		const properties = { src: normalizeUri(node.url) };
		if (node.alt !== null && node.alt !== void 0) properties.alt = node.alt;
		if (node.title !== null && node.title !== void 0) properties.title = node.title;
		/** @type {Element} */
		const result = {
			type: "element",
			tagName: "img",
			properties,
			children: []
		};
		state.patch(node, result);
		return state.applyData(node, result);
	}
	var init_image = __esmMin((() => {
		init_micromark_util_sanitize_uri();
	}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/handlers/inline-code.js
/**
	* @typedef {import('hast').Element} Element
	* @typedef {import('hast').Text} Text
	* @typedef {import('mdast').InlineCode} InlineCode
	* @typedef {import('../state.js').State} State
	*/
	/**
	* Turn an mdast `inlineCode` node into hast.
	*
	* @param {State} state
	*   Info passed around.
	* @param {InlineCode} node
	*   mdast node.
	* @returns {Element}
	*   hast node.
	*/
	function inlineCode(state, node) {
		/** @type {Text} */
		const text = {
			type: "text",
			value: node.value.replace(/\r?\n|\r/g, " ")
		};
		state.patch(node, text);
		/** @type {Element} */
		const result = {
			type: "element",
			tagName: "code",
			properties: {},
			children: [text]
		};
		state.patch(node, result);
		return state.applyData(node, result);
	}
	var init_inline_code = __esmMin((() => {}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/handlers/link-reference.js
/**
	* Turn an mdast `linkReference` node into hast.
	*
	* @param {State} state
	*   Info passed around.
	* @param {LinkReference} node
	*   mdast node.
	* @returns {ElementContent | Array<ElementContent>}
	*   hast node.
	*/
	function linkReference(state, node) {
		const def = state.definition(node.identifier);
		if (!def) return revert(state, node);
		/** @type {Properties} */
		const properties = { href: normalizeUri(def.url || "") };
		if (def.title !== null && def.title !== void 0) properties.title = def.title;
		/** @type {Element} */
		const result = {
			type: "element",
			tagName: "a",
			properties,
			children: state.all(node)
		};
		state.patch(node, result);
		return state.applyData(node, result);
	}
	var init_link_reference = __esmMin((() => {
		init_micromark_util_sanitize_uri();
		init_revert();
	}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/handlers/link.js
/**
	* Turn an mdast `link` node into hast.
	*
	* @param {State} state
	*   Info passed around.
	* @param {Link} node
	*   mdast node.
	* @returns {Element}
	*   hast node.
	*/
	function link(state, node) {
		/** @type {Properties} */
		const properties = { href: normalizeUri(node.url) };
		if (node.title !== null && node.title !== void 0) properties.title = node.title;
		/** @type {Element} */
		const result = {
			type: "element",
			tagName: "a",
			properties,
			children: state.all(node)
		};
		state.patch(node, result);
		return state.applyData(node, result);
	}
	var init_link = __esmMin((() => {
		init_micromark_util_sanitize_uri();
	}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/handlers/list-item.js
/**
	* @typedef {import('hast').Element} Element
	* @typedef {import('hast').ElementContent} ElementContent
	* @typedef {import('hast').Properties} Properties
	* @typedef {import('mdast').Content} Content
	* @typedef {import('mdast').ListItem} ListItem
	* @typedef {import('mdast').Parent} Parent
	* @typedef {import('mdast').Root} Root
	* @typedef {import('../state.js').State} State
	*/
	/**
	* @typedef {Root | Content} Nodes
	* @typedef {Extract<Nodes, Parent>} Parents
	*/
	/**
	* Turn an mdast `listItem` node into hast.
	*
	* @param {State} state
	*   Info passed around.
	* @param {ListItem} node
	*   mdast node.
	* @param {Parents | null | undefined} parent
	*   Parent of `node`.
	* @returns {Element}
	*   hast node.
	*/
	function listItem(state, node, parent) {
		const results = state.all(node);
		const loose = parent ? listLoose(parent) : listItemLoose(node);
		/** @type {Properties} */
		const properties = {};
		/** @type {Array<ElementContent>} */
		const children = [];
		if (typeof node.checked === "boolean") {
			const head = results[0];
			/** @type {Element} */
			let paragraph;
			if (head && head.type === "element" && head.tagName === "p") paragraph = head;
			else {
				paragraph = {
					type: "element",
					tagName: "p",
					properties: {},
					children: []
				};
				results.unshift(paragraph);
			}
			if (paragraph.children.length > 0) paragraph.children.unshift({
				type: "text",
				value: " "
			});
			paragraph.children.unshift({
				type: "element",
				tagName: "input",
				properties: {
					type: "checkbox",
					checked: node.checked,
					disabled: true
				},
				children: []
			});
			properties.className = ["task-list-item"];
		}
		let index = -1;
		while (++index < results.length) {
			const child = results[index];
			if (loose || index !== 0 || child.type !== "element" || child.tagName !== "p") children.push({
				type: "text",
				value: "\n"
			});
			if (child.type === "element" && child.tagName === "p" && !loose) children.push(...child.children);
			else children.push(child);
		}
		const tail = results[results.length - 1];
		if (tail && (loose || tail.type !== "element" || tail.tagName !== "p")) children.push({
			type: "text",
			value: "\n"
		});
		/** @type {Element} */
		const result = {
			type: "element",
			tagName: "li",
			properties,
			children
		};
		state.patch(node, result);
		return state.applyData(node, result);
	}
	/**
	* @param {Parents} node
	* @return {Boolean}
	*/
	function listLoose(node) {
		let loose = false;
		if (node.type === "list") {
			loose = node.spread || false;
			const children = node.children;
			let index = -1;
			while (!loose && ++index < children.length) loose = listItemLoose(children[index]);
		}
		return loose;
	}
	/**
	* @param {ListItem} node
	* @return {Boolean}
	*/
	function listItemLoose(node) {
		const spread = node.spread;
		return spread === void 0 || spread === null ? node.children.length > 1 : spread;
	}
	var init_list_item = __esmMin((() => {}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/handlers/list.js
/**
	* @typedef {import('hast').Element} Element
	* @typedef {import('hast').Properties} Properties
	* @typedef {import('mdast').List} List
	* @typedef {import('../state.js').State} State
	*/
	/**
	* Turn an mdast `list` node into hast.
	*
	* @param {State} state
	*   Info passed around.
	* @param {List} node
	*   mdast node.
	* @returns {Element}
	*   hast node.
	*/
	function list(state, node) {
		/** @type {Properties} */
		const properties = {};
		const results = state.all(node);
		let index = -1;
		if (typeof node.start === "number" && node.start !== 1) properties.start = node.start;
		while (++index < results.length) {
			const child = results[index];
			if (child.type === "element" && child.tagName === "li" && child.properties && Array.isArray(child.properties.className) && child.properties.className.includes("task-list-item")) {
				properties.className = ["contains-task-list"];
				break;
			}
		}
		/** @type {Element} */
		const result = {
			type: "element",
			tagName: node.ordered ? "ol" : "ul",
			properties,
			children: state.wrap(results, true)
		};
		state.patch(node, result);
		return state.applyData(node, result);
	}
	var init_list = __esmMin((() => {}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/handlers/paragraph.js
/**
	* @typedef {import('hast').Element} Element
	* @typedef {import('mdast').Paragraph} Paragraph
	* @typedef {import('../state.js').State} State
	*/
	/**
	* Turn an mdast `paragraph` node into hast.
	*
	* @param {State} state
	*   Info passed around.
	* @param {Paragraph} node
	*   mdast node.
	* @returns {Element}
	*   hast node.
	*/
	function paragraph(state, node) {
		/** @type {Element} */
		const result = {
			type: "element",
			tagName: "p",
			properties: {},
			children: state.all(node)
		};
		state.patch(node, result);
		return state.applyData(node, result);
	}
	var init_paragraph = __esmMin((() => {}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/handlers/root.js
/**
	* @typedef {import('hast').Root} HastRoot
	* @typedef {import('hast').Element} HastElement
	* @typedef {import('mdast').Root} MdastRoot
	* @typedef {import('../state.js').State} State
	*/
	/**
	* Turn an mdast `root` node into hast.
	*
	* @param {State} state
	*   Info passed around.
	* @param {MdastRoot} node
	*   mdast node.
	* @returns {HastRoot | HastElement}
	*   hast node.
	*/
	function root(state, node) {
		/** @type {HastRoot} */
		const result = {
			type: "root",
			children: state.wrap(state.all(node))
		};
		state.patch(node, result);
		return state.applyData(node, result);
	}
	var init_root = __esmMin((() => {}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/handlers/strong.js
/**
	* @typedef {import('hast').Element} Element
	* @typedef {import('mdast').Strong} Strong
	* @typedef {import('../state.js').State} State
	*/
	/**
	* Turn an mdast `strong` node into hast.
	*
	* @param {State} state
	*   Info passed around.
	* @param {Strong} node
	*   mdast node.
	* @returns {Element}
	*   hast node.
	*/
	function strong(state, node) {
		/** @type {Element} */
		const result = {
			type: "element",
			tagName: "strong",
			properties: {},
			children: state.all(node)
		};
		state.patch(node, result);
		return state.applyData(node, result);
	}
	var init_strong = __esmMin((() => {}));

//#endregion
//#region node_modules/unist-util-position/lib/index.js
/**
	* Get the positional info of `node`.
	*
	* @param {NodeLike | Node | null | undefined} [node]
	*   Node.
	* @returns {Position}
	*   Position.
	*/
	function position(node) {
		return {
			start: pointStart(node),
			end: pointEnd(node)
		};
	}
	/**
	* Get the positional info of `node`.
	*
	* @param {'start' | 'end'} type
	*   Side.
	* @returns
	*   Getter.
	*/
	function point(type) {
		return point;
		/**
		* Get the point info of `node` at a bound side.
		*
		* @param {NodeLike | Node | null | undefined} [node]
		* @returns {Point}
		*/
		function point(node) {
			const point = node && node.position && node.position[type] || {};
			return {
				line: point.line || null,
				column: point.column || null,
				offset: point.offset > -1 ? point.offset : null
			};
		}
	}
	var pointStart, pointEnd;
	var init_lib$7 = __esmMin((() => {
		pointStart = point("start");
		pointEnd = point("end");
	}));

//#endregion
//#region node_modules/unist-util-position/index.js
	var init_unist_util_position = __esmMin((() => {
		init_lib$7();
	}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/handlers/table.js
/**
	* Turn an mdast `table` node into hast.
	*
	* @param {State} state
	*   Info passed around.
	* @param {Table} node
	*   mdast node.
	* @returns {Element}
	*   hast node.
	*/
	function table(state, node) {
		const rows = state.all(node);
		const firstRow = rows.shift();
		/** @type {Array<Element>} */
		const tableContent = [];
		if (firstRow) {
			/** @type {Element} */
			const head = {
				type: "element",
				tagName: "thead",
				properties: {},
				children: state.wrap([firstRow], true)
			};
			state.patch(node.children[0], head);
			tableContent.push(head);
		}
		if (rows.length > 0) {
			/** @type {Element} */
			const body = {
				type: "element",
				tagName: "tbody",
				properties: {},
				children: state.wrap(rows, true)
			};
			const start = pointStart(node.children[1]);
			const end = pointEnd(node.children[node.children.length - 1]);
			if (start.line && end.line) body.position = {
				start,
				end
			};
			tableContent.push(body);
		}
		/** @type {Element} */
		const result = {
			type: "element",
			tagName: "table",
			properties: {},
			children: state.wrap(tableContent, true)
		};
		state.patch(node, result);
		return state.applyData(node, result);
	}
	var init_table = __esmMin((() => {
		init_unist_util_position();
	}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/handlers/table-row.js
/**
	* @typedef {import('hast').Properties} Properties
	* @typedef {import('hast').Element} Element
	* @typedef {import('hast').ElementContent} ElementContent
	* @typedef {import('mdast').Content} Content
	* @typedef {import('mdast').Parent} Parent
	* @typedef {import('mdast').Root} Root
	* @typedef {import('mdast').TableRow} TableRow
	* @typedef {import('../state.js').State} State
	*/
	/**
	* @typedef {Root | Content} Nodes
	* @typedef {Extract<Nodes, Parent>} Parents
	*/
	/**
	* Turn an mdast `tableRow` node into hast.
	*
	* @param {State} state
	*   Info passed around.
	* @param {TableRow} node
	*   mdast node.
	* @param {Parents | null | undefined} parent
	*   Parent of `node`.
	* @returns {Element}
	*   hast node.
	*/
	function tableRow(state, node, parent) {
		const siblings = parent ? parent.children : void 0;
		const tagName = (siblings ? siblings.indexOf(node) : 1) === 0 ? "th" : "td";
		const align = parent && parent.type === "table" ? parent.align : void 0;
		const length = align ? align.length : node.children.length;
		let cellIndex = -1;
		/** @type {Array<ElementContent>} */
		const cells = [];
		while (++cellIndex < length) {
			const cell = node.children[cellIndex];
			/** @type {Properties} */
			const properties = {};
			const alignValue = align ? align[cellIndex] : void 0;
			if (alignValue) properties.align = alignValue;
			/** @type {Element} */
			let result = {
				type: "element",
				tagName,
				properties,
				children: []
			};
			if (cell) {
				result.children = state.all(cell);
				state.patch(cell, result);
				result = state.applyData(node, result);
			}
			cells.push(result);
		}
		/** @type {Element} */
		const result = {
			type: "element",
			tagName: "tr",
			properties: {},
			children: state.wrap(cells, true)
		};
		state.patch(node, result);
		return state.applyData(node, result);
	}
	var init_table_row = __esmMin((() => {}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/handlers/table-cell.js
/**
	* @typedef {import('hast').Element} Element
	* @typedef {import('mdast').TableCell} TableCell
	* @typedef {import('../state.js').State} State
	*/
	/**
	* Turn an mdast `tableCell` node into hast.
	*
	* @param {State} state
	*   Info passed around.
	* @param {TableCell} node
	*   mdast node.
	* @returns {Element}
	*   hast node.
	*/
	function tableCell(state, node) {
		/** @type {Element} */
		const result = {
			type: "element",
			tagName: "td",
			properties: {},
			children: state.all(node)
		};
		state.patch(node, result);
		return state.applyData(node, result);
	}
	var init_table_cell = __esmMin((() => {}));

//#endregion
//#region node_modules/trim-lines/index.js
/**
	* Remove initial and final spaces and tabs at the line breaks in `value`.
	* Does not trim initial and final spaces and tabs of the value itself.
	*
	* @param {string} value
	*   Value to trim.
	* @returns {string}
	*   Trimmed value.
	*/
	function trimLines(value) {
		const source = String(value);
		const search = /\r?\n|\r/g;
		let match = search.exec(source);
		let last = 0;
		/** @type {Array<string>} */
		const lines = [];
		while (match) {
			lines.push(trimLine(source.slice(last, match.index), last > 0, true), match[0]);
			last = match.index + match[0].length;
			match = search.exec(source);
		}
		lines.push(trimLine(source.slice(last), last > 0, false));
		return lines.join("");
	}
	/**
	* @param {string} value
	*   Line to trim.
	* @param {boolean} start
	*   Whether to trim the start of the line.
	* @param {boolean} end
	*   Whether to trim the end of the line.
	* @returns {string}
	*   Trimmed line.
	*/
	function trimLine(value, start, end) {
		let startIndex = 0;
		let endIndex = value.length;
		if (start) {
			let code = value.codePointAt(startIndex);
			while (code === tab || code === space) {
				startIndex++;
				code = value.codePointAt(startIndex);
			}
		}
		if (end) {
			let code = value.codePointAt(endIndex - 1);
			while (code === tab || code === space) {
				endIndex--;
				code = value.codePointAt(endIndex - 1);
			}
		}
		return endIndex > startIndex ? value.slice(startIndex, endIndex) : "";
	}
	var tab, space;
	var init_trim_lines = __esmMin((() => {
		tab = 9;
		space = 32;
	}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/handlers/text.js
/**
	* Turn an mdast `text` node into hast.
	*
	* @param {State} state
	*   Info passed around.
	* @param {MdastText} node
	*   mdast node.
	* @returns {HastText | HastElement}
	*   hast node.
	*/
	function text(state, node) {
		/** @type {HastText} */
		const result = {
			type: "text",
			value: trimLines(String(node.value))
		};
		state.patch(node, result);
		return state.applyData(node, result);
	}
	var init_text = __esmMin((() => {
		init_trim_lines();
	}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/handlers/thematic-break.js
/**
	* @typedef {import('hast').Element} Element
	* @typedef {import('mdast').ThematicBreak} ThematicBreak
	* @typedef {import('../state.js').State} State
	*/
	/**
	* Turn an mdast `thematicBreak` node into hast.
	*
	* @param {State} state
	*   Info passed around.
	* @param {ThematicBreak} node
	*   mdast node.
	* @returns {Element}
	*   hast node.
	*/
	function thematicBreak(state, node) {
		/** @type {Element} */
		const result = {
			type: "element",
			tagName: "hr",
			properties: {},
			children: []
		};
		state.patch(node, result);
		return state.applyData(node, result);
	}
	var init_thematic_break = __esmMin((() => {}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/handlers/index.js
	function ignore() {
		return null;
	}
	var handlers;
	var init_handlers = __esmMin((() => {
		init_blockquote();
		init_break();
		init_code();
		init_delete();
		init_emphasis();
		init_footnote_reference();
		init_footnote();
		init_heading();
		init_html$1();
		init_image_reference();
		init_image();
		init_inline_code();
		init_link_reference();
		init_link();
		init_list_item();
		init_list();
		init_paragraph();
		init_root();
		init_strong();
		init_table();
		init_table_row();
		init_table_cell();
		init_text();
		init_thematic_break();
		handlers = {
			blockquote,
			break: hardBreak,
			code,
			delete: strikethrough,
			emphasis,
			footnoteReference,
			footnote,
			heading,
			html: html$2,
			imageReference,
			image,
			inlineCode,
			linkReference,
			link,
			listItem,
			list,
			paragraph,
			root,
			strong,
			table,
			tableCell,
			tableRow,
			text,
			thematicBreak,
			toml: ignore,
			yaml: ignore,
			definition: ignore,
			footnoteDefinition: ignore
		};
	}));

//#endregion
//#region node_modules/unist-util-is/lib/index.js
/**
	* @param {Array<string | Props | TestFunctionAnything>} tests
	* @returns {AssertAnything}
	*/
	function anyFactory(tests) {
		/** @type {Array<AssertAnything>} */
		const checks = [];
		let index = -1;
		while (++index < tests.length) checks[index] = convert(tests[index]);
		return castFactory(any);
		/**
		* @this {unknown}
		* @param {Array<unknown>} parameters
		* @returns {boolean}
		*/
		function any(...parameters) {
			let index = -1;
			while (++index < checks.length) if (checks[index].call(this, ...parameters)) return true;
			return false;
		}
	}
	/**
	* Turn an object into a test for a node with a certain fields.
	*
	* @param {Props} check
	* @returns {AssertAnything}
	*/
	function propsFactory(check) {
		return castFactory(all);
		/**
		* @param {Node} node
		* @returns {boolean}
		*/
		function all(node) {
			/** @type {string} */
			let key;
			for (key in check) if (node[key] !== check[key]) return false;
			return true;
		}
	}
	/**
	* Turn a string into a test for a node with a certain type.
	*
	* @param {string} check
	* @returns {AssertAnything}
	*/
	function typeFactory(check) {
		return castFactory(type);
		/**
		* @param {Node} node
		*/
		function type(node) {
			return node && node.type === check;
		}
	}
	/**
	* Turn a custom test into a test for a node that passes that test.
	*
	* @param {TestFunctionAnything} check
	* @returns {AssertAnything}
	*/
	function castFactory(check) {
		return assertion;
		/**
		* @this {unknown}
		* @param {unknown} node
		* @param {Array<unknown>} parameters
		* @returns {boolean}
		*/
		function assertion(node, ...parameters) {
			return Boolean(node && typeof node === "object" && "type" in node && Boolean(check.call(this, node, ...parameters)));
		}
	}
	function ok() {
		return true;
	}
	var convert;
	var init_lib$6 = __esmMin((() => {
		convert = (
		/**
		* @param {Test} [test]
		* @returns {AssertAnything}
		*/
function(test) {
			if (test === void 0 || test === null) return ok;
			if (typeof test === "string") return typeFactory(test);
			if (typeof test === "object") return Array.isArray(test) ? anyFactory(test) : propsFactory(test);
			if (typeof test === "function") return castFactory(test);
			throw new Error("Expected function, string, or object as test");
		});
	}));

//#endregion
//#region node_modules/unist-util-is/index.js
	var init_unist_util_is = __esmMin((() => {
		init_lib$6();
	}));

//#endregion
//#region node_modules/unist-util-visit-parents/lib/color.browser.js
/**
	* @param {string} d
	* @returns {string}
	*/
	function color(d) {
		return d;
	}
	var init_color_browser = __esmMin((() => {}));

//#endregion
//#region node_modules/unist-util-visit-parents/lib/index.js
/**
	* Turn a return value into a clean result.
	*
	* @param {VisitorResult} value
	*   Valid return values from visitors.
	* @returns {ActionTuple}
	*   Clean result.
	*/
	function toResult(value) {
		if (Array.isArray(value)) return value;
		if (typeof value === "number") return [true, value];
		return [value];
	}
	var CONTINUE, EXIT, SKIP, visitParents;
	var init_lib$5 = __esmMin((() => {
		init_unist_util_is();
		init_color_browser();
		CONTINUE = true;
		EXIT = false;
		SKIP = "skip";
		visitParents = (
		/**
		* @param {Node} tree
		* @param {Test} test
		* @param {Visitor<Node>} visitor
		* @param {boolean | null | undefined} [reverse]
		* @returns {void}
		*/
function(tree, test, visitor, reverse) {
			if (typeof test === "function" && typeof visitor !== "function") {
				reverse = visitor;
				visitor = test;
				test = null;
			}
			const is = convert(test);
			const step = reverse ? -1 : 1;
			factory(tree, void 0, [])();
			/**
			* @param {Node} node
			* @param {number | undefined} index
			* @param {Array<Parent>} parents
			*/
			function factory(node, index, parents) {
				/** @type {Record<string, unknown>} */
				const value = node && typeof node === "object" ? node : {};
				if (typeof value.type === "string") {
					const name = typeof value.tagName === "string" ? value.tagName : typeof value.name === "string" ? value.name : void 0;
					Object.defineProperty(visit, "name", { value: "node (" + color(node.type + (name ? "<" + name + ">" : "")) + ")" });
				}
				return visit;
				function visit() {
					/** @type {ActionTuple} */
					let result = [];
					/** @type {ActionTuple} */
					let subresult;
					/** @type {number} */
					let offset;
					/** @type {Array<Parent>} */
					let grandparents;
					if (!test || is(node, index, parents[parents.length - 1] || null)) {
						result = toResult(visitor(node, parents));
						if (result[0] === false) return result;
					}
					if (node.children && result[0] !== "skip") {
						offset = (reverse ? node.children.length : -1) + step;
						grandparents = parents.concat(node);
						while (offset > -1 && offset < node.children.length) {
							subresult = factory(node.children[offset], offset, grandparents)();
							if (subresult[0] === false) return subresult;
							offset = typeof subresult[1] === "number" ? subresult[1] : offset + step;
						}
					}
					return result;
				}
			}
		});
	}));

//#endregion
//#region node_modules/unist-util-visit-parents/index.js
	var init_unist_util_visit_parents = __esmMin((() => {
		init_lib$5();
	}));

//#endregion
//#region node_modules/unist-util-visit/lib/index.js
	var visit;
	var init_lib$4 = __esmMin((() => {
		init_unist_util_visit_parents();
		visit = (
		/**
		* @param {Node} tree
		* @param {Test} test
		* @param {Visitor} visitor
		* @param {boolean | null | undefined} [reverse]
		* @returns {void}
		*/
function(tree, test, visitor, reverse) {
			if (typeof test === "function" && typeof visitor !== "function") {
				reverse = visitor;
				visitor = test;
				test = null;
			}
			visitParents(tree, test, overload, reverse);
			/**
			* @param {Node} node
			* @param {Array<Parent>} parents
			*/
			function overload(node, parents) {
				const parent = parents[parents.length - 1];
				return visitor(node, parent ? parent.children.indexOf(node) : null, parent);
			}
		});
	}));

//#endregion
//#region node_modules/unist-util-visit/index.js
	var init_unist_util_visit = __esmMin((() => {
		init_lib$4();
	}));

//#endregion
//#region node_modules/unist-util-generated/lib/index.js
/**
	* @typedef PointLike
	* @property {number | null | undefined} [line]
	* @property {number | null | undefined} [column]
	* @property {number | null | undefined} [offset]
	*
	* @typedef PositionLike
	* @property {PointLike | null | undefined} [start]
	* @property {PointLike | null | undefined} [end]
	*
	* @typedef NodeLike
	* @property {PositionLike | null | undefined} [position]
	*/
	/**
	* Check if `node` is generated.
	*
	* @param {NodeLike | null | undefined} [node]
	*   Node to check.
	* @returns {boolean}
	*   Whether `node` is generated (does not have positional info).
	*/
	function generated(node) {
		return !node || !node.position || !node.position.start || !node.position.start.line || !node.position.start.column || !node.position.end || !node.position.end.line || !node.position.end.column;
	}
	var init_lib$3 = __esmMin((() => {}));

//#endregion
//#region node_modules/unist-util-generated/index.js
	var init_unist_util_generated = __esmMin((() => {
		init_lib$3();
	}));

//#endregion
//#region node_modules/mdast-util-definitions/lib/index.js
/**
	* Find definitions in `tree`.
	*
	* Uses CommonMark precedence, which means that earlier definitions are
	* preferred over duplicate later definitions.
	*
	* @param {Node} tree
	*   Tree to check.
	* @returns {GetDefinition}
	*   Getter.
	*/
	function definitions(tree) {
		/** @type {Record<string, Definition>} */
		const cache = Object.create(null);
		if (!tree || !tree.type) throw new Error("mdast-util-definitions expected node");
		visit(tree, "definition", (definition) => {
			const id = clean(definition.identifier);
			if (id && !own$4.call(cache, id)) cache[id] = definition;
		});
		return definition;
		/** @type {GetDefinition} */
		function definition(identifier) {
			const id = clean(identifier);
			return id && own$4.call(cache, id) ? cache[id] : null;
		}
	}
	/**
	* @param {string | null | undefined} [value]
	* @returns {string}
	*/
	function clean(value) {
		return String(value || "").toUpperCase();
	}
	var own$4;
	var init_lib$2 = __esmMin((() => {
		init_unist_util_visit();
		own$4 = {}.hasOwnProperty;
	}));

//#endregion
//#region node_modules/mdast-util-definitions/index.js
	var init_mdast_util_definitions = __esmMin((() => {
		init_lib$2();
	}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/state.js
/**
	* Create `state` from an mdast tree.
	*
	* @param {MdastNodes} tree
	*   mdast node to transform.
	* @param {Options | null | undefined} [options]
	*   Configuration.
	* @returns {State}
	*   `state` function.
	*/
	function createState(tree, options) {
		const settings = options || {};
		const dangerous = settings.allowDangerousHtml || false;
		/** @type {Record<string, MdastFootnoteDefinition>} */
		const footnoteById = {};
		state.dangerous = dangerous;
		state.clobberPrefix = settings.clobberPrefix === void 0 || settings.clobberPrefix === null ? "user-content-" : settings.clobberPrefix;
		state.footnoteLabel = settings.footnoteLabel || "Footnotes";
		state.footnoteLabelTagName = settings.footnoteLabelTagName || "h2";
		state.footnoteLabelProperties = settings.footnoteLabelProperties || { className: ["sr-only"] };
		state.footnoteBackLabel = settings.footnoteBackLabel || "Back to content";
		state.unknownHandler = settings.unknownHandler;
		state.passThrough = settings.passThrough;
		state.handlers = _objectSpread2(_objectSpread2({}, handlers), settings.handlers);
		state.definition = definitions(tree);
		state.footnoteById = footnoteById;
		/** @type {Array<string>} */
		state.footnoteOrder = [];
		/** @type {Record<string, number>} */
		state.footnoteCounts = {};
		state.patch = patch;
		state.applyData = applyData;
		state.one = oneBound;
		state.all = allBound;
		state.wrap = wrap;
		state.augment = augment;
		visit(tree, "footnoteDefinition", (definition) => {
			const id = String(definition.identifier).toUpperCase();
			if (!own$3.call(footnoteById, id)) footnoteById[id] = definition;
		});
		return state;
		/**
		* Finalise the created `right`, a hast node, from `left`, an mdast node.
		*
		* @param {MdastNodeWithData | PositionLike | null | undefined} left
		* @param {HastElementContent} right
		* @returns {HastElementContent}
		*/
		/* c8 ignore start */
		function augment(left, right) {
			if (left && "data" in left && left.data) {
				/** @type {MdastData} */
				const data = left.data;
				if (data.hName) {
					if (right.type !== "element") right = {
						type: "element",
						tagName: "",
						properties: {},
						children: []
					};
					right.tagName = data.hName;
				}
				if (right.type === "element" && data.hProperties) right.properties = _objectSpread2(_objectSpread2({}, right.properties), data.hProperties);
				if ("children" in right && right.children && data.hChildren) right.children = data.hChildren;
			}
			if (left) {
				const ctx = "type" in left ? left : { position: left };
				if (!generated(ctx)) right.position = {
					start: pointStart(ctx),
					end: pointEnd(ctx)
				};
			}
			return right;
		}
		/* c8 ignore stop */
		/**
		* Create an element for `node`.
		*
		* @type {HFunctionProps}
		*/
		/* c8 ignore start */
		function state(node, tagName, props, children) {
			if (Array.isArray(props)) {
				children = props;
				props = {};
			}
			return augment(node, {
				type: "element",
				tagName,
				properties: props || {},
				children: children || []
			});
		}
		/* c8 ignore stop */
		/**
		* Transform an mdast node into a hast node.
		*
		* @param {MdastNodes} node
		*   mdast node.
		* @param {MdastParents | null | undefined} [parent]
		*   Parent of `node`.
		* @returns {HastElementContent | Array<HastElementContent> | null | undefined}
		*   Resulting hast node.
		*/
		function oneBound(node, parent) {
			return one(state, node, parent);
		}
		/**
		* Transform the children of an mdast node into hast nodes.
		*
		* @param {MdastNodes} parent
		*   mdast node to compile
		* @returns {Array<HastElementContent>}
		*   Resulting hast nodes.
		*/
		function allBound(parent) {
			return all(state, parent);
		}
	}
	/**
	* Copy a node’s positional info.
	*
	* @param {MdastNodes} from
	*   mdast node to copy from.
	* @param {HastNodes} to
	*   hast node to copy into.
	* @returns {void}
	*   Nothing.
	*/
	function patch(from, to) {
		if (from.position) to.position = position(from);
	}
	/**
	* Honor the `data` of `from` and maybe generate an element instead of `to`.
	*
	* @template {HastNodes} Type
	*   Node type.
	* @param {MdastNodes} from
	*   mdast node to use data from.
	* @param {Type} to
	*   hast node to change.
	* @returns {Type | HastElement}
	*   Nothing.
	*/
	function applyData(from, to) {
		/** @type {Type | HastElement} */
		let result = to;
		if (from && from.data) {
			const hName = from.data.hName;
			const hChildren = from.data.hChildren;
			const hProperties = from.data.hProperties;
			if (typeof hName === "string") if (result.type === "element") result.tagName = hName;
			else result = {
				type: "element",
				tagName: hName,
				properties: {},
				children: []
			};
			if (result.type === "element" && hProperties) result.properties = _objectSpread2(_objectSpread2({}, result.properties), hProperties);
			if ("children" in result && result.children && hChildren !== null && hChildren !== void 0) result.children = hChildren;
		}
		return result;
	}
	/**
	* Transform an mdast node into a hast node.
	*
	* @param {State} state
	*   Info passed around.
	* @param {MdastNodes} node
	*   mdast node.
	* @param {MdastParents | null | undefined} [parent]
	*   Parent of `node`.
	* @returns {HastElementContent | Array<HastElementContent> | null | undefined}
	*   Resulting hast node.
	*/
	function one(state, node, parent) {
		const type = node && node.type;
		if (!type) throw new Error("Expected node, got `" + node + "`");
		if (own$3.call(state.handlers, type)) return state.handlers[type](state, node, parent);
		if (state.passThrough && state.passThrough.includes(type)) return "children" in node ? _objectSpread2(_objectSpread2({}, node), {}, { children: all(state, node) }) : node;
		if (state.unknownHandler) return state.unknownHandler(state, node, parent);
		return defaultUnknownHandler(state, node);
	}
	/**
	* Transform the children of an mdast node into hast nodes.
	*
	* @param {State} state
	*   Info passed around.
	* @param {MdastNodes} parent
	*   mdast node to compile
	* @returns {Array<HastElementContent>}
	*   Resulting hast nodes.
	*/
	function all(state, parent) {
		/** @type {Array<HastElementContent>} */
		const values = [];
		if ("children" in parent) {
			const nodes = parent.children;
			let index = -1;
			while (++index < nodes.length) {
				const result = one(state, nodes[index], parent);
				if (result) {
					if (index && nodes[index - 1].type === "break") {
						if (!Array.isArray(result) && result.type === "text") result.value = result.value.replace(/^\s+/, "");
						if (!Array.isArray(result) && result.type === "element") {
							const head = result.children[0];
							if (head && head.type === "text") head.value = head.value.replace(/^\s+/, "");
						}
					}
					if (Array.isArray(result)) values.push(...result);
					else values.push(result);
				}
			}
		}
		return values;
	}
	/**
	* Transform an unknown node.
	*
	* @param {State} state
	*   Info passed around.
	* @param {MdastNodes} node
	*   Unknown mdast node.
	* @returns {HastText | HastElement}
	*   Resulting hast node.
	*/
	function defaultUnknownHandler(state, node) {
		const data = node.data || {};
		/** @type {HastText | HastElement} */
		const result = "value" in node && !(own$3.call(data, "hProperties") || own$3.call(data, "hChildren")) ? {
			type: "text",
			value: node.value
		} : {
			type: "element",
			tagName: "div",
			properties: {},
			children: all(state, node)
		};
		state.patch(node, result);
		return state.applyData(node, result);
	}
	/**
	* Wrap `nodes` with line endings between each node.
	*
	* @template {HastContent} Type
	*   Node type.
	* @param {Array<Type>} nodes
	*   List of nodes to wrap.
	* @param {boolean | null | undefined} [loose=false]
	*   Whether to add line endings at start and end.
	* @returns {Array<Type | HastText>}
	*   Wrapped nodes.
	*/
	function wrap(nodes, loose) {
		/** @type {Array<Type | HastText>} */
		const result = [];
		let index = -1;
		if (loose) result.push({
			type: "text",
			value: "\n"
		});
		while (++index < nodes.length) {
			if (index) result.push({
				type: "text",
				value: "\n"
			});
			result.push(nodes[index]);
		}
		if (loose && nodes.length > 0) result.push({
			type: "text",
			value: "\n"
		});
		return result;
	}
	var own$3;
	var init_state = __esmMin((() => {
		init_unist_util_visit();
		init_unist_util_position();
		init_unist_util_generated();
		init_mdast_util_definitions();
		init_handlers();
		init_objectSpread2();
		own$3 = {}.hasOwnProperty;
	}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/footer.js
/**
	* Generate a hast footer for called footnote definitions.
	*
	* @param {State} state
	*   Info passed around.
	* @returns {Element | undefined}
	*   `section` element or `undefined`.
	*/
	function footer(state) {
		/** @type {Array<ElementContent>} */
		const listItems = [];
		let index = -1;
		while (++index < state.footnoteOrder.length) {
			const def = state.footnoteById[state.footnoteOrder[index]];
			if (!def) continue;
			const content = state.all(def);
			const id = String(def.identifier).toUpperCase();
			const safeId = normalizeUri(id.toLowerCase());
			let referenceIndex = 0;
			/** @type {Array<ElementContent>} */
			const backReferences = [];
			while (++referenceIndex <= state.footnoteCounts[id]) {
				/** @type {Element} */
				const backReference = {
					type: "element",
					tagName: "a",
					properties: {
						href: "#" + state.clobberPrefix + "fnref-" + safeId + (referenceIndex > 1 ? "-" + referenceIndex : ""),
						dataFootnoteBackref: true,
						className: ["data-footnote-backref"],
						ariaLabel: state.footnoteBackLabel
					},
					children: [{
						type: "text",
						value: "↩"
					}]
				};
				if (referenceIndex > 1) backReference.children.push({
					type: "element",
					tagName: "sup",
					children: [{
						type: "text",
						value: String(referenceIndex)
					}]
				});
				if (backReferences.length > 0) backReferences.push({
					type: "text",
					value: " "
				});
				backReferences.push(backReference);
			}
			const tail = content[content.length - 1];
			if (tail && tail.type === "element" && tail.tagName === "p") {
				const tailTail = tail.children[tail.children.length - 1];
				if (tailTail && tailTail.type === "text") tailTail.value += " ";
				else tail.children.push({
					type: "text",
					value: " "
				});
				tail.children.push(...backReferences);
			} else content.push(...backReferences);
			/** @type {Element} */
			const listItem = {
				type: "element",
				tagName: "li",
				properties: { id: state.clobberPrefix + "fn-" + safeId },
				children: state.wrap(content, true)
			};
			state.patch(def, listItem);
			listItems.push(listItem);
		}
		if (listItems.length === 0) return;
		return {
			type: "element",
			tagName: "section",
			properties: {
				dataFootnotes: true,
				className: ["footnotes"]
			},
			children: [
				{
					type: "element",
					tagName: state.footnoteLabelTagName,
					properties: _objectSpread2(_objectSpread2({}, JSON.parse(JSON.stringify(state.footnoteLabelProperties))), {}, { id: "footnote-label" }),
					children: [{
						type: "text",
						value: state.footnoteLabel
					}]
				},
				{
					type: "text",
					value: "\n"
				},
				{
					type: "element",
					tagName: "ol",
					properties: {},
					children: state.wrap(listItems, true)
				},
				{
					type: "text",
					value: "\n"
				}
			]
		};
	}
	var init_footer = __esmMin((() => {
		init_micromark_util_sanitize_uri();
		init_objectSpread2();
	}));

//#endregion
//#region node_modules/mdast-util-to-hast/lib/index.js
/**
	* Transform mdast to hast.
	*
	* ##### Notes
	*
	* ###### HTML
	*
	* Raw HTML is available in mdast as `html` nodes and can be embedded in hast
	* as semistandard `raw` nodes.
	* Most utilities ignore `raw` nodes but two notable ones don’t:
	*
	* *   `hast-util-to-html` also has an option `allowDangerousHtml` which will
	*     output the raw HTML.
	*     This is typically discouraged as noted by the option name but is useful
	*     if you completely trust authors
	* *   `hast-util-raw` can handle the raw embedded HTML strings by parsing them
	*     into standard hast nodes (`element`, `text`, etc).
	*     This is a heavy task as it needs a full HTML parser, but it is the only
	*     way to support untrusted content
	*
	* ###### Footnotes
	*
	* Many options supported here relate to footnotes.
	* Footnotes are not specified by CommonMark, which we follow by default.
	* They are supported by GitHub, so footnotes can be enabled in markdown with
	* `mdast-util-gfm`.
	*
	* The options `footnoteBackLabel` and `footnoteLabel` define natural language
	* that explains footnotes, which is hidden for sighted users but shown to
	* assistive technology.
	* When your page is not in English, you must define translated values.
	*
	* Back references use ARIA attributes, but the section label itself uses a
	* heading that is hidden with an `sr-only` class.
	* To show it to sighted users, define different attributes in
	* `footnoteLabelProperties`.
	*
	* ###### Clobbering
	*
	* Footnotes introduces a problem, as it links footnote calls to footnote
	* definitions on the page through `id` attributes generated from user content,
	* which results in DOM clobbering.
	*
	* DOM clobbering is this:
	*
	* ```html
	* <p id=x></p>
	* <script>alert(x) // `x` now refers to the DOM `p#x` element<\/script>
	* ```
	*
	* Elements by their ID are made available by browsers on the `window` object,
	* which is a security risk.
	* Using a prefix solves this problem.
	*
	* More information on how to handle clobbering and the prefix is explained in
	* Example: headings (DOM clobbering) in `rehype-sanitize`.
	*
	* ###### Unknown nodes
	*
	* Unknown nodes are nodes with a type that isn’t in `handlers` or `passThrough`.
	* The default behavior for unknown nodes is:
	*
	* *   when the node has a `value` (and doesn’t have `data.hName`,
	*     `data.hProperties`, or `data.hChildren`, see later), create a hast `text`
	*     node
	* *   otherwise, create a `<div>` element (which could be changed with
	*     `data.hName`), with its children mapped from mdast to hast as well
	*
	* This behavior can be changed by passing an `unknownHandler`.
	*
	* @param {MdastNodes} tree
	*   mdast tree.
	* @param {Options | null | undefined} [options]
	*   Configuration.
	* @returns {HastNodes | null | undefined}
	*   hast tree.
	*/
	function toHast(tree, options) {
		const state = createState(tree, options);
		const node = state.one(tree, null);
		const foot = footer(state);
		if (foot) node.children.push({
			type: "text",
			value: "\n"
		}, foot);
		return Array.isArray(node) ? {
			type: "root",
			children: node
		} : node;
	}
	var init_lib$1 = __esmMin((() => {
		init_footer();
		init_state();
	}));

//#endregion
//#region node_modules/mdast-util-to-hast/index.js
	var init_mdast_util_to_hast = __esmMin((() => {
		init_handlers();
		init_state();
		init_lib$1();
	}));

//#endregion
//#region node_modules/remark-rehype/lib/index.js
/**
	* Bridge-mode.
	* Runs the destination with the new hast tree.
	*
	* @type {import('unified').Plugin<[Processor, Options?], MdastRoot>}
	*/
	function bridge(destination, options) {
		return (node, file, next) => {
			destination.run(toHast(node, options), file, (error) => {
				next(error);
			});
		};
	}
	/**
	* Mutate-mode.
	* Further plugins run on the hast tree.
	*
	* @type {import('unified').Plugin<[Options?]|void[], MdastRoot, HastRoot>}
	*/
	function mutate(options) {
		return (node) => toHast(node, options);
	}
	var remarkRehype;
	var init_lib = __esmMin((() => {
		init_mdast_util_to_hast();
		remarkRehype = (function(destination, options) {
			return destination && "run" in destination ? bridge(destination, options) : mutate(destination || options);
		});
	}));

//#endregion
//#region node_modules/remark-rehype/index.js
	var init_remark_rehype = __esmMin((() => {
		init_mdast_util_to_hast();
		init_lib();
	}));

//#endregion
//#region node_modules/property-information/lib/util/schema.js
	var Schema;
	var init_schema = __esmMin((() => {
		Schema = class {
			/**
			* @constructor
			* @param {Properties} property
			* @param {Normal} normal
			* @param {string} [space]
			*/
			constructor(property, normal, space) {
				this.property = property;
				this.normal = normal;
				if (space) this.space = space;
			}
		};
		/** @type {Properties} */
		Schema.prototype.property = {};
		/** @type {Normal} */
		Schema.prototype.normal = {};
		/** @type {string|null} */
		Schema.prototype.space = null;
	}));

//#endregion
//#region node_modules/property-information/lib/util/merge.js
/**
	* @param {Schema[]} definitions
	* @param {string} [space]
	* @returns {Schema}
	*/
	function merge(definitions, space) {
		/** @type {Properties} */
		const property = {};
		/** @type {Normal} */
		const normal = {};
		let index = -1;
		while (++index < definitions.length) {
			Object.assign(property, definitions[index].property);
			Object.assign(normal, definitions[index].normal);
		}
		return new Schema(property, normal, space);
	}
	var init_merge = __esmMin((() => {
		init_schema();
	}));

//#endregion
//#region node_modules/property-information/lib/normalize.js
/**
	* @param {string} value
	* @returns {string}
	*/
	function normalize(value) {
		return value.toLowerCase();
	}
	var init_normalize = __esmMin((() => {}));

//#endregion
//#region node_modules/property-information/lib/util/info.js
	var Info;
	var init_info = __esmMin((() => {
		Info = class {
			/**
			* @constructor
			* @param {string} property
			* @param {string} attribute
			*/
			constructor(property, attribute) {
				/** @type {string} */
				this.property = property;
				/** @type {string} */
				this.attribute = attribute;
			}
		};
		/** @type {string|null} */
		Info.prototype.space = null;
		Info.prototype.boolean = false;
		Info.prototype.booleanish = false;
		Info.prototype.overloadedBoolean = false;
		Info.prototype.number = false;
		Info.prototype.commaSeparated = false;
		Info.prototype.spaceSeparated = false;
		Info.prototype.commaOrSpaceSeparated = false;
		Info.prototype.mustUseProperty = false;
		Info.prototype.defined = false;
	}));

//#endregion
//#region node_modules/property-information/lib/util/types.js
	var types_exports = /* @__PURE__ */ __exportAll({
		boolean: () => boolean,
		booleanish: () => booleanish,
		commaOrSpaceSeparated: () => commaOrSpaceSeparated,
		commaSeparated: () => commaSeparated,
		number: () => number,
		overloadedBoolean: () => overloadedBoolean,
		spaceSeparated: () => spaceSeparated
	});
	function increment() {
		return Math.pow(2, ++powers);
	}
	var powers, boolean, booleanish, overloadedBoolean, number, spaceSeparated, commaSeparated, commaOrSpaceSeparated;
	var init_types = __esmMin((() => {
		powers = 0;
		boolean = increment();
		booleanish = increment();
		overloadedBoolean = increment();
		number = increment();
		spaceSeparated = increment();
		commaSeparated = increment();
		commaOrSpaceSeparated = increment();
	}));

//#endregion
//#region node_modules/property-information/lib/util/defined-info.js
/**
	* @param {DefinedInfo} values
	* @param {string} key
	* @param {unknown} value
	*/
	function mark(values, key, value) {
		if (value) values[key] = value;
	}
	var checks, DefinedInfo;
	var init_defined_info = __esmMin((() => {
		init_info();
		init_types();
		checks = Object.keys(types_exports);
		DefinedInfo = class extends Info {
			/**
			* @constructor
			* @param {string} property
			* @param {string} attribute
			* @param {number|null} [mask]
			* @param {string} [space]
			*/
			constructor(property, attribute, mask, space) {
				let index = -1;
				super(property, attribute);
				mark(this, "space", space);
				if (typeof mask === "number") while (++index < checks.length) {
					const check = checks[index];
					mark(this, checks[index], (mask & types_exports[check]) === types_exports[check]);
				}
			}
		};
		DefinedInfo.prototype.defined = true;
	}));

//#endregion
//#region node_modules/property-information/lib/util/create.js
/**
	* @param {Definition} definition
	* @returns {Schema}
	*/
	function create(definition) {
		/** @type {Properties} */
		const property = {};
		/** @type {Normal} */
		const normal = {};
		/** @type {string} */
		let prop;
		for (prop in definition.properties) if (own$2.call(definition.properties, prop)) {
			const value = definition.properties[prop];
			const info = new DefinedInfo(prop, definition.transform(definition.attributes || {}, prop), value, definition.space);
			if (definition.mustUseProperty && definition.mustUseProperty.includes(prop)) info.mustUseProperty = true;
			property[prop] = info;
			normal[normalize(prop)] = prop;
			normal[normalize(info.attribute)] = prop;
		}
		return new Schema(property, normal, definition.space);
	}
	var own$2;
	var init_create = __esmMin((() => {
		init_normalize();
		init_schema();
		init_defined_info();
		own$2 = {}.hasOwnProperty;
	}));

//#endregion
//#region node_modules/property-information/lib/xlink.js
	var xlink;
	var init_xlink = __esmMin((() => {
		init_create();
		xlink = create({
			space: "xlink",
			transform(_, prop) {
				return "xlink:" + prop.slice(5).toLowerCase();
			},
			properties: {
				xLinkActuate: null,
				xLinkArcRole: null,
				xLinkHref: null,
				xLinkRole: null,
				xLinkShow: null,
				xLinkTitle: null,
				xLinkType: null
			}
		});
	}));

//#endregion
//#region node_modules/property-information/lib/xml.js
	var xml;
	var init_xml = __esmMin((() => {
		init_create();
		xml = create({
			space: "xml",
			transform(_, prop) {
				return "xml:" + prop.slice(3).toLowerCase();
			},
			properties: {
				xmlLang: null,
				xmlBase: null,
				xmlSpace: null
			}
		});
	}));

//#endregion
//#region node_modules/property-information/lib/util/case-sensitive-transform.js
/**
	* @param {Record<string, string>} attributes
	* @param {string} attribute
	* @returns {string}
	*/
	function caseSensitiveTransform(attributes, attribute) {
		return attribute in attributes ? attributes[attribute] : attribute;
	}
	var init_case_sensitive_transform = __esmMin((() => {}));

//#endregion
//#region node_modules/property-information/lib/util/case-insensitive-transform.js
/**
	* @param {Record<string, string>} attributes
	* @param {string} property
	* @returns {string}
	*/
	function caseInsensitiveTransform(attributes, property) {
		return caseSensitiveTransform(attributes, property.toLowerCase());
	}
	var init_case_insensitive_transform = __esmMin((() => {
		init_case_sensitive_transform();
	}));

//#endregion
//#region node_modules/property-information/lib/xmlns.js
	var xmlns;
	var init_xmlns = __esmMin((() => {
		init_create();
		init_case_insensitive_transform();
		xmlns = create({
			space: "xmlns",
			attributes: { xmlnsxlink: "xmlns:xlink" },
			transform: caseInsensitiveTransform,
			properties: {
				xmlns: null,
				xmlnsXLink: null
			}
		});
	}));

//#endregion
//#region node_modules/property-information/lib/aria.js
	var aria;
	var init_aria = __esmMin((() => {
		init_types();
		init_create();
		aria = create({
			transform(_, prop) {
				return prop === "role" ? prop : "aria-" + prop.slice(4).toLowerCase();
			},
			properties: {
				ariaActiveDescendant: null,
				ariaAtomic: booleanish,
				ariaAutoComplete: null,
				ariaBusy: booleanish,
				ariaChecked: booleanish,
				ariaColCount: number,
				ariaColIndex: number,
				ariaColSpan: number,
				ariaControls: spaceSeparated,
				ariaCurrent: null,
				ariaDescribedBy: spaceSeparated,
				ariaDetails: null,
				ariaDisabled: booleanish,
				ariaDropEffect: spaceSeparated,
				ariaErrorMessage: null,
				ariaExpanded: booleanish,
				ariaFlowTo: spaceSeparated,
				ariaGrabbed: booleanish,
				ariaHasPopup: null,
				ariaHidden: booleanish,
				ariaInvalid: null,
				ariaKeyShortcuts: null,
				ariaLabel: null,
				ariaLabelledBy: spaceSeparated,
				ariaLevel: number,
				ariaLive: null,
				ariaModal: booleanish,
				ariaMultiLine: booleanish,
				ariaMultiSelectable: booleanish,
				ariaOrientation: null,
				ariaOwns: spaceSeparated,
				ariaPlaceholder: null,
				ariaPosInSet: number,
				ariaPressed: booleanish,
				ariaReadOnly: booleanish,
				ariaRelevant: null,
				ariaRequired: booleanish,
				ariaRoleDescription: spaceSeparated,
				ariaRowCount: number,
				ariaRowIndex: number,
				ariaRowSpan: number,
				ariaSelected: booleanish,
				ariaSetSize: number,
				ariaSort: null,
				ariaValueMax: number,
				ariaValueMin: number,
				ariaValueNow: number,
				ariaValueText: null,
				role: null
			}
		});
	}));

//#endregion
//#region node_modules/property-information/lib/html.js
	var html$1;
	var init_html = __esmMin((() => {
		init_types();
		init_create();
		init_case_insensitive_transform();
		html$1 = create({
			space: "html",
			attributes: {
				acceptcharset: "accept-charset",
				classname: "class",
				htmlfor: "for",
				httpequiv: "http-equiv"
			},
			transform: caseInsensitiveTransform,
			mustUseProperty: [
				"checked",
				"multiple",
				"muted",
				"selected"
			],
			properties: {
				abbr: null,
				accept: commaSeparated,
				acceptCharset: spaceSeparated,
				accessKey: spaceSeparated,
				action: null,
				allow: null,
				allowFullScreen: boolean,
				allowPaymentRequest: boolean,
				allowUserMedia: boolean,
				alt: null,
				as: null,
				async: boolean,
				autoCapitalize: null,
				autoComplete: spaceSeparated,
				autoFocus: boolean,
				autoPlay: boolean,
				blocking: spaceSeparated,
				capture: null,
				charSet: null,
				checked: boolean,
				cite: null,
				className: spaceSeparated,
				cols: number,
				colSpan: null,
				content: null,
				contentEditable: booleanish,
				controls: boolean,
				controlsList: spaceSeparated,
				coords: number | commaSeparated,
				crossOrigin: null,
				data: null,
				dateTime: null,
				decoding: null,
				default: boolean,
				defer: boolean,
				dir: null,
				dirName: null,
				disabled: boolean,
				download: overloadedBoolean,
				draggable: booleanish,
				encType: null,
				enterKeyHint: null,
				fetchPriority: null,
				form: null,
				formAction: null,
				formEncType: null,
				formMethod: null,
				formNoValidate: boolean,
				formTarget: null,
				headers: spaceSeparated,
				height: number,
				hidden: boolean,
				high: number,
				href: null,
				hrefLang: null,
				htmlFor: spaceSeparated,
				httpEquiv: spaceSeparated,
				id: null,
				imageSizes: null,
				imageSrcSet: null,
				inert: boolean,
				inputMode: null,
				integrity: null,
				is: null,
				isMap: boolean,
				itemId: null,
				itemProp: spaceSeparated,
				itemRef: spaceSeparated,
				itemScope: boolean,
				itemType: spaceSeparated,
				kind: null,
				label: null,
				lang: null,
				language: null,
				list: null,
				loading: null,
				loop: boolean,
				low: number,
				manifest: null,
				max: null,
				maxLength: number,
				media: null,
				method: null,
				min: null,
				minLength: number,
				multiple: boolean,
				muted: boolean,
				name: null,
				nonce: null,
				noModule: boolean,
				noValidate: boolean,
				onAbort: null,
				onAfterPrint: null,
				onAuxClick: null,
				onBeforeMatch: null,
				onBeforePrint: null,
				onBeforeToggle: null,
				onBeforeUnload: null,
				onBlur: null,
				onCancel: null,
				onCanPlay: null,
				onCanPlayThrough: null,
				onChange: null,
				onClick: null,
				onClose: null,
				onContextLost: null,
				onContextMenu: null,
				onContextRestored: null,
				onCopy: null,
				onCueChange: null,
				onCut: null,
				onDblClick: null,
				onDrag: null,
				onDragEnd: null,
				onDragEnter: null,
				onDragExit: null,
				onDragLeave: null,
				onDragOver: null,
				onDragStart: null,
				onDrop: null,
				onDurationChange: null,
				onEmptied: null,
				onEnded: null,
				onError: null,
				onFocus: null,
				onFormData: null,
				onHashChange: null,
				onInput: null,
				onInvalid: null,
				onKeyDown: null,
				onKeyPress: null,
				onKeyUp: null,
				onLanguageChange: null,
				onLoad: null,
				onLoadedData: null,
				onLoadedMetadata: null,
				onLoadEnd: null,
				onLoadStart: null,
				onMessage: null,
				onMessageError: null,
				onMouseDown: null,
				onMouseEnter: null,
				onMouseLeave: null,
				onMouseMove: null,
				onMouseOut: null,
				onMouseOver: null,
				onMouseUp: null,
				onOffline: null,
				onOnline: null,
				onPageHide: null,
				onPageShow: null,
				onPaste: null,
				onPause: null,
				onPlay: null,
				onPlaying: null,
				onPopState: null,
				onProgress: null,
				onRateChange: null,
				onRejectionHandled: null,
				onReset: null,
				onResize: null,
				onScroll: null,
				onScrollEnd: null,
				onSecurityPolicyViolation: null,
				onSeeked: null,
				onSeeking: null,
				onSelect: null,
				onSlotChange: null,
				onStalled: null,
				onStorage: null,
				onSubmit: null,
				onSuspend: null,
				onTimeUpdate: null,
				onToggle: null,
				onUnhandledRejection: null,
				onUnload: null,
				onVolumeChange: null,
				onWaiting: null,
				onWheel: null,
				open: boolean,
				optimum: number,
				pattern: null,
				ping: spaceSeparated,
				placeholder: null,
				playsInline: boolean,
				popover: null,
				popoverTarget: null,
				popoverTargetAction: null,
				poster: null,
				preload: null,
				readOnly: boolean,
				referrerPolicy: null,
				rel: spaceSeparated,
				required: boolean,
				reversed: boolean,
				rows: number,
				rowSpan: number,
				sandbox: spaceSeparated,
				scope: null,
				scoped: boolean,
				seamless: boolean,
				selected: boolean,
				shadowRootClonable: boolean,
				shadowRootDelegatesFocus: boolean,
				shadowRootMode: null,
				shape: null,
				size: number,
				sizes: null,
				slot: null,
				span: number,
				spellCheck: booleanish,
				src: null,
				srcDoc: null,
				srcLang: null,
				srcSet: null,
				start: number,
				step: null,
				style: null,
				tabIndex: number,
				target: null,
				title: null,
				translate: null,
				type: null,
				typeMustMatch: boolean,
				useMap: null,
				value: booleanish,
				width: number,
				wrap: null,
				writingSuggestions: null,
				align: null,
				aLink: null,
				archive: spaceSeparated,
				axis: null,
				background: null,
				bgColor: null,
				border: number,
				borderColor: null,
				bottomMargin: number,
				cellPadding: null,
				cellSpacing: null,
				char: null,
				charOff: null,
				classId: null,
				clear: null,
				code: null,
				codeBase: null,
				codeType: null,
				color: null,
				compact: boolean,
				declare: boolean,
				event: null,
				face: null,
				frame: null,
				frameBorder: null,
				hSpace: number,
				leftMargin: number,
				link: null,
				longDesc: null,
				lowSrc: null,
				marginHeight: number,
				marginWidth: number,
				noResize: boolean,
				noHref: boolean,
				noShade: boolean,
				noWrap: boolean,
				object: null,
				profile: null,
				prompt: null,
				rev: null,
				rightMargin: number,
				rules: null,
				scheme: null,
				scrolling: booleanish,
				standby: null,
				summary: null,
				text: null,
				topMargin: number,
				valueType: null,
				version: null,
				vAlign: null,
				vLink: null,
				vSpace: number,
				allowTransparency: null,
				autoCorrect: null,
				autoSave: null,
				disablePictureInPicture: boolean,
				disableRemotePlayback: boolean,
				prefix: null,
				property: null,
				results: number,
				security: null,
				unselectable: null
			}
		});
	}));

//#endregion
//#region node_modules/property-information/lib/svg.js
	var svg$1;
	var init_svg = __esmMin((() => {
		init_types();
		init_create();
		init_case_sensitive_transform();
		svg$1 = create({
			space: "svg",
			attributes: {
				accentHeight: "accent-height",
				alignmentBaseline: "alignment-baseline",
				arabicForm: "arabic-form",
				baselineShift: "baseline-shift",
				capHeight: "cap-height",
				className: "class",
				clipPath: "clip-path",
				clipRule: "clip-rule",
				colorInterpolation: "color-interpolation",
				colorInterpolationFilters: "color-interpolation-filters",
				colorProfile: "color-profile",
				colorRendering: "color-rendering",
				crossOrigin: "crossorigin",
				dataType: "datatype",
				dominantBaseline: "dominant-baseline",
				enableBackground: "enable-background",
				fillOpacity: "fill-opacity",
				fillRule: "fill-rule",
				floodColor: "flood-color",
				floodOpacity: "flood-opacity",
				fontFamily: "font-family",
				fontSize: "font-size",
				fontSizeAdjust: "font-size-adjust",
				fontStretch: "font-stretch",
				fontStyle: "font-style",
				fontVariant: "font-variant",
				fontWeight: "font-weight",
				glyphName: "glyph-name",
				glyphOrientationHorizontal: "glyph-orientation-horizontal",
				glyphOrientationVertical: "glyph-orientation-vertical",
				hrefLang: "hreflang",
				horizAdvX: "horiz-adv-x",
				horizOriginX: "horiz-origin-x",
				horizOriginY: "horiz-origin-y",
				imageRendering: "image-rendering",
				letterSpacing: "letter-spacing",
				lightingColor: "lighting-color",
				markerEnd: "marker-end",
				markerMid: "marker-mid",
				markerStart: "marker-start",
				navDown: "nav-down",
				navDownLeft: "nav-down-left",
				navDownRight: "nav-down-right",
				navLeft: "nav-left",
				navNext: "nav-next",
				navPrev: "nav-prev",
				navRight: "nav-right",
				navUp: "nav-up",
				navUpLeft: "nav-up-left",
				navUpRight: "nav-up-right",
				onAbort: "onabort",
				onActivate: "onactivate",
				onAfterPrint: "onafterprint",
				onBeforePrint: "onbeforeprint",
				onBegin: "onbegin",
				onCancel: "oncancel",
				onCanPlay: "oncanplay",
				onCanPlayThrough: "oncanplaythrough",
				onChange: "onchange",
				onClick: "onclick",
				onClose: "onclose",
				onCopy: "oncopy",
				onCueChange: "oncuechange",
				onCut: "oncut",
				onDblClick: "ondblclick",
				onDrag: "ondrag",
				onDragEnd: "ondragend",
				onDragEnter: "ondragenter",
				onDragExit: "ondragexit",
				onDragLeave: "ondragleave",
				onDragOver: "ondragover",
				onDragStart: "ondragstart",
				onDrop: "ondrop",
				onDurationChange: "ondurationchange",
				onEmptied: "onemptied",
				onEnd: "onend",
				onEnded: "onended",
				onError: "onerror",
				onFocus: "onfocus",
				onFocusIn: "onfocusin",
				onFocusOut: "onfocusout",
				onHashChange: "onhashchange",
				onInput: "oninput",
				onInvalid: "oninvalid",
				onKeyDown: "onkeydown",
				onKeyPress: "onkeypress",
				onKeyUp: "onkeyup",
				onLoad: "onload",
				onLoadedData: "onloadeddata",
				onLoadedMetadata: "onloadedmetadata",
				onLoadStart: "onloadstart",
				onMessage: "onmessage",
				onMouseDown: "onmousedown",
				onMouseEnter: "onmouseenter",
				onMouseLeave: "onmouseleave",
				onMouseMove: "onmousemove",
				onMouseOut: "onmouseout",
				onMouseOver: "onmouseover",
				onMouseUp: "onmouseup",
				onMouseWheel: "onmousewheel",
				onOffline: "onoffline",
				onOnline: "ononline",
				onPageHide: "onpagehide",
				onPageShow: "onpageshow",
				onPaste: "onpaste",
				onPause: "onpause",
				onPlay: "onplay",
				onPlaying: "onplaying",
				onPopState: "onpopstate",
				onProgress: "onprogress",
				onRateChange: "onratechange",
				onRepeat: "onrepeat",
				onReset: "onreset",
				onResize: "onresize",
				onScroll: "onscroll",
				onSeeked: "onseeked",
				onSeeking: "onseeking",
				onSelect: "onselect",
				onShow: "onshow",
				onStalled: "onstalled",
				onStorage: "onstorage",
				onSubmit: "onsubmit",
				onSuspend: "onsuspend",
				onTimeUpdate: "ontimeupdate",
				onToggle: "ontoggle",
				onUnload: "onunload",
				onVolumeChange: "onvolumechange",
				onWaiting: "onwaiting",
				onZoom: "onzoom",
				overlinePosition: "overline-position",
				overlineThickness: "overline-thickness",
				paintOrder: "paint-order",
				panose1: "panose-1",
				pointerEvents: "pointer-events",
				referrerPolicy: "referrerpolicy",
				renderingIntent: "rendering-intent",
				shapeRendering: "shape-rendering",
				stopColor: "stop-color",
				stopOpacity: "stop-opacity",
				strikethroughPosition: "strikethrough-position",
				strikethroughThickness: "strikethrough-thickness",
				strokeDashArray: "stroke-dasharray",
				strokeDashOffset: "stroke-dashoffset",
				strokeLineCap: "stroke-linecap",
				strokeLineJoin: "stroke-linejoin",
				strokeMiterLimit: "stroke-miterlimit",
				strokeOpacity: "stroke-opacity",
				strokeWidth: "stroke-width",
				tabIndex: "tabindex",
				textAnchor: "text-anchor",
				textDecoration: "text-decoration",
				textRendering: "text-rendering",
				transformOrigin: "transform-origin",
				typeOf: "typeof",
				underlinePosition: "underline-position",
				underlineThickness: "underline-thickness",
				unicodeBidi: "unicode-bidi",
				unicodeRange: "unicode-range",
				unitsPerEm: "units-per-em",
				vAlphabetic: "v-alphabetic",
				vHanging: "v-hanging",
				vIdeographic: "v-ideographic",
				vMathematical: "v-mathematical",
				vectorEffect: "vector-effect",
				vertAdvY: "vert-adv-y",
				vertOriginX: "vert-origin-x",
				vertOriginY: "vert-origin-y",
				wordSpacing: "word-spacing",
				writingMode: "writing-mode",
				xHeight: "x-height",
				playbackOrder: "playbackorder",
				timelineBegin: "timelinebegin"
			},
			transform: caseSensitiveTransform,
			properties: {
				about: commaOrSpaceSeparated,
				accentHeight: number,
				accumulate: null,
				additive: null,
				alignmentBaseline: null,
				alphabetic: number,
				amplitude: number,
				arabicForm: null,
				ascent: number,
				attributeName: null,
				attributeType: null,
				azimuth: number,
				bandwidth: null,
				baselineShift: null,
				baseFrequency: null,
				baseProfile: null,
				bbox: null,
				begin: null,
				bias: number,
				by: null,
				calcMode: null,
				capHeight: number,
				className: spaceSeparated,
				clip: null,
				clipPath: null,
				clipPathUnits: null,
				clipRule: null,
				color: null,
				colorInterpolation: null,
				colorInterpolationFilters: null,
				colorProfile: null,
				colorRendering: null,
				content: null,
				contentScriptType: null,
				contentStyleType: null,
				crossOrigin: null,
				cursor: null,
				cx: null,
				cy: null,
				d: null,
				dataType: null,
				defaultAction: null,
				descent: number,
				diffuseConstant: number,
				direction: null,
				display: null,
				dur: null,
				divisor: number,
				dominantBaseline: null,
				download: boolean,
				dx: null,
				dy: null,
				edgeMode: null,
				editable: null,
				elevation: number,
				enableBackground: null,
				end: null,
				event: null,
				exponent: number,
				externalResourcesRequired: null,
				fill: null,
				fillOpacity: number,
				fillRule: null,
				filter: null,
				filterRes: null,
				filterUnits: null,
				floodColor: null,
				floodOpacity: null,
				focusable: null,
				focusHighlight: null,
				fontFamily: null,
				fontSize: null,
				fontSizeAdjust: null,
				fontStretch: null,
				fontStyle: null,
				fontVariant: null,
				fontWeight: null,
				format: null,
				fr: null,
				from: null,
				fx: null,
				fy: null,
				g1: commaSeparated,
				g2: commaSeparated,
				glyphName: commaSeparated,
				glyphOrientationHorizontal: null,
				glyphOrientationVertical: null,
				glyphRef: null,
				gradientTransform: null,
				gradientUnits: null,
				handler: null,
				hanging: number,
				hatchContentUnits: null,
				hatchUnits: null,
				height: null,
				href: null,
				hrefLang: null,
				horizAdvX: number,
				horizOriginX: number,
				horizOriginY: number,
				id: null,
				ideographic: number,
				imageRendering: null,
				initialVisibility: null,
				in: null,
				in2: null,
				intercept: number,
				k: number,
				k1: number,
				k2: number,
				k3: number,
				k4: number,
				kernelMatrix: commaOrSpaceSeparated,
				kernelUnitLength: null,
				keyPoints: null,
				keySplines: null,
				keyTimes: null,
				kerning: null,
				lang: null,
				lengthAdjust: null,
				letterSpacing: null,
				lightingColor: null,
				limitingConeAngle: number,
				local: null,
				markerEnd: null,
				markerMid: null,
				markerStart: null,
				markerHeight: null,
				markerUnits: null,
				markerWidth: null,
				mask: null,
				maskContentUnits: null,
				maskUnits: null,
				mathematical: null,
				max: null,
				media: null,
				mediaCharacterEncoding: null,
				mediaContentEncodings: null,
				mediaSize: number,
				mediaTime: null,
				method: null,
				min: null,
				mode: null,
				name: null,
				navDown: null,
				navDownLeft: null,
				navDownRight: null,
				navLeft: null,
				navNext: null,
				navPrev: null,
				navRight: null,
				navUp: null,
				navUpLeft: null,
				navUpRight: null,
				numOctaves: null,
				observer: null,
				offset: null,
				onAbort: null,
				onActivate: null,
				onAfterPrint: null,
				onBeforePrint: null,
				onBegin: null,
				onCancel: null,
				onCanPlay: null,
				onCanPlayThrough: null,
				onChange: null,
				onClick: null,
				onClose: null,
				onCopy: null,
				onCueChange: null,
				onCut: null,
				onDblClick: null,
				onDrag: null,
				onDragEnd: null,
				onDragEnter: null,
				onDragExit: null,
				onDragLeave: null,
				onDragOver: null,
				onDragStart: null,
				onDrop: null,
				onDurationChange: null,
				onEmptied: null,
				onEnd: null,
				onEnded: null,
				onError: null,
				onFocus: null,
				onFocusIn: null,
				onFocusOut: null,
				onHashChange: null,
				onInput: null,
				onInvalid: null,
				onKeyDown: null,
				onKeyPress: null,
				onKeyUp: null,
				onLoad: null,
				onLoadedData: null,
				onLoadedMetadata: null,
				onLoadStart: null,
				onMessage: null,
				onMouseDown: null,
				onMouseEnter: null,
				onMouseLeave: null,
				onMouseMove: null,
				onMouseOut: null,
				onMouseOver: null,
				onMouseUp: null,
				onMouseWheel: null,
				onOffline: null,
				onOnline: null,
				onPageHide: null,
				onPageShow: null,
				onPaste: null,
				onPause: null,
				onPlay: null,
				onPlaying: null,
				onPopState: null,
				onProgress: null,
				onRateChange: null,
				onRepeat: null,
				onReset: null,
				onResize: null,
				onScroll: null,
				onSeeked: null,
				onSeeking: null,
				onSelect: null,
				onShow: null,
				onStalled: null,
				onStorage: null,
				onSubmit: null,
				onSuspend: null,
				onTimeUpdate: null,
				onToggle: null,
				onUnload: null,
				onVolumeChange: null,
				onWaiting: null,
				onZoom: null,
				opacity: null,
				operator: null,
				order: null,
				orient: null,
				orientation: null,
				origin: null,
				overflow: null,
				overlay: null,
				overlinePosition: number,
				overlineThickness: number,
				paintOrder: null,
				panose1: null,
				path: null,
				pathLength: number,
				patternContentUnits: null,
				patternTransform: null,
				patternUnits: null,
				phase: null,
				ping: spaceSeparated,
				pitch: null,
				playbackOrder: null,
				pointerEvents: null,
				points: null,
				pointsAtX: number,
				pointsAtY: number,
				pointsAtZ: number,
				preserveAlpha: null,
				preserveAspectRatio: null,
				primitiveUnits: null,
				propagate: null,
				property: commaOrSpaceSeparated,
				r: null,
				radius: null,
				referrerPolicy: null,
				refX: null,
				refY: null,
				rel: commaOrSpaceSeparated,
				rev: commaOrSpaceSeparated,
				renderingIntent: null,
				repeatCount: null,
				repeatDur: null,
				requiredExtensions: commaOrSpaceSeparated,
				requiredFeatures: commaOrSpaceSeparated,
				requiredFonts: commaOrSpaceSeparated,
				requiredFormats: commaOrSpaceSeparated,
				resource: null,
				restart: null,
				result: null,
				rotate: null,
				rx: null,
				ry: null,
				scale: null,
				seed: null,
				shapeRendering: null,
				side: null,
				slope: null,
				snapshotTime: null,
				specularConstant: number,
				specularExponent: number,
				spreadMethod: null,
				spacing: null,
				startOffset: null,
				stdDeviation: null,
				stemh: null,
				stemv: null,
				stitchTiles: null,
				stopColor: null,
				stopOpacity: null,
				strikethroughPosition: number,
				strikethroughThickness: number,
				string: null,
				stroke: null,
				strokeDashArray: commaOrSpaceSeparated,
				strokeDashOffset: null,
				strokeLineCap: null,
				strokeLineJoin: null,
				strokeMiterLimit: number,
				strokeOpacity: number,
				strokeWidth: null,
				style: null,
				surfaceScale: number,
				syncBehavior: null,
				syncBehaviorDefault: null,
				syncMaster: null,
				syncTolerance: null,
				syncToleranceDefault: null,
				systemLanguage: commaOrSpaceSeparated,
				tabIndex: number,
				tableValues: null,
				target: null,
				targetX: number,
				targetY: number,
				textAnchor: null,
				textDecoration: null,
				textRendering: null,
				textLength: null,
				timelineBegin: null,
				title: null,
				transformBehavior: null,
				type: null,
				typeOf: commaOrSpaceSeparated,
				to: null,
				transform: null,
				transformOrigin: null,
				u1: null,
				u2: null,
				underlinePosition: number,
				underlineThickness: number,
				unicode: null,
				unicodeBidi: null,
				unicodeRange: null,
				unitsPerEm: number,
				values: null,
				vAlphabetic: number,
				vMathematical: number,
				vectorEffect: null,
				vHanging: number,
				vIdeographic: number,
				version: null,
				vertAdvY: number,
				vertOriginX: number,
				vertOriginY: number,
				viewBox: null,
				viewTarget: null,
				visibility: null,
				width: null,
				widths: null,
				wordSpacing: null,
				writingMode: null,
				x: null,
				x1: null,
				x2: null,
				xChannelSelector: null,
				xHeight: number,
				y: null,
				y1: null,
				y2: null,
				yChannelSelector: null,
				z: null,
				zoomAndPan: null
			}
		});
	}));

//#endregion
//#region node_modules/property-information/lib/find.js
/**
	* @param {Schema} schema
	* @param {string} value
	* @returns {Info}
	*/
	function find(schema, value) {
		const normal = normalize(value);
		let prop = value;
		let Type = Info;
		if (normal in schema.normal) return schema.property[schema.normal[normal]];
		if (normal.length > 4 && normal.slice(0, 4) === "data" && valid.test(value)) {
			if (value.charAt(4) === "-") {
				const rest = value.slice(5).replace(dash, camelcase);
				prop = "data" + rest.charAt(0).toUpperCase() + rest.slice(1);
			} else {
				const rest = value.slice(4);
				if (!dash.test(rest)) {
					let dashes = rest.replace(cap, kebab);
					if (dashes.charAt(0) !== "-") dashes = "-" + dashes;
					value = "data" + dashes;
				}
			}
			Type = DefinedInfo;
		}
		return new Type(prop, value);
	}
	/**
	* @param {string} $0
	* @returns {string}
	*/
	function kebab($0) {
		return "-" + $0.toLowerCase();
	}
	/**
	* @param {string} $0
	* @returns {string}
	*/
	function camelcase($0) {
		return $0.charAt(1).toUpperCase();
	}
	var valid, dash, cap;
	var init_find = __esmMin((() => {
		init_normalize();
		init_defined_info();
		init_info();
		valid = /^data[-\w.:]+$/i;
		dash = /-[a-z]/g;
		cap = /[A-Z]/g;
	}));

//#endregion
//#region node_modules/property-information/lib/hast-to-react.js
	var hastToReact;
	var init_hast_to_react = __esmMin((() => {
		hastToReact = {
			classId: "classID",
			dataType: "datatype",
			itemId: "itemID",
			strokeDashArray: "strokeDasharray",
			strokeDashOffset: "strokeDashoffset",
			strokeLineCap: "strokeLinecap",
			strokeLineJoin: "strokeLinejoin",
			strokeMiterLimit: "strokeMiterlimit",
			typeOf: "typeof",
			xLinkActuate: "xlinkActuate",
			xLinkArcRole: "xlinkArcrole",
			xLinkHref: "xlinkHref",
			xLinkRole: "xlinkRole",
			xLinkShow: "xlinkShow",
			xLinkTitle: "xlinkTitle",
			xLinkType: "xlinkType",
			xmlnsXLink: "xmlnsXlink"
		};
	}));

//#endregion
//#region node_modules/property-information/index.js
	var html, svg;
	var init_property_information = __esmMin((() => {
		init_merge();
		init_xlink();
		init_xml();
		init_xmlns();
		init_aria();
		init_html();
		init_svg();
		init_find();
		init_hast_to_react();
		init_normalize();
		html = merge([
			xml,
			xlink,
			xmlns,
			aria,
			html$1
		], "html");
		svg = merge([
			xml,
			xlink,
			xmlns,
			aria,
			svg$1
		], "svg");
	}));

//#endregion
//#region node_modules/react-markdown/lib/rehype-filter.js
/**
	* @typedef {import('unist').Node} Node
	* @typedef {import('hast').Root} Root
	* @typedef {import('hast').Element} Element
	*
	* @callback AllowElement
	* @param {Element} element
	* @param {number} index
	* @param {Element|Root} parent
	* @returns {boolean|undefined}
	*
	* @typedef Options
	* @property {Array<string>} [allowedElements]
	* @property {Array<string>} [disallowedElements=[]]
	* @property {AllowElement} [allowElement]
	* @property {boolean} [unwrapDisallowed=false]
	*/
	/**
	* @type {import('unified').Plugin<[Options], Root>}
	*/
	function rehypeFilter(options) {
		if (options.allowedElements && options.disallowedElements) throw new TypeError("Only one of `allowedElements` and `disallowedElements` should be defined");
		if (options.allowedElements || options.disallowedElements || options.allowElement) return (tree) => {
			visit(tree, "element", (node, index, parent_) => {
				const parent = parent_;
				/** @type {boolean|undefined} */
				let remove;
				if (options.allowedElements) remove = !options.allowedElements.includes(node.tagName);
				else if (options.disallowedElements) remove = options.disallowedElements.includes(node.tagName);
				if (!remove && options.allowElement && typeof index === "number") remove = !options.allowElement(node, index, parent);
				if (remove && typeof index === "number") {
					if (options.unwrapDisallowed && node.children) parent.children.splice(index, 1, ...node.children);
					else parent.children.splice(index, 1);
					return index;
				}
			});
		};
	}
	var init_rehype_filter = __esmMin((() => {
		init_unist_util_visit();
	}));

//#endregion
//#region node_modules/react-is/cjs/react-is.development.js
/**
	* @license React
	* react-is.development.js
	*
	* Copyright (c) Facebook, Inc. and its affiliates.
	*
	* This source code is licensed under the MIT license found in the
	* LICENSE file in the root directory of this source tree.
	*/
	var require_react_is_development = /* @__PURE__ */ __commonJSMin(((exports) => {
		(function() {
			"use strict";
			var REACT_ELEMENT_TYPE = Symbol.for("react.element");
			var REACT_PORTAL_TYPE = Symbol.for("react.portal");
			var REACT_FRAGMENT_TYPE = Symbol.for("react.fragment");
			var REACT_STRICT_MODE_TYPE = Symbol.for("react.strict_mode");
			var REACT_PROFILER_TYPE = Symbol.for("react.profiler");
			var REACT_PROVIDER_TYPE = Symbol.for("react.provider");
			var REACT_CONTEXT_TYPE = Symbol.for("react.context");
			var REACT_SERVER_CONTEXT_TYPE = Symbol.for("react.server_context");
			var REACT_FORWARD_REF_TYPE = Symbol.for("react.forward_ref");
			var REACT_SUSPENSE_TYPE = Symbol.for("react.suspense");
			var REACT_SUSPENSE_LIST_TYPE = Symbol.for("react.suspense_list");
			var REACT_MEMO_TYPE = Symbol.for("react.memo");
			var REACT_LAZY_TYPE = Symbol.for("react.lazy");
			var REACT_OFFSCREEN_TYPE = Symbol.for("react.offscreen");
			var enableScopeAPI = false;
			var enableCacheElement = false;
			var enableTransitionTracing = false;
			var enableLegacyHidden = false;
			var enableDebugTracing = false;
			var REACT_MODULE_REFERENCE = Symbol.for("react.module.reference");
			function isValidElementType(type) {
				if (typeof type === "string" || typeof type === "function") return true;
				if (type === REACT_FRAGMENT_TYPE || type === REACT_PROFILER_TYPE || enableDebugTracing || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || enableLegacyHidden || type === REACT_OFFSCREEN_TYPE || enableScopeAPI || enableCacheElement || enableTransitionTracing) return true;
				if (typeof type === "object" && type !== null) {
					if (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || type.$$typeof === REACT_MODULE_REFERENCE || type.getModuleId !== void 0) return true;
				}
				return false;
			}
			function typeOf(object) {
				if (typeof object === "object" && object !== null) {
					var $$typeof = object.$$typeof;
					switch ($$typeof) {
						case REACT_ELEMENT_TYPE:
							var type = object.type;
							switch (type) {
								case REACT_FRAGMENT_TYPE:
								case REACT_PROFILER_TYPE:
								case REACT_STRICT_MODE_TYPE:
								case REACT_SUSPENSE_TYPE:
								case REACT_SUSPENSE_LIST_TYPE: return type;
								default:
									var $$typeofType = type && type.$$typeof;
									switch ($$typeofType) {
										case REACT_SERVER_CONTEXT_TYPE:
										case REACT_CONTEXT_TYPE:
										case REACT_FORWARD_REF_TYPE:
										case REACT_LAZY_TYPE:
										case REACT_MEMO_TYPE:
										case REACT_PROVIDER_TYPE: return $$typeofType;
										default: return $$typeof;
									}
							}
						case REACT_PORTAL_TYPE: return $$typeof;
					}
				}
			}
			var ContextConsumer = REACT_CONTEXT_TYPE;
			var ContextProvider = REACT_PROVIDER_TYPE;
			var Element = REACT_ELEMENT_TYPE;
			var ForwardRef = REACT_FORWARD_REF_TYPE;
			var Fragment = REACT_FRAGMENT_TYPE;
			var Lazy = REACT_LAZY_TYPE;
			var Memo = REACT_MEMO_TYPE;
			var Portal = REACT_PORTAL_TYPE;
			var Profiler = REACT_PROFILER_TYPE;
			var StrictMode = REACT_STRICT_MODE_TYPE;
			var Suspense = REACT_SUSPENSE_TYPE;
			var SuspenseList = REACT_SUSPENSE_LIST_TYPE;
			var hasWarnedAboutDeprecatedIsAsyncMode = false;
			var hasWarnedAboutDeprecatedIsConcurrentMode = false;
			function isAsyncMode(object) {
				if (!hasWarnedAboutDeprecatedIsAsyncMode) {
					hasWarnedAboutDeprecatedIsAsyncMode = true;
					console["warn"]("The ReactIs.isAsyncMode() alias has been deprecated, and will be removed in React 18+.");
				}
				return false;
			}
			function isConcurrentMode(object) {
				if (!hasWarnedAboutDeprecatedIsConcurrentMode) {
					hasWarnedAboutDeprecatedIsConcurrentMode = true;
					console["warn"]("The ReactIs.isConcurrentMode() alias has been deprecated, and will be removed in React 18+.");
				}
				return false;
			}
			function isContextConsumer(object) {
				return typeOf(object) === REACT_CONTEXT_TYPE;
			}
			function isContextProvider(object) {
				return typeOf(object) === REACT_PROVIDER_TYPE;
			}
			function isElement(object) {
				return typeof object === "object" && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
			}
			function isForwardRef(object) {
				return typeOf(object) === REACT_FORWARD_REF_TYPE;
			}
			function isFragment(object) {
				return typeOf(object) === REACT_FRAGMENT_TYPE;
			}
			function isLazy(object) {
				return typeOf(object) === REACT_LAZY_TYPE;
			}
			function isMemo(object) {
				return typeOf(object) === REACT_MEMO_TYPE;
			}
			function isPortal(object) {
				return typeOf(object) === REACT_PORTAL_TYPE;
			}
			function isProfiler(object) {
				return typeOf(object) === REACT_PROFILER_TYPE;
			}
			function isStrictMode(object) {
				return typeOf(object) === REACT_STRICT_MODE_TYPE;
			}
			function isSuspense(object) {
				return typeOf(object) === REACT_SUSPENSE_TYPE;
			}
			function isSuspenseList(object) {
				return typeOf(object) === REACT_SUSPENSE_LIST_TYPE;
			}
			exports.ContextConsumer = ContextConsumer;
			exports.ContextProvider = ContextProvider;
			exports.Element = Element;
			exports.ForwardRef = ForwardRef;
			exports.Fragment = Fragment;
			exports.Lazy = Lazy;
			exports.Memo = Memo;
			exports.Portal = Portal;
			exports.Profiler = Profiler;
			exports.StrictMode = StrictMode;
			exports.Suspense = Suspense;
			exports.SuspenseList = SuspenseList;
			exports.isAsyncMode = isAsyncMode;
			exports.isConcurrentMode = isConcurrentMode;
			exports.isContextConsumer = isContextConsumer;
			exports.isContextProvider = isContextProvider;
			exports.isElement = isElement;
			exports.isForwardRef = isForwardRef;
			exports.isFragment = isFragment;
			exports.isLazy = isLazy;
			exports.isMemo = isMemo;
			exports.isPortal = isPortal;
			exports.isProfiler = isProfiler;
			exports.isStrictMode = isStrictMode;
			exports.isSuspense = isSuspense;
			exports.isSuspenseList = isSuspenseList;
			exports.isValidElementType = isValidElementType;
			exports.typeOf = typeOf;
		})();
	}));

//#endregion
//#region node_modules/react-is/index.js
	var require_react_is = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		module.exports = require_react_is_development();
	}));

//#endregion
//#region node_modules/hast-util-whitespace/index.js
/**
	* Check if the given value is *inter-element whitespace*.
	*
	* @param {unknown} thing
	*   Thing to check (typically `Node` or `string`).
	* @returns {boolean}
	*   Whether the `value` is inter-element whitespace (`boolean`): consisting of
	*   zero or more of space, tab (`\t`), line feed (`\n`), carriage return
	*   (`\r`), or form feed (`\f`).
	*   If a node is passed it must be a `Text` node, whose `value` field is
	*   checked.
	*/
	function whitespace(thing) {
		/** @type {string} */
		const value = thing && typeof thing === "object" && thing.type === "text" ? thing.value || "" : thing;
		return typeof value === "string" && value.replace(/[ \t\n\f\r]/g, "") === "";
	}
	var init_hast_util_whitespace = __esmMin((() => {}));

//#endregion
//#region node_modules/space-separated-tokens/index.js
/**
	* Serialize an array of strings as space separated-tokens.
	*
	* @param {Array<string|number>} values
	*   List of tokens.
	* @returns {string}
	*   Space-separated tokens.
	*/
	function stringify$1(values) {
		return values.join(" ").trim();
	}
	var init_space_separated_tokens = __esmMin((() => {
		__name(stringify$1, "stringify");
	}));

//#endregion
//#region node_modules/comma-separated-tokens/index.js
/**
	* Serialize an array of strings or numbers to comma-separated tokens.
	*
	* @param {Array<string|number>} values
	*   List of tokens.
	* @param {Options} [options]
	*   Configuration for `stringify` (optional).
	* @returns {string}
	*   Comma-separated tokens.
	*/
	function stringify(values, options) {
		const settings = options || {};
		return (values[values.length - 1] === "" ? [...values, ""] : values).join((settings.padRight ? " " : "") + "," + (settings.padLeft === false ? "" : " ")).trim();
	}
	var init_comma_separated_tokens = __esmMin((() => {}));

//#endregion
//#region node_modules/inline-style-parser/index.js
	var require_inline_style_parser = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		var COMMENT_REGEX = /\/\*[^*]*\*+([^/*][^*]*\*+)*\//g;
		var NEWLINE_REGEX = /\n/g;
		var WHITESPACE_REGEX = /^\s*/;
		var PROPERTY_REGEX = /^(\*?[-#/*\\\w]+(\[[0-9a-z_-]+\])?)\s*/;
		var COLON_REGEX = /^:\s*/;
		var VALUE_REGEX = /^((?:'(?:\\'|.)*?'|"(?:\\"|.)*?"|\([^)]*?\)|[^};])+)/;
		var SEMICOLON_REGEX = /^[;\s]*/;
		var TRIM_REGEX = /^\s+|\s+$/g;
		var NEWLINE = "\n";
		var FORWARD_SLASH = "/";
		var ASTERISK = "*";
		var EMPTY_STRING = "";
		var TYPE_COMMENT = "comment";
		var TYPE_DECLARATION = "declaration";
		/**
		* @param {String} style
		* @param {Object} [options]
		* @return {Object[]}
		* @throws {TypeError}
		* @throws {Error}
		*/
		module.exports = function(style, options) {
			if (typeof style !== "string") throw new TypeError("First argument must be a string");
			if (!style) return [];
			options = options || {};
			/**
			* Positional.
			*/
			var lineno = 1;
			var column = 1;
			/**
			* Update lineno and column based on `str`.
			*
			* @param {String} str
			*/
			function updatePosition(str) {
				var lines = str.match(NEWLINE_REGEX);
				if (lines) lineno += lines.length;
				var i = str.lastIndexOf(NEWLINE);
				column = ~i ? str.length - i : column + str.length;
			}
			/**
			* Mark position and patch `node.position`.
			*
			* @return {Function}
			*/
			function position() {
				var start = {
					line: lineno,
					column
				};
				return function(node) {
					node.position = new Position(start);
					whitespace();
					return node;
				};
			}
			/**
			* Store position information for a node.
			*
			* @constructor
			* @property {Object} start
			* @property {Object} end
			* @property {undefined|String} source
			*/
			function Position(start) {
				this.start = start;
				this.end = {
					line: lineno,
					column
				};
				this.source = options.source;
			}
			/**
			* Non-enumerable source string.
			*/
			Position.prototype.content = style;
			var errorsList = [];
			/**
			* Error `msg`.
			*
			* @param {String} msg
			* @throws {Error}
			*/
			function error(msg) {
				var err = /* @__PURE__ */ new Error(options.source + ":" + lineno + ":" + column + ": " + msg);
				err.reason = msg;
				err.filename = options.source;
				err.line = lineno;
				err.column = column;
				err.source = style;
				if (options.silent) errorsList.push(err);
				else throw err;
			}
			/**
			* Match `re` and return captures.
			*
			* @param {RegExp} re
			* @return {undefined|Array}
			*/
			function match(re) {
				var m = re.exec(style);
				if (!m) return;
				var str = m[0];
				updatePosition(str);
				style = style.slice(str.length);
				return m;
			}
			/**
			* Parse whitespace.
			*/
			function whitespace() {
				match(WHITESPACE_REGEX);
			}
			/**
			* Parse comments.
			*
			* @param {Object[]} [rules]
			* @return {Object[]}
			*/
			function comments(rules) {
				var c;
				rules = rules || [];
				while (c = comment()) if (c !== false) rules.push(c);
				return rules;
			}
			/**
			* Parse comment.
			*
			* @return {Object}
			* @throws {Error}
			*/
			function comment() {
				var pos = position();
				if (FORWARD_SLASH != style.charAt(0) || ASTERISK != style.charAt(1)) return;
				var i = 2;
				while (EMPTY_STRING != style.charAt(i) && (ASTERISK != style.charAt(i) || FORWARD_SLASH != style.charAt(i + 1))) ++i;
				i += 2;
				if (EMPTY_STRING === style.charAt(i - 1)) return error("End of comment missing");
				var str = style.slice(2, i - 2);
				column += 2;
				updatePosition(str);
				style = style.slice(i);
				column += 2;
				return pos({
					type: TYPE_COMMENT,
					comment: str
				});
			}
			/**
			* Parse declaration.
			*
			* @return {Object}
			* @throws {Error}
			*/
			function declaration() {
				var pos = position();
				var prop = match(PROPERTY_REGEX);
				if (!prop) return;
				comment();
				if (!match(COLON_REGEX)) return error("property missing ':'");
				var val = match(VALUE_REGEX);
				var ret = pos({
					type: TYPE_DECLARATION,
					property: trim(prop[0].replace(COMMENT_REGEX, EMPTY_STRING)),
					value: val ? trim(val[0].replace(COMMENT_REGEX, EMPTY_STRING)) : EMPTY_STRING
				});
				match(SEMICOLON_REGEX);
				return ret;
			}
			/**
			* Parse declarations.
			*
			* @return {Object[]}
			*/
			function declarations() {
				var decls = [];
				comments(decls);
				var decl;
				while (decl = declaration()) if (decl !== false) {
					decls.push(decl);
					comments(decls);
				}
				return decls;
			}
			whitespace();
			return declarations();
		};
		/**
		* Trim `str`.
		*
		* @param {String} str
		* @return {String}
		*/
		function trim(str) {
			return str ? str.replace(TRIM_REGEX, EMPTY_STRING) : EMPTY_STRING;
		}
	}));

//#endregion
//#region node_modules/style-to-object/index.js
	var require_style_to_object = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		var parse = require_inline_style_parser();
		/**
		* Parses inline style to object.
		*
		* @example
		* // returns { 'line-height': '42' }
		* StyleToObject('line-height: 42;');
		*
		* @param  {String}      style      - The inline style.
		* @param  {Function}    [iterator] - The iterator function.
		* @return {null|Object}
		*/
		function StyleToObject(style, iterator) {
			var output = null;
			if (!style || typeof style !== "string") return output;
			var declaration;
			var declarations = parse(style);
			var hasIterator = typeof iterator === "function";
			var property;
			var value;
			for (var i = 0, len = declarations.length; i < len; i++) {
				declaration = declarations[i];
				property = declaration.property;
				value = declaration.value;
				if (hasIterator) iterator(property, value, declaration);
				else if (value) {
					output || (output = {});
					output[property] = value;
				}
			}
			return output;
		}
		module.exports = StyleToObject;
		module.exports.default = StyleToObject;
	}));

//#endregion
//#region node_modules/style-to-object/index.mjs
	var import_style_to_object, style_to_object_default;
	var init_style_to_object = __esmMin((() => {
		import_style_to_object = /* @__PURE__ */ __toESM(require_style_to_object(), 1);
		style_to_object_default = import_style_to_object.default;
	}));

//#endregion
//#region node_modules/react-markdown/lib/ast-to-react.js
/**
	* @template T
	* @typedef {Promise.resolve(React).ComponentType<T>} ComponentType<T>
	*/
	/**
	* @template {Promise.resolve(React).ElementType} T
	* @typedef {Promise.resolve(React).ComponentPropsWithoutRef<T>} ComponentPropsWithoutRef<T>
	*/
	/**
	* @typedef {Promise.resolve(React).ReactNode} ReactNode
	* @typedef {import('unist').Position} Position
	* @typedef {import('hast').Element} Element
	* @typedef {import('hast').ElementContent} ElementContent
	* @typedef {import('hast').Root} Root
	* @typedef {import('hast').Text} Text
	* @typedef {import('hast').Comment} Comment
	* @typedef {import('hast').DocType} Doctype
	* @typedef {import('property-information').Info} Info
	* @typedef {import('property-information').Schema} Schema
	* @typedef {import('./complex-types.js').ReactMarkdownProps} ReactMarkdownProps
	*
	* @typedef Raw
	* @property {'raw'} type
	* @property {string} value
	*
	* @typedef Context
	* @property {Options} options
	* @property {Schema} schema
	* @property {number} listDepth
	*
	* @callback TransformLink
	* @param {string} href
	* @param {Array<ElementContent>} children
	* @param {string?} title
	* @returns {string}
	*
	* @callback TransformImage
	* @param {string} src
	* @param {string} alt
	* @param {string?} title
	* @returns {string}
	*
	* @typedef {Promise.resolve(React).HTMLAttributeAnchorTarget} TransformLinkTargetType
	*
	* @callback TransformLinkTarget
	* @param {string} href
	* @param {Array<ElementContent>} children
	* @param {string?} title
	* @returns {TransformLinkTargetType|undefined}
	*
	* @typedef {keyof JSX.IntrinsicElements} ReactMarkdownNames
	*
	* To do: is `data-sourcepos` typeable?
	*
	* @typedef {ComponentPropsWithoutRef<'code'> & ReactMarkdownProps & {inline?: boolean}} CodeProps
	* @typedef {ComponentPropsWithoutRef<'h1'> & ReactMarkdownProps & {level: number}} HeadingProps
	* @typedef {ComponentPropsWithoutRef<'li'> & ReactMarkdownProps & {checked: boolean|null, index: number, ordered: boolean}} LiProps
	* @typedef {ComponentPropsWithoutRef<'ol'> & ReactMarkdownProps & {depth: number, ordered: true}} OrderedListProps
	* @typedef {ComponentPropsWithoutRef<'td'> & ReactMarkdownProps & {style?: Record<string, unknown>, isHeader: false}} TableDataCellProps
	* @typedef {ComponentPropsWithoutRef<'th'> & ReactMarkdownProps & {style?: Record<string, unknown>, isHeader: true}} TableHeaderCellProps
	* @typedef {ComponentPropsWithoutRef<'tr'> & ReactMarkdownProps & {isHeader: boolean}} TableRowProps
	* @typedef {ComponentPropsWithoutRef<'ul'> & ReactMarkdownProps & {depth: number, ordered: false}} UnorderedListProps
	*
	* @typedef {ComponentType<CodeProps>} CodeComponent
	* @typedef {ComponentType<HeadingProps>} HeadingComponent
	* @typedef {ComponentType<LiProps>} LiComponent
	* @typedef {ComponentType<OrderedListProps>} OrderedListComponent
	* @typedef {ComponentType<TableDataCellProps>} TableDataCellComponent
	* @typedef {ComponentType<TableHeaderCellProps>} TableHeaderCellComponent
	* @typedef {ComponentType<TableRowProps>} TableRowComponent
	* @typedef {ComponentType<UnorderedListProps>} UnorderedListComponent
	*
	* @typedef SpecialComponents
	* @property {CodeComponent|ReactMarkdownNames} code
	* @property {HeadingComponent|ReactMarkdownNames} h1
	* @property {HeadingComponent|ReactMarkdownNames} h2
	* @property {HeadingComponent|ReactMarkdownNames} h3
	* @property {HeadingComponent|ReactMarkdownNames} h4
	* @property {HeadingComponent|ReactMarkdownNames} h5
	* @property {HeadingComponent|ReactMarkdownNames} h6
	* @property {LiComponent|ReactMarkdownNames} li
	* @property {OrderedListComponent|ReactMarkdownNames} ol
	* @property {TableDataCellComponent|ReactMarkdownNames} td
	* @property {TableHeaderCellComponent|ReactMarkdownNames} th
	* @property {TableRowComponent|ReactMarkdownNames} tr
	* @property {UnorderedListComponent|ReactMarkdownNames} ul
	*
	* @typedef {Partial<Omit<import('./complex-types.js').NormalComponents, keyof SpecialComponents> & SpecialComponents>} Components
	*
	* @typedef Options
	* @property {boolean} [sourcePos=false]
	* @property {boolean} [rawSourcePos=false]
	* @property {boolean} [skipHtml=false]
	* @property {boolean} [includeElementIndex=false]
	* @property {null|false|TransformLink} [transformLinkUri]
	* @property {TransformImage} [transformImageUri]
	* @property {TransformLinkTargetType|TransformLinkTarget} [linkTarget]
	* @property {Components} [components]
	*/
	/**
	* @param {Context} context
	* @param {Element|Root} node
	*/
	function childrenToReact(context, node) {
		/** @type {Array<ReactNode>} */
		const children = [];
		let childIndex = -1;
		/** @type {Comment|Doctype|Element|Raw|Text} */
		let child;
		while (++childIndex < node.children.length) {
			child = node.children[childIndex];
			if (child.type === "element") children.push(toReact(context, child, childIndex, node));
			else if (child.type === "text") {
				if (node.type !== "element" || !tableElements.has(node.tagName) || !whitespace(child)) children.push(child.value);
			} else if (child.type === "raw" && !context.options.skipHtml) children.push(child.value);
		}
		return children;
	}
	/**
	* @param {Context} context
	* @param {Element} node
	* @param {number} index
	* @param {Element|Root} parent
	*/
	function toReact(context, node, index, parent) {
		const options = context.options;
		const transform = options.transformLinkUri === void 0 ? uriTransformer : options.transformLinkUri;
		const parentSchema = context.schema;
		/** @type {ReactMarkdownNames} */
		const name = node.tagName;
		/** @type {Record<string, unknown>} */
		const properties = {};
		let schema = parentSchema;
		/** @type {string} */
		let property;
		if (parentSchema.space === "html" && name === "svg") {
			schema = svg;
			context.schema = schema;
		}
		if (node.properties) {
			for (property in node.properties) if (own$1.call(node.properties, property)) addProperty(properties, property, node.properties[property], context);
		}
		if (name === "ol" || name === "ul") context.listDepth++;
		const children = childrenToReact(context, node);
		if (name === "ol" || name === "ul") context.listDepth--;
		context.schema = parentSchema;
		const position = node.position || {
			start: {
				line: null,
				column: null,
				offset: null
			},
			end: {
				line: null,
				column: null,
				offset: null
			}
		};
		const component = options.components && own$1.call(options.components, name) ? options.components[name] : name;
		const basic = typeof component === "string" || component === react$1.default.Fragment;
		if (!import_react_is.isValidElementType(component)) throw new TypeError(`Component for name \`${name}\` not defined or is not renderable`);
		properties.key = index;
		if (name === "a" && options.linkTarget) properties.target = typeof options.linkTarget === "function" ? options.linkTarget(String(properties.href || ""), node.children, typeof properties.title === "string" ? properties.title : null) : options.linkTarget;
		if (name === "a" && transform) properties.href = transform(String(properties.href || ""), node.children, typeof properties.title === "string" ? properties.title : null);
		if (!basic && name === "code" && parent.type === "element" && parent.tagName !== "pre") properties.inline = true;
		if (!basic && (name === "h1" || name === "h2" || name === "h3" || name === "h4" || name === "h5" || name === "h6")) properties.level = Number.parseInt(name.charAt(1), 10);
		if (name === "img" && options.transformImageUri) properties.src = options.transformImageUri(String(properties.src || ""), String(properties.alt || ""), typeof properties.title === "string" ? properties.title : null);
		if (!basic && name === "li" && parent.type === "element") {
			const input = getInputElement(node);
			properties.checked = input && input.properties ? Boolean(input.properties.checked) : null;
			properties.index = getElementsBeforeCount(parent, node);
			properties.ordered = parent.tagName === "ol";
		}
		if (!basic && (name === "ol" || name === "ul")) {
			properties.ordered = name === "ol";
			properties.depth = context.listDepth;
		}
		if (name === "td" || name === "th") {
			if (properties.align) {
				if (!properties.style) properties.style = {};
				properties.style.textAlign = properties.align;
				delete properties.align;
			}
			if (!basic) properties.isHeader = name === "th";
		}
		if (!basic && name === "tr" && parent.type === "element") properties.isHeader = Boolean(parent.tagName === "thead");
		if (options.sourcePos) properties["data-sourcepos"] = flattenPosition(position);
		if (!basic && options.rawSourcePos) properties.sourcePosition = node.position;
		if (!basic && options.includeElementIndex) {
			properties.index = getElementsBeforeCount(parent, node);
			properties.siblingCount = getElementsBeforeCount(parent);
		}
		if (!basic) properties.node = node;
		return children.length > 0 ? react$1.default.createElement(component, properties, children) : react$1.default.createElement(component, properties);
	}
	/**
	* @param {Element|Root} node
	* @returns {Element?}
	*/
	function getInputElement(node) {
		let index = -1;
		while (++index < node.children.length) {
			const child = node.children[index];
			if (child.type === "element" && child.tagName === "input") return child;
		}
		return null;
	}
	/**
	* @param {Element|Root} parent
	* @param {Element} [node]
	* @returns {number}
	*/
	function getElementsBeforeCount(parent, node) {
		let index = -1;
		let count = 0;
		while (++index < parent.children.length) {
			if (parent.children[index] === node) break;
			if (parent.children[index].type === "element") count++;
		}
		return count;
	}
	/**
	* @param {Record<string, unknown>} props
	* @param {string} prop
	* @param {unknown} value
	* @param {Context} ctx
	*/
	function addProperty(props, prop, value, ctx) {
		const info = find(ctx.schema, prop);
		let result = value;
		if (result === null || result === void 0 || result !== result) return;
		if (Array.isArray(result)) result = info.commaSeparated ? stringify(result) : stringify$1(result);
		if (info.property === "style" && typeof result === "string") result = parseStyle(result);
		if (info.space && info.property) props[own$1.call(hastToReact, info.property) ? hastToReact[info.property] : info.property] = result;
		else if (info.attribute) props[info.attribute] = result;
	}
	/**
	* @param {string} value
	* @returns {Record<string, string>}
	*/
	function parseStyle(value) {
		/** @type {Record<string, string>} */
		const result = {};
		try {
			style_to_object_default(value, iterator);
		} catch (_unused) {}
		return result;
		/**
		* @param {string} name
		* @param {string} v
		*/
		function iterator(name, v) {
			const k = name.slice(0, 4) === "-ms-" ? `ms-${name.slice(4)}` : name;
			result[k.replace(/-([a-z])/g, styleReplacer)] = v;
		}
	}
	/**
	* @param {unknown} _
	* @param {string} $1
	*/
	function styleReplacer(_, $1) {
		return $1.toUpperCase();
	}
	/**
	* @param {Position|{start: {line: null, column: null, offset: null}, end: {line: null, column: null, offset: null}}} pos
	* @returns {string}
	*/
	function flattenPosition(pos) {
		return [
			pos.start.line,
			":",
			pos.start.column,
			"-",
			pos.end.line,
			":",
			pos.end.column
		].map(String).join("");
	}
	var import_react_is, own$1, tableElements;
	var init_ast_to_react = __esmMin((() => {
		import_react_is = /* @__PURE__ */ __toESM(require_react_is(), 1);
		init_hast_util_whitespace();
		init_property_information();
		init_space_separated_tokens();
		init_comma_separated_tokens();
		init_style_to_object();
		init_uri_transformer();
		own$1 = {}.hasOwnProperty;
		tableElements = /* @__PURE__ */ new Set([
			"table",
			"thead",
			"tbody",
			"tfoot",
			"tr"
		]);
	}));

//#endregion
//#region node_modules/react-markdown/lib/react-markdown.js
/**
	* @typedef {Promise.resolve(React).ReactNode} ReactNode
	* @typedef {Promise.resolve(React).ReactElement<{}>} ReactElement
	* @typedef {import('unified').PluggableList} PluggableList
	* @typedef {import('hast').Root} Root
	* @typedef {import('./rehype-filter.js').Options} FilterOptions
	* @typedef {import('./ast-to-react.js').Options} TransformOptions
	*
	* @typedef CoreOptions
	* @property {string} children
	*
	* @typedef PluginOptions
	* @property {PluggableList} [remarkPlugins=[]]
	* @property {PluggableList} [rehypePlugins=[]]
	* @property {import('remark-rehype').Options | undefined} [remarkRehypeOptions={}]
	*
	* @typedef LayoutOptions
	* @property {string} [className]
	*
	* @typedef {CoreOptions & PluginOptions & LayoutOptions & FilterOptions & TransformOptions} ReactMarkdownOptions
	*
	* @typedef Deprecation
	* @property {string} id
	* @property {string} [to]
	*/
	/**
	* React component to render markdown.
	*
	* @param {ReactMarkdownOptions} options
	* @returns {ReactElement}
	*/
	function ReactMarkdown$1(options) {
		for (const key in deprecated) if (own.call(deprecated, key) && own.call(options, key)) {
			const deprecation = deprecated[key];
			console.warn(`[react-markdown] Warning: please ${deprecation.to ? `use \`${deprecation.to}\` instead of` : "remove"} \`${key}\` (see <${changelog}#${deprecation.id}> for more info)`);
			delete deprecated[key];
		}
		const processor = unified().use(remarkParse).use(options.remarkPlugins || []).use(remarkRehype, _objectSpread2(_objectSpread2({}, options.remarkRehypeOptions), {}, { allowDangerousHtml: true })).use(options.rehypePlugins || []).use(rehypeFilter, options);
		const file = new VFile();
		if (typeof options.children === "string") file.value = options.children;
		else if (options.children !== void 0 && options.children !== null) console.warn(`[react-markdown] Warning: please pass a string as \`children\` (not: \`${options.children}\`)`);
		const hastNode = processor.runSync(processor.parse(file), file);
		if (hastNode.type !== "root") throw new TypeError("Expected a `root` node");
		/** @type {ReactElement} */
		let result = react$1.default.createElement(react$1.default.Fragment, {}, childrenToReact({
			options,
			schema: html,
			listDepth: 0
		}, hastNode));
		if (options.className) result = react$1.default.createElement("div", { className: options.className }, result);
		return result;
	}
	var import_prop_types$1, own, changelog, deprecated;
	var init_react_markdown$1 = __esmMin((() => {
		init_vfile();
		init_unified();
		init_remark_parse();
		init_remark_rehype();
		import_prop_types$1 = /* @__PURE__ */ __toESM(require_prop_types(), 1);
		init_property_information();
		init_rehype_filter();
		init_ast_to_react();
		init_objectSpread2();
		own = {}.hasOwnProperty;
		changelog = "https://github.com/remarkjs/react-markdown/blob/main/changelog.md";
		deprecated = {
			plugins: {
				to: "remarkPlugins",
				id: "change-plugins-to-remarkplugins"
			},
			renderers: {
				to: "components",
				id: "change-renderers-to-components"
			},
			astPlugins: { id: "remove-buggy-html-in-markdown-parser" },
			allowDangerousHtml: { id: "remove-buggy-html-in-markdown-parser" },
			escapeHtml: { id: "remove-buggy-html-in-markdown-parser" },
			source: {
				to: "children",
				id: "change-source-to-children"
			},
			allowNode: {
				to: "allowElement",
				id: "replace-allownode-allowedtypes-and-disallowedtypes"
			},
			allowedTypes: {
				to: "allowedElements",
				id: "replace-allownode-allowedtypes-and-disallowedtypes"
			},
			disallowedTypes: {
				to: "disallowedElements",
				id: "replace-allownode-allowedtypes-and-disallowedtypes"
			},
			includeNodeIndex: {
				to: "includeElementIndex",
				id: "change-includenodeindex-to-includeelementindex"
			}
		};
		__name(ReactMarkdown$1, "ReactMarkdown");
		ReactMarkdown$1.propTypes = {
			children: import_prop_types$1.default.string,
			className: import_prop_types$1.default.string,
			allowElement: import_prop_types$1.default.func,
			allowedElements: import_prop_types$1.default.arrayOf(import_prop_types$1.default.string),
			disallowedElements: import_prop_types$1.default.arrayOf(import_prop_types$1.default.string),
			unwrapDisallowed: import_prop_types$1.default.bool,
			remarkPlugins: import_prop_types$1.default.arrayOf(import_prop_types$1.default.oneOfType([
				import_prop_types$1.default.object,
				import_prop_types$1.default.func,
				import_prop_types$1.default.arrayOf(import_prop_types$1.default.oneOfType([
					import_prop_types$1.default.bool,
					import_prop_types$1.default.string,
					import_prop_types$1.default.object,
					import_prop_types$1.default.func,
					import_prop_types$1.default.arrayOf(import_prop_types$1.default.any)
				]))
			])),
			rehypePlugins: import_prop_types$1.default.arrayOf(import_prop_types$1.default.oneOfType([
				import_prop_types$1.default.object,
				import_prop_types$1.default.func,
				import_prop_types$1.default.arrayOf(import_prop_types$1.default.oneOfType([
					import_prop_types$1.default.bool,
					import_prop_types$1.default.string,
					import_prop_types$1.default.object,
					import_prop_types$1.default.func,
					import_prop_types$1.default.arrayOf(import_prop_types$1.default.any)
				]))
			])),
			sourcePos: import_prop_types$1.default.bool,
			rawSourcePos: import_prop_types$1.default.bool,
			skipHtml: import_prop_types$1.default.bool,
			includeElementIndex: import_prop_types$1.default.bool,
			transformLinkUri: import_prop_types$1.default.oneOfType([import_prop_types$1.default.func, import_prop_types$1.default.bool]),
			linkTarget: import_prop_types$1.default.oneOfType([import_prop_types$1.default.func, import_prop_types$1.default.string]),
			transformImageUri: import_prop_types$1.default.func,
			components: import_prop_types$1.default.object
		};
	}));

//#endregion
//#region node_modules/react-markdown/index.js
	var react_markdown_exports = /* @__PURE__ */ __exportAll({
		default: () => ReactMarkdown$1,
		uriTransformer: () => uriTransformer
	});
	var init_react_markdown = __esmMin((() => {
		init_uri_transformer();
		init_react_markdown$1();
	}));

//#endregion
//#region modules/ai/assets/js/editor/pages/form-code/index.js
	var form_code_exports = /* @__PURE__ */ __exportAll({ default: () => FormCode$1 });
	function ownKeys$5(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	function _objectSpread$5(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$5(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$5(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	var import_regenerator, import_prop_types, ReactMarkdown, generateUniqueId, CodeDisplayWrapper, FormCode$1;
	var init_form_code = __esmMin((() => {
		init_extends();
		init_asyncToGenerator();
		init_slicedToArray();
		init_defineProperty$1();
		import_regenerator = /* @__PURE__ */ __toESM(require_regenerator());
		import_prop_types = /* @__PURE__ */ __toESM(require_prop_types());
		init_actions_data();
		init_loader();
		init_prompt_search();
		init_prompt_suggestions();
		init_generate_button();
		init_prompt_error_message();
		init_code_block();
		init_use_code_prompt();
		init_prompt_history_action_context();
		init_prompt_library_link();
		init_requests_ids();
		init_voice_promotion_alert();
		init_splitTextResult();
		init_preload_helper();
		__name(ownKeys$5, "ownKeys");
		__name(_objectSpread$5, "_objectSpread");
		ReactMarkdown = (0, react.lazy)(function() {
			return __vitePreload(() => Promise.resolve().then(() => (init_react_markdown(), react_markdown_exports)), void 0);
		});
		generateUniqueId = function generateUniqueId() {
			return "custom-css-".concat(Math.random().toString(36).substr(2, 9));
		};
		CodeDisplayWrapper = (0, _elementor_ui.styled)(_elementor_ui.Box)(function() {
			return {
				"& p": {
					mb: "10px",
					fontSize: "13px",
					lineHeight: "1.5"
				},
				"& pre": { position: "relative" },
				"& textarea": {
					fontSize: "13px",
					lineHeight: "1.7"
				}
			};
		});
		FormCode$1 = function FormCode(_ref) {
			var onClose = _ref.onClose;
			var getControlValue = _ref.getControlValue;
			var setControlValue = _ref.setControlValue;
			var additionalOptions = _ref.additionalOptions;
			var credits = _ref.credits;
			var children = _ref.children;
			var _useCodePrompt = useCodePrompt(_objectSpread$5(_objectSpread$5({}, additionalOptions), {}, { credits }));
			var data = _useCodePrompt.data;
			var isLoading = _useCodePrompt.isLoading;
			var error = _useCodePrompt.error;
			var reset = _useCodePrompt.reset;
			var send = _useCodePrompt.send;
			var sendUsageData = _useCodePrompt.sendUsageData;
			var _splitText = splitText(data.result);
			var code = _splitText.code;
			var details = _splitText.details;
			var _useState2 = _slicedToArray((0, react.useState)(""), 2);
			var prompt = _useState2[0];
			var setPrompt = _useState2[1];
			var setGenerate = useRequestIds().setGenerate;
			var styleTagId = (0, react.useRef)(generateUniqueId());
			useSubscribeOnPromptHistoryAction([{
				type: ACTION_TYPES.REUSE,
				handler: function handler(action) {
					reset();
					setPrompt(action.data);
				}
			}]);
			var lastRun = (0, react.useRef)(function() {});
			var autocompleteItems = codeHtmlAutocomplete;
			var promptLibraryLink = "";
			if ("css" === (additionalOptions === null || additionalOptions === void 0 ? void 0 : additionalOptions.codeLanguage)) {
				autocompleteItems = codeCssAutocomplete;
				promptLibraryLink = "https://go.elementor.com/ai-prompt-library-css/";
			} else if (additionalOptions !== null && additionalOptions !== void 0 && additionalOptions.htmlMarkup) promptLibraryLink = "https://go.elementor.com/ai-prompt-library-html/";
			else promptLibraryLink = "https://go.elementor.com/ai-prompt-library-custom-code/";
			var showSuggestions = !prompt;
			var handleSubmit = /*#__PURE__*/ function() {
				var _ref2 = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator.default.mark(function _callee(event) {
					var response;
					return import_regenerator.default.wrap(function(_context) {
						while (1) switch (_context.prev = _context.next) {
							case 0:
								event.preventDefault();
								setGenerate();
								lastRun.current = function() {
									return send({ prompt });
								};
								_context.next = 1;
								return lastRun.current();
							case 1:
								response = _context.sent;
								if ("css" === (additionalOptions === null || additionalOptions === void 0 ? void 0 : additionalOptions.codeLanguage) && response.result) showCssPreview(splitText(response.result).code);
							case 2:
							case "end": return _context.stop();
						}
					}, _callee);
				}));
				return function handleSubmit(_x) {
					return _ref2.apply(this, arguments);
				};
			}();
			(0, react.useEffect)(function() {
				return function() {
					removeStyleTag();
				};
			}, []);
			var showCssPreview = function showCssPreview(cssCode) {
				insertStyleTag(parseCSS(cssCode));
			};
			var parseCSS = function parseCSS(cssCode) {
				var elementId = additionalOptions === null || additionalOptions === void 0 ? void 0 : additionalOptions.elementId;
				var selector = "document" === elementId ? elementor.config.document.settings.cssWrapperSelector : ".elementor-element.elementor-element-".concat(elementId);
				return cssCode && cssCode.replace(/`/g, "").replace(/^css\s*/i, "").replace(/selector/g, selector);
			};
			var isElementorEditor = function isElementorEditor() {
				return window.elementorFrontend;
			};
			var insertStyleTag = function insertStyleTag(cssCode) {
				if (!isElementorEditor()) return;
				var style = document.createElement("style");
				style.id = styleTagId.current;
				style.appendChild(document.createTextNode(cssCode));
				elementorFrontend.elements.$body[0].appendChild(style);
			};
			var removeStyleTag = function removeStyleTag() {
				if (!isElementorEditor()) return;
				var styleTag = elementorFrontend.elements.$body[0].querySelector("#".concat(styleTagId.current));
				if (styleTag) styleTag.remove();
			};
			var applyPrompt = function applyPrompt(inputText) {
				sendUsageData();
				setControlValue(inputText);
				onClose();
			};
			if (isLoading) return /*#__PURE__*/ react.default.createElement(Loader, null);
			return /*#__PURE__*/ react.default.createElement(react.default.Fragment, null, error && /*#__PURE__*/ react.default.createElement(PromptErrorMessage, {
				error,
				onRetry: lastRun.current,
				sx: { mb: 2.5 }
			}), children, !data.result && /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, {
				component: "form",
				onSubmit: handleSubmit
			}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, { sx: { pb: 1.5 } }, /*#__PURE__*/ react.default.createElement(PromptSearch, {
				placeholder: (0, _wordpress_i18n.__)("Describe the code you want to use...", "elementor"),
				name: "prompt",
				value: prompt,
				color: "secondary",
				onChange: function onChange(event) {
					return setPrompt(event.target.value);
				}
			})), showSuggestions && /*#__PURE__*/ react.default.createElement(PromptSuggestions, {
				suggestions: autocompleteItems,
				onSelect: setPrompt
			}, /*#__PURE__*/ react.default.createElement(PromptLibraryLink, { libraryLink: promptLibraryLink })), /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
				direction: "row",
				alignItems: "center",
				sx: {
					py: 1.5,
					mt: 4
				}
			}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
				direction: "row",
				justifyContent: "flex-end",
				flexGrow: 1
			}, /*#__PURE__*/ react.default.createElement(GenerateButton$1, null, (0, _wordpress_i18n.__)("Generate code", "elementor"))))), data.result && /*#__PURE__*/ react.default.createElement(CodeDisplayWrapper, null, /*#__PURE__*/ react.default.createElement(react.Suspense, { fallback: /*#__PURE__*/ react.default.createElement(Loader, null) }, /*#__PURE__*/ react.default.createElement(ReactMarkdown, { components: { code: function code(props) {
				return /*#__PURE__*/ react.default.createElement(CodeBlock, _extends$1({}, props, {
					defaultValue: getControlValue(),
					onInsert: applyPrompt
				}));
			} } }, code)), details, /*#__PURE__*/ react.default.createElement(VoicePromotionAlert, { introductionKey: "ai-context-code-promotion" }), /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
				direction: "row",
				alignItems: "center",
				sx: { mt: 4 }
			}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
				direction: "row",
				gap: 1,
				justifyContent: "flex-end",
				flexGrow: 1
			}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, {
				size: "small",
				color: "secondary",
				variant: "text",
				onClick: function onClick() {
					removeStyleTag();
					reset();
				}
			}, (0, _wordpress_i18n.__)("New prompt", "elementor"))))));
		};
		FormCode$1.propTypes = {
			onClose: import_prop_types.default.func.isRequired,
			getControlValue: import_prop_types.default.func.isRequired,
			setControlValue: import_prop_types.default.func.isRequired,
			additionalOptions: import_prop_types.default.shape({
				codeLanguage: import_prop_types.default.string,
				htmlMarkup: import_prop_types.default.string,
				elementId: import_prop_types.default.string,
				initialCredits: import_prop_types.default.number
			}),
			credits: import_prop_types.default.number,
			children: import_prop_types.default.node
		};
	}));

//#endregion
//#region modules/ai/assets/js/editor/page-content.js
	init_extends();
	init_defineProperty$1();
	init_slicedToArray();
	init_history_types();
	init_prompt_history_action_context();
	init_prompt_history_context();
	init_loader();
	init_requests_ids();
	init_preload_helper();
	function ownKeys$4(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	__name(ownKeys$4, "ownKeys");
	function _objectSpread$4(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$4(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$4(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	__name(_objectSpread$4, "_objectSpread");
	var FormCode = (0, react.lazy)(function() {
		return __vitePreload(() => Promise.resolve().then(() => (init_form_code(), form_code_exports)), void 0);
	});
	var PageContent = function PageContent(_ref) {
		var type = _ref.type;
		var controlType = _ref.controlType;
		var onClose = _ref.onClose;
		var onConnect = _ref.onConnect;
		var getControlValue = _ref.getControlValue;
		var setControlValue = _ref.setControlValue;
		var additionalOptions = _ref.additionalOptions;
		var _ref2 = function(_additionalOptions$us) {
			return (_additionalOptions$us = additionalOptions === null || additionalOptions === void 0 ? void 0 : additionalOptions.useCustomInit) !== null && _additionalOptions$us !== void 0 ? _additionalOptions$us : useUserInfo;
		}()();
		var isLoading = _ref2.isLoading;
		var isConnected = _ref2.isConnected;
		var isGetStarted = _ref2.isGetStarted;
		var connectUrl = _ref2.connectUrl;
		var fetchData = _ref2.fetchData;
		var hasSubscription = _ref2.hasSubscription;
		var credits = _ref2.credits;
		var initialUsagePercentage = _ref2.usagePercentage;
		var _useRequestIds = useRequestIds();
		var updateUsagePercentage = _useRequestIds.updateUsagePercentage;
		var usagePercentage = _useRequestIds.usagePercentage;
		var _useState2 = _slicedToArray((0, react.useState)(false), 2);
		var isInitUsageDone = _useState2[0];
		var setIsInitUsageDone = _useState2[1];
		(0, react.useEffect)(function() {
			if (!isInitUsageDone && !isLoading && (initialUsagePercentage || 0 === initialUsagePercentage)) {
				updateUsagePercentage(initialUsagePercentage);
				setIsInitUsageDone(true);
			}
		}, [
			isLoading,
			initialUsagePercentage,
			isInitUsageDone,
			updateUsagePercentage
		]);
		var showBadge = useUpgradeMessage({
			usagePercentage,
			hasSubscription
		}).showBadge;
		var _useState4 = _slicedToArray((0, react.useState)({ pointerEvents: "none" }), 2);
		var sxStyle = _useState4[0];
		var setSxStyle = _useState4[1];
		var timeoutRef = (0, react.useRef)(null);
		var _useState6 = _slicedToArray((0, react.useState)(), 2);
		var originalControlValue = _useState6[0];
		var setOriginalControlValue = _useState6[1];
		var promptDialogStyleProps = {
			sx: {
				zIndex: 17e4,
				"& .MuiDialog-container": {
					alignItems: "flex-start",
					mt: "media" === type ? "2.5vh" : "18vh"
				},
				"& .MuiDialogContent-root": {
					willChange: "height",
					transition: "height 300ms ease-in-out",
					position: "relative"
				},
				"& .MuiBox-root": { boxSizing: "border-box" }
			},
			PaperProps: { sx: {
				m: 0,
				maxHeight: "media" === type ? "95vh" : "76vh",
				minHeight: "80vh",
				height: "auto"
			} }
		};
		var codePromptDialogStyleProps = {
			sx: _objectSpread$4(_objectSpread$4({}, promptDialogStyleProps.sx), sxStyle),
			PaperProps: {
				onMouseEnter: function onMouseEnter() {
					clearTimeout(timeoutRef.current);
					setSxStyle({ pointerEvents: "all" });
				},
				onMouseLeave: function onMouseLeave() {
					clearTimeout(timeoutRef.current);
					timeoutRef.current = setTimeout(function() {
						setSxStyle({ pointerEvents: "none" });
					}, 200);
				},
				sx: _objectSpread$4(_objectSpread$4({}, promptDialogStyleProps.PaperProps.sx), {}, { pointerEvents: "auto" })
			}
		};
		var maybeRenderUpgradeChip = function maybeRenderUpgradeChip() {
			if (!showBadge) return;
			return /*#__PURE__*/ react.default.createElement(UpgradeChip, {
				hasSubscription,
				usagePercentage
			});
		};
		(0, react.useEffect)(function() {
			if (!originalControlValue) setOriginalControlValue(getControlValue());
		}, []);
		if (isLoading || !isInitUsageDone) return /*#__PURE__*/ react.default.createElement(PromptDialog, _extends$1({ onClose }, promptDialogStyleProps, { maxWidth: "media" === type ? "lg" : "sm" }), /*#__PURE__*/ react.default.createElement(PromptDialog.Header, { onClose }), /*#__PURE__*/ react.default.createElement(PromptDialog.Content, { dividers: true }, (additionalOptions === null || additionalOptions === void 0 ? void 0 : additionalOptions.loadingTitle) && /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, { style: {
			display: "flex",
			justifyContent: "center",
			alignItems: "center",
			width: "100%"
		} }, /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, {
			variant: "body1",
			color: "secondary"
		}, additionalOptions === null || additionalOptions === void 0 ? void 0 : additionalOptions.loadingTitle)), /*#__PURE__*/ react.default.createElement(Loader, null)));
		if (!isConnected) return /*#__PURE__*/ react.default.createElement(WizardDialog, { onClose }, /*#__PURE__*/ react.default.createElement(WizardDialog.Header, { onClose }), /*#__PURE__*/ react.default.createElement(WizardDialog.Content, { dividers: true }, /*#__PURE__*/ react.default.createElement(Connect, {
			connectUrl,
			onSuccess: function onSuccess(data) {
				onConnect(data);
				fetchData();
			}
		})));
		if (!isGetStarted) return /*#__PURE__*/ react.default.createElement(WizardDialog, { onClose }, /*#__PURE__*/ react.default.createElement(WizardDialog.Header, { onClose }), /*#__PURE__*/ react.default.createElement(WizardDialog.Content, { dividers: true }, /*#__PURE__*/ react.default.createElement(GetStarted, { onSuccess: fetchData })));
		if ("media" === type) return /*#__PURE__*/ react.default.createElement(PromptHistoryProvider, { historyType: HISTORY_TYPES.IMAGE }, /*#__PURE__*/ react.default.createElement(PromptHistoryActionProvider, null, /*#__PURE__*/ react.default.createElement(FormMedia, {
			onClose,
			getControlValue,
			setControlValue,
			additionalOptions,
			credits,
			maybeRenderUpgradeChip,
			DialogProps: promptDialogStyleProps,
			hasSubscription,
			usagePercentage
		})));
		if ("code" === type) return /*#__PURE__*/ react.default.createElement(PromptDialog, _extends$1({ onClose }, codePromptDialogStyleProps), /*#__PURE__*/ react.default.createElement(PromptHistoryProvider, { historyType: HISTORY_TYPES.CODE }, /*#__PURE__*/ react.default.createElement(PromptHistoryActionProvider, null, /*#__PURE__*/ react.default.createElement(PromptDialog.Header, { onClose }, /*#__PURE__*/ react.default.createElement(PromptHistory, null), maybeRenderUpgradeChip()), /*#__PURE__*/ react.default.createElement(PromptDialog.Content, {
			className: "e-ai-dialog-content",
			dividers: true
		}, /*#__PURE__*/ react.default.createElement(react.Suspense, { fallback: /*#__PURE__*/ react.default.createElement(Loader, null) }, /*#__PURE__*/ react.default.createElement(FormCode, {
			onClose,
			getControlValue,
			setControlValue,
			additionalOptions,
			credits,
			usagePercentage
		}, /*#__PURE__*/ react.default.createElement(UsageMessages, {
			hasSubscription,
			usagePercentage,
			sx: { mb: 2 },
			feature: FREE_TRIAL_FEATURES_NAMES.CODE
		})))))));
		if ("animation" === type || "hover_animation" === type) {
			var onCloseAnimationDialog = function onCloseAnimationDialog() {
				setControlValue(originalControlValue);
				elementor.documents.getCurrent().history.setActive(true);
				onClose();
			};
			return /*#__PURE__*/ react.default.createElement(PromptDialog, _extends$1({ onClose: onCloseAnimationDialog }, codePromptDialogStyleProps), /*#__PURE__*/ react.default.createElement(PromptDialog.Header, {
				onClose: onCloseAnimationDialog,
				hideAiBetaLogo: additionalOptions === null || additionalOptions === void 0 ? void 0 : additionalOptions.hideAiBetaLogo
			}, maybeRenderUpgradeChip()), /*#__PURE__*/ react.default.createElement(PromptDialog.Content, {
				className: "e-ai-dialog-content",
				dividers: true
			}, /*#__PURE__*/ react.default.createElement(FormAnimation, {
				onClose,
				getControlValue,
				setControlValue,
				additionalOptions: _objectSpread$4(_objectSpread$4({}, additionalOptions), {}, { animationType: "hover_animation" === type ? "hover" : "other" }),
				credits,
				usagePercentage
			}, /*#__PURE__*/ react.default.createElement(UsageMessages, {
				hasSubscription,
				usagePercentage,
				sx: { mb: 2 },
				feature: FREE_TRIAL_FEATURES_NAMES.ANIMATION
			}))));
		}
		return /*#__PURE__*/ react.default.createElement(PromptDialog, _extends$1({ onClose }, promptDialogStyleProps), /*#__PURE__*/ react.default.createElement(PromptHistoryProvider, { historyType: HISTORY_TYPES.TEXT }, /*#__PURE__*/ react.default.createElement(PromptHistoryActionProvider, null, /*#__PURE__*/ react.default.createElement(PromptDialog.Header, {
			onClose,
			hideAiBetaLogo: additionalOptions === null || additionalOptions === void 0 ? void 0 : additionalOptions.hideAiBetaLogo
		}, !(additionalOptions !== null && additionalOptions !== void 0 && additionalOptions.withoutHistory) && /*#__PURE__*/ react.default.createElement(PromptHistory, null), maybeRenderUpgradeChip()), /*#__PURE__*/ react.default.createElement(PromptDialog.Content, {
			className: "e-ai-dialog-content",
			dividers: true
		}, /*#__PURE__*/ react.default.createElement(FormText, {
			type,
			controlType,
			onClose,
			getControlValue,
			setControlValue,
			additionalOptions,
			credits,
			usagePercentage
		}, /*#__PURE__*/ react.default.createElement(UsageMessages, {
			hasSubscription,
			usagePercentage,
			sx: { mb: 2 },
			feature: FREE_TRIAL_FEATURES_NAMES.TEXT
		}))))));
	};
	PageContent.propTypes = {
		type: import_prop_types$7.default.string,
		controlType: import_prop_types$7.default.string,
		onClose: import_prop_types$7.default.func.isRequired,
		onConnect: import_prop_types$7.default.func,
		getControlValue: import_prop_types$7.default.func.isRequired,
		setControlValue: import_prop_types$7.default.func.isRequired,
		additionalOptions: import_prop_types$7.default.object
	};

//#endregion
//#region node_modules/react-dom/client.js
	var require_client = /* @__PURE__ */ __commonJSMin(((exports) => {
		var m = (globalThis.ReactDOM);
		var i = m.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;
		exports.createRoot = function(c, o) {
			i.usingClientEntryPoint = true;
			try {
				return m.createRoot(c, o);
			} finally {
				i.usingClientEntryPoint = false;
			}
		};
	}));

//#endregion
//#region assets/dev/js/utils/react.js
	var import_client = require_client();

//#endregion
//#region modules/ai/assets/js/editor/utils/preview-container.js
/**
	* @typedef {import('elementor/assets/dev/js/editor/container/container')} Container
	*/
	var PREFIX = "e-ai-preview-container";
	var CLASS_HIDDEN = PREFIX + "--hidden";
	var CLASS_IDLE = PREFIX + "--idle";

//#endregion
//#region modules/ai/assets/js/editor/pages/form-layout/components/unsaved-changes-alert.js
	init_extends();
	init_objectWithoutProperties$1();
	var _excluded$4 = [
		"onClose",
		"onCancel",
		"title",
		"text"
	];
	var UnsavedChangesAlert = function UnsavedChangesAlert(_ref) {
		var onClose = _ref.onClose;
		var onCancel = _ref.onCancel;
		var title = _ref.title;
		var text = _ref.text;
		var props = _objectWithoutProperties$1(_ref, _excluded$4);
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Dialog, _extends$1({
			"aria-labelledby": "unsaved-changes-alert-title",
			"aria-describedby": "unsaved-changes-alert-description"
		}, props), /*#__PURE__*/ react.default.createElement(_elementor_ui.DialogTitle, { id: "unsaved-changes-alert-title" }, title), /*#__PURE__*/ react.default.createElement(_elementor_ui.DialogContent, null, /*#__PURE__*/ react.default.createElement(_elementor_ui.DialogContentText, { id: "unsaved-changes-alert-description" }, text)), /*#__PURE__*/ react.default.createElement(_elementor_ui.DialogActions, null, /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, {
			onClick: onCancel,
			color: "secondary"
		}, (0, _wordpress_i18n.__)("Cancel", "elementor")), /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, {
			onClick: onClose,
			color: "error",
			variant: "contained"
		}, (0, _wordpress_i18n.__)("Yes, leave", "elementor"))));
	};
	UnsavedChangesAlert.propTypes = {
		title: import_prop_types$7.default.string,
		text: import_prop_types$7.default.string,
		onCancel: import_prop_types$7.default.func,
		onClose: import_prop_types$7.default.func
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-layout/components/layout-dialog.js
	init_extends();
	init_defineProperty$1();
	init_slicedToArray();
	init_objectWithoutProperties$1();
	var _excluded$3 = ["sx", "PaperProps"];
	function ownKeys$3(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	__name(ownKeys$3, "ownKeys");
	function _objectSpread$3(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$3(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$3(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	__name(_objectSpread$3, "_objectSpread");
	var StyledDialog = (0, _elementor_ui.styled)(PromptDialog)(function() {
		return {
			"& .MuiDialog-container": {
				marginTop: 0,
				alignItems: "flex-end",
				paddingBottom: "16vh"
			},
			"& .MuiPaper-root": {
				margin: 0,
				maxHeight: "80vh"
			}
		};
	});
	var DialogHeader = function DialogHeader(_ref) {
		var onClose = _ref.onClose;
		var children = _ref.children;
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.AppBar, {
			sx: { fontWeight: "normal" },
			color: "transparent",
			position: "relative"
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Toolbar, { variant: "dense" }, /*#__PURE__*/ react.default.createElement(_elementor_icons.AIIcon, { sx: { mr: 1 } }), /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, {
			component: "span",
			variant: "subtitle2",
			sx: {
				fontWeight: "bold",
				textTransform: "uppercase"
			}
		}, (0, _wordpress_i18n.__)("AI", "elementor")), /*#__PURE__*/ react.default.createElement(_elementor_ui.Chip, {
			label: (0, _wordpress_i18n.__)("Beta", "elementor"),
			color: "default",
			size: "small",
			sx: { ml: 1 }
		}), /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			direction: "row",
			spacing: 1,
			alignItems: "center",
			sx: { ml: "auto" }
		}, children, /*#__PURE__*/ react.default.createElement(_elementor_ui.IconButton, {
			size: "small",
			"aria-label": "close",
			onClick: onClose,
			sx: { "&.MuiButtonBase-root": { mr: -1 } }
		}, /*#__PURE__*/ react.default.createElement(_elementor_icons.XIcon, null)))));
	};
	DialogHeader.propTypes = {
		children: import_prop_types$7.default.node,
		onClose: import_prop_types$7.default.func.isRequired
	};
	var StyledDialogContent = (0, _elementor_ui.styled)(PromptDialog.Content)(function() {
		return { "&.MuiDialogContent-root": { padding: 0 } };
	});
	var LayoutDialog = function LayoutDialog(_ref2) {
		var _ref2$sx = _ref2.sx;
		var sx = _ref2$sx === void 0 ? {} : _ref2$sx;
		var _ref2$PaperProps = _ref2.PaperProps;
		var PaperProps = _ref2$PaperProps === void 0 ? {} : _ref2$PaperProps;
		var props = _objectWithoutProperties$1(_ref2, _excluded$3);
		var _useState2 = _slicedToArray((0, react.useState)({ pointerEvents: "none" }), 2);
		var sxStyle = _useState2[0];
		var setSxStyle = _useState2[1];
		var timeoutRef = (0, react.useRef)(null);
		/**
		* The PromptDialog is using disableScrollLock in order to allow scrolling the page when the Dialog is opened.
		* When using the react-draggable library inside the editor, the background page scroll is not working smoothly.
		* Therefore, we need to delay the pointerEvents: none, which allowing to scroll the page content.
		*/
		return /*#__PURE__*/ react.default.createElement(StyledDialog, _extends$1({
			maxWidth: "md",
			PaperProps: _objectSpread$3({
				sx: { pointerEvents: "auto" },
				onMouseEnter: function onMouseEnter() {
					clearTimeout(timeoutRef.current);
					setSxStyle({ pointerEvents: "all" });
				},
				onMouseLeave: function onMouseLeave() {
					clearTimeout(timeoutRef.current);
					timeoutRef.current = setTimeout(function() {
						setSxStyle({ pointerEvents: "none" });
					}, 200);
				}
			}, PaperProps)
		}, props, { sx: _objectSpread$3(_objectSpread$3({}, sxStyle), sx) }));
	};
	LayoutDialog.propTypes = {
		sx: import_prop_types$7.default.object,
		PaperProps: import_prop_types$7.default.object
	};
	LayoutDialog.Header = DialogHeader;
	LayoutDialog.Content = StyledDialogContent;

//#endregion
//#region modules/ai/assets/js/editor/pages/form-layout/components/prompt-autocomplete.js
	init_slicedToArray();
	init_objectWithoutProperties$1();
	init_extends();
	init_defineProperty$1();
	init_prompt_library_link();
	var _excluded$2 = ["onSubmit"];
	function ownKeys$2(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	__name(ownKeys$2, "ownKeys");
	function _objectSpread$2(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$2(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$2(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	__name(_objectSpread$2, "_objectSpread");
	var TextInput = (0, react.forwardRef)(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.TextField, _extends$1({
			autoFocus: true,
			multiline: true,
			size: "small",
			maxRows: 3,
			color: "secondary",
			variant: "standard"
		}, props, {
			inputRef: ref,
			InputProps: _objectSpread$2(_objectSpread$2({}, props.InputProps), {}, {
				type: "search",
				sx: { pt: 0 }
			})
		}));
	});
	TextInput.propTypes = { InputProps: import_prop_types$7.default.object };
	var PaperComponent = function PaperComponent(props) {
		var libraryLink = "variation" === useConfig().mode ? "https://go.elementor.com/ai-prompt-library-variations/" : "https://go.elementor.com/ai-prompt-library-containers/";
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Paper, _extends$1({}, props, {
			elevation: 8,
			sx: { borderRadius: 2 }
		}), /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, {
			component: _elementor_ui.Box,
			color: function color(theme) {
				return theme.palette.text.tertiary;
			},
			variant: "caption",
			paddingX: 2,
			paddingY: 1
		}, (0, _wordpress_i18n.__)("Suggested Prompts", "elementor")), /*#__PURE__*/ react.default.createElement(_elementor_ui.Divider, null), props.children, /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, { sx: { m: 2 } }, /*#__PURE__*/ react.default.createElement(PromptLibraryLink, { libraryLink })));
	};
	PaperComponent.propTypes = { children: import_prop_types$7.default.node };
	var PromptAutocomplete = function PromptAutocomplete(_ref) {
		var onSubmit = _ref.onSubmit;
		var props = _objectWithoutProperties$1(_ref, _excluded$2);
		var _useState2 = _slicedToArray((0, react.useState)(false), 2);
		var showSuggestions = _useState2[0];
		var setShowSuggestions = _useState2[1];
		var theme = (0, _elementor_ui.useTheme)();
		var itemHeight = parseInt(theme.spacing(4));
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Autocomplete, _extends$1({
			PaperComponent,
			ListboxProps: { sx: { maxHeight: 5 * itemHeight } },
			renderOption: function renderOption(optionProps, option) {
				return /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, _extends$1({}, optionProps, {
					title: option.text,
					noWrap: true,
					variant: "body2",
					component: _elementor_ui.Box,
					sx: { "&.MuiAutocomplete-option": {
						display: "block",
						minHeight: itemHeight
					} }
				}), option.text);
			},
			freeSolo: true,
			fullWidth: true,
			disableClearable: true,
			open: showSuggestions,
			onClose: function onClose(e) {
				var _e$relatedTarget;
				return setShowSuggestions("A" === ((_e$relatedTarget = e.relatedTarget) === null || _e$relatedTarget === void 0 ? void 0 : _e$relatedTarget.tagName));
			},
			onKeyDown: function onKeyDown(e) {
				if ("Enter" === e.key && !e.shiftKey && !showSuggestions) onSubmit(e);
				else if ("/" === e.key && "" === e.target.value) {
					e.preventDefault();
					setShowSuggestions(true);
				}
			}
		}, props));
	};
	PromptAutocomplete.propTypes = { onSubmit: import_prop_types$7.default.func.isRequired };
	PromptAutocomplete.TextInput = TextInput;

//#endregion
//#region modules/ai/assets/js/editor/icons/arrow-left-icon.js
	init_extends();
	var ArrowLeftIcon = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 24 24" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M9.53033 7.46967C9.82322 7.76256 9.82322 8.23744 9.53033 8.53033L6.81066 11.25H19C19.4142 11.25 19.75 11.5858 19.75 12C19.75 12.4142 19.4142 12.75 19 12.75H6.81066L9.53033 15.4697C9.82322 15.7626 9.82322 16.2374 9.53033 16.5303C9.23744 16.8232 8.76256 16.8232 8.46967 16.5303L4.46967 12.5303C4.17678 12.2374 4.17678 11.7626 4.46967 11.4697L8.46967 7.46967C8.76256 7.17678 9.23744 7.17678 9.53033 7.46967Z"
		}));
	});

//#endregion
//#region modules/ai/assets/js/editor/icons/x-circle-icon.js
	init_extends();
	var XCircleIcon = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 24 24" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", { d: "M12 2.69231C6.8595 2.69231 2.69231 6.8595 2.69231 12C2.69231 17.1405 6.8595 21.3077 12 21.3077C17.1405 21.3077 21.3077 17.1405 21.3077 12C21.3077 6.8595 17.1405 2.69231 12 2.69231ZM1 12C1 5.92487 5.92487 1 12 1C18.0751 1 23 5.92487 23 12C23 18.0751 18.0751 23 12 23C5.92487 23 1 18.0751 1 12ZM9.14527 9.14527C9.47571 8.81483 10.0115 8.81483 10.3419 9.14527L12 10.8034L13.6581 9.14527C13.9885 8.81483 14.5243 8.81483 14.8547 9.14527C15.1852 9.47571 15.1852 10.0115 14.8547 10.3419L13.1966 12L14.8547 13.6581C15.1852 13.9885 15.1852 14.5243 14.8547 14.8547C14.5243 15.1852 13.9885 15.1852 13.6581 14.8547L12 13.1966L10.3419 14.8547C10.0115 15.1852 9.47571 15.1852 9.14527 14.8547C8.81483 14.5243 8.81483 13.9885 9.14527 13.6581L10.8034 12L9.14527 10.3419C8.81483 10.0115 8.81483 9.47571 9.14527 9.14527Z" }));
	});

//#endregion
//#region modules/ai/assets/js/editor/icons/plus-circle-icon.js
	init_extends();
	var PlusCircleIcon = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 24 24" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", { d: "M12 2.69231C6.8595 2.69231 2.69231 6.8595 2.69231 12C2.69231 17.1405 6.8595 21.3077 12 21.3077C17.1405 21.3077 21.3077 17.1405 21.3077 12C21.3077 6.8595 17.1405 2.69231 12 2.69231ZM1 12C1 5.92487 5.92487 1 12 1C18.0751 1 23 5.92487 23 12C23 18.0751 18.0751 23 12 23C5.92487 23 1 18.0751 1 12ZM12 7.76923C12.4673 7.76923 12.8462 8.14807 12.8462 8.61538V11.1538H15.3846C15.8519 11.1538 16.2308 11.5327 16.2308 12C16.2308 12.4673 15.8519 12.8462 15.3846 12.8462H12.8462V15.3846C12.8462 15.8519 12.4673 16.2308 12 16.2308C11.5327 16.2308 11.1538 15.8519 11.1538 15.3846V12.8462H8.61538C8.14807 12.8462 7.76923 12.4673 7.76923 12C7.76923 11.5327 8.14807 11.1538 8.61538 11.1538H11.1538V8.61538C11.1538 8.14807 11.5327 7.76923 12 7.76923Z" }));
	});

//#endregion
//#region modules/ai/assets/js/editor/components/alert-dialog.js
	init_slicedToArray();
	var AlertDialog = function AlertDialog(props) {
		var _useState2 = _slicedToArray((0, react.useState)(true), 2);
		var isShown = _useState2[0];
		var setIsShown = _useState2[1];
		if (!isShown) return null;
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Dialog, {
			open: true,
			maxWidth: "lg"
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.DialogContent, { sx: { padding: 0 } }, /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, { sx: {
			textAlign: "center",
			padding: 3
		} }, props.message), /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			alignItems: "center",
			spacing: 2,
			marginBottom: 2
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, {
			variant: "contained",
			type: "button",
			color: "primary",
			onClick: function onClick() {
				var _props$onClose;
				setIsShown(false);
				(_props$onClose = props.onClose) === null || _props$onClose === void 0 || _props$onClose.call(props);
			}
		}, (0, _wordpress_i18n.__)("Close", "elementor")))));
	};
	AlertDialog.propTypes = {
		message: import_prop_types$7.default.string.isRequired,
		onClose: import_prop_types$7.default.func
	};

//#endregion
//#region modules/ai/assets/js/editor/hooks/use-timeout.js
	init_slicedToArray();
	var useTimeout = function useTimeout(delay) {
		var _useState2 = _slicedToArray((0, react.useState)(false), 2);
		var isTimeout = _useState2[0];
		var setIsTimeout = _useState2[1];
		var timeoutIdRef = (0, react.useRef)(null);
		var turnOffTimeout = function turnOffTimeout() {
			clearTimeout(timeoutIdRef.current);
			setIsTimeout(false);
		};
		(0, react.useEffect)(function() {
			timeoutIdRef.current = setTimeout(function() {
				setIsTimeout(true);
			}, delay);
			return function() {
				clearTimeout(timeoutIdRef.current);
			};
		}, [delay]);
		return [isTimeout, turnOffTimeout];
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-layout/context/remote-config.js
	init_asyncToGenerator();
	init_slicedToArray();
	init_api();
	var RemoteConfigContext = react.default.createContext({});
	var useRemoteConfig = function useRemoteConfig() {
		return react.default.useContext(RemoteConfigContext);
	};
	var CONFIG_KEYS = {
		WEB_BASED_BUILDER_URL: "webBasedBuilderUrl",
		AUTH_TOKEN: "jwt"
	};
	var RemoteConfigProvider = function RemoteConfigProvider(props) {
		var _useState2 = _slicedToArray((0, react.useState)(false), 2);
		var isLoading = _useState2[0];
		var setIsLoading = _useState2[1];
		var _useState4 = _slicedToArray((0, react.useState)(false), 2);
		var isLoaded = _useState4[0];
		var setIsLoaded = _useState4[1];
		var _useState6 = _slicedToArray((0, react.useState)(false), 2);
		var isError = _useState6[0];
		var setIsError = _useState6[1];
		var _useState8 = _slicedToArray((0, react.useState)({}), 2);
		var remoteConfig = _useState8[0];
		var setRemoteConfig = _useState8[1];
		var fetchData = /*#__PURE__*/ function() {
			var _ref = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee() {
				var result;
				return import_regenerator$2.default.wrap(function(_context) {
					while (1) switch (_context.prev = _context.next) {
						case 0:
							setIsLoading(true);
							setIsError(false);
							_context.prev = 1;
							_context.next = 2;
							return getRemoteConfig().finally(function() {
								setIsLoaded(true);
								setIsLoading(false);
							});
						case 2:
							result = _context.sent;
							if (result.config) {
								_context.next = 3;
								break;
							}
							throw new Error("Invalid remote config");
						case 3:
							setRemoteConfig(result.config);
							_context.next = 5;
							break;
						case 4:
							_context.prev = 4;
							_context["catch"](1);
							setIsError(true);
							setIsLoaded(true);
							setIsLoading(false);
						case 5:
						case "end": return _context.stop();
					}
				}, _callee, null, [[1, 4]]);
			}));
			return function fetchData() {
				return _ref.apply(this, arguments);
			};
		}();
		(0, react.useEffect)(function() {
			window.addEventListener("elementor/connect/success", fetchData);
			return function() {
				window.removeEventListener("elementor/connect/success", fetchData);
			};
		}, []);
		if (!isLoaded && !isLoading) fetchData();
		return /*#__PURE__*/ react.default.createElement(RemoteConfigContext.Provider, { value: {
			isLoading,
			isLoaded,
			isError,
			remoteConfig
		} }, props.children);
	};
	RemoteConfigProvider.propTypes = {
		children: import_prop_types$7.default.node.isRequired,
		onError: import_prop_types$7.default.func.isRequired
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-layout/components/attachments/url-dialog.js
	init_slicedToArray();
	init_requests_ids();
	var UrlDialog = function UrlDialog(props) {
		var _useTimeout2 = _slicedToArray(useTimeout(1e4), 2);
		var isTimeout = _useTimeout2[0];
		var turnOffTimeout = _useTimeout2[1];
		var _useUserInfo = useUserInfo();
		var isLoading = _useUserInfo.isLoading;
		var initialUsagePercentage = _useUserInfo.usagePercentage;
		var updateUsagePercentage = useRequestIds().updateUsagePercentage;
		var _useState2 = _slicedToArray((0, react.useState)(false), 2);
		var isInitUsageDone = _useState2[0];
		var setIsInitUsageDone = _useState2[1];
		var builderUrl = useRemoteConfig().remoteConfig[CONFIG_KEYS.WEB_BASED_BUILDER_URL];
		var iframeOrigin = (builderUrl ? new URL(builderUrl) : {}).origin;
		var isOpen = (0, react.useRef)(false);
		(0, react.useEffect)(function() {
			if (!isInitUsageDone && !isLoading && (initialUsagePercentage || 0 === initialUsagePercentage)) {
				updateUsagePercentage(initialUsagePercentage);
				setIsInitUsageDone(true);
			}
		}, [
			isLoading,
			initialUsagePercentage,
			isInitUsageDone,
			updateUsagePercentage
		]);
		(0, react.useEffect)(function() {
			if (!isOpen.current) try {
				window.$e.run("ai-integration/open-choose-element", { url: props.url });
				isOpen.current = true;
			} catch (error) {
				console.error(error);
			}
		}, [isOpen.current]);
		(0, react.useEffect)(function() {
			var onMessage = function onMessage(event) {
				if (event.origin !== iframeOrigin) return;
				var _event$data = event.data;
				var type = _event$data.type;
				var html = _event$data.html;
				var url = _event$data.url;
				switch (type) {
					case "element-selector/close":
						isOpen.current = false;
						props.onClose();
						break;
					case "element-selector/loaded":
						turnOffTimeout();
						isOpen.current = true;
						break;
					case "element-selector/attach":
						props.onAttach([{
							type: "url",
							previewHTML: html,
							content: html,
							label: url ? new URL(url).href : "",
							source: USER_URL_SOURCE
						}]);
						break;
				}
			};
			window.addEventListener("message", onMessage);
			return function() {
				window.removeEventListener("message", onMessage);
			};
		}, [
			iframeOrigin,
			props,
			turnOffTimeout
		]);
		return /*#__PURE__*/ react.default.createElement(react.default.Fragment, null, !isOpen.current && !isTimeout && /*#__PURE__*/ react.default.createElement(_elementor_ui.Dialog, {
			open: true,
			maxWidth: "lg"
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, { sx: {
			textAlign: "center",
			padding: 3
		} }, (0, _wordpress_i18n.__)("Loading...", "elementor"))), isTimeout && /*#__PURE__*/ react.default.createElement(AlertDialog, {
			message: (0, _wordpress_i18n.__)("The app is not responding. Please try again later. (#408)", "elementor"),
			onClose: props.onClose
		}));
	};
	UrlDialog.propTypes = {
		onAttach: import_prop_types$7.default.func.isRequired,
		onClose: import_prop_types$7.default.func.isRequired,
		url: import_prop_types$7.default.string
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-layout/components/attachments/library-dialog.js
	var LibraryDialog = function LibraryDialog(props) {
		var isApplyingTemplate = (0, react.useRef)(false);
		(0, react.useEffect)(function() {
			var onLibraryHide = function onLibraryHide() {
				if (isApplyingTemplate.current) return;
				props.onClose();
			};
			$e.components.get("library").layout.getModal().on("hide", onLibraryHide);
			return function() {
				$e.components.get("library").layout.getModal().off("hide", onLibraryHide);
			};
		}, [props]);
		(0, react.useEffect)(function() {
			var onMessage = function onMessage(event) {
				var _event$data = event.data;
				var type = _event$data.type;
				var json = _event$data.json;
				var html = _event$data.html;
				var label = _event$data.label;
				var source = _event$data.source;
				switch (type) {
					case "library/attach:start":
						isApplyingTemplate.current = true;
						break;
					case "library/attach":
						props.onAttach([{
							type: ATTACHMENT_TYPE_JSON,
							previewHTML: html,
							content: json,
							label,
							source
						}]);
						isApplyingTemplate.current = false;
						props.onClose();
						break;
				}
			};
			window.addEventListener("message", onMessage);
			return function() {
				window.removeEventListener("message", onMessage);
			};
		});
		$e.run("library/open", {
			toDefault: true,
			mode: "ai-attachment"
		});
		isApplyingTemplate.current = false;
		return null;
	};
	LibraryDialog.propTypes = {
		onAttach: import_prop_types$7.default.func.isRequired,
		onClose: import_prop_types$7.default.func.isRequired
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-layout/components/attachments/attach-dialog.js
	var AttachDialog = function AttachDialog(props) {
		var type = props.type;
		var url = props.url;
		switch (type) {
			case "url": return /*#__PURE__*/ react.default.createElement(UrlDialog, {
				url,
				onAttach: props.onAttach,
				onClose: props.onClose
			});
			case MENU_TYPE_LIBRARY: return /*#__PURE__*/ react.default.createElement(LibraryDialog, {
				onAttach: props.onAttach,
				onClose: props.onClose
			});
		}
		return null;
	};
	AttachDialog.propTypes = {
		type: import_prop_types$7.default.string,
		onAttach: import_prop_types$7.default.func,
		onClose: import_prop_types$7.default.func,
		url: import_prop_types$7.default.string
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-layout/components/attachments/menu.js
	init_slicedToArray();
	init_use_introduction();
	var Menu = function Menu(props) {
		var _useState2 = _slicedToArray((0, react.useState)(false), 2);
		var isOpen = _useState2[0];
		var setIsOpen = _useState2[1];
		var _useState4 = _slicedToArray((0, react.useState)(null), 2);
		var selectedType = _useState4[0];
		var setSelectedType = _useState4[1];
		var direction = (0, _elementor_ui.useTheme)().direction;
		var anchorRef = (0, react.useRef)(null);
		var _useIntroduction = useIntroduction("e-ai-attachment-badge");
		var isViewed = _useIntroduction.isViewed;
		var markAsViewed = _useIntroduction.markAsViewed;
		return /*#__PURE__*/ react.default.createElement(react.default.Fragment, null, /*#__PURE__*/ react.default.createElement(_elementor_ui.IconButton, {
			size: "small",
			ref: anchorRef,
			disabled: props.disabled,
			onClick: function onClick() {
				setIsOpen(true);
				if (!isViewed) markAsViewed();
			},
			color: "secondary"
		}, function() {
			if (isOpen) return /*#__PURE__*/ react.default.createElement(XCircleIcon, { fontSize: "small" });
			else if (isViewed) return /*#__PURE__*/ react.default.createElement(PlusCircleIcon, { fontSize: "small" });
			return /*#__PURE__*/ react.default.createElement(_elementor_ui.Badge, {
				color: "primary",
				badgeContent: " ",
				variant: "dot"
			}, /*#__PURE__*/ react.default.createElement(PlusCircleIcon, { fontSize: "small" }));
		}()), /*#__PURE__*/ react.default.createElement(_elementor_ui.Popover, {
			open: isOpen,
			anchorEl: anchorRef.current,
			onClose: function onClose() {
				return setIsOpen(false);
			},
			anchorOrigin: {
				vertical: "bottom",
				horizontal: "rtl" === direction ? "right" : "left"
			},
			transformOrigin: {
				vertical: "top",
				horizontal: "rtl" === direction ? "right" : "left"
			}
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, { sx: { width: 440 } }, props.items.map(function(item) {
			var IconComponent = item.icon;
			return /*#__PURE__*/ react.default.createElement(_elementor_ui.MenuItem, {
				key: item.type,
				onClick: function onClick() {
					setSelectedType(item.type);
					setIsOpen(false);
				}
			}, /*#__PURE__*/ react.default.createElement(_elementor_ui.ListItemIcon, null, /*#__PURE__*/ react.default.createElement(IconComponent, null)), item.title);
		}))), /*#__PURE__*/ react.default.createElement(AttachDialog, {
			type: selectedType,
			onAttach: props.onAttach,
			onClose: function onClose() {
				setIsOpen(false);
				setSelectedType(null);
			}
		}));
	};
	Menu.propTypes = {
		items: import_prop_types$7.default.arrayOf(import_prop_types$7.default.shape({
			title: import_prop_types$7.default.string.isRequired,
			type: import_prop_types$7.default.string.isRequired,
			icon: import_prop_types$7.default.elementType
		})).isRequired,
		onAttach: import_prop_types$7.default.func.isRequired,
		disabled: import_prop_types$7.default.bool
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-layout/components/attachments/thumbnail.js
	var _templateObject$3;
	var THUMBNAIL_SIZE = 64;
	var StyledBody = (0, _elementor_ui.styled)("body")(_templateObject$3 || (_templateObject$3 = _taggedTemplateLiteral(["\n	html, body {\n		margin: 0;\n		padding: 0;\n		overflow: hidden;\n	}\n\n	body > * {\n		width: 100% !important;\n	}\n\n	body > img {\n		height: 100%;\n		object-fit: cover;\n	}\n\n	body:has(> img) {\n		height: ", "px\n	}\n"])), 64);
	var Thumbnail = function Thumbnail(props) {
		var _props$html$match;
		var _props$html$match2;
		var dataWidth = (_props$html$match = props.html.match("data-width=\"(?<width>\\d+)\"")) === null || _props$html$match === void 0 || (_props$html$match = _props$html$match.groups) === null || _props$html$match === void 0 ? void 0 : _props$html$match.width;
		var dataHeight = (_props$html$match2 = props.html.match("data-height=\"(?<height>\\d+)\"")) === null || _props$html$match2 === void 0 || (_props$html$match2 = _props$html$match2.groups) === null || _props$html$match2 === void 0 ? void 0 : _props$html$match2.height;
		var width = dataWidth ? parseInt(dataWidth) : 64;
		var height = dataHeight ? parseInt(dataHeight) : 64;
		var scale = 64 / Math.min(height, width);
		var top = height > width ? (64 - 64 * (height / width)) / 2 : 0;
		var left = width > height ? (64 - 64 * (width / height)) / 2 : 0;
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, {
			dir: "ltr",
			sx: {
				position: "relative",
				cursor: "default",
				overflow: "hidden",
				border: "1px solid",
				borderColor: "grey.300",
				borderRadius: 1,
				boxSizing: "border-box",
				width: 64,
				height: 64,
				opacity: props.disabled ? .5 : 1
			}
		}, /*#__PURE__*/ react.default.createElement("iframe", {
			title: (0, _wordpress_i18n.__)("Preview", "elementor"),
			sandbox: "",
			srcDoc: "<style>" + StyledBody.componentStyle.rules.join("") + "</style>" + props.html,
			style: {
				border: "none",
				overflow: "hidden",
				width,
				height,
				transform: "scale(".concat(scale, ")"),
				transformOrigin: "".concat(left, "px ").concat(top, "px")
			}
		}));
	};
	Thumbnail.propTypes = {
		html: import_prop_types$7.default.string.isRequired,
		disabled: import_prop_types$7.default.bool
	};

//#endregion
//#region modules/ai/assets/js/editor/types/attachment.js
	var AttachmentPropType = import_prop_types$7.default.shape({
		type: import_prop_types$7.default.string,
		previewHTML: import_prop_types$7.default.string,
		content: import_prop_types$7.default.string,
		label: import_prop_types$7.default.string,
		source: import_prop_types$7.default.string
	});
	var AttachmentsTypesPropType = import_prop_types$7.default.shape({ type: import_prop_types$7.default.shape({
		promptPlaceholder: import_prop_types$7.default.string,
		promptSuggestions: import_prop_types$7.default.arrayOf(import_prop_types$7.default.shape({ text: import_prop_types$7.default.string.isRequired })),
		previewGenerator: import_prop_types$7.default.func
	}) });

//#endregion
//#region modules/ai/assets/js/editor/pages/form-layout/components/attachments/thumbnail-json.js
	var ThumbnailJson = function ThumbnailJson(props) {
		var _props$attachments;
		var attachment = (_props$attachments = props.attachments) === null || _props$attachments === void 0 ? void 0 : _props$attachments.find(function(item) {
			return "json" === item.type;
		});
		if (!attachment) return null;
		if (!attachment.previewHTML) return /*#__PURE__*/ react.default.createElement(_elementor_ui.Skeleton, {
			animation: "wave",
			variant: "rounded",
			width: 60,
			height: 60
		});
		return /*#__PURE__*/ react.default.createElement(Thumbnail, {
			html: attachment.previewHTML,
			disabled: props.disabled
		});
	};
	ThumbnailJson.propTypes = {
		attachments: import_prop_types$7.default.arrayOf(AttachmentPropType).isRequired,
		disabled: import_prop_types$7.default.bool
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-layout/components/attachments/thumbnail-url.js
	var ThumbnailUrl = function ThumbnailUrl(props) {
		var _props$attachments;
		var attachment = (_props$attachments = props.attachments) === null || _props$attachments === void 0 ? void 0 : _props$attachments.find(function(item) {
			return "url" === item.type;
		});
		if (!attachment) return null;
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, { sx: {
			position: "relative",
			"&:hover::before": {
				content: "\"\"",
				position: "absolute",
				userSelect: "none",
				inset: 0,
				backgroundColor: "rgba(0,0,0,0.6)",
				borderRadius: 1,
				zIndex: 1
			},
			"&:hover .remove-attachment": { display: "flex" }
		} }, /*#__PURE__*/ react.default.createElement(_elementor_ui.IconButton, {
			className: "remove-attachment",
			size: "small",
			"aria-label": (0, _wordpress_i18n.__)("Remove", "elementor"),
			disabled: props.disabled,
			onClick: function onClick(event) {
				event.stopPropagation();
				props.onDetach();
			},
			sx: {
				display: "none",
				position: "absolute",
				insetInlineEnd: 4,
				insetBlockStart: 4,
				backgroundColor: "secondary.main",
				zIndex: 1,
				borderRadius: 1,
				p: "3px",
				"&:hover": { backgroundColor: "secondary.dark" }
			}
		}, /*#__PURE__*/ react.default.createElement(_elementor_icons.TrashIcon, { sx: {
			fontSize: "1.125rem",
			color: "common.white"
		} })), /*#__PURE__*/ react.default.createElement(Thumbnail, {
			disabled: props.disabled,
			html: attachment.previewHTML
		}));
	};
	ThumbnailUrl.propTypes = {
		attachments: import_prop_types$7.default.arrayOf(AttachmentPropType).isRequired,
		disabled: import_prop_types$7.default.bool,
		onDetach: import_prop_types$7.default.func
	};

//#endregion
//#region modules/ai/assets/js/editor/icons/website-icon.js
	init_extends();
	var WebsiteIcon = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 24 24" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M4.16707 3.95837C4.11182 3.95837 4.05883 3.98032 4.01976 4.01939C3.98069 4.05846 3.95874 4.11145 3.95874 4.16671V6.04171H6.04207V3.95837H4.16707ZM4.16707 2.70837C3.7803 2.70837 3.40937 2.86202 3.13588 3.13551C2.86239 3.409 2.70874 3.77993 2.70874 4.16671V15.8334C2.70874 16.2201 2.86239 16.5911 3.13588 16.8646C3.40937 17.1381 3.7803 17.2917 4.16707 17.2917H15.8337C16.2205 17.2917 16.5914 17.1381 16.8649 16.8646C17.1384 16.5911 17.2921 16.2201 17.2921 15.8334V4.16671C17.2921 3.77993 17.1384 3.409 16.8649 3.13551C16.5914 2.86202 16.2205 2.70837 15.8337 2.70837H4.16707ZM7.29207 3.95837V6.04171H16.0421V4.16671C16.0421 4.11145 16.0201 4.05846 15.9811 4.01939C15.942 3.98032 15.889 3.95837 15.8337 3.95837H7.29207ZM16.0421 7.29171H3.95874V15.8334C3.95874 15.8886 3.98069 15.9416 4.01976 15.9807C4.05883 16.0198 4.11182 16.0417 4.16707 16.0417H15.8337C15.889 16.0417 15.942 16.0198 15.9811 15.9807C16.0201 15.9416 16.0421 15.8886 16.0421 15.8334V7.29171Z"
		}));
	});

//#endregion
//#region modules/ai/assets/js/editor/icons/copy-page-icon.js
	init_extends();
	var CopyPageIcon = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 24 24" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M16.6667 0.208496C17.0534 0.208496 17.4244 0.362142 17.6979 0.635632C17.9714 0.909123 18.125 1.28006 18.125 1.66683V11.6668C18.125 12.0536 17.9714 12.4245 17.6979 12.698C17.4244 12.9715 17.0534 13.1252 16.6667 13.1252H14.7917V16.6668C14.7917 17.0536 14.638 17.4245 14.3645 17.698C14.091 17.9715 13.7201 18.1252 13.3333 18.1252H3.33333C2.94656 18.1252 2.57563 17.9715 2.30214 17.698C2.02865 17.4245 1.875 17.0536 1.875 16.6668V6.66683C1.875 6.28005 2.02865 5.90912 2.30214 5.63563C2.57563 5.36214 2.94656 5.2085 3.33333 5.2085H5.20833V1.66683C5.20833 1.28005 5.36198 0.909122 5.63547 0.635632C5.90896 0.362142 6.27989 0.208496 6.66667 0.208496H16.6667ZM6.66667 1.4585C6.61141 1.4585 6.55842 1.48045 6.51935 1.51952C6.48028 1.55859 6.45833 1.61158 6.45833 1.66683V3.54183H8.54167V1.4585H6.66667ZM3.125 9.79183V16.6668C3.125 16.7221 3.14695 16.7751 3.18602 16.8141C3.22509 16.8532 3.27808 16.8752 3.33333 16.8752H13.3333C13.3886 16.8752 13.4416 16.8532 13.4806 16.8141C13.5197 16.7751 13.5417 16.7221 13.5417 16.6668V13.1252H6.66667C6.27989 13.1252 5.90896 12.9715 5.63547 12.698C5.36198 12.4245 5.20833 12.0536 5.20833 11.6668V9.79183H3.125ZM5.20833 8.54183H3.125V6.66683C3.125 6.61158 3.14695 6.55859 3.18602 6.51952C3.22509 6.48045 3.27808 6.4585 3.33333 6.4585H5.20833V8.54183ZM6.45833 11.6668C6.45833 11.7221 6.48028 11.7751 6.51935 11.8141C6.55842 11.8532 6.61141 11.8752 6.66667 11.8752H16.6667C16.7219 11.8752 16.7749 11.8532 16.814 11.8141C16.853 11.7751 16.875 11.7221 16.875 11.6668V4.79183H6.45833V11.6668ZM9.79167 1.4585V3.54183H16.875V1.66683C16.875 1.61157 16.853 1.55858 16.814 1.51952C16.7749 1.48045 16.7219 1.4585 16.6667 1.4585H9.79167Z"
		}));
	});

//#endregion
//#region modules/ai/assets/js/editor/pages/form-layout/components/attachments.js
	init_extends();
	var ATTACHMENT_TYPE_JSON = "json";
	var ATTACHMENT_TYPE_URL = "url";
	var MENU_TYPE_LIBRARY = "library";
	var USER_URL_SOURCE = "user-url";
	var Attachments = function Attachments(props) {
		if (!props.attachments.length) return /*#__PURE__*/ react.default.createElement(Menu, {
			disabled: props.disabled,
			onAttach: props.onAttach,
			items: [{
				title: (0, _wordpress_i18n.__)("Reference a website", "elementor"),
				icon: WebsiteIcon,
				type: "url"
			}, {
				title: (0, _wordpress_i18n.__)("Create variations from Template Library", "elementor"),
				icon: CopyPageIcon,
				type: MENU_TYPE_LIBRARY
			}]
		});
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			direction: "row",
			spacing: 1
		}, props.attachments.map(function(attachment, index) {
			switch (attachment.type) {
				case ATTACHMENT_TYPE_JSON: return /*#__PURE__*/ react.default.createElement(ThumbnailJson, _extends$1({ key: index }, props));
				case "url": return /*#__PURE__*/ react.default.createElement(ThumbnailUrl, _extends$1({ key: index }, props));
				default: return null;
			}
		}));
	};
	Attachments.propTypes = {
		attachments: import_prop_types$7.default.arrayOf(AttachmentPropType).isRequired,
		onAttach: import_prop_types$7.default.func.isRequired,
		onDetach: import_prop_types$7.default.func,
		disabled: import_prop_types$7.default.bool
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-layout/components/prompt-form.js
	init_slicedToArray();
	init_extends();
	init_objectWithoutProperties$1();
	var _excluded$1 = ["tooltip"];
	var PROMPT_SUGGESTIONS = Object.freeze([
		{ text: (0, _wordpress_i18n.__)("Hero section on [topic] with heading, text, buttons on the right, and an image on the left", "elementor.com") },
		{ text: (0, _wordpress_i18n.__)("About Us section on [topic] with heading, text, and big image below", "elementor.com") },
		{ text: (0, _wordpress_i18n.__)("Team section with four image boxes showcasing team members", "elementor.com") },
		{ text: (0, _wordpress_i18n.__)("FAQ section with a toggle widget showcasing FAQs about [topic]", "elementor.com") },
		{ text: (0, _wordpress_i18n.__)("Gallery section with a carousel displaying three images at once", "elementor.com") },
		{ text: (0, _wordpress_i18n.__)("Contact section with a form for [topic]", "elementor.com") },
		{ text: (0, _wordpress_i18n.__)("Client section featuring companies' logos", "elementor.com") },
		{ text: (0, _wordpress_i18n.__)("Testimonial section with testimonials, each featuring a star rating and an image", "elementor.com") },
		{ text: (0, _wordpress_i18n.__)("Service section about [topic], showcasing four services with buttons", "elementor.com") },
		{ text: (0, _wordpress_i18n.__)("Stats section with counters displaying data about [topic]", "elementor.com") },
		{ text: (0, _wordpress_i18n.__)("Quote section with colored background, featuring a centered quote", "elementor.com") },
		{ text: (0, _wordpress_i18n.__)("Pricing section for [topic] with a pricing list", "elementor.com") },
		{ text: (0, _wordpress_i18n.__)("Subscribe section featuring a simple email form, inviting users to stay informed on [topic]", "elementor.com") }
	]);
	var IconButtonWithTooltip = function IconButtonWithTooltip(_ref) {
		var tooltip = _ref.tooltip;
		var props = _objectWithoutProperties$1(_ref, _excluded$1);
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Tooltip, { title: tooltip }, /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, {
			component: "span",
			sx: { cursor: props.disabled ? "default" : "pointer" }
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.IconButton, props)));
	};
	IconButtonWithTooltip.propTypes = {
		tooltip: import_prop_types$7.default.string,
		disabled: import_prop_types$7.default.bool
	};
	var BackButton = function BackButton(props) {
		return /*#__PURE__*/ react.default.createElement(IconButtonWithTooltip, _extends$1({
			size: "small",
			color: "secondary",
			tooltip: (0, _wordpress_i18n.__)("Back to results", "elementor")
		}, props), /*#__PURE__*/ react.default.createElement(ArrowLeftIcon, null));
	};
	var EditButton = function EditButton(props) {
		return /*#__PURE__*/ react.default.createElement(IconButtonWithTooltip, _extends$1({
			size: "small",
			color: "primary",
			tooltip: (0, _wordpress_i18n.__)("Edit prompt", "elementor")
		}, props), /*#__PURE__*/ react.default.createElement(EditIcon$1, null));
	};
	var GenerateButton = function GenerateButton(props) {
		return /*#__PURE__*/ react.default.createElement(GenerateSubmit, _extends$1({
			size: "small",
			fullWidth: false
		}, props), (0, _wordpress_i18n.__)("Generate", "elementor"));
	};
	var PromptForm = (0, react.forwardRef)(function(_ref2, ref) {
		var _attachments$;
		var attachments = _ref2.attachments;
		var isActive = _ref2.isActive;
		var isLoading = _ref2.isLoading;
		var _ref2$showActions = _ref2.showActions;
		var showActions = _ref2$showActions === void 0 ? false : _ref2$showActions;
		var onAttach = _ref2.onAttach;
		var onDetach = _ref2.onDetach;
		var _onSubmit = _ref2.onSubmit;
		var onBack = _ref2.onBack;
		var onEdit = _ref2.onEdit;
		var _ref2$shouldResetProm = _ref2.shouldResetPrompt;
		var shouldResetPrompt = _ref2$shouldResetProm === void 0 ? false : _ref2$shouldResetProm;
		var _useState2 = _slicedToArray((0, react.useState)(""), 2);
		var prompt = _useState2[0];
		var setPrompt = _useState2[1];
		(0, react.useEffect)(function() {
			if (shouldResetPrompt) setPrompt("");
		}, [shouldResetPrompt]);
		var _usePromptEnhancer = usePromptEnhancer(prompt, "layout");
		var isEnhancing = _usePromptEnhancer.isEnhancing;
		var enhance = _usePromptEnhancer.enhance;
		var previousPrompt = (0, react.useRef)("");
		var attachmentsTypes = useConfig().attachmentsTypes;
		var isInputDisabled = isLoading || isEnhancing || !isActive;
		var isInputEmpty = "" === prompt && !attachments.length;
		var isGenerateDisabled = isInputDisabled || isInputEmpty;
		var attachmentsConfig = attachmentsTypes[((_attachments$ = attachments[0]) === null || _attachments$ === void 0 ? void 0 : _attachments$.type) || ""];
		var promptSuggestions = (attachmentsConfig === null || attachmentsConfig === void 0 ? void 0 : attachmentsConfig.promptSuggestions) || PROMPT_SUGGESTIONS;
		var promptPlaceholder = (attachmentsConfig === null || attachmentsConfig === void 0 ? void 0 : attachmentsConfig.promptPlaceholder) || (0, _wordpress_i18n.__)("Press '/' for suggested prompts or describe the layout you want to create", "elementor");
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			component: "form",
			onSubmit: function onSubmit(e) {
				return _onSubmit(e, prompt);
			},
			direction: "row",
			sx: { p: 3 },
			alignItems: "start",
			gap: 1
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			direction: "row",
			alignItems: "start",
			flexGrow: 1,
			spacing: 2
		}, showActions && (isActive ? /*#__PURE__*/ react.default.createElement(BackButton, {
			disabled: isLoading || isEnhancing,
			onClick: function handleBack() {
				setPrompt(previousPrompt.current);
				onBack();
			}
		}) : /*#__PURE__*/ react.default.createElement(EditButton, {
			disabled: isLoading,
			onClick: function handleEdit() {
				previousPrompt.current = prompt;
				onEdit();
			}
		})), /*#__PURE__*/ react.default.createElement(Attachments, {
			attachments,
			onAttach,
			onDetach,
			disabled: isInputDisabled
		}), /*#__PURE__*/ react.default.createElement(PromptAutocomplete, {
			value: prompt,
			disabled: isInputDisabled,
			onSubmit: function onSubmit(e) {
				return _onSubmit(e, prompt);
			},
			options: promptSuggestions,
			onChange: function onChange(_, selectedValue) {
				return setPrompt(selectedValue.text + " ");
			},
			renderInput: function renderInput(params) {
				return /*#__PURE__*/ react.default.createElement(PromptAutocomplete.TextInput, _extends$1({}, params, {
					ref,
					onChange: function onChange(e) {
						return setPrompt(e.target.value);
					},
					placeholder: promptPlaceholder
				}));
			}
		})), /*#__PURE__*/ react.default.createElement(EnhanceButton, {
			size: "small",
			disabled: isGenerateDisabled || "" === prompt,
			isLoading: isEnhancing,
			onClick: function onClick() {
				return enhance().then(function(_ref3) {
					var result = _ref3.result;
					return setPrompt(result);
				});
			}
		}), /*#__PURE__*/ react.default.createElement(GenerateButton, { disabled: isGenerateDisabled }));
	});
	PromptForm.propTypes = {
		isActive: import_prop_types$7.default.bool,
		onAttach: import_prop_types$7.default.func,
		onDetach: import_prop_types$7.default.func,
		isLoading: import_prop_types$7.default.bool,
		showActions: import_prop_types$7.default.bool,
		onSubmit: import_prop_types$7.default.func.isRequired,
		onBack: import_prop_types$7.default.func.isRequired,
		onEdit: import_prop_types$7.default.func.isRequired,
		attachments: import_prop_types$7.default.arrayOf(AttachmentPropType),
		shouldResetPrompt: import_prop_types$7.default.bool
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-layout/components/screenshot-container.js
	var ScreenshotContainer = (0, _elementor_ui.styled)(_elementor_ui.Box, { shouldForwardProp: function shouldForwardProp(prop) {
		return prop !== "outlineOffset";
	} })(function(_ref) {
		var theme = _ref.theme;
		var selected = _ref.selected;
		var height = _ref.height;
		var disabled = _ref.disabled;
		var _ref$outlineOffset = _ref.outlineOffset;
		var outlineOffset = _ref$outlineOffset === void 0 ? "0px" : _ref$outlineOffset;
		var outlineColor = selected ? theme.palette.text.primary : theme.palette.text.disabled;
		var outline = "2px solid ".concat(outlineColor);
		return {
			height,
			cursor: disabled ? "default" : "pointer",
			overflow: "hidden",
			boxSizing: "border-box",
			backgroundPosition: "top center",
			backgroundSize: "100% auto",
			backgroundRepeat: "no-repeat",
			backgroundColor: theme.palette.common.white,
			borderRadius: theme.shape.borderRadius * .5,
			outlineOffset,
			outline,
			opacity: disabled ? "0.4" : "1",
			transition: "all 50ms linear",
			"&:hover": disabled ? {} : { outlineColor: theme.palette.text.primary }
		};
	});

//#endregion
//#region modules/ai/assets/js/editor/pages/form-layout/components/screenshot-unavailable.js
	init_extends();
	init_defineProperty$1();
	function ownKeys$1(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	__name(ownKeys$1, "ownKeys");
	function _objectSpread$1(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys$1(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$1(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	__name(_objectSpread$1, "_objectSpread");
	function ScreenshotUnavailable(props) {
		return /*#__PURE__*/ react.default.createElement(ScreenshotContainer, _extends$1({}, props, { sx: _objectSpread$1(_objectSpread$1({}, props.sx || {}), {}, {
			display: "flex",
			alignItems: "center",
			justifyContent: "center",
			backgroundColor: "background.paper",
			color: "text.tertiary",
			fontStyle: "italic",
			fontSize: "12px",
			paddingInline: 12,
			textAlign: "center",
			lineHeight: 1.5
		}) }), (0, _wordpress_i18n.__)("Preview unavailable", "elementor"));
	}
	ScreenshotUnavailable.propTypes = { sx: import_prop_types$7.default.object };

//#endregion
//#region modules/ai/assets/js/editor/icons/lock-icon.js
	init_extends();
	var LockIcon = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 24 24" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M7.8125 11.9996C7.29473 11.9996 6.875 12.4473 6.875 12.9996V18.9996C6.875 19.5519 7.29473 19.9996 7.8125 19.9996H17.1875C17.7053 19.9996 18.125 19.5519 18.125 18.9996V12.9996C18.125 12.4473 17.7053 11.9996 17.1875 11.9996H7.8125ZM5 12.9996C5 11.3428 6.2592 9.99963 7.8125 9.99963H17.1875C18.7408 9.99963 20 11.3428 20 12.9996V18.9996C20 20.6565 18.7408 21.9996 17.1875 21.9996H7.8125C6.2592 21.9996 5 20.6565 5 18.9996V12.9996Z"
		}), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M12.5 3.90527C11.7044 3.90527 10.9413 4.22134 10.3787 4.78395C9.81607 5.34656 9.5 6.10962 9.5 6.90527V10.9053C9.5 11.4576 9.05228 11.9053 8.5 11.9053C7.94772 11.9053 7.5 11.4576 7.5 10.9053V6.90527C7.5 5.57919 8.02678 4.30742 8.96447 3.36974C9.90215 2.43206 11.1739 1.90527 12.5 1.90527C13.8261 1.90527 15.0979 2.43206 16.0355 3.36974C16.9732 4.30742 17.5 5.57919 17.5 6.90527V10.9053C17.5 11.4576 17.0523 11.9053 16.5 11.9053C15.9477 11.9053 15.5 11.4576 15.5 10.9053V6.90527C15.5 6.10962 15.1839 5.34656 14.6213 4.78395C14.0587 4.22134 13.2956 3.90527 12.5 3.90527Z"
		}), /*#__PURE__*/ react.default.createElement("path", { d: "M6 12H19V20H6V12Z" }));
	});

//#endregion
//#region modules/ai/assets/js/editor/pages/form-layout/components/pro-template-indicator.js
	init_slicedToArray();
	var popoverId = "e-pro-upgrade-popover";
	var StyledContent = (0, _elementor_ui.styled)(_elementor_ui.Paper)(function(_ref) {
		var theme = _ref.theme;
		return {
			position: "relative",
			padding: theme.spacing(3),
			boxShadow: theme.shadows[4],
			zIndex: "9999"
		};
	});
	var StyledArrow = (0, _elementor_ui.styled)(_elementor_ui.Box)(function(_ref2) {
		var theme = _ref2.theme;
		return {
			position: "absolute",
			width: theme.spacing(5),
			height: theme.spacing(5),
			overflow: "hidden",
			left: "100% !important",
			transform: "translateX(-50%) translateY(-50%) rotate(var(--rotate, 0deg)) !important",
			"&::after": {
				backgroundColor: theme.palette.background.paper,
				content: "\"\"",
				display: "block",
				position: "absolute",
				width: theme.spacing(2.5),
				height: theme.spacing(2.5),
				top: "50%",
				left: "50%",
				transform: "translateX(-50%) translateY(-50%) rotate(45deg)",
				boxShadow: "5px -5px 5px 0px rgba(0, 0, 0, 0.2)",
				backgroundImage: "linear-gradient(rgba(255, 255, 255, 0.05), rgba(255, 255, 255, 0.05))"
			}
		};
	});
	var ProTemplateIndicator = function ProTemplateIndicator() {
		var actionUrl = "https://go.elementor.com/go-pro-ai/";
		var actionLabel = (0, _wordpress_i18n.__)("Go Pro", "elementor");
		var _useState2 = _slicedToArray((0, react.useState)(false), 2);
		var isPopoverOpen = _useState2[0];
		var setIsPopoverOpen = _useState2[1];
		var anchorEl = (0, react.useRef)(null);
		var arrowEl = (0, react.useRef)(null);
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, {
			flexDirection: "row-reverse",
			component: "span",
			display: "flex",
			onMouseLeave: function hidePopover() {
				return setIsPopoverOpen(false);
			},
			alignItems: "center"
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.IconButton, {
			ref: anchorEl,
			onMouseEnter: function showPopover() {
				return setIsPopoverOpen(true);
			},
			onClick: function onClick(e) {
				return e.stopPropagation();
			},
			"aria-owns": isPopoverOpen ? popoverId : void 0,
			"aria-haspopup": "true",
			sx: {
				m: 1,
				"&:hover": { backgroundColor: "action.selected" }
			}
		}, /*#__PURE__*/ react.default.createElement(LockIcon, { sx: { color: "text.primary" } })), /*#__PURE__*/ react.default.createElement(_elementor_ui.Popper, {
			open: isPopoverOpen,
			popperOptions: {
				placement: "left-start",
				modifiers: [{
					name: "arrow",
					enabled: true,
					options: {
						element: arrowEl.current,
						padding: 5
					}
				}, {
					name: "offset",
					options: { offset: [0, 10] }
				}]
			},
			anchorEl: anchorEl.current,
			sx: {
				zIndex: "9999",
				maxWidth: 300
			}
		}, /*#__PURE__*/ react.default.createElement(StyledContent, null, /*#__PURE__*/ react.default.createElement(StyledArrow, { ref: arrowEl }), /*#__PURE__*/ react.default.createElement(_elementor_ui.Stack, {
			alignItems: "start",
			spacing: 2
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Chip, {
			color: "promotion",
			variant: "outlined",
			size: "small",
			label: (0, _wordpress_i18n.__)("Pro", "elementor"),
			icon: /*#__PURE__*/ react.default.createElement(LockIcon, null)
		}), /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, { variant: "body2" }, (0, _wordpress_i18n.__)("This result includes an Elementor Pro widget that's not available with your current plan. Upgrade to use all the widgets in this result.", "elementor")), /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, {
			variant: "contained",
			color: "promotion",
			size: "small",
			href: actionUrl,
			target: "_blank",
			sx: { alignSelf: "flex-end" }
		}, actionLabel)))));
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-layout/components/template-badge.js
	var TemplateBadge = function TemplateBadge(props) {
		var hasPro = useConfig().hasPro;
		if ("Pro" === props.type && !hasPro) return /*#__PURE__*/ react.default.createElement(ProTemplateIndicator, null);
		return null;
	};
	TemplateBadge.propTypes = { type: import_prop_types$7.default.string };

//#endregion
//#region modules/ai/assets/js/editor/pages/form-layout/components/screenshot.js
	init_defineProperty$1();
	function ownKeys(e, r) {
		var t = Object.keys(e);
		if (Object.getOwnPropertySymbols) {
			var o = Object.getOwnPropertySymbols(e);
			r && (o = o.filter(function(r) {
				return Object.getOwnPropertyDescriptor(e, r).enumerable;
			})), t.push.apply(t, o);
		}
		return t;
	}
	function _objectSpread(e) {
		for (var r = 1; r < arguments.length; r++) {
			var t = null != arguments[r] ? arguments[r] : {};
			r % 2 ? ownKeys(Object(t), !0).forEach(function(r) {
				_defineProperty$1(e, r, t[r]);
			}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function(r) {
				Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
			});
		}
		return e;
	}
	var SCREENSHOT_HEIGHT = "138px";
	var Screenshot = function Screenshot(_ref) {
		var url = _ref.url;
		var type = _ref.type;
		var _ref$isLoading = _ref.isLoading;
		var isLoading = _ref$isLoading === void 0 ? false : _ref$isLoading;
		var _ref$isSelected = _ref.isSelected;
		var isSelected = _ref$isSelected === void 0 ? false : _ref$isSelected;
		var isPlaceholder = _ref.isPlaceholder;
		var disabled = _ref.disabled;
		var onClick = _ref.onClick;
		var _ref$sx = _ref.sx;
		var sx = _ref$sx === void 0 ? {} : _ref$sx;
		var outlineOffset = _ref.outlineOffset;
		if (isPlaceholder) return /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, { sx: _objectSpread({ height: SCREENSHOT_HEIGHT }, sx) });
		if (isLoading) return /*#__PURE__*/ react.default.createElement(_elementor_ui.Skeleton, {
			width: "100%",
			animation: "wave",
			variant: "rounded",
			height: SCREENSHOT_HEIGHT,
			sx
		});
		if (!url) return /*#__PURE__*/ react.default.createElement(ScreenshotUnavailable, {
			selected: isSelected,
			disabled,
			sx,
			onClick,
			height: SCREENSHOT_HEIGHT,
			outlineOffset
		});
		return /*#__PURE__*/ react.default.createElement(ScreenshotContainer, {
			selected: isSelected,
			disabled,
			sx: _objectSpread({ backgroundImage: "url('".concat(url, "')") }, sx),
			onClick,
			height: SCREENSHOT_HEIGHT,
			outlineOffset
		}, /*#__PURE__*/ react.default.createElement(TemplateBadge, { type }));
	};
	Screenshot.propTypes = {
		isSelected: import_prop_types$7.default.bool,
		isLoading: import_prop_types$7.default.bool,
		isPlaceholder: import_prop_types$7.default.bool,
		disabled: import_prop_types$7.default.bool,
		onClick: import_prop_types$7.default.func.isRequired,
		url: import_prop_types$7.default.string,
		type: import_prop_types$7.default.string,
		sx: import_prop_types$7.default.object,
		outlineOffset: import_prop_types$7.default.string
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-layout/hooks/use-layout-prompt.js
	init_api();
	init_use_prompt();
	var useLayoutPrompt = function useLayoutPrompt(type, initialValue) {
		return usePrompt(function(requestBody, signal) {
			requestBody.variationType = type;
			return generateLayout(requestBody, signal);
		}, initialValue);
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-layout/hooks/use-screenshot.js
	init_asyncToGenerator();
	init_slicedToArray();
	var ERROR_INITIAL_VALUE = "";
	var useScreenshot = function useScreenshot(type, onData) {
		var _useState2 = _slicedToArray((0, react.useState)(ERROR_INITIAL_VALUE), 2);
		var error = _useState2[0];
		var setError = _useState2[1];
		var _useState4 = _slicedToArray((0, react.useState)(false), 2);
		var isLoading = _useState4[0];
		var setIsLoading = _useState4[1];
		var layoutData = useLayoutPrompt(type, null);
		return {
			generate: function generate(requestBody, signal) {
				setIsLoading(true);
				setError(ERROR_INITIAL_VALUE);
				return layoutData.send(requestBody, signal).then(/*#__PURE__*/ function() {
					var _ref = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee(data) {
						var createdScreenshot;
						return import_regenerator$2.default.wrap(function(_context) {
							while (1) switch (_context.prev = _context.next) {
								case 0:
									_context.next = 1;
									return onData(data.result);
								case 1:
									createdScreenshot = _context.sent;
									createdScreenshot.sendUsageData = function() {
										return layoutData.sendUsageData(data);
									};
									createdScreenshot.baseTemplateId = data.baseTemplateId;
									createdScreenshot.type = data.type;
									return _context.abrupt("return", createdScreenshot);
								case 2:
								case "end": return _context.stop();
							}
						}, _callee);
					}));
					return function(_x) {
						return _ref.apply(this, arguments);
					};
				}()).catch(function(err) {
					setError(err.extra_data ? err : err.message || err);
					throw err;
				}).finally(function() {
					return setIsLoading(false);
				});
			},
			error,
			isLoading
		};
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-layout/hooks/use-screenshots.js
	init_asyncToGenerator();
	init_slicedToArray();
	init_requests_ids();
	var PENDING_VALUE = { isPending: true };
	var useScreenshots = function useScreenshots(_ref) {
		var onData = _ref.onData;
		var _useState2 = _slicedToArray((0, react.useState)([]), 2);
		var screenshots = _useState2[0];
		var setScreenshots = _useState2[1];
		/**
		* The ids for each request are:
		* - editorSessionId: a unique id for each editor opening
		* - sessionId: a unique id for each session. (open the AI builder)
		* - generateId: a unique id for each generate request. (prompt change)
		* - batchId: a unique id for each batch of generate requests. (generate, regenerate)
		* - requestId: a unique id for each generate request.
		*/
		var currentContext = useConfig().currentContext;
		var _useRequestIds = useRequestIds();
		var editorSessionId = _useRequestIds.editorSessionId;
		var sessionId = _useRequestIds.sessionId;
		var setRequest = _useRequestIds.setRequest;
		var setBatch = _useRequestIds.setBatch;
		var setGenerate = _useRequestIds.setGenerate;
		var generateIdRef = (0, react.useRef)("");
		var batchId = setBatch();
		var screenshotsData = [
			useScreenshot(0, onData),
			useScreenshot(1, onData),
			useScreenshot(2, onData)
		];
		var screenshotsGroupCount = screenshotsData.length;
		var error = screenshotsData.every(function(s) {
			return s === null || s === void 0 ? void 0 : s.error;
		}) ? screenshotsData[0].error : "";
		var isLoading = screenshotsData.some(function(s) {
			return s === null || s === void 0 ? void 0 : s.isLoading;
		});
		var abortController = (0, react.useRef)(null);
		var abort = function abort() {
			var _abortController$curr;
			return (_abortController$curr = abortController.current) === null || _abortController$curr === void 0 ? void 0 : _abortController$curr.abort();
		};
		var createScreenshots = /*#__PURE__*/ function() {
			var _ref2 = _asyncToGenerator$1(/*#__PURE__*/ import_regenerator$2.default.mark(function _callee(prompt, attachments) {
				var onGenerate;
				var onError;
				var promises;
				var results;
				var isAllFailed;
				return import_regenerator$2.default.wrap(function(_context) {
					while (1) switch (_context.prev = _context.next) {
						case 0:
							abortController.current = new AbortController();
							onGenerate = function onGenerate(screenshot) {
								setScreenshots(function(prev) {
									var updatedData = _toConsumableArray(prev);
									var pendingIndex = updatedData.indexOf(PENDING_VALUE);
									updatedData[pendingIndex] = screenshot;
									return updatedData;
								});
								return true;
							};
							onError = function onError() {
								setScreenshots(function(prev) {
									var updatedData = _toConsumableArray(prev);
									var pendingIndex = updatedData.lastIndexOf(PENDING_VALUE);
									updatedData[pendingIndex] = { isError: true };
									return updatedData;
								});
								return false;
							};
							promises = screenshotsData.map(function(_ref3) {
								var generate = _ref3.generate;
								return generate({
									prompt,
									prevGeneratedIds: screenshots.map(function(screenshot) {
										return screenshot.baseTemplateId || "";
									}),
									currentContext,
									ids: {
										editorSessionId: editorSessionId.current,
										sessionId: sessionId.current,
										generateId: generateIdRef.current,
										batchId: batchId.current,
										requestId: setRequest().current
									},
									attachments: attachments.map(function(_ref4) {
										return {
											type: _ref4.type,
											content: _ref4.content,
											label: _ref4.label,
											source: _ref4.source
										};
									})
								}, abortController.current.signal).then(onGenerate).catch(onError);
							});
							_context.next = 1;
							return Promise.all(promises);
						case 1:
							results = _context.sent;
							isAllFailed = results.every(function(value) {
								return false === value;
							});
							if (isAllFailed) setScreenshots(function(prev) {
								var updatedData = _toConsumableArray(prev);
								updatedData.splice(screenshotsGroupCount * -1);
								return updatedData;
							});
						case 2:
						case "end": return _context.stop();
					}
				}, _callee);
			}));
			return function createScreenshots(_x, _x2) {
				return _ref2.apply(this, arguments);
			};
		}();
		return {
			generate: function generate(prompt, attachments) {
				var placeholders = Array(screenshotsGroupCount).fill(PENDING_VALUE);
				generateIdRef.current = setGenerate().current;
				setScreenshots(placeholders);
				createScreenshots(prompt, attachments);
			},
			regenerate: function regenerate(prompt, attachments) {
				var placeholders = Array(screenshotsGroupCount).fill(PENDING_VALUE);
				setScreenshots(function(prev) {
					return [].concat(_toConsumableArray(prev), _toConsumableArray(placeholders));
				});
				createScreenshots(prompt, attachments);
			},
			screenshots,
			isLoading,
			error,
			abort
		};
	};

//#endregion
//#region modules/ai/assets/js/editor/pages/form-layout/hooks/use-slider.js
	init_slicedToArray();
	var SCREENSHOTS_PER_PAGE = 3;
	var useSlider = function useSlider() {
		var _ref = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
		var _ref$slidesCount = _ref.slidesCount;
		var slidesCount = _ref$slidesCount === void 0 ? 0 : _ref$slidesCount;
		var _ref$slidesPerPage = _ref.slidesPerPage;
		var slidesPerPage = _ref$slidesPerPage === void 0 ? 3 : _ref$slidesPerPage;
		var _ref$gapPercentage = _ref.gapPercentage;
		var gapPercentage = _ref$gapPercentage === void 0 ? 2 : _ref$gapPercentage;
		var _useState2 = _slicedToArray((0, react.useState)(1), 2);
		var currentPage = _useState2[0];
		var setCurrentPage = _useState2[1];
		var slideWidthPercentage = (100 - gapPercentage * (slidesPerPage - 1)) / slidesPerPage;
		var offsetXPercentage = (slideWidthPercentage + gapPercentage) * slidesPerPage * (currentPage - 1) * -1;
		var pagesCount = Math.ceil(slidesCount / slidesPerPage);
		(0, react.useEffect)(function() {
			if (currentPage > 1 && currentPage > pagesCount) setCurrentPage(pagesCount);
		}, [pagesCount]);
		return {
			currentPage,
			setCurrentPage,
			pagesCount,
			slidesPerPage,
			gapPercentage,
			offsetXPercentage,
			slideWidthPercentage
		};
	};

//#endregion
//#region modules/ai/assets/js/editor/icons/minimize-diagonal-icon.js
	init_extends();
	var MinimizeDiagonalIcon = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 24 24" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M3.46967 3.46967C3.76256 3.17678 4.23744 3.17678 4.53033 3.46967L9.25 8.18934V6C9.25 5.58579 9.58579 5.25 10 5.25C10.4142 5.25 10.75 5.58579 10.75 6V10C10.75 10.4142 10.4142 10.75 10 10.75H6C5.58579 10.75 5.25 10.4142 5.25 10C5.25 9.58579 5.58579 9.25 6 9.25H8.18934L3.46967 4.53033C3.17678 4.23744 3.17678 3.76256 3.46967 3.46967ZM14 13.25H18C18.4142 13.25 18.75 13.5858 18.75 14C18.75 14.4142 18.4142 14.75 18 14.75H15.8107L20.5303 19.4697C20.8232 19.7626 20.8232 20.2374 20.5303 20.5303C20.2374 20.8232 19.7626 20.8232 19.4697 20.5303L14.75 15.8107V18C14.75 18.4142 14.4142 18.75 14 18.75C13.5858 18.75 13.25 18.4142 13.25 18V14C13.25 13.5858 13.5858 13.25 14 13.25Z"
		}));
	});

//#endregion
//#region modules/ai/assets/js/editor/icons/expand-diagonal-icon.js
	init_extends();
	var ExpandDiagonalIcon = react.default.forwardRef(function(props, ref) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.SvgIcon, _extends$1({ viewBox: "0 0 24 24" }, props, { ref }), /*#__PURE__*/ react.default.createElement("path", {
			fillRule: "evenodd",
			clipRule: "evenodd",
			d: "M4 3.25H8C8.41421 3.25 8.75 3.58579 8.75 4C8.75 4.41421 8.41421 4.75 8 4.75H5.81066L10.5303 9.46967C10.8232 9.76256 10.8232 10.2374 10.5303 10.5303C10.2374 10.8232 9.76256 10.8232 9.46967 10.5303L4.75 5.81066V8C4.75 8.41421 4.41421 8.75 4 8.75C3.58579 8.75 3.25 8.41421 3.25 8V4C3.25 3.58579 3.58579 3.25 4 3.25ZM13.4697 13.4697C13.7626 13.1768 14.2374 13.1768 14.5303 13.4697L19.25 18.1893V16C19.25 15.5858 19.5858 15.25 20 15.25C20.4142 15.25 20.75 15.5858 20.75 16V20C20.75 20.4142 20.4142 20.75 20 20.75H16C15.5858 20.75 15.25 20.4142 15.25 20C15.25 19.5858 15.5858 19.25 16 19.25H18.1893L13.4697 14.5303C13.1768 14.2374 13.1768 13.7626 13.4697 13.4697Z"
		}));
	});

//#endregion
//#region modules/ai/assets/js/editor/pages/form-layout/components/attachments/prompt-power-notice.js
	init_use_introduction();
	var PromptPowerNotice = function PromptPowerNotice() {
		var _useIntroduction = useIntroduction("e-ai-builder-attachments-power");
		var isViewed = _useIntroduction.isViewed;
		var markAsViewed = _useIntroduction.markAsViewed;
		if (isViewed) return null;
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, { sx: {
			pt: 2,
			px: 2,
			pb: 0
		} }, /*#__PURE__*/ react.default.createElement(_elementor_ui.Alert, {
			severity: "info",
			onClose: function onClose() {
				return markAsViewed();
			}
		}, /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, {
			variant: "body2",
			display: "inline-block",
			sx: { paddingInlineEnd: 1 }
		}, (0, _wordpress_i18n.__)("You’ve got the power.", "elementor")), /*#__PURE__*/ react.default.createElement(_elementor_ui.Typography, {
			variant: "body2",
			display: "inline-block"
		}, (0, _wordpress_i18n.__)("Craft your prompt to affect content, images and/or colors - whichever you decide.", "elementor"))));
	};

//#endregion
//#region node_modules/validator/lib/util/assertString.js
	var require_assertString = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		Object.defineProperty(exports, "__esModule", { value: true });
		exports.default = assertString;
		function assertString(input) {
			if (input === void 0 || input === null) throw new TypeError("Expected a string but received a ".concat(input));
			if (input.constructor.name !== "String") throw new TypeError("Expected a string but received a ".concat(input.constructor.name));
		}
		module.exports = exports.default;
		module.exports.default = exports.default;
	}));

//#endregion
//#region node_modules/validator/lib/util/checkHost.js
	var require_checkHost = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		Object.defineProperty(exports, "__esModule", { value: true });
		exports.default = checkHost;
		function isRegExp(obj) {
			return Object.prototype.toString.call(obj) === "[object RegExp]";
		}
		function checkHost(host, matches) {
			for (var i = 0; i < matches.length; i++) {
				var match = matches[i];
				if (host === match || isRegExp(match) && match.test(host)) return true;
			}
			return false;
		}
		module.exports = exports.default;
		module.exports.default = exports.default;
	}));

//#endregion
//#region node_modules/validator/lib/util/includesString.js
	var require_includesString = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		Object.defineProperty(exports, "__esModule", { value: true });
		exports.default = void 0;
		var includes = function includes(str, val) {
			return str.indexOf(val) !== -1;
		};
		exports.default = includes;
		module.exports = exports.default;
		module.exports.default = exports.default;
	}));

//#endregion
//#region node_modules/validator/lib/util/merge.js
	var require_merge = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		Object.defineProperty(exports, "__esModule", { value: true });
		exports.default = merge;
		function merge() {
			var obj = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
			var defaults = arguments.length > 1 ? arguments[1] : void 0;
			for (var key in defaults) if (typeof obj[key] === "undefined") obj[key] = defaults[key];
			return obj;
		}
		module.exports = exports.default;
		module.exports.default = exports.default;
	}));

//#endregion
//#region node_modules/validator/lib/isFQDN.js
	var require_isFQDN = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		Object.defineProperty(exports, "__esModule", { value: true });
		exports.default = isFQDN;
		var _assertString = _interopRequireDefault(require_assertString());
		var _merge = _interopRequireDefault(require_merge());
		function _interopRequireDefault(e) {
			return e && e.__esModule ? e : { default: e };
		}
		var default_fqdn_options = {
			require_tld: true,
			allow_underscores: false,
			allow_trailing_dot: false,
			allow_numeric_tld: false,
			allow_wildcard: false,
			ignore_max_length: false
		};
		function isFQDN(str, options) {
			(0, _assertString.default)(str);
			options = (0, _merge.default)(options, default_fqdn_options);
			if (options.allow_trailing_dot && str[str.length - 1] === ".") str = str.substring(0, str.length - 1);
			if (options.allow_wildcard === true && str.indexOf("*.") === 0) str = str.substring(2);
			var parts = str.split(".");
			var tld = parts[parts.length - 1];
			if (options.require_tld) {
				if (parts.length < 2) return false;
				if (!options.allow_numeric_tld && !/^([a-z\u00A1-\u00A8\u00AA-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF]{2,}|xn[a-z0-9-]{2,})$/i.test(tld)) return false;
				if (/\s/.test(tld)) return false;
			}
			if (!options.allow_numeric_tld && /^\d+$/.test(tld)) return false;
			return parts.every(function(part) {
				if (part.length > 63 && !options.ignore_max_length) return false;
				if (!/^[a-z_\u00a1-\uffff0-9-]+$/i.test(part)) return false;
				if (/[\uff01-\uff5e]/.test(part)) return false;
				if (/^-|-$/.test(part)) return false;
				if (!options.allow_underscores && /_/.test(part)) return false;
				return true;
			});
		}
		module.exports = exports.default;
		module.exports.default = exports.default;
	}));

//#endregion
//#region node_modules/validator/lib/isIP.js
	var require_isIP = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		Object.defineProperty(exports, "__esModule", { value: true });
		exports.default = isIP;
		var _assertString = _interopRequireDefault(require_assertString());
		function _interopRequireDefault(e) {
			return e && e.__esModule ? e : { default: e };
		}
		function _typeof(o) {
			"@babel/helpers - typeof";
			return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o) {
				return typeof o;
			} : function(o) {
				return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
			}, _typeof(o);
		}
		/**
		11.3.  Examples
		
		The following addresses
		
		fe80::1234 (on the 1st link of the node)
		ff02::5678 (on the 5th link of the node)
		ff08::9abc (on the 10th organization of the node)
		
		would be represented as follows:
		
		fe80::1234%1
		ff02::5678%5
		ff08::9abc%10
		
		(Here we assume a natural translation from a zone index to the
		<zone_id> part, where the Nth zone of any scope is translated into
		"N".)
		
		If we use interface names as <zone_id>, those addresses could also be
		represented as follows:
		
		fe80::1234%ne0
		ff02::5678%pvc1.3
		ff08::9abc%interface10
		
		where the interface "ne0" belongs to the 1st link, "pvc1.3" belongs
		to the 5th link, and "interface10" belongs to the 10th organization.
		* * */
		var IPv4SegmentFormat = "(?:[0-9]|[1-9][0-9]|1[0-9][0-9]|2[0-4][0-9]|25[0-5])";
		var IPv4AddressFormat = "(".concat(IPv4SegmentFormat, "[.]){3}").concat(IPv4SegmentFormat);
		var IPv4AddressRegExp = new RegExp("^".concat(IPv4AddressFormat, "$"));
		var IPv6SegmentFormat = "(?:[0-9a-fA-F]{1,4})";
		var IPv6AddressRegExp = new RegExp("^(" + "(?:".concat(IPv6SegmentFormat, ":){7}(?:").concat(IPv6SegmentFormat, "|:)|") + "(?:".concat(IPv6SegmentFormat, ":){6}(?:").concat(IPv4AddressFormat, "|:").concat(IPv6SegmentFormat, "|:)|") + "(?:".concat(IPv6SegmentFormat, ":){5}(?::").concat(IPv4AddressFormat, "|(:").concat(IPv6SegmentFormat, "){1,2}|:)|") + "(?:".concat(IPv6SegmentFormat, ":){4}(?:(:").concat(IPv6SegmentFormat, "){0,1}:").concat(IPv4AddressFormat, "|(:").concat(IPv6SegmentFormat, "){1,3}|:)|") + "(?:".concat(IPv6SegmentFormat, ":){3}(?:(:").concat(IPv6SegmentFormat, "){0,2}:").concat(IPv4AddressFormat, "|(:").concat(IPv6SegmentFormat, "){1,4}|:)|") + "(?:".concat(IPv6SegmentFormat, ":){2}(?:(:").concat(IPv6SegmentFormat, "){0,3}:").concat(IPv4AddressFormat, "|(:").concat(IPv6SegmentFormat, "){1,5}|:)|") + "(?:".concat(IPv6SegmentFormat, ":){1}(?:(:").concat(IPv6SegmentFormat, "){0,4}:").concat(IPv4AddressFormat, "|(:").concat(IPv6SegmentFormat, "){1,6}|:)|") + "(?::((?::".concat(IPv6SegmentFormat, "){0,5}:").concat(IPv4AddressFormat, "|(?::").concat(IPv6SegmentFormat, "){1,7}|:))") + ")(%[0-9a-zA-Z.]{1,})?$");
		function isIP(ipAddress) {
			var options = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
			(0, _assertString.default)(ipAddress);
			var version = (_typeof(options) === "object" ? options.version : arguments[1]) || "";
			if (!version) return isIP(ipAddress, { version: 4 }) || isIP(ipAddress, { version: 6 });
			if (version.toString() === "4") return IPv4AddressRegExp.test(ipAddress);
			if (version.toString() === "6") return IPv6AddressRegExp.test(ipAddress);
			return false;
		}
		module.exports = exports.default;
		module.exports.default = exports.default;
	}));

//#endregion
//#region node_modules/validator/lib/isURL.js
	var require_isURL = /* @__PURE__ */ __commonJSMin(((exports, module) => {
		Object.defineProperty(exports, "__esModule", { value: true });
		exports.default = isURL;
		var _assertString = _interopRequireDefault(require_assertString());
		var _checkHost = _interopRequireDefault(require_checkHost());
		var _includesString = _interopRequireDefault(require_includesString());
		var _isFQDN = _interopRequireDefault(require_isFQDN());
		var _isIP = _interopRequireDefault(require_isIP());
		var _merge = _interopRequireDefault(require_merge());
		function _interopRequireDefault(e) {
			return e && e.__esModule ? e : { default: e };
		}
		function _slicedToArray(r, e) {
			return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest();
		}
		function _nonIterableRest() {
			throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
		}
		function _unsupportedIterableToArray(r, a) {
			if (r) {
				if ("string" == typeof r) return _arrayLikeToArray(r, a);
				var t = {}.toString.call(r).slice(8, -1);
				return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0;
			}
		}
		function _arrayLikeToArray(r, a) {
			(null == a || a > r.length) && (a = r.length);
			for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e];
			return n;
		}
		function _iterableToArrayLimit(r, l) {
			var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"];
			if (null != t) {
				var e;
				var n;
				var i;
				var u;
				var a = [];
				var f = !0;
				var o = !1;
				try {
					if (i = (t = t.call(r)).next, 0 === l) {
						if (Object(t) !== t) return;
						f = !1;
					} else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0);
				} catch (r) {
					o = !0, n = r;
				} finally {
					try {
						if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
					} finally {
						if (o) throw n;
					}
				}
				return a;
			}
		}
		function _arrayWithHoles(r) {
			if (Array.isArray(r)) return r;
		}
		var default_url_options = {
			protocols: [
				"http",
				"https",
				"ftp"
			],
			require_tld: true,
			require_protocol: false,
			require_host: true,
			require_port: false,
			require_valid_protocol: true,
			allow_underscores: false,
			allow_trailing_dot: false,
			allow_protocol_relative_urls: false,
			allow_fragments: true,
			allow_query_components: true,
			validate_length: true,
			max_allowed_length: 2084
		};
		var wrapped_ipv6 = /^\[([^\]]+)\](?::([0-9]+))?$/;
		function isURL(url, options) {
			(0, _assertString.default)(url);
			if (!url || /[\s<>]/.test(url)) return false;
			if (url.indexOf("mailto:") === 0) return false;
			options = (0, _merge.default)(options, default_url_options);
			if (options.validate_length && url.length > options.max_allowed_length) return false;
			if (!options.allow_fragments && (0, _includesString.default)(url, "#")) return false;
			if (!options.allow_query_components && ((0, _includesString.default)(url, "?") || (0, _includesString.default)(url, "&"))) return false;
			var protocol;
			var auth;
			var host;
			var hostname;
			var port;
			var port_str;
			var split = url.split("#");
			var ipv6;
			url = split.shift();
			split = url.split("?");
			url = split.shift();
			var protocol_match = url.match(/^([a-z][a-z0-9+\-.]*):/i);
			var had_explicit_protocol = false;
			var cleanUpProtocol = function cleanUpProtocol(potential_protocol) {
				had_explicit_protocol = true;
				protocol = potential_protocol.toLowerCase();
				if (options.require_valid_protocol && options.protocols.indexOf(protocol) === -1) return false;
				return url.substring(protocol_match[0].length);
			};
			if (protocol_match) {
				var potential_protocol = protocol_match[1];
				var after_colon = url.substring(protocol_match[0].length);
				if (!(after_colon.slice(0, 2) === "//")) {
					var first_slash_position = after_colon.indexOf("/");
					var before_slash = first_slash_position === -1 ? after_colon : after_colon.substring(0, first_slash_position);
					var at_position = before_slash.indexOf("@");
					if (at_position !== -1) {
						var before_at = before_slash.substring(0, at_position);
						if (/^[a-zA-Z0-9\-_.%:]*$/.test(before_at)) {
							if (options.require_protocol) return false;
						} else {
							url = cleanUpProtocol(potential_protocol);
							if (url === false) return false;
						}
					} else if (/^[0-9]/.test(after_colon)) {
						if (options.require_protocol) return false;
					} else {
						url = cleanUpProtocol(potential_protocol);
						if (url === false) return false;
					}
				} else {
					url = cleanUpProtocol(potential_protocol);
					if (url === false) return false;
				}
			} else if (options.require_protocol) return false;
			if (url.slice(0, 2) === "//") {
				if (!had_explicit_protocol && !options.allow_protocol_relative_urls) return false;
				url = url.slice(2);
			}
			if (url === "") return false;
			split = url.split("/");
			url = split.shift();
			if (url === "" && !options.require_host) return true;
			split = url.split("@");
			if (split.length > 1) {
				if (options.disallow_auth) return false;
				if (split[0] === "") return false;
				auth = split.shift();
				if (auth.indexOf(":") >= 0 && auth.split(":").length > 2) return false;
				var _auth$split2 = _slicedToArray(auth.split(":"), 2);
				var user = _auth$split2[0];
				var password = _auth$split2[1];
				if (user === "" && password === "") return false;
			}
			hostname = split.join("@");
			port_str = null;
			ipv6 = null;
			var ipv6_match = hostname.match(wrapped_ipv6);
			if (ipv6_match) {
				host = "";
				ipv6 = ipv6_match[1];
				port_str = ipv6_match[2] || null;
			} else {
				split = hostname.split(":");
				host = split.shift();
				if (split.length) port_str = split.join(":");
			}
			if (port_str !== null && port_str.length > 0) {
				port = parseInt(port_str, 10);
				if (!/^[0-9]+$/.test(port_str) || port <= 0 || port > 65535) return false;
			} else if (options.require_port) return false;
			if (options.host_whitelist) return (0, _checkHost.default)(host, options.host_whitelist);
			if (host === "" && !options.require_host) return true;
			if (!(0, _isIP.default)(host) && !(0, _isFQDN.default)(host, options) && (!ipv6 || !(0, _isIP.default)(ipv6, 6))) return false;
			host = host || ipv6;
			if (options.host_blacklist && (0, _checkHost.default)(host, options.host_blacklist)) return false;
			return true;
		}
		module.exports = exports.default;
		module.exports.default = exports.default;
	}));

//#endregion
//#region modules/ai/assets/js/editor/pages/form-layout/index.js
	init_objectWithoutProperties$1();
	init_slicedToArray();
	init_extends();
	init_prompt_error_message();
	var import_isURL = /* @__PURE__ */ __toESM(require_isURL());
	init_voice_promotion_alert();
	var _excluded = ["children"];
	var DirectionalMinimizeDiagonalIcon = (0, _elementor_ui.withDirection)(MinimizeDiagonalIcon);
	var DirectionalExpandDiagonalIcon = (0, _elementor_ui.withDirection)(ExpandDiagonalIcon);
	/**
	* @typedef {Object} Attachment
	* @property {('json')} type        - The type of the attachment, currently only `json` is supported.
	* @property {string}   previewHTML - HTML content as a string, representing a preview.
	* @property {string}   content     - Actual content of the attachment as a string.
	* @property {string}   label       - Label for the attachment.
	*/
	var RegenerateButton = function RegenerateButton(props) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, _extends$1({
			size: "small",
			color: "secondary",
			startIcon: /*#__PURE__*/ react.default.createElement(RefreshIcon, null)
		}, props), (0, _wordpress_i18n.__)("Regenerate", "elementor"));
	};
	var UseLayoutButton = function UseLayoutButton(props) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.Button, _extends$1({
			size: "small",
			variant: "contained"
		}, props), (0, _wordpress_i18n.__)("Use Layout", "elementor"));
	};
	UseLayoutButton.propTypes = { sx: import_prop_types$7.default.object };
	var isRegenerateButtonDisabled = function isRegenerateButtonDisabled(screenshots, isLoading, isPromptFormActive) {
		if (isLoading || isPromptFormActive) return true;
		return screenshots.length >= 3 * 5;
	};
	var FormLayout = function FormLayout(_ref) {
		var _screenshots$selected;
		var _screenshots$2;
		var _ref$DialogHeaderProp = _ref.DialogHeaderProps;
		var DialogHeaderProps = _ref$DialogHeaderProp === void 0 ? {} : _ref$DialogHeaderProp;
		var _ref$DialogContentPro = _ref.DialogContentProps;
		var DialogContentProps = _ref$DialogContentPro === void 0 ? {} : _ref$DialogContentPro;
		var initialAttachments = _ref.attachments;
		var _useConfig = useConfig();
		var attachmentsTypes = _useConfig.attachmentsTypes;
		var onData = _useConfig.onData;
		var onInsert = _useConfig.onInsert;
		var onSelect = _useConfig.onSelect;
		var onClose = _useConfig.onClose;
		var onGenerate = _useConfig.onGenerate;
		var _useScreenshots = useScreenshots({ onData });
		var screenshots = _useScreenshots.screenshots;
		var generate = _useScreenshots.generate;
		var regenerate = _useScreenshots.regenerate;
		var isLoading = _useScreenshots.isLoading;
		var error = _useScreenshots.error;
		var abort = _useScreenshots.abort;
		var screenshotOutlineOffset = "2px";
		var _useSlider = useSlider({ slidesCount: screenshots.length });
		var currentPage = _useSlider.currentPage;
		var setCurrentPage = _useSlider.setCurrentPage;
		var pagesCount = _useSlider.pagesCount;
		var gapPercentage = _useSlider.gapPercentage;
		var slidesPerPage = _useSlider.slidesPerPage;
		var offsetXPercentage = _useSlider.offsetXPercentage;
		var slideWidthPercentage = _useSlider.slideWidthPercentage;
		var _useState2 = _slicedToArray((0, react.useState)(-1), 2);
		var selectedScreenshotIndex = _useState2[0];
		var setSelectedScreenshotIndex = _useState2[1];
		var _useState4 = _slicedToArray((0, react.useState)(false), 2);
		var showUnsavedChangesAlert = _useState4[0];
		var setShowUnsavedChangesAlert = _useState4[1];
		var _useState6 = _slicedToArray((0, react.useState)(true), 2);
		var isPromptEditable = _useState6[0];
		var setIsPromptEditable = _useState6[1];
		var _useState8 = _slicedToArray((0, react.useState)([]), 2);
		var attachments = _useState8[0];
		var setAttachments = _useState8[1];
		var _useState0 = _slicedToArray((0, react.useState)(false), 2);
		var shouldRenderWebApp = _useState0[0];
		var setShouldRenderWebApp = _useState0[1];
		var _useState10 = _slicedToArray((0, react.useState)(false), 2);
		var isMinimized = _useState10[0];
		var setIsMinimized = _useState10[1];
		var lastRun = (0, react.useRef)(function() {});
		var promptInputRef = (0, react.useRef)(null);
		var selectedTemplate = (_screenshots$selected = screenshots[selectedScreenshotIndex]) === null || _screenshots$selected === void 0 ? void 0 : _screenshots$selected.template;
		var dialogContentChildren = DialogContentProps.children;
		var dialogContentProps = _objectWithoutProperties$1(DialogContentProps, _excluded);
		var shouldFallbackToEditPrompt = !!(error && 0 === screenshots.length);
		var isPromptFormActive = isPromptEditable || shouldFallbackToEditPrompt;
		var abortAndClose = function abortAndClose() {
			abort();
			onClose();
		};
		var onCloseIntent = function onCloseIntent() {
			if (promptInputRef.current.value.trim() !== "" || screenshots.length > 0) return setShowUnsavedChangesAlert(true);
			abortAndClose();
		};
		var handleGenerate = function handleGenerate(event, prompt) {
			event.preventDefault();
			if ("" === prompt.trim() && 0 === attachments.length) return;
			if ((0, import_isURL.default)(prompt)) {
				setShouldRenderWebApp(true);
				return;
			}
			onGenerate();
			lastRun.current = function() {
				setSelectedScreenshotIndex(-1);
				generate(prompt, attachments);
			};
			lastRun.current();
			setIsPromptEditable(false);
			setCurrentPage(1);
		};
		var handleRegenerate = function handleRegenerate() {
			lastRun.current = function() {
				regenerate(promptInputRef.current.value, attachments);
				setCurrentPage(pagesCount + 1);
			};
			lastRun.current();
		};
		var applyTemplate = function applyTemplate() {
			onInsert(selectedTemplate);
			screenshots[selectedScreenshotIndex].sendUsageData();
			abortAndClose();
		};
		var handleScreenshotClick = function handleScreenshotClick(index, template) {
			return function() {
				if (isPromptFormActive) return;
				setSelectedScreenshotIndex(index);
				onSelect(template);
			};
		};
		/**
		* @param {Attachment[]} items
		*/
		var onAttach = function onAttach(items) {
			items.forEach(function(item) {
				if (!attachmentsTypes[item.type]) throw new Error("Invalid attachment type: ".concat(item.type));
				var typeConfig = attachmentsTypes[item.type];
				if (!item.previewHTML && typeConfig.previewGenerator) typeConfig.previewGenerator(item.content).then(function(html) {
					item.previewHTML = html;
					setAttachments(function(prev) {
						return prev.map(function(attachment) {
							if (attachment.content === item.content) return item;
							return attachment;
						});
					});
				});
			});
			setAttachments(items);
			setShouldRenderWebApp(false);
			setIsPromptEditable(true);
		};
		(0, react.useEffect)(function() {
			var _screenshots$;
			if ((_screenshots$ = screenshots[0]) === null || _screenshots$ === void 0 ? void 0 : _screenshots$.template) {
				onSelect(screenshots[0].template);
				setSelectedScreenshotIndex(0);
			}
		}, [(_screenshots$2 = screenshots[0]) === null || _screenshots$2 === void 0 ? void 0 : _screenshots$2.template]);
		(0, react.useEffect)(function() {
			if (initialAttachments !== null && initialAttachments !== void 0 && initialAttachments.length) onAttach(initialAttachments);
		}, []);
		return /*#__PURE__*/ react.default.createElement(LayoutDialog, { onClose: onCloseIntent }, /*#__PURE__*/ react.default.createElement(LayoutDialog.Header, _extends$1({ onClose: onCloseIntent }, DialogHeaderProps), DialogHeaderProps.children, /*#__PURE__*/ react.default.createElement(_elementor_ui.Tooltip, { title: isMinimized ? (0, _wordpress_i18n.__)("Expand", "elementor") : (0, _wordpress_i18n.__)("Minimize", "elementor") }, /*#__PURE__*/ react.default.createElement(_elementor_ui.IconButton, {
			size: "small",
			"aria-label": "minimize",
			onClick: function onClick() {
				return setIsMinimized(function(prev) {
					return !prev;
				});
			}
		}, isMinimized ? /*#__PURE__*/ react.default.createElement(DirectionalExpandDiagonalIcon, null) : /*#__PURE__*/ react.default.createElement(DirectionalMinimizeDiagonalIcon, null)))), /*#__PURE__*/ react.default.createElement(LayoutDialog.Content, _extends$1({ dividers: true }, dialogContentProps), /*#__PURE__*/ react.default.createElement(_elementor_ui.Collapse, { in: !isMinimized }, dialogContentChildren && /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, { sx: {
			pt: 2,
			px: 2,
			pb: 0
		} }, dialogContentChildren), attachments.length > 0 && /*#__PURE__*/ react.default.createElement(PromptPowerNotice, null), error && /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, { sx: {
			pt: 2,
			px: 2,
			pb: 0
		} }, /*#__PURE__*/ react.default.createElement(PromptErrorMessage, {
			error,
			onRetry: lastRun.current
		})), showUnsavedChangesAlert && /*#__PURE__*/ react.default.createElement(UnsavedChangesAlert, {
			open: showUnsavedChangesAlert,
			title: (0, _wordpress_i18n.__)("Leave Elementor AI?", "elementor"),
			text: (0, _wordpress_i18n.__)("Your progress will be deleted, and can't be recovered.", "elementor"),
			onClose: abortAndClose,
			onCancel: function onCancel() {
				return setShowUnsavedChangesAlert(false);
			}
		}), shouldRenderWebApp && /*#__PURE__*/ react.default.createElement(AttachDialog, {
			type: "url",
			url: promptInputRef.current.value,
			onAttach,
			onClose: function onClose() {
				setShouldRenderWebApp(false);
			}
		}), /*#__PURE__*/ react.default.createElement(PromptForm, {
			shouldResetPrompt: shouldRenderWebApp,
			ref: promptInputRef,
			isActive: isPromptFormActive,
			isLoading,
			showActions: screenshots.length > 0 || isLoading,
			attachmentsTypes,
			attachments,
			onAttach,
			onDetach: function onDetach(index) {
				setAttachments(function(prev) {
					var newAttachments = _toConsumableArray(prev);
					newAttachments.splice(index, 1);
					return newAttachments;
				});
				setIsPromptEditable(true);
			},
			onSubmit: handleGenerate,
			onBack: function onBack() {
				return setIsPromptEditable(false);
			},
			onEdit: function onEdit() {
				return setIsPromptEditable(true);
			}
		}), (screenshots.length > 0 || isLoading) && /*#__PURE__*/ react.default.createElement(react.default.Fragment, null, /*#__PURE__*/ react.default.createElement(_elementor_ui.Divider, null), /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, { sx: { p: 1.5 } }, /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, { sx: {
			overflow: "hidden",
			p: .5
		} }, /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, { sx: {
			display: "flex",
			transition: "all 0.4s ease",
			gap: "".concat(gapPercentage, "%"),
			transform: "translateX(".concat(offsetXPercentage, "%)")
		} }, screenshots.map(function(_ref2, index) {
			var screenshot = _ref2.screenshot;
			var type = _ref2.type;
			var template = _ref2.template;
			var isError = _ref2.isError;
			var isPending = _ref2.isPending;
			return /*#__PURE__*/ react.default.createElement(Screenshot, {
				key: index,
				url: screenshot,
				type,
				disabled: isPromptFormActive,
				isPlaceholder: isError,
				isLoading: isPending,
				isSelected: selectedScreenshotIndex === index,
				onClick: handleScreenshotClick(index, template),
				outlineOffset: screenshotOutlineOffset,
				sx: { flex: "0 0 ".concat(slideWidthPercentage, "%") }
			});
		}))), /*#__PURE__*/ react.default.createElement(VoicePromotionAlert, { introductionKey: "ai-context-layout-promotion" })), screenshots.length > 0 && /*#__PURE__*/ react.default.createElement(_elementor_ui.Box, {
			sx: {
				pt: 0,
				px: 2,
				pb: 2
			},
			display: "grid",
			gridTemplateColumns: "repeat(3, 1fr)",
			justifyItems: "center"
		}, /*#__PURE__*/ react.default.createElement(RegenerateButton, {
			onClick: handleRegenerate,
			disabled: isRegenerateButtonDisabled(screenshots, isLoading, isPromptFormActive),
			sx: { justifySelf: "start" }
		}), screenshots.length > slidesPerPage && /*#__PURE__*/ react.default.createElement(_elementor_ui.Pagination, {
			page: currentPage,
			count: pagesCount,
			disabled: isPromptFormActive,
			onChange: function onChange(_, page) {
				return setCurrentPage(page);
			}
		}), /*#__PURE__*/ react.default.createElement(UseLayoutButton, {
			onClick: applyTemplate,
			disabled: isPromptFormActive || -1 === selectedScreenshotIndex,
			sx: {
				justifySelf: "end",
				gridColumn: 3
			}
		}))))));
	};
	FormLayout.propTypes = {
		DialogHeaderProps: import_prop_types$7.default.object,
		DialogContentProps: import_prop_types$7.default.object,
		attachments: import_prop_types$7.default.arrayOf(AttachmentPropType)
	};

//#endregion
//#region modules/ai/assets/js/editor/layout-content.js
	init_slicedToArray();
	init_loader();
	init_requests_ids();
	var LayoutContent = function LayoutContent(props) {
		var _useUserInfo = useUserInfo();
		var isLoading = _useUserInfo.isLoading;
		var isConnected = _useUserInfo.isConnected;
		var isGetStarted = _useUserInfo.isGetStarted;
		var connectUrl = _useUserInfo.connectUrl;
		var fetchData = _useUserInfo.fetchData;
		var hasSubscription = _useUserInfo.hasSubscription;
		var initialUsagePercentage = _useUserInfo.usagePercentage;
		var _useConfig = useConfig();
		var onClose = _useConfig.onClose;
		var onConnect = _useConfig.onConnect;
		var _useRequestIds = useRequestIds();
		var updateUsagePercentage = _useRequestIds.updateUsagePercentage;
		var usagePercentage = _useRequestIds.usagePercentage;
		var _useState2 = _slicedToArray((0, react.useState)(false), 2);
		var isInitUsageDone = _useState2[0];
		var setIsInitUsageDone = _useState2[1];
		(0, react.useEffect)(function() {
			if (!isInitUsageDone && !isLoading && (initialUsagePercentage || 0 === initialUsagePercentage)) {
				updateUsagePercentage(initialUsagePercentage);
				setIsInitUsageDone(true);
			}
		}, [
			isLoading,
			initialUsagePercentage,
			isInitUsageDone,
			updateUsagePercentage
		]);
		if (isLoading || !isInitUsageDone) return /*#__PURE__*/ react.default.createElement(LayoutDialog, { onClose }, /*#__PURE__*/ react.default.createElement(LayoutDialog.Header, { onClose }), /*#__PURE__*/ react.default.createElement(LayoutDialog.Content, { dividers: true }, /*#__PURE__*/ react.default.createElement(Loader, { BoxProps: { sx: { px: 3 } } })));
		if (!isConnected) return /*#__PURE__*/ react.default.createElement(WizardDialog, { onClose }, /*#__PURE__*/ react.default.createElement(LayoutDialog, { onClose }), /*#__PURE__*/ react.default.createElement(WizardDialog.Content, { dividers: true }, /*#__PURE__*/ react.default.createElement(Connect, {
			connectUrl,
			onSuccess: function onSuccess(data) {
				onConnect(data);
				fetchData();
			}
		})));
		if (!isGetStarted) return /*#__PURE__*/ react.default.createElement(WizardDialog, { onClose }, /*#__PURE__*/ react.default.createElement(LayoutDialog, { onClose }), /*#__PURE__*/ react.default.createElement(WizardDialog.Content, { dividers: true }, /*#__PURE__*/ react.default.createElement(GetStarted, { onSuccess: fetchData })));
		var showUpgradeChip = !hasSubscription || 80 <= usagePercentage;
		return /*#__PURE__*/ react.default.createElement(FormLayout, {
			attachments: props.attachments,
			DialogHeaderProps: { children: showUpgradeChip && /*#__PURE__*/ react.default.createElement(UpgradeChip, {
				hasSubscription,
				usagePercentage
			}) }
		});
	};
	LayoutContent.propTypes = { attachments: import_prop_types$7.default.arrayOf(AttachmentPropType) };

//#endregion
//#region modules/ai/assets/js/editor/layout-app.js
	init_requests_ids();
	var LayoutApp = function LayoutApp(props) {
		return /*#__PURE__*/ react.default.createElement(RemoteConfigProvider, { onError: props.onClose }, /*#__PURE__*/ react.default.createElement(RequestIdsProvider, null, /*#__PURE__*/ react.default.createElement(ConfigProvider, {
			mode: props.mode,
			attachmentsTypes: props.attachmentsTypes,
			onClose: props.onClose,
			onConnect: props.onConnect,
			onData: props.onData,
			onInsert: props.onInsert,
			onSelect: props.onSelect,
			onGenerate: props.onGenerate,
			currentContext: props.currentContext,
			hasPro: props.hasPro
		}, /*#__PURE__*/ react.default.createElement(LayoutContent, { attachments: props.attachments }))));
	};
	LayoutApp.propTypes = {
		mode: import_prop_types$7.default.oneOf(LAYOUT_APP_MODES).isRequired,
		attachmentsTypes: AttachmentsTypesPropType,
		attachments: import_prop_types$7.default.arrayOf(AttachmentPropType),
		onClose: import_prop_types$7.default.func.isRequired,
		onConnect: import_prop_types$7.default.func.isRequired,
		onData: import_prop_types$7.default.func.isRequired,
		onInsert: import_prop_types$7.default.func.isRequired,
		onSelect: import_prop_types$7.default.func.isRequired,
		onGenerate: import_prop_types$7.default.func.isRequired,
		currentContext: import_prop_types$7.default.object,
		hasPro: import_prop_types$7.default.bool
	};

//#endregion
//#region modules/ai/assets/js/editor/utils/screenshot.js
	init_requests_ids();

//#endregion
//#region modules/ai/assets/js/editor/layout-app-wrapper.js
	var LayoutAppWrapper = function LayoutAppWrapper(props) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.DirectionProvider, { rtl: props.isRTL }, /*#__PURE__*/ react.default.createElement(_elementor_ui.ThemeProvider, { colorScheme: props.colorScheme }, props.children));
	};
	LayoutAppWrapper.propTypes = {
		children: import_prop_types$7.default.node,
		isRTL: import_prop_types$7.default.bool,
		colorScheme: import_prop_types$7.default.oneOf([
			"auto",
			"light",
			"dark"
		])
	};

//#endregion
//#region modules/ai/assets/js/editor/utils/editor-integration.js
	init_requests_ids();
	var onConnect = function onConnect(data) {
		elementorCommon.config.library_connect.is_connected = true;
		elementorCommon.config.library_connect.current_access_level = data.kits_access_level || data.access_level || 0;
		elementorCommon.config.library_connect.current_access_tier = data.access_tier;
		elementorCommon.config.library_connect.plan_type = data.plan_type;
	};
	var VARIATIONS_PROMPTS = [
		{ text: (0, _wordpress_i18n.__)("Minimalist design with bold typography about", "elementor") },
		{ text: (0, _wordpress_i18n.__)("Elegant style with serif fonts discussing", "elementor") },
		{ text: (0, _wordpress_i18n.__)("Retro vibe with muted colors and classic fonts about", "elementor") },
		{ text: (0, _wordpress_i18n.__)("Futuristic design with neon accents about", "elementor") },
		{ text: (0, _wordpress_i18n.__)("Professional look with clean lines for", "elementor") },
		{ text: (0, _wordpress_i18n.__)("Earthy tones and organic shapes featuring", "elementor") },
		{ text: (0, _wordpress_i18n.__)("Luxurious theme with rich colors discussing", "elementor") },
		{ text: (0, _wordpress_i18n.__)("Tech-inspired style with modern fonts about", "elementor") },
		{ text: (0, _wordpress_i18n.__)("Warm hues with comforting visuals about", "elementor") }
	];
	var WEB_BASED_PROMPTS = [
		{ text: (0, _wordpress_i18n.__)("Change the content to be about [topic]", "elementor") },
		{ text: (0, _wordpress_i18n.__)("Generate lorem ipsum placeholder text for all paragraphs", "elementor") },
		{ text: (0, _wordpress_i18n.__)("Revise the content to focus on [topic] and then translate it into Spanish", "elementor") },
		{ text: (0, _wordpress_i18n.__)("Shift the focus of the content to [topic] in order to showcase our company's mission and values", "elementor") },
		{ text: (0, _wordpress_i18n.__)("Alter the content to provide helpful tips related to [topic]", "elementor") },
		{ text: (0, _wordpress_i18n.__)("Adjust the content to include FAQs and answers for common inquiries about [topic]", "elementor") }
	];
	var PROMPT_PLACEHOLDER = (0, _wordpress_i18n.__)("Press '/' for suggestions or describe the changes you want to apply (optional)...", "elementor");

//#endregion
//#region modules/ai/assets/js/editor/app.js
	init_requests_ids();
	var App = function App(props) {
		return /*#__PURE__*/ react.default.createElement(_elementor_ui.DirectionProvider, { rtl: props.isRTL }, /*#__PURE__*/ react.default.createElement(_elementor_ui.ThemeProvider, { colorScheme: props.colorScheme }, /*#__PURE__*/ react.default.createElement(RequestIdsProvider, null, /*#__PURE__*/ react.default.createElement(PageContent, {
			type: props.type,
			controlType: props.controlType,
			onClose: props.onClose,
			onConnect,
			getControlValue: props.getControlValue,
			setControlValue: props.setControlValue,
			additionalOptions: props.additionalOptions
		}))));
	};
	App.propTypes = {
		colorScheme: import_prop_types$7.default.oneOf([
			"auto",
			"light",
			"dark"
		]),
		type: import_prop_types$7.default.string,
		controlType: import_prop_types$7.default.string,
		onClose: import_prop_types$7.default.func,
		getControlValue: import_prop_types$7.default.func,
		setControlValue: import_prop_types$7.default.func,
		additionalOptions: import_prop_types$7.default.object,
		isRTL: import_prop_types$7.default.bool
	};

//#endregion
//#region modules/ai/assets/js/media-library/components.js
	var AIMediaGenerateApp = function AIMediaGenerateApp(_ref) {
		var onClose = _ref.onClose;
		var setControlValue = _ref.setControlValue;
		var _ref$predefinedPrompt = _ref.predefinedPrompt;
		var predefinedPrompt = _ref$predefinedPrompt === void 0 ? "" : _ref$predefinedPrompt;
		var _ref$textToImageHook = _ref.textToImageHook;
		var textToImageHook = _ref$textToImageHook === void 0 ? null : _ref$textToImageHook;
		var _ref$initialSettings = _ref.initialSettings;
		var initialSettings = _ref$initialSettings === void 0 ? {} : _ref$initialSettings;
		return /*#__PURE__*/ react.default.createElement(react.default.Fragment, null, /*#__PURE__*/ react.default.createElement(App, {
			type: "media",
			getControlValue: function getControlValue() {},
			setControlValue,
			onClose,
			isRTL: elementorCommon.config.isRTL,
			additionalOptions: {
				defaultImageType: Object.keys(IMAGE_PROMPT_CATEGORIES)[1],
				textToImageHook,
				predefinedPrompt,
				initialSettings
			}
		}));
	};
	AIMediaGenerateApp.propTypes = {
		onClose: import_prop_types$7.default.func.isRequired,
		predefinedPrompt: import_prop_types$7.default.string,
		textToImageHook: import_prop_types$7.default.func,
		setControlValue: import_prop_types$7.default.func.isRequired,
		initialSettings: import_prop_types$7.default.object
	};
	var AIMediaEditApp = function AIMediaEditApp(_ref2) {
		var onClose = _ref2.onClose;
		var imageId = _ref2.imageId;
		var image = wp.media.attachment(imageId);
		return /*#__PURE__*/ react.default.createElement(react.default.Fragment, null, /*#__PURE__*/ react.default.createElement(App, {
			type: "media",
			getControlValue: function getControlValue() {
				return image.attributes;
			},
			setControlValue: function setControlValue() {},
			onClose,
			isRTL: elementorCommon.config.isRTL,
			additionalOptions: { location: LOCATIONS.IMAGE_TOOLS }
		}));
	};
	AIMediaEditApp.propTypes = {
		onClose: import_prop_types$7.default.func.isRequired,
		imageId: import_prop_types$7.default.string
	};

//#endregion
//#region modules/ai/assets/js/media-library/generate.js
	init_slicedToArray();
	init_requests_ids();
	var _templateObject$2;
	var _templateObject2$1;
	var Icon$1 = (0, _elementor_ui.styled)("i")(_templateObject$2 || (_templateObject$2 = _taggedTemplateLiteral(["\n	padding-right: 0.5em;\n	cursor: pointer;\n"])));
	var StyledButton$1 = (0, _elementor_ui.styled)("a")(_templateObject2$1 || (_templateObject2$1 = _taggedTemplateLiteral(["\n	color: var(--e-a-color-primary-bold);\n	cursor: pointer;\n	font: inherit;\n	display: inline-block;\n	position: relative;\n	top: -3px;\n	margin-left: 4px;\n	border: 1px solid var(--e-a-color-primary-bold);\n	border-radius: 3px;\n	line-height: 2.15384615;\n	padding: 0 10px;\n	background: #f6f7f7;\n\n	&:hover {\n		color: var(--e-a-color-primary-bold);\n		background: #eeefef;\n	}\n"])));
	var AIMediaGenerateAppWrapper = function AIMediaGenerateAppWrapper() {
		var _useState2 = _slicedToArray((0, react.useState)(false), 2);
		var isOpen = _useState2[0];
		var setIsOpen = _useState2[1];
		return /*#__PURE__*/ react.default.createElement("div", { style: { padding: "0.5em" } }, /*#__PURE__*/ react.default.createElement(RequestIdsProvider, null, /*#__PURE__*/ react.default.createElement(StyledButton$1, { onClick: function handleClick() {
			setIsOpen(true);
		} }, /*#__PURE__*/ react.default.createElement(Icon$1, { className: "eicon-ai" }), (0, _wordpress_i18n.__)("Generate with Elementor AI", "elementor")), isOpen && /*#__PURE__*/ react.default.createElement(AIMediaGenerateApp, {
			onClose: function handleClose() {
				var _wp$media$frame;
				(_wp$media$frame = wp.media.frame) === null || _wp$media$frame === void 0 || (_wp$media$frame = _wp$media$frame.content) === null || _wp$media$frame === void 0 || (_wp$media$frame = _wp$media$frame.get()) === null || _wp$media$frame === void 0 || (_wp$media$frame = _wp$media$frame.collection) === null || _wp$media$frame === void 0 || _wp$media$frame._requery(true);
				setIsOpen(false);
			},
			setControlValue: function setControlValue() {}
		})));
	};

//#endregion
//#region node_modules/@wordpress/element/build-module/react-platform.js
/**
	* External dependencies
	*/

//#endregion
//#region modules/ai/assets/js/media-library/utils.js
	var getImageId = function getImageId() {
		var _wp$media;
		var imageId = (_wp$media = wp.media) === null || _wp$media === void 0 || (_wp$media = _wp$media.frames) === null || _wp$media === void 0 || (_wp$media = _wp$media.edit) === null || _wp$media === void 0 || (_wp$media = _wp$media.model) === null || _wp$media === void 0 || (_wp$media = _wp$media.id) === null || _wp$media === void 0 ? void 0 : _wp$media.toString();
		if (imageId) return imageId;
		return getImageIdByUrl();
	};
	var getImageIdByUrl = function getImageIdByUrl() {
		var _document$getElementB;
		var _wp$media$frame;
		var imageUrl = (_document$getElementB = document.getElementById("attachment-details-copy-link")) === null || _document$getElementB === void 0 ? void 0 : _document$getElementB.value;
		var images = (_wp$media$frame = wp.media.frame) === null || _wp$media$frame === void 0 || (_wp$media$frame = _wp$media$frame.content) === null || _wp$media$frame === void 0 || (_wp$media$frame = _wp$media$frame.get()) === null || _wp$media$frame === void 0 || (_wp$media$frame = _wp$media$frame.collection) === null || _wp$media$frame === void 0 ? void 0 : _wp$media$frame.models;
		var image = Array.isArray(images) && images.find(function(img) {
			return img.attributes.url === imageUrl;
		});
		return image ? image.attributes.id.toString() : null;
	};
	var isImageFile = function isImageFile() {
		var _wp$media2;
		var _filename$split$pop;
		var filename = (_wp$media2 = wp.media) === null || _wp$media2 === void 0 || (_wp$media2 = _wp$media2.frames) === null || _wp$media2 === void 0 || (_wp$media2 = _wp$media2.edit) === null || _wp$media2 === void 0 || (_wp$media2 = _wp$media2.model) === null || _wp$media2 === void 0 || (_wp$media2 = _wp$media2.attributes) === null || _wp$media2 === void 0 ? void 0 : _wp$media2.filename;
		var imageExtensions = [
			"jpg",
			"jpeg",
			"png",
			"gif",
			"bmp",
			"webp",
			"tiff",
			"svg",
			"ico",
			"heic",
			"heif"
		];
		var fileExtension = (_filename$split$pop = filename.split(".").pop()) === null || _filename$split$pop === void 0 ? void 0 : _filename$split$pop.toLowerCase();
		return imageExtensions.includes(fileExtension || "");
	};

//#endregion
//#region modules/ai/assets/js/media-library/edit-button.js
	init_slicedToArray();
	init_requests_ids();
	var _templateObject$1;
	var _templateObject2;
	var Icon = (0, _elementor_ui.styled)("i")(_templateObject$1 || (_templateObject$1 = _taggedTemplateLiteral(["\n	color: var(--e-a-color-primary-bold);\n	padding-inline-end: 0.5em;\n	cursor: pointer;\n"])));
	var StyledButton = (0, _elementor_ui.styled)("a")(_templateObject2 || (_templateObject2 = _taggedTemplateLiteral(["\n	font: inherit;\n	display: inline-block;\n	font-size: 13px;\n	line-height: 2.15384615;\n	padding: 0 10px;\n	cursor: pointer;\n	border-width: 1px;\n	border-style: solid;\n	border-radius: 3px;\n	background: #f6f7f7;\n	color: var(--e-a-color-primary-bold);\n\n	&:hover {\n		color: var(--e-a-color-primary-bold);\n		background: #eeefef;\n	}\n"])));
	var AIMediaEditAppButtonWrapper = function AIMediaEditAppButtonWrapper() {
		var _useState2 = _slicedToArray((0, react.useState)(false), 2);
		var isOpen = _useState2[0];
		var setIsOpen = _useState2[1];
		var _useState4 = _slicedToArray((0, react.useState)(getImageId), 2);
		var imageId = _useState4[0];
		var setImageId = _useState4[1];
		return isImageFile() && /*#__PURE__*/ react.default.createElement("div", { style: { marginLeft: "0.5em" } }, /*#__PURE__*/ react.default.createElement(RequestIdsProvider, null, /*#__PURE__*/ react.default.createElement(StyledButton, { onClick: function handleClick() {
			setImageId(getImageId());
			setIsOpen(true);
		} }, /*#__PURE__*/ react.default.createElement(Icon, { className: "eicon-ai" }), (0, _wordpress_i18n.__)("Edit with Elementor AI", "elementor")), isOpen && /*#__PURE__*/ react.default.createElement(AIMediaEditApp, {
			onClose: function handleClose() {
				var _wp$media$frame;
				(_wp$media$frame = wp.media.frame) === null || _wp$media$frame === void 0 || (_wp$media$frame = _wp$media$frame.controller) === null || _wp$media$frame === void 0 || (_wp$media$frame = _wp$media$frame.content) === null || _wp$media$frame === void 0 || (_wp$media$frame = _wp$media$frame.get().collection) === null || _wp$media$frame === void 0 || _wp$media$frame._requery(true);
				setIsOpen(false);
			},
			imageId
		})));
	};

//#endregion
//#region modules/ai/assets/js/media-library/edit-link.js
	init_slicedToArray();
	init_requests_ids();
	var _templateObject;
	var ImageLink = (0, _elementor_ui.styled)("a")(_templateObject || (_templateObject = _taggedTemplateLiteral(["\n	&& {\n		color: var(--e-a-color-primary-bold);\n		cursor: pointer;\n		font-size: inherit;\n		display: inline-block;\n\n		&:hover {\n			color: var(--e-a-color-primary-bold);\n		}\n	}\n"])));
	var AIMediaEditAppLinkWrapper = function AIMediaEditAppLinkWrapper() {
		var _useState2 = _slicedToArray((0, react.useState)(false), 2);
		var isOpen = _useState2[0];
		var setIsOpen = _useState2[1];
		var _useState4 = _slicedToArray((0, react.useState)(getImageId), 2);
		var imageId = _useState4[0];
		var setImageId = _useState4[1];
		return /*#__PURE__*/ react.default.createElement("div", { style: { paddingTop: "0.2em" } }, /*#__PURE__*/ react.default.createElement(RequestIdsProvider, null, /*#__PURE__*/ react.default.createElement(ImageLink, { onClick: function handleClick() {
			setImageId(getImageId());
			setIsOpen(true);
		} }, (0, _wordpress_i18n.__)("Edit with Elementor AI", "elementor")), isOpen && /*#__PURE__*/ react.default.createElement(AIMediaEditApp, {
			onClose: function handleClose() {
				var _wp$media$frame;
				(_wp$media$frame = wp.media.frame) === null || _wp$media$frame === void 0 || (_wp$media$frame = _wp$media$frame.content) === null || _wp$media$frame === void 0 || (_wp$media$frame = _wp$media$frame.get().collection) === null || _wp$media$frame === void 0 || _wp$media$frame._requery(true);
				setIsOpen(false);
			},
			imageId
		})));
	};

//#endregion
//#region modules/ai/assets/js/media-library/index.js
	init_requests_ids();
	var isMediaLibrary = function isMediaLibrary() {
		return window.location.href.includes("/upload.php");
	};
	var insertStyleTag = function insertStyleTag() {
		var style = document.createElement("style");
		style.appendChild(document.createTextNode("\n		#e-image-ai-attachment-details,\n		#e-image-ai-media-library {\n			display: inline-block;\n		}\n		#e-form-media * {\n			box-sizing: border-box;\n		}\n		#menu- {\n			z-index: 180000;\n		}\n	"));
		document.head.appendChild(style);
	};
	var renderComponent = function renderComponent(containerId, Component) {
		var container = document.getElementById(containerId);
		if (container) (0, import_client.createRoot)(container).render(/*#__PURE__*/ react.default.createElement(Component, null));
	};
	var addEventListener = function addEventListener(eventName, containerId, Component) {
		window.addEventListener(eventName, function() {
			setTimeout(function() {
				return renderComponent(containerId, Component);
			}, 1);
		});
	};
	var addEditorPostIdRequestConstant = function addEditorPostIdRequestConstant() {
		var _elementorCommon;
		if (!((_elementorCommon = elementorCommon) !== null && _elementorCommon !== void 0 && (_elementorCommon = _elementorCommon.ajax) !== null && _elementorCommon !== void 0 && (_elementorCommon = _elementorCommon.requestConstants) !== null && _elementorCommon !== void 0 && _elementorCommon.editor_post_id)) {
			var _elementorCommon2;
			var _window$document$quer;
			(_elementorCommon2 = elementorCommon) === null || _elementorCommon2 === void 0 || (_elementorCommon2 = _elementorCommon2.ajax) === null || _elementorCommon2 === void 0 || _elementorCommon2.addRequestConstant("editor_post_id", (_window$document$quer = window.document.querySelector("#post_ID")) === null || _window$document$quer === void 0 ? void 0 : _window$document$quer.value);
		}
	};
	(function(_wp, _wp2) {
		if (isMediaLibrary()) {
			window.EDITOR_SESSION_ID = getUniqueId("wp-media-lib-session");
			var mediaLibrary = document.querySelector(".page-title-action");
			if (mediaLibrary) {
				var container = document.createElement("div");
				container.id = "e-image-ai-media-library";
				mediaLibrary.insertAdjacentElement("afterend", container);
				renderComponent("e-image-ai-media-library", AIMediaGenerateAppWrapper);
			}
		}
		if ((_wp = wp) !== null && _wp !== void 0 && (_wp = _wp.media) !== null && _wp !== void 0 && (_wp = _wp.view) !== null && _wp !== void 0 && (_wp = _wp.Attachment) !== null && _wp !== void 0 && _wp.Details) wp.media.view.Attachment.Details = wp.media.view.Attachment.Details.extend({ template: function template(view) {
			var html = wp.media.template("attachment-details")(view);
			var content = document.createElement("div");
			content.innerHTML = html;
			var compatMeta = content.querySelector(".dimensions");
			if (!compatMeta) return content.innerHTML;
			var container = document.createElement("div");
			container.id = "e-image-ai-insert-media";
			compatMeta.insertAdjacentElement("beforeend", container);
			addEditorPostIdRequestConstant();
			window.dispatchEvent(new CustomEvent("renderInsertMediaEvent"));
			return content.innerHTML;
		} });
		if ((_wp2 = wp) !== null && _wp2 !== void 0 && (_wp2 = _wp2.media) !== null && _wp2 !== void 0 && (_wp2 = _wp2.view) !== null && _wp2 !== void 0 && (_wp2 = _wp2.Attachment) !== null && _wp2 !== void 0 && (_wp2 = _wp2.Details) !== null && _wp2 !== void 0 && _wp2.TwoColumn) wp.media.view.Attachment.Details.TwoColumn = wp.media.view.Attachment.Details.TwoColumn.extend({ template: function template(view) {
			var html = wp.media.template("attachment-details-two-column")(view);
			var content = document.createElement("div");
			content.innerHTML = html;
			var details = content.querySelector(".attachment-actions");
			if (!details) return content.innerHTML;
			var container = document.createElement("div");
			container.id = "e-image-ai-attachment-details";
			details.appendChild(container);
			addEditorPostIdRequestConstant();
			window.dispatchEvent(new CustomEvent("renderAttachmentsDetailsEvent"));
			return content.innerHTML;
		} });
		addEventListener("renderInsertMediaEvent", "e-image-ai-insert-media", AIMediaEditAppLinkWrapper);
		addEventListener("renderAttachmentsDetailsEvent", "e-image-ai-attachment-details", AIMediaEditAppButtonWrapper);
		insertStyleTag();
	})();

//#endregion
})(React, wp.i18n, elementorV2.ui, elementorV2.icons, elementorV2.ui['TextField']);
//# sourceMappingURL=ai-media-library.js.map